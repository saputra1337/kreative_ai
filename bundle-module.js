
// --- FIREBASE IMPORTS (needed for Family tab) ---
import { initializeApp } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-app.js";
import { getAuth, signInAnonymously, signInWithCustomToken } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-auth.js";
import { getFirestore, setLogLevel } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js";

document.addEventListener('DOMContentLoaded', () => {
    // --- SIDEBAR TOGGLE FUNCTIONALITY ---
    const toggleSidebarBtn = document.getElementById('toggle-sidebar-btn');
    const sidebar = document.querySelector('.sidebar');
    const mainContentWrapper = document.querySelector('.main-content-wrapper');

    // Load saved sidebar state from localStorage
    const sidebarCollapsed = localStorage.getItem('sidebarCollapsed') === 'true';
    if (sidebarCollapsed) {
        sidebar.classList.add('collapsed');
        mainContentWrapper.classList.add('expanded');
        toggleSidebarBtn.classList.add('collapsed');
    }

    toggleSidebarBtn.addEventListener('click', () => {
        const isCollapsed = sidebar.classList.toggle('collapsed');
        mainContentWrapper.classList.toggle('expanded');
        toggleSidebarBtn.classList.toggle('collapsed');

        // Save state to localStorage
        localStorage.setItem('sidebarCollapsed', isCollapsed);
    });

    // --- MAIN TAB SWITCHING LOGIC ---
    const mainTabButtons = document.querySelectorAll('.main-tab-btn');
    const mobileTabButtons = document.querySelectorAll('.mobile-tab-btn');
    const mainContentPanels = document.querySelectorAll('.main-content-panel');

    // Global Preview Modal Function
    window.showPreviewModal = function(imageUrl) {
        const modal = document.createElement('div');
        modal.className = 'image-preview-modal';
        modal.innerHTML = `
            <div class="preview-modal-overlay"></div>
            <div class="preview-modal-content">
                <button class="preview-close-btn" aria-label="Close">
                    <i class="fas fa-times"></i>
                </button>
                <img src="${imageUrl}" alt="Preview" class="preview-modal-image">
            </div>
        `;
        document.body.appendChild(modal);

        // Close modal handlers
        const closeBtn = modal.querySelector('.preview-close-btn');
        const overlay = modal.querySelector('.preview-modal-overlay');

        const closeModal = () => {
            modal.classList.add('closing');
            setTimeout(() => modal.remove(), 300);
        };

        closeBtn.addEventListener('click', closeModal);
        overlay.addEventListener('click', closeModal);

        // ESC key to close
        const handleEsc = (e) => {
            if (e.key === 'Escape') {
                closeModal();
                document.removeEventListener('keydown', handleEsc);
            }
        };
        document.addEventListener('keydown', handleEsc);

        // Touch feedback for mobile
        closeBtn.addEventListener('touchstart', function() {
            this.style.transform = 'scale(0.9)';
        }, { passive: true });
        closeBtn.addEventListener('touchend', function() {
            this.style.transform = '';
        }, { passive: true });

        // Animate in
        setTimeout(() => modal.classList.add('show'), 10);
    };

    function switchTab(tabName) {
        // Update sidebar tabs
        mainTabButtons.forEach(btn => btn.classList.remove('active'));
        const sidebarBtn = document.querySelector(`.main-tab-btn[data-tab="${tabName}"]`);
        if (sidebarBtn) sidebarBtn.classList.add('active');

        // Update mobile tabs
        mobileTabButtons.forEach(btn => btn.classList.remove('active'));
        const mobileBtn = document.querySelector(`.mobile-tab-btn[data-tab="${tabName}"]`);
        if (mobileBtn) {
            mobileBtn.classList.add('active');
            // Auto-scroll to active tab in mobile view
            mobileBtn.scrollIntoView({ behavior: 'smooth', inline: 'center', block: 'nearest' });
        }

        // Switch content panels
        mainContentPanels.forEach(panel => {
            panel.classList.toggle('hidden', panel.id !== `content-${tabName}`);
        });

        // Re-apply language translation to freshly-visible panel content
        // (covers tabs yang punya dynamic content yang di-inject via JS)
        if (window._i18nApplyNow) window._i18nApplyNow();
    }

    // Sidebar tab buttons
    mainTabButtons.forEach(button => {
        button.addEventListener('click', () => {
            switchTab(button.dataset.tab);
        });
    });

    // Mobile tab buttons
    mobileTabButtons.forEach(button => {
        button.addEventListener('click', () => {
            switchTab(button.dataset.tab);
        });
    });

    // --- HUB PILL NAVIGATION (data-goto) — pindah antar tab yang di-merge di level nav ---
    document.addEventListener('click', function(e) {
        const pill = e.target.closest('[data-goto]');
        if (!pill) return;
        const target = document.querySelector('.main-tab-btn[data-tab="' + pill.dataset.goto + '"]');
        if (target) target.click();
    });

    // --- FAVORITES LOGIC (saran Dims Bagsi) ---
    // [v33+] Berbayar lewat Lynk.id "fitur favorit tab affgo" (lifetime).
    // Gate ada di toggleFavorite() — kalau window.favoritFeatureActive false →
        (function() {
        const FAV_KEY = 'affiliatego_favorites';
        const sidebar = document.querySelector('.sidebar');
        const favCategory = document.getElementById('favorites-category');
        const favContainer = document.getElementById('favorites-container');
        if (!sidebar || !favCategory || !favContainer) return;

        // Mobile bottom sheet elements
        const mobileFavBtn = document.getElementById('mobile-fav-access-btn');
        const mobileFavCount = document.getElementById('mobile-fav-count');
        const mobileFavSheet = document.getElementById('mobile-fav-sheet');
        const mobileFavList = document.getElementById('mobile-fav-sheet-list');
        const mobileFavEmpty = document.getElementById('mobile-fav-sheet-empty');

        // Cloud sync: single-flight + debounce. Pelajaran dari bug: debounce saja
        // tidak cukup — kalau user toggle dengan jeda >debounce_ms, tiap klik fire
        // fetch sendiri-sendiri. 5 fetch race di network → Apps Script proses
        // serial, last-arrived-wins → fetch pertama (stale) bisa overwrite fetch
        // terakhir (lengkap), final state di sheet jadi cuma 1 favorit.
        // Solusi: at most 1 request in-flight. Kalau ada toggle baru selama
        // menunggu, set pending=true → setelah complete, re-fire 1× lagi dengan
        // state TERBARU (read fresh dari localStorage di fetch time).
        let cloudSyncTimer = null;
        let cloudSyncInFlight = false;
        let cloudSyncPending = false;
        const CLOUD_SYNC_DEBOUNCE_MS = 1500;

        function getFavs() {
            try { return JSON.parse(localStorage.getItem(FAV_KEY) || '[]'); } catch(e) { return []; }
        }
        function saveFavs(favs) {
            try { localStorage.setItem(FAV_KEY, JSON.stringify(favs)); } catch(e) {}
        }

                function showFavoritPaywall() {
            const isEN = (localStorage.getItem('app_language') || 'id') === 'en';
            const T = isEN ? {
                title: 'Fitur Favorit Tab — Premium',
                pitch: 'Save tabs as favorites — synced across devices, never lost on cache clear',
                feat1: 'Pin unlimited tabs to top',
                feat2: 'Sync across phone & laptop via account',
                feat3: 'Lifetime access — pay once, use forever',
                cta: 'Buy Now — Lifetime',
                skip: 'Maybe Later',
                note: 'After payment, log in again with the same email'
            } : {
                title: 'Fitur Favorit Tab — Premium',
                pitch: 'Simpan tab favorit di akunmu — sinkron antar device, tidak hilang saat clear cache',
                feat1: 'Tandai tab tanpa batas ke atas',
                feat2: 'Sinkron HP & laptop lewat akun',
                feat3: 'Akses selamanya — bayar sekali, pakai terus',
                cta: 'Beli Sekarang — Selamanya',
                skip: 'Nanti Saja',
                note: 'Setelah bayar, login ulang dengan email yang sama'
            };

            const html =
                '<div class="text-center">' +
                    '<div class="text-5xl mb-3" style="filter: drop-shadow(0 4px 12px rgba(234, 179, 8, 0.4));">' +
                        '<i class="fas fa-star" style="color:#eab308;"></i>' +
                    '</div>' +
                    '<p class="text-base font-semibold text-gray-700 mb-4">' + T.pitch + '</p>' +
                    '<ul class="text-left space-y-2 mb-5 max-w-sm mx-auto">' +
                        '<li class="flex items-start gap-2 text-sm text-gray-700"><i class="fas fa-check-circle text-green-600 mt-1"></i><span>' + T.feat1 + '</span></li>' +
                        '<li class="flex items-start gap-2 text-sm text-gray-700"><i class="fas fa-check-circle text-green-600 mt-1"></i><span>' + T.feat2 + '</span></li>' +
                        '<li class="flex items-start gap-2 text-sm text-gray-700"><i class="fas fa-check-circle text-green-600 mt-1"></i><span>' + T.feat3 + '</span></li>' +
                    '</ul>' +
                    '<a href="' + LYNK_FAVORIT_FEATURE_URL + '" target="_blank" rel="noopener noreferrer" ' +
                        'class="inline-flex items-center justify-center gap-2 w-full bg-gradient-to-r from-yellow-500 to-amber-600 text-white font-bold py-3 px-6 rounded-xl shadow-lg hover:shadow-xl hover:scale-[1.02] active:scale-[0.98] transition-all min-h-[44px]">' +
                        '<i class="fas fa-star"></i><span>' + T.cta + '</span>' +
                    '</a>' +
                    '<button type="button" id="favorit-paywall-skip-btn" class="mt-3 w-full text-sm font-semibold text-gray-500 hover:text-gray-700 py-2 min-h-[44px]">' + T.skip + '</button>' +
                    '<p class="text-xs text-gray-500 mt-3">' + T.note + '</p>' +
                '</div>';

            if (typeof window.showUniversalModal !== 'function') return;
            window.showUniversalModal(T.title, html);

            // Wire "Nanti Saja" → close modal (sama pattern dengan adfree promo)
            setTimeout(function() {
                const skipBtn = document.getElementById('favorit-paywall-skip-btn');
                const closeBtn = document.getElementById('close-modal-btn');
                if (skipBtn && closeBtn) {
                    skipBtn.addEventListener('click', function() { closeBtn.click(); });
                }
            }, 50);
        }

        // ===== Cloud sync (single-flight, debounced, fresh-read) =====
        function scheduleCloudSync() {
            if (cloudSyncTimer) clearTimeout(cloudSyncTimer);
            cloudSyncTimer = setTimeout(triggerCloudSync, CLOUD_SYNC_DEBOUNCE_MS);
        }

        function triggerCloudSync() {
            // Single-flight gate. Kalau request lain masih in-flight, tandai pending
            // → setelah dia complete, akan auto re-fire dengan state localStorage TERBARU.
            // Mencegah out-of-order overwrite (bug v33: kolom N cuma berisi 1 favorit
            // padahal user bintangi 5 tab — race condition antar 5 fetch terpisah).
            if (cloudSyncInFlight) {
                cloudSyncPending = true;
                return;
            }
            const email = localStorage.getItem('affiliatego_email');
            const token = localStorage.getItem('affiliatego_token');
            if (!email || !token) return; // belum login → skip
            if (typeof SCRIPT_URL !== 'string' || typeof APP_SECRET !== 'string') return;

            cloudSyncInFlight = true;
            // Fresh-read dari localStorage di fetch time (bukan dari closure di
            // schedule time). Memastikan request yang dikirim selalu mengandung
            // CUMULATIVE state terbaru, bukan snapshot lama.
            const favs = getFavs();

            const url = SCRIPT_URL +
                '?action=set_favorites' +
                '&email=' + encodeURIComponent(email) +
                '&token=' + encodeURIComponent(token) +
                '&app_secret=' + encodeURIComponent(APP_SECRET) +
                '&product=' + encodeURIComponent(PRODUCT_ID) +
                '&favorites=' + encodeURIComponent(JSON.stringify(favs));
            fetch(url).then(r => r.json()).then(function(data) {
                if (data && data.feature_inactive) {
                    // Server bilang fitur tidak aktif → sync flag lokal supaya popup muncul
                    // di toggle berikutnya. Jangan rollback localStorage favs (UI sudah optimistic).
                    window.favoritFeatureActive = false;
                }
            }).catch(function() {
                /* fail-soft: data sudah aman di localStorage, akan retry di toggle berikut */
            }).finally(function() {
                cloudSyncInFlight = false;
                if (cloudSyncPending) {
                    cloudSyncPending = false;
                    // Re-fire dengan state terbaru — TIDAK pakai debounce ulang supaya
                    // tidak menunda lebih lama (debounce sudah dilakukan di klik awal).
                    triggerCloudSync();
                }
            });
        }

        // ===== Apply favorites dari server (dipanggil oleh login response handler) =====
        // Default: server = source of truth → override localStorage. TAPI ada satu
        // exception: kalau localStorage punya item yang TIDAK ADA di server, kemungkinan
        // ada toggle yang gagal sync ke cloud (bug race condition v33). Dalam kasus itu
        // pakai UNION (gabungan) lalu push ke cloud supaya server catch up — bukan
        // hanya destruktif overwrite local. Setelah satu toggle berikutnya, semuanya
        // konsisten di cloud.
        window._affApplyFavoritesFromServer = function(serverFavs) {
            if (!Array.isArray(serverFavs)) return;
            const localFavs = getFavs();
            const serverSet = new Set(serverFavs);
            const localOnly = localFavs.filter(function(t) { return !serverSet.has(t); });
            let finalFavs = serverFavs;
            if (localOnly.length > 0) {
                // Ada item lokal yang belum di cloud → union dan push ke cloud sekali.
                // Concatenate dengan urutan: server dulu (preserve order user di device lain),
                // baru lokal-only (di akhir).
                finalFavs = serverFavs.concat(localOnly);
                saveFavs(finalFavs);
                // Schedule push ke cloud — tidak immediate supaya tidak block login flow.
                scheduleCloudSync();
            } else {
                saveFavs(finalFavs);
            }
            // Re-sync semua star UI + render category
            document.querySelectorAll('[data-fav-for]').forEach(function(el) {
                const tabId = el.dataset.favFor;
                const isFav = finalFavs.indexOf(tabId) !== -1;
                el.classList.toggle('favorited', isFav);
            });
            renderFavorites();
            reorderMobileTabs();
        };

        function createStar(tabId, isFavorited) {
            const star = document.createElement('span');
            star.className = 'fav-star-btn' + (isFavorited ? ' favorited' : '');
            star.innerHTML = '<i class="fas fa-star"></i>';
            star.title = isFavorited ? 'Hapus dari Favorit' : 'Tambah ke Favorit';
            star.dataset.favFor = tabId;
            star.addEventListener('click', (e) => {
                e.stopPropagation();
                e.preventDefault();
                toggleFavorite(tabId);
            });
            return star;
        }

        function syncStarUI(tabId, isFavorited) {
            // Desktop star (di sidebar)
            sidebar.querySelectorAll('.fav-star-btn[data-fav-for="' + tabId + '"]').forEach(s => {
                if (isFavorited) {
                    s.classList.add('favorited');
                    s.title = 'Hapus dari Favorit';
                } else {
                    s.classList.remove('favorited');
                    s.title = 'Tambah ke Favorit';
                }
            });
            // Mobile star (di bottom nav) — sync juga
            document.querySelectorAll('.fav-star-mobile[data-fav-for="' + tabId + '"]').forEach(s => {
                if (isFavorited) s.classList.add('favorited');
                else s.classList.remove('favorited');
            });
        }

        function toggleFavorite(tabId) {
            // [v33+] Paid feature gate. Cek window.favoritFeatureActive (di-set oleh
                        if (window.favoritFeatureActive !== true) {
                showFavoritPaywall();
                return;
            }
            let favs = getFavs();
            const idx = favs.indexOf(tabId);
            const wasFav = idx !== -1;
            if (wasFav) {
                favs.splice(idx, 1);
                syncStarUI(tabId, false);
            } else {
                favs.push(tabId);
                syncStarUI(tabId, true);
            }
            saveFavs(favs);
            reorderMobileTabs();
            if (!wasFav) scrollMobileNavToStart();
            renderFavorites();
            // Cloud sync (single-flight + debounced fire-and-forget). Membaca fresh
            // dari localStorage di fetch time, jadi tidak perlu pass `favs` lagi.
            scheduleCloudSync();
        }

        // Reorder mobile bottom-nav tabs so favorited ones appear leftmost.
        // Uses CSS `order` so DOM stays untouched — unfavorite restores original position.
        function reorderMobileTabs() {
            const favs = getFavs();
            document.querySelectorAll('.mobile-tab-btn').forEach(btn => {
                const tabId = btn.dataset.tab;
                if (!tabId) return;
                const favIdx = favs.indexOf(tabId);
                if (favIdx !== -1) {
                    // FIFO: first favorited gets lowest order = leftmost slot
                    btn.style.order = String(favIdx - 1000);
                } else {
                    btn.style.order = '';
                }
            });
        }

        function scrollMobileNavToStart() {
            const nav = document.querySelector('.mobile-bottom-nav');
            if (!nav) return;
            try {
                nav.scrollTo({ left: 0, behavior: 'smooth' });
            } catch (e) {
                nav.scrollLeft = 0;
            }
        }

        function addStarsToOriginals() {
            const favs = getFavs();
            // Desktop sidebar buttons
            sidebar.querySelectorAll('.main-tab-btn').forEach(btn => {
                if (btn.closest('#favorites-container')) return;
                if (btn.querySelector('.fav-star-btn')) return;
                const tabId = btn.dataset.tab;
                if (!tabId) return;
                const star = createStar(tabId, favs.indexOf(tabId) !== -1);
                btn.appendChild(star);
            });
            // Mobile bottom nav buttons — tambah star kecil di pojok
            document.querySelectorAll('.mobile-tab-btn').forEach(btn => {
                if (btn.querySelector('.fav-star-mobile')) return;
                const tabId = btn.dataset.tab;
                if (!tabId) return;
                const isFav = favs.indexOf(tabId) !== -1;
                const star = document.createElement('span');
                star.className = 'fav-star-mobile' + (isFav ? ' favorited' : '');
                star.innerHTML = '<i class="fas fa-star"></i>';
                star.dataset.favFor = tabId;
                star.setAttribute('aria-label', isFav ? 'Hapus dari Favorit' : 'Tambah ke Favorit');
                star.addEventListener('click', (e) => {
                    e.stopPropagation();
                    e.preventDefault();
                    toggleFavorite(tabId);
                });
                btn.appendChild(star);
            });
        }

        function renderFavorites() {
            const favs = getFavs();
            favContainer.innerHTML = '';
            if (favs.length === 0) {
                favCategory.style.display = 'none';
            } else {
                favs.forEach(tabId => {
                    const originals = sidebar.querySelectorAll('.main-tab-btn[data-tab="' + tabId + '"]');
                    let original = null;
                    originals.forEach(b => {
                        if (!b.closest('#favorites-container')) original = b;
                    });
                    if (!original) return;

                    const clone = original.cloneNode(true);
                    clone.classList.remove('hidden');
                    const oldStar = clone.querySelector('.fav-star-btn');
                    if (oldStar) oldStar.remove();
                    clone.appendChild(createStar(tabId, true));

                    clone.addEventListener('click', (e) => {
                        if (e.target.closest('.fav-star-btn')) return;
                        e.stopPropagation();
                        original.click();
                    });

                    favContainer.appendChild(clone);
                });
                favCategory.style.display = '';
            }
            // Sync mobile bottom sheet + badge
            renderMobileFavSheet();
            // Re-apply language translation to cloned buttons
            if (window._i18nApplyNow) window._i18nApplyNow();
        }

        // ===== Mobile bottom sheet logic =====
        function getTabLabel(original) {
            // Prefer .btn-text (sidebar label), fall back to first text node
            const labelEl = original.querySelector('.btn-text');
            if (labelEl && labelEl.textContent.trim()) return labelEl.textContent.trim();
            return (original.textContent || '').trim();
        }

        function getTabIconClass(original) {
            const iconEl = original.querySelector('i');
            return iconEl ? iconEl.className : 'fas fa-star';
        }

        function renderMobileFavSheet() {
            const favs = getFavs();
            // Update badge count in header button
            if (mobileFavCount) mobileFavCount.textContent = String(favs.length);
            if (mobileFavBtn) {
                if (favs.length === 0) mobileFavBtn.classList.add('no-favs');
                else mobileFavBtn.classList.remove('no-favs');
            }
            if (!mobileFavList) return;
            mobileFavList.innerHTML = '';
            if (favs.length === 0) {
                if (mobileFavEmpty) mobileFavEmpty.classList.remove('hidden');
                return;
            }
            if (mobileFavEmpty) mobileFavEmpty.classList.add('hidden');
            favs.forEach(tabId => {
                const originals = sidebar.querySelectorAll('.main-tab-btn[data-tab="' + tabId + '"]');
                let original = null;
                originals.forEach(b => { if (!b.closest('#favorites-container')) original = b; });
                if (!original) return;

                const item = document.createElement('div');
                item.className = 'mobile-fav-item';
                item.dataset.tab = tabId;
                item.setAttribute('role', 'button');
                item.setAttribute('tabindex', '0');
                const label = getTabLabel(original);
                const iconClass = getTabIconClass(original);

                const iconSpan = document.createElement('i');
                iconSpan.className = iconClass + ' fav-item-icon';
                const labelSpan = document.createElement('span');
                labelSpan.className = 'fav-item-label';
                labelSpan.textContent = label;
                const removeBtn = document.createElement('button');
                removeBtn.type = 'button';
                removeBtn.className = 'fav-item-remove';
                removeBtn.setAttribute('aria-label', 'Hapus dari Favorit');
                removeBtn.innerHTML = '<i class="fas fa-times"></i>';
                removeBtn.addEventListener('click', (e) => {
                    e.stopPropagation();
                    toggleFavorite(tabId);
                });

                item.appendChild(iconSpan);
                item.appendChild(labelSpan);
                item.appendChild(removeBtn);

                item.addEventListener('click', (e) => {
                    if (e.target.closest('.fav-item-remove')) return;
                    e.stopPropagation();
                    closeMobileFavSheet();
                    // Navigate via the mobile-tab-btn if it exists (fires mobile switchTab handler),
                    // otherwise fall back to the desktop original.
                    const mobileBtn = document.querySelector('.mobile-tab-btn[data-tab="' + tabId + '"]');
                    if (mobileBtn) mobileBtn.click();
                    else original.click();
                });

                mobileFavList.appendChild(item);
            });
        }

        function openMobileFavSheet() {
            if (!mobileFavSheet) return;
            renderMobileFavSheet();
            mobileFavSheet.classList.add('open');
            mobileFavSheet.setAttribute('aria-hidden', 'false');
            document.body.style.overflow = 'hidden';
        }
        function closeMobileFavSheet() {
            if (!mobileFavSheet) return;
            mobileFavSheet.classList.remove('open');
            mobileFavSheet.setAttribute('aria-hidden', 'true');
            document.body.style.overflow = '';
        }

        if (mobileFavBtn) {
            mobileFavBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                e.preventDefault();
                openMobileFavSheet();
            });
        }
        if (mobileFavSheet) {
            mobileFavSheet.querySelectorAll('[data-fav-sheet-close]').forEach(el => {
                el.addEventListener('click', (e) => {
                    e.stopPropagation();
                    closeMobileFavSheet();
                });
            });
            // ESC key (desktop preview / tablet with keyboard)
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape' && mobileFavSheet.classList.contains('open')) {
                    closeMobileFavSheet();
                }
            });
        }

        addStarsToOriginals();
        reorderMobileTabs();
        renderFavorites();
    })();

    // --- UNIVERSAL MODAL LOGIC ---
    const universalModal = document.getElementById('universal-modal');
    const modalTitle = document.getElementById('modal-title');
    const modalBody = document.getElementById('modal-body');
    const closeModalBtn = document.getElementById('close-modal-btn');

    // Make universalModal globally accessible
    window.universalModal = universalModal;

    function showUniversalModal(title, content) {
        modalTitle.innerText = title;
        modalBody.innerHTML = content;
        universalModal.classList.add('visible');
        // Re-apply translations to freshly inserted content (data-i18n inside modal body)
        if (window._i18nApplyNow) window._i18nApplyNow();
    }
    window.showUniversalModal = showUniversalModal;

    // ==================== iOS DOWNLOAD HELPERS ====================
    // Safari/Chrome iOS tidak respect attribute `download` pada <a> untuk blob/data URLs —
    // browser navigate ke blob URL di tab baru → Canvas iframe ke-reset (session hilang,
    // user harus login lagi). Solusi: pakai Web Share API (iOS 15+) supaya muncul native
    // share sheet (Save to Photos / Files / AirDrop) tanpa navigate keluar Canvas.
    // Android dan Desktop tidak terdampak — `<a download>` jalan native di sana.
    window.__isIOS = function () {
        var ua = navigator.userAgent || '';
        if (/iPad|iPhone|iPod/.test(ua)) return true;
        // iPadOS 13+ pakai userAgent "Mac" tapi punya touch — cek touchPoints
        if (navigator.platform === 'MacIntel' && navigator.maxTouchPoints > 1) return true;
        return false;
    };

    window.__iosShareOrSaveImage = async function (blob, filename) {
        var mimeType = blob.type || 'image/png';
        var file = null;
        try {
            file = new File([blob], filename, { type: mimeType });
        } catch (e) {
            file = null;
        }

        // Path A: native iOS share sheet (Save to Photos, Save to Files, AirDrop, etc).
        // Web Share API butuh user gesture (click) + HTTPS — keduanya ada karena ini
        // dipanggil dari click handler. Kalau iframe Canvas restrict (no allow="web-share"),
        // navigator.share akan throw NotAllowedError → fallback ke modal.
        if (file && navigator.canShare) {
            try {
                if (navigator.canShare({ files: [file] })) {
                    await navigator.share({ files: [file], title: filename });
                    return; // Sukses (atau user cancel — keduanya aman, tidak perlu fallback)
                }
            } catch (err) {
                if (err && err.name === 'AbortError') return; // User cancel — no fallback needed
                // Error lain (NotAllowedError, dll) → lanjut ke fallback modal
            }
        }

        // Path B: modal fallback — tampilkan gambar, user long-press untuk save.
        // Tidak senyaman share sheet tapi tetap tidak navigate keluar Canvas.
        var url = URL.createObjectURL(blob);
        var isEN = (localStorage.getItem('app_language') || 'id') === 'en';
        var title = isEN ? 'Save Image' : 'Simpan Gambar';
        var instruction = isEN
            ? 'Press and <b>hold</b> the image below, then choose <b>"Save to Photos"</b> or <b>"Add to Photos"</b>.'
            : 'Tekan dan <b>tahan</b> gambar di bawah, lalu pilih <b>"Simpan ke Foto"</b> atau <b>"Tambah ke Foto"</b>.';
        var html =
            '<p class="mb-4 text-gray-700 text-sm sm:text-base">' + instruction + '</p>' +
            '<div class="bg-gray-100 p-3 rounded-lg">' +
                '<img src="' + url + '" alt="' + filename + '" ' +
                    'class="w-full rounded-lg" ' +
                    'style="-webkit-touch-callout: default; pointer-events: auto;">' +
            '</div>' +
            '<p class="mt-3 text-xs text-gray-500 break-all">' + filename + '</p>';
        if (window.showUniversalModal) {
            window.showUniversalModal(title, html);
        }
    };

    async function downloadImage(imageUrl, filename) {
        console.log('downloadImage called:', filename);

        try {
            // iOS path: native share sheet untuk hindari blob URL navigation yang reset Canvas session
            if (window.__isIOS && window.__isIOS()) {
                const iosResp = await fetch(imageUrl);
                if (!iosResp.ok) throw new Error('Network response was not ok');
                const iosBlob = await iosResp.blob();
                await window.__iosShareOrSaveImage(iosBlob, filename);
                return;
            }

            // Method 1: Try using fetch + blob (works on most modern browsers)
            if (imageUrl.startsWith('data:')) {
                // For data URLs, direct download
                const a = document.createElement('a');
                a.href = imageUrl;
                a.download = filename;
                a.style.display = 'none';
                document.body.appendChild(a);

                // Force click - important for mobile
                setTimeout(() => {
                    a.click();
                    setTimeout(() => {
                        document.body.removeChild(a);
                    }, 100);
                }, 10);

                console.log('Downloaded via data URL method');
                return;
            }

            // For regular URLs
            const response = await fetch(imageUrl);
            if (!response.ok) throw new Error('Network response was not ok');

            const blob = await response.blob();

            // Try multiple methods for maximum compatibility

            // Method 2: Try using FileSaver.js if available
            if (typeof saveAs !== 'undefined') {
                console.log('Using FileSaver.js');
                saveAs(blob, filename);
                return;
            }

            // Method 3: Create object URL and trigger download
            const url = window.URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = filename;
            a.style.display = 'none';

            document.body.appendChild(a);

            // Trigger download - use timeout for better mobile support
            setTimeout(() => {
                a.click();
                setTimeout(() => {
                    window.URL.revokeObjectURL(url);
                    document.body.removeChild(a);
                }, 100);
            }, 10);

            console.log('Downloaded via blob method');

        } catch (error) {
            console.error('Error downloading image:', error);

            // Fallback: Open in new tab for mobile
            try {
                const newWindow = window.open(imageUrl, '_blank');
                if (newWindow) {
                    showUniversalModal('Download', `<p class="mb-4">Gambar dibuka di tab baru. Tekan dan tahan gambar untuk menyimpan.</p>`);
                } else {
                    throw new Error('Popup blocked');
                }
            } catch (fallbackError) {
                // Last resort: Show in modal
                showUniversalModal('Download Gambar', `
                    <p class="mb-4 text-gray-700">Tekan dan tahan gambar di bawah, lalu pilih "Simpan Gambar" atau "Download Image".</p>
                    <div class="bg-gray-100 p-4 rounded-lg">
                        <img src="${imageUrl}" class="w-full rounded-lg border-2 border-gray-300" alt="${filename}">
                    </div>
                    <p class="mt-4 text-sm text-gray-500">Nama file: ${filename}</p>
                `);
            }
        }
    }
    window.downloadImage = downloadImage;

    // Alias untuk showModal
    window.showModal = showUniversalModal;

    // ============================================
    // HELPER FUNCTIONS FOR AFFILIATE FEATURES
    // ============================================

    // Helper function untuk toggle buttons (Platform, Length, Type, dll)
    function setupToggleButtons(containerSelector, btnClass) {
        const container = document.querySelector(containerSelector);
        if (!container) return;

        container.addEventListener('click', (e) => {
            const btn = e.target.closest(`.${btnClass}`);
            if (!btn) return;

            container.querySelectorAll(`.${btnClass}`).forEach(b => b.classList.remove('active'));
            btn.classList.add('active');
        });
    }

    // Helper function untuk copy to clipboard
    function setupCopyButton(buttonId, targetId) {
        const button = document.getElementById(buttonId);
        const target = document.getElementById(targetId);

        if (!button || !target) return;

        const fallbackCopy = (text) => {
            const textArea = document.createElement('textarea');
            textArea.value = text;
            textArea.style.position = 'fixed';
            textArea.style.left = '-9999px';
            textArea.style.top = '-9999px';
            document.body.appendChild(textArea);
            textArea.focus();
            textArea.select();
            try {
                const ok = document.execCommand('copy');
                document.body.removeChild(textArea);
                return ok;
            } catch (err) {
                document.body.removeChild(textArea);
                return false;
            }
        };

        const handleCopy = async (e) => {
            e.preventDefault();
            e.stopPropagation();

            const text = target.textContent;
            const originalText = button.innerHTML;

            try {
                if (navigator.clipboard && window.isSecureContext) {
                    await navigator.clipboard.writeText(text);
                } else {
                    fallbackCopy(text);
                }
                button.innerHTML = '<i class="fas fa-check"></i>Tersalin!';
                setTimeout(() => { button.innerHTML = originalText; }, 2000);
            } catch (err) {
                console.error('Failed to copy:', err);
                const ok = fallbackCopy(text);
                if (ok) {
                    button.innerHTML = '<i class="fas fa-check"></i>Tersalin!';
                    setTimeout(() => { button.innerHTML = originalText; }, 2000);
                }
            }
        };

        button.addEventListener('click', handleCopy);

        button.addEventListener('touchend', (e) => {
            e.preventDefault();
            e.stopPropagation();
            handleCopy(e);
        });
    }

    // Helper function untuk membersihkan markdown dari teks
    function cleanMarkdown(text) {
        if (!text) return text;
        // Hapus bold markdown ** atau __
        let cleaned = text.replace(/\*\*(.*?)\*\*/g, '$1');
        cleaned = cleaned.replace(/__(.*?)__/g, '$1');
        // Hapus italic markdown * atau _
        cleaned = cleaned.replace(/\*(.*?)\*/g, '$1');
        cleaned = cleaned.replace(/_(.*?)_/g, '$1');
        // Hapus markdown heading ###
        cleaned = cleaned.replace(/^#{1,6}\s+/gm, '');
        return cleaned;
    }

    // Copy to clipboard function
    async function copyToClipboard(text, button) {
        try {
            await navigator.clipboard.writeText(text);
            const originalText = button.innerHTML;
            button.innerHTML = '<i class="fas fa-check"></i> Tersalin!';
            button.classList.add('bg-green-500');
            setTimeout(() => {
                button.innerHTML = originalText;
                button.classList.remove('bg-green-500');
            }, 2000);
        } catch (error) {
            console.error('Error copying to clipboard:', error);
            // Fallback untuk browser yang tidak mendukung clipboard API
            const textarea = document.createElement('textarea');
            textarea.value = text;
            textarea.style.position = 'fixed';
            textarea.style.opacity = '0';
            document.body.appendChild(textarea);
            textarea.select();
            try {
                document.execCommand('copy');
                const originalText = button.innerHTML;
                button.innerHTML = '<i class="fas fa-check"></i> Tersalin!';
                button.classList.add('bg-green-500');
                setTimeout(() => {
                    button.innerHTML = originalText;
                    button.classList.remove('bg-green-500');
                }, 2000);
            } catch (err) {
                showUniversalModal('Gagal Menyalin', '<p class="text-red-500">Gagal menyalin ke clipboard. Silakan salin secara manual.</p>');
            }
            document.body.removeChild(textarea);
        }
    }
    window.copyToClipboard = copyToClipboard;

    // Helper function to add mobile touch support to buttons
    function addMobileTouchSupport(button) {
        if (!button) return;
        // Note: intentionally no-op - do NOT add e.preventDefault() to touchend
        // as it would block the synthesized click event on mobile devices
    }
    window.addMobileTouchSupport = addMobileTouchSupport;

    closeModalBtn.addEventListener('click', () => universalModal.classList.remove('visible'));
    universalModal.addEventListener('click', (e) => {
        if (e.target.classList.contains('modal-backdrop')) {
             universalModal.classList.remove('visible');
        }
    });
    
    // --- UPDATE INFO BUTTON LOGIC ---
    (function() {
        const UPDATE_VERSION = '48';
        // Badge versi di halaman login — auto-sync, tidak perlu bump manual
        (function() {
            var lvb = document.getElementById('login-version-badge');
            if (lvb) lvb.textContent = 'V' + UPDATE_VERSION;
        })();
        const STORAGE_KEY = 'affiliatego_update_seen';
        const btn = document.getElementById('update-info-btn');
        if (!btn) return;

        // Releases history — newest first. Max 3 displayed in modal.
        // Saat user bilang "rilis" untuk versi baru: prepend entry baru di sini, geser yang lain ke bawah, drop entry ke-4.
        const RELEASES = [
            {
                version: '48',
                dateId: 'Agustus 2026',
                dateEn: 'August 2026',
                badgeKey: 'modal.fix-v48-badge',
                badgeText: 'Update v48',
                gradient: 'linear-gradient(135deg,#16a34a,#0d9488)',
                borderColor: '#16a34a',
                icon: 'fa-moon',
                iconColor: '#16a34a',
                items: [
                    {
                        titleKey: 'modal.fix-v48-title-affiliate',
                        titleText: 'Program Affiliate — Cuan Rp50.000/Penjualan',
                        bodyKey: 'modal.fix-v48-body-affiliate',
                        bodyText: 'Sekarang Anda bisa jadi affiliate Affiliate GO dan dapat komisi Rp50.000 untuk setiap penjualan aplikasi lewat link Anda. Buka menu Ads untuk lihat detail + tonton tutorial cara jadi affiliate.'
                    },
                    {
                        titleKey: 'modal.fix-v48-title-dark',
                        titleText: 'Mode Gelap Manual & Warna Lebih Nyaman',
                        bodyKey: 'modal.fix-v48-body-dark',
                        bodyText: 'Mode gelap kini hanya aktif kalau Anda menyalakannya sendiri lewat tombol bulan/matahari — tidak lagi otomatis mengikuti setting HP/komputer. Warna yang tadinya bertabrakan di mode gelap (panel terang, teks sulit dibaca) sudah dirapikan di semua fitur, desktop maupun HP.'
                    }
                ]
            },
            {
                version: '47',
                dateId: 'Agustus 2026',
                dateEn: 'August 2026',
                badgeKey: 'modal.fix-v47-badge',
                badgeText: 'Update v47',
                gradient: 'linear-gradient(135deg,#16a34a,#0d9488)',
                borderColor: '#16a34a',
                icon: 'fa-moon',
                iconColor: '#16a34a',
                items: [
                    {
                        titleKey: 'modal.fix-v47-title-dark',
                        titleText: 'Mode Gelap (Dark Mode)',
                        bodyKey: 'modal.fix-v47-body-dark',
                        bodyText: 'Tampilan gelap untuk seluruh aplikasi — nyaman di mata saat dipakai malam hari. Aktifkan atau matikan lewat tombol bulan/matahari di samping pilihan bahasa. Pilihan Anda tersimpan permanen.'
                    }
                ]
            },
            {
                version: '46',
                dateId: 'Juli 2026',
                dateEn: 'July 2026',
                badgeKey: 'modal.fix-v46-badge',
                badgeText: 'Update v46',
                gradient: 'linear-gradient(135deg,#16a34a,#0d9488)',
                borderColor: '#16a34a',
                icon: 'fa-baby',
                iconColor: '#16a34a',
                items: [
                    {
                        titleKey: 'modal.fix-v46-title-mode',
                        titleText: 'New Born: Milestone Bulanan & Kartu Ucapan',
                        bodyKey: 'modal.fix-v46-body-mode',
                        bodyText: 'Tab Bayi Newborn kini punya 3 mode: Foto Tema, Milestone Bulanan (usia bayi terdeteksi otomatis dari tanggal lahir, props angka rajut/kartu kayu/balon/bunga), dan Kartu Ucapan Kelahiran (4 gaya termasuk Islami/Aqiqah, lengkap nama + tanggal + berat + panjang). Bayi tetap asli — hanya suasana & props yang dibuat AI.'
                    },
                    {
                        titleKey: 'modal.fix-v46-title-preset',
                        titleText: 'New Born: Preset Tema + Zodiak + Kolase 4-in-1',
                        bodyKey: 'modal.fix-v46-body-preset',
                        bodyText: 'Pilih kategori tema sekali klik: Profesi Cilik, Dongeng & Fantasi, Adat Nusantara, Islami & Aqiqah, Seasonal & Perayaan, plus tema Zodiak otomatis dari tanggal lahir. Ada juga Kolase 4-in-1: satu gambar berisi 4 tema berbeda dalam grid 2×2.'
                    },
                    {
                        titleKey: 'modal.fix-v46-title-ref',
                        titleText: 'New Born: Upload Gambar Referensi',
                        bodyKey: 'modal.fix-v46-body-ref',
                        bodyText: 'Punya contoh foto dari Pinterest atau fotografer? Upload sebagai referensi — tema, kostum, pose, dan pencahayaan hasil akan mengikuti gambar referensi, wajah bayi tetap dari foto Anda.'
                    }
                ]
            },
            {
                version: '45',
                dateId: 'Juli 2026',
                dateEn: 'July 2026',
                badgeKey: 'modal.fix-v45-badge',
                badgeText: 'Update v45',
                gradient: 'linear-gradient(135deg,#16a34a,#0d9488)',
                borderColor: '#16a34a',
                icon: 'fa-camera',
                iconColor: '#16a34a',
                items: [
                    {
                        titleKey: 'modal.fix-v45-title-preview',
                        titleText: 'Fix: Preview POV Selfie',
                        bodyKey: 'modal.fix-v45-body-preview',
                        bodyText: 'Tombol Preview di POV Selfie kini berfungsi normal — sebelumnya modal terbuka tapi tidak terlihat. Bisa ditutup dengan tombol X, klik area gelap, atau tombol ESC.'
                    },
                    {
                        titleKey: 'modal.fix-v45-title-kamera',
                        titleText: 'POV Selfie: Pilihan Jenis Kamera',
                        bodyKey: 'modal.fix-v45-body-kamera',
                        bodyText: 'Pilih karakter hasil foto sesuai kamera: Kamera HP (paling natural khas selfie), DSLR (tajam profesional dengan bokeh), Mirrorless (rendering modern bersih), atau Analog/Film (grain vintage hangat). Regenerate otomatis memakai kamera yang sama.'
                    }
                ]
            },
            {
                version: '44',
                dateId: 'Juli 2026',
                dateEn: 'July 2026',
                badgeKey: 'modal.fix-v44-badge',
                badgeText: 'Update v44',
                gradient: 'linear-gradient(135deg,#16a34a,#0d9488)',
                borderColor: '#16a34a',
                icon: 'fa-camera-retro',
                iconColor: '#16a34a',
                items: [
                    {
                        titleKey: 'modal.fix-v44-title-povselfie',
                        titleText: 'Fitur Baru: POV Selfie + Hub POV Studio',
                        bodyKey: 'modal.fix-v44-body-povselfie',
                        bodyText: 'Upload foto produk + foto model, AI membuat foto selfie bareng produk dengan background yang otomatis mengikuti tema produk (skincare ke meja rias, kopi ke cafe, olahraga ke gym). POV Tangan, POV Selfie, dan Mirror Selfie kini jadi satu menu POV Studio. Bonus: tema POV Tangan naik dari 20 jadi 48 dalam 8 kategori dan diacak tiap generate.'
                    },
                    {
                        titleKey: 'modal.fix-v44-title-fotomakanan',
                        titleText: 'Foto Makanan Profesional (ex Food Selfie)',
                        bodyKey: 'modal.fix-v44-body-fotomakanan',
                        bodyText: 'Food Selfie naik kelas jadi Foto Makanan Profesional: 40+ tema diperkaya ala food photographer (Studio & Editorial, Restoran & Kafe, Outdoor & Lifestyle, Kreatif & Artistik), gaya kamera diacak tiap generate, dan pilihan rasio lengkap 10 (termasuk 4:5 IG Feed, 2:3 Pinterest, 21:9 Cinematic). Review Makanan pindah ke menu ini dengan 8 preset tema sekali klik.'
                    },
                    {
                        titleKey: 'modal.fix-v44-title-reorg',
                        titleText: 'Menu Lebih Rapi + UI Konsisten',
                        bodyKey: 'modal.fix-v44-body-reorg',
                        bodyText: 'Generator Story Iklan (ex Iklan Produk) pindah ke hub Review Generator. Tab POV Tangan, Mockup Studio, dan Review Makanan di-redesign 2 kolom yang konsisten: langkah bernomor di kiri, hasil di kanan, plus tips di tiap tab.'
                    }
                ]
            },
            {
                version: '43',
                dateId: 'Juli 2026',
                dateEn: 'July 2026',
                badgeKey: 'modal.fix-v43-badge',
                badgeText: 'Update v43',
                gradient: 'linear-gradient(135deg,#16a34a,#0d9488)',
                borderColor: '#16a34a',
                icon: 'fa-sliders-h',
                iconColor: '#16a34a',
                items: [
                    {
                        titleKey: 'modal.fix-v43-title-feedmode',
                        titleText: 'Feed Branding: 3 Mode Topik',
                        bodyKey: 'modal.fix-v43-body-feedmode',
                        bodyText: 'Feed Branding sekarang punya 3 mode topik biar konten harian tidak monoton: Standar (6 post format tetap), Acak (AI mengarang 6 topik segar & beda tiap klik), dan Custom (tulis 1 topik sendiri mis. “Promo Ramadan”, AI buat 6 post yang nyambung dari berbagai sudut).'
                    }
                ]
            },
            {
                version: '42',
                dateId: 'Juli 2026',
                dateEn: 'July 2026',
                badgeKey: 'modal.fix-v42-badge',
                badgeText: 'Update v42',
                gradient: 'linear-gradient(135deg,#16a34a,#0d9488)',
                borderColor: '#16a34a',
                icon: 'fa-layer-group',
                iconColor: '#16a34a',
                items: [
                    {
                        titleKey: 'modal.fix-v42-title-feedbranding',
                        titleText: 'Fitur Baru: Feed Branding',
                        bodyKey: 'modal.fix-v42-body-feedbranding',
                        bodyText: 'Cukup 1 foto produk + nama brand, AI langsung membuat 6 desain feed Instagram yang seragam satu branding: Cover, Benefit, Testimoni, Cara Pakai, Harga, dan CTA — bergaya kampanye premium. Bisa atur 2 warna brand (kode hex), pilih rasio 4:5 / 1:1 / 9:16, plus preview & generate ulang per post.'
                    }
                ]
            },
            {
                version: '41',
                dateId: 'Juli 2026',
                dateEn: 'July 2026',
                badgeKey: 'modal.fix-v41-badge',
                badgeText: 'Update v41',
                gradient: 'linear-gradient(135deg,#16a34a,#15803d)',
                borderColor: '#16a34a',
                icon: 'fa-tags',
                iconColor: '#16a34a',
                items: [
                    {
                        titleKey: 'modal.fix-v41-title-harga',
                        titleText: 'Fitur Baru: Foto Produk Affiliate — Tema Harga',
                        bodyKey: 'modal.fix-v41-body-harga',
                        bodyText: 'Tab baru di Foto Produk Affiliate: sama seperti Tema Umum, tapi kamu bisa memasukkan harga di tiap foto produk. Hasil foto otomatis diberi panah kecil + label harga yang rapi & estetik di dekat tiap produk — cocok untuk konten katalog & promo. Lengkap dengan foto model opsional, background, pilihan rasio, dan jumlah generate 1-10.'
                    }
                ]
            },
            {
                version: '40',
                dateId: 'Juli 2026',
                dateEn: 'July 2026',
                badgeKey: 'modal.fix-v40-badge',
                badgeText: 'Update v40',
                gradient: 'linear-gradient(135deg,#16a34a,#ec4899)',
                borderColor: '#16a34a',
                icon: 'fa-layer-group',
                iconColor: '#16a34a',
                items: [
                    {
                        titleKey: 'modal.fix-v40-title-composite',
                        titleText: 'Fitur Baru: Foto Composite Anak (4-in-1)',
                        bodyKey: 'modal.fix-v40-body-composite',
                        bodyText: 'Mode baru di tab Foto Profesi Anak: satu foto studio seamless berisi 4 pose anak yang sama sekaligus — bukan grid atau kolase. Pilih dari 12 tema (Pilot, Astronot, Dokter, Bomba, dll) atau tulis tema sendiri, lengkap dengan nama & tagline custom, pilihan rasio, jumlah generate 1-10, dan preview per foto.'
                    }
                ]
            },
            {
                version: '39',
                dateId: 'Juni 2026',
                dateEn: 'June 2026',
                badgeKey: 'modal.fix-v39-badge',
                badgeText: 'Update v39',
                gradient: 'linear-gradient(135deg,#0ea5e9,#6366f1)',
                borderColor: '#0ea5e9',
                icon: 'fa-language',
                iconColor: '#0ea5e9',
                items: [
                    {
                        titleKey: 'modal.fix-v39-title-melayu',
                        titleText: 'Sokongan Penuh Bahasa Melayu',
                        bodyKey: 'modal.fix-v39-body-melayu',
                        bodyText: 'Seluruh aplikasi kini boleh ditukar ke Bahasa Melayu. Lebih 88 tab beserta nama menu navigasi (sidebar & mobile) sudah diterjemahkan penuh. Tekan penyukar bahasa di atas untuk bertukar antara Indonesia, English, dan Melayu — pilihan kamu disimpan automatik untuk lawatan seterusnya.'
                    }
                ]
            },
            {
                version: '38',
                dateId: 'Juni 2026',
                dateEn: 'June 2026',
                badgeKey: 'modal.fix-v38-badge',
                badgeText: 'Update v38',
                gradient: 'linear-gradient(135deg,#7c3aed,#db2777)',
                borderColor: '#7c3aed',
                icon: 'fa-bolt',
                iconColor: '#7c3aed',
                items: [
                    {
                        titleKey: 'modal.fix-v38-title-podcast',
                        titleText: 'Fitur Baru: Studio Podcast',
                        bodyKey: 'modal.fix-v38-body-podcast',
                        bodyText: 'Tab baru untuk bikin foto seolah sedang siaran podcast. Upload 1 foto karakter, pilih dari 5 tema suasana (Studio Profesional, Cozy Home, Neon/Gaming, Dark Moody Cinematic, atau Custom), dan AI akan mendudukkanmu di depan mic podcast. Tiap hasil otomatis dapat angle kamera berbeda. Rasio & jumlah generate bisa diatur.'
                    },
                    {
                        titleKey: 'modal.fix-v38-title-worldcup',
                        titleText: 'Fitur Baru: Foto World Cup',
                        bodyKey: 'modal.fix-v38-body-worldcup',
                        bodyText: 'Tab baru bertema Piala Dunia. Upload 1-5 foto wajah + ketik negara (AI otomatis pakai warna jersey & bendera negara itu), lalu pilih dari 11 tema: Suporter di Stadion, Jadi Pemain di Lapangan, Angkat Trophy, Jersey Portrait, Selebrasi Gol, Nobar Fan Zone, Player Tunnel, Foto Tim Resmi, Press Conference, Kartu Pemain, atau Custom. Tiap hasil otomatis dapat angle berbeda.'
                    },
                    {
                        titleKey: 'modal.fix-v38-title-tryon',
                        titleText: 'Virtual Try-On: Selektor Rasio (perbaikan konsistensi)',
                        bodyKey: 'modal.fix-v38-body-tryon',
                        bodyText: 'Tab Virtual Try-On sekarang punya selektor Rasio (3:4, 1:1, 4:5, 9:16, 16:9, 2:3). Sebelumnya rasio terkunci 3:4 dan sering tidak konsisten — sekarang rasio yang kamu pilih dikirim ke AI dan tampilan hasil mengikuti rasio itu, jadi yang dilihat sama dengan yang didownload.'
                    }
                ]
            },
            {
                version: '37',
                dateId: 'Mei 2026',
                dateEn: 'May 2026',
                badgeKey: 'modal.fix-v37-badge',
                badgeText: 'Update v37',
                gradient: 'linear-gradient(135deg,#16a34a,#0ea5e9)',
                borderColor: '#16a34a',
                icon: 'fa-wrench',
                iconColor: '#16a34a',
                items: [
                    {
                        titleKey: 'modal.fix-v37-title-hotfix',
                        titleText: 'Nonton Bareng: Hotfix Candid Stadion + Body Orientation + Bahasa',
                        bodyKey: 'modal.fix-v37-body-hotfix',
                        bodyText: '3 perbaikan di tab Nonton Bareng: (1) Theme Candid sekarang default di stadion (bukan di rumah) — crowd, flares, lapangan visible di background. (2) Semua theme: subject sekarang natural menghadap layar/lapangan (bukan membelakangi lapangan). (3) Bahasa overlay/teks dibatasi Indonesian + English saja — tidak lagi muncul teks Melayu/bahasa SEA lain di hasil generate.'
                    }
                ]
            },
            {
                version: '36',
                dateId: 'Mei 2026',
                dateEn: 'May 2026',
                badgeKey: 'modal.fix-v36-badge',
                badgeText: 'Update v36',
                gradient: 'linear-gradient(135deg,#16a34a,#0ea5e9)',
                borderColor: '#0ea5e9',
                icon: 'fa-camera-retro',
                iconColor: '#0ea5e9',
                items: [
                    {
                        titleKey: 'modal.fix-v36-title-candid',
                        titleText: 'Nonton Bareng: Theme Baru "Candid (Kedapatan Difoto)"',
                        bodyKey: 'modal.fix-v36-body-candid',
                        bodyText: 'Theme ke-7 di tab Nonton Bareng — foto candid unposed, subject NOT looking at camera, kedapatan difoto saat mid-reaction (mid-cheer, mid-laugh, mid-gasp). Smartphone snapshot quality dengan natural ambient lighting dan slight imperfect framing. Cocok untuk konten authentic "ketangkep difoto teman" yang lebih relatable dan natural daripada theme posed.'
                    }
                ]
            },
            {
                version: '35',
                dateId: 'Mei 2026',
                dateEn: 'May 2026',
                badgeKey: 'modal.fix-v35-badge',
                badgeText: 'Update v35',
                gradient: 'linear-gradient(135deg,#16a34a,#0ea5e9)',
                borderColor: '#0ea5e9',
                icon: 'fa-tv',
                iconColor: '#0ea5e9',
                items: [
                    {
                        titleKey: 'modal.fix-v35-title-teams-logo',
                        titleText: 'Nonton Bareng: Tim Sendiri vs Tim Lawan + Logo Team',
                        bodyKey: 'modal.fix-v35-body-teams-logo',
                        bodyText: 'Tab Nonton Bareng sekarang punya 2 kolom team terpisah (Tim Sendiri + Tim Lawan) untuk broadcast bar "LIVE: TIM A VS TIM B" yang akurat. Plus upload optional logo team — AI preserve EXACT logo design di overlay TV / flags supporter / banner tergantung scene yang dipilih.'
                    },
                    {
                        titleKey: 'modal.fix-v35-title-angle',
                        titleText: 'Nonton Bareng: Camera Angle Selector (6 opsi)',
                        bodyKey: 'modal.fix-v35-body-angle',
                        bodyText: 'Pilih sudut kamera: Wide (group visible), Medium, Close-up, Low Angle (dramatic), High Angle (TV broadcast style), Over Shoulder (POV nonton TV). Hasil generate jadi bervariasi gak monoton — same theme + angle berbeda = komposisi yang berbeda.'
                    },
                    {
                        titleKey: 'modal.fix-v35-title-preview',
                        titleText: 'Nonton Bareng: Preview Button + Lightbox Modal',
                        bodyKey: 'modal.fix-v35-body-preview',
                        bodyText: 'Hasil generate sekarang punya tombol Preview (selain Download) — klik untuk buka full-screen lightbox. Dismiss dengan ESC, klik backdrop, atau tombol close. Klik thumbnail langsung juga trigger preview.'
                    }
                ]
            },
            {
                version: '34',
                dateId: 'Mei 2026',
                dateEn: 'May 2026',
                badgeKey: 'modal.fix-v34-badge',
                badgeText: 'Update v34',
                gradient: 'linear-gradient(135deg,#16a34a,#0ea5e9)',
                borderColor: '#0ea5e9',
                icon: 'fa-rocket',
                iconColor: '#0ea5e9',
                items: [
                    {
                        titleKey: 'modal.fix-v34-title-watch',
                        titleText: 'Tab Baru: Nonton Bareng (Watch Together)',
                        bodyKey: 'modal.fix-v34-body-watch',
                        bodyText: 'Generate foto seolah-olah kamu nonton pertandingan bareng. Upload 1-5 foto wajah + foto jersey (opsional), pilih dari 6 scene: TV Broadcast Moment (kena kamera TV), Cafe/Warung, Living Room, Stadion, Watch Party Outdoor, Sport Bar/Pub. Plus 4 mood (excited/tense/celebrating/relaxed). Perfect untuk konten viral fans bola Indonesia!'
                    },
                    {
                        titleKey: 'modal.fix-v34-title-premium',
                        titleText: 'Tab Baru: Foto Produk Premium',
                        bodyKey: 'modal.fix-v34-body-premium',
                        bodyText: 'Studio foto produk cinematic untuk e-commerce & social media. 2 mode (Artistic & Lifestyle) dengan total 12 theme: Surreal Nature, Surreal Cosmic, Dark Luxury, Hyper Minimal, Liquid Splash, Floating Lifestyle, Hand Holding, Cafe Desk, Outdoor Adventure, Kitchen Counter, Vanity Bathroom, Gym Fitness. Plus 4 mood & custom prompt.'
                    },
                    {
                        titleKey: 'modal.fix-v34-title-memory',
                        titleText: 'Foto Ulang Tahun: Mode Kenangan (Past vs Present)',
                        bodyKey: 'modal.fix-v34-body-memory',
                        bodyText: 'Sub-mode baru di tab Foto Ulang Tahun: upload 2 foto (dewasa sekarang + masa kecil), pilih dari 6 theme cinematic (Memory Projection on Wall, Holding Childhood Photo, Mirror Reflection, Then & Now Split, Time Portal, Cake Photo Topper) + 4 mood. Hasil foto nostalgic menggabungkan past & present dari orang yang sama.'
                    }
                ]
            },
            {
                version: '33',
                dateId: 'Mei 2026',
                dateEn: 'May 2026',
                badgeKey: 'modal.fix-v33-badge',
                badgeText: 'Update v33',
                gradient: 'linear-gradient(135deg,#fbbf24,#f97316)',
                borderColor: '#f97316',
                icon: 'fa-icons',
                iconColor: '#f97316',
                items: [
                    {
                        titleKey: 'modal.fix-v33-title-pacar',
                        titleText: 'Sticker Pack Studio: Template "Untuk Pacar" Baru',
                        bodyKey: 'modal.fix-v33-body-pacar',
                        bodyText: '16 ekspresi romantic baru di Sticker Pack Studio &#8212; flying kiss "muach!", love eyes "cintaku", kangen, peluk, malu, wink, sampai love finger ala kpop. Background pink-rose gradient, teks Indonesia cursive sweetheart. Cocok kirim ke pasangan via Telegram/WA.'
                    },
                    {
                        titleKey: 'modal.fix-v33-title-anak',
                        titleText: 'Sticker Pack Studio: Template "Untuk Anak" Baru',
                        bodyKey: 'modal.fix-v33-body-anak',
                        bodyText: '16 ekspresi imut khas bayi/anak &#8212; menangis, hehehe, ngantuk, ohayoo, lagi kesel, gemes aku, Cape jirrr~, dan lainnya. Wajah real anak preserved (tidak berubah jadi chibi), text bahasa Indonesia di bawah-tengah ala kid sticker pack di Tokopedia/Shopee.'
                    },
                    {
                        titleKey: 'modal.fix-v33-title-expand',
                        titleText: 'Mood Pack & Reaction Pack: Sekarang 16 Ekspresi',
                        bodyKey: 'modal.fix-v33-body-expand',
                        bodyText: 'Mood Pack di-expand dari 10 ke 16 (focused, inspired, tired, accomplished, adventurous, contemplative). Reaction Pack juga ditambahkan 6 reaction baru (vibing, dead, mind-blown, sus, lfg, meh). Total Sticker Pack Studio sekarang 80 preset.'
                    }
                ]
            },
            {
                version: '32',
                dateId: 'Mei 2026',
                dateEn: 'May 2026',
                badgeKey: 'modal.fix-v32-badge',
                badgeText: 'Update v32',
                gradient: 'linear-gradient(135deg,#ec4899,#a855f7)',
                borderColor: '#ec4899',
                icon: 'fa-wand-magic-sparkles',
                iconColor: '#ec4899',
                items: [
                    {
                        titleKey: 'modal.fix-v32-title-studio-profil',
                        titleText: 'Studio Profil Sosmed: Foto Viral Tembus Kertas Sobek',
                        bodyKey: 'modal.fix-v32-body-studio-profil',
                        bodyText: 'Tab baru untuk bikin foto cinematic ala viral TikTok &#8212; karaktermu menembus kertas sobek dari layout profil sosmed (IG/TikTok/YouTube/X/FB) dengan tangan menyengkeram tepi sobekan. Bisa upload screenshot profil asli untuk hasil yang lebih akurat.'
                    },
                    {
                        titleKey: 'modal.fix-v32-title-studio-aesthetic',
                        titleText: 'Studio Aesthetic: Foto Korean Chibi Sticker',
                        bodyKey: 'modal.fix-v32-body-studio-aesthetic',
                        bodyText: 'Tab baru bikin komposisi foto aesthetic ala feed Instagram Korean style &#8212; karaktermu jadi pusat dengan chibi clones di sekitarnya, lengkap dengan doodle bunga, hati, sparkle, dan tulisan handwriting yang Instagrammable.'
                    },
                    {
                        titleKey: 'modal.fix-v32-title-sticker-pack',
                        titleText: 'Sticker Pack Studio: 1 Foto = Multi Ekspresi Terpisah',
                        bodyKey: 'modal.fix-v32-body-sticker-pack',
                        bodyText: 'Tab baru bikin sticker pack dari 1 foto wajah &#8212; pilih max 10 dari 36 preset (Expression / Mood / Reaction Pack), tiap pilihan jadi foto terpisah dengan tulisan Indonesia hand-written, doodle dekorasi, dan vibe sticker pack realistic.'
                    }
                ]
            },
            {
                version: '31',
                dateId: 'Mei 2026',
                dateEn: 'May 2026',
                badgeKey: 'modal.fix-v31-badge',
                badgeText: 'Perbaikan v31',
                gradient: 'linear-gradient(135deg,#0088cc,#229ED9)',
                borderColor: '#0088cc',
                icon: 'fa-expand',
                iconColor: '#0088cc',
                items: [
                    {
                        titleKey: 'modal.fix-v31-title-upscale',
                        titleText: 'Tab Upscale Baru &#8212; Cetak Foto Tanpa Burik',
                        bodyKey: 'modal.fix-v31-body-upscale',
                        bodyText: 'Naikkan resolusi foto kamu jadi 2K atau 4K dengan AI. Pas buat cetak foto produk affiliate atau posting di sosmed tanpa hasil pecah/burik. Beli paket 200x upscale di Lynk.id, sekali beli pakai sampai habis tanpa kadaluarsa.'
                    },
                    {
                        titleKey: 'modal.fix-v31-title-ruangsaku',
                        titleText: 'Tab Ruang Saku: Tampilan Baru',
                        bodyKey: 'modal.fix-v31-body-ruangsaku',
                        bodyText: 'Tab Ruang Saku sekarang punya halaman penjelasan singkat fitur Rindu (AI keuangan) + tombol langsung ke RuangSaku.com. Lebih nyaman dipakai di HP &#8212; tinggal klik dan terbuka di tab browser.'
                    },
                    {
                        titleKey: 'modal.fix-v31-title-telegram',
                        titleText: 'Tombol Telegram di Pojok Layar',
                        bodyKey: 'modal.fix-v31-body-telegram',
                        bodyText: 'Tombol bundar Telegram sekarang ada di pojok kanan bawah aplikasi. Sekali klik langsung join grup Telegram Affiliate Go &#8212; tempat update fitur, tips, dan tanya jawab dengan komunitas.'
                    }
                ]
            },
        ];

        // Hide dot if already seen
        function checkSeen() {
            try {
                if (localStorage.getItem(STORAGE_KEY) === UPDATE_VERSION) {
                    const dot = btn.querySelector('.update-notif-dot');
                    if (dot) dot.style.display = 'none';
                }
            } catch(e) {}
        }
        checkSeen();

        btn.addEventListener('click', function() {
            // Mark as seen
            try { localStorage.setItem(STORAGE_KEY, UPDATE_VERSION); } catch(e) {}
            const dot = btn.querySelector('.update-notif-dot');
            if (dot) dot.style.display = 'none';

            var _lang = (typeof localStorage !== 'undefined' && localStorage.getItem('app_language')) || 'id';
            var latest = RELEASES[0];
            var headerDate = (_lang === 'en') ? latest.dateEn : latest.dateId;

            var html = '<div style="font-family:Inter,sans-serif;">' +

            // Header badge
            '<div class="flex items-center justify-center gap-2 mb-3 p-3 rounded-xl" style="background:linear-gradient(135deg,#f0fdf4 0%,#dcfce7 100%);border:2px solid #16a34a;">' +
            '<i class="fas fa-bolt" style="color:#16a34a;font-size:1.1rem;"></i>' +
            '<span class="font-bold text-lg" style="color:#16a34a;">Versi ' + latest.version + '</span>' +
            '<span class="text-xs text-gray-500 ml-1">&#8212; ' + headerDate + '</span>' +
            '</div>' +

            // 90+ Fitur badge
            '<div class="flex items-center justify-center gap-2 mb-5 px-4 py-2 rounded-xl" style="background:linear-gradient(135deg,#fef3c7 0%,#fde68a 100%);border:2px solid #f59e0b;">' +
            '<i class="fas fa-layer-group" style="color:#d97706;"></i>' +
            '<span class="font-bold text-sm" style="color:#92400e;">90+ Fitur</span>' +
            '<span class="text-xs" style="color:#b45309;">tersedia di aplikasi ini</span>' +
            '</div>';

            // Loop releases — max 3 ditampilkan, urut tanggal terbaru di atas
            RELEASES.slice(0, 3).forEach(function(rel) {
                var date = (_lang === 'en') ? rel.dateEn : rel.dateId;
                html += '<div class="update-modal-section" style="border-color:' + rel.borderColor + ';">' +
                    '<div class="flex items-center gap-2 mb-3">' +
                    '<span style="background:' + rel.gradient + ';color:white;" class="update-modal-badge" data-i18n="' + rel.badgeKey + '">' +
                    '<i class="fas ' + rel.icon + ' mr-1"></i>' + rel.badgeText + '</span>' +
                    '<span class="text-xs text-gray-400 ml-auto">' + date + '</span>' +
                    '</div>';
                rel.items.forEach(function(item) {
                    html += '<div class="update-modal-item">' +
                        '<i class="fas ' + rel.icon + ' mt-0.5 flex-shrink-0" style="color:' + rel.iconColor + ';"></i>' +
                        '<div>' +
                        '<p class="font-semibold text-gray-800 text-sm" data-i18n="' + item.titleKey + '">' + item.titleText + '</p>' +
                        '<p class="text-xs text-gray-500 mt-0.5" data-i18n="' + item.bodyKey + '">' + item.bodyText + '</p>' +
                        '</div>' +
                        '</div>';
                });
                html += '</div>';
            });

            // Footer
            html += '<div class="text-center pt-2 text-xs text-gray-400">' +
            'Affiliate Go Foto Studio by Arul CG &#8212; Terima kasih sudah menggunakan!' +
            '</div>' +
            '</div>';

            if (window.showUniversalModal) {
                // Gunakan title terjemahan berdasarkan bahasa aktif
                var _title = (_lang === 'en')
                    ? '\u26a1 Latest Update \u2014 Version ' + latest.version
                    : '\u26a1 Update Terbaru \u2014 Versi ' + latest.version;
                window.showUniversalModal(_title, html);
            }
        });
    })();

    // --- BERANDA CHATBOT LOGIC ---
    (function() {
        const apiKey = "";
        const MODEL_NAME = "gemini-2.5-flash-preview-09-2025";
        const API_URL = `https://generativelanguage.googleapis.com/v1beta/models/${MODEL_NAME}:generateContent`;

        const chatContainer = document.getElementById('chat-container');
        const chatInput = document.getElementById('chat-input');
        const sendChatBtn = document.getElementById('send-chat-btn');

        if (!chatContainer || !chatInput || !sendChatBtn) return;

        // Chat history untuk context
        let chatHistory = [];

        // System prompt sebagai first message
        const systemPrompt = `Kamu adalah AI Assistant yang ramah dan helpful untuk "Affiliate Go Foto studio" by Arul CG.

Kamu adalah chatbot tanya jawab umum yang siap membantu user dengan berbagai pertanyaan dan topik. Kamu bisa:

• Menjawab pertanyaan umum tentang berbagai topik
• Memberikan penjelasan & informasi
• Membantu brainstorming ide kreatif
• Memberikan saran & tips
• Mengobrol dengan ramah dan informatif

Gaya komunikasi:
- Ramah, santai, dan mudah dipahami
- Gunakan Bahasa Indonesia yang natural
- Berikan jawaban yang jelas dan ringkas
- Gunakan emoji secukupnya untuk membuat percakapan lebih hidup
- Jika tidak tahu jawabannya, jujur saja dan tawarkan alternatif

Jawab pertanyaan user dengan helpful dan informatif!`;

        async function callGeminiAPI(userMessage) {
            // Ambil 10 pesan terakhir untuk context (mengikuti pattern dari chatai.txt)
            const recentHistory = chatHistory.slice(-10);

            // Build contents array
            const contents = recentHistory.map(msg => ({
                role: msg.role,
                parts: [{ text: msg.text }]
            }));

            // Add system prompt as first user message if history is empty
            if (contents.length === 0) {
                contents.push({
                    role: 'user',
                    parts: [{ text: systemPrompt }]
                });
                contents.push({
                    role: 'model',
                    parts: [{ text: 'Baik, saya siap membantu Anda dengan aplikasi Affiliate Go Foto Studio!' }]
                });
            }

            // Add new user message
            contents.push({
                role: 'user',
                parts: [{ text: userMessage }]
            });

            const payload = {
                contents: contents,
                generationConfig: {
                    temperature: 0.7,
                    maxOutputTokens: 2048
                }
            };

            const response = await fetch(`${API_URL}?key=${apiKey}`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            });

            if (!response.ok) {
                throw new Error(`HTTP Error: ${response.status}`);
            }

            const data = await response.json();

            if (data.candidates && data.candidates.length > 0) {
                return data.candidates[0].content.parts[0].text;
            } else {
                throw new Error('Tidak ada respon dari AI');
            }
        }

        async function sendMessage(message) {
            if (!message.trim()) return;

            const userMsg = message.trim();

            // Add user message to chat UI
            addMessageToChat(userMsg, 'user');

            // Add to history
            chatHistory.push({ role: 'user', text: userMsg });

            chatInput.value = '';
            chatInput.disabled = true;
            sendChatBtn.disabled = true;
            sendChatBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i>';

            // Add thinking indicator
            const thinkingId = addThinkingIndicator();

            try {
                const aiResponse = await callGeminiAPI(userMsg);

                // Remove thinking indicator
                removeThinkingIndicator(thinkingId);

                // Add AI response to chat UI
                addMessageToChat(aiResponse, 'assistant');

                // Add to history
                chatHistory.push({ role: 'model', text: aiResponse });

            } catch (error) {
                console.error('Error sending message:', error);
                removeThinkingIndicator(thinkingId);
                const warningEmoji = '\u26A0\uFE0F'; // ⚠️
                addMessageToChat(warningEmoji + ' Maaf, terjadi kesalahan. Silakan coba lagi.', 'assistant');
            } finally {
                chatInput.disabled = false;
                sendChatBtn.disabled = false;
                sendChatBtn.innerHTML = '<i class="fas fa-paper-plane"></i><span class="hidden sm:inline">Kirim</span>';
                chatInput.focus();
            }
        }

        function addMessageToChat(text, sender) {
            const messageDiv = document.createElement('div');
            messageDiv.className = `chat-message ${sender}-message mb-4`;

            if (sender === 'user') {
                messageDiv.innerHTML = `
                    <div class="flex items-start gap-3 justify-end">
                        <div class="bg-gradient-to-r from-purple-600 to-indigo-600 rounded-2xl rounded-tr-none p-4 shadow-md max-w-[80%]">
                            <p class="text-white leading-relaxed">${escapeHtml(text)}</p>
                        </div>
                        <div class="w-10 h-10 rounded-full bg-gradient-to-br from-gray-400 to-gray-600 flex items-center justify-center flex-shrink-0 shadow-lg">
                            <i class="fas fa-user text-white"></i>
                        </div>
                    </div>
                `;
            } else {
                // Process markdown-like formatting
                let formattedText = text
                    .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')  // Bold
                    .replace(/\n- /g, '<br>• ')  // Bullet points
                    .replace(/\n\d+\. /g, '<br>$&')  // Numbered lists
                    .replace(/\n\n/g, '<br><br>')  // Paragraphs
                    .replace(/\n/g, '<br>');  // Line breaks

                messageDiv.innerHTML = `
                    <div class="flex items-start gap-3">
                        <div class="w-10 h-10 rounded-full bg-gradient-to-br from-purple-500 to-indigo-600 flex items-center justify-center flex-shrink-0 shadow-lg">
                            <i class="fas fa-robot text-white"></i>
                        </div>
                        <div class="flex-1 bg-white rounded-2xl rounded-tl-none p-4 shadow-md border border-gray-100 max-w-[80%]">
                            <p class="text-gray-800 leading-relaxed">${formattedText}</p>
                        </div>
                    </div>
                `;
            }

            chatContainer.appendChild(messageDiv);
            chatContainer.scrollTop = chatContainer.scrollHeight;
        }

        function addThinkingIndicator() {
            const thinkingDiv = document.createElement('div');
            const thinkingId = 'thinking-' + Date.now();
            thinkingDiv.id = thinkingId;
            thinkingDiv.className = 'chat-message assistant-message mb-4';
            thinkingDiv.innerHTML = `
                <div class="flex items-start gap-3">
                    <div class="w-10 h-10 rounded-full bg-gradient-to-br from-purple-500 to-indigo-600 flex items-center justify-center flex-shrink-0 shadow-lg">
                        <i class="fas fa-robot text-white"></i>
                    </div>
                    <div class="bg-white rounded-2xl rounded-tl-none p-4 shadow-md border border-gray-100">
                        <div class="flex gap-2">
                            <div class="w-2 h-2 bg-purple-500 rounded-full animate-bounce" style="animation-delay: 0s"></div>
                            <div class="w-2 h-2 bg-purple-500 rounded-full animate-bounce" style="animation-delay: 0.2s"></div>
                            <div class="w-2 h-2 bg-purple-500 rounded-full animate-bounce" style="animation-delay: 0.4s"></div>
                        </div>
                    </div>
                </div>
            `;
            chatContainer.appendChild(thinkingDiv);
            chatContainer.scrollTop = chatContainer.scrollHeight;
            return thinkingId;
        }

        function removeThinkingIndicator(thinkingId) {
            const thinkingDiv = document.getElementById(thinkingId);
            if (thinkingDiv) thinkingDiv.remove();
        }

        function escapeHtml(text) {
            const div = document.createElement('div');
            div.textContent = text;
            return div.innerHTML;
        }

        // Event Listeners
        sendChatBtn.addEventListener('click', () => {
            sendMessage(chatInput.value);
        });

        chatInput.addEventListener('keydown', (e) => {
            if (e.key === 'Enter' && !e.shiftKey) {
                e.preventDefault();
                sendMessage(chatInput.value);
            }
        });

    })();

    // --- MINIATURE ME LOGIC ---
    (function() {
        const uploader = document.getElementById('miniature-person-image-uploader');
        const fileInput = document.getElementById('miniature-person-file-input');
        const uploadPrompt = document.getElementById('miniature-person-upload-prompt');
        const imagePreview = document.getElementById('miniature-person-image-preview');
        const ratioSelection = document.getElementById('miniature-ratio-selection');
        const manualPrompt = document.getElementById('miniature-manual-prompt');
        const generateBtn = document.getElementById('miniature-generate-btn');
        const loader = document.getElementById('miniature-loader');
        const placeholderResult = document.getElementById('miniature-placeholder-result');
        const resultContent = document.getElementById('miniature-result-content');
        const errorMessage = document.getElementById('miniature-error-message');
        let personBase64Image = null;
        let selectedRatio = '16:9';

        function setupUploader(uploaderEl, inputEl, previewEl, promptEl, storageCallback) {
            uploaderEl.addEventListener('click', () => inputEl.click());
            uploaderEl.addEventListener('dragover', (e) => { e.preventDefault(); uploaderEl.classList.add('border-indigo-400', 'bg-indigo-50'); });
            uploaderEl.addEventListener('dragleave', () => uploaderEl.classList.remove('border-indigo-400', 'bg-indigo-50'));
            uploaderEl.addEventListener('drop', (e) => {
                e.preventDefault();
                uploaderEl.classList.remove('border-indigo-400', 'bg-indigo-50');
                if (e.dataTransfer.files.length > 0) {
                    inputEl.files = e.dataTransfer.files;
                    handleFileSelect({ target: inputEl }, previewEl, promptEl, storageCallback);
                }
            });
            inputEl.addEventListener('change', (e) => handleFileSelect(e, previewEl, promptEl, storageCallback));
        }

        function handleFileSelect(event, preview, prompt, storageCallback) {
            const file = event.target.files[0];
            if (file && file.type.startsWith('image/')) {
                const reader = new FileReader();
                reader.onloadend = () => {
                    storageCallback(reader.result.split(',')[1]);
                    preview.src = reader.result;
                    preview.classList.remove('hidden');
                    prompt.classList.add('hidden');
                    validateInputs();
                };
                reader.readAsDataURL(file);
            }
        }

        setupUploader(uploader, fileInput, imagePreview, uploadPrompt, (data) => { personBase64Image = data; });
        ratioSelection.addEventListener('click', (e) => {
            const button = e.target.closest('.ratio-btn-mini');
            if (button) {
                document.querySelectorAll('#miniature-ratio-selection .ratio-btn-mini').forEach(btn => btn.classList.remove('selected'));
                button.classList.add('selected');
                selectedRatio = button.dataset.ratio;
            }
        });

        [fileInput, manualPrompt].forEach(el => el.addEventListener('input', validateInputs));
        function validateInputs() { generateBtn.disabled = !personBase64Image; }

        const miniaturePrompts = [
            "figur skala 1/7 realistis di meja komputer.",
            "berdiri di atas layar HP raksasa.",
            "gantungan kunci action figure.",
            "mockup produk figur miniatur.",
            "memegang figur miniatur dirinya di studio.",
            "action figure di dalam lemari kaca.",
            "miniatur duduk di atas buku tebal.",
            "figur kecil di atas keyboard laptop.",
            "action figure di dashboard mobil.",
            "miniatur berdiri di atas cangkir kopi.",
            "figur kecil di dalam botol kaca.",
            "miniatur di atas tumpukan koin.",
            "action figure pose superhero di meja kerja.",
            "figur kecil dengan latar cityscape miniatur.",
            "miniatur di atas tanaman pot kecil.",
            "action figure di rak display koleksi.",
            "figur kecil sedang naik tangga pensil.",
            "miniatur dengan lighting studio profesional.",
            "action figure di antara makanan miniatur.",
            "figur kecil di landscape diorama."
        ];

        async function generateSingleMiniature(index, baseScene) {
            const card = document.getElementById(`miniature-card-${index}`);
            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                let finalPrompt = `Based on the uploaded image, create a photorealistic image of the person as a miniature figure. Aspect ratio: ${selectedRatio}. Scene: ${baseScene}.`;
                if (manualPrompt.value.trim()) finalPrompt += ` Additional style: ${manualPrompt.value.trim()}.`;

                const payload = {
                    contents: [{ parts: [{ text: finalPrompt }, { inlineData: { mimeType: "image/jpeg", data: personBase64Image } }] }],
                    generationConfig: { responseModalities: ['TEXT', 'IMAGE'], imageConfig: { aspectRatio: selectedRatio } }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const aspectClass = selectedRatio === '1:1' ? 'aspect-square' : selectedRatio === '16:9' ? 'aspect-video' : selectedRatio === '3:4' ? 'aspect-[3/4]' : 'aspect-[9/16]';
                if (card) {
                    card.className = 'text-center';
                    card.innerHTML = `<img src="${imageUrl}" class="rounded-lg shadow-md w-full h-auto ${aspectClass} object-cover"><a href="#" data-action="download" data-base64="${base64Data}" data-filename="miniature-${index}.png" class="download-btn mt-2 inline-block bg-[#4f46e5] text-white py-1 px-4 rounded-full text-xs hover:bg-[#4338ca]"><i class="fas fa-download mr-1"></i> Download</a>`;
                }
            } catch (error) {
                console.error(`Error miniature ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        generateBtn.addEventListener('click', async () => {
            if (generateBtn.disabled) { showError("Silakan unggah foto Anda."); return; }
            showLoading(true);
            showError('');

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;
            const aspectClass = selectedRatio === '1:1' ? 'aspect-square' : selectedRatio === '16:9' ? 'aspect-video' : selectedRatio === '3:4' ? 'aspect-[3/4]' : 'aspect-[9/16]';

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                resultContent.innerHTML = '';

                miniaturePrompts.forEach((_, i) => {
                    const card = document.createElement('div');
                    card.id = `miniature-card-${i + 1}`;
                    card.className = 'text-center';
                    card.innerHTML = `<div class="rounded-lg shadow-md w-full ${aspectClass} bg-indigo-100 flex items-center justify-center"><div class="animate-spin rounded-full h-8 w-8 border-b-2 border-indigo-600"></div></div>`;
                    resultContent.appendChild(card);
                });

                resultContent.classList.remove('hidden');

                const generationPromises = miniaturePrompts.map((scene, i) => generateSingleMiniature(i + 1, scene));
                await Promise.allSettled(generationPromises);

                // Remove empty cards
                resultContent.querySelectorAll('[id^="miniature-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });

                successCount = resultContent.querySelectorAll('img').length;
            }

            showLoading(false);
            if (successCount === 0) {
                resultContent.classList.add('hidden');
                showError('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            }
        });
        
        resultContent.addEventListener('click', async function(e) {
            const btn = e.target.closest('.download-btn');
            if (btn) {
                e.stopPropagation();
                const base64 = btn.dataset.base64;
                const filename = btn.dataset.filename;
                if (base64 && filename) {
                    const imageUrl = `data:image/png;base64,${base64}`;
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageUrl, filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageUrl, filename);
                    }
                }
            }
        });

        function showLoading(isLoading) {
            loader.classList.toggle('hidden', !isLoading);
            loader.classList.toggle('flex', isLoading);
            placeholderResult.classList.toggle('hidden', isLoading);
            resultContent.classList.toggle('hidden', !resultContent.children.length > 0 || isLoading);
            generateBtn.disabled = isLoading;
        }

        function showError(message) {
            errorMessage.textContent = message;
            errorMessage.classList.toggle('hidden', !message);
            if(message) { placeholderResult.classList.add('hidden'); resultContent.classList.add('hidden'); }
        }
        
        validateInputs();
    })();

    // --- HALU IDOL LOGIC ---
    (function() {
        const imageInput = document.getElementById('halu-image-input');
        const imageUploadArea = document.getElementById('halu-image-upload-area');
        const imagePreviewContainer = document.getElementById('halu-image-preview-container');
        const imagePreview = document.getElementById('halu-image-preview');
        const removeImageBtn = document.getElementById('halu-remove-image-btn');
        const idolImageInput = document.getElementById('halu-idol-image-input');
        const idolImageUploadArea = document.getElementById('halu-idol-image-upload-area');
        const idolImagePreviewContainer = document.getElementById('halu-idol-image-preview-container');
        const idolImagePreview = document.getElementById('halu-idol-image-preview');
        const removeIdolImageBtn = document.getElementById('halu-remove-idol-image-btn');
        const momentDescInput = document.getElementById('halu-moment-desc-input');
        const photoStyleInput = document.getElementById('halu-photo-style-input');
        const generateBtn = document.getElementById('halu-generate-btn');
        const bRollGrid = document.getElementById('halu-b-roll-grid');
        const haluRatioSelection = document.getElementById('halu-ratio-selection');

        let uploadedImageBase64 = null, imageMimeType = null;
        let uploadedIdolImageBase64 = null, idolImageMimeType = null;
        let selectedHaluRatio = '16:9';

        function updateButtonState() {
            generateBtn.disabled = !uploadedImageBase64 || !uploadedIdolImageBase64;
        }

        function setupImageUpload(input, uploadArea, previewContainer, previewImg, removeBtn, onUpload) {
            input.addEventListener('change', (event) => {
                const file = event.target.files[0];
                if (file) {
                    const reader = new FileReader();
                    reader.onload = (e) => {
                        previewImg.src = e.target.result;
                        const [header, base64] = e.target.result.split(',');
                        const mimeType = header.match(/:(.*?);/)[1];
                        onUpload(base64, mimeType);
                        uploadArea.classList.add('hidden');
                        previewContainer.classList.remove('hidden');
                        updateButtonState();
                    };
                    reader.readAsDataURL(file);
                }
            });
            removeBtn.addEventListener('click', () => {
                onUpload(null, null);
                input.value = '';
                uploadArea.classList.remove('hidden');
                previewContainer.classList.add('hidden');
                updateButtonState();
            });
        }
        
        setupImageUpload(imageInput, imageUploadArea, imagePreviewContainer, imagePreview, removeImageBtn, (base64, mime) => { uploadedImageBase64 = base64; imageMimeType = mime; });
        setupImageUpload(idolImageInput, idolImageUploadArea, idolImagePreviewContainer, idolImagePreview, removeIdolImageBtn, (base64, mime) => { uploadedIdolImageBase64 = base64; idolImageMimeType = mime; });
        
        if (haluRatioSelection) {
            haluRatioSelection.addEventListener('click', (e) => {
                const button = e.target.closest('.ratio-btn-halu');
                if (button) {
                    document.querySelectorAll('#halu-ratio-selection .ratio-btn-halu').forEach(btn => btn.classList.remove('selected'));
                    button.classList.add('selected');
                    selectedHaluRatio = button.dataset.ratio;
                }
            });
        }
        
        const createInitialPlaceholders = () => {
            bRollGrid.innerHTML = '';
            for(let i = 1; i <= 6; i++) {
                const card = document.createElement('div');
                card.className = 'b-roll-card card p-4 flex flex-col justify-between animate-pulse';
                card.innerHTML = `<div class="h-6 bg-pink-200 rounded w-3/4 mb-4"></div><div class="mt-4 aspect-square bg-pink-100 rounded-md"></div>`;
                bRollGrid.appendChild(card);
            }
        };
        createInitialPlaceholders();
        
        generateBtn.addEventListener('click', async () => {
            if (!uploadedImageBase64 || !uploadedIdolImageBase64) return;
            generateBtn.disabled = true;
            generateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Mencari Ide...</span>`;

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                try {
                    const bRollIdeas = await analyzeAndGetPrompts();
                    generateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Membuat Foto...</span>`;
                    createDynamicCards(bRollIdeas);
                    const generationPromises = bRollIdeas.map((idea, index) => generateSingleBroll(index + 1, idea.title, idea.prompt));
                    await Promise.allSettled(generationPromises);

                    bRollGrid.querySelectorAll('[id^="halu-card-"]').forEach(card => {
                        if (!card.querySelector('img')) card.remove();
                    });
                    successCount = bRollGrid.querySelectorAll('img').length;
                } catch (error) {
                    console.error("Error:", error);
                }
            }

            generateBtn.disabled = false;
            generateBtn.innerHTML = `<span>Buat Foto Halu</span>`;
            if (successCount === 0) alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
        });

        async function analyzeAndGetPrompts() {
             const apiKey = "";
             const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
             const systemPrompt = `You are an AI photo blending assistant. Analyze a fan's photo and an idol's photo. Generate 6 distinct, creative photo concepts where they appear together as a couple. For each, provide a short title (in Indonesian) and a detailed prompt (in English) for an AI image generator. Respond ONLY with a valid JSON array of 6 objects with 'title' and 'prompt' keys.`;
             let userQuery = `Moment: "${momentDescInput.value.trim()}"\nStyle: "${photoStyleInput.value.trim()}"\nAspect Ratio: "${selectedHaluRatio}"`;
             const parts = [{ text: userQuery }, { inlineData: { mimeType: imageMimeType, data: uploadedImageBase64 } }, { inlineData: { mimeType: idolImageMimeType, data: uploadedIdolImageBase64 } }];
             const payload = { contents: [{ parts }], systemInstruction: { parts: [{ text: systemPrompt }] }, generationConfig: { responseMimeType: "application/json", responseSchema: { type: "ARRAY", items: { type: "OBJECT", properties: { "title": { "type": "STRING" }, "prompt": { "type": "STRING" } } } } } };
             const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
             const result = await response.json();
             return JSON.parse(result.candidates[0].content.parts[0].text);
        }

        async function generateSingleBroll(id, title, prompt) {
            const card = document.getElementById(`halu-card-${id}`);
            if (!card) return;
            const outputContainer = card.querySelector('.aspect-square');
            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const finalPrompt = `${prompt}, realistic photo, romantic, candid moment, high quality, detailed faces, cinematic lighting, 8k, aspect ratio ${selectedHaluRatio}`;
                const parts = [{ text: finalPrompt }, { inlineData: { mimeType: imageMimeType, data: uploadedImageBase64 } }, { inlineData: { mimeType: idolImageMimeType, data: uploadedIdolImageBase64 } }];
                const payload = { contents: [{ parts }], generationConfig: { responseModalities: ['TEXT', 'IMAGE'], imageConfig: { aspectRatio: selectedHaluRatio } } };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error("No image data from API.");
                const imageUrl = `data:image/png;base64,${base64Data}`;
                const safeTitle = title.replace(/[^a-z0-9]/gi, '_').toLowerCase();
                outputContainer.innerHTML = `
                    <div class="relative w-full h-full group">
                        <img src="${imageUrl}" class="w-full h-full object-cover rounded-md" alt="${title}">
                        <div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/70 via-black/50 to-transparent p-4 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-all duration-300 ease-in-out rounded-b-md">
                            <div class="flex gap-2 justify-end">
                                <button data-action="halu-download" data-image-url="${imageUrl}" data-filename="halu_${id}_${safeTitle}.png" class="action-btn bg-pink-600 text-white px-3 py-2 rounded-lg hover:bg-pink-700 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Unduh Gambar">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M3 17a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm3.293-7.707a1 1 0 011.414 0L9 10.586V3a1 1 0 112 0v7.586l1.293-1.293a1 1 0 111.414 1.414l-3 3a1 1 0 01-1.414 0l-3-3a1 1 0 010-1.414z" clip-rule="evenodd" /></svg>
                                    <span class="hidden sm:inline">Unduh</span>
                                </button>
                            </div>
                        </div>
                    </div>`;
            } catch (error) {
                console.error(`Error halu ${id}:`, error);
                if (card) card.innerHTML = '';
            }
        }
        
        const createDynamicCards = (prompts) => {
            bRollGrid.innerHTML = '';
            prompts.forEach((p, index) => {
                const card = document.createElement('div');
                card.id = `halu-card-${index + 1}`;
                card.className = 'b-roll-card card p-4 flex flex-col justify-between';
                card.innerHTML = `<div><h3 class="text-lg font-semibold text-gray-800 mb-4">${p.title}</h3></div> <div class="aspect-square bg-pink-100 rounded-md flex items-center justify-center"><div class="loader"></div></div>`;
                bRollGrid.appendChild(card);
            });
        };

        const heartsContainer = document.querySelector('#content-halu .hearts-container');
        if (heartsContainer) {
            for (let i = 0; i < 20; i++) {
                const heart = document.createElement('div');
                heart.className = 'heart';
                heart.style.left = `${Math.random() * 100}vw`;
                heart.style.animationDuration = `${8 + Math.random() * 10}s`;
                heart.style.animationDelay = `${Math.random() * 7}s`;
                heartsContainer.appendChild(heart);
            }
        }

        // Event delegation for halu download buttons
        if (bRollGrid) {
            bRollGrid.addEventListener('click', async (e) => {
                const downloadBtn = e.target.closest('[data-action="halu-download"]');
                if (downloadBtn) {
                    e.stopPropagation();
                    const imageUrl = downloadBtn.dataset.imageUrl;
                    const filename = downloadBtn.dataset.filename;
                    if (imageUrl && filename) {
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(imageUrl, filename);
                        } else if (window.downloadImage) {
                            await window.downloadImage(imageUrl, filename);
                        }
                    }
                }
            });
        }
    })();

    // --- FOTO PRODUK (AFFILIATE GO) LOGIC ---
    (function() {
        // --- B-Roll (Foto Produk) Logic ---
        const brollImageInput = document.getElementById('broll-image-input');
        const brollImageUploadArea = document.getElementById('broll-image-upload-area');
        const brollImagePreviewContainer = document.getElementById('broll-image-preview-container');
        const brollImagePreview = document.getElementById('broll-image-preview');
        const brollRemoveImageBtn = document.getElementById('broll-remove-image-btn');
        const brollModelImageInput = document.getElementById('broll-model-image-input');
        const brollModelImageUploadArea = document.getElementById('broll-model-image-upload-area');
        const brollModelImagePreviewContainer = document.getElementById('broll-model-image-preview-container');
        const brollModelImagePreview = document.getElementById('broll-model-image-preview');
        const brollRemoveModelImageBtn = document.getElementById('broll-remove-model-image-btn');
        const brollBackgroundImageInput = document.getElementById('broll-background-image-input');
        const brollBackgroundImageUploadArea = document.getElementById('broll-background-image-upload-area');
        const brollBackgroundImagePreviewContainer = document.getElementById('broll-background-image-preview-container');
        const brollBackgroundImagePreview = document.getElementById('broll-background-image-preview');
        const brollRemoveBackgroundImageBtn = document.getElementById('broll-remove-background-image-btn');
        const brollProductDescInput = document.getElementById('broll-product-desc-input');
        const brollGenerateBtn = document.getElementById('broll-generate-btn');
        const brollGrid = document.getElementById('broll-b-roll-grid');
        const brollGenerateDescBtn = document.getElementById('broll-generate-desc-btn');
        const brollResultsHeader = document.getElementById('broll-results-header');
        const brollResultCount = document.getElementById('broll-result-count');
        const brollDownloadAllBtn = document.getElementById('broll-download-all-btn');
        const brollThemeOptions = document.getElementById('broll-theme-options');
        const brollCustomThemeContainer = document.getElementById('broll-custom-theme-container');
        const brollPhotoThemeInput = document.getElementById('broll-photo-theme-input');
        let brollProductImages = [];
        let brollModelImageBase64 = null, brollModelImageMimeType = null;
        let brollBackgroundImageBase64 = null, brollBackgroundImageMimeType = null;
        const brollRatioSelection = document.getElementById('broll-ratio-selection');
        let selectedBrollRatio = '16:9';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let selectedBrollTheme = '';
        let generatedBrollImages = [];

        // Count selection (1-10), default 4
        const brollCountSel = document.getElementById('broll-count-selection-grid');
        if (brollCountSel) {
            brollCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                brollCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }
        
        function updateBrollButtonState() {
            if(brollGenerateBtn) brollGenerateBtn.disabled = brollProductImages.length === 0 || !brollProductDescInput.value.trim();
            if(brollGenerateDescBtn) brollGenerateDescBtn.disabled = brollProductImages.length === 0;
        }

        // Handle multiple product images
        function handleBrollProductFiles(files) {
            if (!files || files.length === 0) return;

            brollImageUploadArea.classList.add('hidden');
            brollImagePreviewContainer.classList.remove('hidden');

            let filesToProcess = files.length;

            Array.from(files).forEach((file) => {
                const reader = new FileReader();
                reader.onload = (e) => {
                    const [header, base64] = e.target.result.split(',');
                    const mimeType = header.match(/:(.*?);/)[1];
                    const imageData = {
                        id: Date.now() + Math.random(),
                        base64: base64,
                        mimeType: mimeType
                    };

                    brollProductImages.push(imageData);

                    const previewWrapper = document.createElement('div');
                    previewWrapper.className = 'relative group';
                    previewWrapper.innerHTML = `
                        <img src="${e.target.result}" alt="Pratinjau Produk" class="rounded-lg w-full h-24 object-cover">
                        <button data-id="${imageData.id}" class="absolute top-1 right-1 bg-red-500 text-white rounded-full p-1 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity focus:opacity-100">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                        </button>
                    `;

                    brollImagePreviewContainer.appendChild(previewWrapper);

                    previewWrapper.querySelector('button').addEventListener('click', (btnEvent) => {
                        const idToRemove = parseFloat(btnEvent.currentTarget.getAttribute('data-id'));
                        brollProductImages = brollProductImages.filter(img => img.id !== idToRemove);
                        previewWrapper.remove();
                        if (brollProductImages.length === 0) {
                            brollImageUploadArea.classList.remove('hidden');
                            brollImagePreviewContainer.classList.add('hidden');
                            const addButton = document.getElementById('broll-add-more-button');
                            if (addButton) addButton.remove();
                        }
                        updateBrollButtonState();
                    });

                    updateBrollButtonState();

                    filesToProcess--;
                    if (filesToProcess === 0) {
                        manageAddMoreButton();
                    }
                };
                reader.readAsDataURL(file);
            });
        }

        function manageAddMoreButton() {
            const existingButton = document.getElementById('broll-add-more-button');
            if (existingButton) {
                existingButton.remove();
            }

            const addButtonWrapper = document.createElement('div');
            addButtonWrapper.id = 'broll-add-more-button';
            addButtonWrapper.className = 'relative group cursor-pointer';
            addButtonWrapper.innerHTML = `
                <div class="rounded-lg w-full h-24 border-2 border-dashed border-gray-400 hover:border-cyan-500 bg-gray-50 hover:bg-cyan-50 transition-colors flex items-center justify-center">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-gray-400 group-hover:text-cyan-500" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path></svg>
                </div>
            `;

            addButtonWrapper.addEventListener('click', () => {
                brollImageInput.click();
            });

            brollImagePreviewContainer.appendChild(addButtonWrapper);
        }

        if (brollImageInput) {
            brollImageInput.addEventListener('change', (event) => {
                handleBrollProductFiles(event.target.files);
                event.target.value = null;
            });
        }

        // Setup model image upload (single image)
        function setupBrollModelImageUpload(input, uploadArea, previewContainer, previewImg, removeBtn) {
            if (!input) return;
            input.addEventListener('change', (event) => {
                const file = event.target.files[0];
                if (!file) return;
                const reader = new FileReader();
                reader.onload = (e) => {
                    previewImg.src = e.target.result;
                    const [header, base64] = e.target.result.split(',');
                    const mimeType = header.match(/:(.*?);/)[1];
                    brollModelImageBase64 = base64;
                    brollModelImageMimeType = mimeType;
                    uploadArea.classList.add('hidden');
                    previewContainer.classList.remove('hidden');
                };
                reader.readAsDataURL(file);
            });
            removeBtn.addEventListener('click', () => {
                brollModelImageBase64 = null;
                brollModelImageMimeType = null;
                input.value = '';
                uploadArea.classList.remove('hidden');
                previewContainer.classList.add('hidden');
            });
        }
        setupBrollModelImageUpload(brollModelImageInput, brollModelImageUploadArea, brollModelImagePreviewContainer, brollModelImagePreview, brollRemoveModelImageBtn);

        // Setup background image upload (single image)
        function setupBrollBackgroundImageUpload(input, uploadArea, previewContainer, previewImg, removeBtn) {
            if (!input) return;
            input.addEventListener('change', (event) => {
                const file = event.target.files[0];
                if (!file) return;
                const reader = new FileReader();
                reader.onload = (e) => {
                    previewImg.src = e.target.result;
                    const [header, base64] = e.target.result.split(',');
                    const mimeType = header.match(/:(.*?);/)[1];
                    brollBackgroundImageBase64 = base64;
                    brollBackgroundImageMimeType = mimeType;
                    uploadArea.classList.add('hidden');
                    previewContainer.classList.remove('hidden');
                };
                reader.readAsDataURL(file);
            });
            removeBtn.addEventListener('click', () => {
                brollBackgroundImageBase64 = null;
                brollBackgroundImageMimeType = null;
                input.value = '';
                uploadArea.classList.remove('hidden');
                previewContainer.classList.add('hidden');
            });
        }
        setupBrollBackgroundImageUpload(brollBackgroundImageInput, brollBackgroundImageUploadArea, brollBackgroundImagePreviewContainer, brollBackgroundImagePreview, brollRemoveBackgroundImageBtn);

        if(brollProductDescInput) brollProductDescInput.addEventListener('input', updateBrollButtonState);

        if(brollRatioSelection) {
            brollRatioSelection.addEventListener('click', (e) => {
                const button = e.target.closest('.ratio-btn-broll');
                if (button) {
                    document.querySelectorAll('#broll-ratio-selection .ratio-btn-broll').forEach(btn => btn.classList.remove('selected'));
                    button.classList.add('selected');
                    selectedBrollRatio = button.dataset.ratio;
                }
            });
        }

        // Handle theme selection
        if(brollThemeOptions) {
            brollThemeOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-theme]');
                if (button) {
                    document.querySelectorAll('#broll-theme-options .theme-btn-broll').forEach(btn => btn.classList.remove('selected'));
                    button.classList.add('selected');
                    selectedBrollTheme = button.dataset.theme;

                    // Show/hide custom theme textarea
                    if (selectedBrollTheme === 'custom') {
                        if (brollCustomThemeContainer) brollCustomThemeContainer.classList.remove('hidden');
                    } else {
                        if (brollCustomThemeContainer) brollCustomThemeContainer.classList.add('hidden');
                    }
                }
            });
        }

        const createInitialBrollPlaceholders = () => {
            if(!brollGrid) return;
            brollGrid.innerHTML = Array.from({length: 20}, () => `
                <div class="result-card card p-4 flex flex-col justify-between animate-pulse">
                    <div><div class="h-6 bg-gray-200 rounded w-3/4 mb-4"></div></div>
                    <div class="mt-4 aspect-video bg-gray-300 rounded-md"></div>
                </div>`).join('');
        };
        createInitialBrollPlaceholders();

        if(brollGenerateBtn) brollGenerateBtn.addEventListener('click', async () => {
            if (brollGenerateBtn.disabled) return;
            brollGenerateBtn.disabled = true;
            brollGenerateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Menganalisa...</span>`;
            if (brollResultsHeader) brollResultsHeader.classList.add('hidden');

            let successCount = 0;
            let attempts = 0;
            const MAX_ATTEMPTS = 3;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                generatedBrollImages = [];
                try {
                    const ideas = await analyzeAndGetPrompts();
                    brollGenerateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Membuat Visual...</span>`;
                    createBrollDynamicCards(ideas);
                    await Promise.allSettled(ideas.map((idea, i) => generateSingleBroll(i + 1, idea.title, idea.prompt)));
                    successCount = generatedBrollImages.length;
                } catch (error) {
                    console.error("Error:", error);
                    if (brollGrid) brollGrid.innerHTML = '';
                }
                attempts++;
            }

            brollGenerateBtn.disabled = false;
            brollGenerateBtn.innerHTML = `<span>Buat Pose Produk</span>`;
            updateBrollButtonState();

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                return;
            }

            // Remove failed cards (empty innerHTML after fail-silent catch)
            if (brollGrid) {
                brollGrid.querySelectorAll('.result-card').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
            }

            if (brollResultsHeader) brollResultsHeader.classList.remove('hidden');
            if (brollResultCount) brollResultCount.textContent = successCount;
        });

        // Download all button event listener
        if (brollDownloadAllBtn) {
            brollDownloadAllBtn.addEventListener('click', async () => {
                if (generatedBrollImages.length === 0) return;

                brollDownloadAllBtn.disabled = true;
                brollDownloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < generatedBrollImages.length; i++) {
                    const img = generatedBrollImages[i];
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }
                }

                brollDownloadAllBtn.disabled = false;
                brollDownloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
            });
        }

        async function analyzeAndGetPrompts() {
             const apiKey = ""; const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

             let systemPrompt;
             if (brollModelImageBase64) {
                 systemPrompt = `You are a professional product photographer's AI assistant. Analyze product image(s), description, and a model's photo. Generate ${selectedCount} distinct, creative, professional shots. If multiple product images are provided (e.g., a shirt and pants), generate prompts that show the model wearing or using ALL products together in a complete outfit/setup. The model MUST naturally interact with or wear ALL the products. ${brollBackgroundImageBase64 ? 'A custom background image is provided - use it as the setting/backdrop for the photos.' : ''} For each, provide a short, descriptive title (in Indonesian) and a detailed prompt (in English) for an AI image generator, emphasizing a high-end commercial aesthetic. Respond ONLY with a valid JSON array of ${selectedCount} objects.`;
             } else {
                 systemPrompt = `You are a professional product photographer's AI assistant. Analyze product image(s) and description. Generate ${selectedCount} distinct, creative, professional product-only shots with NO human models. If multiple products are provided, show them together in creative compositions. ${brollBackgroundImageBase64 ? 'A custom background image is provided - use it as the setting/backdrop for the product photos.' : ''} Focus on creative backgrounds, lighting, and product arrangements. For each, provide a short, descriptive title (in Indonesian) and a detailed prompt (in English) for an AI image generator, emphasizing a high-end commercial aesthetic. Respond ONLY with a valid JSON array of ${selectedCount} objects.`;
             }

             // Get theme value: use selected theme or custom input if "custom" is selected
             let theme = '';
             if (selectedBrollTheme === 'custom') {
                 theme = brollPhotoThemeInput ? brollPhotoThemeInput.value.trim() : '';
             } else if (selectedBrollTheme) {
                 theme = selectedBrollTheme;
             }

             let userQuery = `Product Description: "${brollProductDescInput.value.trim()}" ${theme ? `\nPhoto Theme: "${theme}"` : ''} ${brollBackgroundImageBase64 ? '\nNote: A custom background image is provided for the product photos.' : ''} \nAspect Ratio: "${selectedBrollRatio}"`;

             const parts = [{ text: userQuery }];
             brollProductImages.forEach(img => {
                 parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
             });
             if (brollModelImageBase64) {
                 parts.push({ inlineData: { mimeType: brollModelImageMimeType, data: brollModelImageBase64 } });
             }
             if (brollBackgroundImageBase64) {
                 parts.push({ inlineData: { mimeType: brollBackgroundImageMimeType, data: brollBackgroundImageBase64 } });
             }

             const payload = { contents: [{ parts }], systemInstruction: { parts: [{ text: systemPrompt }] }, generationConfig: { responseMimeType: "application/json", responseSchema: { type: "ARRAY", items: { type: "OBJECT", properties: { "title": { "type": "STRING" }, "prompt": { "type": "STRING" } }, required: ["title", "prompt"] } } } };
             const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
             if (!response.ok) { const err = await response.json(); throw new Error(err.error?.message || response.statusText); }
             const result = await response.json();
             return JSON.parse(result.candidates[0].content.parts[0].text);
        }

        async function generateSingleBroll(id, title, prompt) {
            const card = document.getElementById(`broll-card-${id}`);
            const outputContainer = card.querySelector('.aspect-video');
            try {
                const apiKey = ""; const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const finalPrompt = `${prompt}, elegant, cinematic, professional product photography, dramatic lighting, 8k, photorealistic, aspect ratio ${selectedBrollRatio}`;
                const parts = [{ text: finalPrompt }];

                // Add all product images
                brollProductImages.forEach(img => {
                    parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
                });

                // Add model image if available
                if (brollModelImageBase64) {
                    parts.push({ inlineData: { mimeType: brollModelImageMimeType, data: brollModelImageBase64 } });
                }

                // Add background image if available
                if (brollBackgroundImageBase64) {
                    parts.push({ inlineData: { mimeType: brollBackgroundImageMimeType, data: brollBackgroundImageBase64 } });
                }

                // Use selected aspect ratio directly
                const payload = { contents: [{ parts }], generationConfig: { responseModalities: ['TEXT', 'IMAGE'], imageConfig: { aspectRatio: selectedBrollRatio } } };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error("No image data from API.");
                const imageUrl = `data:image/png;base64,${base64Data}`;
                const safeTitle = title.replace(/[^a-z0-9]/gi, '_').toLowerCase();

                // Add to generated images array for download all functionality
                generatedBrollImages.push({
                    url: imageUrl,
                    filename: `affiliatego_${id}_${safeTitle}.png`
                });

                // Store generation parameters for regeneration
                if (!window.brollGenerationParams) window.brollGenerationParams = [];
                window.brollGenerationParams[id - 1] = {
                    prompt: prompt,
                    title: title,
                    productImages: brollProductImages,
                    modelImage: brollModelImageBase64 ? { base64: brollModelImageBase64, mimeType: brollModelImageMimeType } : null,
                    ratio: selectedBrollRatio
                };

                outputContainer.innerHTML = `<div class="relative w-full h-full group"><img src="${imageUrl}" class="w-full h-full object-cover rounded-md" alt="${title}"><div class="absolute bottom-2 right-2 flex flex-wrap gap-2 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity"><button data-action="broll-preview" data-image-url="${imageUrl}" class="action-btn bg-blue-500 text-white p-2 rounded-full hover:bg-blue-600" title="Preview"><i class="fas fa-search-plus"></i></button><button data-action="broll-edit" data-index="${id - 1}" data-prompt="${prompt.replace(/"/g, '&quot;')}" class="action-btn bg-purple-500 text-white p-2 rounded-full hover:bg-purple-600" title="Edit Prompt"><i class="fas fa-edit"></i></button><button data-action="broll-regenerate" data-index="${id - 1}" class="action-btn bg-green-500 text-white p-2 rounded-full hover:bg-green-600" title="Regenerate"><i class="fas fa-sync-alt"></i></button><button data-action="broll-video" data-image-url="${imageUrl}" data-title="${title.replace(/"/g, '&quot;')}" class="action-btn bg-fuchsia-500 text-white p-2 rounded-full hover:bg-fuchsia-600" title="Buat Prompt Video"><i class="fas fa-film"></i></button><button data-action="broll-caption" data-image-url="${imageUrl}" data-title="${title.replace(/"/g, '&quot;')}" class="action-btn bg-rose-500 text-white p-2 rounded-full hover:bg-rose-600" title="Buat Caption"><i class="fas fa-pencil-alt"></i></button><button data-action="broll-download" data-image-url="${imageUrl}" data-filename="affiliatego_${id}_${safeTitle}.png" class="action-btn bg-cyan-600 text-white p-2 rounded-full hover:bg-cyan-700" title="Unduh"><i class="fas fa-download"></i></button></div></div>`;
            } catch (error) {
                console.error(`Error generating broll ${id}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        const createBrollDynamicCards = (prompts) => {
            brollGrid.innerHTML = prompts.map((p, i) => `
                <div id="broll-card-${i + 1}" class="result-card card p-4 flex flex-col justify-between">
                    <div><h3 class="text-lg font-semibold text-gray-800 mb-4">${p.title}</h3></div>
                    <div class="aspect-video bg-gray-100 rounded-md flex items-center justify-center"><div class="loader"></div></div>
                </div>`).join('');
        };
        
        if(brollGenerateDescBtn) brollGenerateDescBtn.addEventListener('click', async () => {
            const originalButtonText = brollGenerateDescBtn.innerHTML;
            brollGenerateDescBtn.innerHTML = `<div class="loader !w-4 !h-4 !border-2"></div>`;
            brollGenerateDescBtn.disabled = true;
            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `You are a professional copywriter. Analyze the product(s) in the image(s) and write a concise, compelling, and SEO-friendly product description in Indonesian. Highlight key features and potential benefits for customers. Keep it under 500 characters.`;
                const parts = [ { text: "Buatkan deskripsi produk untuk gambar ini." }];
                if (brollProductImages.length > 0) {
                     parts.push({ inlineData: { mimeType: brollProductImages[0].mimeType, data: brollProductImages[0].base64 } });
                }
                const payload = {
                    contents: [{ parts: parts }],
                    systemInstruction: { parts: [{ text: systemPrompt }] }
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
                const result = await response.json();
                const description = result.candidates[0].content.parts[0].text;
                brollProductDescInput.value = description.trim();
                updateBrollButtonState();
            } catch(error) {
                console.error("Error generating description:", error);
                brollProductDescInput.value = "Gagal membuat deskripsi. Coba lagi.";
            } finally {
                brollGenerateDescBtn.innerHTML = originalButtonText;
                brollGenerateDescBtn.disabled = false;
            }
        });
        window.handleAffiliateCaption = async (imageUrl, title) => {
            try {
                // Tampilkan loading modal
                const modal = document.createElement('div');
                modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4 overflow-y-auto';
                modal.style.WebkitOverflowScrolling = 'touch'; // Enable smooth scrolling on iOS
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full my-8" onclick="event.stopPropagation()">
                        <h3 class="text-xl font-bold mb-4 text-gray-800">
                            <i class="fas fa-pencil-alt text-rose-500 mr-2"></i>Membuat Caption...
                        </h3>
                        <div class="flex items-center justify-center py-8">
                            <div class="loader !border-l-rose-500"></div>
                        </div>
                    </div>
                `;
                document.body.appendChild(modal);

                // Close modal on backdrop click
                modal.addEventListener('click', (e) => {
                    if (e.target === modal) {
                        modal.remove();
                    }
                });

                // Konversi imageUrl ke base64
                const response = await fetch(imageUrl);
                const blob = await response.blob();
                const reader = new FileReader();
                const base64Promise = new Promise((resolve) => {
                    reader.onloadend = () => {
                        const base64 = reader.result.split(',')[1];
                        resolve(base64);
                    };
                    reader.readAsDataURL(blob);
                });
                const base64Data = await base64Promise;

                // Call Gemini API untuk generate caption
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const systemPrompt = `You are an expert social media copywriter and content strategist. Analyze this product image and create 3 engaging, conversion-focused Instagram captions in Indonesian.

Each caption should:
1. Hook attention in the first line
2. Highlight product benefits or create desire
3. Include relevant emojis naturally
4. End with a clear call-to-action (CTA)
5. Be optimized for engagement and sales
6. Have different tones: Professional, Casual/Fun, and Inspirational

Format your response as JSON with this structure:
{
  "professional": "caption text",
  "casual": "caption text",
  "inspirational": "caption text"
}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: `Analyze this product image titled "${title}" and create 3 engaging Instagram captions in Indonesian for different audiences.` },
                            { inlineData: { mimeType: 'image/png', data: base64Data } }
                        ]
                    }],
                    systemInstruction: { parts: [{ text: systemPrompt }] },
                    generationConfig: {
                        responseMimeType: "application/json"
                    }
                };

                const apiResponse = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!apiResponse.ok) {
                    const errorData = await apiResponse.json();
                    throw new Error(errorData.error?.message || 'API request failed');
                }

                const result = await apiResponse.json();
                const captionText = result.candidates[0].content.parts[0].text;
                const captions = JSON.parse(captionText);

                // Unicode escape sequences untuk obfuscation safety
                const targetEmoji = '\uD83C\uDFAF'; // 🎯
                const smileEmoji = '\uD83D\uDE0A'; // 😊
                const sparkleEmoji = '\u2728'; // ✨

                // Update modal dengan hasil caption
                const modalContent = modal.querySelector('.bg-white');
                modalContent.innerHTML = `
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-xl font-bold text-gray-800">
                            <i class="fas fa-pencil-alt text-rose-500 mr-2"></i>Caption Instagram
                        </h3>
                        <button data-action="close-modal" class="text-gray-500 hover:text-gray-700 text-2xl leading-none" title="Tutup">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>

                    <div class="space-y-4 mb-6 max-h-[60vh] overflow-y-auto" style="-webkit-overflow-scrolling: touch;">
                        ${Object.entries(captions).map(([tone, caption]) => `
                            <div class="border border-gray-200 rounded-lg p-4 bg-gray-50">
                                <div class="flex items-center justify-between mb-2">
                                    <label class="text-sm font-bold text-gray-700 uppercase">${tone === 'professional' ? targetEmoji + ' Profesional' : tone === 'casual' ? smileEmoji + ' Casual & Fun' : sparkleEmoji + ' Inspiratif'}</label>
                                    <button data-action="copy-caption" data-caption="${caption.replace(/"/g, '&quot;')}"
                                        class="caption-copy-btn text-xs bg-rose-500 hover:bg-rose-600 text-white px-3 py-1 rounded-full transition-colors flex-shrink-0">
                                        <i class="fas fa-copy mr-1"></i>Salin
                                    </button>
                                </div>
                                <p class="text-sm text-gray-800 whitespace-pre-wrap leading-relaxed">${caption}</p>
                            </div>
                        `).join('')}
                    </div>

                    <div class="bg-blue-50 border border-blue-200 rounded-lg p-3 mb-4">
                        <p class="text-xs text-blue-800">
                            <i class="fas fa-info-circle mr-1"></i>
                            <strong>Tips:</strong> Pilih caption yang sesuai dengan brand voice Anda!
                        </p>
                    </div>

                    <button data-action="close-modal"
                        class="w-full bg-gray-500 hover:bg-gray-600 text-white font-bold py-3 px-4 rounded-lg transition-colors">
                        Tutup
                    </button>
                `;

                // Add event delegation for copy and close buttons (works on desktop & mobile)
                modalContent.addEventListener('click', async (e) => {
                    e.stopPropagation();

                    const copyBtn = e.target.closest('[data-action="copy-caption"]');
                    const closeBtn = e.target.closest('[data-action="close-modal"]');

                    if (copyBtn) {
                        const captionText = copyBtn.dataset.caption;
                        try {
                            await navigator.clipboard.writeText(captionText);
                            const originalHTML = copyBtn.innerHTML;
                            copyBtn.innerHTML = '<i class="fas fa-check mr-1"></i>Tersalin!';
                            copyBtn.classList.remove('bg-rose-500');
                            copyBtn.classList.add('bg-green-500');
                            setTimeout(() => {
                                copyBtn.innerHTML = originalHTML;
                                copyBtn.classList.remove('bg-green-500');
                                copyBtn.classList.add('bg-rose-500');
                            }, 2000);
                        } catch (err) {
                            console.error('Failed to copy:', err);
                            // Fallback for older browsers
                            alert('Caption berhasil dipilih. Salin dengan Ctrl+C atau Command+C');
                        }
                    } else if (closeBtn) {
                        modal.remove();
                    }
                });

            } catch (error) {
                console.error('Error generating caption:', error);

                // Tampilkan error modal
                const errorModal = document.createElement('div');
                errorModal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';
                errorModal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-md w-full">
                        <h3 class="text-xl font-bold mb-4 text-red-600">
                            <i class="fas fa-exclamation-triangle mr-2"></i>Error
                        </h3>
                        <p class="text-gray-700 mb-4">${error.message}</p>
                        <button onclick="this.closest('.fixed').remove()"
                            class="w-full bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                            Tutup
                        </button>
                    </div>
                `;
                document.body.appendChild(errorModal);

                // Remove loading modal if it exists
                const existingModal = document.querySelector('.fixed.inset-0');
                if (existingModal && existingModal !== errorModal) {
                    existingModal.remove();
                }
            }
        };

        window.handleAffiliateVideoPrompt = async (imageUrl, title) => {
            try {
                // Tampilkan loading
                const modal = document.createElement('div');
                modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full max-h-[80vh] overflow-y-auto">
                        <h3 class="text-xl font-bold mb-4 text-gray-800">
                            <i class="fas fa-film text-fuchsia-500 mr-2"></i>Membuat Prompt Video...
                        </h3>
                        <div class="flex items-center justify-center py-8">
                            <div class="loader !border-l-fuchsia-500"></div>
                        </div>
                    </div>
                `;
                document.body.appendChild(modal);

                // Konversi imageUrl ke base64
                const response = await fetch(imageUrl);
                const blob = await response.blob();
                const reader = new FileReader();
                const base64Promise = new Promise((resolve) => {
                    reader.onloadend = () => {
                        const base64 = reader.result.split(',')[1];
                        resolve(base64);
                    };
                    reader.readAsDataURL(blob);
                });
                const base64Data = await base64Promise;

                // Call Gemini API untuk generate prompt
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const systemPrompt = `You are an expert video prompt engineer. Analyze this product image and create a detailed, cinematic prompt for an AI image-to-video generator (like Runway, Pika, or Stable Video Diffusion).

The prompt should:
1. Describe the product's visual appearance and key features
2. Suggest natural, engaging camera movements (zoom in, pan, rotation, etc.)
3. Include dynamic elements (floating particles, light effects, product rotation)
4. Specify lighting and atmosphere
5. Be optimized for text-to-video AI models
6. Be in English for maximum compatibility
7. Keep it under 200 words but highly detailed

Output ONLY the video prompt, nothing else.`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: `Create a cinematic video prompt for this product image titled "${title}". Make it dynamic and engaging for image-to-video AI.` },
                            { inlineData: { mimeType: 'image/png', data: base64Data } }
                        ]
                    }],
                    systemInstruction: { parts: [{ text: systemPrompt }] }
                };

                const apiResponse = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!apiResponse.ok) {
                    const errorData = await apiResponse.json();
                    throw new Error(errorData.error?.message || 'Failed to generate prompt');
                }

                const result = await apiResponse.json();
                const videoPrompt = result.candidates[0].content.parts[0].text.trim();

                // Tampilkan hasil
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full max-h-[80vh] overflow-y-auto">
                        <div class="flex items-center justify-between mb-4">
                            <h3 class="text-xl font-bold text-gray-800">
                                <i class="fas fa-film text-fuchsia-500 mr-2"></i>Prompt Video untuk: ${title}
                            </h3>
                            <button onclick="this.closest('.fixed').remove()" class="text-gray-500 hover:text-gray-700">
                                <i class="fas fa-times text-xl"></i>
                            </button>
                        </div>

                        <div class="mb-4">
                            <img src="${imageUrl}" alt="${title}" class="w-full rounded-lg mb-3 max-h-64 object-contain bg-gray-100">
                        </div>

                        <div class="bg-gray-50 border-2 border-gray-200 rounded-lg p-4 mb-4">
                            <div class="flex items-center justify-between mb-2">
                                <label class="text-sm font-semibold text-gray-700">Video Prompt:</label>
                                <button id="copyPromptBtn" class="text-xs bg-fuchsia-500 hover:bg-fuchsia-600 text-white px-3 py-1 rounded-full transition-colors">
                                    <i class="fas fa-copy mr-1"></i>Copy
                                </button>
                            </div>
                            <textarea id="videoPromptText" rows="8" readonly class="w-full p-3 border border-gray-300 rounded-lg bg-white text-gray-800 text-sm font-mono resize-none focus:outline-none focus:ring-2 focus:ring-fuchsia-500">${videoPrompt}</textarea>
                        </div>

                        <div class="bg-blue-50 border border-blue-200 rounded-lg p-3 mb-4">
                            <p class="text-xs text-blue-800">
                                <i class="fas fa-info-circle mr-1"></i>
                                <strong>Cara Pakai:</strong> Copy prompt ini dan gunakan di platform Image-to-Video seperti Runway, Pika Labs, Stable Video Diffusion, atau yang lainnya. Upload gambar produk Anda dan paste prompt ini untuk hasil terbaik.
                            </p>
                        </div>

                        <div class="flex gap-2">
                            <button onclick="this.closest('.fixed').remove()" class="flex-1 bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                                Tutup
                            </button>
                        </div>
                    </div>
                `;

                // Add copy to clipboard functionality
                const copyBtn = modal.querySelector('#copyPromptBtn');
                const promptTextarea = modal.querySelector('#videoPromptText');

                copyBtn.addEventListener('click', () => {
                    promptTextarea.select();
                    document.execCommand('copy');
                    copyBtn.innerHTML = '<i class="fas fa-check mr-1"></i>Copied!';
                    copyBtn.classList.remove('bg-fuchsia-500', 'hover:bg-fuchsia-600');
                    copyBtn.classList.add('bg-green-500');
                    setTimeout(() => {
                        copyBtn.innerHTML = '<i class="fas fa-copy mr-1"></i>Copy';
                        copyBtn.classList.remove('bg-green-500');
                        copyBtn.classList.add('bg-fuchsia-500', 'hover:bg-fuchsia-600');
                    }, 2000);
                });

            } catch (error) {
                console.error('Error generating video prompt:', error);
                // Tampilkan error
                const errorModal = document.createElement('div');
                errorModal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';
                errorModal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-md w-full">
                        <h3 class="text-xl font-bold mb-4 text-red-600">
                            <i class="fas fa-exclamation-triangle mr-2"></i>Error
                        </h3>
                        <p class="text-gray-700 mb-4">${error.message}</p>
                        <button onclick="this.closest('.fixed').remove()" class="w-full bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded-lg">
                            Tutup
                        </button>
                    </div>
                `;
                document.body.appendChild(errorModal);

                // Remove existing loading modal if exists
                const existingModal = document.querySelector('.fixed.inset-0');
                if (existingModal && existingModal !== errorModal) {
                    existingModal.remove();
                }
            }
        };

        // Event delegation for broll grid action buttons (works on desktop & mobile)
        if (brollGrid) {
            brollGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const imageUrl = actionBtn.dataset.imageUrl;
                const title = actionBtn.dataset.title;
                const filename = actionBtn.dataset.filename;
                const index = actionBtn.dataset.index;

                if (action === 'broll-preview' && imageUrl) {
                    showPreviewModal(imageUrl);
                } else if (action === 'broll-edit' && index !== undefined) {
                    handleEditBroll(parseInt(index), actionBtn.dataset.prompt);
                } else if (action === 'broll-regenerate' && index !== undefined) {
                    await handleRegenerateBroll(parseInt(index));
                } else if (action === 'broll-video' && imageUrl && title) {
                    window.handleAffiliateVideoPrompt(imageUrl, title);
                } else if (action === 'broll-caption' && imageUrl && title) {
                    window.handleAffiliateCaption(imageUrl, title);
                } else if (action === 'broll-download' && imageUrl && filename) {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageUrl, filename);
                    }
                }
            });

            // Handler functions for edit and regenerate
            function handleEditBroll(index, currentPrompt) {
                const modal = document.createElement('div');
                modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full">
                        <h3 class="text-xl font-bold mb-4 text-gray-800">
                            <i class="fas fa-edit text-purple-500 mr-2"></i>Edit Scene Prompt
                        </h3>
                        <div class="mb-4">
                            <label class="block text-sm font-semibold text-gray-700 mb-2">Scene Description:</label>
                            <textarea id="edit-broll-input" rows="4" class="w-full p-3 border-2 border-gray-300 rounded-lg focus:border-purple-500 focus:outline-none resize-none" placeholder="Contoh: A serene mountain landscape with a river flowing through, vibrant flowers in foreground, golden hour lighting...">${currentPrompt}</textarea>
                            <p class="text-xs text-gray-500 mt-2">
                                <i class="fas fa-info-circle mr-1"></i>
                                Edit deskripsi scene/style untuk hasil yang berbeda
                            </p>
                        </div>
                        <div class="flex gap-2">
                            <button onclick="this.closest('.fixed').remove()" class="flex-1 bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                                Batal
                            </button>
                            <button id="confirm-edit-broll-btn" class="flex-1 bg-purple-500 hover:bg-purple-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                                <i class="fas fa-check mr-2"></i>Generate Ulang
                            </button>
                        </div>
                    </div>
                `;
                document.body.appendChild(modal);

                const confirmBtn = modal.querySelector('#confirm-edit-broll-btn');
                const promptInput = modal.querySelector('#edit-broll-input');

                confirmBtn.addEventListener('click', async () => {
                    const newPrompt = promptInput.value.trim();
                    if (!newPrompt) return;

                    modal.remove();

                    // Update the prompt for this image
                    if (window.brollGenerationParams && window.brollGenerationParams[index]) {
                        window.brollGenerationParams[index].prompt = newPrompt;
                    }

                    await handleRegenerateBroll(index);
                });
            }

            async function handleRegenerateBroll(index) {
                const targetCard = document.getElementById(`broll-card-${index + 1}`);
                if (!targetCard) return;

                const params = window.brollGenerationParams ? window.brollGenerationParams[index] : null;
                if (!params) return;

                // Show loading state
                const outputContainer = targetCard.querySelector('.aspect-video, .relative');
                if (!outputContainer) return;

                outputContainer.innerHTML = `
                    <div class="flex flex-col items-center justify-center h-full min-h-[200px]">
                        <div class="loader !border-l-purple-500"></div>
                        <p class="mt-4 text-sm text-gray-600">Regenerating...</p>
                    </div>
                `;

                try {
                    const apiKey = "";
                    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                    // Add variation to ensure different results
                    const variations = [
                        ', slightly different angle',
                        ', alternative perspective',
                        ', different composition',
                        ', varied lighting mood',
                        ', alternative styling',
                        ', different camera angle'
                    ];
                    const randomVariation = variations[Math.floor(Math.random() * variations.length)];
                    const variedPrompt = params.prompt + randomVariation;

                    const parts = [{ text: variedPrompt }];

                    // Add all product images
                    params.productImages.forEach(img => {
                        parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
                    });

                    // Add model image if available
                    if (params.modelImage) {
                        parts.push({ inlineData: { mimeType: params.modelImage.mimeType, data: params.modelImage.base64 } });
                    }

                    const payload = { contents: [{ parts }], generationConfig: { responseModalities: ['IMAGE'], imageConfig: { aspectRatio: params.ratio } } };
                    const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });

                    if (!response.ok) throw new Error(`API error: ${response.statusText}`);

                    const result = await response.json();
                    const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                    if (base64Data) {
                        const imageUrl = `data:image/png;base64,${base64Data}`;
                        const safeTitle = params.title.replace(/[^a-z0-9]/gi, '_').toLowerCase();
                        const safePrompt = params.prompt.replace(/"/g, '&quot;');

                        // Update stored image in generatedBrollImages
                        if (generatedBrollImages[index]) {
                            generatedBrollImages[index].url = imageUrl;
                        }

                        outputContainer.innerHTML = `<div class="relative w-full h-full group"><img src="${imageUrl}" class="w-full h-full object-cover rounded-md" alt="${params.title}"><div class="absolute bottom-2 right-2 flex flex-wrap gap-2 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity"><button data-action="broll-preview" data-image-url="${imageUrl}" class="action-btn bg-blue-500 text-white p-2 rounded-full hover:bg-blue-600" title="Preview"><i class="fas fa-search-plus"></i></button><button data-action="broll-edit" data-index="${index}" data-prompt="${safePrompt}" class="action-btn bg-purple-500 text-white p-2 rounded-full hover:bg-purple-600" title="Edit Prompt"><i class="fas fa-edit"></i></button><button data-action="broll-regenerate" data-index="${index}" class="action-btn bg-green-500 text-white p-2 rounded-full hover:bg-green-600" title="Regenerate"><i class="fas fa-sync-alt"></i></button><button data-action="broll-video" data-image-url="${imageUrl}" data-title="${params.title.replace(/"/g, '&quot;')}" class="action-btn bg-fuchsia-500 text-white p-2 rounded-full hover:bg-fuchsia-600" title="Buat Prompt Video"><i class="fas fa-film"></i></button><button data-action="broll-caption" data-image-url="${imageUrl}" data-title="${params.title.replace(/"/g, '&quot;')}" class="action-btn bg-rose-500 text-white p-2 rounded-full hover:bg-rose-600" title="Buat Caption"><i class="fas fa-pencil-alt"></i></button><button data-action="broll-download" data-image-url="${imageUrl}" data-filename="affiliatego_${index + 1}_${safeTitle}.png" class="action-btn bg-cyan-600 text-white p-2 rounded-full hover:bg-cyan-700" title="Unduh"><i class="fas fa-download"></i></button></div></div>`;
                    } else {
                        throw new Error('Failed to generate image');
                    }
                } catch (error) {
                    console.error('Regenerate error:', error);
                    outputContainer.innerHTML = `
                        <div class="flex flex-col items-center justify-center h-full min-h-[200px] text-red-600">
                            <i class="fas fa-exclamation-triangle text-3xl mb-2"></i>
                            <p class="text-sm">Gagal regenerate</p>
                            <button onclick="location.reload()" class="mt-2 text-xs bg-red-500 text-white px-3 py-1 rounded-full hover:bg-red-600">
                                Refresh
                            </button>
                        </div>
                    `;
                }
            }

        }
    })();

    // --- FOTO PRODUK AFFILIATE + HARGA (TEMA HARGA) LOGIC ---
    (function() {
        // --- Tema Harga (Foto Produk + Harga) Logic ---
        const hrgImageInput = document.getElementById('hrg-image-input');
        const hrgImageUploadArea = document.getElementById('hrg-image-upload-area');
        const hrgImagePreviewContainer = document.getElementById('hrg-image-preview-container');
        const hrgImagePreview = document.getElementById('hrg-image-preview');
        const hrgRemoveImageBtn = document.getElementById('hrg-remove-image-btn');
        const hrgModelImageInput = document.getElementById('hrg-model-image-input');
        const hrgModelImageUploadArea = document.getElementById('hrg-model-image-upload-area');
        const hrgModelImagePreviewContainer = document.getElementById('hrg-model-image-preview-container');
        const hrgModelImagePreview = document.getElementById('hrg-model-image-preview');
        const hrgRemoveModelImageBtn = document.getElementById('hrg-remove-model-image-btn');
        const hrgBackgroundImageInput = document.getElementById('hrg-background-image-input');
        const hrgBackgroundImageUploadArea = document.getElementById('hrg-background-image-upload-area');
        const hrgBackgroundImagePreviewContainer = document.getElementById('hrg-background-image-preview-container');
        const hrgBackgroundImagePreview = document.getElementById('hrg-background-image-preview');
        const hrgRemoveBackgroundImageBtn = document.getElementById('hrg-remove-background-image-btn');
        const hrgProductDescInput = document.getElementById('hrg-product-desc-input');
        const hrgGenerateBtn = document.getElementById('hrg-generate-btn');
        const hrgGrid = document.getElementById('hrg-b-roll-grid');
        const hrgGenerateDescBtn = document.getElementById('hrg-generate-desc-btn');
        const hrgResultsHeader = document.getElementById('hrg-results-header');
        const hrgResultCount = document.getElementById('hrg-result-count');
        const hrgDownloadAllBtn = document.getElementById('hrg-download-all-btn');
        let hrgProductImages = [];
        let hrgModelImageBase64 = null, hrgModelImageMimeType = null;
        let hrgBackgroundImageBase64 = null, hrgBackgroundImageMimeType = null;
        const hrgRatioSelection = document.getElementById('hrg-ratio-selection');
        let selectedHrgRatio = '16:9';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedHrgImages = [];

        // Bangun instruksi anotasi panah + harga untuk AI (Tema Harga)
        function buildHrgPriceInstruction(images) {
            const lines = images.map((img, i) => `Product ${i + 1} price: "${img.price ? img.price : '(no price)'}"`).join('; ');
            return `IMPORTANT PRICE ANNOTATION (aesthetic): The product images are given in order. For each product, add an elegant modern price tag connected to the product by a thin, gently curved pointer line that ends with a small dot on the product. Style the price tag as a clean rounded-corner pill/label with a soft semi-transparent background, subtle drop shadow, and crisp high-contrast sans-serif typography so the price is perfectly legible; keep a cohesive premium e-commerce look with one consistent color accent and identical label style for all products. Prices in order: ${lines}. Position each tag in nearby empty/negative space so it never overlaps or covers the product, keep tags small and balanced in the composition. Any product marked "(no price)" gets NO tag and NO arrow.`;
        }

        // Count selection (1-10), default 4
        const hrgCountSel = document.getElementById('hrg-count-selection-grid');
        if (hrgCountSel) {
            hrgCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                hrgCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }
        
        function updateHrgButtonState() {
            if(hrgGenerateBtn) hrgGenerateBtn.disabled = hrgProductImages.length === 0 || !hrgProductDescInput.value.trim();
            if(hrgGenerateDescBtn) hrgGenerateDescBtn.disabled = hrgProductImages.length === 0;
        }

        // Handle multiple product images
        function handleHrgProductFiles(files) {
            if (!files || files.length === 0) return;

            hrgImageUploadArea.classList.add('hidden');
            hrgImagePreviewContainer.classList.remove('hidden');

            let filesToProcess = files.length;

            Array.from(files).forEach((file) => {
                const reader = new FileReader();
                reader.onload = (e) => {
                    const [header, base64] = e.target.result.split(',');
                    const mimeType = header.match(/:(.*?);/)[1];
                    const imageData = {
                        id: Date.now() + Math.random(),
                        base64: base64,
                        mimeType: mimeType,
                        price: ''
                    };

                    hrgProductImages.push(imageData);

                    const previewWrapper = document.createElement('div');
                    previewWrapper.className = 'relative group';
                    previewWrapper.innerHTML = `
                        <img src="${e.target.result}" alt="Pratinjau Produk" class="rounded-lg w-full h-24 object-cover">
                        <button data-id="${imageData.id}" class="absolute top-1 right-1 bg-red-500 text-white rounded-full p-1 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity focus:opacity-100">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                        </button>
                        <input type="text" class="hrg-price-input mt-1 w-full text-xs p-1.5 border border-gray-300 rounded focus:ring-2 focus:ring-green-500 focus:border-green-500" data-i18n-placeholder="hrg.price-placeholder" placeholder="Harga (mis. Rp50.000)">
                    `;

                    hrgImagePreviewContainer.appendChild(previewWrapper);

                    const priceInput = previewWrapper.querySelector('.hrg-price-input');
                    if (priceInput) {
                        priceInput.addEventListener('input', (ev) => {
                            const target = hrgProductImages.find(im => im.id === imageData.id);
                            if (target) target.price = ev.target.value.trim();
                        });
                    }

                    previewWrapper.querySelector('button').addEventListener('click', (btnEvent) => {
                        const idToRemove = parseFloat(btnEvent.currentTarget.getAttribute('data-id'));
                        hrgProductImages = hrgProductImages.filter(img => img.id !== idToRemove);
                        previewWrapper.remove();
                        if (hrgProductImages.length === 0) {
                            hrgImageUploadArea.classList.remove('hidden');
                            hrgImagePreviewContainer.classList.add('hidden');
                            const addButton = document.getElementById('hrg-add-more-button');
                            if (addButton) addButton.remove();
                        }
                        updateHrgButtonState();
                    });

                    updateHrgButtonState();

                    filesToProcess--;
                    if (filesToProcess === 0) {
                        manageAddMoreButton();
                    }
                };
                reader.readAsDataURL(file);
            });
        }

        function manageAddMoreButton() {
            const existingButton = document.getElementById('hrg-add-more-button');
            if (existingButton) {
                existingButton.remove();
            }

            const addButtonWrapper = document.createElement('div');
            addButtonWrapper.id = 'hrg-add-more-button';
            addButtonWrapper.className = 'relative group cursor-pointer';
            addButtonWrapper.innerHTML = `
                <div class="rounded-lg w-full h-24 border-2 border-dashed border-gray-400 hover:border-cyan-500 bg-gray-50 hover:bg-cyan-50 transition-colors flex items-center justify-center">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-gray-400 group-hover:text-cyan-500" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path></svg>
                </div>
            `;

            addButtonWrapper.addEventListener('click', () => {
                hrgImageInput.click();
            });

            hrgImagePreviewContainer.appendChild(addButtonWrapper);
        }

        if (hrgImageInput) {
            hrgImageInput.addEventListener('change', (event) => {
                handleHrgProductFiles(event.target.files);
                event.target.value = null;
            });
        }

        // Setup model image upload (single image)
        function setupHrgModelImageUpload(input, uploadArea, previewContainer, previewImg, removeBtn) {
            if (!input) return;
            input.addEventListener('change', (event) => {
                const file = event.target.files[0];
                if (!file) return;
                const reader = new FileReader();
                reader.onload = (e) => {
                    previewImg.src = e.target.result;
                    const [header, base64] = e.target.result.split(',');
                    const mimeType = header.match(/:(.*?);/)[1];
                    hrgModelImageBase64 = base64;
                    hrgModelImageMimeType = mimeType;
                    uploadArea.classList.add('hidden');
                    previewContainer.classList.remove('hidden');
                };
                reader.readAsDataURL(file);
            });
            removeBtn.addEventListener('click', () => {
                hrgModelImageBase64 = null;
                hrgModelImageMimeType = null;
                input.value = '';
                uploadArea.classList.remove('hidden');
                previewContainer.classList.add('hidden');
            });
        }
        setupHrgModelImageUpload(hrgModelImageInput, hrgModelImageUploadArea, hrgModelImagePreviewContainer, hrgModelImagePreview, hrgRemoveModelImageBtn);

        // Setup background image upload (single image)
        function setupHrgBackgroundImageUpload(input, uploadArea, previewContainer, previewImg, removeBtn) {
            if (!input) return;
            input.addEventListener('change', (event) => {
                const file = event.target.files[0];
                if (!file) return;
                const reader = new FileReader();
                reader.onload = (e) => {
                    previewImg.src = e.target.result;
                    const [header, base64] = e.target.result.split(',');
                    const mimeType = header.match(/:(.*?);/)[1];
                    hrgBackgroundImageBase64 = base64;
                    hrgBackgroundImageMimeType = mimeType;
                    uploadArea.classList.add('hidden');
                    previewContainer.classList.remove('hidden');
                };
                reader.readAsDataURL(file);
            });
            removeBtn.addEventListener('click', () => {
                hrgBackgroundImageBase64 = null;
                hrgBackgroundImageMimeType = null;
                input.value = '';
                uploadArea.classList.remove('hidden');
                previewContainer.classList.add('hidden');
            });
        }
        setupHrgBackgroundImageUpload(hrgBackgroundImageInput, hrgBackgroundImageUploadArea, hrgBackgroundImagePreviewContainer, hrgBackgroundImagePreview, hrgRemoveBackgroundImageBtn);

        if(hrgProductDescInput) hrgProductDescInput.addEventListener('input', updateHrgButtonState);

        if(hrgRatioSelection) {
            hrgRatioSelection.addEventListener('click', (e) => {
                const button = e.target.closest('.ratio-btn-broll');
                if (button) {
                    document.querySelectorAll('#hrg-ratio-selection .ratio-btn-broll').forEach(btn => btn.classList.remove('selected'));
                    button.classList.add('selected');
                    selectedHrgRatio = button.dataset.ratio;
                }
            });
        }

        const createInitialHrgPlaceholders = () => {
            if(!hrgGrid) return;
            hrgGrid.innerHTML = Array.from({length: 20}, () => `
                <div class="result-card card p-4 flex flex-col justify-between animate-pulse">
                    <div><div class="h-6 bg-gray-200 rounded w-3/4 mb-4"></div></div>
                    <div class="mt-4 aspect-video bg-gray-300 rounded-md"></div>
                </div>`).join('');
        };
        createInitialHrgPlaceholders();

        if(hrgGenerateBtn) hrgGenerateBtn.addEventListener('click', async () => {
            if (hrgGenerateBtn.disabled) return;
            hrgGenerateBtn.disabled = true;
            hrgGenerateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Menganalisa...</span>`;
            if (hrgResultsHeader) hrgResultsHeader.classList.add('hidden');

            let successCount = 0;
            let attempts = 0;
            const MAX_ATTEMPTS = 3;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                generatedHrgImages = [];
                try {
                    const ideas = await analyzeAndGetPrompts();
                    hrgGenerateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Membuat Visual...</span>`;
                    createHrgDynamicCards(ideas);
                    await Promise.allSettled(ideas.map((idea, i) => generateSingleHrg(i + 1, idea.title, idea.prompt)));
                    successCount = generatedHrgImages.length;
                } catch (error) {
                    console.error("Error:", error);
                    if (hrgGrid) hrgGrid.innerHTML = '';
                }
                attempts++;
            }

            hrgGenerateBtn.disabled = false;
            hrgGenerateBtn.innerHTML = `<span>Buat Pose Produk</span>`;
            updateHrgButtonState();

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                return;
            }

            // Remove failed cards (empty innerHTML after fail-silent catch)
            if (hrgGrid) {
                hrgGrid.querySelectorAll('.result-card').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
            }

            if (hrgResultsHeader) hrgResultsHeader.classList.remove('hidden');
            if (hrgResultCount) hrgResultCount.textContent = successCount;
        });

        // Download all button event listener
        if (hrgDownloadAllBtn) {
            hrgDownloadAllBtn.addEventListener('click', async () => {
                if (generatedHrgImages.length === 0) return;

                hrgDownloadAllBtn.disabled = true;
                hrgDownloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < generatedHrgImages.length; i++) {
                    const img = generatedHrgImages[i];
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }
                }

                hrgDownloadAllBtn.disabled = false;
                hrgDownloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
            });
        }

        async function analyzeAndGetPrompts() {
             const apiKey = ""; const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

             let systemPrompt;
             if (hrgModelImageBase64) {
                 systemPrompt = `You are a professional product photographer's AI assistant. Analyze product image(s), description, and a model's photo. Generate ${selectedCount} distinct, creative, professional shots. If multiple product images are provided (e.g., a shirt and pants), generate prompts that show the model wearing or using ALL products together in a complete outfit/setup. The model MUST naturally interact with or wear ALL the products. ${hrgBackgroundImageBase64 ? 'A custom background image is provided - use it as the setting/backdrop for the photos.' : ''} For each, provide a short, descriptive title (in Indonesian) and a detailed prompt (in English) for an AI image generator, emphasizing a high-end commercial aesthetic. Respond ONLY with a valid JSON array of ${selectedCount} objects.`;
             } else {
                 systemPrompt = `You are a professional product photographer's AI assistant. Analyze product image(s) and description. Generate ${selectedCount} distinct, creative, professional product-only shots with NO human models. If multiple products are provided, show them together in creative compositions. ${hrgBackgroundImageBase64 ? 'A custom background image is provided - use it as the setting/backdrop for the product photos.' : ''} Focus on creative backgrounds, lighting, and product arrangements. For each, provide a short, descriptive title (in Indonesian) and a detailed prompt (in English) for an AI image generator, emphasizing a high-end commercial aesthetic. Respond ONLY with a valid JSON array of ${selectedCount} objects.`;
             }

             let userQuery = `Product Description: "${hrgProductDescInput.value.trim()}" ${hrgBackgroundImageBase64 ? '\nNote: A custom background image is provided for the product photos.' : ''} \nAspect Ratio: "${selectedHrgRatio}"`;
             userQuery += `\n${buildHrgPriceInstruction(hrgProductImages)}`;

             const parts = [{ text: userQuery }];
             hrgProductImages.forEach(img => {
                 parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
             });
             if (hrgModelImageBase64) {
                 parts.push({ inlineData: { mimeType: hrgModelImageMimeType, data: hrgModelImageBase64 } });
             }
             if (hrgBackgroundImageBase64) {
                 parts.push({ inlineData: { mimeType: hrgBackgroundImageMimeType, data: hrgBackgroundImageBase64 } });
             }

             const payload = { contents: [{ parts }], systemInstruction: { parts: [{ text: systemPrompt }] }, generationConfig: { responseMimeType: "application/json", responseSchema: { type: "ARRAY", items: { type: "OBJECT", properties: { "title": { "type": "STRING" }, "prompt": { "type": "STRING" } }, required: ["title", "prompt"] } } } };
             const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
             if (!response.ok) { const err = await response.json(); throw new Error(err.error?.message || response.statusText); }
             const result = await response.json();
             return JSON.parse(result.candidates[0].content.parts[0].text);
        }

        async function generateSingleHrg(id, title, prompt) {
            const card = document.getElementById(`hrg-card-${id}`);
            const outputContainer = card.querySelector('.aspect-video');
            try {
                const apiKey = ""; const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const finalPrompt = `${prompt}, elegant, cinematic, professional product photography, dramatic lighting, 8k, photorealistic, aspect ratio ${selectedHrgRatio}. ${buildHrgPriceInstruction(hrgProductImages)}`;
                const parts = [{ text: finalPrompt }];

                // Add all product images
                hrgProductImages.forEach(img => {
                    parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
                });

                // Add model image if available
                if (hrgModelImageBase64) {
                    parts.push({ inlineData: { mimeType: hrgModelImageMimeType, data: hrgModelImageBase64 } });
                }

                // Add background image if available
                if (hrgBackgroundImageBase64) {
                    parts.push({ inlineData: { mimeType: hrgBackgroundImageMimeType, data: hrgBackgroundImageBase64 } });
                }

                // Use selected aspect ratio directly
                const payload = { contents: [{ parts }], generationConfig: { responseModalities: ['TEXT', 'IMAGE'], imageConfig: { aspectRatio: selectedHrgRatio } } };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error("No image data from API.");
                const imageUrl = `data:image/png;base64,${base64Data}`;
                const safeTitle = title.replace(/[^a-z0-9]/gi, '_').toLowerCase();

                // Add to generated images array for download all functionality
                generatedHrgImages.push({
                    url: imageUrl,
                    filename: `affiliatego_${id}_${safeTitle}.png`
                });

                // Store generation parameters for regeneration
                if (!window.hrgGenerationParams) window.hrgGenerationParams = [];
                window.hrgGenerationParams[id - 1] = {
                    prompt: prompt,
                    title: title,
                    productImages: hrgProductImages,
                    modelImage: hrgModelImageBase64 ? { base64: hrgModelImageBase64, mimeType: hrgModelImageMimeType } : null,
                    ratio: selectedHrgRatio
                };

                outputContainer.innerHTML = `<div class="relative w-full h-full group"><img src="${imageUrl}" class="w-full h-full object-cover rounded-md" alt="${title}"><div class="absolute bottom-2 right-2 flex flex-wrap gap-2 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity"><button data-action="hrg-preview" data-image-url="${imageUrl}" class="action-btn bg-blue-500 text-white p-2 rounded-full hover:bg-blue-600" title="Preview"><i class="fas fa-search-plus"></i></button><button data-action="hrg-edit" data-index="${id - 1}" data-prompt="${prompt.replace(/"/g, '&quot;')}" class="action-btn bg-purple-500 text-white p-2 rounded-full hover:bg-purple-600" title="Edit Prompt"><i class="fas fa-edit"></i></button><button data-action="hrg-regenerate" data-index="${id - 1}" class="action-btn bg-green-500 text-white p-2 rounded-full hover:bg-green-600" title="Regenerate"><i class="fas fa-sync-alt"></i></button><button data-action="hrg-video" data-image-url="${imageUrl}" data-title="${title.replace(/"/g, '&quot;')}" class="action-btn bg-fuchsia-500 text-white p-2 rounded-full hover:bg-fuchsia-600" title="Buat Prompt Video"><i class="fas fa-film"></i></button><button data-action="hrg-caption" data-image-url="${imageUrl}" data-title="${title.replace(/"/g, '&quot;')}" class="action-btn bg-rose-500 text-white p-2 rounded-full hover:bg-rose-600" title="Buat Caption"><i class="fas fa-pencil-alt"></i></button><button data-action="hrg-download" data-image-url="${imageUrl}" data-filename="affiliatego_${id}_${safeTitle}.png" class="action-btn bg-cyan-600 text-white p-2 rounded-full hover:bg-cyan-700" title="Unduh"><i class="fas fa-download"></i></button></div></div>`;
            } catch (error) {
                console.error(`Error generating hrg ${id}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        const createHrgDynamicCards = (prompts) => {
            hrgGrid.innerHTML = prompts.map((p, i) => `
                <div id="hrg-card-${i + 1}" class="result-card card p-4 flex flex-col justify-between">
                    <div><h3 class="text-lg font-semibold text-gray-800 mb-4">${p.title}</h3></div>
                    <div class="aspect-video bg-gray-100 rounded-md flex items-center justify-center"><div class="loader"></div></div>
                </div>`).join('');
        };
        
        if(hrgGenerateDescBtn) hrgGenerateDescBtn.addEventListener('click', async () => {
            const originalButtonText = hrgGenerateDescBtn.innerHTML;
            hrgGenerateDescBtn.innerHTML = `<div class="loader !w-4 !h-4 !border-2"></div>`;
            hrgGenerateDescBtn.disabled = true;
            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `You are a professional copywriter. Analyze the product(s) in the image(s) and write a concise, compelling, and SEO-friendly product description in Indonesian. Highlight key features and potential benefits for customers. Keep it under 500 characters.`;
                const parts = [ { text: "Buatkan deskripsi produk untuk gambar ini." }];
                if (hrgProductImages.length > 0) {
                     parts.push({ inlineData: { mimeType: hrgProductImages[0].mimeType, data: hrgProductImages[0].base64 } });
                }
                const payload = {
                    contents: [{ parts: parts }],
                    systemInstruction: { parts: [{ text: systemPrompt }] }
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
                const result = await response.json();
                const description = result.candidates[0].content.parts[0].text;
                hrgProductDescInput.value = description.trim();
                updateHrgButtonState();
            } catch(error) {
                console.error("Error generating description:", error);
                hrgProductDescInput.value = "Gagal membuat deskripsi. Coba lagi.";
            } finally {
                hrgGenerateDescBtn.innerHTML = originalButtonText;
                hrgGenerateDescBtn.disabled = false;
            }
        });
        window.handleAffiliateCaption = async (imageUrl, title) => {
            try {
                // Tampilkan loading modal
                const modal = document.createElement('div');
                modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4 overflow-y-auto';
                modal.style.WebkitOverflowScrolling = 'touch'; // Enable smooth scrolling on iOS
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full my-8" onclick="event.stopPropagation()">
                        <h3 class="text-xl font-bold mb-4 text-gray-800">
                            <i class="fas fa-pencil-alt text-rose-500 mr-2"></i>Membuat Caption...
                        </h3>
                        <div class="flex items-center justify-center py-8">
                            <div class="loader !border-l-rose-500"></div>
                        </div>
                    </div>
                `;
                document.body.appendChild(modal);

                // Close modal on backdrop click
                modal.addEventListener('click', (e) => {
                    if (e.target === modal) {
                        modal.remove();
                    }
                });

                // Konversi imageUrl ke base64
                const response = await fetch(imageUrl);
                const blob = await response.blob();
                const reader = new FileReader();
                const base64Promise = new Promise((resolve) => {
                    reader.onloadend = () => {
                        const base64 = reader.result.split(',')[1];
                        resolve(base64);
                    };
                    reader.readAsDataURL(blob);
                });
                const base64Data = await base64Promise;

                // Call Gemini API untuk generate caption
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const systemPrompt = `You are an expert social media copywriter and content strategist. Analyze this product image and create 3 engaging, conversion-focused Instagram captions in Indonesian.

Each caption should:
1. Hook attention in the first line
2. Highlight product benefits or create desire
3. Include relevant emojis naturally
4. End with a clear call-to-action (CTA)
5. Be optimized for engagement and sales
6. Have different tones: Professional, Casual/Fun, and Inspirational

Format your response as JSON with this structure:
{
  "professional": "caption text",
  "casual": "caption text",
  "inspirational": "caption text"
}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: `Analyze this product image titled "${title}" and create 3 engaging Instagram captions in Indonesian for different audiences.` },
                            { inlineData: { mimeType: 'image/png', data: base64Data } }
                        ]
                    }],
                    systemInstruction: { parts: [{ text: systemPrompt }] },
                    generationConfig: {
                        responseMimeType: "application/json"
                    }
                };

                const apiResponse = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!apiResponse.ok) {
                    const errorData = await apiResponse.json();
                    throw new Error(errorData.error?.message || 'API request failed');
                }

                const result = await apiResponse.json();
                const captionText = result.candidates[0].content.parts[0].text;
                const captions = JSON.parse(captionText);

                // Unicode escape sequences untuk obfuscation safety
                const targetEmoji = '\uD83C\uDFAF'; // 🎯
                const smileEmoji = '\uD83D\uDE0A'; // 😊
                const sparkleEmoji = '\u2728'; // ✨

                // Update modal dengan hasil caption
                const modalContent = modal.querySelector('.bg-white');
                modalContent.innerHTML = `
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-xl font-bold text-gray-800">
                            <i class="fas fa-pencil-alt text-rose-500 mr-2"></i>Caption Instagram
                        </h3>
                        <button data-action="close-modal" class="text-gray-500 hover:text-gray-700 text-2xl leading-none" title="Tutup">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>

                    <div class="space-y-4 mb-6 max-h-[60vh] overflow-y-auto" style="-webkit-overflow-scrolling: touch;">
                        ${Object.entries(captions).map(([tone, caption]) => `
                            <div class="border border-gray-200 rounded-lg p-4 bg-gray-50">
                                <div class="flex items-center justify-between mb-2">
                                    <label class="text-sm font-bold text-gray-700 uppercase">${tone === 'professional' ? targetEmoji + ' Profesional' : tone === 'casual' ? smileEmoji + ' Casual & Fun' : sparkleEmoji + ' Inspiratif'}</label>
                                    <button data-action="copy-caption" data-caption="${caption.replace(/"/g, '&quot;')}"
                                        class="caption-copy-btn text-xs bg-rose-500 hover:bg-rose-600 text-white px-3 py-1 rounded-full transition-colors flex-shrink-0">
                                        <i class="fas fa-copy mr-1"></i>Salin
                                    </button>
                                </div>
                                <p class="text-sm text-gray-800 whitespace-pre-wrap leading-relaxed">${caption}</p>
                            </div>
                        `).join('')}
                    </div>

                    <div class="bg-blue-50 border border-blue-200 rounded-lg p-3 mb-4">
                        <p class="text-xs text-blue-800">
                            <i class="fas fa-info-circle mr-1"></i>
                            <strong>Tips:</strong> Pilih caption yang sesuai dengan brand voice Anda!
                        </p>
                    </div>

                    <button data-action="close-modal"
                        class="w-full bg-gray-500 hover:bg-gray-600 text-white font-bold py-3 px-4 rounded-lg transition-colors">
                        Tutup
                    </button>
                `;

                // Add event delegation for copy and close buttons (works on desktop & mobile)
                modalContent.addEventListener('click', async (e) => {
                    e.stopPropagation();

                    const copyBtn = e.target.closest('[data-action="copy-caption"]');
                    const closeBtn = e.target.closest('[data-action="close-modal"]');

                    if (copyBtn) {
                        const captionText = copyBtn.dataset.caption;
                        try {
                            await navigator.clipboard.writeText(captionText);
                            const originalHTML = copyBtn.innerHTML;
                            copyBtn.innerHTML = '<i class="fas fa-check mr-1"></i>Tersalin!';
                            copyBtn.classList.remove('bg-rose-500');
                            copyBtn.classList.add('bg-green-500');
                            setTimeout(() => {
                                copyBtn.innerHTML = originalHTML;
                                copyBtn.classList.remove('bg-green-500');
                                copyBtn.classList.add('bg-rose-500');
                            }, 2000);
                        } catch (err) {
                            console.error('Failed to copy:', err);
                            // Fallback for older browsers
                            alert('Caption berhasil dipilih. Salin dengan Ctrl+C atau Command+C');
                        }
                    } else if (closeBtn) {
                        modal.remove();
                    }
                });

            } catch (error) {
                console.error('Error generating caption:', error);

                // Tampilkan error modal
                const errorModal = document.createElement('div');
                errorModal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';
                errorModal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-md w-full">
                        <h3 class="text-xl font-bold mb-4 text-red-600">
                            <i class="fas fa-exclamation-triangle mr-2"></i>Error
                        </h3>
                        <p class="text-gray-700 mb-4">${error.message}</p>
                        <button onclick="this.closest('.fixed').remove()"
                            class="w-full bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                            Tutup
                        </button>
                    </div>
                `;
                document.body.appendChild(errorModal);

                // Remove loading modal if it exists
                const existingModal = document.querySelector('.fixed.inset-0');
                if (existingModal && existingModal !== errorModal) {
                    existingModal.remove();
                }
            }
        };

        window.handleAffiliateVideoPrompt = async (imageUrl, title) => {
            try {
                // Tampilkan loading
                const modal = document.createElement('div');
                modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full max-h-[80vh] overflow-y-auto">
                        <h3 class="text-xl font-bold mb-4 text-gray-800">
                            <i class="fas fa-film text-fuchsia-500 mr-2"></i>Membuat Prompt Video...
                        </h3>
                        <div class="flex items-center justify-center py-8">
                            <div class="loader !border-l-fuchsia-500"></div>
                        </div>
                    </div>
                `;
                document.body.appendChild(modal);

                // Konversi imageUrl ke base64
                const response = await fetch(imageUrl);
                const blob = await response.blob();
                const reader = new FileReader();
                const base64Promise = new Promise((resolve) => {
                    reader.onloadend = () => {
                        const base64 = reader.result.split(',')[1];
                        resolve(base64);
                    };
                    reader.readAsDataURL(blob);
                });
                const base64Data = await base64Promise;

                // Call Gemini API untuk generate prompt
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const systemPrompt = `You are an expert video prompt engineer. Analyze this product image and create a detailed, cinematic prompt for an AI image-to-video generator (like Runway, Pika, or Stable Video Diffusion).

The prompt should:
1. Describe the product's visual appearance and key features
2. Suggest natural, engaging camera movements (zoom in, pan, rotation, etc.)
3. Include dynamic elements (floating particles, light effects, product rotation)
4. Specify lighting and atmosphere
5. Be optimized for text-to-video AI models
6. Be in English for maximum compatibility
7. Keep it under 200 words but highly detailed

Output ONLY the video prompt, nothing else.`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: `Create a cinematic video prompt for this product image titled "${title}". Make it dynamic and engaging for image-to-video AI.` },
                            { inlineData: { mimeType: 'image/png', data: base64Data } }
                        ]
                    }],
                    systemInstruction: { parts: [{ text: systemPrompt }] }
                };

                const apiResponse = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!apiResponse.ok) {
                    const errorData = await apiResponse.json();
                    throw new Error(errorData.error?.message || 'Failed to generate prompt');
                }

                const result = await apiResponse.json();
                const videoPrompt = result.candidates[0].content.parts[0].text.trim();

                // Tampilkan hasil
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full max-h-[80vh] overflow-y-auto">
                        <div class="flex items-center justify-between mb-4">
                            <h3 class="text-xl font-bold text-gray-800">
                                <i class="fas fa-film text-fuchsia-500 mr-2"></i>Prompt Video untuk: ${title}
                            </h3>
                            <button onclick="this.closest('.fixed').remove()" class="text-gray-500 hover:text-gray-700">
                                <i class="fas fa-times text-xl"></i>
                            </button>
                        </div>

                        <div class="mb-4">
                            <img src="${imageUrl}" alt="${title}" class="w-full rounded-lg mb-3 max-h-64 object-contain bg-gray-100">
                        </div>

                        <div class="bg-gray-50 border-2 border-gray-200 rounded-lg p-4 mb-4">
                            <div class="flex items-center justify-between mb-2">
                                <label class="text-sm font-semibold text-gray-700">Video Prompt:</label>
                                <button id="copyPromptBtn" class="text-xs bg-fuchsia-500 hover:bg-fuchsia-600 text-white px-3 py-1 rounded-full transition-colors">
                                    <i class="fas fa-copy mr-1"></i>Copy
                                </button>
                            </div>
                            <textarea id="videoPromptText" rows="8" readonly class="w-full p-3 border border-gray-300 rounded-lg bg-white text-gray-800 text-sm font-mono resize-none focus:outline-none focus:ring-2 focus:ring-fuchsia-500">${videoPrompt}</textarea>
                        </div>

                        <div class="bg-blue-50 border border-blue-200 rounded-lg p-3 mb-4">
                            <p class="text-xs text-blue-800">
                                <i class="fas fa-info-circle mr-1"></i>
                                <strong>Cara Pakai:</strong> Copy prompt ini dan gunakan di platform Image-to-Video seperti Runway, Pika Labs, Stable Video Diffusion, atau yang lainnya. Upload gambar produk Anda dan paste prompt ini untuk hasil terbaik.
                            </p>
                        </div>

                        <div class="flex gap-2">
                            <button onclick="this.closest('.fixed').remove()" class="flex-1 bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                                Tutup
                            </button>
                        </div>
                    </div>
                `;

                // Add copy to clipboard functionality
                const copyBtn = modal.querySelector('#copyPromptBtn');
                const promptTextarea = modal.querySelector('#videoPromptText');

                copyBtn.addEventListener('click', () => {
                    promptTextarea.select();
                    document.execCommand('copy');
                    copyBtn.innerHTML = '<i class="fas fa-check mr-1"></i>Copied!';
                    copyBtn.classList.remove('bg-fuchsia-500', 'hover:bg-fuchsia-600');
                    copyBtn.classList.add('bg-green-500');
                    setTimeout(() => {
                        copyBtn.innerHTML = '<i class="fas fa-copy mr-1"></i>Copy';
                        copyBtn.classList.remove('bg-green-500');
                        copyBtn.classList.add('bg-fuchsia-500', 'hover:bg-fuchsia-600');
                    }, 2000);
                });

            } catch (error) {
                console.error('Error generating video prompt:', error);
                // Tampilkan error
                const errorModal = document.createElement('div');
                errorModal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';
                errorModal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-md w-full">
                        <h3 class="text-xl font-bold mb-4 text-red-600">
                            <i class="fas fa-exclamation-triangle mr-2"></i>Error
                        </h3>
                        <p class="text-gray-700 mb-4">${error.message}</p>
                        <button onclick="this.closest('.fixed').remove()" class="w-full bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded-lg">
                            Tutup
                        </button>
                    </div>
                `;
                document.body.appendChild(errorModal);

                // Remove existing loading modal if exists
                const existingModal = document.querySelector('.fixed.inset-0');
                if (existingModal && existingModal !== errorModal) {
                    existingModal.remove();
                }
            }
        };

        // Event delegation for hrg grid action buttons (works on desktop & mobile)
        if (hrgGrid) {
            hrgGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const imageUrl = actionBtn.dataset.imageUrl;
                const title = actionBtn.dataset.title;
                const filename = actionBtn.dataset.filename;
                const index = actionBtn.dataset.index;

                if (action === 'hrg-preview' && imageUrl) {
                    showPreviewModal(imageUrl);
                } else if (action === 'hrg-edit' && index !== undefined) {
                    handleEditHrg(parseInt(index), actionBtn.dataset.prompt);
                } else if (action === 'hrg-regenerate' && index !== undefined) {
                    await handleRegenerateHrg(parseInt(index));
                } else if (action === 'hrg-video' && imageUrl && title) {
                    window.handleAffiliateVideoPrompt(imageUrl, title);
                } else if (action === 'hrg-caption' && imageUrl && title) {
                    window.handleAffiliateCaption(imageUrl, title);
                } else if (action === 'hrg-download' && imageUrl && filename) {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageUrl, filename);
                    }
                }
            });

            // Handler functions for edit and regenerate
            function handleEditHrg(index, currentPrompt) {
                const modal = document.createElement('div');
                modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full">
                        <h3 class="text-xl font-bold mb-4 text-gray-800">
                            <i class="fas fa-edit text-purple-500 mr-2"></i>Edit Scene Prompt
                        </h3>
                        <div class="mb-4">
                            <label class="block text-sm font-semibold text-gray-700 mb-2">Scene Description:</label>
                            <textarea id="edit-hrg-input" rows="4" class="w-full p-3 border-2 border-gray-300 rounded-lg focus:border-purple-500 focus:outline-none resize-none" placeholder="Contoh: A serene mountain landscape with a river flowing through, vibrant flowers in foreground, golden hour lighting...">${currentPrompt}</textarea>
                            <p class="text-xs text-gray-500 mt-2">
                                <i class="fas fa-info-circle mr-1"></i>
                                Edit deskripsi scene/style untuk hasil yang berbeda
                            </p>
                        </div>
                        <div class="flex gap-2">
                            <button onclick="this.closest('.fixed').remove()" class="flex-1 bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                                Batal
                            </button>
                            <button id="confirm-edit-hrg-btn" class="flex-1 bg-purple-500 hover:bg-purple-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                                <i class="fas fa-check mr-2"></i>Generate Ulang
                            </button>
                        </div>
                    </div>
                `;
                document.body.appendChild(modal);

                const confirmBtn = modal.querySelector('#confirm-edit-hrg-btn');
                const promptInput = modal.querySelector('#edit-hrg-input');

                confirmBtn.addEventListener('click', async () => {
                    const newPrompt = promptInput.value.trim();
                    if (!newPrompt) return;

                    modal.remove();

                    // Update the prompt for this image
                    if (window.hrgGenerationParams && window.hrgGenerationParams[index]) {
                        window.hrgGenerationParams[index].prompt = newPrompt;
                    }

                    await handleRegenerateHrg(index);
                });
            }

            async function handleRegenerateHrg(index) {
                const targetCard = document.getElementById(`hrg-card-${index + 1}`);
                if (!targetCard) return;

                const params = window.hrgGenerationParams ? window.hrgGenerationParams[index] : null;
                if (!params) return;

                // Show loading state
                const outputContainer = targetCard.querySelector('.aspect-video, .relative');
                if (!outputContainer) return;

                outputContainer.innerHTML = `
                    <div class="flex flex-col items-center justify-center h-full min-h-[200px]">
                        <div class="loader !border-l-purple-500"></div>
                        <p class="mt-4 text-sm text-gray-600">Regenerating...</p>
                    </div>
                `;

                try {
                    const apiKey = "";
                    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                    // Add variation to ensure different results
                    const variations = [
                        ', slightly different angle',
                        ', alternative perspective',
                        ', different composition',
                        ', varied lighting mood',
                        ', alternative styling',
                        ', different camera angle'
                    ];
                    const randomVariation = variations[Math.floor(Math.random() * variations.length)];
                    const variedPrompt = params.prompt + randomVariation + '. ' + buildHrgPriceInstruction(params.productImages);

                    const parts = [{ text: variedPrompt }];

                    // Add all product images
                    params.productImages.forEach(img => {
                        parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
                    });

                    // Add model image if available
                    if (params.modelImage) {
                        parts.push({ inlineData: { mimeType: params.modelImage.mimeType, data: params.modelImage.base64 } });
                    }

                    const payload = { contents: [{ parts }], generationConfig: { responseModalities: ['IMAGE'], imageConfig: { aspectRatio: params.ratio } } };
                    const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });

                    if (!response.ok) throw new Error(`API error: ${response.statusText}`);

                    const result = await response.json();
                    const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                    if (base64Data) {
                        const imageUrl = `data:image/png;base64,${base64Data}`;
                        const safeTitle = params.title.replace(/[^a-z0-9]/gi, '_').toLowerCase();
                        const safePrompt = params.prompt.replace(/"/g, '&quot;');

                        // Update stored image in generatedHrgImages
                        if (generatedHrgImages[index]) {
                            generatedHrgImages[index].url = imageUrl;
                        }

                        outputContainer.innerHTML = `<div class="relative w-full h-full group"><img src="${imageUrl}" class="w-full h-full object-cover rounded-md" alt="${params.title}"><div class="absolute bottom-2 right-2 flex flex-wrap gap-2 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity"><button data-action="hrg-preview" data-image-url="${imageUrl}" class="action-btn bg-blue-500 text-white p-2 rounded-full hover:bg-blue-600" title="Preview"><i class="fas fa-search-plus"></i></button><button data-action="hrg-edit" data-index="${index}" data-prompt="${safePrompt}" class="action-btn bg-purple-500 text-white p-2 rounded-full hover:bg-purple-600" title="Edit Prompt"><i class="fas fa-edit"></i></button><button data-action="hrg-regenerate" data-index="${index}" class="action-btn bg-green-500 text-white p-2 rounded-full hover:bg-green-600" title="Regenerate"><i class="fas fa-sync-alt"></i></button><button data-action="hrg-video" data-image-url="${imageUrl}" data-title="${params.title.replace(/"/g, '&quot;')}" class="action-btn bg-fuchsia-500 text-white p-2 rounded-full hover:bg-fuchsia-600" title="Buat Prompt Video"><i class="fas fa-film"></i></button><button data-action="hrg-caption" data-image-url="${imageUrl}" data-title="${params.title.replace(/"/g, '&quot;')}" class="action-btn bg-rose-500 text-white p-2 rounded-full hover:bg-rose-600" title="Buat Caption"><i class="fas fa-pencil-alt"></i></button><button data-action="hrg-download" data-image-url="${imageUrl}" data-filename="affiliatego_${index + 1}_${safeTitle}.png" class="action-btn bg-cyan-600 text-white p-2 rounded-full hover:bg-cyan-700" title="Unduh"><i class="fas fa-download"></i></button></div></div>`;
                    } else {
                        throw new Error('Failed to generate image');
                    }
                } catch (error) {
                    console.error('Regenerate error:', error);
                    outputContainer.innerHTML = `
                        <div class="flex flex-col items-center justify-center h-full min-h-[200px] text-red-600">
                            <i class="fas fa-exclamation-triangle text-3xl mb-2"></i>
                            <p class="text-sm">Gagal regenerate</p>
                            <button onclick="location.reload()" class="mt-2 text-xs bg-red-500 text-white px-3 py-1 rounded-full hover:bg-red-600">
                                Refresh
                            </button>
                        </div>
                    `;
                }
            }

        }
    })();

    // ==================== VIRTUAL TRY-ON PRO ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const tryonProductUploadBox = document.getElementById('tryon-product-upload-box');
        const tryonProductInput = document.getElementById('tryon-product-input');
        const tryonProductPreview = document.getElementById('tryon-product-preview');
        const tryonProductPlaceholder = document.getElementById('tryon-product-placeholder');

        const tryonModelUploadBox = document.getElementById('tryon-model-upload-box');
        const tryonModelInput = document.getElementById('tryon-model-input');
        const tryonModelPreview = document.getElementById('tryon-model-preview');
        const tryonModelPlaceholder = document.getElementById('tryon-model-placeholder');

        const tryonAngleSelection = document.getElementById('tryon-angle-selection');
        const tryonCustomAngleContainer = document.getElementById('tryon-custom-angle-container');
        const tryonCustomAngle = document.getElementById('tryon-custom-angle');

        const tryonGenerateBtn = document.getElementById('tryon-generate-btn');
        const tryonPlaceholder = document.getElementById('tryon-placeholder');
        const tryonLoading = document.getElementById('tryon-loading');
        const tryonProgressText = document.getElementById('tryon-progress-text');
        const tryonResultsHeader = document.getElementById('tryon-results-header');
        const tryonResultsCount = document.getElementById('tryon-results-count');
        const tryonResultsGrid = document.getElementById('tryon-results-grid');
        const tryonDownloadAllBtn = document.getElementById('tryon-download-all-btn');

        // State variables
        let tryonProductData = null;
        let tryonModelData = null;
        let selectedAngle = 'Front View';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let selectedTryonRatio = '3:4'; // ratio selector, default 3:4
        let generatedImages = [];

        // Map rasio terpilih -> Tailwind aspect class (untuk bingkai kartu konsisten)
        function tryonAspectClass() {
            const map = { '1:1': 'aspect-square', '3:4': 'aspect-[3/4]', '4:5': 'aspect-[4/5]', '9:16': 'aspect-[9/16]', '16:9': 'aspect-video', '2:3': 'aspect-[2/3]' };
            return map[selectedTryonRatio] || 'aspect-[3/4]';
        }

        // Count selection (1-10), default 4
        const tryonCountSel = document.getElementById('tryon-count-selection-grid');
        if (tryonCountSel) {
            tryonCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                tryonCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Ratio selection, default 3:4
        const tryonRatioSel = document.getElementById('tryon-ratio-selection');
        if (tryonRatioSel) {
            tryonRatioSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-ratio]');
                if (!btn) return;
                tryonRatioSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedTryonRatio = btn.dataset.ratio;
            });
        }

        // Setup image uploaders
        function setupImageUploader(uploadBox, input, preview, placeholder, dataStore) {
            if (!uploadBox || !input) return;

            uploadBox.addEventListener('click', () => input.click());
            uploadBox.addEventListener('dragover', (e) => {
                e.preventDefault();
                uploadBox.style.borderColor = '#14b8a6';
            });
            uploadBox.addEventListener('dragleave', () => {
                uploadBox.style.borderColor = '';
            });
            uploadBox.addEventListener('drop', (e) => {
                e.preventDefault();
                uploadBox.style.borderColor = '';
                if (e.dataTransfer.files[0]) handleFile(e.dataTransfer.files[0], preview, placeholder, dataStore);
            });
            input.addEventListener('change', (e) => {
                if (e.target.files[0]) handleFile(e.target.files[0], preview, placeholder, dataStore);
            });
        }

        async function handleFile(file, preview, placeholder, dataStore) {
            try {
                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    const heic2any = window.heic2any;
                    if (heic2any) {
                        const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }

                const reader = new FileReader();
                reader.onload = (e) => {
                    preview.src = e.target.result;
                    preview.classList.remove('hidden');
                    placeholder.classList.add('hidden');

                    const data = {
                        mimeType: processedFile.type,
                        base64: e.target.result.split(',')[1]
                    };

                    if (dataStore === 'product') {
                        tryonProductData = data;
                    } else {
                        tryonModelData = data;
                    }

                    updateButtonState();
                };
                reader.readAsDataURL(processedFile);
            } catch (error) {
                console.error('Error processing file:', error);
                alert('Gagal memproses gambar. Silakan coba lagi.');
            }
        }

        function updateButtonState() {
            if (tryonGenerateBtn) {
                tryonGenerateBtn.disabled = !tryonProductData || !tryonModelData;
            }
        }

        // Setup uploaders
        setupImageUploader(tryonProductUploadBox, tryonProductInput, tryonProductPreview, tryonProductPlaceholder, 'product');
        setupImageUploader(tryonModelUploadBox, tryonModelInput, tryonModelPreview, tryonModelPlaceholder, 'model');

        // Angle selection handler
        if (tryonAngleSelection) {
            tryonAngleSelection.addEventListener('click', (e) => {
                const button = e.target.closest('[data-angle]');
                if (!button) return;

                // Update selected state
                tryonAngleSelection.querySelectorAll('.option-btn-tryon').forEach(btn => btn.classList.remove('selected'));
                button.classList.add('selected');
                selectedAngle = button.dataset.angle;

                // Show/hide custom angle container
                if (selectedAngle === 'custom' && tryonCustomAngleContainer) {
                    tryonCustomAngleContainer.classList.remove('hidden');
                } else if (tryonCustomAngleContainer) {
                    tryonCustomAngleContainer.classList.add('hidden');
                }
            });
        }

        // Generate button handler
        if (tryonGenerateBtn) {
            tryonGenerateBtn.addEventListener('click', async () => {
                if (!tryonProductData || !tryonModelData) {
                    alert('Silakan unggah foto produk dan model terlebih dahulu.');
                    return;
                }

                // Determine angle description
                let angleDesc = selectedAngle;
                if (selectedAngle === 'custom' && tryonCustomAngle) {
                    const customAngleText = tryonCustomAngle.value.trim();
                    if (!customAngleText) {
                        alert('Silakan isi deskripsi custom angle atau pilih angle lain.');
                        return;
                    }
                    angleDesc = customAngleText;
                }

                await generateTryonSet(angleDesc);
            });
        }

        async function generateTryonSet(angleDesc) {
            const originalBtnHTML = tryonGenerateBtn.innerHTML;
            tryonGenerateBtn.disabled = true;

            if (tryonPlaceholder) tryonPlaceholder.classList.add('hidden');
            if (tryonLoading) tryonLoading.classList.remove('hidden');
            if (tryonResultsHeader) tryonResultsHeader.classList.add('hidden');
            if (tryonResultsGrid) tryonResultsGrid.classList.remove('hidden');

            let successCount = 0;
            let attempts = 0;
            const MAX_ATTEMPTS = 3;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                generatedImages = new Array(selectedCount).fill(null);

                if (tryonResultsGrid) {
                    tryonResultsGrid.innerHTML = '';
                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `tryon-card-${i}`;
                        card.className = `relative group rounded-xl overflow-hidden shadow-lg bg-gray-100 ${tryonAspectClass()} animate-pulse flex items-center justify-center`;
                        card.innerHTML = `<div class="text-center text-gray-400 p-4"><i class="fas fa-spinner fa-spin text-2xl mb-2"></i><p class="text-xs mt-1">${i}</p></div>`;
                        tryonResultsGrid.appendChild(card);
                    }
                }

                if (tryonProgressText) tryonProgressText.textContent = `0/${selectedCount}`;

                await Promise.allSettled(
                    Array.from({ length: selectedCount }, (_, i) => generateSingleTryon(i + 1, angleDesc))
                );

                successCount = generatedImages.filter(img => img).length;
                attempts++;
            }

            if (tryonLoading) tryonLoading.classList.add('hidden');
            tryonGenerateBtn.disabled = false;
            tryonGenerateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                if (tryonPlaceholder) tryonPlaceholder.classList.remove('hidden');
                if (tryonResultsGrid) tryonResultsGrid.classList.add('hidden');
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                return;
            }

            renderSuccessfulTryonResults(angleDesc);
        }

        async function generateSingleTryon(index, angleDesc) {
            const card = document.getElementById(`tryon-card-${index}`);
            try {
                if (tryonProgressText) tryonProgressText.textContent = `${index}/${selectedCount}`;

                const prompt = `Create a professional photorealistic image of the model wearing or using the product.

CAMERA ANGLE: ${angleDesc}

Requirements:
- The model MUST be wearing or using the product from the first image
- Maintain the model's identity and appearance from the second image
- Professional fashion photography quality
- Natural lighting and realistic composition
- High resolution, 4K quality
- Camera angle: ${angleDesc}
- Make it look like a professional product photoshoot

CRITICAL: The model must be clearly wearing/using the product in the specified angle.`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const payload = {
                    contents: [{ parts: [
                        { text: prompt },
                        { inlineData: { mimeType: tryonProductData.mimeType, data: tryonProductData.base64 } },
                        { inlineData: { mimeType: tryonModelData.mimeType, data: tryonModelData.base64 } }
                    ]}],
                    generationConfig: { responseModalities: ['TEXT', 'IMAGE'], imageConfig: { aspectRatio: selectedTryonRatio } }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const filename = `tryon_${angleDesc.replace(/\s+/g, '_')}_${index}.png`;
                generatedImages[index - 1] = { url: imageUrl, filename };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 bg-white';
                    card.innerHTML = `<img src="${imageUrl}" class="w-full object-contain ${tryonAspectClass()}" alt="Try-On ${index}"><div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2"><button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2"><i class="fas fa-eye"></i><span class="hidden sm:inline">Preview</span></button><button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-teal-500 to-lime-500 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2"><i class="fas fa-download"></i><span class="hidden sm:inline">Download</span></button></div>`;
                }
            } catch (error) {
                console.error(`Error generating try-on ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        function renderSuccessfulTryonResults(angleDesc) {
            const successful = generatedImages.filter(img => img);
            generatedImages = successful;
            if (tryonResultsGrid) {
                tryonResultsGrid.innerHTML = '';
                successful.forEach((img, idx) => {
                    const card = document.createElement('div');
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 bg-white';
                    card.innerHTML = `<img src="${img.url}" class="w-full object-contain ${tryonAspectClass()}" alt="Try-On ${idx + 1}"><div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2"><button data-action="preview" data-index="${idx}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2"><i class="fas fa-eye"></i><span class="hidden sm:inline">Preview</span></button><button data-action="download" data-index="${idx}" class="action-btn bg-gradient-to-r from-teal-500 to-lime-500 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2"><i class="fas fa-download"></i><span class="hidden sm:inline">Download</span></button></div>`;
                    tryonResultsGrid.appendChild(card);
                });
            }
            if (tryonResultsHeader) tryonResultsHeader.classList.remove('hidden');
            if (tryonResultsCount) tryonResultsCount.textContent = successful.length;
            if (tryonResultsGrid) {
                tryonResultsGrid.classList.remove('hidden');
                tryonResultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' });
            }
        }

        // Event delegation for action buttons (mobile friendly)
        if (tryonResultsGrid) {
            tryonResultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all functionality
        if (tryonDownloadAllBtn) {
            tryonDownloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || tryonDownloadAllBtn.disabled) return;

                const originalHTML = tryonDownloadAllBtn.innerHTML;
                tryonDownloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        tryonDownloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    tryonDownloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        tryonDownloadAllBtn.disabled = false;
                        tryonDownloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    tryonDownloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        tryonDownloadAllBtn.disabled = false;
                        tryonDownloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }
    })();

    // ==================== POSE FASHION ====================
    (function() {
        const apiKey = "";

        const uploadBox      = document.getElementById('pose-upload-box');
        const fileInput      = document.getElementById('pose-file-input');
        const previewCont    = document.getElementById('pose-preview-container');
        const previewImg     = document.getElementById('pose-preview-img');
        const removeBtn      = document.getElementById('pose-remove-btn');
        const descInput      = document.getElementById('pose-desc-input');
        const generateBtn    = document.getElementById('pose-generate-btn');
        const emptyState     = document.getElementById('pose-empty-state');
        const resultsHeader  = document.getElementById('pose-results-header');
        const resultsGrid    = document.getElementById('pose-results-grid');
        const resultCount    = document.getElementById('pose-result-count');
        const downloadAllBtn = document.getElementById('pose-download-all-btn');
        const quickBtns      = document.querySelectorAll('.pose-quick-btn');

        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const poseCountSel = document.getElementById('pose-count-selection-grid');
        if (poseCountSel) {
            poseCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                poseCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // 15 default fashion poses (used when no custom desc given)
        const DEFAULT_POSES = [
            'confident standing pose with both hands on hips, smiling at camera, full body',
            'dynamic walking pose mid-stride, looking forward with natural expression, full body',
            'arms crossed at chest, powerful fashion stance, direct eye contact, full body',
            'casual relaxed pose, one hand in pocket, slight weight shift, full body',
            'elegant sitting pose with legs crossed, one hand on knee, poised, 3/4 body',
            'over-the-shoulder look, body turned slightly away, face looking back at camera, full body',
            'leaning against wall casually, one leg bent, cool relaxed expression, full body',
            'contrapposto pose, weight on one leg, hip slightly tilted, S-curve silhouette, full body',
            'fierce direct gaze at camera, serious powerful high-fashion expression, portrait',
            'both hands in pockets, relaxed casual smile, standing tall, full body',
            'spinning or twirling pose, fabric in motion, joyful expression, full body',
            'one arm raised up, other hand on hip, dynamic energetic fashion pose, full body',
            'side profile view, looking forward, elegant pose showing jawline, full body',
            'sitting on floor with knees up, leaning forward on arms, candid natural pose, 3/4 body',
            'back pose facing away from camera, looking over shoulder slightly, mysterious fashion editorial, full body'
        ];

        // Camera angle suffix for custom pose variations (15 entries)
        const ANGLE_SUFFIX = [
            ', front-facing camera angle',
            ', slight three-quarter angle to the right',
            ', slight three-quarter angle to the left',
            ', low angle shot looking slightly upward',
            ', high angle shot looking slightly downward',
            ', side profile angle',
            ', close-up from waist up',
            ', full body wide shot',
            ', intimate close-up portrait',
            ', dramatic low angle',
            ', soft overhead angle',
            ', three-quarter body shot',
            ', slightly elevated camera angle',
            ', ground level dramatic angle',
            ', candid natural angle'
        ];

        let imageBase64  = null;
        let imageMime    = 'image/jpeg';
        let generatedImages = [];

        // ── Image handling ────────────────────────────────────────
        function compressAndStore(file) {
            const reader = new FileReader();
            reader.onload = (e) => {
                const img = new Image();
                img.onload = () => {
                    const MAX = 1024;
                    let w = img.width, h = img.height;
                    if (w > MAX || h > MAX) {
                        if (w > h) { h = Math.round(h * MAX / w); w = MAX; }
                        else       { w = Math.round(w * MAX / h); h = MAX; }
                    }
                    const canvas = document.createElement('canvas');
                    canvas.width = w; canvas.height = h;
                    canvas.getContext('2d').drawImage(img, 0, 0, w, h);
                    const compressed = canvas.toDataURL('image/jpeg', 0.85);
                    imageBase64 = compressed.split(',')[1];
                    imageMime   = 'image/jpeg';
                    previewImg.src = compressed;
                    previewCont.classList.remove('hidden');
                    uploadBox.classList.add('hidden');
                    generateBtn.disabled = false;
                };
                img.src = e.target.result;
            };
            reader.readAsDataURL(file);
        }

        async function handleFile(file) {
            if (!file) return;
            if (!file.type.startsWith('image/') && !file.name.toLowerCase().endsWith('.heic')) return;
            if (file.size > 5 * 1024 * 1024) { alert('Ukuran file maksimal 5MB'); return; }
            if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                try {
                    const converted = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                    compressAndStore(new File([converted], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' }));
                } catch { compressAndStore(file); }
            } else {
                compressAndStore(file);
            }
        }

        // ── Upload events ─────────────────────────────────────────
        if (uploadBox) {
            uploadBox.addEventListener('click', () => fileInput && fileInput.click());
            uploadBox.addEventListener('dragover',  (e) => { e.preventDefault(); uploadBox.classList.add('drag-over'); });
            uploadBox.addEventListener('dragleave', () => uploadBox.classList.remove('drag-over'));
            uploadBox.addEventListener('drop', (e) => {
                e.preventDefault(); uploadBox.classList.remove('drag-over');
                if (e.dataTransfer.files[0]) handleFile(e.dataTransfer.files[0]);
            });
        }
        if (fileInput) fileInput.addEventListener('change', () => { if (fileInput.files[0]) handleFile(fileInput.files[0]); });

        if (removeBtn) removeBtn.addEventListener('click', () => {
            imageBase64 = null;
            previewImg.src = '';
            previewCont.classList.add('hidden');
            uploadBox.classList.remove('hidden');
            generateBtn.disabled = true;
            if (fileInput) fileInput.value = '';
        });

        // ── Quick pose buttons ────────────────────────────────────
        quickBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                quickBtns.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                if (descInput) descInput.value = btn.dataset.pose;
            });
        });

        // ── Generate ──────────────────────────────────────────────
        if (generateBtn) generateBtn.addEventListener('click', async () => {
            if (generateBtn.disabled) return;
            if (!imageBase64) { alert('Upload foto terlebih dahulu'); return; }

            generateBtn.disabled = true;

            const customDesc = descInput ? descInput.value.trim() : '';
            generatedImages = [];

            if (emptyState)     emptyState.classList.add('hidden');
            if (resultsHeader)  resultsHeader.classList.remove('hidden');
            if (downloadAllBtn) downloadAllBtn.classList.add('hidden');
            if (resultCount)    resultCount.textContent = '0';
            resultsGrid.innerHTML = '';

            // Pre-create placeholder cards
            for (let i = 1; i <= selectedCount; i++) {
                const card = document.createElement('div');
                card.id = `pose-card-${i}`;
                card.className = 'relative rounded-xl overflow-hidden bg-gray-100 aspect-[3/4] flex items-center justify-center';
                card.innerHTML = `<div class="animate-spin rounded-full h-10 w-10 border-b-2" style="border-color:#06b6d4;"></div>`;
                resultsGrid.appendChild(card);
            }

            // Scroll to results on mobile
            if (window.innerWidth < 1024) {
                setTimeout(() => resultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' }), 300);
            }

            let attempts = 0;
            while (attempts < 3) {
                attempts++;
                generatedImages = [];

                await Promise.allSettled(
                    Array.from({ length: selectedCount }, (_, i) => generateSinglePose(i + 1, customDesc))
                );

                if (generatedImages.filter(Boolean).length > 0) break;
            }

            // Remove empty spinner cards
            document.querySelectorAll('[id^="pose-card-"]').forEach(card => {
                if (!card.querySelector('img')) card.remove();
            });

            renderResults();
            generateBtn.disabled = false;
        });

        async function generateSinglePose(index, customDesc) {
            const card = document.getElementById(`pose-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const poseInstruction = customDesc
                    ? (customDesc + ANGLE_SUFFIX[(index - 1) % ANGLE_SUFFIX.length])
                    : DEFAULT_POSES[index - 1];

                const prompt = `You are a professional fashion photographer AI. Using the person in the reference photo as the subject, generate a completely new high-quality fashion photo.

SUBJECT: The EXACT same person from the reference — same face, skin tone, hair, body type, and outfit.
POSE: ${poseInstruction}
BACKGROUND: Clean neutral studio background (white or soft light gray)
LIGHTING: Professional studio lighting, soft and flattering
QUALITY: Ultra sharp, photorealistic, 8K quality, professional fashion photography
FORMAT: Portrait 3:4 aspect ratio

IMPORTANT: Generate a brand new photo of this person in the specified pose. Keep all personal features identical to the reference photo.`;

                const payload = {
                    contents: [{ parts: [
                        { text: prompt },
                        { inlineData: { mimeType: imageMime, data: imageBase64 } }
                    ]}],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: '3:4' }
                    },
                    safetySettings: [
                        { category: 'HARM_CATEGORY_HARASSMENT',        threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_HATE_SPEECH',        threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_SEXUALLY_EXPLICIT',  threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_DANGEROUS_CONTENT',  threshold: 'BLOCK_NONE' }
                    ]
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imgData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imgData) throw new Error('No image data');

                const url = `data:image/png;base64,${imgData}`;
                generatedImages[index - 1] = { url, filename: `pose-fashion-${index}.png` };
                if (card) card.innerHTML = `<img src="${url}" class="w-full h-full object-cover" alt="Pose Fashion ${index}">`;

            } catch (err) {
                console.error(`Pose ${index} error:`, err);
                if (card) card.innerHTML = '';
            }
        }

        function renderResults() {
            const successful = generatedImages.filter(Boolean);
            if (successful.length === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                if (emptyState)    emptyState.classList.remove('hidden');
                if (resultsHeader) resultsHeader.classList.add('hidden');
                return;
            }

            resultsGrid.innerHTML = '';
            successful.forEach((img, idx) => {
                const card = document.createElement('div');
                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 cursor-pointer';
                card.innerHTML = `
                    <img src="${img.url}" class="w-full h-full object-cover aspect-[3/4]" alt="Pose Fashion ${idx + 1}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/30 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${idx}" class="bg-white/90 hover:bg-white text-gray-800 px-3 py-2 rounded-full font-semibold shadow-lg text-sm flex items-center gap-1 transition-all">
                            <i class="fas fa-eye"></i><span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${idx}" class="bg-cyan-500 hover:bg-cyan-600 text-white px-3 py-2 rounded-full font-semibold shadow-lg text-sm flex items-center gap-1 transition-all">
                            <i class="fas fa-download"></i><span class="hidden sm:inline">Unduh</span>
                        </button>
                    </div>
                `;
                resultsGrid.appendChild(card);
            });

            if (resultCount)    resultCount.textContent = successful.length;
            if (downloadAllBtn) downloadAllBtn.classList.remove('hidden');
        }

        // ── Event delegation ──────────────────────────────────────
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();
                const previewBtn  = e.target.closest('[data-action="preview"]');
                const downloadBtn = e.target.closest('[data-action="download"]');
                if (previewBtn) {
                    const successful = generatedImages.filter(Boolean);
                    const img = successful[parseInt(previewBtn.dataset.index)];
                    if (img && window.showPreviewModal) window.showPreviewModal(img.url);
                }
                if (downloadBtn) {
                    const successful = generatedImages.filter(Boolean);
                    const img = successful[parseInt(downloadBtn.dataset.index)];
                    if (img && window.downloadDataURINew) await window.downloadDataURINew(img.url, img.filename);
                }
            });
        }

        // ── Download all ──────────────────────────────────────────
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async () => {
                const valid = generatedImages.filter(Boolean);
                if (!valid.length) return;
                downloadAllBtn.disabled = true;
                downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';
                for (const img of valid) {
                    if (window.downloadDataURINew) await window.downloadDataURINew(img.url, img.filename);
                    await new Promise(r => setTimeout(r, 500));
                }
                downloadAllBtn.disabled = false;
                downloadAllBtn.innerHTML = '<i class="fas fa-download mr-1"></i>Download Semua';
            });
        }

        // ── Tab click (desktop + mobile) ──────────────────────────
        const tabDesktop = document.querySelector('.main-tab-btn[data-tab="pose-fashion"]');
        const tabMobile  = document.querySelector('.mobile-tab-btn[data-tab="pose-fashion"]');
        // No special initialization needed for this tab

    })();

    // --- FAMILY PHOTO LOGIC ---
    (function() {
        const apiKey = "";
        const fileInputArea = document.getElementById('family-file-input-area');
        const addPhotoBtn = document.getElementById('family-add-photo-btn');
        const contentFamily = document.getElementById('content-family');
        const ratioOptions = document.getElementById('family-ratio-options');
        const customThemeInput = document.getElementById('family-custom-theme-input');
        const generateBtn = document.getElementById('family-generate-btn');
        const loadingIndicator = document.getElementById('loading-indicator');
        const resultImageContainer = document.getElementById('result-image-container');
        const resultsPlaceholder = document.getElementById('family-results-placeholder');
        const resultsHeader = document.getElementById('family-results-header');
        const downloadAllBtn = document.getElementById('family-download-all-btn');
        const randomThemeBtn = document.getElementById('family-random-theme-btn');
        const modal = document.getElementById('family-custom-modal');
        const modalMessage = document.getElementById('family-modal-message');

        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const familyCountSel = document.getElementById('family-count-selection-grid');
        if (familyCountSel) {
            familyCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                familyCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        let photos = [null, null]; // Will store base64 strings
        let photoMimeTypes = [null, null]; // Will store mime types
        let customBackgroundData = null; // Optional background upload
        let selectedRatio = '3:4'; // Default aspect ratio
        let generatedImages = []; // Store generated images for download all
        let currentTheme = ''; // Store current theme for regenerate

        // Background upload elements
        const backgroundInput = document.getElementById('family-background-input');
        const backgroundUploadArea = document.getElementById('family-background-upload-area');
        const backgroundPreview = document.getElementById('family-background-preview');
        const backgroundPreviewContainer = document.getElementById('family-background-preview-container');
        const removeBackgroundBtn = document.getElementById('family-remove-background');

        function familyShowModal(message) {
            if (modalMessage) modalMessage.textContent = message;
            if (modal) modal.style.display = 'flex';
        }

        window.familyCloseModal = () => {
            if (modal) modal.style.display = 'none';
        };

        function updateFileInputs() {
            if (!fileInputArea) return;
            fileInputArea.innerHTML = '';
            photos.forEach((photo, i) => {
                const wrapper = document.createElement('div');
                wrapper.className = 'file-input-wrapper';
                if (photo) wrapper.classList.add('has-photo');

                const input = document.createElement('input');
                input.type = 'file';
                input.accept = 'image/*';
                input.className = 'file-input';
                input.dataset.index = i;
                input.addEventListener('change', handleFileChange);

                const preview = document.createElement('img');
                preview.className = 'file-preview';
                if (photo) {
                    preview.src = `data:${photoMimeTypes[i]};base64,${photo}`;
                } else {
                    preview.classList.add('hidden');
                }
                
                const placeholder = document.createElement('span');
                placeholder.className = 'text-gray-500 text-center text-sm';
                if (!photo) placeholder.textContent = 'Tambahkan Foto';

                const deleteBtn = document.createElement('button');
                deleteBtn.className = 'delete-btn';
                deleteBtn.innerHTML = '×';
                deleteBtn.onclick = (e) => familyDeletePhoto(e, i);

                wrapper.append(input, preview, placeholder, deleteBtn);
                fileInputArea.appendChild(wrapper);
            });
        }

        function updateGenerateBtn() {
            if (generateBtn) generateBtn.disabled = photos.filter(Boolean).length < 2;
        }

        function handleFileChange(e) {
            const index = parseInt(e.target.dataset.index, 10);
            const file = e.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = (event) => {
                    photos[index] = event.target.result.split(',')[1];
                    photoMimeTypes[index] = file.type;
                    updateFileInputs();
                    updateGenerateBtn();
                };
                reader.readAsDataURL(file);
            }
        }

        window.familyDeletePhoto = (event, index) => {
            event.stopPropagation();
            photos[index] = null;
            photoMimeTypes[index] = null;
            // Only remove the slot if there are more than 2
            if (photos.length > 2) {
                photos.splice(index, 1);
                photoMimeTypes.splice(index, 1);
            }
            updateFileInputs();
            updateGenerateBtn();
        };

        if (addPhotoBtn) {
            addPhotoBtn.addEventListener('click', () => {
                if (photos.length < 10) {
                    photos.push(null);
                    photoMimeTypes.push(null);
                    updateFileInputs();
                } else {
                    familyShowModal('Anda hanya dapat menambahkan maksimal 10 foto.');
                }
            });
        }

        // Background upload handler with HEIC support
        if (backgroundInput) {
            backgroundInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                try {
                    let processedFile = file;

                    // Convert HEIC to JPEG if needed
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        const heic2any = window.heic2any;
                        if (heic2any) {
                            const convertedBlob = await heic2any({
                                blob: file,
                                toType: 'image/jpeg',
                                quality: 0.9
                            });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                                type: 'image/jpeg'
                            });
                        }
                    }

                    const reader = new FileReader();
                    reader.onloadend = () => {
                        customBackgroundData = reader.result;

                        // Show preview
                        if (backgroundPreview && backgroundPreviewContainer && backgroundUploadArea) {
                            backgroundPreview.src = reader.result;
                            backgroundUploadArea.classList.add('hidden');
                            backgroundPreviewContainer.classList.remove('hidden');
                        }
                    };
                    reader.readAsDataURL(processedFile);
                } catch (error) {
                    console.error('Error processing background image:', error);
                    familyShowModal('Gagal memproses gambar background');
                }
            });
        }

        // Remove background handler
        if (removeBackgroundBtn) {
            removeBackgroundBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                customBackgroundData = null;
                if (backgroundInput) backgroundInput.value = '';
                if (backgroundUploadArea) backgroundUploadArea.classList.remove('hidden');
                if (backgroundPreviewContainer) backgroundPreviewContainer.classList.add('hidden');
                if (backgroundPreview) backgroundPreview.src = '';
            });
        }

        // Event delegation for theme buttons
        if (contentFamily) {
            contentFamily.addEventListener('click', (e) => {
                if (e.target.classList.contains('theme-btn') && e.target.id !== 'family-random-theme-btn') {
                    contentFamily.querySelectorAll('.theme-btn').forEach(btn => btn.classList.remove('active'));
                    e.target.classList.add('active');
                }
            });
        }

        if (randomThemeBtn) {
            randomThemeBtn.addEventListener('click', () => {
                if (contentFamily) {
                    const allThemeButtons = Array.from(contentFamily.querySelectorAll('.theme-btn:not(#family-random-theme-btn)'));
                    if (allThemeButtons.length > 0) {
                        contentFamily.querySelectorAll('.theme-btn').forEach(btn => btn.classList.remove('active'));
                        const randomIndex = Math.floor(Math.random() * allThemeButtons.length);
                        allThemeButtons[randomIndex].classList.add('active');
                    }
                }
            });
        }

        // Ratio button selection handler
        if (ratioOptions) {
            ratioOptions.addEventListener('click', (e) => {
                const button = e.target.closest('.option-btn-family');
                if (button) {
                    ratioOptions.querySelectorAll('.option-btn-family').forEach(btn => btn.classList.remove('selected'));
                    button.classList.add('selected');
                    selectedRatio = button.dataset.value;
                }
            });
        }

        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (generateBtn.disabled) return;

                const uploadedFiles = photos.map((p, i) => ({ base64: p, mimeType: photoMimeTypes[i] })).filter(f => f.base64);

                // Check for custom theme or selected theme
                const customTheme = customThemeInput ? customThemeInput.value.trim() : '';
                const activeThemeBtn = contentFamily ? contentFamily.querySelector('.theme-btn.active') : null;

                let finalTheme = '';
                if (customTheme) {
                    finalTheme = customTheme;
                } else if (activeThemeBtn) {
                    finalTheme = activeThemeBtn.dataset.theme;
                } else {
                    familyShowModal('Silakan pilih tema atau tulis tema custom terlebih dahulu.');
                    return;
                }

                const originalBtnHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Membuat Foto Keluarga...';
                generatedImages = [];
                currentTheme = finalTheme;

                // Build ultra-natural prompt FIRST before any DOM changes
                let promptText;
                const parts = [];

                if (customBackgroundData) {
                    // With custom background - composite mode for ultra-realism
                    const base64Background = customBackgroundData.split(',')[1];

                    promptText = `CRITICAL: Create an EXTREMELY PHOTOREALISTIC family photo that looks 100% NATURAL and REAL, NOT AI-GENERATED.

TASK: Seamlessly composite ALL people from ALL uploaded photos into the background location from the last image.

ULTRA-NATURAL REQUIREMENTS (MOST IMPORTANT):
✓ Photo must look like it was taken by a REAL CAMERA with REAL PEOPLE at a REAL LOCATION
✓ NO AI-generated look - must be indistinguishable from genuine photography
✓ Natural camera imperfections: slight motion blur, natural noise/grain, authentic depth of field
✓ Real-world lighting with natural color temperature variations
✓ Authentic human expressions and body language (NO stiff poses, NO artificial smiles)
✓ Natural skin texture with pores, imperfections, and realistic color
✓ Genuine shadows and highlights matching the environment
✓ Realistic clothing wrinkles and fabric behavior

PEOPLE COUNT & IDENTITY (ABSOLUTELY CRITICAL - MOST IMPORTANT RULE):
- ONLY use people from the uploaded photos - DO NOT add any other people
- Number of people in result MUST EXACTLY MATCH the number of uploaded photos (${uploadedFiles.length} people ONLY)
- If ${uploadedFiles.length} photos uploaded = ${uploadedFiles.length} people in result (NO MORE, NO LESS)
- DO NOT generate children, babies, or additional family members if they are not in the uploaded photos
- ONLY the people whose photos were uploaded should appear in the final image
- Keep EVERY person's face, features, skin tone, hair EXACTLY identical to their original photo
- Preserve each person's unique characteristics: facial structure, expressions, eye color, etc.
- No face morphing or blending - each person must be 100% recognizable

BACKGROUND INTEGRATION:
- Use the LOCATION from the background image as the setting
- Match exact lighting direction, color temperature, and intensity from background
- Align perspective and camera angle to match background
- Create natural shadows from environment light sources
- Ensure all people fit naturally into the background scene
- Match time of day, weather conditions, and atmospheric effects

COMPOSITION & POSING:
- Natural family grouping - ${finalTheme}
- Authentic body language and interaction between family members
- Varied poses (not everyone looking at camera)
- Natural hand placement and gestures
- Realistic spatial positioning with proper depth

LIGHTING & COLOR (for genuine look):
- Match background's natural lighting exactly
- Consistent color grading across all people and background
- Natural skin tones matching the environment's light
- Realistic bounce light and ambient occlusion
- No artificial rim lighting or studio effects

CAMERA REALISM:
- Natural depth of field from real camera lens
- Slight lens distortion at edges (like real wide-angle photo)
- Authentic focus with background slightly softer
- Natural vignetting if appropriate
- Real camera sensor noise/grain (not smooth/perfect)

TECHNICAL QUALITY:
- Professional photography quality
- Sharp focus on faces, natural background blur
- Aspect ratio: ${selectedRatio}
- High resolution with natural detail (not over-sharpened)

FINAL CHECK - Must appear as:
✓ A REAL photograph taken with a professional camera
✓ GENUINE moment captured, not staged or artificial
✓ Natural imperfections that prove it's REAL photography
✓ All people clearly recognizable from their original photos
✗ REJECT if it looks AI-generated, synthetic, or fake
✗ REJECT if faces are morphed or distorted
✗ REJECT if lighting is inconsistent
✗ REJECT if composition looks unnatural

The result MUST be so realistic that NO ONE can tell it's AI-generated!`;

                    parts.push({ text: promptText });
                    parts.push({ inlineData: { mimeType: 'image/jpeg', data: base64Background } });
                    uploadedFiles.forEach(file => {
                        parts.push({ inlineData: { mimeType: file.mimeType, data: file.base64 } });
                    });

                } else {
                    // Without background - AI-generated scene (ultra-natural)
                    promptText = `CRITICAL: Create an EXTREMELY PHOTOREALISTIC family photo that looks 100% NATURAL and REAL, NOT AI-GENERATED.

SCENE: ${finalTheme}

ULTRA-NATURAL REQUIREMENTS (MOST IMPORTANT):
✓ Photo must look like it was taken by a REAL CAMERA with REAL PEOPLE at a REAL LOCATION
✓ NO AI-generated appearance - must be indistinguishable from authentic photography
✓ Natural camera characteristics: slight motion blur on moving elements, authentic grain/noise, realistic depth of field
✓ Real-world lighting with natural variations (NOT perfect studio lighting)
✓ Genuine human expressions and body language (NO stiff poses, NO forced smiles)
✓ Natural skin texture showing pores, slight blemishes, realistic skin tone variations
✓ Authentic shadows, reflections, and light interaction with surfaces
✓ Realistic clothing with natural wrinkles, textures, and how fabric actually drapes

PEOPLE COUNT & IDENTITY (ABSOLUTELY CRITICAL - MOST IMPORTANT RULE):
- ONLY use people from the uploaded photos - DO NOT add any other people
- Number of people in result MUST EXACTLY MATCH the number of uploaded photos (${uploadedFiles.length} people ONLY)
- If ${uploadedFiles.length} photos uploaded = ${uploadedFiles.length} people in result (NO MORE, NO LESS)
- DO NOT generate children, babies, or additional family members if they are not in the uploaded photos
- ONLY the people whose photos were uploaded should appear in the final image
- Keep EVERY person's face, features, skin tone, hair, body type EXACTLY identical to their original photo
- Preserve each person's unique characteristics completely
- Each person must be 100% recognizable as themselves
- NO face morphing, blending, or alterations

COMPOSITION & POSING:
- Natural family grouping appropriate to the scene
- Authentic interaction between family members (hugs, hands on shoulders, natural proximity)
- Varied poses - not everyone staring at camera (some looking at each other, environment)
- Realistic hand placement and natural gestures
- Proper spatial relationships with natural depth between people

ENVIRONMENT & SETTING:
- Create a REAL location that matches the theme (not generic or artificial)
- Specific environmental details: actual textures, realistic props, authentic setting elements
- Natural background with depth and realistic distance
- Time-of-day appropriate lighting for the scene
- Weather effects if applicable (natural sunlight, cloud shadows, etc.)

LIGHTING (for genuine photography look):
- Natural light sources only (sun, sky, ambient, practical lights in scene)
- Consistent lighting direction and color temperature
- Realistic shadows with proper softness based on light source
- Natural skin tones matching the environment's light quality
- Bounce light and color cast from surroundings
- NO artificial rim lights, NO studio lighting effects
- Authentic contrast levels (not HDR or overly processed)

CAMERA REALISM:
- Natural depth of field as if shot with 35mm-85mm lens
- Slight lens characteristics (real glass optics, not perfect rendering)
- Authentic focus with natural background blur
- Real sensor noise/grain especially in shadows
- Natural color response (not oversaturated)
- Slight vignetting if wide angle
- Natural sharpness (not AI over-sharp)

IMPERFECTIONS (prove it's real):
- Slight motion blur on any moving elements (clothing, hair)
- Natural lighting variations across the frame
- Authentic shadows with realistic edges
- Small environmental details (dust particles in sunbeams, natural textures)
- Realistic color balance (not perfect white balance)

TECHNICAL SPECIFICATIONS:
- Professional photography quality (like DSLR/mirrorless camera)
- Sharp focus on people's faces, natural background separation
- Aspect ratio: ${selectedRatio}
- High resolution with natural detail level

FINAL VERIFICATION - Photo must appear as:
✓ Taken by a PROFESSIONAL PHOTOGRAPHER with a REAL CAMERA
✓ GENUINE candid or posed family moment
✓ Natural setting with REAL location
✓ All family members clearly recognizable
✓ Has natural imperfections that prove authenticity
✗ REJECT if it looks AI-generated, synthetic, or computer-rendered
✗ REJECT if faces are altered from originals
✗ REJECT if lighting seems artificial or inconsistent
✗ REJECT if composition feels staged/unnatural
✗ REJECT if it's too perfect (real photos have imperfections)

The result MUST fool even professional photographers - it should be IMPOSSIBLE to detect it's AI-generated!`;

                    parts.push({ text: promptText });
                    uploadedFiles.forEach(file => {
                        parts.push({ inlineData: { mimeType: file.mimeType, data: file.base64 } });
                    });
                }

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                };

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                // iOS WebKit fix: save scroll position BEFORE DOM changes.
                // WebKit (iOS Chrome & Safari) scrolls to top when display:none is applied
                // to a large element below the viewport. We restore after.
                const savedScrollY = window.scrollY || window.pageYOffset;

                if (resultsPlaceholder) resultsPlaceholder.classList.add('hidden');
                if (resultsHeader) resultsHeader.classList.add('hidden');
                resultImageContainer.style.display = 'grid';
                resultImageContainer.innerHTML = '';

                // selectedCount sudah di-declare di scope luar (count selector)
                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                // Pre-create placeholder cards
                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `family-gen-card-${i}`;
                    card.className = 'relative bg-gray-100 rounded-lg flex items-center justify-center';
                    card.style.minHeight = '200px';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-teal-500"></div>';
                    resultImageContainer.appendChild(card);
                }

                // Restore scroll position after DOM changes (iOS WebKit fix)
                requestAnimationFrame(() => window.scrollTo(0, savedScrollY));

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedImages = [];

                    const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                        generateSingleFamilyImage(i + 1, apiUrl, payload)
                    );
                    await Promise.allSettled(generationPromises);
                    successCount = generatedImages.filter(Boolean).length;

                    if (successCount === 0 && attempts < MAX_ATTEMPTS) {
                        // Reset spinner cards for next attempt
                        resultImageContainer.innerHTML = '';
                        for (let i = 1; i <= selectedCount; i++) {
                            const card = document.createElement('div');
                            card.id = `family-gen-card-${i}`;
                            card.className = 'relative bg-gray-100 rounded-lg flex items-center justify-center';
                            card.style.minHeight = '200px';
                            card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-teal-500"></div>';
                            resultImageContainer.appendChild(card);
                        }
                    }
                }

                resultImageContainer.querySelectorAll('[id^="family-gen-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });

                const successCount2 = generatedImages.filter(Boolean).length;
                if (successCount2 > 0) {
                    if (resultsHeader) resultsHeader.classList.remove('hidden');
                    const familyResultCount = document.getElementById('family-result-count');
                    if (familyResultCount) familyResultCount.textContent = successCount2;
                } else {
                    if (resultsPlaceholder) resultsPlaceholder.classList.remove('hidden');
                    resultImageContainer.style.display = 'none';
                }

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                }

                generateBtn.disabled = false;
                generateBtn.innerHTML = originalBtnHTML;
            });
        }
        
        async function generateSingleFamilyImage(index, apiUrl, payload) {
            const card = document.getElementById(`family-gen-card-${index}`);
            try {
                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const filename = `family_photo_${index}.png`;
                generatedImages[index - 1] = { url: imageUrl, filename, index: index - 1 };

                if (card) {
                    card.className = 'image-card group relative';
                    card.style.animationDelay = `${(index - 1) * 0.1}s`;
                    card.dataset.index = String(index - 1);
                    card.innerHTML = `
                        <img src="${imageUrl}" alt="Generated family photo ${index}" class="generated-image w-full rounded-lg"/>
                        <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/30 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 rounded-lg flex flex-col justify-end p-4 gap-2">
                            <div class="flex gap-2">
                                <button data-action="family-preview" data-index="${index - 1}" class="action-btn flex-1 bg-blue-500 hover:bg-blue-600 text-white px-3 py-2 rounded-lg text-sm font-semibold transition flex items-center justify-center gap-1">
                                    <i class="fas fa-eye"></i>
                                    <span class="hidden sm:inline">Preview</span>
                                </button>
                                <button data-action="family-edit" data-index="${index - 1}" class="action-btn flex-1 bg-purple-500 hover:bg-purple-600 text-white px-3 py-2 rounded-lg text-sm font-semibold transition flex items-center justify-center gap-1">
                                    <i class="fas fa-edit"></i>
                                    <span class="hidden sm:inline">Edit</span>
                                </button>
                            </div>
                            <div class="flex gap-2">
                                <button data-action="family-regenerate" data-index="${index - 1}" class="action-btn flex-1 bg-green-500 hover:bg-green-600 text-white px-3 py-2 rounded-lg text-sm font-semibold transition flex items-center justify-center gap-1">
                                    <i class="fas fa-sync-alt"></i>
                                    <span class="hidden sm:inline">Regenerate</span>
                                </button>
                                <button data-action="family-download" data-index="${index - 1}" class="action-btn flex-1 bg-cyan-600 hover:bg-cyan-700 text-white px-3 py-2 rounded-lg text-sm font-semibold transition flex items-center justify-center gap-1">
                                    <i class="fas fa-download"></i>
                                    <span class="hidden sm:inline">Download</span>
                                </button>
                            </div>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`Family image ${index} error:`, error);
                if (card) card.innerHTML = '';
            }
        }

        async function generateImageWithRetry(apiUrl, payload, retries = 3, delay = 1000) {
            for (let i = 0; i < retries; i++) {
                try {
                    const response = await fetch(apiUrl, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(payload)
                    });
                    return await response.json();
                } catch (error) {
                    console.error(`Attempt ${i + 1} failed:`, error);
                    if (i === retries - 1) throw error;
                    await new Promise(res => setTimeout(res, delay * Math.pow(2, i)));
                }
            }
        }

        // Download all button
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async () => {
                const validImages = generatedImages.filter(Boolean);
                for (let i = 0; i < validImages.length; i++) {
                    const img = validImages[i];
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                        await new Promise(resolve => setTimeout(resolve, 200));
                    } else if (window.downloadImage) {
                        await window.downloadImage(img.url, img.filename);
                        await new Promise(resolve => setTimeout(resolve, 200));
                    }
                }
            });

            // Add mobile touch support
            if (window.addMobileTouchSupport) {
                window.addMobileTouchSupport(downloadAllBtn);
            }
        }

        // Event delegation for action buttons
        if (resultImageContainer) {
            resultImageContainer.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (action === 'family-preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'family-edit') {
                    handleEditTheme(index);
                } else if (action === 'family-regenerate') {
                    await handleRegenerateImage(index);
                } else if (action === 'family-download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Edit theme function
        function handleEditTheme(index) {
            const editModal = document.createElement('div');
            editModal.className = 'fixed inset-0 bg-black/70 flex items-center justify-center z-50 p-4';
            editModal.innerHTML = `
                <div class="bg-white rounded-2xl p-6 max-w-md w-full shadow-2xl">
                    <h3 class="text-xl font-bold mb-4" style="color: #0d9488;">Edit Tema</h3>
                    <textarea id="edit-theme-input" rows="4" class="w-full p-3 border-2 border-gray-200 rounded-xl focus:ring-2 focus:ring-teal-500 focus:border-teal-500 transition resize-none" placeholder="Tulis tema baru...">${currentTheme}</textarea>
                    <div class="flex gap-3 mt-4">
                        <button id="confirm-edit-btn" class="flex-1 px-6 py-3 rounded-xl font-semibold text-white shadow-lg hover:shadow-xl transition" style="background: linear-gradient(135deg, #14b8a6, #0d9488);">
                            <i class="fas fa-check mr-2"></i>Regenerate
                        </button>
                        <button id="cancel-edit-btn" class="flex-1 px-6 py-3 bg-gray-200 hover:bg-gray-300 rounded-xl font-semibold text-gray-700 transition">
                            Batal
                        </button>
                    </div>
                </div>
            `;
            document.body.appendChild(editModal);

            document.getElementById('confirm-edit-btn').addEventListener('click', async () => {
                const newTheme = document.getElementById('edit-theme-input').value.trim();
                if (newTheme) {
                    currentTheme = newTheme;
                    document.body.removeChild(editModal);
                    await handleRegenerateImage(index);
                }
            });

            document.getElementById('cancel-edit-btn').addEventListener('click', () => {
                document.body.removeChild(editModal);
            });
        }

        // Regenerate image function
        async function handleRegenerateImage(index) {
            const card = resultImageContainer.querySelector(`[data-index="${index}"]`);
            if (!card) return;

            const img = card.querySelector('.generated-image');
            const originalSrc = img.src;

            // Show loading state
            img.style.opacity = '0.5';
            img.style.filter = 'blur(4px)';

            try {
                const uploadedFiles = photos.map((p, i) => ({ base64: p, mimeType: photoMimeTypes[i] })).filter(f => f.base64);
                const promptText = `PENTING: Buat foto keluarga yang sangat realistis dan fotorealistis. Gabungkan semua orang dari semua foto yang diunggah ke dalam satu adegan yang kohesif. Pastikan semua wajah terlihat jelas dan menyerupai orang di foto aslinya. Tema foto adalah: '${currentTheme}'. Gunakan pencahayaan sinematik dan komposisi profesional.`;

                const parts = [{ text: promptText }];
                uploadedFiles.forEach(file => {
                    parts.push({ inlineData: { mimeType: file.mimeType, data: file.base64 } });
                });

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                };

                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const result = await generateImageWithRetry(apiUrl, payload);
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (base64Data) {
                    const newImageUrl = `data:image/png;base64,${base64Data}`;
                    img.src = newImageUrl;
                    generatedImages[index].url = newImageUrl;
                }
            } catch (error) {
                console.error('Error regenerating image:', error);
                img.src = originalSrc; // Restore original on error
                familyShowModal('Gagal regenerate gambar. Silakan coba lagi.');
            } finally {
                img.style.opacity = '1';
                img.style.filter = 'none';
            }
        }

        updateFileInputs(); // Initial call
    })();

    // --- FOOD SELFIE LOGIC ---
    (function() {
        const foodImageUploader = document.getElementById('food-selfie-food-image-uploader');
        const foodFileInput = document.getElementById('food-selfie-food-file-input');
        const foodUploadPrompt = document.getElementById('food-selfie-food-upload-prompt');
        const foodImagePreview = document.getElementById('food-selfie-food-image-preview');

        const styleImageUploader = document.getElementById('food-selfie-style-image-uploader');
        const styleFileInput = document.getElementById('food-selfie-style-file-input');
        const styleUploadPrompt = document.getElementById('food-selfie-style-upload-prompt');
        const styleImagePreview = document.getElementById('food-selfie-style-image-preview');

        // Background Upload Elements
        const backgroundInput = document.getElementById('food-selfie-background-input');
        const backgroundUploadArea = document.getElementById('food-selfie-background-upload-area');
        const backgroundPreview = document.getElementById('food-selfie-background-preview');
        const backgroundPreviewContainer = document.getElementById('food-selfie-background-preview-container');
        const removeBackgroundBtn = document.getElementById('food-selfie-remove-background');

        // Model Upload Elements
        const modelInput = document.getElementById('food-selfie-model-input');
        const modelUploadArea = document.getElementById('food-selfie-model-upload-area');
        const modelPreview = document.getElementById('food-selfie-model-preview');
        const modelPreviewContainer = document.getElementById('food-selfie-model-preview-container');
        const removeModelBtn = document.getElementById('food-selfie-remove-model');

        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const foodSelfieCountSel = document.getElementById('food-selfie-count-selection-grid');
        if (foodSelfieCountSel) {
            foodSelfieCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                foodSelfieCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        const themeSelection = document.getElementById('food-selfie-theme-selection');
        const customThemeContainer = document.getElementById('food-selfie-custom-theme-container');
        const customThemeInput = document.getElementById('food-selfie-custom-theme-input');
        const ratioSelection = document.getElementById('food-selfie-ratio-selection');
        const generateBtn = document.getElementById('food-selfie-generate-btn');
        const loader = document.getElementById('food-selfie-loader');
        const progressText = document.getElementById('food-selfie-progress-text');
        const progressBar = document.getElementById('food-selfie-progress-bar');
        const currentTaskText = document.getElementById('food-selfie-current-task');
        const placeholderResult = document.getElementById('food-selfie-placeholder-result');
        const resultContent = document.getElementById('food-selfie-result-content');
        const errorMessage = document.getElementById('food-selfie-error-message');
        const resultsHeader = document.getElementById('food-selfie-results-header');
        const resultCount = document.getElementById('food-selfie-result-count');
        const downloadAllBtn = document.getElementById('food-selfie-download-all-btn');

        let foodBase64Image = null;
        let styleBase64Image = null;
        let customBackgroundData = null;
        let customModelData = null;
        let foodMimeType = null;
        let styleMimeType = null;
        let selectedTheme = '';
        let selectedFoodSelfieRatio = '1:1';
        let generatedFoodImages = [];
        let currentGenerationParams = {};

        function setupUploader(uploader, input, preview, prompt, storageCallback) {
            if (!uploader) return;
            uploader.addEventListener('click', () => input.click());
            uploader.addEventListener('dragover', (e) => { e.preventDefault(); uploader.classList.add('border-green-400', 'bg-green-50'); });
            uploader.addEventListener('dragleave', () => { uploader.classList.remove('border-green-400', 'bg-green-50'); });
            uploader.addEventListener('drop', (e) => {
                e.preventDefault();
                uploader.classList.remove('border-green-400', 'bg-green-50');
                const files = e.dataTransfer.files;
                if (files.length > 0) {
                    input.files = files;
                    handleFileSelect({ target: input }, preview, prompt, storageCallback);
                }
            });
            input.addEventListener('change', (e) => handleFileSelect(e, preview, prompt, storageCallback));
        }

        function handleFileSelect(event, preview, prompt, storageCallback) {
            const file = event.target.files[0];
            if (file && file.type.startsWith('image/')) {
                const reader = new FileReader();
                reader.onloadend = () => {
                    const [header, base64] = reader.result.split(',');
                    const mimeType = header.match(/:(.*?);/)[1];
                    storageCallback(base64, mimeType);
                    preview.src = reader.result;
                    preview.classList.remove('hidden');
                    prompt.classList.add('hidden');
                    validateInputs();
                };
                reader.readAsDataURL(file);
            }
        }

        setupUploader(foodImageUploader, foodFileInput, foodImagePreview, foodUploadPrompt, (data, mime) => {
            foodBase64Image = data;
            foodMimeType = mime;
        });
        setupUploader(styleImageUploader, styleFileInput, styleImagePreview, styleUploadPrompt, (data, mime) => {
            styleBase64Image = data;
            styleMimeType = mime;
            validateInputs();
        });

        // Background Upload Handler with HEIC Support
        if (backgroundInput) {
            backgroundInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    customBackgroundData = reader.result;

                    // Show background preview
                    if (backgroundPreview && backgroundPreviewContainer && backgroundUploadArea) {
                        backgroundPreview.src = reader.result;
                        backgroundUploadArea.classList.add('hidden');
                        backgroundPreviewContainer.classList.remove('hidden');
                    }
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove Background Handler
        if (removeBackgroundBtn) {
            removeBackgroundBtn.addEventListener('click', () => {
                customBackgroundData = null;
                if (backgroundInput) backgroundInput.value = '';
                if (backgroundUploadArea) backgroundUploadArea.classList.remove('hidden');
                if (backgroundPreviewContainer) backgroundPreviewContainer.classList.add('hidden');
                if (backgroundPreview) backgroundPreview.src = '';
            });
        }

        // Model Upload Handler with HEIC Support
        if (modelInput) {
            modelInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    customModelData = reader.result;

                    // Show model preview
                    if (modelPreview && modelPreviewContainer && modelUploadArea) {
                        modelPreview.src = reader.result;
                        modelUploadArea.classList.add('hidden');
                        modelPreviewContainer.classList.remove('hidden');

                        // Auto-scroll to next section on mobile
                        setTimeout(() => {
                            if (window.innerWidth < 768 && ratioSelection) {
                                ratioSelection.scrollIntoView({ behavior: 'smooth', block: 'center' });
                            }
                        }, 300);
                    }
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove Model Handler
        if (removeModelBtn) {
            removeModelBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                customModelData = null;
                if (modelInput) modelInput.value = '';
                if (modelUploadArea) modelUploadArea.classList.remove('hidden');
                if (modelPreviewContainer) modelPreviewContainer.classList.add('hidden');
                if (modelPreview) modelPreview.src = '';
            });
        }

        // Mobile touch feedback for model upload area
        if (modelUploadArea) {
            modelUploadArea.addEventListener('touchstart', (e) => {
                modelUploadArea.style.transform = 'scale(0.98)';
            }, { passive: true });

            modelUploadArea.addEventListener('touchend', (e) => {
                setTimeout(() => {
                    modelUploadArea.style.transform = '';
                }, 100);
            }, { passive: true });
        }

        // Theme selection handler
        if (themeSelection) {
            themeSelection.addEventListener('click', (e) => {
                const button = e.target.closest('.theme-btn-food');
                if (button) {
                    document.querySelectorAll('#food-selfie-theme-selection .theme-btn-food').forEach(btn => btn.classList.remove('selected'));
                    button.classList.add('selected');
                    selectedTheme = button.dataset.theme;

                    // Show/hide custom theme input
                    if (customThemeContainer) {
                        if (selectedTheme === 'custom') {
                            customThemeContainer.classList.remove('hidden');
                        } else {
                            customThemeContainer.classList.add('hidden');
                        }
                    }
                }
            });
        }

        // Ratio selection handler
        if (ratioSelection) {
            ratioSelection.addEventListener('click', (e) => {
                const button = e.target.closest('.option-btn-food');
                if (button) {
                    document.querySelectorAll('#food-selfie-ratio-selection .option-btn-food').forEach(btn => btn.classList.remove('selected'));
                    button.classList.add('selected');
                    selectedFoodSelfieRatio = button.dataset.ratio;
                }
            });
        }

        function validateInputs() {
            if (generateBtn) {
                 generateBtn.disabled = !foodBase64Image;
            }
        }

        function showLoading(show) {
            if (show) {
                if (placeholderResult) placeholderResult.classList.add('hidden');
                if (loader) loader.classList.remove('hidden');
                if (resultsHeader) resultsHeader.classList.add('hidden');
                if (resultContent) resultContent.classList.add('hidden');
            } else {
                if (loader) loader.classList.add('hidden');
            }
        }

        function showError(message) {
            if (errorMessage && message) {
                errorMessage.textContent = message;
                errorMessage.classList.remove('hidden');
            } else if (errorMessage) {
                errorMessage.classList.add('hidden');
            }
        }

        async function generateImageWithRetry(payload, retries = 3, delay = 1000) {
           for (let i = 0; i < retries; i++) {
                try {
                    const apiKey = "";
                    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                    
                    const response = await fetch(apiUrl, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(payload)
                    });

                    const responseText = await response.text();

                    if (!response.ok) {
                        let errorMsg = response.statusText;
                        try {
                           const errorJson = JSON.parse(responseText);
                           errorMsg = errorJson.error?.message || errorMsg;
                        } catch(e) {
                           if(responseText) errorMsg = responseText;
                        }
                        throw new Error(`API Error: ${errorMsg}`);
                    }
                    
                    try {
                        return JSON.parse(responseText);
                    } catch (e) {
                        console.error("Failed to parse successful response:", responseText);
                        throw new Error("Received an invalid JSON response from the server.");
                    }

                } catch (error) {
                    console.error(`Attempt ${i + 1} failed:`, error);
                    if (i === retries - 1) return null;
                    await new Promise(res => setTimeout(res, delay * Math.pow(2, i)));
                }
            }
            return null;
        }

        if (generateBtn) generateBtn.addEventListener('click', async () => {
            validateInputs();
            if (generateBtn.disabled) {
                showError("Unggah foto makanan dan pilih tema atau gambar referensi.");
                return;
            }

            // Save current generation parameters
            currentGenerationParams = {
                foodBase64Image,
                foodMimeType,
                styleBase64Image,
                styleMimeType,
                selectedTheme,
                selectedFoodSelfieRatio
            };

            const originalBtnText = generateBtn.innerHTML;
            generateBtn.disabled = true;
            generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
            generateBtn.classList.add('opacity-75', 'cursor-not-allowed');

            showLoading(true);
            showError('');

            const baseStyles = [
                'Top-Down Flat Lay',
                'Classic 45-Degree Angle',
                'Dynamic Action Shot (e.g., pouring, steam)',
                'Close-up Macro Shot',
                'Minimalist Studio Shot (Clean Background)',
                'Bright Natural Window Light',
                'Moody Dark Background',
                'Diagonal Angle with Depth',
                'Side Profile View',
                'Overhead Birds Eye View',
                'Rustic Natural Lighting',
                'Studio White Background',
                'Warm Golden Hour Light',
                'Romantic Candlelight',
                'Urban Coffee Shop Aesthetic'
            ];
            // Shuffle supaya kombinasi gaya kamera beda tiap generate (tidak selalu 4 gaya pertama)
            const allStyles = (() => {
                const copy = [...baseStyles];
                for (let i = copy.length - 1; i > 0; i--) {
                    const j = Math.floor(Math.random() * (i + 1));
                    [copy[i], copy[j]] = [copy[j], copy[i]];
                }
                return copy;
            })();

            const MAX_ATTEMPTS = 3;
            let attempts = 0;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedFoodImages = new Array(selectedCount).fill(null);
                resultContent.innerHTML = '';
                if (resultsHeader) resultsHeader.classList.add('hidden');

                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateFoodImage(i + 1, allStyles[i])
                );
                await Promise.allSettled(generationPromises);

                successCount = generatedFoodImages.filter(Boolean).length;
            }

            showLoading(false);
            if (progressBar) progressBar.style.width = '0%';
            if (progressText) progressText.textContent = '';
            if (currentTaskText) currentTaskText.textContent = '';

            generateBtn.disabled = false;
            generateBtn.innerHTML = originalBtnText;
            generateBtn.classList.remove('opacity-75', 'cursor-not-allowed');

            if (successCount > 0) {
                renderSuccessfulFoodResults();
                setTimeout(() => {
                    if (window.innerWidth < 768 && resultsHeader) {
                        resultsHeader.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }
                }, 300);
            } else {
                placeholderResult.classList.remove('hidden');
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            }
        });

        function renderSuccessfulFoodResults() {
            const successful = generatedFoodImages.filter(Boolean);
            if (!successful.length) return;
            generatedFoodImages = successful;
            resultContent.innerHTML = '';
            successful.forEach((imgData, idx) => {
                const card = document.createElement('div');
                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 bg-white';
                card.innerHTML = `
                    <img src="${imgData.url}" class="w-full h-full object-cover aspect-square" alt="${imgData.style}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${idx}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${idx}" class="action-btn bg-gradient-to-r from-indigo-600 to-purple-600 hover:from-indigo-700 hover:to-purple-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                    <div class="absolute top-2 left-2 bg-black/70 text-white text-xs px-2 py-1 rounded-lg">
                        ${imgData.style}
                    </div>
                `;
                resultContent.appendChild(card);
            });
            if (resultsHeader) resultsHeader.classList.remove('hidden');
            if (resultCount) resultCount.textContent = successful.length;
            resultContent.classList.remove('hidden');
        }

        // Detail teknik fotografi profesional per gaya kamera (label pendek tetap dipakai di badge/filename)
        const foodStyleDetails = {
            'Top-Down Flat Lay': 'top-down 90-degree flat lay composition, balanced negative space, styled napkin and cutlery props, soft diffused overhead lighting',
            'Classic 45-Degree Angle': 'classic 45-degree hero angle, 50mm lens rendering, shallow depth of field, natural table setting with supporting props',
            'Dynamic Action Shot (e.g., pouring, steam)': 'dynamic action moment frozen at high shutter speed - pouring sauce, rising steam or dusting powder, dramatic backlight highlighting the motion',
            'Close-up Macro Shot': 'extreme macro close-up with 100mm macro lens rendering, razor-sharp texture detail, glistening surface highlights, creamy background bokeh',
            'Minimalist Studio Shot (Clean Background)': 'minimalist studio composition on seamless backdrop, single large softbox with smooth gradient falloff, commercial product-grade lighting',
            'Bright Natural Window Light': 'bright natural window side-light softened by sheer curtain, airy fresh mood, gentle organic shadows',
            'Moody Dark Background': 'low-key chiaroscuro lighting with single directional side light, deep shadows on dark slate or aged wood, fine-art editorial mood',
            'Diagonal Angle with Depth': 'diagonal leading-line composition with layered depth, foreground and background elements softly out of focus',
            'Side Profile View': 'straight-on side profile at table level showing the layers and height of the dish, subtle backlit rim light',
            'Overhead Birds Eye View': 'overhead birds-eye view of the full table spread, storytelling props arrangement, cohesive color palette',
            'Rustic Natural Lighting': 'rustic natural light on weathered wood, artisan linen and handmade ceramic props, warm earthy color grade',
            'Studio White Background': 'pure white seamless studio background, high-key even lighting, crisp catalog and menu-listing quality',
            'Warm Golden Hour Light': 'golden hour sunlight streaking low across the table, long soft shadows, glowing appetizing highlights',
            'Romantic Candlelight': 'intimate candlelight scene with warm flickering glow, soft-focus atmosphere, elegant evening table setting',
            'Urban Coffee Shop Aesthetic': 'urban specialty coffee shop ambience, window light with soft city bokeh, lifestyle editorial framing'
        };

        // Generate single food image
        async function generateFoodImage(index, style) {
            try {
                // Update progress
                if (progressText) {
                    progressText.textContent = `${index}/${selectedCount}`;
                }

                // Update progress bar
                if (progressBar) {
                    const percentage = (index / selectedCount) * 100;
                    progressBar.style.width = `${percentage}%`;
                }

                // Update current task
                if (currentTaskText) {
                    const taskDesc = customModelData
                        ? `Creating photo ${index} with person and ${style} style...`
                        : `Creating photo ${index} with ${style} style...`;
                    currentTaskText.textContent = taskDesc;
                }

                // Auto-scroll to loader on mobile when first image starts
                if (index === 1 && window.innerWidth < 768 && loader) {
                    setTimeout(() => {
                        loader.scrollIntoView({ behavior: 'smooth', block: 'center' });
                    }, 200);
                }

                let prompt = '';
                const parts = [];
                const styleDesc = foodStyleDetails[style] || style;

                // Get final theme (use custom input if custom theme selected)
                let finalTheme = selectedTheme;
                if (selectedTheme === 'custom' && customThemeInput && customThemeInput.value.trim()) {
                    finalTheme = customThemeInput.value.trim();
                } else if (selectedTheme === 'custom') {
                    finalTheme = 'Professional food photography';
                }

                // Check if custom background or model is uploaded
                if (customBackgroundData || customModelData) {
                    // With custom background/model - composite mode
                    const base64Background = customBackgroundData ? customBackgroundData.split(',')[1] : null;
                    const base64Model = customModelData ? customModelData.split(',')[1] : null;

                    let imageDescriptions = '';
                    let imageCount = 1;

                    if (base64Background) {
                        imageDescriptions += `IMAGE ${imageCount} (Background Reference) = The LOCATION/ENVIRONMENT where the food will be photographed\n`;
                        imageCount++;
                    }
                    imageDescriptions += `IMAGE ${imageCount} (Food Subject) = The FOOD ITEM to be featured in the photo\n`;
                    imageCount++;
                    if (base64Model) {
                        imageDescriptions += `IMAGE ${imageCount} (Model/Person Reference) = The PERSON/MODEL who will appear in the photo with the food\n`;
                    }

                    prompt = `Create a PHOTOREALISTIC food photography by seamlessly integrating these ${base64Background && base64Model ? 'three' : base64Model ? 'two' : 'two'} images:

${imageDescriptions}

YOUR TASK: Create a single, natural-looking photograph that ${base64Model ? 'MUST SHOW the PERSON from the Model image appearing with the food, holding, presenting, or enjoying it' : 'appears as if the food was actually photographed in this location'}${base64Background ? ' in the location from the background reference' : ''}. The result must be completely seamless and photorealistic.

${base64Model ? `
⚠️ CRITICAL REQUIREMENT - MODEL/PERSON MUST BE VISIBLE:
- The final photo MUST show the PERSON/MODEL from the Model image (not just hands)
- The person should be visible from chest-up (upper body) or full body, depending on composition
- The person should be naturally holding, presenting, or enjoying the food
- DO NOT show only hands - the PERSON's face, body, and presence must be clearly visible
- The person should occupy 40-60% of the frame, with food taking 30-40%
- Think lifestyle food photography or food blogger content - person with food, not just food alone
- The person's identity, face, clothing, and appearance from the original image must be preserved
` : ''}

PHOTOGRAPHY STYLE: ${styleDesc}
SCENE SETTING: ${finalTheme || 'Professional food photography'}

CRITICAL PHOTOREALISM REQUIREMENTS:

${base64Model ? `0. MODEL/PERSON INTEGRATION (MANDATORY - THIS IS THE MOST IMPORTANT REQUIREMENT):
   - ⚠️ THE PERSON FROM THE MODEL IMAGE MUST BE CLEARLY VISIBLE IN THE FINAL PHOTO
   - PRESERVE THE PERSON'S EXACT IDENTITY: Face, facial features, skin tone, hair, clothing style from original image
   - Show the person from chest-up (upper body portrait) or full body depending on scene
   - DO NOT crop to only show hands - the PERSON must be recognizable in the photo
   - Position the person naturally in the scene: sitting at table, holding food, or presenting dish
   - The person should be interacting with the food in a natural, lifestyle way
   - Match the person's clothing style to the theme/setting (e.g., casual for cafe, elegant for fine dining)
   - If theme requires specific attire, dress the person appropriately while preserving their identity
   - The person's pose should look natural and authentic for the food photography context
   - Create proper lighting on the person's face and body to match the scene
   - Eye contact can be: looking at food, looking at camera, or natural gaze depending on composition
   - COMPOSITION STYLES:
     * Portrait style: Person holding food/drink, smiling or enjoying it
     * Lifestyle style: Person sitting at table with food, casual and relaxed
     * Food blogger style: Person presenting the dish to camera
     * Dining style: Person about to eat or tasting the food
   - COMPOSITION RULE: Person 40-60% of frame, food 30-40%, background/setting 10-20%
   - The person and food should feel like they belong together in the same moment

` : ''}1. LIGHTING INTEGRATION (MOST IMPORTANT):
   ${base64Background ? '- Analyze the lighting direction, intensity, and color temperature from the Background Reference' : '- Create professional lighting that matches the scene setting'}
   - Apply IDENTICAL lighting to the food${base64Model ? ' AND person' : ''}
   - Match highlights, shadows, and ambient light perfectly
   - If warm lighting, food${base64Model ? ' and person' : ''} must have warm lighting too
   - If directional light (window/lamp), shadows must match that direction
   - Create natural light falloff and reflections on the food surface${base64Model ? ' and person\'s skin' : ''}
   ${base64Model ? '- Ensure the person\'s face is properly lit with natural highlights and shadows' : ''}

2. PERSPECTIVE & CAMERA ANGLE:
   ${base64Background ? '- Match the camera height and angle from the Background Reference' : '- Use natural lifestyle/food photography camera angle'}
   - Ensure food placement perspective fits the scene naturally
   - Scale the food appropriately for the scene (not too big, not too small)
   ${base64Model ? '- Position camera at eye-level or slightly above to capture both person and food naturally' : ''}
   ${base64Model ? '- Person should be positioned naturally in the scene (sitting, standing, or holding food)' : ''}
   ${base64Background ? '- Position food at realistic table/surface height matching the reference' : '- Create realistic table/surface height and depth'}

3. SHADOWS & REFLECTIONS:
   - Generate realistic shadows under and around the food${base64Model ? ' and person' : ''}
   ${base64Background ? '- Shadow direction MUST match light direction from Background Reference' : '- Shadow direction should match the scene\'s natural lighting'}
   - Shadow hardness/softness must match ambient lighting
   - If on glossy surface, add subtle reflections
   - No floating objects - food${base64Model ? ' and person' : ''} must interact with the surface
   ${base64Model ? '- Create contact shadows where person touches food, plate, or table' : ''}
   ${base64Model ? '- Person should cast realistic shadow on background/surface' : ''}

4. COLOR GRADING & ATMOSPHERE:
   ${base64Background ? '- Match the color grading/mood of Background Reference exactly' : '- Create cohesive color grading matching the scene setting'}
   - Apply same color temperature to the food${base64Model ? ' and person' : ''}
   - Maintain atmospheric consistency (warm/cool tones)
   - Blend color palette so food${base64Model ? ' and person feel' : ' feels'} part of the scene
   - Adjust food saturation to match scene's overall color intensity
   ${base64Model ? '- Person\'s skin tone and clothing colors should match the lighting and color temperature' : ''}

5. DEPTH OF FIELD & FOCUS:
   ${base64Background ? '- Match the depth of field from Background Reference' : '- Use natural depth of field for lifestyle food photography'}
   - Keep food in sharp focus while maintaining scene's blur characteristics
   ${base64Model ? '- Person\'s face should be in sharp focus, especially eyes and expression' : ''}
   ${base64Model ? '- If person is close to camera, use natural bokeh on background' : ''}
   - Background blur should be consistent
   - Create natural bokeh if appropriate

6. SURFACE INTEGRATION:
   - Food must sit ON the surface, not float
   - Show contact shadows where food touches surface
   ${base64Background ? '- Match surface texture interaction from reference (plate on wood table, etc)' : '- Create realistic surface texture interaction'}
   ${base64Background ? '- Food placement should follow table/surface plane from reference' : '- Create natural table/surface plane'}
   ${base64Model ? '- Person should be naturally positioned: sitting at table, standing near food, or holding plate/dish' : ''}
   ${base64Model ? '- Person\'s body and hands should interact naturally with the environment' : ''}

7. AMBIENT OCCLUSION & MICRO-DETAILS:
   - Add subtle darkening where food meets surface
   ${base64Model ? '- Add darkening where person touches food, table, or objects' : ''}
   - Include micro-shadows in crevices and edges
   ${base64Background ? '- Match the overall contrast ratio of Background Reference' : '- Create natural contrast ratio'}
   - Ensure no harsh edges or obvious compositing lines
   ${base64Model ? '- Smooth transition between person and background, no cut-out edges or halos' : ''}

COMPOSITION:
- Place food naturally within the scene (not dead center unless appropriate)
- Consider rule of thirds and natural placement
${base64Model ? '- Person should be positioned naturally: portrait style showing upper body, or lifestyle style showing person at table' : ''}
${base64Model ? '- Create natural interaction between person and food (holding, presenting, about to eat, enjoying)' : ''}
${base64Background ? '- Make sure food doesn\'t obstruct important scene elements from reference' : ''}
- Create a believable "${base64Model ? 'lifestyle food photography with person' : 'someone just placed this here'}" look

FINAL CHECK - The photo must look like:
✓ A single photograph taken in one moment
${base64Model ? '✓ ⚠️ MANDATORY: PERSON is CLEARLY VISIBLE with recognizable face and upper body (NOT just hands)' : ''}
${base64Model ? '✓ Person occupies 40-60% of the frame, food 30-40%, clearly recognizable and in focus' : ''}
${base64Model ? '✓ Person\'s FACE, identity, and features are preserved from original model image' : ''}
${base64Model ? '✓ Lifestyle/food blogger perspective showing person WITH food, naturally interacting' : ''}
${base64Background ? '✓ Food and background are from the same scene' : ''}
✓ Same lighting conditions throughout
✓ Natural, unedited, authentic food photography
${base64Model ? '✗ REJECTED: Close-up food shot without visible person' : ''}
${base64Model ? '✗ REJECTED: Only showing hands without face/body - person must be recognizable' : ''}
${base64Model ? '✗ REJECTED: Person cropped out or too small to see identity clearly' : ''}
${base64Model ? '✗ REJECTED: Face obscured or not visible - face must be clear and recognizable' : ''}
✗ NOT a composite or photoshopped image
✗ NO visible cut-out edges or halos around food${base64Model ? ' or person' : ''}
✗ NO mismatched lighting or shadows
✗ NO floating or pasted appearance
${base64Model ? '✗ NO disconnected person (person must naturally belong in the scene)' : ''}

TECHNICAL SPECS:
- Aspect ratio: ${selectedFoodSelfieRatio}
- High resolution, sharp focus on food${base64Model ? ' and person\'s face' : ''}
- Professional food photography quality
- Instagram/social media ready
${base64Model ? '- Lifestyle food photography or food blogger style' : ''}
${base64Model ? '- Person should look natural, relaxed, and authentic with the food' : ''}

The result MUST be indistinguishable from a real ${base64Model ? 'lifestyle food photography with person ' : ''}photograph taken at that location!`;

                    parts.push({ text: prompt });
                    if (base64Background) {
                        parts.push({ inlineData: { mimeType: 'image/jpeg', data: base64Background } });
                    }
                    parts.push({ inlineData: { mimeType: foodMimeType, data: foodBase64Image } });
                    if (base64Model) {
                        parts.push({ inlineData: { mimeType: 'image/jpeg', data: base64Model } });
                    }

                } else if (styleBase64Image) {
                    // With style reference image
                    prompt = `IMPORTANT: Your task is to place the food from the first image into the exact scene of the second image. The final output MUST perfectly replicate the composition, camera angle, and lighting from the second uploaded image. The setting and photographic style must be identical. The food from the first image is the new subject, but it must be integrated photorealistically into the replicated scene.`;
                    if (finalTheme) {
                        prompt += ` The overall mood should have a '${finalTheme}' feel.`;
                    }
                    parts.push({ text: prompt });
                    parts.push({ inlineData: { mimeType: foodMimeType, data: foodBase64Image } });
                    parts.push({ inlineData: { mimeType: styleMimeType, data: styleBase64Image } });
                } else {
                    // Without custom background or style reference - AI generated background
                    prompt = `You are an award-winning professional food photographer and food stylist. Create a professional, appetizing photograph featuring the EXACT food from the uploaded image.

PHOTOGRAPHY STYLE: ${styleDesc}
SCENE / THEME: ${finalTheme || 'professional commercial food photography'}

FOOD FIDELITY (MOST IMPORTANT):
- The food must stay 100% identical to the uploaded image: same dish, portion, plating, toppings, and colors
- Only the environment, camera angle, props, and lighting may change

PROFESSIONAL FOOD STYLING:
- Add supporting props (surface, tableware, garnish, ingredients) that complement the dish without stealing focus
- Appetizing enhancement: vibrant fresh colors, visible texture detail, gentle steam for hot food or condensation droplets for cold drinks where appropriate
- Styling level: professional food stylist for culinary magazines and premium restaurant menus

TECHNICAL QUALITY:
- Commercial-grade lighting with controlled highlights and soft natural shadows
- Sharp focus on the food, natural depth of field, ${selectedFoodSelfieRatio} composition
- Photorealistic result - must look like a real photograph from a professional camera, not CGI
- Quality benchmark: culinary magazine spread, high-end delivery app listing, premium restaurant menu`;

                    parts.push({ text: prompt });
                    parts.push({ inlineData: { mimeType: foodMimeType, data: foodBase64Image } });
                }

                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedFoodSelfieRatio }
                    }
                };

                const result = await generateImageWithRetry(payload);

                const imagePart = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData);
                const base64Data = imagePart?.inlineData?.data;

                if (!base64Data) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const fileName = `food_${style.replace(/[^a-z0-9]/gi, '_').toLowerCase()}_${index}.png`;
                generatedFoodImages[index - 1] = { url: imageUrl, filename: fileName, style };
            } catch (error) {
                console.error(`Error generating food image ${index}:`, error);
            }
        }

        // Event delegation for result actions (works on desktop & mobile)
        if (resultContent) {
            resultContent.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedFoodImages[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all button handler (works on desktop & mobile)
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedFoodImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedFoodImages.length; i++) {
                        const img = generatedFoodImages[i];
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedFoodImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        validateInputs();
    })();

    // --- NEW BORN STUDIO LOGIC ---
    (function() {
        const imageUpload = document.getElementById('new-born-image-upload');
        const uploadButton = document.getElementById('new-born-upload-button');
        const imagePreview = document.getElementById('new-born-image-preview');
        const previewText = document.getElementById('new-born-preview-text');
        const generateButton = document.getElementById('new-born-generate-button');
        const loadingIndicator = document.getElementById('new-born-loading-indicator');
        const resultsGrid = document.getElementById('new-born-results-grid');
        const resultsHeader = document.getElementById('new-born-results-header');
        const downloadAllBtn = document.getElementById('new-born-download-all-btn');
        const errorMessage = document.getElementById('new-born-error-message');
        const customThemeInput = document.getElementById('new-born-custom-theme');
        const genderRadios = document.querySelectorAll('#content-new-born input[name="gender"]');
        const ratioOptions = document.getElementById('newborn-ratio-options');
        const refUpload = document.getElementById('new-born-ref-upload');
        const refUploadButton = document.getElementById('new-born-ref-upload-button');
        const refPreview = document.getElementById('new-born-ref-preview');
        const refPreviewText = document.getElementById('new-born-ref-preview-text');
        const refClearBtn = document.getElementById('new-born-ref-clear');

        let base64ImageData = null;
        let refBase64ImageData = null;
        let selectedRatio = '3:4'; // Default aspect ratio
        let generatedImages = []; // Store generated images

        if (uploadButton) {
            uploadButton.addEventListener('click', () => imageUpload.click());
        }

        if (refUploadButton) {
            refUploadButton.addEventListener('click', () => refUpload.click());
        }

        if (refUpload) {
            refUpload.addEventListener('change', event => {
                const file = event.target.files[0];
                if (file) {
                    const reader = new FileReader();
                    reader.onload = e => {
                        refPreview.src = e.target.result;
                        refPreview.classList.remove('hidden');
                        refPreviewText.classList.add('hidden');
                        refClearBtn.classList.remove('hidden');
                        refBase64ImageData = e.target.result.split(',')[1];
                    };
                    reader.readAsDataURL(file);
                }
            });
        }

        if (refClearBtn) {
            refClearBtn.addEventListener('click', () => {
                refBase64ImageData = null;
                refPreview.src = '';
                refPreview.classList.add('hidden');
                refPreviewText.classList.remove('hidden');
                refClearBtn.classList.add('hidden');
                if (refUpload) refUpload.value = '';
            });
        }

        // --- Mode selector (tema / milestone / kartu) ---
        let nbMode = 'tema';
        let selectedThemeCat = 'random';
        let selectedMilestoneAge = null;
        let milestoneAgePicked = false;
        let selectedMilestoneProps = 'random';
        let selectedKartuStyle = 'pastel';

        const nbModeSelection = document.getElementById('new-born-mode-selection');
        const nbTemaSection = document.getElementById('new-born-tema-section');
        const nbMilestoneSection = document.getElementById('new-born-milestone-section');
        const nbKartuSection = document.getElementById('new-born-kartu-section');
        const nbCompositeToggle = document.getElementById('new-born-composite-toggle');
        const nbDobInput = document.getElementById('new-born-dob');

        if (nbModeSelection) {
            nbModeSelection.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-mode]');
                if (!btn) return;
                nbModeSelection.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                nbMode = btn.dataset.mode;
                if (nbTemaSection) nbTemaSection.classList.toggle('hidden', nbMode !== 'tema');
                if (nbMilestoneSection) nbMilestoneSection.classList.toggle('hidden', nbMode !== 'milestone');
                if (nbKartuSection) nbKartuSection.classList.toggle('hidden', nbMode !== 'kartu');
                const nbRefBlock = document.getElementById('new-born-ref-block');
                if (nbRefBlock) nbRefBlock.classList.toggle('hidden', nbMode !== 'tema');
                if (nbMode === 'milestone') updateNewbornAgeInfo();
            });
        }

        // --- Kategori tema preset + zodiak ---
        const nbThemeCatSelection = document.getElementById('new-born-theme-cat-selection');
        const nbZodiakInfo = document.getElementById('new-born-zodiak-info');
        if (nbThemeCatSelection) {
            nbThemeCatSelection.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-theme-cat]');
                if (!btn) return;
                nbThemeCatSelection.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedThemeCat = btn.dataset.themeCat;
                updateNewbornZodiakInfo();
            });
        }

        function updateNewbornZodiakInfo() {
            if (!nbZodiakInfo) return;
            if (selectedThemeCat !== 'zodiak') { nbZodiakInfo.classList.add('hidden'); return; }
            const zod = getNewbornZodiac(nbDobInput ? nbDobInput.value : '');
            if (zod) {
                nbZodiakInfo.textContent = window.tr3('Zodiak bayi: ' + zod, 'Baby zodiac: ' + zod, 'Zodiak bayi: ' + zod);
            } else {
                nbZodiakInfo.textContent = window.tr3('Isi tanggal lahir dulu untuk deteksi zodiak', 'Fill in the date of birth first to detect the zodiac', 'Isi tarikh lahir dahulu untuk kesan zodiak');
            }
            nbZodiakInfo.classList.remove('hidden');
        }

        const nbZodiacTable = [
            { name: 'Aquarius', from: [1, 20] }, { name: 'Pisces', from: [2, 19] },
            { name: 'Aries', from: [3, 21] }, { name: 'Taurus', from: [4, 20] },
            { name: 'Gemini', from: [5, 21] }, { name: 'Cancer', from: [6, 21] },
            { name: 'Leo', from: [7, 23] }, { name: 'Virgo', from: [8, 23] },
            { name: 'Libra', from: [9, 23] }, { name: 'Scorpio', from: [10, 23] },
            { name: 'Sagittarius', from: [11, 22] }, { name: 'Capricorn', from: [12, 22] }
        ];
        function getNewbornZodiac(dateStr) {
            if (!dateStr) return null;
            const d = new Date(dateStr);
            if (isNaN(d)) return null;
            const m = d.getMonth() + 1, day = d.getDate();
            let name = 'Capricorn';
            for (const z of nbZodiacTable) {
                if (m > z.from[0] || (m === z.from[0] && day >= z.from[1])) name = z.name;
            }
            return name;
        }

        // --- Milestone: umur auto dari tanggal lahir + chips manual ---
        const nbAgeGrid = document.getElementById('new-born-milestone-age-grid');
        const nbAgeInfo = document.getElementById('new-born-age-info');
        if (nbAgeGrid) {
            nbAgeGrid.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-age]');
                if (!btn) return;
                nbAgeGrid.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedMilestoneAge = btn.dataset.age;
                milestoneAgePicked = true;
            });
        }

        function updateNewbornAgeInfo() {
            if (!nbAgeInfo) return;
            const dob = nbDobInput ? nbDobInput.value : '';
            if (!dob) { nbAgeInfo.classList.add('hidden'); return; }
            const d = new Date(dob);
            if (isNaN(d)) { nbAgeInfo.classList.add('hidden'); return; }
            const days = Math.floor((Date.now() - d.getTime()) / 86400000);
            if (days < 0) { nbAgeInfo.classList.add('hidden'); return; }
            const months = Math.floor(days / 30.44);
            nbAgeInfo.textContent = window.tr3(
                'Usia bayi saat ini: ' + months + ' bulan (' + days + ' hari)',
                'Current baby age: ' + months + ' months (' + days + ' days)',
                'Usia bayi sekarang: ' + months + ' bulan (' + days + ' hari)'
            );
            nbAgeInfo.classList.remove('hidden');
            if (!milestoneAgePicked && nbAgeGrid) {
                let autoAge = 'newborn';
                if (months >= 1 && months <= 12) autoAge = String(months);
                else if (months > 12) autoAge = '1tahun';
                const autoBtn = nbAgeGrid.querySelector('button[data-age="' + autoAge + '"]');
                if (autoBtn) {
                    nbAgeGrid.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                    autoBtn.classList.add('selected');
                    selectedMilestoneAge = autoAge;
                }
            }
        }

        if (nbDobInput) {
            nbDobInput.addEventListener('change', () => {
                updateNewbornAgeInfo();
                updateNewbornZodiakInfo();
            });
        }

        const nbPropsGrid = document.getElementById('new-born-milestone-props-grid');
        if (nbPropsGrid) {
            nbPropsGrid.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-props]');
                if (!btn) return;
                nbPropsGrid.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedMilestoneProps = btn.dataset.props;
            });
        }

        // --- Kartu Ucapan: gaya kartu ---
        const nbKartuStyleGrid = document.getElementById('new-born-kartu-style-grid');
        if (nbKartuStyleGrid) {
            nbKartuStyleGrid.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-kartu-style]');
                if (!btn) return;
                nbKartuStyleGrid.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedKartuStyle = btn.dataset.kartuStyle;
            });
        }

        if (imageUpload) {
            imageUpload.addEventListener('change', event => {
                const file = event.target.files[0];
                if (file) {
                    const reader = new FileReader();
                    reader.onload = e => {
                        imagePreview.src = e.target.result;
                        imagePreview.classList.remove('hidden');
                        previewText.classList.add('hidden');
                        base64ImageData = e.target.result.split(',')[1];
                    };
                    reader.readAsDataURL(file);
                    showError(''); // Clear previous error
                }
            });
        }

        // Ratio button selection handler
        if (ratioOptions) {
            ratioOptions.addEventListener('click', (e) => {
                const button = e.target.closest('.option-btn-newborn');
                if (button) {
                    ratioOptions.querySelectorAll('.option-btn-newborn').forEach(btn => btn.classList.remove('selected'));
                    button.classList.add('selected');
                    selectedRatio = button.dataset.value;
                }
            });
        }

        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const newbornCountSel = document.getElementById('new-born-count-selection-grid');
        if (newbornCountSel) {
            newbornCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                newbornCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        if (generateButton) {
            generateButton.addEventListener('click', async () => {
                if (!base64ImageData) {
                    showError("Harap pilih foto bayi terlebih dahulu.");
                    return;
                }
                if (nbMode === 'kartu' && !document.getElementById('new-born-name').value.trim()) {
                    showError(window.tr3('Isi nama bayi dulu untuk Kartu Ucapan.', 'Fill in the baby name first for the announcement card.', 'Isi nama bayi dahulu untuk Kad Ucapan.'));
                    return;
                }
                if (nbMode === 'milestone') {
                    if (!selectedMilestoneAge) updateNewbornAgeInfo();
                    if (!selectedMilestoneAge) {
                        showError(window.tr3('Pilih usia milestone atau isi tanggal lahir dulu.', 'Pick a milestone age or fill in the date of birth first.', 'Pilih usia milestone atau isi tarikh lahir dahulu.'));
                        return;
                    }
                }
                showError('');
                loadingIndicator.classList.remove('hidden');
                generateButton.disabled = true;
                generateButton.textContent = 'Memproses...';

                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedImages = [];
                    resultsGrid.innerHTML = '';

                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `new-born-card-${i}`;
                        card.className = 'relative group aspect-w-1 aspect-h-1 bg-gray-200 rounded-lg overflow-hidden shadow-md flex items-center justify-center';
                        card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-indigo-600"></div>';
                        resultsGrid.appendChild(card);
                    }

                    const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                        generateSingleImage(i + 1)
                    );
                    await Promise.allSettled(generationPromises);
                    successCount = resultsGrid.querySelectorAll('img').length;
                }

                resultsGrid.querySelectorAll('[id^="new-born-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                loadingIndicator.classList.add('hidden');
                generateButton.disabled = false;
                generateButton.textContent = 'Generate Foto Ajaib';

                if (generatedImages.filter(Boolean).length > 0 && resultsHeader) {
                    resultsHeader.classList.remove('hidden');
                    const resultCount = document.getElementById('new-born-result-count');
                    if (resultCount) resultCount.textContent = generatedImages.filter(Boolean).length;
                }

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                }
            });
        }

        function showError(message) {
            if (message) {
                errorMessage.textContent = message;
                errorMessage.classList.remove('hidden');
            } else {
                errorMessage.classList.add('hidden');
            }
        }
        
        function getRandomPrompt() {
            // Check for custom theme first
            const customTheme = customThemeInput ? customThemeInput.value.trim() : '';

            const babyName = document.getElementById('new-born-name').value.trim();
            const babyDob = document.getElementById('new-born-dob').value;
            let detailsPrompt = '';
            if (babyName || babyDob) {
                let details = [];
                if (babyName) details.push(`the name "${babyName}"`);
                if (babyDob) {
                    const dateObj = new Date(babyDob);
                    if (!isNaN(dateObj)) {
                        const formattedDate = dateObj.toLocaleDateString('en-US', {
                            day: 'numeric', month: 'long', year: 'numeric'
                        });
                        details.push(`the date "${formattedDate}"`);
                    }
                }
                if (details.length > 0) {
                     detailsPrompt = ` Artistically include ${details.join(' and ')} as part of the photo's props (e.g., carved on wooden blocks, elegant calligraphy on a blanket), not as a digital text overlay.`;
                }
            }

            // Mode Milestone / Kartu Ucapan pakai prompt builder sendiri (identity lock)
            if (nbMode === 'milestone') {
                return buildNewbornMilestonePrompt(detailsPrompt);
            }
            if (nbMode === 'kartu') {
                return buildNewbornKartuPrompt();
            }

            // If a reference image is provided, recreate its theme/style with the baby from the first image
            if (refBase64ImageData) {
                const themeNote = customTheme ? ` Additional theme notes: ${customTheme}.` : '';
                return `Ultra-realistic, high-quality newborn studio photo of the baby from the first image. Recreate the theme, costume, props, pose, composition, background, and lighting of the second reference image as closely as possible, but the baby must be the baby from the first image.${themeNote}${detailsPrompt} The baby's face must match the first image: natural, cute, and the main focus. Important: 1080p quality, hyperrealistic.`;
            }

            // Kolase 4-in-1: satu gambar berisi 4 tema dalam grid 2x2
            if (nbCompositeToggle && nbCompositeToggle.checked) {
                return buildNewbornCompositePrompt(customTheme, detailsPrompt);
            }

            // If custom theme is provided, use it directly
            if (customTheme) {
                const finalPrompt = `Ultra-realistic, high-quality newborn studio photo of the provided baby. Theme: ${customTheme}.${detailsPrompt} Photo style: professional studio photography with natural skin texture and detail. The baby's face must be natural, cute, and the main focus. Important: 1080p quality, hyperrealistic.`;
                return finalPrompt;
            }

            // Kategori tema preset (profesi/dongeng/adat/islami/seasonal/zodiak)
            const catTheme = pickNewbornCategoryTheme();
            if (catTheme) {
                return `Ultra-realistic, high-quality newborn studio photo of the provided baby. Theme: ${catTheme}.${detailsPrompt} Photo style: professional studio photography with natural skin texture and detail. The baby's face must be natural, cute, and the main focus. Important: 1080p quality, hyperrealistic.`;
            }

            // Otherwise, use random theme logic
            const maleThemes = ['superhero cilik', 'astronot penjelajah galaksi', 'petualang safari', 'pangeran kerajaan awan', 'pilot pesawat jet mainan', 'pembalap mobil mini'];
            const femaleThemes = ['peri di hutan ajaib', 'putri negeri dongeng', 'penari balet cilik', 'putri duyung di laut berkilau', 'gadis bunga di taman ajaib'];
            const neutralThemes = ['koki cilik di dapur mini', 'penyelam laut dalam', 'musisi kecil dengan gitar mini', 'pelukis dengan kanvas warna-warni', 'bayi hewan lucu (kostum singa/panda)', 'kurcaci di taman jamur'];
            const settings = ['di atas bulan sabit', 'di dalam buku cerita raksasa', 'terbang di antara awan permen kapas', 'di taman bunga ajaib', 'di planet penuh bintang', 'di istana bawah laut', 'di dapur yang nyaman', 'di tengah hutan belantara yang ceria', 'di kokpit pesawat luar angkasa mainan'];
            const styles = ['foto studio profesional high-end', 'sinematik, pencahayaan dramatis', 'hyperrealistic, detail sangat tajam', 'gaya fotografi potret profesional', 'cahaya lembut dan alami seperti di dekat jendela', 'pencahayaan high-key studio yang bersih dan cerah'];
            const elements = ['dikelilingi bintang berkelip', 'dengan kupu-kupu bercahaya', 'memegang balon berbentuk planet', 'duduk di atas jamur raksasa', 'menunggangi kura-kura laut yang ramah', 'tertawa di antara gelembung sabun'];

            const gender = document.querySelector('#content-new-born input[name="gender"]:checked').value;
            let availableThemes = (gender === 'laki-laki') ? [...maleThemes, ...neutralThemes] : [...femaleThemes, ...neutralThemes];

            const randomTheme = availableThemes[Math.floor(Math.random() * availableThemes.length)];
            const randomSetting = settings[Math.floor(Math.random() * settings.length)];
            const randomStyle = styles[Math.floor(Math.random() * styles.length)];
            const randomElement = elements[Math.floor(Math.random() * elements.length)];

            const finalPrompt = `Ultra-realistic, high-quality newborn studio photo of the provided baby. Theme: ${randomTheme}. The baby is ${randomSetting}, ${randomElement}.${detailsPrompt} Photo style: ${randomStyle}, with natural skin texture and detail. The baby's face must be natural, cute, and the main focus. Important: 1080p quality, hyperrealistic.`;

            return finalPrompt;
        }

        // --- Pool tema preset per kategori ---
        const nbThemeCategories = {
            profesi: [
                'tiny doctor with a miniature white coat and cute stethoscope, pastel clinic backdrop',
                'little pilot with a mini aviator jacket and toy airplane, soft cloud backdrop',
                'baby chef with a mini chef hat surrounded by soft felt vegetables in a cozy toy kitchen',
                'little police officer with a tiny uniform and soft toy car, friendly studio set',
                'baby astronaut in a plush spacesuit surrounded by felt stars and planets',
                'little firefighter with a mini helmet and plush dalmatian puppy, soft red accents',
                'baby farmer with a tiny straw hat, basket of plush fruits, warm rustic backdrop'
            ],
            dongeng: [
                'fairy in a magical glowing forest with soft butterflies and mushrooms',
                'storybook prince or princess on a plush cloud throne with a tiny crown',
                'little superhero with a soft cape flying between cotton clouds',
                'mermaid resting on a shimmering pearl shell in a soft underwater dreamscape',
                'tiny gnome sleeping under a giant plush mushroom in an enchanted garden',
                'baby dragon keeper with a cute plush dragon and castle backdrop'
            ],
            adat: [
                'wearing a miniature Javanese traditional outfit with soft batik wrap and tiny blangkon, elegant classic Javanese studio backdrop',
                'wearing a miniature Minang traditional outfit with soft songket accents, elegant West Sumatran backdrop',
                'wearing a miniature Sundanese traditional outfit with soft kebaya-inspired wrap, warm classic backdrop',
                'wearing a miniature Balinese traditional outfit with soft udeng headband and frangipani flowers, serene Balinese backdrop',
                'wearing a miniature Bugis traditional outfit with rich colored soft fabrics, elegant South Sulawesi backdrop',
                'wrapped in a soft Batak ulos-inspired woven blanket, warm traditional North Sumatran backdrop'
            ],
            islami: [
                'aqiqah celebration setup with soft green and gold accents, elegant Islamic geometric patterns',
                'wearing a tiny soft peci and sarong, peaceful mosque silhouette backdrop with warm lanterns',
                'wrapped in a soft prayer-themed blanket with beautiful Arabic calligraphy decor (accurate, elegant)',
                'Ramadan theme with soft crescent moon pillow, star garlands and gentle lantern glow',
                'soft white and gold Islamic theme with miniature tasbih beads and elegant ornaments',
                'sleeping on a soft cloud with a tiny mosque dome silhouette and twinkling star backdrop'
            ],
            seasonal: [
                'Lebaran theme with soft ketupat decorations, pastel green tones and festive ribbons',
                'cozy Christmas theme with soft knitted stocking, warm fairy lights and plush reindeer',
                'Chinese New Year theme with soft red lanterns and gold accents, cheerful festive backdrop',
                'New Year celebration theme with soft confetti, pastel balloons and a tiny "Happy New Year" banner',
                'Indonesian Independence Day theme with soft red and white decorations and tiny flag garlands',
                'birthday celebration theme with a soft smash cake, pastel bunting and balloons'
            ]
        };

        const nbZodiacThemes = {
            'Aries': 'little Aries zodiac baby with a cute plush ram, warm coral tones and tiny star garlands',
            'Taurus': 'little Taurus zodiac baby with a soft plush bull, earthy green tones and flower accents',
            'Gemini': 'little Gemini zodiac baby with twin plush dolls, playful yellow pastel tones and stars',
            'Cancer': 'little Cancer zodiac baby with a cute plush crab, soft silver moonlight tones and seashells',
            'Leo': 'little Leo zodiac baby with a cuddly plush lion and golden sun accents',
            'Virgo': 'little Virgo zodiac baby with soft wheat and flower arrangements, gentle earthy pastel tones',
            'Libra': 'little Libra zodiac baby with a cute balanced scale ornament, airy pink and blue pastels',
            'Scorpio': 'little Scorpio zodiac baby with a friendly plush scorpion, deep red and plum soft tones',
            'Sagittarius': 'little Sagittarius zodiac baby with a tiny bow and arrow prop and plush horse, adventurous purple tones',
            'Capricorn': 'little Capricorn zodiac baby with a soft plush mountain goat, cozy winter earth tones',
            'Aquarius': 'little Aquarius zodiac baby with soft water wave fabrics and star constellations, aqua blue tones',
            'Pisces': 'little Pisces zodiac baby with two cute plush fish, dreamy sea-green and lavender tones'
        };

        function pickNewbornCategoryTheme() {
            if (selectedThemeCat === 'zodiak') {
                const zod = getNewbornZodiac(nbDobInput ? nbDobInput.value : '');
                return zod ? nbZodiacThemes[zod] : null;
            }
            const pool = nbThemeCategories[selectedThemeCat];
            if (!pool || !pool.length) return null;
            return pool[Math.floor(Math.random() * pool.length)];
        }

        function buildNewbornCompositePrompt(customTheme, detailsPrompt) {
            let pool;
            if (selectedThemeCat === 'zodiak') {
                const zod = getNewbornZodiac(nbDobInput ? nbDobInput.value : '');
                pool = zod ? [nbZodiacThemes[zod]] : Object.values(nbThemeCategories).flat();
            } else if (nbThemeCategories[selectedThemeCat]) {
                pool = nbThemeCategories[selectedThemeCat];
            } else {
                pool = Object.values(nbThemeCategories).flat();
            }
            const shuffled = [...pool].sort(() => Math.random() - 0.5);
            const picks = [];
            if (customTheme) picks.push(customTheme);
            for (const t of shuffled) {
                if (picks.length >= 4) break;
                if (!picks.includes(t)) picks.push(t);
            }
            while (picks.length < 4) {
                picks.push(shuffled[Math.floor(Math.random() * shuffled.length)] || customTheme);
            }
            return `Create ONE single image that is a seamless 2x2 grid collage of FOUR different professional newborn studio photos of the SAME baby from the provided photo. The baby's face and identity must be EXACTLY the same in all four quadrants — natural, cute, and the main focus. Quadrant themes — top-left: ${picks[0]}; top-right: ${picks[1]}; bottom-left: ${picks[2]}; bottom-right: ${picks[3]}. Thin clean white borders between quadrants, consistent professional studio lighting across all quadrants.${detailsPrompt} Important: 1080p quality, hyperrealistic.`;
        }

        function getNewbornMilestoneAgeText() {
            if (selectedMilestoneAge === 'newborn') return 'Newborn';
            if (selectedMilestoneAge === '100hari') return window.tr3('100 Hari', '100 Days', '100 Hari');
            if (selectedMilestoneAge === '1tahun') return window.tr3('1 Tahun', '1 Year', '1 Tahun');
            const n = parseInt(selectedMilestoneAge, 10);
            return window.tr3(n + ' Bulan', n + (n > 1 ? ' Months' : ' Month'), n + ' Bulan');
        }

        function buildNewbornMilestonePrompt(detailsPrompt) {
            const propsMap = {
                rajut: 'soft knitted number pillows and a chunky knit blanket',
                kayu: 'a rustic wooden milestone card with elegant engraved lettering',
                balon: 'pastel balloons and a small balloon garland',
                bunga: 'a delicate flower wreath and soft floral arrangements'
            };
            let propsDesc = propsMap[selectedMilestoneProps];
            if (!propsDesc) {
                const keys = Object.keys(propsMap);
                propsDesc = propsMap[keys[Math.floor(Math.random() * keys.length)]];
            }
            const ageText = getNewbornMilestoneAgeText();
            return `Professional baby monthly milestone studio photo. Keep the baby EXACTLY as in the provided photo — same face, same expression, same skin tone, do NOT alter the baby's identity. Only change the scene around the baby: a cozy professional studio setup with ${propsDesc}, clearly displaying the milestone text "${ageText}" with EXACT correct spelling as part of the props (carved, knitted, or printed — never a digital text overlay). Soft natural window lighting, warm tones, high-end baby photography.${detailsPrompt} Important: 1080p quality, hyperrealistic.`;
        }

        function buildNewbornKartuPrompt() {
            const styleMap = {
                pastel: 'soft elegant pastel palette, dreamy clouds and tiny gold stars, delicate gold accents',
                aqiqah: 'elegant Islamic ornamental frame with soft arabesque patterns and crescent moon accents, include the heading text "Tasyakuran Aqiqah"',
                floral: 'romantic watercolor florals framing the design, blush pink and sage green tones',
                minimalis: 'clean minimalist layout with generous white space and thin modern typography accents'
            };
            const name = document.getElementById('new-born-name').value.trim();
            const dob = nbDobInput ? nbDobInput.value : '';
            const weightEl = document.getElementById('new-born-weight');
            const lengthEl = document.getElementById('new-born-length');
            const weight = weightEl ? weightEl.value.trim() : '';
            const length = lengthEl ? lengthEl.value.trim() : '';
            const textParts = [`the baby's name "${name}" in beautiful large calligraphy`];
            if (dob) {
                const dateObj = new Date(dob);
                if (!isNaN(dateObj)) {
                    const locale = window.tr3('id-ID', 'en-US', 'ms-MY');
                    const formattedDate = dateObj.toLocaleDateString(locale, { day: 'numeric', month: 'long', year: 'numeric' });
                    textParts.push(`the birth date "${formattedDate}"`);
                }
            }
            if (weight) textParts.push(`the weight "${weight} kg"`);
            if (length) textParts.push(`the length "${length} cm"`);
            return `Elegant premium baby birth announcement card design featuring the provided baby photo as the centerpiece. Keep the baby EXACTLY as in the photo — same face, do NOT alter the baby's identity. Card style: ${styleMap[selectedKartuStyle] || styleMap.pastel}. Beautifully and accurately render this text on the card: ${textParts.join(', ')}. Every word and number must be spelled EXACTLY as given, in elegant typography. Premium printable invitation card quality, soft studio lighting on the baby. Important: 1080p quality.`;
        }

        async function generateSingleImage(index) {
            const card = document.getElementById(`new-born-card-${index}`);
            try {
                const userPrompt = getRandomPrompt();
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const newbornParts = [ { text: userPrompt }, { inlineData: { mimeType: "image/png", data: base64ImageData } } ];
                if (refBase64ImageData && nbMode === 'tema') {
                    newbornParts.push({ inlineData: { mimeType: "image/png", data: refBase64ImageData } });
                }
                const payload = {
                    contents: [{ parts: newbornParts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const filename = `bayi-ajaib-${Date.now()}-${Math.random().toString(36).substr(2, 9)}.png`;

                generatedImages[index - 1] = { url: imageUrl, filename: filename };

                if (card) {
                    card.className = 'relative group aspect-w-1 aspect-h-1 bg-gray-200 rounded-lg overflow-hidden shadow-md';
                    card.innerHTML = `
                        <img src="${imageUrl}" alt="Generated baby photo ${index}" class="w-full h-full object-cover transform group-hover:scale-105 transition-transform duration-300"/>
                        <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/30 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 rounded-lg flex flex-col justify-end p-4 gap-2">
                            <div class="flex gap-2">
                                <button data-action="newborn-preview" data-index="${index - 1}" class="action-btn flex-1 bg-blue-500 hover:bg-blue-600 text-white px-3 py-2 rounded-lg text-sm font-semibold transition flex items-center justify-center gap-1">
                                    <i class="fas fa-eye"></i>
                                    <span class="hidden sm:inline">Preview</span>
                                </button>
                                <button data-action="newborn-download" data-index="${index - 1}" class="action-btn flex-1 bg-indigo-600 hover:bg-indigo-700 text-white px-3 py-2 rounded-lg text-sm font-semibold transition flex items-center justify-center gap-1">
                                    <i class="fas fa-download"></i>
                                    <span class="hidden sm:inline">Download</span>
                                </button>
                            </div>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`New born image ${index} error:`, error);
                if (card) card.innerHTML = '';
            }
        }

        function displayImage(imageUrl, index) {
            const imgContainer = document.createElement('div');
            imgContainer.className = 'relative group aspect-w-1 aspect-h-1 bg-gray-200 rounded-lg overflow-hidden shadow-md';

            imgContainer.innerHTML = `
                <img src="${imageUrl}" alt="Generated baby photo ${index + 1}" class="w-full h-full object-cover transform group-hover:scale-105 transition-transform duration-300"/>
                <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/30 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 rounded-lg flex flex-col justify-end p-4 gap-2">
                    <div class="flex gap-2">
                        <button data-action="newborn-preview" data-index="${index}" class="action-btn flex-1 bg-blue-500 hover:bg-blue-600 text-white px-3 py-2 rounded-lg text-sm font-semibold transition flex items-center justify-center gap-1">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="newborn-download" data-index="${index}" class="action-btn flex-1 bg-indigo-600 hover:bg-indigo-700 text-white px-3 py-2 rounded-lg text-sm font-semibold transition flex items-center justify-center gap-1">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                </div>
            `;

            resultsGrid.appendChild(imgContainer);
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'newborn-preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'newborn-download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all functionality
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async () => {
                if (generatedImages.length === 0) return;

                downloadAllBtn.disabled = true;
                downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < generatedImages.length; i++) {
                    const img = generatedImages[i];
                    if (!img) continue;  // Skip failed/empty slots
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                        // Small delay between downloads for better UX
                        await new Promise(resolve => setTimeout(resolve, 500));
                    } else if (window.downloadImage) {
                        await window.downloadImage(img.url, img.filename);
                        // Small delay between downloads for better UX
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }
                }

                downloadAllBtn.disabled = false;
                downloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
            });

            // Add mobile touch support
            if (window.addMobileTouchSupport) {
                window.addMobileTouchSupport(downloadAllBtn);
            }
        }
    })();

    // ==================== FOTO PROFESI ANAK - PROFESSIONAL VERSION ====================
    (function() {
        const apiKey = "";

        // DOM Elements - Upload & Preview
        const childUpload = document.getElementById('profesi-child-upload');
        const childUploadBox = document.getElementById('profesi-child-upload-box');
        const childPlaceholder = document.getElementById('profesi-child-placeholder');
        const childPreviewContainer = document.getElementById('profesi-child-preview-container');
        const childPreview = document.getElementById('profesi-child-preview');
        const removeChildPreview = document.getElementById('profesi-remove-child-preview');

        // DOM Elements - Options
        const ageSelect = document.getElementById('profesi-age-select');
        const themeInput = document.getElementById('profesi-theme-input');
        const ratioOptions = document.getElementById('profesi-ratio-options');

        // DOM Elements - UI States
        const generateBtn = document.getElementById('profesi-generate-btn');
        const loadingDiv = document.getElementById('profesi-loading');
        const loadingText = document.getElementById('profesi-loading-text');
        const progressContainer = document.getElementById('profesi-progress-container');
        const progressBar = document.getElementById('profesi-progress-bar');
        const progressText = document.getElementById('profesi-progress-text');
        const progressPercentage = document.getElementById('profesi-progress-percentage');
        const emptyState = document.getElementById('profesi-empty-state');
        const resultsHeader = document.getElementById('profesi-results-header');
        const resultsCount = document.getElementById('profesi-results-count');
        const resultsGrid = document.getElementById('profesi-results-grid');
        const downloadAllBtn = document.getElementById('profesi-download-all-btn');

        if (!childUpload) return; // Exit if elements not present

        // State
        let childImageData = null;
        let selectedRatio = '9:16';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedImages = [];

        // Count selection (1-10), default 4
        const profesiCountSel = document.getElementById('profesi-count-selection-grid');
        if (profesiCountSel) {
            profesiCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                profesiCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        const RANDOM_THEMES = [
            'doctor with stethoscope in modern clinic with medical equipment',
            'chef with cooking utensils in bright professional kitchen',
            'astronaut in space suit with stars and planets backdrop',
            'firefighter with helmet and fire truck in station',
            'teacher with books and colorful classroom setting',
            'artist with paintbrush and colorful art studio',
            'police officer with uniform and friendly police car',
            'pilot with captain hat in realistic airplane cockpit',
            'scientist with lab coat and science equipment',
            'musician with instrument on performance stage',
            'veterinarian caring for cute animals in clinic',
            'engineer with hard hat at construction site',
            'farmer with plants and friendly farm setting',
            'dancer in ballet outfit on elegant stage',
            'athlete with sports equipment in gym setting',
            'photographer with camera in modern studio',
            'baker with chef hat and fresh pastries',
            'gardener with flowers in beautiful garden',
            'mechanic with tools in realistic garage',
            'architect with blueprints and building models',
            'fashion designer with fabrics and sewing',
            'soccer player with ball on soccer field',
            'swimmer with goggles at swimming pool',
            'basketball player on indoor court',
            'detective with magnifying glass solving mystery'
        ];

        // Upload Child Photo with HEIC support
        if (childUpload) {
            childUpload.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    childImageData = reader.result;

                    // Show preview on mobile
                    if (childPreview && childPreviewContainer) {
                        childPreview.src = reader.result;
                        childPreviewContainer.classList.remove('hidden');
                    }

                    // Update placeholder
                    if (childPlaceholder) {
                        childPlaceholder.innerHTML = `
                            <i class="fas fa-check-circle text-purple-500" style="font-size: 2.5rem;"></i>
                            <p class="mt-2 text-sm font-semibold text-purple-600">Foto anak berhasil diupload!</p>
                        `;
                    }

                    // Enable generate button
                    if (generateBtn) {
                        generateBtn.disabled = false;
                    }
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove child preview
        if (removeChildPreview) {
            removeChildPreview.addEventListener('click', () => {
                childImageData = null;
                if (childUpload) childUpload.value = '';
                if (childPreviewContainer) childPreviewContainer.classList.add('hidden');
                if (childPreview) childPreview.src = '';
                if (childPlaceholder) {
                    childPlaceholder.innerHTML = `
                        <i class="fas fa-child transition-transform duration-300" style="font-size: 2.5rem; color: #a855f7;"></i>
                        <p class="mt-2 text-sm font-semibold">👦 Klik untuk upload foto anak</p>
                        <p class="mt-1 text-xs text-gray-400">Format: JPG, PNG, HEIC</p>
                    `;
                }
                if (generateBtn) generateBtn.disabled = true;
            });
        }

        // Ratio selection
        if (ratioOptions) {
            ratioOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                ratioOptions.querySelectorAll('.option-btn-profesi').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedRatio = button.dataset.value;
            });
        }

        // Generate Profession Photos
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (compositeMode) return; // composite mode ditangani listener composite (di target, capture flag diabaikan — urutan registrasi yang menang)
                if (!childImageData) {
                    alert('Silakan upload foto anak terlebih dahulu');
                    return;
                }

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                if (emptyState) emptyState.classList.add('hidden');
                if (loadingDiv) loadingDiv.classList.remove('hidden');
                if (resultsHeader) resultsHeader.classList.add('hidden');

                const age = ageSelect.value;

                const childPhotographySettings = [
                    'Canon EOS R6 with 50mm f/1.8 lens, natural shallow depth of field, soft bokeh',
                    'Sony A7 III with 35mm f/1.4 lens, natural perspective, genuine child expressions',
                    'Nikon Z6 with 85mm f/1.8 lens, flattering portrait compression, natural skin tones',
                    'Fujifilm X-T4 with 56mm f/1.2 lens, beautiful color rendering, film-like quality',
                    'Canon 6D with 24-70mm f/2.8 lens, versatile natural framing'
                ];

                const naturalLightingForKids = [
                    'soft natural window light from the side, gentle shadows, warm skin tones',
                    'bright daylight from large windows, even soft illumination, natural colors',
                    'golden hour warm glow, soft shadows, natural child photography lighting',
                    'overcast natural light for soft even tones, no harsh shadows',
                    'morning natural light with fresh atmosphere, soft gentle illumination',
                    'backlit natural light with hair rim lighting, professional child portrait style'
                ];

                const childRealismDetails = [
                    'natural child expressions and genuine emotions, authentic playful energy',
                    'real natural imperfections - messy hair, natural skin texture, authentic childhood charm',
                    'candid natural moment captured, unposed authentic child behavior',
                    'genuine joy and curiosity in expression, real spontaneous child gestures',
                    'natural clothing wrinkles and realistic fabric, authentic everyday wear',
                    'real environmental interaction, child naturally engaged with surroundings',
                    'subtle natural lighting variations, realistic shadow play on face'
                ];

                let attempts = 0;
                const MAX_ATTEMPTS = 3;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedImages = [];
                    if (resultsGrid) resultsGrid.innerHTML = '';

                    // Pre-create placeholder cards
                    for (let i = 1; i <= selectedCount; i++) {
                        const placeholder = document.createElement('div');
                        placeholder.id = `profesi-card-${i}`;
                        placeholder.className = 'group relative rounded-xl overflow-hidden shadow-2xl border-2 border-transparent bg-gray-100 flex items-center justify-center aspect-[9/16]';
                        placeholder.innerHTML = '<div class="loader"></div>';
                        if (resultsGrid) resultsGrid.appendChild(placeholder);
                    }

                    const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                        generateSingleProfession(i + 1, age, childPhotographySettings, naturalLightingForKids, childRealismDetails)
                    );
                    await Promise.allSettled(generationPromises);

                    // Remove empty cards
                    if (resultsGrid) {
                        resultsGrid.querySelectorAll('[id^="profesi-card-"]').forEach(card => {
                            if (!card.querySelector('img')) card.remove();
                        });
                    }
                    successCount = generatedImages.filter(Boolean).length;
                }

                if (loadingDiv) loadingDiv.classList.add('hidden');

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    if (emptyState) emptyState.classList.remove('hidden');
                } else {
                    if (resultsHeader) resultsHeader.classList.remove('hidden');
                    if (resultsCount) resultsCount.textContent = successCount;
                    if (emptyState) emptyState.classList.add('hidden');
                }

                generateBtn.disabled = false;
                generateBtn.innerHTML = originalHTML;
            });
        }

        // Generate single profession photo
        async function generateSingleProfession(index, age, childPhotographySettings, naturalLightingForKids, childRealismDetails) {
            const card = document.getElementById(`profesi-card-${index}`);
            const selectedTheme = themeInput.value.trim() || RANDOM_THEMES[Math.floor(Math.random() * RANDOM_THEMES.length)];
            const camera = childPhotographySettings[Math.floor(Math.random() * childPhotographySettings.length)];
            const lighting = naturalLightingForKids[Math.floor(Math.random() * naturalLightingForKids.length)];
            const realism = childRealismDetails[Math.floor(Math.random() * childRealismDetails.length)];

            const prompt = `**PROFESSIONAL CHILD PHOTOGRAPHY - IDENTITY PRESERVATION**

**CRITICAL CHARACTER CONSISTENCY:**
You are provided with a reference photo of a child. Generate a new photo of this EXACT SAME child in a profession-themed scene.

**IDENTITY REQUIREMENTS (ABSOLUTELY CRITICAL):**
- Maintain the child's EXACT facial features: face shape, eyes, nose, mouth, ears, smile
- Keep IDENTICAL eye color, eye shape, and eye placement
- Preserve EXACT skin tone, complexion, and natural skin texture
- Maintain SAME hair color, hair texture, and hairstyle
- Keep consistent facial proportions and bone structure
- The child MUST be recognizable as the same individual from reference photo
- NO changes to the child's unique identifying features

**SCENE & PROFESSION THEME:**
${selectedTheme}
Natural interaction with profession elements, child genuinely engaged in activity

**CAMERA & LIGHTING (NATURAL REALISM):**
Shot with ${camera}
${lighting}
Professional child photography lighting, soft and flattering
Natural color grading preserving skin tones

**AUTHENTIC CHILD PHOTOGRAPHY STYLE:**
${realism}
Style of professional child photographers (Anne Geddes, Elena Shumilova, Lisa Holloway)
Natural documentary-style child photography
Genuine childhood wonder and authentic personality
NOT staged or overly posed - natural child behavior
Captures real emotional moment and authentic expression

**NATURAL REALISM (CRITICAL - MUST NOT LOOK AI):**
- Real child photography aesthetic, NOT AI-generated
- Natural lighting conditions like real photo shoot
- Authentic child proportions and anatomy
- Real fabric textures and natural clothing fit
- Genuine environmental details and realistic props
- Subtle film grain for authentic photo quality
- Natural eye catchlights and real shadow behavior
- Slight natural imperfections that prove authenticity
- Professional retouching level - natural enhancement only

**TECHNICAL PHOTOGRAPHY SPECS:**
- Sharp focus on child's face with natural bokeh background
- Natural color palette, avoid cartoon-like saturation
- Realistic depth of field for the chosen lens
- Authentic perspective and spatial relationships
- Real-world lighting physics and shadow casting
- Natural pose that fits child's age and personality

**BACKGROUND & ENVIRONMENT:**
Realistic profession-themed setting with authentic details
Natural environmental lighting and genuine atmosphere
Professional but warm and approachable setting
Clean composition but not sterile - feels real and lived-in

CRITICAL: This is a REAL professional child photograph, NOT an AI render. The child looks exactly like the reference photo. Natural, authentic, genuine childhood captured professionally.`;

            try {
                const childBase64 = childImageData.split(',')[1];
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: 'image/jpeg', data: childBase64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                    safetySettings: [
                        { category: 'HARM_CATEGORY_HARASSMENT', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_HATE_SPEECH', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_SEXUALLY_EXPLICIT', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_DANGEROUS_CONTENT', threshold: 'BLOCK_NONE' }
                    ]
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `profesi-anak-${Date.now()}-${index}.jpg`;
                generatedImages[index - 1] = { url: imageUrl, filename };

                if (card) {
                    card.className = 'group relative rounded-xl overflow-hidden shadow-2xl border-2 border-transparent hover:border-purple-500 transition-all duration-300 transform hover:scale-105';
                    card.innerHTML = `
                        <img src="${imageUrl}" alt="Profession photo ${index}" class="w-full h-full object-cover aspect-[9/16]"/>
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-4 gap-2">
                            <div class="flex gap-2">
                                <button data-action="profesi-preview" data-index="${index - 1}" class="action-btn flex-1 bg-blue-500 hover:bg-blue-600 text-white px-3 py-2 rounded-full text-sm font-bold transition flex items-center justify-center gap-1 shadow-lg">
                                    <i class="fas fa-eye"></i>
                                    <span class="hidden sm:inline">Preview</span>
                                </button>
                                <button data-action="profesi-download" data-index="${index - 1}" class="action-btn flex-1 bg-gradient-to-r from-purple-500 to-pink-500 hover:from-purple-600 hover:to-pink-600 text-white px-3 py-2 rounded-full text-sm font-bold transition flex items-center justify-center gap-1 shadow-lg">
                                    <i class="fas fa-download"></i>
                                    <span class="hidden sm:inline">Download</span>
                                </button>
                            </div>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`Error generating profession photo ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'profesi-preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'profesi-download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all functionality
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;  // Skip failed/empty slots
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }
        // ── COMPOSITE MODE ─────────────────────────────────────────────
        let compositeMode = false;

        const regularControls  = document.getElementById('profesi-regular-controls');
        const compositeControls = document.getElementById('profesi-composite-controls');
        const modeRegularBtn   = document.getElementById('profesi-mode-regular');
        const modeCompositeBtn = document.getElementById('profesi-mode-composite');
        const compResult       = document.getElementById('profesi-composite-result');
        const compLoading      = document.getElementById('profesi-comp-loading');
        const compGrid         = document.getElementById('profesi-comp-grid');
        const compProgressBar  = document.getElementById('profesi-comp-progress-bar');
        const compProgressText = document.getElementById('profesi-comp-progress-text');
        const compDownloadBtn  = document.getElementById('profesi-comp-download-btn');
        const compThemeGrid    = document.getElementById('profesi-comp-theme-grid');
        const compNameInput    = document.getElementById('profesi-comp-name');
        const compPhrase1      = document.getElementById('profesi-comp-phrase1');
        const compPhrase2      = document.getElementById('profesi-comp-phrase2');
        const compGenderGrid   = document.getElementById('profesi-comp-gender');
        const compAgeSelect    = document.getElementById('profesi-comp-age');
        const compCustomTheme  = document.getElementById('profesi-comp-custom-theme');
        const compRatioOptions = document.getElementById('profesi-comp-ratio-options');
        const compCountGrid    = document.getElementById('profesi-comp-count-grid');

        let selectedCompTheme  = 'pilot';
        let selectedCompGender = 'male';
        let selectedCompRatio  = '4:5';
        let selectedCompCount  = 1;
        let compImages         = [];

        // Theme config → fed into one master seamless-canvas template (mirrors the reference prompt)
        const COMPOSITE_THEMES = {
            pilot: {
                label: 'Little Pilot', tagline: 'fly high', backdropName: 'pastel sky-blue', backdropHex: '#8ED3ED', textColor: 'brown',
                costume: 'a brown distressed leather bomber jacket with a thick cream shearling collar, a long flowing cream-white knit scarf, brown corduroy trousers, and a brown leather aviator cap with brass-rimmed flight goggles pushed up onto the forehead',
                props: 'fluffy white three-dimensional cloud props floating at varying depths, an antique globe on a wooden stand, a brass pocket compass, several white folded paper airplanes scattered across the floor, and one extra wooden toy biplane',
                fg: 'leans very close to the lens so the head and shoulders fill this third of the frame naturally through perspective, calm confident half smile, direct eye contact, slightly soft focus at the very edge',
                mgLeft: 'standing full body, feet on the floor, holding a small wooden toy biplane with both hands, wide joyful open-mouth smile, in sharp focus',
                mgCenter: 'sitting on a stack of two vintage brown leather suitcases, three-quarter view, unfolding a paper world map across the lap, head tilted down, focused expression, slightly smaller due to distance',
                bg: 'smallest, captured mid-jump with both arms stretched wide like airplane wings, scarf flying behind, laughing, feet off the ground',
                doodles: 'cloud outlines, four-point stars, long dashed curved flight paths that travel from one figure to another, wind swirl lines, and small paper airplane sketches'
            },
            astronaut: {
                label: 'Little Astronaut', tagline: 'to the moon', backdropName: 'deep space navy', backdropHex: '#1B2A5B', textColor: 'white',
                costume: 'a white astronaut suit with an Indonesian flag patch and mission badges, silver zippers and hoses, chunky white gloves and moon boots, with a bubble helmet carried under one arm',
                props: 'fluffy white cloud props, a toy rocket resting on the floor, a small planet Earth globe, scattered white paper star props, and a toy moon rover at varying depths',
                fg: 'leans very close to the lens so the helmet and shoulders fill this third of the frame through perspective, calm confident smile, direct eye contact',
                mgLeft: 'standing full body holding a small planet Earth globe with both hands, gazing at it with wonder, in sharp focus',
                mgCenter: 'sitting on a small toy rocket, three-quarter view, one hand raised as if counting down, focused expression, slightly smaller due to distance',
                bg: 'smallest, floating in a weightless jumping pose with arms stretched wide, laughing, feet off the ground',
                doodles: 'rocket outlines, planets with rings, four-point stars, long dashed orbit paths that travel from one figure to another, and small comet sketches'
            },
            princess: {
                label: 'Little Princess', tagline: 'once upon a time', backdropName: 'soft pastel pink', backdropHex: '#F7C9DE', textColor: 'plum purple',
                costume: 'a layered pastel tulle princess gown, a sparkling tiara, pearl jewelry and short white gloves',
                props: 'fluffy pastel cloud props, a small ornate throne, an open storybook, scattered paper stars and hearts, and a toy castle turret at varying depths',
                fg: 'leans very close to the lens so the head and shoulders fill this third of the frame through perspective, gentle royal smile, direct eye contact',
                mgLeft: 'standing gracefully holding a sparkling magic wand raised in one hand, in sharp focus',
                mgCenter: 'seated on a small ornate throne, three-quarter view, holding an open storybook on the lap, focused gentle expression, slightly smaller due to distance',
                bg: 'smallest, twirling with the dress flowing outward, arms wide, joyful spin, feet light off the ground',
                doodles: 'crown outlines, hearts, four-point stars, long dashed sparkling trails that travel from one figure to another, and small butterfly sketches'
            },
            superhero: {
                label: 'Little Hero', tagline: 'save the day', backdropName: 'bold cobalt blue', backdropHex: '#2453C7', textColor: 'bright yellow',
                costume: 'a bright primary-color superhero suit with a chest emblem, a matching flowing cape and a domino mask',
                props: 'fluffy white cloud props, a cardboard city-skyline prop, scattered paper star props, and a toy shield at varying depths',
                fg: 'leans very close to the lens so the head and shoulders fill this third of the frame through perspective, determined heroic expression, chin slightly up',
                mgLeft: 'standing in a strong hero pose with fists on hips, cape billowing, in sharp focus',
                mgCenter: 'crouched in a ready-to-launch power pose with one fist on the floor, three-quarter view, focused expression, slightly smaller due to distance',
                bg: 'smallest, captured mid-jump in a flying pose with one arm punched forward and cape streaming behind, laughing',
                doodles: 'lightning bolts, four-point stars, speed lines, long dashed flight trails that travel from one figure to another, and small pow-burst sketches'
            },
            chef: {
                label: 'Little Chef', tagline: 'cooking with love', backdropName: 'warm cream', backdropHex: '#F3E6CF', textColor: 'chocolate brown',
                costume: 'a white double-breasted chef jacket, a tall toque blanche hat, a small apron and a red neckerchief',
                props: 'fluffy white cloud-like flour-puff props, a wooden prep table with mini vegetables, a mixing bowl and whisk, and scattered paper recipe cards at varying depths',
                fg: 'leans very close to the lens so the head and shoulders fill this third of the frame through perspective, big delighted smile, cheeks lifted',
                mgLeft: 'standing holding a rolling pin and a small mixing bowl, focused happy expression, in sharp focus',
                mgCenter: 'sitting at a mini prep table with tiny vegetables and utensils, three-quarter view, focused expression, slightly smaller due to distance',
                bg: 'smallest, playfully tossing a small ball of dough into the air, arms up, laughing, feet off the ground',
                doodles: 'whisk and spoon outlines, four-point stars, steam swirl lines, long dashed trails that travel from one figure to another, and small heart sketches'
            },
            explorer: {
                label: 'Little Explorer', tagline: 'adventure awaits', backdropName: 'warm khaki-tan', backdropHex: '#D8C29A', textColor: 'deep forest green',
                costume: 'a khaki explorer shirt with rolled sleeves, cargo shorts, a wide-brim safari hat, a pair of binoculars on a strap and a small backpack',
                props: 'fluffy white cloud props, a wooden crate, a rolled treasure map, a brass compass, a toy tent, and scattered paper leaves at varying depths',
                fg: 'leans very close to the lens so the head and shoulders fill this third of the frame through perspective, adventurous confident smile, binoculars around the neck',
                mgLeft: 'standing holding an unfolded treasure map with both hands, looking off toward the horizon, in sharp focus',
                mgCenter: 'crouching to examine a brass compass on the floor, three-quarter view, curious focused expression, slightly smaller due to distance',
                bg: 'smallest, mid-stride pointing forward with one arm as if discovering something, feet off the ground',
                doodles: 'compass-rose outlines, four-point stars, dashed footprint trails that travel from one figure to another, mountain silhouettes, and small leaf sketches'
            },
            doctor: {
                label: 'Little Doctor', tagline: 'here to help', backdropName: 'soft mint', backdropHex: '#CDEBDD', textColor: 'teal',
                costume: 'a clean white doctor coat over a light blue scrub top, a stethoscope around the neck, and a reflector headband',
                props: 'fluffy white cloud props, a friendly plush-toy patient, a toy medical kit, a clipboard, and scattered paper heart-cross doodles at varying depths',
                fg: 'leans very close to the lens so the head and shoulders fill this third of the frame through perspective, warm reassuring smile, direct eye contact',
                mgLeft: 'standing holding a stethoscope out with both hands as if listening, kind expression, in sharp focus',
                mgCenter: 'sitting on a small stool beside a plush-toy patient, three-quarter view, gently examining it, focused caring expression, slightly smaller due to distance',
                bg: 'smallest, mid-jump with arms raised cheerfully, laughing, feet off the ground',
                doodles: 'plus-cross outlines, hearts, four-point stars, long dashed trails that travel from one figure to another, and small heartbeat-line sketches'
            },
            firefighter: {
                label: 'Little Firefighter', tagline: 'brave and strong', backdropName: 'warm cream-peach', backdropHex: '#F2D6C4', textColor: 'fire red',
                costume: 'a yellow-and-navy firefighter turnout coat with reflective stripes, a red fire helmet, and sturdy boots',
                props: 'fluffy white cloud props, a coiled toy fire hose, a small red fire hydrant prop, a toy fire truck, and scattered paper flame doodles at varying depths',
                fg: 'leans very close to the lens so the head and shoulders fill this third of the frame through perspective, brave confident smile under the helmet',
                mgLeft: 'standing holding a toy fire hose nozzle with both hands in a ready stance, in sharp focus',
                mgCenter: 'sitting on a small toy fire truck, three-quarter view, gripping a toy steering wheel, focused expression, slightly smaller due to distance',
                bg: 'smallest, mid-jump with one arm raised heroically, laughing, feet off the ground',
                doodles: 'flame outlines, four-point stars, water-spray swirl lines, long dashed trails that travel from one figure to another, and small ladder sketches'
            },
            police: {
                label: 'Little Officer', tagline: 'keeping us safe', backdropName: 'soft sky blue', backdropHex: '#CFE3F5', textColor: 'navy blue',
                costume: 'a navy-blue police uniform with a shiny badge, a peaked cap, a utility belt and a friendly whistle',
                props: 'fluffy white cloud props, a toy police car, a small traffic-cone prop, a toy walkie-talkie, and scattered paper star-badge doodles at varying depths',
                fg: 'leans very close to the lens so the head and shoulders fill this third of the frame through perspective, friendly confident smile',
                mgLeft: 'standing giving a cheerful salute with one hand and the other hand on the belt, in sharp focus',
                mgCenter: 'sitting on a toy police car, three-quarter view, holding a toy walkie-talkie, focused expression, slightly smaller due to distance',
                bg: 'smallest, mid-jump with arms wide, laughing, feet off the ground',
                doodles: 'star-badge outlines, four-point stars, long dashed patrol paths that travel from one figure to another, and small siren sketches'
            },
            ballerina: {
                label: 'Little Ballerina', tagline: 'dance your dream', backdropName: 'soft blush lavender', backdropHex: '#EAD9F2', textColor: 'rose pink',
                costume: 'a pastel ballet leotard with a layered tutu skirt, satin ballet slippers with ribbons, and a neat bun with a small flower',
                props: 'fluffy pastel cloud props, a small practice barre, scattered paper flower petals, a music box, and a pair of pointe shoes hanging at varying depths',
                fg: 'leans very close to the lens so the head and shoulders fill this third of the frame through perspective, graceful gentle smile',
                mgLeft: 'standing in a ballet arabesque pose with arms extended, poised, in sharp focus',
                mgCenter: 'seated on the floor in a graceful stretch beside a small music box, three-quarter view, serene expression, slightly smaller due to distance',
                bg: 'smallest, captured mid-leap in a joyful grand jeté, arms wide, feet off the ground',
                doodles: 'musical notes, flower outlines, four-point stars, long dashed swirling ribbons that travel from one figure to another, and small butterfly sketches'
            },
            footballer: {
                label: 'Little Striker', tagline: 'play to win', backdropName: 'fresh grass green', backdropHex: '#CDE9C4', textColor: 'deep green',
                costume: 'a bright team football jersey with matching shorts, tall striped socks, cleats, and a captain armband',
                props: 'fluffy white cloud props, several footballs at varying depths, a mini goal net, a water bottle, and scattered paper star doodles',
                fg: 'leans very close to the lens so the head and shoulders fill this third of the frame through perspective, confident happy smile',
                mgLeft: 'standing with one foot resting on a football, arms crossed proudly, in sharp focus',
                mgCenter: 'crouched tying a cleat next to a football, three-quarter view, focused expression, slightly smaller due to distance',
                bg: 'smallest, captured mid-air in a dynamic kicking pose, arms out for balance, feet off the ground',
                doodles: 'football-pentagon outlines, four-point stars, long dashed ball trajectories that travel from one figure to another, and small trophy sketches'
            },
            musician: {
                label: 'Little Star', tagline: 'follow the music', backdropName: 'warm peach', backdropHex: '#F6D9C4', textColor: 'maroon',
                costume: 'a stylish little stage outfit — a shimmering jacket over a shirt, with a small bow tie',
                props: 'fluffy white cloud props, a small guitar, a toy piano keyboard, a microphone stand, and scattered paper musical-note doodles at varying depths',
                fg: 'leans very close to the lens so the head and shoulders fill this third of the frame through perspective, joyful bright smile',
                mgLeft: 'standing playing a small guitar with both hands mid-strum, in sharp focus',
                mgCenter: 'sitting at a toy piano keyboard, three-quarter view, hands on the keys, focused happy expression, slightly smaller due to distance',
                bg: 'smallest, captured mid-jump singing into a handheld microphone with one arm raised, feet off the ground',
                doodles: 'musical notes, treble-clef outlines, four-point stars, long dashed melody lines that travel from one figure to another, and small star-burst sketches'
            }
        };

        function compGenderNoun(g) { return g === 'female' ? 'girl' : 'boy'; }

        function buildCompositePrompt(themeKey, o) {
            const t = COMPOSITE_THEMES[themeKey] || COMPOSITE_THEMES.pilot;
            const noun = compGenderNoun(o.gender);
            const p1 = o.phrase1 || o.name || t.label;
            const p2 = o.phrase2 || t.tagline;
            return `Buat gambar A single seamless studio photograph on one continuous canvas — NOT a grid, NOT a collage, NOT split panels, no dividing lines, no borders, one unbroken scene.

IMPORTANT — the attached photo is ONLY a facial identity reference: copy the child's face, hair and skin tone from it, but do NOT reuse its pose, outfit, crop or background. Build a completely new scene from scratch. The final single image MUST show FOUR separate full-body appearances of the SAME child at the same time within one frame — four distinct figures, never just one.

The SAME ${o.age}-year-old Indonesian ${noun} appears four times within this one continuous space, each at a different distance from the camera, all four with the IDENTICAL face, hair and features from the reference photo. Identical wardrobe in every instance: ${t.costume}. Same face, same hair, same costume, perfectly consistent.

Placement within the single space:
— FOREGROUND, right edge: ${t.fg}.
— MID-GROUND, left: ${t.mgLeft}.
— MID-GROUND, center-back: ${t.mgCenter}.
— BACKGROUND, upper left: ${t.bg}.

Environment: one seamless ${t.backdropName} studio cyclorama, solid color ${t.backdropHex}, no gradient, no horizon line, floor and wall blend into each other continuously. Shared props resting on the same continuous floor: ${t.props}.

Lighting: one single consistent large softbox from the front, bright and even across the whole scene, all four instances share the exact same light direction, soft diffused contact shadows on the floor that anchor every figure to the same ground plane.

Overlay across the entire canvas, flowing continuously and never interrupted: hand-drawn thin white doodle line art — ${t.doodles}.
Text overlay integrated into the open background space: "${p1}" in ${t.textColor} handwritten script at the upper left, "${p2}" in ${t.textColor} handwritten script at the lower center, both placed in empty background areas without covering any face.

Style: clean commercial children's photography, bright, cheerful, airy, high resolution, natural warm skin tone, consistent perspective and a single vanishing point throughout.
Aspect ratio ${o.ratio}.`;
        }

        function buildCustomCompositePrompt(themeText, o) {
            const noun = compGenderNoun(o.gender);
            const p1 = o.phrase1 || o.name || 'Little Star';
            const p2 = o.phrase2 || 'dream big';
            return `Buat gambar A single seamless studio photograph on one continuous canvas — NOT a grid, NOT a collage, NOT split panels, no dividing lines, no borders, one unbroken scene.

IMPORTANT — the attached photo is ONLY a facial identity reference: copy the child's face, hair and skin tone from it, but do NOT reuse its pose, outfit, crop or background. Build a completely new scene from scratch. The final single image MUST show FOUR separate full-body appearances of the SAME child at the same time within one frame — four distinct figures, never just one.

The SAME ${o.age}-year-old Indonesian ${noun} appears four times within this one continuous space, each at a different distance from the camera, all four with the IDENTICAL face, hair and features from the reference photo. Identical wardrobe in every instance: a fully detailed themed costume for "${themeText}", the exact same outfit on all four appearances.

Placement within the single space:
— FOREGROUND, right edge: leans very close to the lens so the head and shoulders fill this third of the frame naturally through perspective, calm confident half smile, direct eye contact, slightly soft focus at the very edge.
— MID-GROUND, left: standing full body, feet on the floor, holding a signature prop related to "${themeText}" with both hands, wide joyful smile, in sharp focus.
— MID-GROUND, center-back: sitting on stacked themed props related to "${themeText}", three-quarter view, doing a themed activity, focused expression, slightly smaller due to distance.
— BACKGROUND, upper left: smallest, captured mid-jump with both arms stretched wide, laughing, feet off the ground.

Environment: one seamless soft pastel studio cyclorama in a flat solid color that complements "${themeText}", no gradient, no horizon line, floor and wall blend into each other continuously. Shared props resting on the same continuous floor: several three-dimensional props related to "${themeText}" arranged at varying depths.

Lighting: one single consistent large softbox from the front, bright and even across the whole scene, all four instances share the exact same light direction, soft diffused contact shadows on the floor that anchor every figure to the same ground plane.

Overlay across the entire canvas, flowing continuously and never interrupted: hand-drawn thin white doodle line art — outlines, four-point stars, long dashed curved paths that travel from one figure to another, and small sketches related to "${themeText}".
Text overlay integrated into the open background space: "${p1}" in a warm handwritten script at the upper left, "${p2}" in a warm handwritten script at the lower center, both placed in empty background areas without covering any face.

Style: clean commercial children's photography, bright, cheerful, airy, high resolution, natural warm skin tone, consistent perspective and a single vanishing point throughout.
Aspect ratio ${o.ratio}.`;
        }

        // Mode toggle
        function setGenerateLabel(key, text) {
            const span = generateBtn ? generateBtn.querySelector('span[data-i18n]') : null;
            if (!span) return; // tombol sedang menampilkan spinner (mid-generate) — skip, label direstore oleh finally
            span.setAttribute('data-i18n', key);
            span.textContent = text;
        }

        if (modeRegularBtn) {
            modeRegularBtn.addEventListener('click', () => {
                compositeMode = false;
                modeRegularBtn.classList.add('active');
                modeCompositeBtn.classList.remove('active');
                regularControls.classList.remove('hidden');
                compositeControls.classList.add('hidden');
                if (compResult) compResult.classList.add('hidden');
                if (resultsGrid) resultsGrid.classList.remove('hidden');
                if (emptyState && !resultsGrid.innerHTML.trim()) emptyState.classList.remove('hidden');
                setGenerateLabel('pa.generate-btn', window.tr3('Generate Foto Profesi', 'Generate Profession Photo', null));
            });
        }

        if (modeCompositeBtn) {
            modeCompositeBtn.addEventListener('click', () => {
                compositeMode = true;
                modeCompositeBtn.classList.add('active');
                modeRegularBtn.classList.remove('active');
                regularControls.classList.add('hidden');
                compositeControls.classList.remove('hidden');
                if (resultsGrid) resultsGrid.classList.add('hidden');
                if (resultsHeader) resultsHeader.classList.add('hidden');
                if (compResult) compResult.classList.remove('hidden');
                if (emptyState && !(compGrid && compGrid.children.length)) emptyState.classList.add('hidden');
                setGenerateLabel('pa.comp-generate-btn', window.tr3('Generate Composite', 'Generate Composite', null));
            });
        }

        // Composite theme selection
        if (compThemeGrid) {
            compThemeGrid.addEventListener('click', (e) => {
                const btn = e.target.closest('.profesi-comp-theme-btn');
                if (!btn) return;
                compThemeGrid.querySelectorAll('.profesi-comp-theme-btn').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCompTheme = btn.dataset.theme;
            });
        }

        // Composite gender selection
        if (compGenderGrid) {
            compGenderGrid.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-gender]');
                if (!btn) return;
                compGenderGrid.querySelectorAll('.option-btn-profesi').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCompGender = btn.dataset.gender;
            });
        }

        // Composite ratio selection
        if (compRatioOptions) {
            compRatioOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;
                compRatioOptions.querySelectorAll('.option-btn-profesi').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCompRatio = btn.dataset.value;
            });
        }

        // Composite count selection
        if (compCountGrid) {
            compCountGrid.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                compCountGrid.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCompCount = parseInt(btn.dataset.count, 10);
            });
        }

        function compUpdateProgress(done, total) {
            if (compProgressText) compProgressText.textContent = `${done}/${total}`;
            if (compProgressBar) compProgressBar.style.width = `${Math.round((done / total) * 100)}%`;
        }

        function compActiveThemeSlug() {
            const custom = compCustomTheme ? compCustomTheme.value.trim() : '';
            if (custom) return custom.replace(/\s+/g, '-').toLowerCase().slice(0, 24) || 'custom';
            return selectedCompTheme;
        }

        function renderCompCard(dataUrl, idx) {
            const card = document.createElement('div');
            card.className = 'group relative rounded-xl overflow-hidden shadow-2xl border-2 border-transparent hover:border-green-500 transition-all duration-300';
            card.innerHTML = `
                <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-4 gap-2">
                    <div class="flex gap-2">
                        <button data-action="comp-preview" data-index="${idx}" class="comp-action-btn flex-1 bg-blue-500 hover:bg-blue-600 text-white px-3 py-2 rounded-full text-sm font-bold transition flex items-center justify-center gap-1 shadow-lg">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="comp-download" data-index="${idx}" class="comp-action-btn flex-1 bg-gradient-to-r from-green-500 to-pink-500 hover:from-green-600 hover:to-pink-600 text-white px-3 py-2 rounded-full text-sm font-bold transition flex items-center justify-center gap-1 shadow-lg">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                </div>`;
            // img via DOM API (bukan interpolasi innerHTML) — anti XSS dari data eksternal
            const img = document.createElement('img');
            img.src = dataUrl;
            img.alt = 'Foto Composite Anak';
            img.className = 'w-full h-full object-cover';
            card.insertBefore(img, card.firstChild);
            return card;
        }

        // Event delegation untuk tombol Preview/Download kartu composite
        if (compGrid) {
            compGrid.addEventListener('click', async (e) => {
                const btn = e.target.closest('.comp-action-btn[data-action]');
                if (!btn) return;
                e.stopPropagation();
                const idx = parseInt(btn.dataset.index, 10);
                const url = compImages[idx];
                if (!url) return;
                if (btn.dataset.action === 'comp-preview') {
                    if (window.showPreviewModal) window.showPreviewModal(url);
                } else {
                    const fn = `composite-${compActiveThemeSlug()}-${idx + 1}.png`;
                    if (window.downloadDataURINew) await window.downloadDataURINew(url, fn);
                    else if (window.downloadImage) await window.downloadImage(url, fn);
                }
            });
        }

        async function compFetchOne(prompt, ratio) {
            const base64 = childImageData.split(',')[1];
            const payload = {
                contents: [{
                    parts: [
                        { text: prompt },
                        { inlineData: { mimeType: 'image/jpeg', data: base64 } }
                    ]
                }],
                generationConfig: {
                    responseModalities: ['TEXT', 'IMAGE'],
                    imageConfig: { aspectRatio: ratio }
                },
                safetySettings: [
                    { category: 'HARM_CATEGORY_HARASSMENT',        threshold: 'BLOCK_NONE' },
                    { category: 'HARM_CATEGORY_HATE_SPEECH',        threshold: 'BLOCK_NONE' },
                    { category: 'HARM_CATEGORY_SEXUALLY_EXPLICIT',  threshold: 'BLOCK_NONE' },
                    { category: 'HARM_CATEGORY_DANGEROUS_CONTENT',  threshold: 'BLOCK_NONE' }
                ]
            };
            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
            const resp = await fetch(apiUrl, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            });
            const result = await resp.json();
            const imgData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
            if (!imgData) throw new Error('No image data received');
            if (!/^[A-Za-z0-9+/=]+$/.test(imgData)) throw new Error('Invalid image data'); // guard: hanya base64 murni (anti HTML injection)
            return `data:image/png;base64,${imgData}`;
        }

        // Override generate button for composite mode
        if (generateBtn && !generateBtn._compositeSetup) {
            generateBtn._compositeSetup = true;
            generateBtn.addEventListener('click', async (e) => {
                if (!compositeMode) return; // let existing listener handle regular mode
                e.stopImmediatePropagation();

                if (!childImageData) {
                    alert(window.tr3('Silakan upload foto anak terlebih dahulu', 'Please upload a child photo first', null));
                    return;
                }

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                if (emptyState) emptyState.classList.add('hidden');
                if (compGrid) compGrid.innerHTML = '';
                compImages = [];
                if (compDownloadBtn) compDownloadBtn.classList.add('hidden');
                if (compLoading) compLoading.classList.remove('hidden');

                const count = selectedCompCount || 1;
                compUpdateProgress(0, count);

                try {
                    const opts = {
                        name:    compNameInput ? compNameInput.value.trim() : '',
                        phrase1: compPhrase1 ? compPhrase1.value.trim() : '',
                        phrase2: compPhrase2 ? compPhrase2.value.trim() : '',
                        age:     compAgeSelect ? compAgeSelect.value : '6',
                        gender:  selectedCompGender,
                        ratio:   selectedCompRatio
                    };
                    const custom = compCustomTheme ? compCustomTheme.value.trim() : '';
                    const prompt = custom
                        ? buildCustomCompositePrompt(custom, opts)
                        : buildCompositePrompt(selectedCompTheme, opts);

                    let done = 0;
                    const tasks = Array.from({ length: count }, () =>
                        compFetchOne(prompt, opts.ratio)
                            .then(url => { done++; compUpdateProgress(done, count); return url; })
                            .catch(err => { console.error('Composite item error:', err); done++; compUpdateProgress(done, count); return null; })
                    );
                    const urls = (await Promise.all(tasks)).filter(Boolean);

                    if (!urls.length) throw new Error('No composite images generated');

                    urls.forEach((url, i) => {
                        compImages.push(url);
                        if (compGrid) compGrid.appendChild(renderCompCard(url, i));
                    });
                    if (compDownloadBtn && compImages.length) compDownloadBtn.classList.remove('hidden');

                } catch (err) {
                    console.error('Composite error:', err);
                    alert(window.tr3('Gagal membuat foto composite. Coba lagi.', 'Failed to create composite photo. Try again.', null));
                } finally {
                    if (compLoading) compLoading.classList.add('hidden');
                    generateBtn.disabled = false;
                    generateBtn.innerHTML = originalHTML;
                }
            }, true); // capture=true runs before existing bubble-phase listener
        }

        // Composite download-all button
        if (compDownloadBtn) {
            compDownloadBtn.addEventListener('click', async () => {
                if (!compImages.length) return;
                for (let i = 0; i < compImages.length; i++) {
                    const fn = `composite-${compActiveThemeSlug()}-${i + 1}.png`;
                    if (window.downloadDataURINew) await window.downloadDataURINew(compImages[i], fn);
                    else if (window.downloadImage) await window.downloadImage(compImages[i], fn);
                }
            });
        }
        // ── END COMPOSITE MODE ──────────────────────────────────────────
    })();

        // ==================== DESAIN KAMAR (ROOM DESIGN) - PROFESSIONAL VERSION ====================
        (function() {
            const apiKey = "";

            // DOM Elements - Upload & Preview
            const roomUpload = document.getElementById('room-upload');
            const roomUploadBox = document.getElementById('room-upload-box');
            const roomPlaceholder = document.getElementById('room-placeholder');
            const roomPreviewContainer = document.getElementById('room-preview-container');
            const roomPreview = document.getElementById('room-preview');
            const roomRemovePreview = document.getElementById('room-remove-preview');

            const referenceUpload = document.getElementById('room-reference-upload');
            const referenceUploadBox = document.getElementById('room-reference-upload-box');
            const referencePlaceholder = document.getElementById('room-reference-placeholder');
            const referencePreviewContainer = document.getElementById('room-reference-preview-container');
            const referencePreview = document.getElementById('room-reference-preview');
            const roomRemoveReferencePreview = document.getElementById('room-remove-reference-preview');

            // DOM Elements - Theme Selection
            const themeOptions = document.getElementById('room-theme-options');
            const customThemeContainer = document.getElementById('room-custom-theme-container');
            const customThemeInput = document.getElementById('room-custom-theme-input');

            // DOM Elements - Ratio Selection
            const ratioOptions = document.getElementById('room-ratio-options');

            // DOM Elements - Generate & Results
            const generateBtn = document.getElementById('room-generate-btn');

            // Count selection (1-10), default 4
            let selectedCount = 4;
            const roomCountSel = document.getElementById('room-count-selection-grid');
            if (roomCountSel) {
                roomCountSel.addEventListener('click', (e) => {
                    const btn = e.target.closest('button[data-count]');
                    if (!btn) return;
                    roomCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    selectedCount = parseInt(btn.dataset.count, 10);
                });
            }

            const loadingDiv = document.getElementById('room-loading');
            const loadingText = document.getElementById('room-loading-text');
            const progressContainer = document.getElementById('room-progress-container');
            const progressBar = document.getElementById('room-progress-bar');
            const progressText = document.getElementById('room-progress-text');
            const progressPercentage = document.getElementById('room-progress-percentage');
            const emptyState = document.getElementById('room-empty-state');
            const resultsHeader = document.getElementById('room-results-header');
            const resultsCount = document.getElementById('room-results-count');
            const resultsGrid = document.getElementById('room-results-grid');
            const downloadAllBtn = document.getElementById('room-download-all-btn');

            // State Variables
            let roomImageData = null;
            let referenceImageData = null;
            let selectedTheme = 'modern-minimalist';
            let selectedRatio = '1:1';
            // selectedCount sudah di-declare di scope luar (count selector)
            let generatedImages = [];

            // Theme Descriptions (Comprehensive & Detailed)
            const themeDescriptions = {
                'modern-minimalist': {
                    colors: 'Warna netral dominan: putih bersih, abu-abu terang, beige, dengan maksimal 1 aksen warna (hitam atau navy)',
                    furniture: 'Furniture minimalis dengan garis lurus, tanpa ornamen, material modern (metal, kaca, kayu polos)',
                    walls: 'Dinding polos putih atau abu-abu muda, tanpa wallpaper atau dekorasi berlebihan',
                    decor: 'Dekorasi sangat minimal: maksimal 2-3 item (vas sederhana, artwork abstract monokrom)',
                    lighting: 'Pencahayaan modern: lampu gantung geometris minimalis, LED strip, atau recessed lighting',
                    avoid: 'HINDARI: warna-warna cerah, banyak dekorasi, furniture ornamental, pattern ramai'
                },
                'scandinavian': {
                    colors: 'Palet warna terang: putih dominan, abu-abu muda, beige, kayu natural terang (birch/oak), aksen pastel (soft blue/pink)',
                    furniture: 'Furniture kayu natural terang dengan desain simpel dan fungsional, kaki kayu ramping',
                    walls: 'Dinding putih bersih atau abu-abu sangat terang',
                    decor: 'Dekorasi cozy: bantal rajut, selimut wool, tanaman hijau dalam pot keramik putih, lilin',
                    lighting: 'Pencahayaan hangat: pendant lamp bulat putih/krem, lampu meja kayu, maksimalkan cahaya alami',
                    avoid: 'HINDARI: warna gelap, furniture ornamental, dekorasi mewah, metal mengkilap'
                },
                'industrial': {
                    colors: 'Palet gelap: abu-abu tua, coklat kayu reclaimed, hitam metal, ekspos bata merah/concrete',
                    furniture: 'Furniture metal & kayu reclaimed: meja besi, kursi metal industrial, rak pipa besi',
                    walls: 'Ekspos bata merah ATAU concrete ATAU cat abu-abu gelap dengan aksen pipa metal terlihat',
                    decor: 'Dekorasi industrial: lampu Edison bulbs tergantung, gear metal, signage vintage metal',
                    lighting: 'Pencahayaan industrial: Edison bulbs dengan kabel terlihat, pendant metal hitam, track lighting',
                    avoid: 'HINDARI: warna pastel, furniture modern sleek, dekorasi cute, dinding polos putih'
                },
                'bohemian': {
                    colors: 'Palet warna hangat & vibrant: terracotta, mustard, teal, burgundy, dengan banyak pattern etnik',
                    furniture: 'Furniture eklektik mix-match: rotan, kayu vintage, metal antik, pouf leather, cushion berlapis',
                    walls: 'Dinding dengan tapestry/macrame besar ATAU wallpaper motif etnik ATAU gallery wall eklektik',
                    decor: 'Dekorasi berlimpah: banyak tanaman gantung, macrame, bantal pattern beragam, karpet layering, dreamcatcher',
                    lighting: 'Pencahayaan warm: lampu gantung rotan/rattan, string lights, lantern metal/kaca warna',
                    avoid: 'HINDARI: minimalis, warna monokrom, furniture matching set, ruang terlalu rapi'
                },
                'mid-century-modern': {
                    colors: 'Palet retro 50-60an: mustard yellow, teal, orange burnt, coklat teak, dengan aksen hitam/putih',
                    furniture: 'Furniture iconic mid-century: Eames chair style, teak sideboard berkaki ramping miring, sofa low-profile',
                    walls: 'Dinding dengan accent wall warna bold (mustard/teal) ATAU wood paneling ATAU wallpaper geometric',
                    decor: 'Dekorasi retro: starburst clock, abstract art geometric, vinyl record display, keramik vintage',
                    lighting: 'Pencahayaan retro: Sputnik chandelier, arc floor lamp, pendant bulat dengan kaki tripod',
                    avoid: 'HINDARI: furniture modern sleek, warna pastel, dekorasi rustic, industrial style'
                },
                'contemporary': {
                    colors: 'Palet modern 2020s: neutral base (gray/beige) dengan bold accent (emerald/navy/terracotta)',
                    furniture: 'Furniture trendy terkini: curved sofa, bouclé fabric, mixed materials (velvet+metal+marble)',
                    walls: 'Dinding dengan textured wall (3D panel/fluted) ATAU bold accent color ATAU large artwork modern',
                    decor: 'Dekorasi statement: sculpture modern, vase oversized, coffee table books, mirror artistic',
                    lighting: 'Pencahayaan layered: chandelier modern statement, floor lamp artistic, LED strip accent',
                    avoid: 'HINDARI: furniture jadul, dekorasi vintage, warna-warna kusam, style klasik'
                },
                'traditional': {
                    colors: 'Palet klasik: burgundy, navy, forest green, gold accent, dengan kayu gelap (mahogany/cherry)',
                    furniture: 'Furniture klasik ornamental: sofa tufted dengan kaki kayu carved, meja kayu gelap dengan detail ukiran',
                    walls: 'Dinding dengan wallpaper damask/floral klasik ATAU wainscoting ATAU crown molding',
                    decor: 'Dekorasi elegant: chandelier crystal, heavy curtains dengan valance, painting klasik frame gold, vas porselen',
                    lighting: 'Pencahayaan mewah: chandelier crystal besar, lampu meja dengan lampshade kain, sconce dinding',
                    avoid: 'HINDARI: furniture modern, warna-warna terang, minimalis, dekorasi industrial'
                },
                'rustic': {
                    colors: 'Palet earthy: coklat kayu natural, krem, beige, dengan aksen hijau sage/terracotta',
                    furniture: 'Furniture kayu solid natural: meja kayu reclaimed, kursi kayu polos, rak kayu open shelving',
                    walls: 'Dinding dengan shiplap kayu ATAU exposed wooden beams ATAU stone accent wall',
                    decor: 'Dekorasi natural: tanaman dalam pot terracotta, basket anyaman, metal vintage, wood sign',
                    lighting: 'Pencahayaan warm rustic: pendant mason jar, lantern metal, lampu dengan base kayu',
                    avoid: 'HINDARI: furniture sleek modern, warna bold, metal chrome, dekorasi glamor'
                },
                'coastal': {
                    colors: 'Palet pantai: putih dominan, navy/light blue, sandy beige, dengan aksen coral/aqua',
                    furniture: 'Furniture natural & light: rotan/rattan putih, kayu weathered white-wash, linen fabric',
                    walls: 'Dinding putih bersih ATAU light blue ATAU shiplap putih horizontal',
                    decor: 'Dekorasi nautical: rope decor, shell collection, driftwood, striped pattern (navy-putih), anchor',
                    lighting: 'Pencahayaan bright: pendant rotan natural, lantern putih, lampu dengan rope detail',
                    avoid: 'HINDARI: warna gelap, furniture berat, dekorasi formal, material luxurious'
                },
                'farmhouse': {
                    colors: 'Palet farmhouse: putih dominan, krem, gray, dengan aksen kayu natural dan metal hitam',
                    furniture: 'Furniture farmhouse: meja kayu distressed, kursi Windsor, bench kayu panjang, cabinet white-wash',
                    walls: 'Dinding shiplap putih ATAU board & batten ATAU brick ekspos dicat putih',
                    decor: 'Dekorasi country: galvanized metal decor, mason jars, vintage signs, cotton stems, gingham pattern',
                    lighting: 'Pencahayaan farmhouse: pendant barn light hitam, chandelier farmhouse dengan Edison bulbs',
                    avoid: 'HINDARI: furniture modern sleek, warna bold, dekorasi glamor, material chrome'
                },
                'art-deco': {
                    colors: 'Palet glamor: hitam & gold, navy & brass, emerald & gold, dengan accent metallics',
                    furniture: 'Furniture luxe geometric: velvet sofa dengan brass legs, mirror furniture, lacquered cabinet',
                    walls: 'Dinding dengan wallpaper geometric bold ATAU painted geometric patterns ATAU mirrored panels',
                    decor: 'Dekorasi glamorous: geometric mirrors, metallic sculptures, crystal vases, art deco frames',
                    lighting: 'Pencahayaan statement: geometric chandelier brass/gold, sconce dengan crystal, globe pendant',
                    avoid: 'HINDARI: rustic, minimalis plain, warna pastel, furniture kayu polos'
                },
                'japanese-zen': {
                    colors: 'Palet natural tenang: putih, beige, abu-abu, coklat kayu natural, dengan sentuhan hijau/hitam minimal',
                    furniture: 'Furniture low-profile Jepang: futon/platform bed rendah, meja tatami rendah, cushion lantai, storage minimal',
                    walls: 'Dinding polos putih/beige ATAU shoji screen paper ATAU wood slat wall',
                    decor: 'Dekorasi zen minimal: bonsai tree, bamboo plant, stone garden display, wabi-sabi ceramics, tatami mat',
                    lighting: 'Pencahayaan soft diffused: paper lantern, shoji lamp, recessed warm lighting',
                    avoid: 'HINDARI: furniture tinggi Western, banyak dekorasi, warna bold, pattern ramai'
                },
                'hollywood-glam': {
                    colors: 'Palet mewah dramatic: putih, hitam, silver/gold metallics, dengan accent purple/pink luxe',
                    furniture: 'Furniture glamorous: velvet tufted sofa, mirrored furniture, lucite/acrylic chairs, fur throws',
                    walls: 'Dinding dengan wallpaper luxe (damask metallic) ATAU painted high-gloss ATAU mirrored accent',
                    decor: 'Dekorasi dramatic: crystal chandelier besar, gold-frame mirrors, fur pillows, metallic sculptures',
                    lighting: 'Pencahayaan statement: crystal chandelier, Hollywood vanity lights, table lamps dengan crystal base',
                    avoid: 'HINDARI: rustic, minimalis, warna earthy, furniture kayu polos'
                },
                'mediterranean': {
                    colors: 'Palet Mediterranean: warm terracotta, blue (cobalt/turquoise), putih, yellow ochre',
                    furniture: 'Furniture Mediterranean: wrought iron, dark wood distressed, curved lines, mosaic-topped tables',
                    walls: 'Dinding textured plaster warm-toned ATAU arched doorways ATAU mosaic tile accent',
                    decor: 'Dekorasi Mediterranean: ceramic tiles colorful, wrought iron fixtures, terracotta pots, olive branches',
                    lighting: 'Pencahayaan warm: wrought iron chandelier, lantern metal dengan glass, pendant mosaic',
                    avoid: 'HINDARI: minimalis modern, warna cool/gray, furniture sleek, industrial style'
                },
                'tropical': {
                    colors: 'Palet tropis: putih/krem base, dengan banyak hijau (emerald/sage), accent coral/turquoise/yellow',
                    furniture: 'Furniture natural tropical: rattan/wicker, bamboo, teak wood, fabric botanical print',
                    walls: 'Dinding putih/krem ATAU wallpaper botanical/palm leaf print ATAU bamboo wall panels',
                    decor: 'Dekorasi tropical lush: BANYAK tanaman hijau besar (monstera/palm), rattan baskets, tropical artwork, banana leaf pillows',
                    lighting: 'Pencahayaan natural: rattan pendant, bamboo lamp, woven fixtures',
                    avoid: 'HINDARI: minimalis stark, warna gelap, furniture metal industrial, dekorasi formal'
                }
            };

            // Upload Room Photo with HEIC support
            if (roomUpload) {
                roomUpload.addEventListener('change', async (e) => {
                    const file = e.target.files[0];
                    if (!file) return;

                    let processedFile = file;

                    // HEIC to JPEG conversion
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        try {
                            const convertedBlob = await heic2any({
                                blob: file,
                                toType: 'image/jpeg',
                                quality: 0.9
                            });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                                type: 'image/jpeg'
                            });
                        } catch (err) {
                            console.error('HEIC conversion failed:', err);
                            alert('Gagal mengkonversi gambar HEIC');
                            return;
                        }
                    }

                    const reader = new FileReader();
                    reader.onloadend = () => {
                        roomImageData = reader.result;

                        // Show preview image on mobile
                        if (roomPreview && roomPreviewContainer) {
                            roomPreview.src = reader.result;
                            roomPreviewContainer.classList.remove('hidden');
                        }

                        // Update upload box
                        if (roomPlaceholder) {
                            roomPlaceholder.innerHTML = `
                                <i class="fas fa-check-circle text-green-500 text-3xl mb-2"></i>
                                <p class="text-sm font-semibold text-green-600">Foto kamar berhasil diupload!</p>
                            `;
                        }

                        // Enable generate button if ready
                        updateGenerateButton();
                    };
                    reader.readAsDataURL(processedFile);
                });
            }

            // Remove room preview (mobile)
            if (roomRemovePreview) {
                roomRemovePreview.addEventListener('click', () => {
                    roomImageData = null;
                    roomUpload.value = '';
                    if (roomPreviewContainer) roomPreviewContainer.classList.add('hidden');
                    if (roomPreview) roomPreview.src = '';
                    if (roomPlaceholder) {
                        roomPlaceholder.innerHTML = `
                            <i class="fas fa-door-open transition-transform duration-300" style="font-size: 2.5rem; color: #0ea5e9;"></i>
                            <p class="mt-2 text-sm font-semibold">🏠 Klik untuk upload foto kamar</p>
                            <p class="mt-1 text-xs text-gray-400">Format: JPG, PNG, HEIC</p>
                        `;
                    }
                    updateGenerateButton();
                });
            }

            // Upload Reference Photo with HEIC support (optional)
            if (referenceUpload) {
                referenceUpload.addEventListener('change', async (e) => {
                    const file = e.target.files[0];
                    if (!file) return;

                    let processedFile = file;

                    // HEIC to JPEG conversion
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        try {
                            const convertedBlob = await heic2any({
                                blob: file,
                                toType: 'image/jpeg',
                                quality: 0.9
                            });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                                type: 'image/jpeg'
                            });
                        } catch (err) {
                            console.error('HEIC conversion failed:', err);
                            alert('Gagal mengkonversi gambar HEIC');
                            return;
                        }
                    }

                    const reader = new FileReader();
                    reader.onloadend = () => {
                        referenceImageData = reader.result;

                        // Show preview image on mobile
                        if (referencePreview && referencePreviewContainer) {
                            referencePreview.src = reader.result;
                            referencePreviewContainer.classList.remove('hidden');
                        }

                        // Update upload box
                        if (referencePlaceholder) {
                            referencePlaceholder.innerHTML = `
                                <i class="fas fa-check-circle text-green-500 text-2xl mb-1"></i>
                                <p class="text-xs font-semibold text-green-600">Referensi diupload!</p>
                            `;
                        }
                    };
                    reader.readAsDataURL(processedFile);
                });
            }

            // Remove reference preview (mobile)
            if (roomRemoveReferencePreview) {
                roomRemoveReferencePreview.addEventListener('click', () => {
                    referenceImageData = null;
                    referenceUpload.value = '';
                    if (referencePreviewContainer) referencePreviewContainer.classList.add('hidden');
                    if (referencePreview) referencePreview.src = '';
                    if (referencePlaceholder) {
                        referencePlaceholder.innerHTML = `
                            <i class="fas fa-image transition-transform duration-300" style="font-size: 2rem; color: #0ea5e9;"></i>
                            <p class="mt-2 text-xs font-semibold">Upload referensi desain (opsional)</p>
                        `;
                    }
                });
            }

            // Theme Selection
            if (themeOptions) {
                themeOptions.addEventListener('click', (e) => {
                    const button = e.target.closest('[data-theme]');
                    if (!button) return;

                    // Remove selected state from all buttons
                    themeOptions.querySelectorAll('.option-btn-room').forEach(btn => {
                        btn.classList.remove('selected');
                    });

                    // Add selected state to clicked button
                    button.classList.add('selected');

                    // Update selected theme
                    selectedTheme = button.dataset.theme;

                    // Show/hide custom theme input
                    if (selectedTheme === 'custom' && customThemeContainer) {
                        customThemeContainer.classList.remove('hidden');
                    } else if (customThemeContainer) {
                        customThemeContainer.classList.add('hidden');
                    }
                });
            }

            // Ratio Selection
            if (ratioOptions) {
                ratioOptions.addEventListener('click', (e) => {
                    const button = e.target.closest('[data-value]');
                    if (!button) return;

                    // Remove selected state from all buttons
                    ratioOptions.querySelectorAll('.option-btn-room').forEach(btn => {
                        btn.classList.remove('selected');
                    });

                    // Add selected state to clicked button
                    button.classList.add('selected');

                    // Update selected ratio
                    selectedRatio = button.dataset.value;
                });
            }

            // Update Generate Button State
            function updateGenerateButton() {
                if (generateBtn) {
                    if (roomImageData) {
                        generateBtn.disabled = false;
                    } else {
                        generateBtn.disabled = true;
                    }
                }
            }

            // Generate Room Designs
            if (generateBtn) {
                generateBtn.addEventListener('click', async () => {
                    if (!roomImageData) {
                        alert('Please upload a room photo first!');
                        return;
                    }

                    let themeDescription = '';
                    if (selectedTheme === 'custom' && customThemeInput && customThemeInput.value.trim()) {
                        themeDescription = customThemeInput.value.trim();
                    }

                    if (emptyState) emptyState.classList.add('hidden');
                    if (resultsHeader) resultsHeader.classList.add('hidden');
                    if (loadingDiv) loadingDiv.classList.remove('hidden');

                    let attempts = 0;
                    const MAX_ATTEMPTS = 3;
                    let successCount = 0;

                    while (attempts < MAX_ATTEMPTS && successCount === 0) {
                        attempts++;
                        generatedImages = [];
                        if (resultsGrid) resultsGrid.innerHTML = '';

                        for (let i = 1; i <= selectedCount; i++) {
                            const card = document.createElement('div');
                            card.id = `room-card-${i}`;
                            card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-square';
                            card.innerHTML = '<div class="loader"></div>';
                            if (resultsGrid) resultsGrid.appendChild(card);
                        }

                        const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                            generateSingleDesign(i + 1, themeDescription)
                        );
                        await Promise.allSettled(generationPromises);

                        if (resultsGrid) {
                            resultsGrid.querySelectorAll('[id^="room-card-"]').forEach(card => {
                                if (!card.querySelector('img')) card.remove();
                            });
                        }

                        successCount = generatedImages.filter(Boolean).length;
                    }

                    if (loadingDiv) loadingDiv.classList.add('hidden');

                    if (successCount === 0) {
                        alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    } else {
                        if (resultsHeader) resultsHeader.classList.remove('hidden');
                        if (resultsCount) resultsCount.textContent = successCount;
                    }
                });
            }

            // Generate Single Room Design
            async function generateSingleDesign(index, themeDescription) {
                const card = document.getElementById(`room-card-${index}`);
                if (!card) return null;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                // Get theme details
                let themeDetails = '';
                if (selectedTheme !== 'custom') {
                    const theme = themeDescriptions[selectedTheme];
                    themeDetails = `
**WARNA & PALET:**
${theme.colors}

**FURNITURE:**
${theme.furniture}

**DINDING & WALL TREATMENT:**
${theme.walls}

**DEKORASI & AKSESORI:**
${theme.decor}

**PENCAHAYAAN:**
${theme.lighting}

**PENTING - ${theme.avoid}**`;
                } else {
                    themeDetails = `\n**DESKRIPSI CUSTOM:**\n${themeDescription}`;
                }

                // Build detailed prompt in Bahasa Indonesia
                let prompt = `**DESAIN INTERIOR PROFESIONAL - TEMA ${selectedTheme.replace(/-/g, ' ').toUpperCase()}**

Transformasikan foto ruangan yang diberikan menjadi desain interior profesional dengan tema ${selectedTheme.replace(/-/g, ' ')}.

**INSTRUKSI UTAMA:**
1. **PERTAHANKAN struktur arsitektur ruangan** (posisi dinding, jendela, pintu, plafon, dimensi ruangan)
2. **TERAPKAN TEMA SECARA KONSISTEN** di semua elemen ruangan
3. **PASTIKAN hasil terlihat REALISTIS** seperti foto interior profesional, BUKAN render 3D atau hasil AI

${themeDetails}

**DETAIL PENERAPAN TEMA:**

**A. WARNA:**
- Gunakan HANYA palet warna yang sesuai tema
- Pastikan konsistensi warna di semua elemen (dinding, furniture, dekorasi)
- Hindari warna yang tidak sesuai tema

**B. FURNITURE:**
- Pilih HANYA furniture yang cocok dengan style tema
- Atur penempatan furniture secara proporsional dan fungsional
- Pastikan skala furniture sesuai dengan ukuran ruangan

**C. DINDING:**
- Aplikasikan treatment dinding sesuai tema (cat/wallpaper/tekstur/panel)
- Jika tema memerlukan wallpaper atau tekstur khusus, pastikan terlihat jelas
- Perhatikan detail seperti molding, wainscoting jika sesuai tema

**D. DEKORASI:**
- Tambahkan dekorasi yang SPESIFIK sesuai tema
- Jumlah dekorasi harus sesuai karakteristik tema (minimal untuk minimalist, abundant untuk bohemian)
- Setiap item dekorasi harus mendukung tema

**E. PENCAHAYAAN:**
- Gunakan fixture lampu yang sesuai dengan style tema
- Perhatikan ambient lighting yang cocok dengan mood tema
- Lampu harus terlihat natural dan profesional

**F. TEKSTUR & MATERIAL:**
- Pilih material yang konsisten dengan tema (kayu/metal/fabric/stone/glass)
- Perhatikan finish material (matte/glossy/distressed/polished)

**CRITICAL REQUIREMENTS:**
- Hasil WAJIB terlihat seperti FOTO INTERIOR NYATA dari majalah desain
- TIDAK boleh terlihat seperti render 3D atau hasil AI generation
- Pencahayaan harus NATURAL dan PROFESIONAL
- Semua elemen harus KOHEREN dan HARMONIS
- Shadow, reflection, dan lighting harus REALISTIS`;

                // Add reference guidance if provided
                if (referenceImageData) {
                    prompt += `\n\n**FOTO REFERENSI DESAIN:**
Gunakan foto referensi yang disediakan sebagai INSPIRASI untuk:
- Skema warna dan palet yang digunakan
- Style dan arrangement furniture
- Jenis dekorasi dan aksesori
- Overall aesthetic dan mood ruangan

PENTING: Adaptasi elemen dari referensi agar cocok dengan struktur ruangan asli sambil tetap mempertahankan prinsip desain tema ${selectedTheme.replace(/-/g, ' ')}.`;
                }

                // Build payload
                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: 'image/jpeg', data: roomImageData.split(',')[1] } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                if (referenceImageData) {
                    payload.contents[0].parts.splice(2, 0, {
                        inlineData: { mimeType: 'image/jpeg', data: referenceImageData.split(',')[1] }
                    });
                }

                try {
                    const response = await fetch(apiUrl, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(payload)
                    });

                    const result = await response.json();
                    const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                    if (!imageData) throw new Error('No image data in response');

                    const imageUrl = `data:image/jpeg;base64,${imageData}`;
                    const filename = `room-design-${selectedTheme}-${index}-${Date.now()}.jpg`;

                    generatedImages[index - 1] = { url: imageUrl, filename: filename };

                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Room Design ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-cyan-500 to-blue-500 hover:from-cyan-600 hover:to-blue-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                } catch (err) {
                    console.error(`Error for design ${index}:`, err);
                    if (card) card.innerHTML = '';
                }
            }

            // Action Buttons Event Delegation
            if (resultsGrid) {
                resultsGrid.addEventListener('click', async (e) => {
                    e.stopPropagation();

                    const actionBtn = e.target.closest('[data-action]');
                    if (!actionBtn) return;

                    const action = actionBtn.dataset.action;
                    const index = parseInt(actionBtn.dataset.index, 10);
                    const imageData = generatedImages[index];

                    if (!imageData) return;

                    if (action === 'preview') {
                        if (window.showPreviewModal) {
                            window.showPreviewModal(imageData.url);
                        }
                    } else if (action === 'download') {
                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(imageData.url, imageData.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(imageData.url, imageData.filename);
                            } else {
                                // Fallback download
                                const link = document.createElement('a');
                                link.href = imageData.url;
                                link.download = imageData.filename;
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        } catch (error) {
                            console.error('Download error:', error);
                        }
                    }
                });
            }

            // Download All Button
            if (downloadAllBtn) {
                downloadAllBtn.addEventListener('click', async (e) => {
                    e.stopPropagation();

                    if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                    const originalHTML = downloadAllBtn.innerHTML;
                    downloadAllBtn.disabled = true;

                    try {
                        for (let i = 0; i < generatedImages.length; i++) {
                            const img = generatedImages[i];
                            if (!img) continue;  // Skip failed/empty slots
                            downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                            try {
                                if (window.downloadDataURINew) {
                                    await window.downloadDataURINew(img.url, img.filename);
                                } else if (window.downloadImage) {
                                    await window.downloadImage(img.url, img.filename);
                                }
                                await new Promise(resolve => setTimeout(resolve, 500));
                            } catch (error) {
                                console.error(`Error downloading image ${i + 1}:`, error);
                            }
                        }

                        downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                        setTimeout(() => {
                            downloadAllBtn.disabled = false;
                            downloadAllBtn.innerHTML = originalHTML;
                        }, 2000);
                    } catch (error) {
                        console.error('Download all error:', error);
                        downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                        setTimeout(() => {
                            downloadAllBtn.disabled = false;
                            downloadAllBtn.innerHTML = originalHTML;
                        }, 2000);
                    }
                });
            }

        })();

    // --- STORY UPDATE GENERATOR LOGIC ---
    (function() {
        const apiKey = "";
        const form = document.getElementById('story-update-form');
        const generateBtn = document.getElementById('story-update-generate-btn');
        const loader = document.getElementById('story-update-loader');
        const resultsPlaceholder = document.getElementById('story-update-placeholder');
        const errorMessage = document.getElementById('story-update-error-message');
        const resultsWrapper = document.getElementById('story-update-results-wrapper');
        const downloadAllBtn = document.getElementById('story-download-all-btn');
        const resultGrid = document.getElementById('story-update-result-grid');
        const photoUpload = document.getElementById('story-update-photo-upload');
        const photoPreview = document.getElementById('story-update-photo-preview');
        const uploadLabel = document.getElementById('story-update-upload-label');

        // Count selection (1-10), default 4
        let selectedCount = 4;
        const storyUpdateCountSel = document.getElementById('story-update-count-selection-grid');
        if (storyUpdateCountSel) {
            storyUpdateCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                storyUpdateCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Aspect ratio selector
        const ratioButtons = document.querySelectorAll('.story-ratio-btn');
        let selectedRatio = '9:16'; // Default

        let uploadedImageBase64 = null;
        let uploadedMimeType = null;
        let generatedImages = [];
        generateBtn.disabled = true;

        // Aspect ratio selection handler (mobile-friendly)
        ratioButtons.forEach(btn => {
            btn.addEventListener('click', (e) => {
                e.stopPropagation(); // Mobile-safe: ONLY stopPropagation

                // Remove selected state from all buttons
                ratioButtons.forEach(b => {
                    b.classList.remove('story-ratio-selected');
                    // Reset to default styling
                    b.classList.add('bg-gray-50', 'hover:bg-gray-100', 'text-gray-700', 'border-gray-300', 'font-medium');
                    b.classList.remove('bg-indigo-600', 'text-white', 'border-indigo-600', 'font-bold', 'shadow-md');
                });

                // Add selected state to clicked button
                btn.classList.remove('bg-gray-50', 'hover:bg-gray-100', 'text-gray-700', 'border-gray-300', 'font-medium');
                btn.classList.add('story-ratio-selected');

                selectedRatio = btn.dataset.value;
            });
        });

        photoUpload.addEventListener('change', (event) => {
            const file = event.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = (e) => {
                    uploadedImageBase64 = e.target.result.split(',')[1];
                    uploadedMimeType = file.type;
                    photoPreview.src = e.target.result;
                    photoPreview.style.display = 'block';
                    uploadLabel.textContent = 'Ganti Foto Referensi';
                    generateBtn.disabled = false;
                };
                reader.readAsDataURL(file);
            }
        });

        const getRandomElement = (arr) => arr[Math.floor(Math.random() * arr.length)];

        function constructPrompt(economicStatus, theme, photoStyle) {
            let scenario = '';
            const indoCities = ['Jakarta', 'Bandung', 'Yogyakarta', 'Surabaya', 'Semarang'];
            const internationalCities = ['Paris', 'Tokyo', 'New York', 'London', 'Singapore'];
            const themes = {
                'makan': 'eating',
                'jalan-jalan': 'traveling and sightseeing',
                'kerja': 'working',
                'kuliah': 'studying at a university campus',
                'olahraga': 'exercising',
                'nongkrong': 'hanging out'
            };
            let activity = themes[theme] || 'doing a daily activity';
            const specificActivity = document.getElementById('story-update-specific-activity').value.trim();

            if (specificActivity) {
                activity = specificActivity;
                scenario = `in a place that looks like ${getRandomElement(indoCities)}`;
            } else {
                switch (economicStatus) {
                    case 'gaji_bawah_umk':
                        switch (theme) {
                            case 'makan': scenario = getRandomElement([ `at a simple street food stall ('warteg') in ${getRandomElement(indoCities)}`,  `at a bustling 'mie ayam' cart on the sidewalk in ${getRandomElement(indoCities)}`,  `at a 'nasi goreng' street vendor at night in ${getRandomElement(indoCities)}`, `at a 'bubur ayam' street cart in the morning in ${getRandomElement(indoCities)}`, `at a 'sate padang' stall in a crowded market in ${getRandomElement(indoCities)}`, `at a 'ketoprak' or 'gado-gado' vendor in a residential area in ${getRandomElement(indoCities)}` ]); break;
                            case 'jalan-jalan': scenario = getRandomElement([ `walking through a traditional market ('pasar tradisional') in ${getRandomElement(indoCities)}`,  `sitting on a bench in a public city park ('alun-alun') in ${getRandomElement(indoCities)}`,  `exploring a crowded, narrow city alleyway ('gang') in ${getRandomElement(indoCities)}`, `visiting a free city museum on a quiet afternoon in ${getRandomElement(indoCities)}`, `window shopping at a local, non-branded mall in ${getRandomElement(indoCities)}`, `watching street performers at a bustling city square at night in ${getRandomElement(indoCities)}`, `taking photos on an iconic city bridge or flyover in ${getRandomElement(indoCities)}`, `relaxing at a city forest ('hutan kota') in ${getRandomElement(indoCities)}`, `enjoying the sunset from a public spot overlooking the city in ${getRandomElement(indoCities)}`, `visiting a historical monument in ${getRandomElement(indoCities)}` ]); break;
                            case 'kerja': scenario = getRandomElement([ `in a small, modest office in Indonesia`,  `at a cashier in a minimarket in ${getRandomElement(indoCities)}`,  `in a factory production line in an industrial area in Indonesia`, `as a ride-hailing driver waiting for an order`, `as a package courier sorting parcels`, `as a construction worker on a building site` ]); break;
                            case 'kuliah': scenario = getRandomElement([ `in a simple classroom at a public university in ${getRandomElement(indoCities)}`,  `studying in the campus library with old books in ${getRandomElement(indoCities)}`,  `eating at the university's cheap food court ('kantin') in ${getRandomElement(indoCities)}`, `discussing a group project on the campus floor corridor`, `photocopying lecture notes at a shop near campus` ]); break;
                            case 'olahraga': scenario = getRandomElement([ `jogging in a public city park ('alun-alun') in ${getRandomElement(indoCities)}`,  `playing badminton on a public court in ${getRandomElement(indoCities)}`,  `doing street workout at a public facility in ${getRandomElement(indoCities)}`, `playing football on a neighborhood field`, `joining a mass aerobics session during car-free day` ]); break;
                            case 'nongkrong': scenario = getRandomElement([ `at a humble streetside coffee stall ('warkop') in ${getRandomElement(indoCities)}`,  `sitting on the roadside with friends in ${getRandomElement(indoCities)}`,  `at a free public Wi-Fi corner in ${getRandomElement(indoCities)}`, `at a food stall ('angkringan') at night`, `playing guitar at a neighborhood security post ('pos ronda')` ]); break;
                        }
                        break;
                    case 'gaji_sesuai_umk':
                        switch (theme) {
                            case 'makan': scenario = getRandomElement([ `at a trendy, modern local cafe in ${getRandomElement(indoCities)}`,  `at a popular all-you-can-eat restaurant in ${getRandomElement(indoCities)}`,  `at a Japanese ramen shop in a mall in ${getRandomElement(indoCities)}`, `at a Korean BBQ restaurant with friends in ${getRandomElement(indoCities)}`, `at a popular pizza place in ${getRandomElement(indoCities)}`, `at a 'ayam geprek' restaurant with a high spice level in ${getRandomElement(indoCities)}` ]); break;
                            case 'jalan-jalan': scenario = getRandomElement([ `at a popular shopping mall in ${getRandomElement(indoCities)}`,  `visiting a local tourist attraction like a waterfall near Bandung`,  `watching a movie at the cinema in ${getRandomElement(indoCities)}`, `at a local zoo or botanical garden on the weekend`, `attending a concert of a popular local band`, `exploring a trendy, Instagrammable spot in the city` ]); break;
                            case 'kerja': scenario = getRandomElement([ `in a bright, modern co-working space in ${getRandomElement(indoCities)}`,  `in an open-plan office in ${getRandomElement(indoCities)}`,  `attending a meeting in a glass-walled conference room in ${getRandomElement(indoCities)}`, `working from home at a cozy desk setup`, `giving a presentation to clients`, `working overtime late at night in the office` ]); break;
                            case 'kuliah': scenario = getRandomElement([ `on a busy, modern university campus in ${getRandomElement(indoCities)}`,  `collaborating on a project at a campus cafe in ${getRandomElement(indoCities)}`,  `in a modern university laboratory in ${getRandomElement(indoCities)}`, `attending a seminar or workshop on campus`, `as a committee member for a campus event` ]); break;
                            case 'olahraga': scenario = getRandomElement([ `at a local commercial gym in ${getRandomElement(indoCities)}`,  `taking a zumba or yoga class in ${getRandomElement(indoCities)}`,  `cycling on the city's car-free day in ${getRandomElement(indoCities)}`, `playing futsal with office mates after work`, `joining a fun run event on the weekend` ]); break;
                            case 'nongkrong': scenario = getRandomElement([ `at a popular, bustling coffee shop in ${getRandomElement(indoCities)}`,  `at a cafe with live music in ${getRandomElement(indoCities)}`,  `at a modern food court with friends in ${getRandomElement(indoCities)}`, `at an art exhibition or local bazaar`, `watching a stand-up comedy show` ]); break;
                        }
                        break;
                    case 'gaji_diatas_umk':
                         switch (theme) {
                            case 'makan': scenario = getRandomElement([ `at an upscale fine dining restaurant in Jakarta`,  `at a rooftop bar with a city view in Jakarta`,  `at a private teppanyaki dinner in Jakarta`, `at a high-end sushi omakase restaurant in SCBD, Jakarta`, `at a premium steakhouse in a 5-star hotel in Jakarta`, `at a modern Indonesian gastronomy restaurant in South Jakarta` ]); break;
                            case 'jalan-jalan': scenario = getRandomElement([ `on a holiday at a private beach villa in Bali`,  `exploring a high-end resort in Labuan Bajo`,  `hiking a scenic mountain like Rinjani with premium gear`, `on a staycation at a 5-star hotel in Jakarta`, `on a liveaboard sailing trip around Komodo National Park`, `attending a major music festival like DWP` ]); break;
                            case 'kerja': scenario = getRandomElement([ `in a high-floor corporate office with a city view of SCBD, Jakarta`,  `giving a presentation in a large auditorium in Jakarta`,  `on a business lunch at a fancy restaurant in Jakarta`, `on a business trip, at an airport lounge`, `signing a major contract in a boardroom` ]); break;
                            case 'kuliah': scenario = getRandomElement([ `at a prestigious private university's modern library in Jakarta`,  `presenting a thesis in front of professors`,  `participating in an international student conference`, `joining an overseas student exchange program`, `working as a research assistant for a professor` ]); break;
                            case 'olahraga': scenario = getRandomElement([ `playing golf at a country club near Jakarta`,  `playing tennis at an exclusive sports club in Jakarta`,  `scuba diving in Raja Ampat`, `training for a marathon with a personal coach`, `doing reformer pilates at a boutique studio` ]); break;
                            case 'nongkrong': scenario = getRandomElement([ `at a fancy rooftop bar overlooking the city skyline of Jakarta`,  `at a premium lounge in a 5-star hotel in Jakarta`,  `attending a gallery opening in Kemang, Jakarta`, `at a wine tasting event`, `attending a movie premiere` ]); break;
                        }
                        break;
                    case 'orang_kaya_baru':
                        switch (theme) {
                            case 'makan': scenario = getRandomElement([ `at a famous, trendy Michelin-starred restaurant in ${getRandomElement(internationalCities)}`,  `popping a bottle of champagne at a beach club in Bali`,  `eating gold-flaked steak in Dubai`, `eating fresh caviar on a private jet`, `showcasing a tower of fresh seafood in Saint-Tropez`, `attending a celebrity chef's exclusive pop-up dinner` ]); break;
                            case 'jalan-jalan': scenario = getRandomElement([ `shopping in a luxury district like Orchard Road Singapore, holding many designer bags`,  `in a first-class airplane seat, showing off the amenities`,  `on a private helicopter tour over New York`, `driving a rented Ferrari through Monaco`, `attending Paris Fashion Week from a front-row seat`, `staying in a lavish overwater bungalow in the Maldives` ]); break;
                            case 'kerja': scenario = getRandomElement([ `working on a MacBook on a yacht in the Mediterranean`,  `taking a video call from a luxury hotel suite in Paris`,  `closing a deal in a supercar in London`, `as a guest speaker at a crypto conference`, `monitoring stock charts from a beachside villa` ]); break;
                            case 'kuliah': scenario = getRandomElement([ `next to a Lamborghini on the campus of Stanford`,  `studying in a penthouse apartment with a view of Tokyo Tower`,  `on a university trip to the Amazon rainforest`, `attending a spring break party in Cancun`, `joining a startup incubator program in Silicon Valley` ]); break;
                            case 'olahraga': scenario = getRandomElement([ `attending a Formula 1 Grand Prix in Monaco`,  `skiing in the Swiss Alps with branded gear`,  `watching a ringside boxing match in Las Vegas`, `skydiving over The Palm, Dubai`, `racing a supercar on a professional track` ]); break;
                            case 'nongkrong': scenario = getRandomElement([ `at an exclusive beach club in Ibiza`,  `partying in a VIP section of a famous nightclub in Mykonos`,  `on a yacht party with friends near the coast of Italy`, `attending a high-profile fashion week afterparty`, `flying to Las Vegas just to see a concert` ]); break;
                        }
                        break;
                    case 'orang_kaya_lama':
                        switch (theme) {
                            case 'makan': scenario = getRandomElement([ `having a quiet, elegant private dinner on a classic sailboat`,  `at a private wine tasting event in a vineyard in Tuscany`,  `hosting a garden party at a family estate in the English countryside`, `enjoying a bespoke multi-course meal prepared by a private chef at home`, `having a quiet lunch at a members-only countryside club`, `enjoying rare cheeses and vintage wine in a private family cellar` ]); break;
                            case 'jalan-jalan': scenario = getRandomElement([ `attending an art auction at Sotheby's in London`,  `visiting a classical music concert in Vienna`,  `on a cultural heritage tour in Rome`, `on a bespoke luxury safari in Tanzania`, `visiting their family's summer home in the South of France`, `attending the exclusive Venice Biennale art exhibition` ]); break;
                            case 'kerja': scenario = getRandomElement([ `in a classic, wood-paneled home library, reviewing documents`,  `attending a board meeting for a family foundation in Geneva`,  `inspecting a family-owned vineyard in Bordeaux`, `visiting a family-owned art gallery`, `attending a philanthropy event for charity` ]); break;
                            case 'kuliah': scenario = getRandomElement([ `participating in a polo match at Cambridge`,  `debating in a historic university hall at Oxford`,  `attending a lecture by a Nobel laureate at Harvard`, `joining a competitive rowing (regatta) team`, `studying philosophy in a historic campus garden` ]); break;
                            case 'olahraga': scenario = getRandomElement([ `horseback riding on a vast countryside estate`,  `sailing a classic yacht in the French Riviera`,  `playing a game of polo`, `fox hunting (dressage) in the English countryside`, `fencing at an exclusive club` ]); break;
                            case 'nongkrong': scenario = getRandomElement([ `attending a sophisticated charity gala event in New York`,  `at a private member's club in London`,  `at a classical music soiree in a private mansion`, `attending a horse racing event like the Royal Ascot`, `enjoying an opera at the Royal Opera House in London` ]); break;
                        }
                        break;
                }
            }

            let finalPrompt;
            if (photoStyle === 'sendiri') {
                const selfShotTypes = [ { type: 'classic_high_angle_selfie', description: 'The photo is a high-angle selfie, taken from the perspective of a camera held by the person themselves, looking up towards the camera. Do not show the phone or arm. It should feel like looking through the phone\'s camera.' }, { type: 'classic_low_angle_selfie', description: 'The photo is a low-angle selfie, taken from the perspective of a camera held by the person themselves, looking down towards the camera. Do not show the phone or arm. It should feel like looking through the phone\'s camera.' }, { type: 'candid_laughing_selfie', description: 'The photo is a candid selfie with a genuine laugh or smile, slightly off-center, as if taken in a happy moment. Taken from the perspective of their own phone camera. Do not show the phone or arm.' }, { type: 'close_up_selfie', description: 'The photo is a close-up selfie focusing on their expression, taken from the perspective of a camera held by the person. Do not show the phone or their arm holding it.' }, { type: 'mirror_selfie', description: 'The photo is a mirror selfie, showing them holding the phone to take the picture in a reflection.' }, { type: 'pov_hands', description: 'The photo is a point-of-view (POV) shot where only their hands are visible, interacting with an object (like a coffee cup, a book, or a passport). Their face is not visible.' }, { type: 'pov_feet', description: 'The photo is a feet-selfie (a shot looking down at their own feet), showing the interesting ground or environment below. Their face is not visible.' }, { type: 'half_face_selfie', description: 'The photo is an artistic selfie where only half of their face is in the frame, creating a candid look. Taken from the perspective of their own phone camera. Do not show the phone or arm.' }, { type: 'walking_selfie', description: 'The photo is a slightly motion-blurred selfie, as if taken while walking, capturing a dynamic and natural moment. Taken from the perspective of their own phone camera. Do not show the phone or arm.' } ];
                const chosenType = getRandomElement(selfShotTypes);
                finalPrompt = `A realistic, natural, candid photo for a social media story, featuring the person from the uploaded image. Match their face and features from the reference photo. ${chosenType.description} The person is ${activity} ${scenario}. Photographed on an iPhone 15 Pro, ultra-realistic, high detail, natural lighting. No text or social media UI elements.`;
            } else { // bareng_temen
                const friendCameraAngles = [ 'candid shot taken by another friend from a slight distance', 'a group selfie with the main person and their friends', 'a shot where everyone is laughing, taken from a natural, eye-level perspective by a friend', 'an over-the-shoulder shot from one friend, looking towards the main person and other friends' ];
                const randomAngle = getRandomElement(friendCameraAngles);
                activity = activity === 'hanging out' ? 'hanging out with friends' : `${activity} with friends`;
                finalPrompt = `A realistic, natural, candid photo for a social media story. It features the person from the uploaded image and one or two friends, all interacting happily. Match the main person's face and features from the reference photo. The photo is taken by another friend, capturing a genuine moment (${randomAngle}). They are ${activity} ${scenario}. Photographed on an iPhone 15 Pro, ultra-realistic, high detail, natural lighting. No text or social media UI elements.`;
            }
            return finalPrompt;
        }

        // selectedCount sudah di-declare di scope luar (count selector)

        async function generateSingleStory(index) {
            const card = document.getElementById(`story-card-${index}`);
            try {
                const finalPrompt = constructPrompt(
                    document.getElementById('story-update-economic-status').value,
                    document.getElementById('story-update-theme').value,
                    document.querySelector('#content-story-update input[name="photo-style"]:checked').value
                );
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const payload = {
                    contents: [{ parts: [{ text: finalPrompt }, { inlineData: { mimeType: uploadedMimeType, data: uploadedImageBase64 } }] }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                    safetySettings: [
                        { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" }
                    ]
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data');

                const url = `data:image/png;base64,${base64Data}`;
                const filename = `story-update-${index}.png`;
                generatedImages[index - 1] = { url, filename };

                if (card) {
                    card.innerHTML = `
                        <img src="${url}" alt="Story Update ${index}" class="w-full h-auto object-cover">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-all duration-300 flex items-end justify-center gap-2 p-4">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-indigo-500 to-purple-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-pink-500 to-rose-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`Error generating story ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        generateBtn.addEventListener('click', async () => {
            if (generateBtn.disabled) return;
            if (!uploadedImageBase64) {
                alert("Silakan upload foto referensi terlebih dahulu.");
                return;
            }

            generatedImages = [];
            generateBtn.disabled = true;
            generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating Stories...';
            loader.classList.add('hidden');
            resultsWrapper.classList.add('hidden');
            resultGrid.innerHTML = '';
            resultsPlaceholder.classList.add('hidden');
            errorMessage.classList.add('hidden');

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = new Array(selectedCount).fill(null);
                resultGrid.innerHTML = '';

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `story-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = `<div class="bg-gray-100 flex items-center justify-center" style="aspect-ratio:${selectedRatio.replace(':','/')}"><div class="animate-spin rounded-full h-10 w-10 border-b-2 border-indigo-600"></div></div>`;
                    resultGrid.appendChild(card);
                }

                resultsWrapper.classList.remove('hidden');

                const promises = Array.from({ length: selectedCount }, (_, i) => generateSingleStory(i + 1));
                await Promise.allSettled(promises);

                resultGrid.querySelectorAll('[id^="story-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                successCount = generatedImages.filter(Boolean).length;
            }

            generateBtn.disabled = false;
            generateBtn.innerHTML = '<i class="fas fa-magic mr-2"></i>Generate Stories';

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            }
        });

        // Event delegation for preview and download buttons
        if (resultGrid) {
            resultGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.classList.contains('action-btn') ?
                    e.target : e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all button handler
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                // Prevent multiple clicks
                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;  // Skip failed/empty slots
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                            // Continue with next image even if one fails
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }
    })();

    // --- PHOTO ANGLE GENERATOR PRO LOGIC ---
    (function() {
        // DOM Elements
        const uploadArea = document.getElementById('pa-upload-area');
        const photoUpload = document.getElementById('pa-photo-upload');
        const photoPreview = document.getElementById('pa-photo-preview');
        const uploadPlaceholder = document.getElementById('pa-upload-placeholder');
        const presetRadios = document.querySelectorAll('input[name="pa-preset"]');
        const ratioBtns = document.querySelectorAll('.pa-ratio-btn');
        const generateBtn = document.getElementById('pa-generate-btn');
        const placeholder = document.getElementById('pa-placeholder');
        const loading = document.getElementById('pa-loading');
        const loadingText = document.getElementById('pa-loading-text');
        const progressBar = document.getElementById('pa-progress-bar');
        const progressText = document.getElementById('pa-progress-text');
        const resultsHeader = document.getElementById('pa-results-header');
        const resultsGrid = document.getElementById('pa-results-grid');
        const downloadAllBtn = document.getElementById('pa-download-all-btn');
        const errorMessage = document.getElementById('pa-error-message');
        const errorText = document.getElementById('pa-error-text');

        let uploadedImageBase64 = null;
        let uploadedMimeType = null;
        let selectedPreset = 'all';
        let selectedRatio = '1:1';
        let generatedImages = [];

        // Angle definitions for each preset
        const anglePresets = {
            all: [
                { name: 'Front View', prompt: 'front-facing view, direct frontal angle, centered composition' },
                { name: 'Side View', prompt: 'side profile view, 90-degree angle, showing the side' },
                { name: 'Top View', prompt: 'bird\'s eye view, top-down perspective, overhead angle' },
                { name: 'Bottom View', prompt: 'low angle view, looking up perspective, worm\'s eye view' },
                { name: '45° Angle', prompt: '45-degree angle view, three-quarter perspective, diagonal view' },
                { name: 'Back View', prompt: 'rear view, back-facing angle, showing the back' }
            ],
            product: [
                { name: '0° View', prompt: 'straight front view, product facing forward' },
                { name: '60° View', prompt: '60-degree rotation, slight right side visible' },
                { name: '120° View', prompt: '120-degree rotation, right side view' },
                { name: '180° View', prompt: '180-degree rotation, back view of product' },
                { name: '240° View', prompt: '240-degree rotation, left side view' },
                { name: '300° View', prompt: '300-degree rotation, slight left side visible' }
            ],
            portrait: [
                { name: 'Full Face', prompt: 'straight portrait, face directly to camera, front view' },
                { name: 'Left Profile', prompt: 'left side profile, face turned 90 degrees left' },
                { name: 'Right Profile', prompt: 'right side profile, face turned 90 degrees right' },
                { name: '3/4 Left', prompt: 'three-quarter left view, face slightly turned left' },
                { name: '3/4 Right', prompt: 'three-quarter right view, face slightly turned right' },
                { name: 'Looking Up', prompt: 'upward looking angle, chin up, looking towards camera above' }
            ]
        };

        // Upload area click handler
        if (uploadArea) {
            uploadArea.addEventListener('click', () => photoUpload.click());
        }

        // File upload handler
        if (photoUpload) {
            photoUpload.addEventListener('change', async (event) => {
                const file = event.target.files[0];
                if (!file) return;

                try {
                    // Handle HEIC conversion
                    let processedFile = file;
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        if (typeof heic2any !== 'undefined') {
                            const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg' });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                        }
                    }

                    const reader = new FileReader();
                    reader.onload = (e) => {
                        uploadedImageBase64 = e.target.result.split(',')[1];
                        uploadedMimeType = processedFile.type;
                        photoPreview.src = e.target.result;
                        photoPreview.classList.remove('hidden');
                        uploadPlaceholder.classList.add('hidden');
                        generateBtn.disabled = false;
                    };
                    reader.readAsDataURL(processedFile);
                } catch (error) {
                    console.error('Error processing image:', error);
                    showError('Failed to process image. Please try another file.');
                }
            });
        }

        // Preset selection
        presetRadios.forEach(radio => {
            radio.addEventListener('change', (e) => {
                selectedPreset = e.target.value;
            });
        });

        // Aspect ratio selection
        ratioBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                ratioBtns.forEach(b => {
                    b.classList.remove('bg-cyan-600', 'text-white', 'ring-2', 'ring-cyan-400');
                    b.classList.add('bg-gray-200', 'text-gray-700');
                });
                btn.classList.remove('bg-gray-200', 'text-gray-700');
                btn.classList.add('bg-cyan-600', 'text-white', 'ring-2', 'ring-cyan-400');
                selectedRatio = btn.dataset.ratio;
            });
        });

        // Generate single angle (fail-silent)
        async function generateSingleAngle(index, angle) {
            const card = document.getElementById(`pa-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const prompt = `Transform this image to show a ${angle.prompt}.

IMPORTANT INSTRUCTIONS:
- Maintain the exact same subject, object, or person from the original image
- Change ONLY the camera angle and perspective as specified
- Keep all details, colors, lighting, and characteristics identical
- DO NOT add or remove any elements
- DO NOT change the background significantly
- Make it look like the same photo taken from a different angle
- Ensure professional photo quality
- Keep the composition natural and realistic

Generate a ${angle.prompt} of the exact same subject.`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: uploadedMimeType, data: uploadedImageBase64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Image = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Image) throw new Error('No image data');

                const imageUrl = `data:image/png;base64,${base64Image}`;
                const filename = `photo-angle-${selectedPreset}-${index}.png`;
                generatedImages[index - 1] = { url: imageUrl, filename, angleName: angle.name };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:scale-105';
                    card.innerHTML = `
                        <img src="${imageUrl}" alt="${angle.name}" class="w-full h-auto object-cover">
                        <div class="absolute top-2 left-2 bg-cyan-600 text-white text-xs px-3 py-1 rounded-full font-semibold shadow-lg">${angle.name}</div>
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-all duration-300 flex items-end justify-center gap-2 p-4">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-cyan-500 to-blue-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                                <i class="fas fa-eye"></i><span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-green-500 to-green-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                                <i class="fas fa-download"></i><span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`Error generating angle ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Generate button
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!uploadedImageBase64) {
                    showError('Please upload a photo first.');
                    return;
                }

                generatedImages = [];
                generateBtn.disabled = true;
                placeholder.classList.add('hidden');
                loading.classList.remove('hidden');
                resultsHeader.classList.add('hidden');
                resultsGrid.classList.add('hidden');
                resultsGrid.innerHTML = '';
                errorMessage.classList.add('hidden');

                const angles = anglePresets[selectedPreset];
                const totalAngles = angles.length;

                let attempts = 0;
                const MAX_ATTEMPTS = 3;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedImages = new Array(totalAngles).fill(null);
                    resultsGrid.innerHTML = '';
                    progressBar.style.width = '0%';
                    progressText.textContent = `0/${totalAngles} completed`;
                    loadingText.textContent = 'Generating angles...';
                    resultsGrid.classList.remove('hidden');

                    for (let i = 1; i <= totalAngles; i++) {
                        const card = document.createElement('div');
                        card.id = `pa-card-${i}`;
                        card.className = 'relative rounded-xl overflow-hidden bg-gradient-to-br from-cyan-50 to-blue-50 border-2 border-cyan-200 flex items-center justify-center aspect-square';
                        card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-cyan-600"></div>';
                        resultsGrid.appendChild(card);
                    }

                    const promises = angles.map((angle, i) => generateSingleAngle(i + 1, angle));
                    await Promise.allSettled(promises);

                    resultsGrid.querySelectorAll('[id^="pa-card-"]').forEach(card => {
                        if (!card.querySelector('img')) card.remove();
                    });
                    successCount = generatedImages.filter(Boolean).length;
                }

                loading.classList.add('hidden');
                generateBtn.disabled = false;

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                } else {
                    resultsHeader.classList.remove('hidden');
                }
            });
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.classList.contains('action-btn') ?
                    e.target : e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all button
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async () => {
                if (generatedImages.length === 0) return;

                downloadAllBtn.disabled = true;
                downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < generatedImages.length; i++) {
                    const img = generatedImages[i];
                    if (!img) continue;
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(img.url, img.filename);
                    }
                    if (i < generatedImages.length - 1) {
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }
                }

                downloadAllBtn.disabled = false;
                downloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download All';
            });

            // Add mobile touch support
            if (window.addMobileTouchSupport) {
                window.addMobileTouchSupport(downloadAllBtn);
            }
        }

        // Show error function
        function showError(message) {
            errorText.textContent = message;
            errorMessage.classList.remove('hidden');
            setTimeout(() => {
                errorMessage.classList.add('hidden');
            }, 5000);
        }
    })();

    // --- PHOTO STYLE MATCHER PRO LOGIC ---
    (function() {
        // DOM Elements
        const yourUploadArea = document.getElementById('sm-your-upload-area');
        const yourPhoto = document.getElementById('sm-your-photo');
        const yourPreview = document.getElementById('sm-your-preview');
        const yourPlaceholder = document.getElementById('sm-your-placeholder');

        const refUploadArea = document.getElementById('sm-ref-upload-area');
        const refPhoto = document.getElementById('sm-ref-photo');
        const refPreview = document.getElementById('sm-ref-preview');
        const refPlaceholder = document.getElementById('sm-ref-placeholder');

        const strengthRadios = document.querySelectorAll('input[name="sm-strength"]');
        const ratioBtns = document.querySelectorAll('.sm-ratio-btn');
        const generateBtn = document.getElementById('sm-generate-btn');

        const placeholder = document.getElementById('sm-placeholder');
        const loading = document.getElementById('sm-loading');
        const loadingText = document.getElementById('sm-loading-text');
        const progressBar = document.getElementById('sm-progress-bar');
        const progressText = document.getElementById('sm-progress-text');

        const resultsHeader = document.getElementById('sm-results-header');
        const resultsGrid = document.getElementById('sm-results-grid');
        const compareBtn = document.getElementById('sm-compare-btn');
        const downloadAllBtn = document.getElementById('sm-download-all-btn');

        const comparisonModal = document.getElementById('sm-comparison-modal');
        const closeComparison = document.getElementById('sm-close-comparison');
        const comparisonOriginal = document.getElementById('sm-comparison-original');
        const comparisonStyled = document.getElementById('sm-comparison-styled');

        const errorMessage = document.getElementById('sm-error-message');
        const errorText = document.getElementById('sm-error-text');

        let yourImageBase64 = null;
        let yourImageMimeType = null;
        let refImageBase64 = null;
        let refImageMimeType = null;
        let selectedStrength = 'moderate';
        let selectedRatio = '1:1';
        let generatedImages = [];

        // Style strength variations
        const strengthVariations = {
            subtle: [
                { name: 'Var 1', weight: 0.2 },
                { name: 'Var 2', weight: 0.25 },
                { name: 'Var 3', weight: 0.3 },
                { name: 'Var 4', weight: 0.35 },
                { name: 'Var 5', weight: 0.4 },
                { name: 'Var 6', weight: 0.45 }
            ],
            moderate: [
                { name: 'Var 1', weight: 0.4 },
                { name: 'Var 2', weight: 0.5 },
                { name: 'Var 3', weight: 0.6 },
                { name: 'Var 4', weight: 0.65 },
                { name: 'Var 5', weight: 0.7 },
                { name: 'Var 6', weight: 0.75 }
            ],
            strong: [
                { name: 'Var 1', weight: 0.7 },
                { name: 'Var 2', weight: 0.75 },
                { name: 'Var 3', weight: 0.8 },
                { name: 'Var 4', weight: 0.85 },
                { name: 'Var 5', weight: 0.9 },
                { name: 'Var 6', weight: 0.95 }
            ]
        };

        // Your photo upload
        if (yourUploadArea) {
            yourUploadArea.addEventListener('click', () => yourPhoto.click());
        }

        if (yourPhoto) {
            yourPhoto.addEventListener('change', async (event) => {
                const file = event.target.files[0];
                if (!file) return;

                try {
                    let processedFile = file;
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        if (typeof heic2any !== 'undefined') {
                            const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg' });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                        }
                    }

                    const reader = new FileReader();
                    reader.onload = (e) => {
                        yourImageBase64 = e.target.result.split(',')[1];
                        yourImageMimeType = processedFile.type;
                        yourPreview.src = e.target.result;
                        yourPreview.classList.remove('hidden');
                        yourPlaceholder.classList.add('hidden');
                        checkReadyToGenerate();
                    };
                    reader.readAsDataURL(processedFile);
                } catch (error) {
                    console.error('Error processing your image:', error);
                    showError('Failed to process your photo. Please try another file.');
                }
            });
        }

        // Reference photo upload
        if (refUploadArea) {
            refUploadArea.addEventListener('click', () => refPhoto.click());
        }

        if (refPhoto) {
            refPhoto.addEventListener('change', async (event) => {
                const file = event.target.files[0];
                if (!file) return;

                try {
                    let processedFile = file;
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        if (typeof heic2any !== 'undefined') {
                            const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg' });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                        }
                    }

                    const reader = new FileReader();
                    reader.onload = (e) => {
                        refImageBase64 = e.target.result.split(',')[1];
                        refImageMimeType = processedFile.type;
                        refPreview.src = e.target.result;
                        refPreview.classList.remove('hidden');
                        refPlaceholder.classList.add('hidden');
                        checkReadyToGenerate();
                    };
                    reader.readAsDataURL(processedFile);
                } catch (error) {
                    console.error('Error processing reference image:', error);
                    showError('Failed to process reference photo. Please try another file.');
                }
            });
        }

        // Strength selection
        strengthRadios.forEach(radio => {
            radio.addEventListener('change', (e) => {
                selectedStrength = e.target.value;
            });
        });

        // Aspect ratio selection
        ratioBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                ratioBtns.forEach(b => {
                    b.classList.remove('bg-pink-600', 'text-white', 'ring-2', 'ring-pink-400');
                    b.classList.add('bg-gray-200', 'text-gray-700');
                });
                btn.classList.remove('bg-gray-200', 'text-gray-700');
                btn.classList.add('bg-pink-600', 'text-white', 'ring-2', 'ring-pink-400');
                selectedRatio = btn.dataset.ratio;
            });
        });

        // Check if ready to generate
        function checkReadyToGenerate() {
            if (yourImageBase64 && refImageBase64) {
                generateBtn.disabled = false;
            }
        }

        // Generate single style match (fail-silent)
        async function generateSingleStyleMatch(index, variation) {
            const card = document.getElementById(`sm-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const styleWeight = Math.round(variation.weight * 100);
                const prompt = `Transform the subject from image 1 to match the visual style, aesthetics, and mood of image 2.

STYLE TRANSFER INSTRUCTIONS:
- Image 1: The subject/person to be styled
- Image 2: The reference style to copy

APPLY THESE STYLE ELEMENTS FROM IMAGE 2:
1. **Color Grading**: Match the exact color tones, saturation, temperature, and color palette
2. **Lighting Style**: Copy the lighting direction, intensity, softness/hardness, and shadows
3. **Contrast & Exposure**: Match the overall brightness, contrast levels, and dynamic range
4. **Mood & Atmosphere**: Replicate the emotional feel and vibe
5. **Post-Processing**: Apply similar filters, grain, vignetting, or effects
6. **Composition Style**: Maintain similar framing and depth of field if applicable

IMPORTANT RULES:
- Keep the subject/person from Image 1 EXACTLY the same (face, body, pose)
- DO NOT change the subject's identity, just the photo style
- Apply ${styleWeight}% of the reference style intensity
- Maintain photo realism and natural look
- The result should look like Image 1's subject photographed in Image 2's style
- DO NOT merge or blend the two images
- Focus on color grading, lighting, and post-processing transfer

Generate a photo of the subject from Image 1 with the visual style of Image 2 applied.`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: yourImageMimeType, data: yourImageBase64 } },
                            { inlineData: { mimeType: refImageMimeType, data: refImageBase64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Image = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Image) throw new Error('No image data');

                const imageUrl = `data:image/png;base64,${base64Image}`;
                const filename = `style-match-${selectedStrength}-${index}.png`;
                generatedImages[index - 1] = { url: imageUrl, filename, variationName: variation.name, weight: variation.weight };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:scale-105';
                    card.innerHTML = `
                        <img src="${imageUrl}" alt="${variation.name}" class="w-full h-auto object-cover">
                        <div class="absolute top-2 left-2 bg-gradient-to-r from-pink-500 to-purple-600 text-white text-xs px-3 py-1 rounded-full font-semibold shadow-lg">${variation.name} (${styleWeight}%)</div>
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-all duration-300 flex items-end justify-center gap-2 p-4">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-cyan-500 to-blue-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                                <i class="fas fa-eye"></i><span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="compare" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-purple-500 to-indigo-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                                <i class="fas fa-columns"></i><span class="hidden sm:inline">Compare</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-green-500 to-green-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                                <i class="fas fa-download"></i><span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`Error generating style match ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Generate button
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!yourImageBase64 || !refImageBase64) {
                    showError('Please upload both your photo and reference photo.');
                    return;
                }

                generatedImages = [];
                generateBtn.disabled = true;
                placeholder.classList.add('hidden');
                loading.classList.remove('hidden');
                resultsHeader.classList.add('hidden');
                resultsGrid.classList.add('hidden');
                resultsGrid.innerHTML = '';
                errorMessage.classList.add('hidden');

                const variations = strengthVariations[selectedStrength];
                const totalVariations = variations.length;

                let attempts = 0;
                const MAX_ATTEMPTS = 3;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedImages = new Array(totalVariations).fill(null);
                    resultsGrid.innerHTML = '';
                    progressBar.style.width = '0%';
                    progressText.textContent = `0/${totalVariations} completed`;
                    loadingText.textContent = 'Matching style...';
                    resultsGrid.classList.remove('hidden');

                    for (let i = 1; i <= totalVariations; i++) {
                        const card = document.createElement('div');
                        card.id = `sm-card-${i}`;
                        card.className = 'relative rounded-xl overflow-hidden bg-gradient-to-br from-pink-50 to-purple-50 border-2 border-pink-200 flex items-center justify-center aspect-square';
                        card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-pink-600"></div>';
                        resultsGrid.appendChild(card);
                    }

                    const promises = variations.map((variation, i) => generateSingleStyleMatch(i + 1, variation));
                    await Promise.allSettled(promises);

                    resultsGrid.querySelectorAll('[id^="sm-card-"]').forEach(card => {
                        if (!card.querySelector('img')) card.remove();
                    });
                    successCount = generatedImages.filter(Boolean).length;
                }

                loading.classList.add('hidden');
                generateBtn.disabled = false;
                checkReadyToGenerate();

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                } else {
                    resultsHeader.classList.remove('hidden');
                }
            });
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.classList.contains('action-btn') ?
                    e.target : e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'compare') {
                    showComparison(imageData);
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Compare button (show first variation)
        if (compareBtn) {
            compareBtn.addEventListener('click', () => {
                if (generatedImages.length > 0) {
                    showComparison(generatedImages[0]);
                }
            });
        }

        // Show comparison modal
        function showComparison(imageData) {
            comparisonOriginal.src = yourPreview.src;
            comparisonStyled.src = imageData.url;
            comparisonModal.classList.remove('hidden');
        }

        // Close comparison
        if (closeComparison) {
            closeComparison.addEventListener('click', () => {
                comparisonModal.classList.add('hidden');
            });
        }

        if (comparisonModal) {
            comparisonModal.addEventListener('click', (e) => {
                if (e.target === comparisonModal) {
                    comparisonModal.classList.add('hidden');
                }
            });
        }

        // Download all button
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async () => {
                if (generatedImages.length === 0) return;

                downloadAllBtn.disabled = true;
                downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < generatedImages.length; i++) {
                    const img = generatedImages[i];
                    if (!img) continue;
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(img.url, img.filename);
                    }
                    if (i < generatedImages.length - 1) {
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }
                }

                downloadAllBtn.disabled = false;
                downloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download All';
            });

            // Add mobile touch support
            if (window.addMobileTouchSupport) {
                window.addMobileTouchSupport(downloadAllBtn);
            }
        }

        // Show error function
        function showError(message) {
            errorText.textContent = message;
            errorMessage.classList.remove('hidden');
            setTimeout(() => {
                errorMessage.classList.add('hidden');
            }, 5000);
        }
    })();

    // --- THUMBNAIL GENERATOR PRO LOGIC ---
    (function() {
        // DOM Elements
        const modeReferenceBtn = document.getElementById('tg-mode-reference');
        const modeTextBtn = document.getElementById('tg-mode-text');
        const referencePanel = document.getElementById('tg-reference-panel');
        const textPanel = document.getElementById('tg-text-panel');

        const refUploadArea = document.getElementById('tg-ref-upload-area');
        const refPhoto = document.getElementById('tg-ref-photo');
        const refPreview = document.getElementById('tg-ref-preview');
        const refPlaceholder = document.getElementById('tg-ref-placeholder');
        const refCustomInstructions = document.getElementById('tg-ref-custom-instructions');

        const titleInput = document.getElementById('tg-title-input');
        const subtitleInput = document.getElementById('tg-subtitle-input');
        const styleSelect = document.getElementById('tg-style-select');
        const expressionSelect = document.getElementById('tg-expression-select');
        const categorySelect = document.getElementById('tg-category-select');
        const generateBtn = document.getElementById('tg-generate-btn');
        const resultsCount = document.getElementById('tg-results-count');

        const placeholder = document.getElementById('tg-placeholder');
        const loading = document.getElementById('tg-loading');
        const loadingText = document.getElementById('tg-loading-text');
        const progressBar = document.getElementById('tg-progress-bar');
        const progressText = document.getElementById('tg-progress-text');

        const resultsHeader = document.getElementById('tg-results-header');
        const resultsGrid = document.getElementById('tg-results-grid');
        const downloadAllBtn = document.getElementById('tg-download-all-btn');

        const errorMessage = document.getElementById('tg-error-message');
        const errorText = document.getElementById('tg-error-text');

        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const tgCountSel = document.getElementById('tg-count-selection-grid');
        if (tgCountSel) {
            tgCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                tgCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        let currentMode = 'reference';
        let refImageBase64 = null;
        let refImageMimeType = null;
        let generatedImages = [];

        // Category to aspect ratio mapping
        const categoryRatios = {
            youtube: '16:9',
            instagram: '1:1',
            story: '9:16',
            facebook: '16:9',
            twitter: '16:9'
        };

        // Expression/Vibe descriptions
        const expressionDescriptions = {
            shocked: 'dengan ekspresi shocked/surprised, mata terbuka lebar, mulut terbuka, gesture kaget',
            excited: 'dengan ekspresi excited/amazed, sangat antusias, energi tinggi, gesture bersemangat',
            serious: 'dengan ekspresi serious/professional, tatapan tajam, pose percaya diri',
            happy: 'dengan ekspresi happy/cheerful, senyum lebar, vibe positif dan ceria',
            dramatic: 'dengan ekspresi dramatic/intense, gesture dramatis, lighting dramatis',
            mysterious: 'dengan ekspresi mysterious/intriguing, pose misterius, vibe penasaran',
            energetic: 'dengan vibe energetic/dynamic, penuh energi, komposisi dinamis',
            calm: 'dengan ekspresi calm/peaceful, damai, relaxed, vibe tenang'
        };

        // Style templates for text-based mode
        const styleTemplates = {
            tech: {
                colors: ['neon blue', 'electric purple', 'cyber pink'],
                mood: 'futuristic, high-tech, gaming aesthetic',
                elements: 'glowing effects, digital elements, tech patterns'
            },
            business: {
                colors: ['professional blue', 'corporate gray', 'gold accents'],
                mood: 'professional, trustworthy, corporate',
                elements: 'clean lines, minimalist design, business icons'
            },
            vlog: {
                colors: ['vibrant colors', 'warm tones', 'pastel highlights'],
                mood: 'fun, energetic, lifestyle vibes',
                elements: 'dynamic composition, expressive elements'
            },
            tutorial: {
                colors: ['clear colors', 'educational blue', 'white space'],
                mood: 'educational, clear, informative',
                elements: 'arrows, steps, clean layout, readable text'
            },
            dramatic: {
                colors: ['bold red', 'dramatic yellow', 'high contrast black'],
                mood: 'attention-grabbing, clickbait, dramatic',
                elements: 'arrows, exclamation marks, intense expressions, bold shadows'
            },
            minimal: {
                colors: ['monochrome', 'elegant gray', 'subtle accents'],
                mood: 'modern, elegant, sophisticated',
                elements: 'clean space, minimalist typography, geometric shapes'
            }
        };

        // Mode switching
        if (modeReferenceBtn && modeTextBtn) {
            modeReferenceBtn.addEventListener('click', () => {
                currentMode = 'reference';
                modeReferenceBtn.classList.remove('bg-gray-200', 'text-gray-700');
                modeReferenceBtn.classList.add('bg-gradient-to-r', 'from-red-500', 'to-orange-500', 'text-white', 'shadow-lg');
                modeTextBtn.classList.remove('bg-gradient-to-r', 'from-red-500', 'to-orange-500', 'text-white', 'shadow-lg');
                modeTextBtn.classList.add('bg-gray-200', 'text-gray-700');
                referencePanel.classList.remove('hidden');
                textPanel.classList.add('hidden');
                checkReadyToGenerate();
            });

            modeTextBtn.addEventListener('click', () => {
                currentMode = 'text';
                modeTextBtn.classList.remove('bg-gray-200', 'text-gray-700');
                modeTextBtn.classList.add('bg-gradient-to-r', 'from-red-500', 'to-orange-500', 'text-white', 'shadow-lg');
                modeReferenceBtn.classList.remove('bg-gradient-to-r', 'from-red-500', 'to-orange-500', 'text-white', 'shadow-lg');
                modeReferenceBtn.classList.add('bg-gray-200', 'text-gray-700');
                referencePanel.classList.add('hidden');
                textPanel.classList.remove('hidden');
                checkReadyToGenerate();
            });
        }

        // Reference upload
        if (refUploadArea) {
            refUploadArea.addEventListener('click', () => refPhoto.click());
        }

        if (refPhoto) {
            refPhoto.addEventListener('change', async (event) => {
                const file = event.target.files[0];
                if (!file) return;

                try {
                    let processedFile = file;
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        if (typeof heic2any !== 'undefined') {
                            const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg' });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                        }
                    }

                    const reader = new FileReader();
                    reader.onload = (e) => {
                        refImageBase64 = e.target.result.split(',')[1];
                        refImageMimeType = processedFile.type;
                        refPreview.src = e.target.result;
                        refPreview.classList.remove('hidden');
                        refPlaceholder.classList.add('hidden');
                        checkReadyToGenerate();
                    };
                    reader.readAsDataURL(processedFile);
                } catch (error) {
                    console.error('Error processing reference image:', error);
                    showError('Failed to process image. Please try another file.');
                }
            });
        }

        // Text inputs
        if (titleInput) {
            titleInput.addEventListener('input', checkReadyToGenerate);
        }

        // Check if ready to generate
        function checkReadyToGenerate() {
            if (currentMode === 'reference') {
                generateBtn.disabled = !refImageBase64;
            } else {
                generateBtn.disabled = !titleInput.value.trim();
            }
        }

        // Generate button
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (currentMode === 'reference' && !refImageBase64) {
                    showError('Please upload a reference image.');
                    return;
                }

                if (currentMode === 'text' && !titleInput.value.trim()) {
                    showError('Please enter a thumbnail title.');
                    return;
                }

                generatedImages = [];
                generateBtn.disabled = true;
                placeholder.classList.add('hidden');
                loading.classList.remove('hidden');
                resultsHeader.classList.add('hidden');
                resultsGrid.classList.add('hidden');
                resultsGrid.innerHTML = '';
                errorMessage.classList.add('hidden');
                progressBar.style.width = '0%';
                progressText.textContent = `0/${selectedCount} completed`;

                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    loadingText.textContent = `Generating thumbnails (attempt ${attempts})...`;
                    generatedImages = new Array(selectedCount).fill(null);

                    resultsGrid.innerHTML = '';
                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `tg-card-${i}`;
                        card.className = 'relative group rounded-xl overflow-hidden shadow-lg flex items-center justify-center bg-gray-100 min-h-[160px]';
                        card.innerHTML = '<div class="loader"></div>';
                        resultsGrid.appendChild(card);
                    }

                    await Promise.allSettled(
                        Array.from({ length: selectedCount }, (_, i) => generateSingleThumbnail(i + 1))
                    );

                    successCount = generatedImages.filter(Boolean).length;
                }

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    loading.classList.add('hidden');
                    placeholder.classList.remove('hidden');
                    generateBtn.disabled = false;
                    checkReadyToGenerate();
                    return;
                }

                document.querySelectorAll('[id^="tg-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                const validTgCount = generatedImages.filter(Boolean).length;
                if (resultsCount) resultsCount.textContent = validTgCount;
                progressBar.style.width = '100%';
                progressText.textContent = `${validTgCount}/${selectedCount} completed`;
                loading.classList.add('hidden');
                resultsHeader.classList.remove('hidden');
                resultsGrid.classList.remove('hidden');
                generateBtn.disabled = false;
                checkReadyToGenerate();
            });
        }

        async function generateSingleThumbnail(index) {
            const card = document.getElementById(`tg-card-${index}`);
            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const category = categorySelect.value;
                const aspectRatio = categoryRatios[category];
                let payload;

                if (currentMode === 'reference') {
                    const customInstructions = refCustomInstructions?.value.trim() || '';
                    const selectedExpression = expressionSelect?.value || '';
                    const expressionText = selectedExpression ? expressionDescriptions[selectedExpression] : '';
                    const prompt = `Buat thumbnail profesional yang eye-catching untuk platform ${category} (rasio ${aspectRatio}) berdasarkan gambar referensi ini.

PERSYARATAN THUMBNAIL:
- Buatlah sangat menarik perhatian dan clickable (CTR tinggi)
- Teks BESAR, BOLD, dan mudah dibaca bahkan di ukuran kecil
- Warna kontras tinggi untuk visibilitas maksimal
- Komposisi dinamis dengan hierarki visual yang kuat
- Kualitas profesional tapi dioptimalkan untuk platform ${category}
- Variasi ${index}: Gunakan skema warna, layout teks, atau penekanan visual yang berbeda

${expressionText ? `EKSPRESI/VIBE:\n- ${expressionText}\n` : ''}

ELEMEN DESAIN YANG HARUS DISERTAKAN:
- Focal point yang jelas dan menarik mata
- Warna vibrant dan saturated (hindari warna yang kusam)
- Kontras kuat antara teks dan background
- Tipografi profesional dengan drop shadow atau outline untuk readability
- Komposisi bersih, tidak berantakan
- Desain mobile-friendly (teks terbaca di layar kecil)
- Gunakan efek visual modern (glow, shadows, gradients) untuk membuat lebih menarik

OPTIMASI PLATFORM ${category.toUpperCase()}:
${category === 'youtube' ? '- Style YouTube: Teks bold dramatis, ekspresi wajah ekspresif, 3-second rule (langsung paham dalam 3 detik)\n- Target CTR tinggi dengan komposisi yang powerful' : ''}
${category === 'instagram' ? '- Style Instagram: Estetik trendy, visually appealing, feed-friendly\n- Gunakan warna dan komposisi yang Instagram-worthy' : ''}
${category === 'story' ? '- Format Story: Orientasi vertikal, thumb-stopping content, cepat dipahami\n- Desain untuk mobile viewing experience' : ''}
${category === 'facebook' ? '- Style Facebook: Engaging, shareable, social media optimized\n- Fokus pada clarity dan immediate impact' : ''}
${category === 'twitter' ? '- Style Twitter Card: Compact, informative, eye-catching\n- Maksimalkan space dengan layout efisien' : ''}

${customInstructions ? `INSTRUKSI CUSTOM DARI USER:\n${customInstructions}\n` : ''}

PENTING: Pastikan hasil akhir adalah thumbnail yang akan membuat orang INGIN mengklik. Gunakan prinsip-prinsip clickability maksimal!`;
                    payload = {
                        contents: [{ parts: [{ text: prompt }, { inlineData: { mimeType: refImageMimeType, data: refImageBase64 } }] }],
                        generationConfig: { responseModalities: ['TEXT', 'IMAGE'], imageConfig: { aspectRatio } }
                    };
                } else {
                    const title = titleInput.value.trim();
                    const subtitle = subtitleInput.value.trim();
                    const style = styleSelect.value;
                    const styleTemplate = styleTemplates[style];
                    const selectedExpression = expressionSelect?.value || '';
                    const expressionText = selectedExpression ? expressionDescriptions[selectedExpression] : '';
                    const prompt = `Buat thumbnail profesional yang eye-catching untuk platform ${category} (rasio ${aspectRatio}) dengan spesifikasi berikut:

JUDUL UTAMA (HARUS MENONJOL): "${title}"
${subtitle ? `SUBJUDUL: "${subtitle}"` : ''}

STYLE: ${style.toUpperCase()}
- Skema Warna: ${styleTemplate.colors.join(', ')}
- Mood: ${styleTemplate.mood}
- Elemen Desain: ${styleTemplate.elements}

${expressionText ? `EKSPRESI/VIBE:\n- ${expressionText}\n` : ''}

PERSYARATAN DESAIN THUMBNAIL:
- Buat teks judul SANGAT BESAR, BOLD, dan MUDAH DIBACA
- Gunakan warna KONTRAS TINGGI (teks harus menonjol dari background)
- Aplikasikan drop shadow atau outline pada teks untuk readability maksimal
- Buat komposisi DINAMIS dan menarik perhatian
- Sertakan elemen visual relevan yang mendukung judul
- Tampilan PROFESIONAL dan CLICKABLE
- Optimasi untuk mobile viewing (teks terbaca di ukuran thumbnail)
- Variasi ${index}: Gunakan layout, kombinasi warna, atau style visual yang berbeda

PENEMPATAN TEKS:
- Posisikan teks di area paling visible
- Gunakan background colors atau shapes yang komplementer di belakang teks
- Pastikan teks tidak bersaing dengan elemen background
- Buat hierarki visual yang jelas (judul > subjudul > elemen lain)

OPTIMASI PLATFORM ${category.toUpperCase()}:
${category === 'youtube' ? '- Style YouTube: Bold dramatis, clickbait-worthy tapi tetap profesional, expressive\n- Target CTR tinggi dengan komposisi powerful' : ''}
${category === 'instagram' ? '- Style Instagram: Estetik trendy, visually appealing, feed-friendly\n- Gunakan warna dan komposisi Instagram-worthy' : ''}
${category === 'story' ? '- Format Story: Vertikal, thumb-stopping, cepat dipahami\n- Desain untuk mobile viewing experience' : ''}
${category === 'facebook' ? '- Style Facebook: Engaging, shareable, social media optimized' : ''}
${category === 'twitter' ? '- Style Twitter Card: Compact, informative, eye-catching' : ''}

PENTING: Buat thumbnail yang akan memaksimalkan klik dan engagement. Teks "${title}" HARUS menjadi elemen paling menonjol!`;
                    payload = {
                        contents: [{ parts: [{ text: prompt }] }],
                        generationConfig: { responseModalities: ['TEXT', 'IMAGE'], imageConfig: { aspectRatio } }
                    };
                }

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');
                const imageUrl = `data:image/png;base64,${imageData}`;
                const filename = `thumbnail-${Date.now()}-${index}.png`;
                generatedImages[index - 1] = { url: imageUrl, filename, variation: index };
                if (card) card.innerHTML = `
                    <img src="${imageUrl}" alt="Thumbnail ${index}" class="w-full h-auto object-cover">
                    <div class="absolute top-2 left-2 bg-gradient-to-r from-red-500 to-orange-500 text-white text-xs px-3 py-1 rounded-full font-semibold shadow-lg">
                        Variation ${index}
                    </div>
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-all duration-300 flex items-end justify-center gap-2 p-4">
                        <button data-action="preview" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-cyan-500 to-blue-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-green-500 to-green-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
            } catch (error) {
                console.error(`Error thumbnail ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.classList.contains('action-btn') ?
                    e.target : e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all button
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async () => {
                if (generatedImages.length === 0) return;

                downloadAllBtn.disabled = true;
                downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < generatedImages.length; i++) {
                    const img = generatedImages[i];
                    if (!img) continue;
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(img.url, img.filename);
                    }
                    if (i < generatedImages.length - 1) {
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }
                }

                downloadAllBtn.disabled = false;
                downloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download All';
            });

            // Add mobile touch support
            if (window.addMobileTouchSupport) {
                window.addMobileTouchSupport(downloadAllBtn);
            }
        }

        // Show error function
        function showError(message) {
            errorText.textContent = message;
            errorMessage.classList.remove('hidden');
            setTimeout(() => {
                errorMessage.classList.add('hidden');
            }, 5000);
        }
    })();

    // --- COVER PHOTO GENERATOR PRO LOGIC ---
    (function() {
        // DOM Elements
        const modePhotoBtn = document.getElementById('cpg-mode-photo');
        const modeFacelessBtn = document.getElementById('cpg-mode-faceless');
        const photoPanel = document.getElementById('cpg-photo-panel');

        const photoUploadArea = document.getElementById('cpg-photo-upload-area');
        const photoInput = document.getElementById('cpg-photo-input');
        const photoPreview = document.getElementById('cpg-photo-preview');
        const photoPlaceholder = document.getElementById('cpg-photo-placeholder');

        const brandNameInput = document.getElementById('cpg-brand-name');
        const taglineInput = document.getElementById('cpg-tagline');
        const textStyleSelect = document.getElementById('cpg-text-style');
        const themePresetSelect = document.getElementById('cpg-theme-preset');
        const customThemeInput = document.getElementById('cpg-custom-theme');
        const platformSelect = document.getElementById('cpg-platform');
        const generateBtn = document.getElementById('cpg-generate-btn');

        const placeholder = document.getElementById('cpg-placeholder');
        const loading = document.getElementById('cpg-loading');
        const loadingText = document.getElementById('cpg-loading-text');
        const progressBar = document.getElementById('cpg-progress-bar');
        const progressText = document.getElementById('cpg-progress-text');

        const resultsHeader = document.getElementById('cpg-results-header');
        const resultsGrid = document.getElementById('cpg-results-grid');
        const downloadAllBtn = document.getElementById('cpg-download-all-btn');

        const errorMessage = document.getElementById('cpg-error-message');
        const errorText = document.getElementById('cpg-error-text');

        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const cpgCountSel = document.getElementById('cpg-count-selection-grid');
        if (cpgCountSel) {
            cpgCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                cpgCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        let currentMode = 'photo';
        let photoBase64 = null;
        let photoMimeType = null;
        let generatedImages = [];

        // Text style definitions
        const textStyleDefinitions = {
            bold: {
                description: 'Bold, impactful, strong typography',
                font: 'Heavy sans-serif fonts, thick strokes',
                colors: 'High contrast, bold colors'
            },
            elegant: {
                description: 'Elegant, luxurious, sophisticated typography',
                font: 'Serif fonts, refined letterforms',
                colors: 'Gold, black, white, metallic accents'
            },
            modern: {
                description: 'Modern, clean, minimalist typography',
                font: 'Sans-serif, geometric fonts, clean lines',
                colors: 'Monochrome, subtle colors, white space'
            },
            creative: {
                description: 'Creative, artistic, unique typography',
                font: 'Custom fonts, decorative elements, hand-drawn',
                colors: 'Vibrant, artistic color combinations'
            },
            tech: {
                description: 'Tech, futuristic, digital typography',
                font: 'Digital fonts, neon effects, cyberpunk style',
                colors: 'Neon blue, electric purple, cyber colors'
            },
            casual: {
                description: 'Casual, friendly, approachable typography',
                font: 'Rounded fonts, handwritten style, playful',
                colors: 'Warm, friendly, pastel colors'
            }
        };

        // Theme preset definitions
        const themePresetDefinitions = {
            business: {
                description: 'Business professional theme',
                colors: 'Professional blue, corporate gray, navy, white',
                mood: 'Formal, trustworthy, corporate, professional',
                elements: 'Clean lines, geometric shapes, business icons, minimal design'
            },
            creative: {
                description: 'Creative and colorful theme',
                colors: 'Vibrant rainbow colors, bold color blocking, artistic palette',
                mood: 'Energetic, creative, expressive, artistic',
                elements: 'Abstract shapes, artistic patterns, creative compositions'
            },
            minimalist: {
                description: 'Minimalist clean theme',
                colors: 'White, black, single accent color, monochrome',
                mood: 'Simple, elegant, modern, uncluttered',
                elements: 'Lots of white space, minimal elements, clean typography'
            },
            dark: {
                description: 'Dark and moody theme',
                colors: 'Black, dark gray, deep colors, moody tones',
                mood: 'Dramatic, mysterious, elegant, cinematic',
                elements: 'Dark backgrounds, dramatic lighting, cinematic effects'
            },
            gradient: {
                description: 'Gradient modern theme',
                colors: 'Trendy gradients, color transitions, vibrant blends',
                mood: 'Modern, trendy, dynamic, eye-catching',
                elements: 'Smooth gradients, modern design, flowing colors'
            },
            nature: {
                description: 'Nature and fresh theme',
                colors: 'Green, earth tones, natural colors, organic palette',
                mood: 'Fresh, organic, natural, peaceful',
                elements: 'Natural textures, organic shapes, botanical elements'
            }
        };

        // Mode switching
        if (modePhotoBtn && modeFacelessBtn) {
            modePhotoBtn.addEventListener('click', () => {
                currentMode = 'photo';
                modePhotoBtn.classList.remove('bg-gray-200', 'text-gray-700');
                modePhotoBtn.classList.add('bg-gradient-to-r', 'from-purple-600', 'to-indigo-600', 'text-white', 'shadow-lg');
                modeFacelessBtn.classList.remove('bg-gradient-to-r', 'from-purple-600', 'to-indigo-600', 'text-white', 'shadow-lg');
                modeFacelessBtn.classList.add('bg-gray-200', 'text-gray-700');
                photoPanel.classList.remove('hidden');
                checkReadyToGenerate();
            });

            modeFacelessBtn.addEventListener('click', () => {
                currentMode = 'faceless';
                modeFacelessBtn.classList.remove('bg-gray-200', 'text-gray-700');
                modeFacelessBtn.classList.add('bg-gradient-to-r', 'from-purple-600', 'to-indigo-600', 'text-white', 'shadow-lg');
                modePhotoBtn.classList.remove('bg-gradient-to-r', 'from-purple-600', 'to-indigo-600', 'text-white', 'shadow-lg');
                modePhotoBtn.classList.add('bg-gray-200', 'text-gray-700');
                photoPanel.classList.add('hidden');
                checkReadyToGenerate();
            });
        }

        // Photo upload
        if (photoUploadArea) {
            photoUploadArea.addEventListener('click', () => photoInput.click());
        }

        if (photoInput) {
            photoInput.addEventListener('change', async (event) => {
                const file = event.target.files[0];
                if (!file) return;

                try {
                    let processedFile = file;
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        if (typeof heic2any !== 'undefined') {
                            const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg' });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                        }
                    }

                    const reader = new FileReader();
                    reader.onload = (e) => {
                        photoBase64 = e.target.result.split(',')[1];
                        photoMimeType = processedFile.type;
                        photoPreview.src = e.target.result;
                        photoPreview.classList.remove('hidden');
                        photoPlaceholder.classList.add('hidden');
                        checkReadyToGenerate();
                    };
                    reader.readAsDataURL(processedFile);
                } catch (error) {
                    console.error('Error processing photo:', error);
                    showError('Failed to process image. Please try another file.');
                }
            });
        }

        // Brand name input
        if (brandNameInput) {
            brandNameInput.addEventListener('input', checkReadyToGenerate);
        }

        // Check if ready to generate
        function checkReadyToGenerate() {
            const brandName = brandNameInput.value.trim();

            if (currentMode === 'photo') {
                generateBtn.disabled = !brandName || !photoBase64;
            } else {
                generateBtn.disabled = !brandName;
            }
        }

        // Generate button
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                const brandName = brandNameInput.value.trim();

                if (!brandName) {
                    showError('Please enter your brand name.');
                    return;
                }

                if (currentMode === 'photo' && !photoBase64) {
                    showError('Please upload your photo.');
                    return;
                }

                generatedImages = [];
                generateBtn.disabled = true;
                placeholder.classList.add('hidden');
                loading.classList.remove('hidden');
                resultsHeader.classList.add('hidden');
                resultsGrid.classList.add('hidden');
                resultsGrid.innerHTML = '';
                errorMessage.classList.add('hidden');
                progressBar.style.width = '0%';
                progressText.textContent = `0/${selectedCount} completed`;

                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    loadingText.textContent = `Generating cover photos (attempt ${attempts})...`;
                    generatedImages = new Array(selectedCount).fill(null);

                    resultsGrid.innerHTML = '';
                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `cpg-card-${i}`;
                        card.className = 'relative group rounded-xl overflow-hidden shadow-lg flex items-center justify-center bg-gray-100 min-h-[160px]';
                        card.innerHTML = '<div class="loader"></div>';
                        resultsGrid.appendChild(card);
                    }

                    await Promise.allSettled(
                        Array.from({ length: selectedCount }, (_, i) => generateSingleCoverPhoto(i + 1))
                    );

                    successCount = generatedImages.filter(Boolean).length;
                }

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    loading.classList.add('hidden');
                    placeholder.classList.remove('hidden');
                    generateBtn.disabled = false;
                    checkReadyToGenerate();
                    return;
                }

                document.querySelectorAll('[id^="cpg-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                progressBar.style.width = '100%';
                progressText.textContent = `${successCount}/${selectedCount} completed`;
                loading.classList.add('hidden');
                resultsHeader.classList.remove('hidden');
                resultsGrid.classList.remove('hidden');
                generateBtn.disabled = false;
                checkReadyToGenerate();
            });
        }

        async function generateSingleCoverPhoto(index) {
            const card = document.getElementById(`cpg-card-${index}`);
            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const brandName = brandNameInput.value.trim();
                const tagline = taglineInput.value.trim();
                const textStyle = textStyleSelect.value;
                const themePreset = themePresetSelect.value;
                const customTheme = customThemeInput.value.trim();
                const platform = platformSelect.value;
                const textStyleDef = textStyleDefinitions[textStyle];
                let themeDescription;
                if (customTheme) {
                    themeDescription = `CUSTOM THEME: ${customTheme}`;
                } else if (themePreset) {
                    const themeDef = themePresetDefinitions[themePreset];
                    themeDescription = `THEME: ${themeDef.description.toUpperCase()}\n- Colors: ${themeDef.colors}\n- Mood: ${themeDef.mood}\n- Elements: ${themeDef.elements}`;
                } else {
                    themeDescription = 'THEME: Modern professional with clean design';
                }
                let payload;
                if (currentMode === 'photo') {
                    const prompt = `Create a professional vertical cover photo for ${platform.toUpperCase()} personal branding (9:16 aspect ratio - portrait/vertical format).

BRAND INFORMATION:
- Brand Name: "${brandName}"
${tagline ? `- Tagline: "${tagline}"` : ''}
- This is the person/model in the photo

TEXT STYLE:
- Style: ${textStyleDef.description}
- Typography: ${textStyleDef.font}
- Color Treatment: ${textStyleDef.colors}

${themeDescription}

DESIGN REQUIREMENTS:
1. Use the person in the photo as the main visual element
2. Display the brand name "${brandName}" prominently on the cover
${tagline ? `3. Include the tagline "${tagline}" in smaller text` : ''}
3. Apply the ${textStyle} text style with appropriate typography
4. Create a professional, eye-catching personal brand cover photo
5. Ensure text is HIGHLY READABLE with proper contrast
6. Optimize for ${platform} vertical cover display (9:16 portrait ratio)
7. Make it look polished and professional for personal branding
8. VERTICAL COMPOSITION - portrait orientation (taller than wide)

COVER PHOTO COMPOSITION:
- VERTICAL/PORTRAIT layout (9:16 - height is greater than width)
- Position the person prominently (professional portrait style for vertical format)
- Add the brand name as overlay text or integrated design element
- Use professional lighting and color grading
- Apply appropriate background treatment (blur, gradient, solid color, or thematic)
- Ensure the person stands out while text remains readable
- Create depth with proper layering
- Optimize for mobile vertical viewing

VARIATION ${index}:
Apply different composition, text placement, or color treatment for this variation.

PLATFORM OPTIMIZATION:
${platform === 'tiktok' ? '- TikTok Cover: Bold, eye-catching, youthful energy, optimized for TikTok profile vertical banner' : ''}
${platform === 'instagram' ? '- Instagram Story/Cover: Aesthetic, polished, Instagram-worthy style, vertical story format' : ''}

Create a professional personal branding VERTICAL cover photo (9:16) that will make a strong first impression.`;
                    payload = {
                        contents: [{ parts: [{ text: prompt }, { inlineData: { mimeType: photoMimeType, data: photoBase64 } }] }],
                        generationConfig: { responseModalities: ['TEXT', 'IMAGE'], imageConfig: { aspectRatio: '9:16' } }
                    };
                } else {
                    const prompt = `Create a professional faceless vertical cover photo for ${platform.toUpperCase()} personal branding (9:16 aspect ratio - portrait/vertical format).

BRAND INFORMATION:
- Brand Name: "${brandName}"
${tagline ? `- Tagline: "${tagline}"` : ''}
- FACELESS BRANDING (no human face, text and design focused)

TEXT STYLE:
- Style: ${textStyleDef.description}
- Typography: ${textStyleDef.font}
- Color Treatment: ${textStyleDef.colors}

${themeDescription}

DESIGN REQUIREMENTS:
1. Create a FACELESS personal brand cover (NO HUMAN FACES)
2. Display the brand name "${brandName}" as the PRIMARY FOCAL POINT
${tagline ? `3. Include the tagline "${tagline}" in complementary text` : ''}
3. Apply the ${textStyle} text style with exceptional typography
4. Create a professional, eye-catching design without using human faces
5. Text must be EXTREMELY READABLE with perfect contrast
6. Optimize for ${platform} vertical cover display (9:16 portrait ratio)
7. Make it professional and memorable for personal branding
8. VERTICAL COMPOSITION - portrait orientation (taller than wide)

FACELESS DESIGN ELEMENTS TO USE:
- Abstract shapes and patterns
- Geometric designs
- Gradients and color blocks
- Icons or symbols relevant to the brand
- Textural backgrounds
- Minimalist compositions
- Artistic illustrations (non-human)
- Professional color schemes

COVER PHOTO COMPOSITION:
- VERTICAL/PORTRAIT layout (9:16 - height is greater than width)
- Make the brand name the hero element
- Use strong visual hierarchy
- Create depth with layering and shadows
- Apply professional color grading
- Ensure clean, uncluttered design
- Balance text and visual elements perfectly
- Optimize for mobile vertical viewing

VARIATION ${index}:
Apply different design style, composition, or color scheme for this variation.

PLATFORM OPTIMIZATION:
${platform === 'tiktok' ? '- TikTok Cover: Bold, modern, attention-grabbing, optimized for TikTok profile vertical banner' : ''}
${platform === 'instagram' ? '- Instagram Story/Cover: Aesthetic, trendy, feed-worthy, vertical story format' : ''}

Create a professional faceless personal branding VERTICAL cover photo (9:16) that communicates professionalism and uniqueness.`;
                    payload = {
                        contents: [{ parts: [{ text: prompt }] }],
                        generationConfig: { responseModalities: ['TEXT', 'IMAGE'], imageConfig: { aspectRatio: '9:16' } }
                    };
                }

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');
                const imageUrl = `data:image/png;base64,${imageData}`;
                const filename = `cover-photo-${Date.now()}-${index}.png`;
                generatedImages[index - 1] = { url: imageUrl, filename, variation: index };
                if (card) card.innerHTML = `
                    <img src="${imageUrl}" alt="Cover Photo ${index}" class="w-full h-auto object-cover">
                    <div class="absolute top-2 left-2 bg-gradient-to-r from-purple-600 to-indigo-600 text-white text-xs px-3 py-1 rounded-full font-semibold shadow-lg">
                        Variation ${index}
                    </div>
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-all duration-300 flex items-end justify-center gap-2 p-4">
                        <button data-action="preview" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-cyan-500 to-blue-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-green-500 to-green-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
            } catch (error) {
                console.error(`Error cover photo ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.classList.contains('action-btn') ?
                    e.target : e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all button
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async () => {
                const validImages = generatedImages.filter(Boolean);
                if (validImages.length === 0) return;

                downloadAllBtn.disabled = true;
                downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < validImages.length; i++) {
                    const img = validImages[i];
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(img.url, img.filename);
                    }
                    if (i < validImages.length - 1) {
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }
                }

                downloadAllBtn.disabled = false;
                downloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download All';
            });

            // Add mobile touch support
            if (window.addMobileTouchSupport) {
                window.addMobileTouchSupport(downloadAllBtn);
            }
        }

        // Show error function
        function showError(message) {
            errorText.textContent = message;
            errorMessage.classList.remove('hidden');
            setTimeout(() => {
                errorMessage.classList.add('hidden');
            }, 5000);
        }
    })();

    // --- PHOTO RESTORATION GENERATOR LOGIC ---
    (function() {
        // DOM Elements
        const uploadArea = document.getElementById('pr-upload-area');
        const photoInput = document.getElementById('pr-photo-input');
        const photoPreview = document.getElementById('pr-photo-preview');
        const photoPlaceholder = document.getElementById('pr-photo-placeholder');

        const modeSelect = document.getElementById('pr-mode-select');
        const intensityBtns = document.querySelectorAll('.pr-intensity-btn');
        const ratioBtns = document.querySelectorAll('.pr-ratio-btn');
        const generateBtn = document.getElementById('pr-generate-btn');

        const placeholder = document.getElementById('pr-placeholder');
        const loading = document.getElementById('pr-loading');
        const loadingText = document.getElementById('pr-loading-text');
        const progressBar = document.getElementById('pr-progress-bar');
        const progressText = document.getElementById('pr-progress-text');

        const resultsHeader = document.getElementById('pr-results-header');
        const resultsGrid = document.getElementById('pr-results-grid');
        const downloadAllBtn = document.getElementById('pr-download-all-btn');
        const comparisonSection = document.getElementById('pr-comparison-section');
        const comparisonBefore = document.getElementById('pr-comparison-before');
        const comparisonAfter = document.getElementById('pr-comparison-after');

        const errorMessage = document.getElementById('pr-error-message');
        const errorText = document.getElementById('pr-error-text');

        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const prCountSel = document.getElementById('pr-count-selection-grid');
        if (prCountSel) {
            prCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                prCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        let photoBase64 = null;
        let photoMimeType = null;
        let selectedIntensity = 'light';
        let selectedRatio = 'original';
        let generatedImages = [];
        let originalPhotoUrl = null;

        // Restoration mode definitions
        const restorationModes = {
            'old-photo': {
                name: 'Old Photo Restoration',
                description: 'Restore faded colors, fix yellowing, remove aging artifacts',
                prompt: 'restore this old/vintage photograph to its original quality'
            },
            'damaged': {
                name: 'Damaged Photo Repair',
                description: 'Fix scratches, tears, stains, and physical damage',
                prompt: 'repair all physical damage, scratches, tears, and imperfections in this photograph'
            },
            'colorize': {
                name: 'Colorize Black & White',
                description: 'Add realistic natural colors to B&W photos',
                prompt: 'colorize this black and white photograph with realistic, natural colors'
            },
            'enhance': {
                name: 'Quality Enhancement',
                description: 'Enhance resolution, clarity, and overall quality',
                prompt: 'enhance the overall quality, resolution, and clarity of this photograph'
            },
            'deblur': {
                name: 'Deblur & Sharpen',
                description: 'Remove blur and increase sharpness',
                prompt: 'remove blur and significantly sharpen this photograph'
            },
            'denoise': {
                name: 'Denoise',
                description: 'Remove grain and digital noise',
                prompt: 'remove all grain, noise, and artifacts from this photograph'
            }
        };

        // Intensity levels
        const intensityLevels = {
            light: { value: 30, desc: 'Subtle, natural-looking restoration' },
            medium: { value: 60, desc: 'Balanced restoration with noticeable improvement' },
            strong: { value: 90, desc: 'Aggressive restoration for heavily damaged photos' }
        };

        // Photo upload
        if (uploadArea) {
            uploadArea.addEventListener('click', () => photoInput.click());
        }

        if (photoInput) {
            photoInput.addEventListener('change', async (event) => {
                const file = event.target.files[0];
                if (!file) return;

                try {
                    let processedFile = file;
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        if (typeof heic2any !== 'undefined') {
                            const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg' });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                        }
                    }

                    const reader = new FileReader();
                    reader.onload = (e) => {
                        photoBase64 = e.target.result.split(',')[1];
                        photoMimeType = processedFile.type;
                        originalPhotoUrl = e.target.result;
                        photoPreview.src = e.target.result;
                        photoPreview.classList.remove('hidden');
                        photoPlaceholder.classList.add('hidden');
                        checkReadyToGenerate();
                    };
                    reader.readAsDataURL(processedFile);
                } catch (error) {
                    console.error('Error processing photo:', error);
                    showError('Failed to process image. Please try another file.');
                }
            });
        }

        // Intensity buttons
        intensityBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                intensityBtns.forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                selectedIntensity = btn.dataset.intensity;
            });
        });

        // Ratio buttons
        ratioBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                ratioBtns.forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                selectedRatio = btn.dataset.ratio;
            });
        });

        // Check if ready to generate
        function checkReadyToGenerate() {
            generateBtn.disabled = !photoBase64;
        }

        // Generate button
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!photoBase64) {
                    showError('Please upload a photo first.');
                    return;
                }

                generatedImages = [];
                generateBtn.disabled = true;
                placeholder.classList.add('hidden');
                loading.classList.remove('hidden');
                resultsHeader.classList.add('hidden');
                resultsGrid.classList.add('hidden');
                comparisonSection.classList.add('hidden');
                resultsGrid.innerHTML = '';
                errorMessage.classList.add('hidden');
                progressBar.style.width = '0%';
                progressText.textContent = `0/${selectedCount} completed`;

                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    loadingText.textContent = `Restoring photo (attempt ${attempts})...`;
                    generatedImages = new Array(selectedCount).fill(null);

                    resultsGrid.innerHTML = '';
                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `pr-card-${i}`;
                        card.className = 'relative group rounded-xl overflow-hidden shadow-lg flex items-center justify-center bg-gray-100 min-h-[160px]';
                        card.innerHTML = '<div class="loader"></div>';
                        resultsGrid.appendChild(card);
                    }

                    await Promise.allSettled(
                        Array.from({ length: selectedCount }, (_, i) => generateSingleRestoration(i + 1))
                    );

                    successCount = generatedImages.filter(Boolean).length;
                }

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    loading.classList.add('hidden');
                    placeholder.classList.remove('hidden');
                    generateBtn.disabled = false;
                    checkReadyToGenerate();
                    return;
                }

                document.querySelectorAll('[id^="pr-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                const validPrCount = generatedImages.filter(Boolean).length;
                progressBar.style.width = '100%';
                progressText.textContent = `${validPrCount}/${selectedCount} completed`;
                loading.classList.add('hidden');
                resultsHeader.classList.remove('hidden');
                resultsGrid.classList.remove('hidden');

                if (generatedImages.length > 0) {
                    comparisonBefore.src = originalPhotoUrl;
                    comparisonAfter.src = generatedImages[0].url;
                    comparisonSection.classList.remove('hidden');
                }

                generateBtn.disabled = false;
                checkReadyToGenerate();
            });
        }

        async function generateSingleRestoration(index) {
            const card = document.getElementById(`pr-card-${index}`);
            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const mode = modeSelect.value;
                const intensity = intensityLevels[selectedIntensity];
                let aspectRatioConfig = {};
                if (selectedRatio !== 'original') {
                    aspectRatioConfig = { aspectRatio: selectedRatio };
                }
                let basePrompt = '';

            if (mode === 'old-photo') {
                basePrompt = `CRITICAL RESTORATION TASK - OLD PHOTO REVIVAL:

You MUST transform this aged photograph into a restored, vibrant version. This is NOT a simple enhancement - you must ACTIVELY FIX aging damage.

MANDATORY CHANGES TO APPLY:
✓ RESTORE all faded colors back to their original vibrant state
✓ REMOVE ALL yellow/sepia tones caused by aging - return to natural colors
✓ ELIMINATE age-related discoloration, spots, and stains
✓ FIX faded areas - restore proper contrast and exposure
✓ REMOVE grain, noise, and paper texture artifacts from aging
✓ REPAIR any cracks, creases, or physical damage visible
✓ ENHANCE sharpness and clarity significantly
✓ IMPROVE overall image quality to modern standards

The result MUST look noticeably better, clearer, and more vibrant than the original.`;

            } else if (mode === 'damaged') {
                basePrompt = `CRITICAL RESTORATION TASK - DAMAGED PHOTO REPAIR:

You MUST repair ALL visible damage in this photograph. This requires ACTIVE reconstruction.

MANDATORY REPAIRS TO PERFORM:
✓ FILL IN all missing, torn, or damaged areas completely
✓ REMOVE ALL scratches, tears, and physical damage marks
✓ ELIMINATE stains, water damage, and discoloration
✓ RECONSTRUCT damaged portions using surrounding context
✓ FIX all creases, folds, and crumple marks
✓ REPAIR edge damage and corners
✓ BLEND all repairs seamlessly with the rest of the image
✓ RESTORE image to pristine condition

The result MUST show complete repair of all damage - no scratches or tears should remain.`;

            } else if (mode === 'colorize') {
                basePrompt = `CRITICAL RESTORATION TASK - BLACK & WHITE COLORIZATION:

You MUST add realistic, natural colors to this black and white photograph.

MANDATORY COLORIZATION REQUIREMENTS:
✓ ADD realistic skin tones (considering ethnicity and lighting)
✓ COLORIZE clothing with period-appropriate colors
✓ ADD natural colors to backgrounds, landscapes, objects
✓ ENSURE proper color harmony and balance
✓ MAINTAIN natural shadows and highlights in color
✓ CREATE smooth color transitions and gradients
✓ APPLY historically accurate colors for the era
✓ AVOID oversaturation - keep colors natural and believable

The result MUST be a fully colorized image that looks like it was originally shot in color.`;

            } else if (mode === 'enhance') {
                basePrompt = `CRITICAL RESTORATION TASK - QUALITY ENHANCEMENT:

You MUST dramatically improve the quality and resolution of this photograph.

MANDATORY ENHANCEMENTS TO APPLY:
✓ UPSCALE resolution significantly - add fine details
✓ SHARPEN all edges and improve overall clarity dramatically
✓ ENHANCE texture details in faces, clothing, objects
✓ IMPROVE dynamic range - deepen shadows, brighten highlights
✓ ADD micro-details that may be lost in original
✓ OPTIMIZE color balance and saturation
✓ INCREASE overall sharpness and definition
✓ MAKE the image look like it was shot with modern high-res camera

The result MUST look significantly sharper, clearer, and more detailed than the original.`;

            } else if (mode === 'deblur') {
                basePrompt = `CRITICAL RESTORATION TASK - DEBLUR & SHARPEN:

You MUST remove ALL blur and dramatically sharpen this photograph.

MANDATORY DEBLUR OPERATIONS:
✓ REMOVE motion blur completely - restore crisp edges
✓ ELIMINATE camera shake effects
✓ SHARPEN all blurred areas significantly
✓ RECOVER fine details lost to blur
✓ ENHANCE edge definition dramatically
✓ IMPROVE focus across entire image
✓ ADD clarity and crispness to all elements
✓ MAKE everything tack-sharp and in focus

The result MUST be dramatically sharper with NO blur remaining anywhere.`;

            } else if (mode === 'denoise') {
                basePrompt = `CRITICAL RESTORATION TASK - NOISE REMOVAL:

You MUST remove ALL noise, grain, and artifacts from this photograph.

MANDATORY DENOISE OPERATIONS:
✓ ELIMINATE ALL digital noise and ISO grain
✓ REMOVE grainy texture completely
✓ SMOOTH out noisy areas while preserving details
✓ CLEAN UP compression artifacts
✓ REMOVE color noise and banding
✓ PRESERVE important details and edges during denoising
✓ CREATE smooth, clean image surfaces
✓ MAINTAIN natural texture without grain

The result MUST be completely noise-free and smooth.`;
            }

                const prompt = `${basePrompt}

INTENSITY SETTING: ${selectedIntensity.toUpperCase()} (${intensity.value}% strength)
${selectedIntensity === 'strong' ? '⚡ Apply MAXIMUM restoration - aggressive improvements' : ''}
${selectedIntensity === 'medium' ? '⚡ Apply STRONG restoration - significant improvements' : ''}
${selectedIntensity === 'light' ? '⚡ Apply MODERATE restoration - noticeable improvements' : ''}

CRITICAL RULES:
1. The output MUST look DIFFERENT from input - this is NOT a copy task
2. You MUST make VISIBLE improvements - the changes should be OBVIOUS
3. This is RESTORATION, not preservation - CHANGE the image for the better
4. Result ${index}: Use a unique restoration approach for this variation
5. NEVER output the original image unchanged - ALWAYS apply improvements

Generate a professionally restored photograph that clearly demonstrates significant improvement over the original.`;

                const payload = {
                    contents: [{ parts: [{ text: prompt }, { inlineData: { mimeType: photoMimeType, data: photoBase64 } }] }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        ...(selectedRatio !== 'original' ? { imageConfig: aspectRatioConfig } : {})
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');
                const imageUrl = `data:image/png;base64,${imageData}`;
                const filename = `restored-photo-${Date.now()}-${index}.png`;
                generatedImages[index - 1] = { url: imageUrl, filename, result: index };
                if (card) card.innerHTML = `
                    <img src="${imageUrl}" alt="Restored Photo ${index}" class="w-full h-auto object-cover">
                    <div class="absolute top-2 left-2 bg-gradient-to-r from-amber-600 to-orange-600 text-white text-xs px-3 py-1 rounded-full font-semibold shadow-lg">
                        Result ${index}
                    </div>
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-all duration-300 flex items-end justify-center gap-2 p-4">
                        <button data-action="preview" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-cyan-500 to-blue-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="action-btn flex items-center gap-2 bg-gradient-to-r from-green-500 to-green-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition-all">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
            } catch (error) {
                console.error(`Error restoration ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.classList.contains('action-btn') ?
                    e.target : e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all button
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async () => {
                if (generatedImages.length === 0) return;

                downloadAllBtn.disabled = true;
                downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < generatedImages.length; i++) {
                    const img = generatedImages[i];
                    if (!img) continue;
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(img.url, img.filename);
                    }
                    if (i < generatedImages.length - 1) {
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }
                }

                downloadAllBtn.disabled = false;
                downloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download All';
            });
        }

        // Show error function
        function showError(message) {
            errorText.textContent = message;
            errorMessage.classList.remove('hidden');
            setTimeout(() => {
                errorMessage.classList.add('hidden');
            }, 5000);
        }
    })();

    // ==================== WEDDING & PRE-WEDDING - PROFESSIONAL VERSION ====================
    (function() {
        const apiKey = "";

        // DOM Elements - Upload & Preview
        const maleUpload = document.getElementById('wedding-male-upload');
        const maleUploadBox = document.getElementById('wedding-male-upload-box');
        const malePlaceholder = document.getElementById('wedding-male-placeholder');
        const malePreviewContainer = document.getElementById('wedding-male-preview-container');
        const malePreview = document.getElementById('wedding-male-preview');
        const removeMalePreview = document.getElementById('wedding-remove-male-preview');

        const femaleUpload = document.getElementById('wedding-female-upload');
        const femaleUploadBox = document.getElementById('wedding-female-upload-box');
        const femalePlaceholder = document.getElementById('wedding-female-placeholder');
        const femalePreviewContainer = document.getElementById('wedding-female-preview-container');
        const femalePreview = document.getElementById('wedding-female-preview');
        const removeFemalePreview = document.getElementById('wedding-remove-female-preview');

        // DOM Elements - Options
        const themeInput = document.getElementById('wedding-theme-input');
        const typeOptions = document.getElementById('wedding-type-options');
        const ratioOptions = document.getElementById('wedding-ratio-options');

        // DOM Elements - UI States
        const generateBtn = document.getElementById('wedding-generate-btn');
        const loadingDiv = document.getElementById('wedding-loading');
        const loadingText = document.getElementById('wedding-loading-text');
        const progressContainer = document.getElementById('wedding-progress-container');
        const progressBar = document.getElementById('wedding-progress-bar');
        const progressText = document.getElementById('wedding-progress-text');
        const progressPercentage = document.getElementById('wedding-progress-percentage');
        const emptyState = document.getElementById('wedding-empty-state');
        const resultsHeader = document.getElementById('wedding-results-header');
        const resultsCount = document.getElementById('wedding-results-count');
        const resultsGrid = document.getElementById('wedding-results-grid');
        const downloadAllBtn = document.getElementById('wedding-download-all-btn');
        const heartsContainer = document.getElementById('wedding-hearts-container');

        if (!maleUpload) return; // Exit if elements not present

        // State
        let maleImageData = null;
        let femaleImageData = null;
        let selectedType = 'wedding';
        let selectedRatio = '9:16';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedImages = [];

        // Count selection (1-10), default 4
        const weddingCountSel = document.getElementById('wedding-count-selection-grid');
        if (weddingCountSel) {
            weddingCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                weddingCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        const defaultThemes = [
            "in a lush enchanted forest", "on a beach at sunset with dramatic clouds", "in a fairytale castle ballroom",
            "in a rustic barn decorated with fairy lights", "on a modern city rooftop overlooking the skyline", "with a vintage 1920s Great Gatsby style",
            "in a vibrant flower garden during spring", "at a glamorous Hollywood red carpet event", "having a picnic in the lavender fields of Provence",
            "under the northern lights (aurora borealis)", "in a traditional Japanese garden with cherry blossoms", "on a gondola in Venice, Italy",
            "in a bohemian desert landscape with pampas grass", "in a grand library surrounded by old books", "at a snowy mountain peak during winter",
            "in a magical underwater scene with coral reefs", "in a sci-fi futuristic setting", "with a whimsical hot air balloon festival in the background",
            "in an elegant art deco hotel lobby", "celebrating during a vibrant Holi festival in India"
        ];

        // Upload Male Photo with HEIC support
        if (maleUpload) {
            maleUpload.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    maleImageData = reader.result;

                    // Show preview on mobile
                    if (malePreview && malePreviewContainer) {
                        malePreview.src = reader.result;
                        malePreviewContainer.classList.remove('hidden');
                    }

                    // Update placeholder
                    if (malePlaceholder) {
                        malePlaceholder.innerHTML = `
                            <i class="fas fa-check-circle text-rose-500" style="font-size: 2.5rem;"></i>
                            <p class="mt-2 text-sm font-semibold text-rose-600">Foto pria berhasil diupload!</p>
                        `;
                    }

                    // Enable generate button if both uploaded
                    if (maleImageData && femaleImageData && generateBtn) {
                        generateBtn.disabled = false;
                    }
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove male preview
        if (removeMalePreview) {
            removeMalePreview.addEventListener('click', () => {
                maleImageData = null;
                if (maleUpload) maleUpload.value = '';
                if (malePreviewContainer) malePreviewContainer.classList.add('hidden');
                if (malePreview) malePreview.src = '';
                if (malePlaceholder) {
                    malePlaceholder.innerHTML = `
                        <i class="fas fa-user-tie transition-transform duration-300" style="font-size: 2.5rem; color: #f43f5e;"></i>
                        <p class="mt-2 text-sm font-semibold">🤵 Klik untuk upload foto pria</p>
                        <p class="mt-1 text-xs text-gray-400">Format: JPG, PNG, HEIC</p>
                    `;
                }
                if (generateBtn) generateBtn.disabled = true;
            });
        }

        // Upload Female Photo with HEIC support
        if (femaleUpload) {
            femaleUpload.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    femaleImageData = reader.result;

                    // Show preview on mobile
                    if (femalePreview && femalePreviewContainer) {
                        femalePreview.src = reader.result;
                        femalePreviewContainer.classList.remove('hidden');
                    }

                    // Update placeholder
                    if (femalePlaceholder) {
                        femalePlaceholder.innerHTML = `
                            <i class="fas fa-check-circle text-pink-500" style="font-size: 2.5rem;"></i>
                            <p class="mt-2 text-sm font-semibold text-pink-600">Foto wanita berhasil diupload!</p>
                        `;
                    }

                    // Enable generate button if both uploaded
                    if (maleImageData && femaleImageData && generateBtn) {
                        generateBtn.disabled = false;
                    }
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove female preview
        if (removeFemalePreview) {
            removeFemalePreview.addEventListener('click', () => {
                femaleImageData = null;
                if (femaleUpload) femaleUpload.value = '';
                if (femalePreviewContainer) femalePreviewContainer.classList.add('hidden');
                if (femalePreview) femalePreview.src = '';
                if (femalePlaceholder) {
                    femalePlaceholder.innerHTML = `
                        <i class="fas fa-user transition-transform duration-300" style="font-size: 2.5rem; color: #ec4899;"></i>
                        <p class="mt-2 text-sm font-semibold">👰 Klik untuk upload foto wanita</p>
                        <p class="mt-1 text-xs text-gray-400">Format: JPG, PNG, HEIC</p>
                    `;
                }
                if (generateBtn) generateBtn.disabled = true;
            });
        }

        // Type selection (Wedding / Pre-wedding)
        if (typeOptions) {
            typeOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                typeOptions.querySelectorAll('.option-btn-wedding').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedType = button.dataset.value;
            });
        }

        // Ratio selection
        if (ratioOptions) {
            ratioOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                ratioOptions.querySelectorAll('.option-btn-wedding').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedRatio = button.dataset.value;
            });
        }

        // Heart animation
        if (heartsContainer) {
            function createHeart() {
                const heart = document.createElement('div');
                heart.classList.add('heart');
                heart.style.left = Math.random() * 100 + 'vw';
                heart.style.animationName = 'wedding-floatUp';
                heart.style.animationDuration = Math.random() * 5 + 5 + 's';
                const heartEmoji = '\u2764\uFE0F';
                heart.innerHTML = heartEmoji;
                heartsContainer.appendChild(heart);

                setTimeout(() => {
                    heart.remove();
                }, 10000);
            }
            setInterval(createHeart, 500);
        }

        // Generate Wedding Photos
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!maleImageData || !femaleImageData) {
                    alert('Silakan upload foto pria dan wanita terlebih dahulu');
                    return;
                }

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                if (emptyState) emptyState.classList.add('hidden');
                if (loadingDiv) loadingDiv.classList.remove('hidden');
                if (resultsHeader) resultsHeader.classList.add('hidden');

                const userTheme = themeInput.value.trim();
                const photoShootType = selectedType === 'wedding' ? 'wedding photograph' : 'pre-wedding photograph';
                const attireDescription = selectedType === 'wedding'
                    ? 'beautiful wedding attire (groom in a sharp tuxedo, bride in a stunning wedding gown)'
                    : 'stylish and romantic pre-wedding outfits that match the theme';

                const cameraSettings = [
                    'Canon 5D Mark IV with 85mm f/1.4 lens, shallow depth of field, bokeh background',
                    'Sony A7R IV with 50mm f/1.2 lens, creamy bokeh, natural skin tones',
                    'Nikon Z9 with 70-200mm f/2.8 lens, compressed perspective, beautiful background separation',
                    'Fujifilm GFX 100S medium format, exceptional detail, film-like color rendering',
                    'Canon EOS R5 with 24-70mm f/2.8 lens, versatile framing, professional color science'
                ];

                const lightingStyles = [
                    'natural golden hour light with warm glow, soft shadows, sun-kissed skin',
                    'soft diffused window light, Rembrandt lighting, gentle shadows on face',
                    'backlit with sun flare, rim lighting highlighting hair and silhouette',
                    'overcast daylight for even skin tones, no harsh shadows, soft natural light',
                    'magic hour lighting with deep golden tones, romantic amber glow',
                    'dappled sunlight through trees, natural light and shadow interplay',
                    'soft morning light with cool blue undertones, fresh and ethereal atmosphere'
                ];

                const realismTechniques = [
                    'subtle film grain texture, natural skin pores visible, authentic human expressions',
                    'slight motion blur in background, natural depth variation, real-world imperfections',
                    'genuine candid moments, authentic emotional connection, unposed natural gestures',
                    'real fabric textures and wrinkles, natural wind movement in clothing and hair',
                    'environmental reflections and natural ambient occlusion, realistic shadow casting',
                    'authentic color grading like Kodak Portra film, natural skin undertones',
                    'real-world lighting conditions, natural eye catchlights, genuine smile lines'
                ];

                let attempts = 0;
                const MAX_ATTEMPTS = 3;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedImages = [];
                    if (resultsGrid) resultsGrid.innerHTML = '';

                    // Pre-create placeholder cards
                    for (let i = 1; i <= selectedCount; i++) {
                        const placeholder = document.createElement('div');
                        placeholder.id = `wedding-card-${i}`;
                        placeholder.className = 'group relative rounded-xl overflow-hidden shadow-2xl border-2 border-transparent bg-gray-100 flex items-center justify-center aspect-[9/16]';
                        placeholder.innerHTML = '<div class="loader"></div>';
                        if (resultsGrid) resultsGrid.appendChild(placeholder);
                    }

                    const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                        generateSingleWedding(i + 1, userTheme, photoShootType, attireDescription, cameraSettings, lightingStyles, realismTechniques)
                    );
                    await Promise.allSettled(generationPromises);

                    // Remove empty cards
                    if (resultsGrid) {
                        resultsGrid.querySelectorAll('[id^="wedding-card-"]').forEach(card => {
                            if (!card.querySelector('img')) card.remove();
                        });
                    }
                    successCount = generatedImages.filter(Boolean).length;
                }

                if (loadingDiv) loadingDiv.classList.add('hidden');

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    if (emptyState) emptyState.classList.remove('hidden');
                } else {
                    if (resultsHeader) resultsHeader.classList.remove('hidden');
                    if (resultsCount) resultsCount.textContent = successCount;
                    if (emptyState) emptyState.classList.add('hidden');
                }

                generateBtn.disabled = false;
                generateBtn.innerHTML = originalHTML;
            });
        }

        // Generate single wedding photo
        async function generateSingleWedding(index, userTheme, photoShootType, attireDescription, cameraSettings, lightingStyles, realismTechniques) {
            const card = document.getElementById(`wedding-card-${index}`);
            const randomTheme = defaultThemes[Math.floor(Math.random() * defaultThemes.length)];
            const chosenTheme = userTheme || randomTheme;
            const camera = cameraSettings[Math.floor(Math.random() * cameraSettings.length)];
            const lighting = lightingStyles[Math.floor(Math.random() * lightingStyles.length)];
            const realism = realismTechniques[Math.floor(Math.random() * realismTechniques.length)];

            const prompt = `**PROFESSIONAL WEDDING PHOTOGRAPHY**

**SUBJECTS & IDENTITY:**
- The man from the first image and the woman from the second image EXACTLY as they appear
- Maintain their EXACT facial features, bone structure, eye shape, nose, skin tone, and unique characteristics
- Preserve natural body proportions and authentic human anatomy
- Capture genuine emotional connection and authentic romantic moment

**SCENE & THEME:**
${chosenTheme}
Attire: ${attireDescription}

**CAMERA & TECHNICAL:**
Shot with ${camera}
${lighting}
Professional color grading with natural skin tones
ISO 100-400 for clean image, proper exposure

**NATURAL REALISM (CRITICAL - MUST NOT LOOK AI-GENERATED):**
${realism}
Real wedding photography aesthetic, NOT AI-generated look
Natural posing like real couples, authentic body language
Real environmental details and genuine atmosphere
Subtle natural imperfections make it look authentic
Professional retouching level - enhances but keeps natural

**PHOTOGRAPHY STYLE:**
Style of elite wedding photographers (Jose Villa, Ryan Brenizer, Jasmine Star)
Real photojournalistic wedding moment
Natural documentary style capturing genuine emotion
Timeless romantic aesthetic with authentic feel
Professional but not overly polished or artificial

**TECHNICAL REQUIREMENTS:**
- Sharp focus on faces with beautiful bokeh
- Natural color palette, avoid oversaturation
- Realistic lighting physics and shadow behavior
- Genuine fabric draping and material textures
- Real-world perspective and spatial relationships
- Authentic environmental integration

This is NOT an AI render - this is a REAL professional wedding photograph with natural human subjects and authentic moments.`;

            try {
                const maleBase64 = maleImageData.split(',')[1];
                const femaleBase64 = femaleImageData.split(',')[1];
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: 'image/jpeg', data: maleBase64 } },
                            { inlineData: { mimeType: 'image/jpeg', data: femaleBase64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                    safetySettings: [
                        { category: 'HARM_CATEGORY_HARASSMENT', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_HATE_SPEECH', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_SEXUALLY_EXPLICIT', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_DANGEROUS_CONTENT', threshold: 'BLOCK_NONE' }
                    ]
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `${selectedType}-photo-${Date.now()}-${index}.jpg`;
                generatedImages[index - 1] = { url: imageUrl, filename };

                if (card) {
                    card.className = 'group relative rounded-xl overflow-hidden shadow-2xl border-2 border-transparent hover:border-rose-500 transition-all duration-300 transform hover:scale-105';
                    card.innerHTML = `
                        <img src="${imageUrl}" alt="Wedding photo ${index}" class="w-full h-full object-cover aspect-[9/16]"/>
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-4 gap-2">
                            <div class="flex gap-2">
                                <button data-action="wedding-preview" data-index="${index - 1}" class="action-btn flex-1 bg-blue-500 hover:bg-blue-600 text-white px-3 py-2 rounded-full text-sm font-bold transition flex items-center justify-center gap-1 shadow-lg">
                                    <i class="fas fa-eye"></i>
                                    <span class="hidden sm:inline">Preview</span>
                                </button>
                                <button data-action="wedding-download" data-index="${index - 1}" class="action-btn flex-1 bg-gradient-to-r from-rose-500 to-pink-500 hover:from-rose-600 hover:to-pink-600 text-white px-3 py-2 rounded-full text-sm font-bold transition flex items-center justify-center gap-1 shadow-lg">
                                    <i class="fas fa-download"></i>
                                    <span class="hidden sm:inline">Download</span>
                                </button>
                            </div>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`Error generating wedding photo ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'wedding-preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'wedding-download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all functionality
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }
    })();

    // --- VOICE OVER STUDIO PRO TAB LOGIC ---
    (function() {
        const voiceOverTabDesktop = document.querySelector('.main-tab-btn[data-tab="voice-over"]');
        const voiceOverTabMobile = document.querySelector('.mobile-tab-btn[data-tab="voice-over"]');

        console.log('Voice Over tabs found - Desktop:', !!voiceOverTabDesktop, 'Mobile:', !!voiceOverTabMobile);

        // Initialize when either desktop or mobile tab is clicked
        const initOnClick = () => {
            console.log('Voice Over tab clicked, initialized:', window.voiceOverInitialized);
            if (!window.voiceOverInitialized) {
                console.log('Initializing Voice Over Generator...');
                initVoiceOverGenerator();
                window.voiceOverInitialized = true;
            }
        };

        if (voiceOverTabDesktop) {
            voiceOverTabDesktop.addEventListener('click', initOnClick);
        }
        if (voiceOverTabMobile) {
            voiceOverTabMobile.addEventListener('click', initOnClick);
        }

        function initVoiceOverGenerator() {
            // DOM Elements
            const generateBtn = document.getElementById('vo-generate-btn');
            const clearTextBtn = document.getElementById('vo-clear-text-btn');
            const copyTextBtn = document.getElementById('vo-copy-text-btn');
            const textInput = document.getElementById('vo-text-input');
            const styleInput = document.getElementById('vo-style-input');
            const voiceList = document.getElementById('vo-voice-list');
            const resultContainer = document.getElementById('vo-result-container');
            const loaderContainer = document.getElementById('vo-loader-container');
            const audioContainer = document.getElementById('vo-audio-container');
            const audioPlayer = document.getElementById('vo-audio-player');
            const audioDuration = document.getElementById('vo-audio-duration');
            const downloadBtn = document.getElementById('vo-download-btn');
            const addToHistoryBtn = document.getElementById('vo-add-to-history-btn');
            const errorMessage = document.getElementById('vo-error-message');
            const charCounter = document.getElementById('vo-char-counter');
            const wordCounter = document.getElementById('vo-word-counter');
            const openGuidanceBtn = document.getElementById('vo-open-guidance-btn');
            const closeGuidanceBtn = document.getElementById('vo-close-guidance-btn');
            const understandBtn = document.getElementById('vo-understand-btn');
            const guidanceModal = document.getElementById('vo-guidance-modal');

            // Control Elements
            const pitchSlider = document.getElementById('vo-pitch-slider');
            const pitchValue = document.getElementById('vo-pitch-value');
            const resetPitchBtn = document.getElementById('vo-reset-pitch-btn');
            const volumeSlider = document.getElementById('vo-volume-slider');
            const volumeValue = document.getElementById('vo-volume-value');
            const resetVolumeBtn = document.getElementById('vo-reset-volume-btn');
            const speedSlider = document.getElementById('vo-speed-slider');
            const speedValue = document.getElementById('vo-speed-value');
            const resetSpeedBtn = document.getElementById('vo-reset-speed-btn');

            // Template & History
            const templateBtns = document.querySelectorAll('.vo-template-btn');
            const historyContainer = document.getElementById('vo-history-container');
            const historyList = document.getElementById('vo-history-list');
            const clearHistoryBtn = document.getElementById('vo-clear-history-btn');

            const CHAR_LIMIT = 5000;
            const previewText = "Halo, ini adalah contoh suara saya untuk pratinjau.";

            const textTemplates = {
                intro: "Halo semuanya! Selamat datang di channel kami. Hari ini kita akan membahas topik yang sangat menarik.",
                narasi: "Di sebuah tempat yang jauh, terdapat sebuah cerita yang belum pernah diceritakan. Cerita tentang keberanian dan pengorbanan.",
                iklan: "Dapatkan penawaran terbaik hari ini! Produk berkualitas tinggi dengan harga yang terjangkau. Jangan lewatkan kesempatan emas ini!",
                podcast: "Terima kasih sudah bergabung di episode kali ini. Mari kita mulai diskusi mendalam tentang topik yang sedang trending."
            };

            const voices = [
                { name: "Puck", gender: "Pria", id: "Puck" },
                { name: "Kore", gender: "Wanita", id: "Kore" },
                { name: "Zephyr", gender: "Wanita", id: "Zephyr" },
                { name: "Fenrir", gender: "Pria", id: "Fenrir" },
                { name: "Achird", gender: "Pria", id: "Achird"},
                { name: "Charon", gender: "Pria", id: "Charon" },
                { name: "Sadachbia", gender: "Pria", id: "Sadachbia" },
                { name: "Sulafat", gender: "Wanita", id: "Sulafat" },
                { name: "Aoede", gender: "Wanita", id: "Aoede" },
                { name: "Umbriel", gender: "Pria", id: "Umbriel" },
                { name: "Gacrux", gender: "Wanita", id: "Gacrux"},
                { name: "Leda", gender: "Wanita", id: "Leda" },
                { name: "Orus", gender: "Pria", id: "Orus" },
                { name: "Enceladus", gender: "Pria", id: "Enceladus" },
                { name: "Algenib", gender: "Pria", id: "Algenib" },
                { name: "Vindemiatrix", gender: "Wanita", id: "Vindemiatrix" },
                { name: "Sadaltager", gender: "Pria", id: "Sadaltager" },
                { name: "Schedar", gender: "Pria", id: "Schedar" },
                { name: "Iapetus", gender: "Pria", id: "Iapetus" },
            ];

            let selectedVoiceId = voices.length > 0 ? voices[0].id : null;
            let currentPreviewAudio = null;
            let audioHistory = JSON.parse(localStorage.getItem('voiceOverHistory') || '[]');
            let currentAudioUrl = null;
            let currentAudioText = '';

            // Text Templates
            templateBtns.forEach(btn => {
                btn.addEventListener('click', () => {
                    const template = btn.dataset.template;
                    textInput.value = textTemplates[template] || '';
                    textInput.dispatchEvent(new Event('input'));
                });
            });

            function populateVoiceList() {
                if (!voiceList) {
                    console.error('Voice list element not found!');
                    return;
                }
                console.log('Populating voice list with', voices.length, 'voices');
                voiceList.innerHTML = '';
                voices.forEach((voice, index) => {
                    const voiceItem = document.createElement('div');
                    voiceItem.className = 'voice-item flex items-center justify-between p-3 border border-gray-200 rounded-lg cursor-pointer transition-all hover:bg-blue-50';
                    voiceItem.dataset.voiceId = voice.id;
                    if (index === 0) { voiceItem.classList.add('selected'); }
                    const nameAndGender = document.createElement('div');
                    nameAndGender.innerHTML = `<span class="font-semibold text-gray-800">${voice.name}</span> <span class="text-xs text-gray-500 ml-2 px-2 py-1 bg-gray-200 rounded-full">${voice.gender}</span>`;
                    const previewButton = document.createElement('button');
                    previewButton.className = 'preview-btn text-gray-500 hover:text-blue-600 transition-colors p-1 rounded-full flex items-center justify-center';
                    previewButton.title = `Pratinjau Suara ${voice.name}`;
                    previewButton.dataset.voiceId = voice.id;
                    previewButton.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 pointer-events-none" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM9.555 7.168A1 1 0 008 8v4a1 1 0 001.555.832l3-2a1 1 0 000-1.664l-3-2z" clip-rule="evenodd" /></svg>`;
                    voiceItem.append(nameAndGender, previewButton);
                    voiceList.appendChild(voiceItem);
                });
                console.log('Voice list populated successfully, items:', voiceList.children.length);
            }
            populateVoiceList();

            // Event delegation for voice selection and preview (works on desktop & mobile)
            voiceList.addEventListener('click', async (e) => {
                e.stopPropagation();

                const previewBtn = e.target.closest('.preview-btn');
                if (previewBtn) {
                    // Handle preview button click
                    if (currentPreviewAudio) { currentPreviewAudio.pause(); }
                    const voiceId = previewBtn.dataset.voiceId;
                    const voice = voices.find(v => v.id === voiceId);
                    if (!voice) return;

                    const originalIcon = previewBtn.innerHTML;
                    previewBtn.innerHTML = `<div class="preview-loader"></div>`;
                    previewBtn.disabled = true;
                    try {
                        const pitch = parseFloat(pitchSlider.value);
                        const volume = parseFloat(volumeSlider.value);
                        const audioUrl = await generateSpeech(previewText, voice.id, pitch, volume);
                        currentPreviewAudio = new Audio(audioUrl);
                        currentPreviewAudio.play();
                        currentPreviewAudio.onended = () => {
                            previewBtn.innerHTML = originalIcon;
                            previewBtn.disabled = false;
                        };
                    } catch (error) {
                        console.error("Preview failed:", error);
                        showError("Gagal membuat pratinjau.");
                        previewBtn.innerHTML = originalIcon;
                        previewBtn.disabled = false;
                    }
                    return;
                }

                // Handle voice item selection
                const voiceItem = e.target.closest('.voice-item');
                if (voiceItem) {
                    document.querySelectorAll('.voice-item').forEach(item => item.classList.remove('selected'));
                    voiceItem.classList.add('selected');
                    selectedVoiceId = voiceItem.dataset.voiceId;
                }
            });

            // Event Listeners for controls
            pitchSlider.addEventListener('input', () => { pitchValue.textContent = parseFloat(pitchSlider.value).toFixed(1); });
            resetPitchBtn.addEventListener('click', () => { pitchSlider.value = 0; pitchSlider.dispatchEvent(new Event('input')); });

            volumeSlider.addEventListener('input', () => {
                const value = parseFloat(volumeSlider.value);
                volumeValue.textContent = `${value.toFixed(1)} dB`;
            });
            resetVolumeBtn.addEventListener('click', () => { volumeSlider.value = 0; volumeSlider.dispatchEvent(new Event('input')); });

            // Speed control
            speedSlider.addEventListener('input', () => {
                const value = parseFloat(speedSlider.value);
                speedValue.textContent = `${value.toFixed(2)}x`;
                if (audioPlayer.src) {
                    audioPlayer.playbackRate = value;
                }
            });
            resetSpeedBtn.addEventListener('click', () => { speedSlider.value = 1; speedSlider.dispatchEvent(new Event('input')); });

            // Text input with word counter
            textInput.addEventListener('input', () => {
                const text = textInput.value;
                const count = text.length;
                const words = text.trim() ? text.trim().split(/\s+/).length : 0;
                charCounter.textContent = `${count} / ${CHAR_LIMIT} characters`;
                wordCounter.textContent = `${words} words`;
                charCounter.classList.toggle('text-red-500', count > CHAR_LIMIT);
            });

            clearTextBtn.addEventListener('click', () => {
                textInput.value = '';
                styleInput.value = '';
                textInput.dispatchEvent(new Event('input'));
                textInput.focus();
            });

            copyTextBtn.addEventListener('click', async () => {
                if (textInput.value) {
                    try {
                        await navigator.clipboard.writeText(textInput.value);
                        copyTextBtn.innerHTML = '<i class="fas fa-check text-green-500"></i>';
                        setTimeout(() => {
                            copyTextBtn.innerHTML = '<i class="fas fa-copy"></i>';
                        }, 2000);
                    } catch(e) {}
                }
            });

            // Modal handlers
            const closeModal = () => { guidanceModal.classList.add('hidden'); };
            if (openGuidanceBtn) openGuidanceBtn.addEventListener('click', () => { guidanceModal.classList.remove('hidden'); });
            if (closeGuidanceBtn) closeGuidanceBtn.addEventListener('click', closeModal);
            if (understandBtn) understandBtn.addEventListener('click', closeModal);
            if (guidanceModal) guidanceModal.addEventListener('click', (event) => { if (event.target === guidanceModal) { closeModal(); } });

            // Audio player listeners
            audioPlayer.addEventListener('play', () => {
                audioContainer.classList.add('playing');
                audioPlayer.playbackRate = parseFloat(speedSlider.value);
            });
            audioPlayer.addEventListener('pause', () => audioContainer.classList.remove('playing'));
            audioPlayer.addEventListener('ended', () => audioContainer.classList.remove('playing'));
            audioPlayer.addEventListener('loadedmetadata', () => {
                const duration = audioPlayer.duration;
                const mins = Math.floor(duration / 60);
                const secs = Math.floor(duration % 60);
                audioDuration.textContent = `${mins}:${secs.toString().padStart(2, '0')}`;
            });

            // Add to history button
            addToHistoryBtn.addEventListener('click', () => {
                if (currentAudioUrl && currentAudioText) {
                    const historyItem = {
                        id: Date.now(),
                        text: currentAudioText.substring(0, 100) + (currentAudioText.length > 100 ? '...' : ''),
                        voiceName: voices.find(v => v.id === selectedVoiceId)?.name || 'Unknown',
                        audioUrl: currentAudioUrl,
                        timestamp: new Date().toLocaleString()
                    };
                    audioHistory.unshift(historyItem);
                    if (audioHistory.length > 10) audioHistory = audioHistory.slice(0, 10);
                    localStorage.setItem('voiceOverHistory', JSON.stringify(audioHistory));
                    renderHistory();
                    // Show feedback
                    addToHistoryBtn.innerHTML = '<i class="fas fa-check mr-2"></i>Saved!';
                    setTimeout(() => {
                        addToHistoryBtn.innerHTML = '<i class="fas fa-save"></i><span>Save to History</span>';
                    }, 2000);
                }
            });

            // Clear history
            clearHistoryBtn.addEventListener('click', () => {
                if (confirm('Clear all audio history?')) {
                    audioHistory = [];
                    localStorage.removeItem('voiceOverHistory');
                    renderHistory();
                }
            });

            // Render history function
            function renderHistory() {
                if (audioHistory.length === 0) {
                    historyContainer.classList.add('hidden');
                    return;
                }

                historyContainer.classList.remove('hidden');
                historyList.innerHTML = '';

                audioHistory.forEach((item) => {
                    const historyItem = document.createElement('div');
                    historyItem.className = 'bg-gradient-to-r from-purple-50 to-pink-50 p-3 rounded-lg border border-purple-200 hover:shadow-md transition-all';
                    historyItem.innerHTML = `
                        <div class="flex items-center justify-between">
                            <div class="flex-1">
                                <p class="text-sm font-semibold text-gray-800 truncate">${item.text}</p>
                                <p class="text-xs text-gray-500 mt-1">
                                    <i class="fas fa-microphone mr-1"></i>${item.voiceName} • ${item.timestamp}
                                </p>
                            </div>
                            <div class="flex gap-2 ml-3">
                                <button data-id="${item.id}" data-action="play" class="history-btn text-purple-600 hover:text-purple-800 p-2">
                                    <i class="fas fa-play"></i>
                                </button>
                                <button data-id="${item.id}" data-action="download" class="history-btn text-green-600 hover:text-green-800 p-2">
                                    <i class="fas fa-download"></i>
                                </button>
                                <button data-id="${item.id}" data-action="delete" class="history-btn text-red-600 hover:text-red-800 p-2">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </div>
                    `;
                    historyList.appendChild(historyItem);
                });

                // History button event delegation (works on desktop & mobile)
                historyList.addEventListener('click', async (e) => {
                    e.stopPropagation();

                    const btn = e.target.closest('.history-btn');
                    if (!btn) return;

                    const id = parseInt(btn.dataset.id);
                    const action = btn.dataset.action;
                    const item = audioHistory.find(h => h.id === id);
                    if (!item) return;

                    if (action === 'play') {
                        audioPlayer.src = item.audioUrl;
                        audioPlayer.play();
                        resultContainer.classList.remove('hidden');
                        audioContainer.classList.remove('hidden');
                        loaderContainer.classList.add('hidden');
                        errorMessage.classList.add('hidden');
                    } else if (action === 'download') {
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(item.audioUrl, `voice-over-${id}.wav`);
                        } else if (window.downloadImage) {
                            await window.downloadImage(item.audioUrl, `voice-over-${id}.wav`);
                        }
                    } else if (action === 'delete') {
                        audioHistory = audioHistory.filter(h => h.id !== id);
                        localStorage.setItem('voiceOverHistory', JSON.stringify(audioHistory));
                        renderHistory();
                    }
                });
            }

            // Initial render of history
            renderHistory();

            // Core Functions
            function base64ToArrayBuffer(base64) {
                const binaryString = window.atob(base64); const len = binaryString.length; const bytes = new Uint8Array(len);
                for (let i = 0; i < len; i++) { bytes[i] = binaryString.charCodeAt(i); } return bytes.buffer;
            }
            function pcmToWav(pcmData, sampleRate) {
                const numChannels = 1; const bitsPerSample = 16; const blockAlign = (numChannels * bitsPerSample) / 8;
                const byteRate = sampleRate * blockAlign; const dataSize = pcmData.byteLength; const buffer = new ArrayBuffer(44 + dataSize);
                const view = new DataView(buffer);
                function writeString(view, offset, string) { for (let i = 0; i < string.length; i++) { view.setUint8(offset + i, string.charCodeAt(i)); } }
                writeString(view, 0, 'RIFF'); view.setUint32(4, 36 + dataSize, true); writeString(view, 8, 'WAVE');
                writeString(view, 12, 'fmt '); view.setUint32(16, 16, true); view.setUint16(20, 1, true);
                view.setUint16(22, numChannels, true); view.setUint32(24, sampleRate, true); view.setUint32(28, byteRate, true);
                view.setUint16(32, blockAlign, true); view.setUint16(34, bitsPerSample, true); writeString(view, 36, 'data');
                view.setUint32(40, dataSize, true);
                const wavBytes = new Uint8Array(buffer); wavBytes.set(new Uint8Array(pcmData), 44);
                return new Blob([wavBytes], { type: 'audio/wav' });
            }
            async function generateSpeech(text, voiceId, pitch, volume, retries = 3, delay = 1000) {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-tts:generateContent?key=${apiKey}`;
                const payload = {
                    model: "gemini-2.5-flash-preview-tts",
                    contents: [{ parts: [{ text: text }] }],
                    generationConfig: {
                        responseModalities: ["AUDIO"],
                        speechConfig: {
                            pitch: pitch,
                            volumeGainDb: volume,
                            voiceConfig: { prebuiltVoiceConfig: { voiceName: voiceId } }
                        }
                    },
                };
                try {
                    const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                    if (!response.ok) {
                        if (response.status === 429 && retries > 0) { await new Promise(r => setTimeout(r, delay)); return generateSpeech(text, voiceId, pitch, volume, retries - 1, delay * 2); }
                        const errorBody = await response.json(); throw new Error(`API Error: ${response.status} - ${errorBody.error.message}`);
                    }
                    const result = await response.json();
                    const part = result?.candidates?.[0]?.content?.parts?.[0];
                    if (part?.inlineData?.data) {
                        const sampleRate = parseInt(part.inlineData.mimeType.match(/rate=(\d+)/)[1], 10) || 24000;
                        const pcmData = base64ToArrayBuffer(part.inlineData.data);
                        return URL.createObjectURL(pcmToWav(pcmData, sampleRate));
                    } else { throw new Error("Tidak ada data audio yang diterima."); }
                } catch (error) { console.error("API call failed:", error); throw error; }
            }
            generateBtn.addEventListener('click', async () => {
                const mainText = textInput.value.trim();
                const styleText = styleInput.value.trim();

                if (!mainText) {
                    showError('Mohon masukkan teks terlebih dahulu.');
                    return;
                }
                if (mainText.length > CHAR_LIMIT) {
                    showError(`Teks melebihi batas ${CHAR_LIMIT} karakter.`);
                    return;
                }

                let finalText = mainText;
                if (styleText) {
                    finalText = `${styleText}: ${mainText}`;
                }

                setLoadingState(true);

                try {
                    const pitch = parseFloat(pitchSlider.value);
                    const volume = parseFloat(volumeSlider.value);
                    const audioUrl = await generateSpeech(finalText, selectedVoiceId, pitch, volume);

                    // Store for history
                    currentAudioUrl = audioUrl;
                    currentAudioText = mainText;

                    // Set audio player
                    audioPlayer.src = audioUrl;
                    audioPlayer.playbackRate = parseFloat(speedSlider.value);

                    // Set download button
                    downloadBtn.dataset.audioUrl = audioUrl;
                    downloadBtn.dataset.filename = `voice-over-${Date.now()}.wav`;

                    // Show audio container
                    audioContainer.classList.remove('hidden');

                } catch (error) {
                    showError(`Terjadi kesalahan: ${error.message}`);
                } finally {
                    setLoadingState(false);
                }
            });

            // Download button click handler
            downloadBtn.addEventListener('click', async (e) => {
                e.stopPropagation();
                const audioUrl = downloadBtn.dataset.audioUrl;
                const filename = downloadBtn.dataset.filename;
                if (audioUrl && filename) {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(audioUrl, filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(audioUrl, filename);
                    }
                }
            });

            function setLoadingState(isLoading) {
                generateBtn.disabled = isLoading;
                if (isLoading) {
                    resultContainer.classList.remove('hidden'); loaderContainer.classList.remove('hidden');
                    audioContainer.classList.add('hidden'); errorMessage.classList.add('hidden');
                    generateBtn.innerHTML = 'Membuat...';
                } else {
                    loaderContainer.classList.add('hidden');
                    generateBtn.innerHTML = 'Buat Voice Over';
                }
            }
            function showError(message) {
                resultContainer.classList.remove('hidden'); audioContainer.classList.add('hidden');
                loaderContainer.classList.add('hidden'); errorMessage.textContent = message;
                errorMessage.classList.remove('hidden');
            }
        }
    })();

    // --- STICKER TEMPEL TAB LOGIC ---
    (function() {
        const stickerTabDesktop = document.querySelector('.main-tab-btn[data-tab="sticker"]');
        const stickerTabMobile  = document.querySelector('.mobile-tab-btn[data-tab="sticker"]');
        if (!stickerTabDesktop && !stickerTabMobile) return;

        function onStickerTabClick() {
            if (!window.stickerInitialized) {
                initStickerTempel();
                window.stickerInitialized = true;
            }
        }

        if (stickerTabDesktop) stickerTabDesktop.addEventListener('click', onStickerTabClick);
        if (stickerTabMobile)  stickerTabMobile.addEventListener('click', onStickerTabClick);

        function initStickerTempel() {
            // DOM Elements
            const imageInput = document.getElementById('sticker-image-input');
            const uploadButton = document.getElementById('sticker-upload-button');
            const generateButton = document.getElementById('sticker-generate-button');
            const imagePreview = document.getElementById('sticker-image-preview');
            const uploadPlaceholder = document.getElementById('sticker-upload-placeholder');

            const loadingSpinner = document.getElementById('sticker-loading-spinner');
            const outputPlaceholder = document.getElementById('sticker-output-placeholder');
            const errorMessage = document.getElementById('sticker-error-message');
            const resultContainer = document.getElementById('sticker-result-container');
            const originalOutputImage = document.getElementById('sticker-original-output-image');
            const stickerWrapper = document.getElementById('sticker-sticker-wrapper');
            const cartoonOutputImage = document.getElementById('sticker-cartoon-output-image');
            const resizeHandle = document.getElementById('sticker-resize-handle');
            const dragInstruction = document.getElementById('sticker-drag-instruction');

            const promptAdditionInput = document.getElementById('sticker-prompt-addition');
            const downloadButton = document.getElementById('sticker-download-button');
            const removeBgButton = document.getElementById('sticker-remove-bg-button');
            const toleranceSliderContainer = document.getElementById('sticker-tolerance-slider-container');
            const toleranceSlider = document.getElementById('sticker-tolerance-slider');
            const buttonContainer = document.getElementById('sticker-button-container');

            let base64ImageData = null;
            let fileMimeType = null;
            let stickerAspectRatio = 1;
            let originalCartoonSrc = null;

            // Event Listeners
            uploadButton.addEventListener('click', () => imageInput.click());
            imageInput.addEventListener('change', handleImageUpload);
            generateButton.addEventListener('click', handleGenerateClick);
            downloadButton.addEventListener('click', handleDownload);
            removeBgButton.addEventListener('click', handleRemoveBackground);
            toleranceSlider.addEventListener('input', (e) => applyChromaKey(e.target.value));

            // Add mobile touch support for download and remove bg buttons
            if (window.addMobileTouchSupport) {
                window.addMobileTouchSupport(downloadButton);
                window.addMobileTouchSupport(removeBgButton);
            }

            makeDraggable(stickerWrapper, resultContainer);
            makeResizable(stickerWrapper, resizeHandle, resultContainer);

            function handleImageUpload(event) {
                const file = event.target.files[0];
                if (!file) return;

                fileMimeType = file.type;

                const reader = new FileReader();
                reader.onload = e => {
                    const dataUrl = e.target.result;
                    imagePreview.src = dataUrl;
                    imagePreview.classList.remove('hidden');
                    uploadPlaceholder.classList.add('hidden');
                    generateButton.disabled = false;

                    base64ImageData = dataUrl.split(',')[1];
                };
                reader.readAsDataURL(file);
            }

            async function handleGenerateClick() {
                if (!base64ImageData || !fileMimeType) {
                    showError("Silakan unggah gambar terlebih dahulu.");
                    return;
                }

                setLoadingState(true);

                const additionalPrompt = promptAdditionInput.value.trim();
                let basePrompt;

                if (additionalPrompt) {
                    basePrompt = `CRITICAL INSTRUCTION: The final image MUST be in a "${additionalPrompt}" style. This is the most important rule. Generate a sticker of the subject from the user's image. The sticker MUST be placed on a solid, vibrant green screen background (color #00FF00). Do NOT use any other background. The subject must be clearly defined against the green background.`;
                } else {
                    basePrompt = "Generate a cartoon/anime style sticker of the main subject. The sticker MUST be placed on a solid, vibrant green screen background (color #00FF00). Do NOT use any other background. The subject must be clearly defined against the green background.";
                }

                try {
                    const generatedImage = await callImageGenerationAPI(basePrompt, base64ImageData, fileMimeType);
                    if (generatedImage) {
                        const imageUrl = `data:image/png;base64,${generatedImage}`;
                        originalCartoonSrc = imageUrl;
                        originalOutputImage.src = imagePreview.src;
                        cartoonOutputImage.src = imageUrl;

                        cartoonOutputImage.onload = () => {
                            stickerAspectRatio = cartoonOutputImage.naturalWidth / cartoonOutputImage.naturalHeight;
                            const newWidth = stickerWrapper.offsetWidth;
                            stickerWrapper.style.height = `${newWidth / stickerAspectRatio}px`;
                        }

                        resultContainer.classList.remove('hidden');
                        stickerWrapper.classList.remove('hidden');
                        dragInstruction.classList.remove('hidden');
                        outputPlaceholder.classList.add('hidden');

                        buttonContainer.classList.remove('grid-cols-1');
                        buttonContainer.classList.add('grid-cols-2');
                        removeBgButton.classList.remove('hidden');
                        downloadButton.classList.remove('hidden');
                        toleranceSliderContainer.classList.add('hidden');

                    } else {
                        showError("Gagal membuat stiker. API tidak memberikan hasil.");
                    }
                } catch (error) {
                    console.error('API call failed:', error);
                    showError(`Terjadi kesalahan: ${error.message}. Silakan coba lagi.`);
                } finally {
                    setLoadingState(false);
                }
            }

            async function callImageGenerationAPI(prompt, imageData, mimeType, retries = 3, delay = 1000) {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const payload = {
                    contents: [{ parts: [{ text: prompt }, { inlineData: { mimeType, data: imageData } }] }],
                    generationConfig: { responseModalities: ['TEXT', 'IMAGE'] }
                };
                for (let i = 0; i < retries; i++) {
                    try {
                        const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                        if (!response.ok) {
                            let errorText = `HTTP error! status: ${response.status} ${response.statusText}`;
                            try { const errorBody = await response.json(); errorText += `, message: ${errorBody?.error?.message || 'No error message'}`; } catch (e) { /* Ignore */ }
                            throw new Error(errorText);
                        }
                        const result = await response.json();
                        const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                        if (!base64Data) throw new Error("Tidak ada data gambar yang diterima dari API.");
                        return base64Data;
                    } catch (error) {
                        console.error(`Attempt ${i + 1} failed:`, error.message);
                        if (i === retries - 1) throw error;
                        await new Promise(res => setTimeout(res, delay * Math.pow(2, i)));
                    }
                }
            }

            function setLoadingState(isLoading) {
                generateButton.disabled = isLoading;
                generateButton.textContent = isLoading ? 'Membuat...' : 'Buat Stiker!';
                loadingSpinner.classList.toggle('hidden', !isLoading);
                if(isLoading) {
                    resultContainer.classList.add('hidden'); stickerWrapper.classList.add('hidden');
                    downloadButton.classList.add('hidden'); removeBgButton.classList.add('hidden');
                    dragInstruction.classList.add('hidden'); outputPlaceholder.classList.add('hidden');
                    errorMessage.classList.add('hidden'); toleranceSliderContainer.classList.add('hidden');
                }
            }

            function showError(message) {
                errorMessage.textContent = message;
                errorMessage.classList.remove('hidden');
                outputPlaceholder.classList.remove('hidden');
                resultContainer.classList.add('hidden'); stickerWrapper.classList.add('hidden');
                downloadButton.classList.add('hidden'); removeBgButton.classList.add('hidden');
                dragInstruction.classList.add('hidden');
            }

            function handleRemoveBackground() {
                toleranceSliderContainer.classList.remove('hidden');
                removeBgButton.classList.add('hidden');
                buttonContainer.classList.remove('grid-cols-2');
                buttonContainer.classList.add('grid-cols-1');
                applyChromaKey(toleranceSlider.value);
            }

            function applyChromaKey(tolerance) {
                if (!originalCartoonSrc) return;
                const tempImg = new Image();
                tempImg.crossOrigin = "anonymous";
                tempImg.onload = () => {
                    const canvas = document.createElement('canvas');
                    const ctx = canvas.getContext('2d');
                    canvas.width = tempImg.naturalWidth;
                    canvas.height = tempImg.naturalHeight;
                    ctx.drawImage(tempImg, 0, 0);

                    const imageData = ctx.getImageData(0, 0, canvas.width, canvas.height);
                    const data = imageData.data;
                    const keyR = 0, keyG = 255, keyB = 0;
                    const toleranceValue = parseInt(tolerance, 10);

                    for (let i = 0; i < data.length; i += 4) {
                        const r = data[i], g = data[i + 1], b = data[i + 2];
                        const distance = Math.sqrt(Math.pow(r - keyR, 2) + Math.pow(g - keyG, 2) + Math.pow(b - keyB, 2));
                        if (distance < toleranceValue) {
                            data[i + 3] = 0;
                        }
                    }
                    ctx.putImageData(imageData, 0, 0);
                    cartoonOutputImage.src = canvas.toDataURL('image/png');
                };
                tempImg.src = originalCartoonSrc;
            }

            function makeDraggable(element, container) {
                let isDragging = false, initialX, initialY;
                const dragStart = (e) => {
                    if (e.target.id === 'sticker-resize-handle') return;
                    isDragging = true;
                    initialX = (e.type === 'touchstart' ? e.touches[0].clientX : e.clientX) - element.offsetLeft;
                    initialY = (e.type === 'touchstart' ? e.touches[0].clientY : e.clientY) - element.offsetTop;
                };
                const dragEnd = () => isDragging = false;
                const drag = (e) => {
                    if (!isDragging) return;
                    e.preventDefault();
                    const currentX = (e.type === 'touchmove' ? e.touches[0].clientX : e.clientX) - initialX;
                    const currentY = (e.type === 'touchmove' ? e.touches[0].clientY : e.clientY) - initialY;
                    const xPos = Math.max(0, Math.min(currentX, container.offsetWidth - element.offsetWidth));
                    const yPos = Math.max(0, Math.min(currentY, container.offsetHeight - element.offsetHeight));
                    element.style.left = `${xPos}px`;
                    element.style.top = `${yPos}px`;
                };
                element.addEventListener("mousedown", dragStart);
                document.addEventListener("mouseup", dragEnd);
                document.addEventListener("mousemove", drag);
                element.addEventListener("touchstart", dragStart, { passive: true });
                document.addEventListener("touchend", dragEnd);
                document.addEventListener("touchmove", drag, { passive: false });
            }
            function makeResizable(element, handle, container) {
                let isResizing = false, initialMouseX, initialWidth;
                const resizeStart = (e) => {
                    e.preventDefault();
                    isResizing = true;
                    initialMouseX = e.type === 'touchstart' ? e.touches[0].clientX : e.clientX;
                    initialWidth = element.offsetWidth;
                    document.addEventListener("mousemove", resize);
                    document.addEventListener("mouseup", resizeEnd);
                    document.addEventListener("touchmove", resize, { passive: false });
                    document.addEventListener("touchend", resizeEnd);
                };
                const resize = (e) => {
                    if (!isResizing) return;
                    e.preventDefault();
                    const currentMouseX = e.type === 'touchmove' ? e.touches[0].clientX : e.clientX;
                    const dx = currentMouseX - initialMouseX;
                    let newWidth = initialWidth + dx;
                    const minWidth = 50;
                    const maxWidth = container.offsetWidth - element.offsetLeft;
                    newWidth = Math.max(minWidth, Math.min(newWidth, maxWidth));
                    element.style.width = `${newWidth}px`;
                    element.style.height = `${newWidth / stickerAspectRatio}px`;
                };
                const resizeEnd = () => {
                    isResizing = false;
                    document.removeEventListener("mousemove", resize);
                    document.removeEventListener("mouseup", resizeEnd);
                    document.removeEventListener("touchmove", resize);
                    document.removeEventListener("touchend", resizeEnd);
                };
                handle.addEventListener("mousedown", resizeStart);
                handle.addEventListener("touchstart", resizeStart, { passive: true });
            }

            function handleDownload() {
                const canvas = document.createElement('canvas');
                const ctx = canvas.getContext('2d');
                const originalImg = originalOutputImage;
                const cartoonImg = cartoonOutputImage;

                canvas.width = originalImg.naturalWidth;
                canvas.height = originalImg.naturalHeight;

                ctx.drawImage(originalImg, 0, 0, canvas.width, canvas.height);

                const container = resultContainer;
                const containerWidth = container.offsetWidth;
                const containerHeight = container.offsetHeight;
                const imgNaturalWidth = originalImg.naturalWidth;
                const imgNaturalHeight = originalImg.naturalHeight;

                const containerRatio = containerWidth / containerHeight;
                const imgRatio = imgNaturalWidth / imgNaturalHeight;

                let renderedWidth, renderedHeight, offsetX, offsetY, scale;

                if (imgRatio > containerRatio) {
                    renderedWidth = containerWidth;
                    scale = imgNaturalWidth / renderedWidth;
                    renderedHeight = imgNaturalHeight / scale;
                    offsetX = 0;
                    offsetY = (containerHeight - renderedHeight) / 2;
                } else {
                    renderedHeight = containerHeight;
                    scale = imgNaturalHeight / renderedHeight;
                    renderedWidth = imgNaturalWidth / scale;
                    offsetY = 0;
                    offsetX = (containerWidth - renderedWidth) / 2;
                }

                const adjustedStickerX = stickerWrapper.offsetLeft - offsetX;
                const adjustedStickerY = stickerWrapper.offsetTop - offsetY;

                const finalCanvasX = adjustedStickerX * scale;
                const finalCanvasY = adjustedStickerY * scale;
                const finalCanvasWidth = stickerWrapper.offsetWidth * scale;
                const finalCanvasHeight = stickerWrapper.offsetHeight * scale;

                ctx.drawImage(cartoonImg, finalCanvasX, finalCanvasY, finalCanvasWidth, finalCanvasHeight);

                const imageDataUrl = canvas.toDataURL('image/png');
                if (window.downloadDataURINew) {
                    window.downloadDataURINew(imageDataUrl, 'hasil-stiker.png');
                } else if (window.downloadImage) {
                    window.downloadImage(imageDataUrl, 'hasil-stiker.png');
                }
            }
        }
    })();

    // ==================== STUDIO AESTHETIC ====================
    (function() {
        const apiKey = "";

        const imageInput = document.getElementById('studio-aesthetic-image-input');
        const imagePreview = document.getElementById('studio-aesthetic-image-preview');
        const placeholder = document.getElementById('studio-aesthetic-placeholder');
        const removeBtn = document.getElementById('studio-aesthetic-remove-btn');

        const templateOptions = document.getElementById('studio-aesthetic-template-options');
        const ratioOptions = document.getElementById('studio-aesthetic-ratio-options');
        const countSelGrid = document.getElementById('studio-aesthetic-count-selection-grid');
        const customInput = document.getElementById('studio-aesthetic-custom-input');

        const generateBtn = document.getElementById('studio-aesthetic-generate-btn');
        const downloadAllBtn = document.getElementById('studio-aesthetic-download-all-btn');
        const resultsGrid = document.getElementById('studio-aesthetic-results-grid');
        const emptyState = document.getElementById('studio-aesthetic-empty-state');

        let uploadedImageData = null;
        let selectedTemplate = 'korean-chibi-sticker';
        let selectedRatio = '1:1';
        let selectedCount = 4;
        let generatedImages = [];

        // Template registry — adding new template = 1 entry here + enable button in HTML.
        // Each builder takes {customAddition} ctx and returns full prompt string.
        const templateBuilders = {
            'korean-chibi-sticker': (ctx) => {
                const { customAddition } = ctx;
                const customLine = customAddition
                    ? `\nAdditional user customization: ${customAddition}`
                    : '';
                return `Use the uploaded reference photo as the MAIN CHARACTER at the center of the composition. Preserve real facial features, expression, and identity exactly as in the reference photo. Keep the original photo's background.

Apply warm, soft, clean lighting with subtle shadows for a natural and aesthetic feel that's comfortable to look at.

Around the main character, add MULTIPLE MINI CHIBI VERSIONS inspired by the same person — 3D cute style chibi clones rendered in various poses and expressions:
- Jumping joyfully
- Waving hand
- Sitting relaxed
- Holding a drink
- Happy and playful expressions

Each chibi maintains the person's recognizable real facial features but rendered cuter, more playful, and more expressive in chibi proportions.

Add WHITE DOODLE HAND-DRAWN ELEMENTS scattered throughout the composition:
- White outline glow around the main subject
- Small hand-drawn stars
- Hearts
- Sparkles
- Motion lines / movement marks
- Tiny cute icons

Add AESTHETIC HAND-WRITTEN TEXT in casual doodle font with phrases like: "shine", "bright day", "happy", "smile" — placed organically around the composition.

Overall style: clean and aesthetic composition, white sticker outline aesthetic, soft pastel color tone, high detail, glossy 3D chibi look, cute Korean style, instagrammable, modern social media visual, kawaii vibe.${customLine}`;
            }
            // Future templates registered here:
            //   'y2k-sticker-pack': (ctx) => `...chrome metallic, butterfly clips, holographic Y2K aesthetic...`,
            //   'pastel-dream': (ctx) => `...soft pastel gradient, dreamy clouds, glitter sparkles...`,
        };

        function checkReady() {
            generateBtn.disabled = !uploadedImageData;
        }

        if (countSelGrid) {
            countSelGrid.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                countSelGrid.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        templateOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('button[data-value]:not([disabled])');
            if (!btn) return;
            templateOptions.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
            btn.classList.add('selected');
            selectedTemplate = btn.dataset.value;
        });

        ratioOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('button[data-value]');
            if (!btn) return;
            ratioOptions.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
            btn.classList.add('selected');
            selectedRatio = btn.dataset.value;
        });

        imageInput.addEventListener('change', async (e) => {
            const file = e.target.files[0];
            if (!file) return;
            try {
                let processedFile = file;
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    if (window.heic2any) {
                        const blob = await window.heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([blob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }
                const reader = new FileReader();
                reader.onload = (ev) => {
                    const dataUrl = ev.target.result;
                    uploadedImageData = {
                        base64: dataUrl.split(',')[1],
                        mimeType: processedFile.type,
                        dataUrl: dataUrl
                    };
                    imagePreview.src = dataUrl;
                    imagePreview.classList.remove('hidden');
                    placeholder.classList.add('hidden');
                    removeBtn.classList.remove('hidden');
                    checkReady();
                };
                reader.readAsDataURL(processedFile);
            } catch (err) {
                console.error('Studio Aesthetic image processing failed:', err);
                alert('Gagal memproses gambar. Silakan coba lagi.');
            }
        });

        removeBtn.addEventListener('click', () => {
            uploadedImageData = null;
            imageInput.value = '';
            imagePreview.src = '';
            imagePreview.classList.add('hidden');
            placeholder.classList.remove('hidden');
            removeBtn.classList.add('hidden');
            checkReady();
        });

        function buildPrompt() {
            const ctx = {
                customAddition: customInput.value.trim()
            };
            const builder = templateBuilders[selectedTemplate] || templateBuilders['korean-chibi-sticker'];
            return builder(ctx);
        }

        generateBtn.addEventListener('click', async () => {
            if (!uploadedImageData) {
                alert('Silakan upload foto karakter dulu.');
                return;
            }
            const prompt = buildPrompt();
            await generateStudioAestheticSet(prompt);
        });

        async function generateStudioAestheticSet(prompt) {
            const originalBtnHTML = generateBtn.innerHTML;
            generateBtn.disabled = true;
            generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i><span data-i18n="aes.generate-progress">Membuat foto...</span>';

            emptyState.classList.add('hidden');
            resultsGrid.classList.remove('hidden');
            downloadAllBtn.classList.add('hidden');

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                resultsGrid.innerHTML = '';

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `studio-aesthetic-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gradient-to-br from-pink-100 to-purple-100 flex items-center justify-center aspect-[3/4]';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-pink-500"></div>';
                    resultsGrid.appendChild(card);
                }

                const promises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSingleStudioAesthetic(i + 1, prompt)
                );
                await Promise.allSettled(promises);

                resultsGrid.querySelectorAll('[id^="studio-aesthetic-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });

                successCount = generatedImages.filter(Boolean).length;
            }

            generateBtn.disabled = false;
            generateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                downloadAllBtn.classList.remove('hidden');
            }
        }

        async function generateSingleStudioAesthetic(index, basePrompt) {
            const card = document.getElementById(`studio-aesthetic-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const finalPrompt = `${basePrompt}\n\nVariation ${index}: Create a unique interpretation with slightly different chibi poses, doodle placements, or text positioning while keeping the core aesthetic composition consistent.`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: finalPrompt },
                            { inlineData: { mimeType: uploadedImageData.mimeType, data: uploadedImageData.base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data in response');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const filename = `studio_aesthetic_${selectedTemplate}_${index}.png`;

                generatedImages[index - 1] = { url: imageUrl, filename: filename };

                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Studio Aesthetic ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="action-btn text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2" style="background: linear-gradient(135deg, #f472b6 0%, #a855f7 100%);">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
            } catch (error) {
                console.error(`Error generating studio aesthetic ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        resultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();
            const btn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
            if (!btn) return;
            const action = btn.dataset.action;
            const idx = parseInt(btn.dataset.index, 10);
            const img = generatedImages[idx];
            if (!img) return;

            if (action === 'preview' && window.showPreviewModal) {
                window.showPreviewModal(img.url);
            } else if (action === 'download') {
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(img.url, img.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(img.url, img.filename);
                }
            }
        });

        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();
                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;
                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;
                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-1"></i>Downloading ${i + 1}/${generatedImages.length}...`;
                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(r => setTimeout(r, 500));
                        } catch (err) {
                            console.error(`Download ${i + 1} failed:`, err);
                        }
                    }
                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-1"></i>Selesai!';
                    setTimeout(() => { downloadAllBtn.disabled = false; downloadAllBtn.innerHTML = originalHTML; }, 2000);
                } catch (err) {
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-1"></i>Error';
                    setTimeout(() => { downloadAllBtn.disabled = false; downloadAllBtn.innerHTML = originalHTML; }, 2000);
                }
            });
        }
    })();

    // --- HAIR GENERATOR TAB LOGIC ---
    (function() {
        const hairTab = document.querySelector('.main-tab-btn[data-tab="hair-generator"]');
        const hairTabMobile = document.querySelector('.mobile-tab-btn[data-tab="hair-generator"]');

        const initOnClick = () => {
            if (!window.hairGeneratorInitialized) {
                initHairGenerator();
                window.hairGeneratorInitialized = true;
            }
        };

        if (hairTab) hairTab.addEventListener('click', initOnClick);
        if (hairTabMobile) hairTabMobile.addEventListener('click', initOnClick);

        function initHairGenerator() {
            // Data Pilihan Rambut
            const hairData = {
                wanita: {
                    styles: ["Pixie cut", "Bob cut", "Lob (long bob)", "Shag haircut", "Rambut panjang lurus", "Rambut panjang bergelombang", "Rambut ikal (curly)", "Rambut keriting (kinky)", "Poni (Bangs)", "Poni samping (Side-swept bangs)", "Sanggul (Bun)", "Kuncir kuda (Ponytail)", "Kepang (Braids)", "Gaya Asimetris", "Undercut wanita", "Mullet wanita", "Layered haircut", "Gaya rambut pernikahan", "Gaya rambut formal", "Rambut super pendek"],
                    colors: ["Hitam alami", "Cokelat tua", "Cokelat muda", "Cokelat karamel", "Merah burgundy", "Merah tembaga", "Pirang platinum", "Pirang ash blonde", "Pirang madu", "Rose gold", "Biru tua", "Biru muda", "Ungu lavender", "Ungu tua", "Hijau emerald", "Pink pastel", "Abu-abu (Granny hair)", "Ombre (gelap ke terang)", "Balayage", "Highlight pirang"]
                },
                pria: {
                    styles: ["Crew cut", "Buzz cut", "Undercut", "Fade (high fade)", "Fade (low fade)", "Pompadour", "Quiff", "Slick back", "Man bun", "Comma hair (gaya Korea)", "Two block haircut", "Mullet pria", "Rambut gondrong (panjang)", "Rambut ikal pria", "Caesar cut", "French crop", "Side part", "Spiky hair", "Rambut sedang berantakan", "Rambut super pendek"],
                    colors: ["Hitam alami", "Cokelat tua", "Cokelat muda", "Pirang platinum", "Pirang ash blonde", "Abu-abu", "Salt and pepper", "Biru tua", "Hijau tua", "Merah burgundy", "Highlight tipis", "Cokelat kemerahan", "Pria pirang", "Ungu gelap", "Silver", "Putih", "Cokelat espresso", "Pirang gelap", "Merah jahe", "Warna natural"]
                }
            };

            // DOM Elements
            const imageInput = document.getElementById('hair-image-input');
            const uploadText = document.getElementById('hair-upload-text');
            const previewContainer = document.getElementById('hair-preview-container');
            const imagePreview = document.getElementById('hair-image-preview');
            const removePreviewBtn = document.getElementById('hair-remove-preview');
            const genderWanita = document.getElementById('hair-gender-wanita');
            const genderPria = document.getElementById('hair-gender-pria');
            const styleSelect = document.getElementById('hair-style-select');
            const colorSelect = document.getElementById('hair-color-select');
            const generateBtn = document.getElementById('hair-generate-btn');
            const errorMessage = document.getElementById('hair-error-message');
            const errorText = document.getElementById('hair-error-text');

            const resultsPlaceholder = document.getElementById('hair-results-placeholder');
            const resultsContainer = document.getElementById('hair-results-container');
            const resultsGrid = document.getElementById('hair-results-grid');

            // Count selection (1-10), default 4
            let selectedCount = 4;
            const hairCountSel = document.getElementById('hair-count-selection-grid');
            if (hairCountSel) {
                hairCountSel.addEventListener('click', (e) => {
                    const btn = e.target.closest('button[data-count]');
                    if (!btn) return;
                    hairCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    selectedCount = parseInt(btn.dataset.count, 10);
                });
            }
            const downloadAllBtn = document.getElementById('hair-download-all-btn');
            const hairRatioBtns = document.querySelectorAll('.hair-ratio-btn');

            let currentGender = 'wanita';
            let originalImageData = null;
            let originalMimeType = '';
            let generatedImages = [];
            let selectedRatio = '3:4';

            // Initialize
            populateSelects(currentGender);

            // Event Listeners
            imageInput.addEventListener('change', handleImageUpload);
            genderWanita.addEventListener('click', () => handleGenderChange('wanita'));
            genderPria.addEventListener('click', () => handleGenderChange('pria'));
            generateBtn.addEventListener('click', generateImages);

            // Remove preview button
            if (removePreviewBtn) {
                removePreviewBtn.addEventListener('click', () => {
                    originalImageData = null;
                    originalMimeType = '';
                    imageInput.value = '';
                    uploadText.textContent = 'Unggah Foto';
                    if (previewContainer) previewContainer.classList.add('hidden');
                    if (imagePreview) imagePreview.src = '';
                    resetResults();
                });
            }

            // Aspect Ratio Selection
            hairRatioBtns.forEach(btn => {
                btn.addEventListener('click', () => {
                    hairRatioBtns.forEach(b => {
                        b.classList.remove('hair-ratio-selected', 'bg-blue-600', 'text-white', 'border-blue-500', 'font-medium');
                        b.classList.add('bg-gray-800', 'hover:bg-gray-700', 'text-gray-300', 'border-gray-700');
                    });
                    btn.classList.add('hair-ratio-selected', 'bg-blue-600', 'text-white', 'border-2', 'border-blue-500', 'font-medium');
                    btn.classList.remove('bg-gray-800', 'hover:bg-gray-700', 'text-gray-300', 'border-gray-700');
                    selectedRatio = btn.dataset.value;
                });
            });

            // Download All
            if (downloadAllBtn) {
                downloadAllBtn.addEventListener('click', async () => {
                    if (generatedImages.length === 0) return;
                    downloadAllBtn.disabled = true;
                    downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(img.url, img.filename);
                        } else if (window.downloadImage) {
                            await window.downloadImage(img.url, img.filename);
                        }
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }

                    downloadAllBtn.disabled = false;
                    downloadAllBtn.innerHTML = '<i class="fas fa-download"></i> Download Semua';
                });
            }

            // Event delegation for preview & download buttons
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (action === 'preview') {
                    window.showPreviewModal(imageData.url);
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });

            function populateSelects(gender) {
                // Populate styles
                styleSelect.innerHTML = '';
                hairData[gender].styles.forEach(style => {
                    const option = document.createElement('option');
                    option.value = style;
                    option.textContent = style;
                    styleSelect.appendChild(option);
                });

                // Populate colors
                colorSelect.innerHTML = '';
                hairData[gender].colors.forEach(color => {
                    const option = document.createElement('option');
                    option.value = color;
                    option.textContent = color;
                    colorSelect.appendChild(option);
                });
            }

            async function handleImageUpload(e) {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        uploadText.textContent = 'Converting HEIC...';
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                originalMimeType = processedFile.type;
                const reader = new FileReader();
                reader.onloadend = () => {
                    originalImageData = reader.result;
                    uploadText.textContent = 'Ganti Foto';

                    // Show preview image (mobile only)
                    if (imagePreview && previewContainer) {
                        imagePreview.src = reader.result;
                        previewContainer.classList.remove('hidden');
                    }

                    hideError();
                    resetResults();
                };
                reader.onerror = () => {
                    showError("Gagal membaca file gambar.");
                };
                reader.readAsDataURL(processedFile);
            }

            function handleGenderChange(gender) {
                currentGender = gender;
                if (gender === 'wanita') {
                    genderWanita.classList.add('active');
                    genderPria.classList.remove('active');
                } else {
                    genderPria.classList.add('active');
                    genderWanita.classList.remove('active');
                }
                populateSelects(gender);
            }

            // selectedCount sudah di-declare di scope luar (count selector)
            async function generateSingleHair(index, userPrompt, base64ImageData, systemPrompt, apiUrl) {
                const card = document.getElementById(`hair-card-${index}`);
                try {
                    const payload = {
                        contents: [{
                            parts: [
                                { text: userPrompt },
                                { inlineData: { mimeType: originalMimeType, data: base64ImageData } }
                            ]
                        }],
                        systemInstruction: { parts: [{ text: systemPrompt }] },
                        generationConfig: {
                            responseModalities: ['TEXT', 'IMAGE'],
                            imageConfig: { aspectRatio: selectedRatio }
                        }
                    };

                    const response = await fetch(apiUrl, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(payload)
                    });

                    const result = await response.json();
                    const imagePart = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData);

                    if (!imagePart?.inlineData?.data) throw new Error('No image data');

                    const imageUrl = `data:image/png;base64,${imagePart.inlineData.data}`;
                    const styleVal = styleSelect.value;
                    const filename = `hair-${styleVal.replace(/\s+/g, '-')}-${index}.png`;

                    generatedImages[index - 1] = { url: imageUrl, filename };

                    if (card) {
                        card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all';
                        card.innerHTML = `
                            <img src="${imageUrl}" alt="Hair Result ${index}" class="w-full h-auto object-cover"/>
                            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity flex items-end justify-center pb-4 gap-2">
                                <button data-action="preview" data-index="${index - 1}" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg font-semibold transition flex items-center gap-2">
                                    <i class="fas fa-eye"></i>
                                    <span class="hidden sm:inline">Preview</span>
                                </button>
                                <button data-action="download" data-index="${index - 1}" class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg font-semibold transition flex items-center gap-2">
                                    <i class="fas fa-download"></i>
                                    <span class="hidden sm:inline">Download</span>
                                </button>
                            </div>
                        `;
                    }
                } catch (err) {
                    console.error(`Error generating hair ${index}:`, err);
                    if (card) card.innerHTML = '';
                }
            }

            async function generateImages() {
                if (!originalImageData) {
                    showError("Silakan unggah foto terlebih dahulu.");
                    return;
                }

                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
                hideError();
                resultsPlaceholder.classList.add('hidden');

                const base64ImageData = originalImageData.split(',')[1];
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const selectedStyle = styleSelect.value;
                const selectedColor = colorSelect.value;
                const systemPrompt = "Anda adalah AI ahli penata rambut. Tugas Anda adalah mengedit foto pengguna untuk mengubah gaya dan warna rambut mereka sesuai permintaan. Anda HARUS mempertahankan wajah, fitur wajah, dan latar belakang asli. HANYA ubah rambutnya.";

                let attempts = 0;
                const MAX_ATTEMPTS = 3;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedImages = [];
                    resultsGrid.innerHTML = '';
                    resultsContainer.classList.remove('hidden');

                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `hair-card-${i}`;
                        card.className = 'relative group rounded-xl overflow-hidden bg-gray-800 flex items-center justify-center aspect-[3/4]';
                        card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-blue-400"></div>';
                        resultsGrid.appendChild(card);
                    }

                    const generationPromises = Array.from({ length: selectedCount }, (_, i) => {
                        const userPrompt = `Ubah rambut orang di foto ini menjadi gaya ${selectedStyle} dengan warna ${selectedColor}. Variasi ${i + 1}.`;
                        return generateSingleHair(i + 1, userPrompt, base64ImageData, systemPrompt, apiUrl);
                    });
                    await Promise.allSettled(generationPromises);

                    resultsGrid.querySelectorAll('[id^="hair-card-"]').forEach(card => {
                        if (!card.querySelector('img')) card.remove();
                    });
                    successCount = generatedImages.filter(Boolean).length;
                }

                generateBtn.disabled = false;
                generateBtn.innerHTML = '<i class="fas fa-magic mr-2"></i>ubah Rambutnya!';

                if (successCount === 0) {
                    resultsContainer.classList.add('hidden');
                    resultsPlaceholder.classList.remove('hidden');
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                }
            }

            function resetResults() {
                generatedImages = [];
                resultsGrid.innerHTML = '';
                resultsContainer.classList.add('hidden');
                resultsPlaceholder.classList.remove('hidden');
            }

            function showError(message) {
                errorText.textContent = message;
                errorMessage.classList.remove('hidden');
            }

            function hideError() {
                errorMessage.classList.add('hidden');
            }
        }
    })();

    // ==================== FOTO TOURING ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const fileInputArea = document.getElementById('touring-file-input-area');
        const addPhotoBtn = document.getElementById('touring-add-photo-btn');
        const touringValidation = document.getElementById('touring-validation');
        const roadOptions = document.getElementById('touring-road-options');
        const moodOptions = document.getElementById('touring-mood-options');
        const angleOptions = document.getElementById('touring-angle-options');
        const aspectOptions = document.getElementById('touring-aspect-options');
        const generateBtn = document.getElementById('touring-generate-btn');
        const placeholder = document.getElementById('touring-placeholder');
        const loading = document.getElementById('touring-loading');
        const loadingText = document.getElementById('touring-loading-text');
        const progressBar = document.getElementById('touring-progress-bar');
        const progressText = document.getElementById('touring-progress-text');
        const resultsContainer = document.getElementById('touring-results-container');
        const resultsCount = document.getElementById('touring-results-count');
        const resultsGrid = document.getElementById('touring-results-grid');
        const downloadAllBtn = document.getElementById('touring-download-all-btn');
        const bgInput = document.getElementById('touring-bg-input');
        const bgUploadArea = document.getElementById('touring-bg-upload-area');
        const bgPreviewContainer = document.getElementById('touring-bg-preview-container');
        const bgPreview = document.getElementById('touring-bg-preview');
        const removeBgBtn = document.getElementById('touring-remove-bg-btn');

        // State
        let photos = [null, null]; // Min 2 slots, max 5
        let photoMimeTypes = [null, null];
        let customBgBase64 = null;
        let customBgMimeType = null;
        let selectedRoad = 'Indonesian highway scenic route with mountains, tropical green landscape, smooth asphalt road';
        let selectedMood = 'Golden Hour sunset, warm orange golden light, dramatic sky, cinematic mood, magic hour photography';
        let selectedAngle = 'Side profile angle, rider and motorcycle from the side, classic motorcycle photography composition, showing full bike and rider silhouette';
        let selectedAspect = '16:9';
        let selectedCount = 4; // 1-10, default 4
        let generatedImages = [];
        let stopTouring = false;
        const touringStopBtn = document.getElementById('touring-stop-btn');
        if (touringStopBtn) touringStopBtn.addEventListener('click', () => { stopTouring = true; });

        // Count selection (1-10), default 4
        const touringCountSel = document.getElementById('touring-count-selection-grid');
        if (touringCountSel) {
            touringCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                touringCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Multiple File Input Handler
        function updateFileInputs() {
            if (!fileInputArea) return;
            fileInputArea.innerHTML = '';
            photos.forEach((photo, i) => {
                const wrapper = document.createElement('div');
                wrapper.className = 'file-input-wrapper';
                if (photo) wrapper.classList.add('has-photo');

                const input = document.createElement('input');
                input.type = 'file';
                input.accept = 'image/png, image/jpeg, image/webp, image/heic, .heic, .HEIC';
                input.className = 'file-input';
                input.dataset.index = i;
                input.addEventListener('change', handleFileChange);

                const preview = document.createElement('img');
                preview.className = 'file-preview';
                if (photo) {
                    preview.src = `data:${photoMimeTypes[i]};base64,${photo}`;
                } else {
                    preview.classList.add('hidden');
                }

                const placeholderEl = document.createElement('span');
                placeholderEl.className = 'text-gray-500 text-center text-sm flex items-center justify-center h-full pointer-events-none';
                if (!photo) {
                    placeholderEl.innerHTML = '<div><i class="fas fa-motorcycle text-2xl mb-2" style="color: #f97316;"></i><br><span class="text-xs sm:text-sm">Tambah Foto</span></div>';
                }

                const deleteBtn = document.createElement('button');
                deleteBtn.className = 'delete-btn';
                deleteBtn.innerHTML = '×';
                deleteBtn.type = 'button';

                // Mobile-friendly click handler
                deleteBtn.addEventListener('click', (e) => touringDeletePhoto(e, i));
                deleteBtn.addEventListener('touchstart', (e) => {
                    e.stopPropagation();
                    deleteBtn.style.transform = 'scale(0.9)';
                }, { passive: true });
                deleteBtn.addEventListener('touchend', (e) => {
                    e.stopPropagation();
                    deleteBtn.style.transform = '';
                }, { passive: true });

                wrapper.append(input, preview, placeholderEl, deleteBtn);
                fileInputArea.appendChild(wrapper);
            });
            updateValidation();
        }

        async function handleFileChange(e) {
            const index = parseInt(e.target.dataset.index, 10);
            const file = e.target.files[0];
            if (!file) return;

            // Show loading state
            const wrapper = e.target.closest('.file-input-wrapper');
            const placeholderEl = wrapper.querySelector('span');
            if (placeholderEl && !wrapper.classList.contains('has-photo')) {
                placeholderEl.innerHTML = '<div><i class="fas fa-spinner fa-spin text-2xl mb-2" style="color: #f97316;"></i><br><span class="text-xs">Loading...</span></div>';
            }

            let processedFile = file;

            // Convert HEIC to JPEG if needed
            if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                try {
                    if (touringValidation) {
                        touringValidation.textContent = 'Converting HEIC...';
                        touringValidation.className = 'text-center text-sm mt-3 font-medium text-blue-600';
                    }

                    const convertedBlob = await heic2any({
                        blob: file,
                        toType: 'image/jpeg',
                        quality: 0.9
                    });
                    processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                        type: 'image/jpeg'
                    });
                } catch (err) {
                    console.error('HEIC conversion failed:', err);
                    if (touringValidation) {
                        touringValidation.textContent = '❌ Failed to convert HEIC';
                        touringValidation.className = 'text-center text-sm mt-3 font-medium text-red-600';
                    }
                    updateFileInputs();
                    return;
                }
            }

            const reader = new FileReader();
            reader.onload = (event) => {
                photos[index] = event.target.result.split(',')[1];
                photoMimeTypes[index] = processedFile.type;
                updateFileInputs();
            };
            reader.readAsDataURL(processedFile);
        }

        window.touringDeletePhoto = (event, index) => {
            event.preventDefault();
            event.stopPropagation();

            // Visual feedback
            const wrapper = event.target.closest('.file-input-wrapper');
            if (wrapper) {
                wrapper.style.transition = 'transform 0.2s, opacity 0.2s';
                wrapper.style.transform = 'scale(0.9)';
                wrapper.style.opacity = '0.5';
            }

            setTimeout(() => {
                photos[index] = null;
                photoMimeTypes[index] = null;
                // Only remove the slot if there are more than 2
                if (photos.length > 2) {
                    photos.splice(index, 1);
                    photoMimeTypes.splice(index, 1);
                }
                updateFileInputs();
            }, 150);
        };

        function updateValidation() {
            if (!touringValidation) return;
            const uploadedCount = photos.filter(p => p !== null).length;

            if (uploadedCount === 0) {
                touringValidation.textContent = 'Upload minimal 1 foto';
                touringValidation.className = 'text-center text-sm mt-3 font-medium text-gray-500';
                if (generateBtn) generateBtn.disabled = true;
            } else {
                touringValidation.textContent = `✓ ${uploadedCount} foto ready`;
                touringValidation.className = 'text-center text-sm mt-3 font-medium text-orange-600';
                if (generateBtn) generateBtn.disabled = false;
            }
        }

        if (addPhotoBtn) {
            addPhotoBtn.addEventListener('click', () => {
                if (photos.length < 5) {
                    photos.push(null);
                    photoMimeTypes.push(null);
                    updateFileInputs();

                    // Scroll to bottom on mobile
                    if (window.innerWidth < 640) {
                        setTimeout(() => {
                            const lastWrapper = fileInputArea.lastElementChild;
                            if (lastWrapper) {
                                lastWrapper.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
                            }
                        }, 100);
                    }
                } else {
                    if (touringValidation) {
                        touringValidation.textContent = '❌ Maksimal 5 foto';
                        touringValidation.className = 'text-center text-sm mt-3 font-medium text-red-600';
                        setTimeout(() => {
                            updateValidation();
                        }, 2000);
                    }
                }
            });

            // Mobile touch feedback
            addPhotoBtn.addEventListener('touchstart', () => {
                addPhotoBtn.style.transform = 'scale(0.95)';
            }, { passive: true });

            addPhotoBtn.addEventListener('touchend', () => {
                addPhotoBtn.style.transform = '';
            }, { passive: true });
        }

        // Road Background Selection
        if (roadOptions) {
            roadOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                roadOptions.querySelectorAll('.option-btn-touring').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedRoad = btn.dataset.value;
            });
        }

        // Mood Selection
        if (moodOptions) {
            moodOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                moodOptions.querySelectorAll('.option-btn-touring').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedMood = btn.dataset.value;
            });
        }

        // Angle Selection
        if (angleOptions) {
            angleOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                angleOptions.querySelectorAll('.option-btn-touring').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAngle = btn.dataset.value;
            });
        }

        // Aspect Ratio Selection
        if (aspectOptions) {
            aspectOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                aspectOptions.querySelectorAll('.option-btn-touring').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAspect = btn.dataset.value;
            });
        }

        // Custom Background Upload Handler
        if (bgInput) {
            bgInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                // Show loading state
                if (bgUploadArea) {
                    bgUploadArea.innerHTML = '<i class="fas fa-spinner fa-spin text-4xl" style="color: #f97316;"></i><p class="text-sm text-gray-500 mt-2">Loading...</p>';
                }

                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        // Reset upload area
                        if (bgUploadArea) {
                            bgUploadArea.innerHTML = '<i class="fas fa-mountain text-4xl mb-2" style="color: #f97316;"></i><p class="text-sm font-medium text-gray-700">Upload Background Sendiri</p><p class="text-xs text-gray-500 mt-1">Atau gunakan pilihan di atas</p>';
                        }
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onload = (event) => {
                    customBgBase64 = event.target.result.split(',')[1];
                    customBgMimeType = processedFile.type;

                    // Show preview
                    if (bgPreview) bgPreview.src = event.target.result;
                    if (bgUploadArea) bgUploadArea.classList.add('hidden');
                    if (bgPreviewContainer) bgPreviewContainer.classList.remove('hidden');

                    // Disable road selection when custom background is uploaded
                    if (roadOptions) {
                        roadOptions.querySelectorAll('.option-btn-touring').forEach(btn => {
                            btn.style.opacity = '0.5';
                            btn.style.pointerEvents = 'none';
                        });
                    }
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove Custom Background Handler
        if (removeBgBtn) {
            removeBgBtn.addEventListener('click', (e) => {
                e.preventDefault();
                e.stopPropagation();

                customBgBase64 = null;
                customBgMimeType = null;

                if (bgInput) bgInput.value = '';
                if (bgUploadArea) bgUploadArea.classList.remove('hidden');
                if (bgPreviewContainer) bgPreviewContainer.classList.add('hidden');
                if (bgPreview) bgPreview.src = '';

                // Re-enable road selection
                if (roadOptions) {
                    roadOptions.querySelectorAll('.option-btn-touring').forEach(btn => {
                        btn.style.opacity = '1';
                        btn.style.pointerEvents = 'auto';
                    });
                }
            });

            // Mobile touch feedback
            removeBgBtn.addEventListener('touchstart', (e) => {
                e.stopPropagation();
                removeBgBtn.style.transform = 'scale(0.9)';
            }, { passive: true });

            removeBgBtn.addEventListener('touchend', (e) => {
                e.stopPropagation();
                removeBgBtn.style.transform = '';
            }, { passive: true });
        }

        // Generate Touring Photos
        async function runTouringGeneration(uploadedFiles) {
            generatedImages = new Array(selectedCount).fill(null);
            if (resultsGrid) resultsGrid.innerHTML = '';

            // Create placeholder cards
            for (let i = 1; i <= selectedCount; i++) {
                const card = document.createElement('div');
                card.id = `touring-card-${i}`;
                card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-video';
                card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2" style="border-color: #f97316;"></div>';
                if (resultsGrid) resultsGrid.appendChild(card);
            }

            if (placeholder) placeholder.classList.add('hidden');
            if (loading) loading.classList.add('hidden');
            if (resultsContainer) resultsContainer.classList.remove('hidden');

            if (window.innerWidth < 1024) {
                setTimeout(() => {
                    if (resultsContainer) resultsContainer.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }, 300);
            }

            await Promise.allSettled(
                Array.from({ length: selectedCount }, (_, i) => generateSingleTouringPhoto(i + 1, uploadedFiles))
            );
        }

        async function generateTouringPhotos() {
            if (generateBtn && generateBtn.disabled) return;
            const uploadedFiles = photos.map((p, i) => ({ base64: p, mimeType: photoMimeTypes[i] })).filter(f => f.base64);

            if (uploadedFiles.length === 0) {
                alert('Upload minimal 1 foto terlebih dahulu!');
                return;
            }

            stopTouring = false;
            if (generateBtn) { generateBtn.disabled = true; generateBtn.classList.add('hidden'); }
            if (touringStopBtn) touringStopBtn.classList.remove('hidden');

            try {
                // First attempt
                await runTouringGeneration(uploadedFiles);

                let successCount = generatedImages.filter(img => img).length;

                // Auto-retry once if all failed and not stopped
                if (successCount === 0 && !stopTouring) {
                    await runTouringGeneration(uploadedFiles);
                }

                renderSuccessfulTouringResults();
            } finally {
                if (generateBtn) { generateBtn.disabled = false; generateBtn.classList.remove('hidden'); }
                if (touringStopBtn) touringStopBtn.classList.add('hidden');
            }
        }

        function renderSuccessfulTouringResults() {
            const successful = generatedImages.filter(img => img);

            // Re-index generatedImages to only successful ones
            generatedImages = successful;

            if (resultsGrid) resultsGrid.innerHTML = '';

            if (successful.length === 0) {
                if (resultsContainer) resultsContainer.classList.add('hidden');
                if (placeholder) placeholder.classList.remove('hidden');
                alert('Tidak ada foto yang berhasil digenerate. Silakan coba lagi.');
                return;
            }

            successful.forEach((imgData, idx) => {
                const card = document.createElement('div');
                card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 aspect-video';
                card.innerHTML = `
                    <img src="${imgData.url}" class="w-full h-full object-cover" alt="Touring Photo ${idx + 1}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${idx}" class="touring-action-btn bg-white/90 hover:bg-white text-gray-800">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${idx}" class="touring-action-btn text-white" style="background: linear-gradient(135deg, #f97316, #dc2626);">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
                if (resultsGrid) resultsGrid.appendChild(card);
            });

            if (resultsCount) resultsCount.textContent = successful.length;
            if (resultsContainer) resultsContainer.classList.remove('hidden');

            if (window.innerWidth < 1024) {
                setTimeout(() => {
                    if (resultsContainer) resultsContainer.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }, 300);
            }
        }

        // Generate Single Touring Photo
        async function generateSingleTouringPhoto(index, uploadedFiles) {
            if (stopTouring) return;
            const card = document.getElementById(`touring-card-${index}`);

            try {

                // Build detailed prompt for touring photo composite
                const numPhotos = uploadedFiles.length;
                let imageDescriptions = [];

                for (let i = 0; i < numPhotos; i++) {
                    imageDescriptions.push(`IMAGE ${i + 1}`);
                }

                // Add custom background to description if uploaded
                let bgDescription = '';
                if (customBgBase64) {
                    imageDescriptions.push(`IMAGE ${numPhotos + 1}`);
                    bgDescription = `IMAGE ${numPhotos + 1} = CUSTOM BACKGROUND/LOCATION (use this as the background setting)\n\n`;
                }

                const prompt = `Create an EPIC professional touring motorcycle photo by combining these ${customBgBase64 ? numPhotos + 1 : numPhotos} separate images:

${imageDescriptions.map((desc, i) => {
    if (customBgBase64 && i === numPhotos) {
        return `${desc} = CUSTOM BACKGROUND/LOCATION (use as backdrop)`;
    }
    return `${desc} = ${i === 0 ? 'MODEL/RIDER (main subject)' : i === 1 ? 'MOTORCYCLE (vehicle)' : i === 2 ? 'HELMET (gear)' : i === 3 ? 'JACKET/RIDING GEAR' : 'ACCESSORY/EQUIPMENT'}`;
}).join('\n')}

COMPOSITING REQUIREMENTS:
- The rider MUST be wearing/using ALL the uploaded items naturally
- Position rider ON the motorcycle in realistic riding position
- Match helmet, jacket, and all accessories to the rider
- Ensure all gear looks naturally worn and integrated

BACKGROUND & SETTING: ${customBgBase64 ? `USE IMAGE ${numPhotos + 1} as the background location (Custom uploaded background)` : selectedRoad}
- ${customBgBase64 ? 'Integrate rider and motorcycle naturally into the custom background scene' : 'Epic scenic road environment'}
- Professional location photography quality
- Natural integration of subject with environment
- ${customBgBase64 ? 'Match lighting and perspective perfectly with the background image' : 'Realistic depth and atmosphere'}

LIGHTING & MOOD: ${selectedMood}
- Cinematic photography lighting
- Dramatic atmosphere matching the time of day
- Professional color grading and tone

CAMERA ANGLE & COMPOSITION: ${selectedAngle}
- Professional motorcycle photography framing
- Dynamic composition following photography best practices
- Capture the essence of adventure and freedom

PHOTOGRAPHY STYLE:
- Shot with professional DSLR camera (Canon EOS R5, Sony A7R IV equivalent)
- Shallow depth of field (f/2.8 - f/4)
- Motion blur on wheels suggesting speed (if dynamic shot)
- Sharp focus on rider and motorcycle
- Epic composition with rule of thirds
- Magazine cover quality

TECHNICAL REQUIREMENTS:
- Keep rider's face/identity EXACTLY from IMAGE 1
- Motorcycle MUST be EXACTLY from IMAGE 2 (same model, color, details)
${numPhotos >= 3 ? '- Helmet MUST match EXACTLY from IMAGE 3' : ''}
${numPhotos >= 4 ? '- Riding gear/jacket MUST match from IMAGE 4' : ''}
${numPhotos >= 5 ? '- Include accessory/equipment from IMAGE 5 naturally' : ''}
- Photorealistic compositing with perfect lighting match
- Natural shadows and reflections
- Professional motorcycle photography aesthetic
- NO watermarks, NO text overlays
- High resolution, print-ready quality

FINAL OUTPUT: An Instagram-worthy, magazine-quality touring photo that looks like it was shot by a professional motorcycle photographer on an epic adventure! 🏍️✨`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payloadParts = [{ text: prompt }];

                // Add all uploaded photos
                uploadedFiles.forEach(file => {
                    payloadParts.push({ inlineData: { mimeType: file.mimeType, data: file.base64 } });
                });

                // Add custom background if uploaded
                if (customBgBase64 && customBgMimeType) {
                    payloadParts.push({ inlineData: { mimeType: customBgMimeType, data: customBgBase64 } });
                }

                const payload = {
                    contents: [{
                        parts: payloadParts
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedAspect }
                    },
                    safetySettings: [
                        { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" }
                    ]
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (!imageData) {
                    throw new Error('No image data received');
                }

                const imageUrl = `data:image/png;base64,${imageData}`;
                const filename = `touring-epic-${Date.now()}-${index}.png`;

                generatedImages[index - 1] = { url: imageUrl, filename };

                // Show image inline immediately (buttons added by renderSuccessfulTouringResults at the end)
                if (card) {
                    card.innerHTML = `<img src="${imageUrl}" class="w-full h-full object-cover" alt="Touring Photo ${index}">`;
                }

            } catch (error) {
                console.error(`Error generating photo ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event Delegation for Action Buttons (Mobile-Friendly)
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download All Handler
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        // Generate Button Click Handler
        if (generateBtn) {
            generateBtn.addEventListener('click', generateTouringPhotos);
        }

        // Initialize file inputs on page load
        updateFileInputs();

    })();

    // --- POV TANGAN TAB LOGIC ---
    // Direct initialization like Try-On (no lazy loading)
    (function initPOVTangan() {
            // DOM Elements
            const imageInput = document.getElementById('pov-image-input');
            const uploadArea = document.getElementById('pov-upload-area');
            const uploadPlaceholder = document.getElementById('pov-upload-placeholder');
            const previewContainer = document.getElementById('pov-preview-container');
            const previewImage = document.getElementById('pov-preview-image');
            const productDescription = document.getElementById('pov-product-description');
            const generateBtn = document.getElementById('pov-generate-btn');
            const loadingSection = document.getElementById('pov-loading-section');
            const progressText = document.getElementById('pov-progress-text');
            const progressBar = document.getElementById('pov-progress-bar');
            const resultsSection = document.getElementById('pov-results-section');
            const resultCount = document.getElementById('pov-result-count');
            const resultsGrid = document.getElementById('pov-results-grid');
            const downloadAllBtn = document.getElementById('pov-download-all-btn');
            const errorSection = document.getElementById('pov-error-section');
            const errorMessage = document.getElementById('pov-error-message');
            const retryBtn = document.getElementById('pov-retry-btn');
            const povEmptyState = document.getElementById('pov-empty-state');

            // Custom Background Elements (NEW)
            const bgInput = document.getElementById('pov-bg-input');
            const bgUploadArea = document.getElementById('pov-bg-upload-area');
            const bgPlaceholder = document.getElementById('pov-bg-placeholder');
            const bgPreviewContainer = document.getElementById('pov-bg-preview-container');
            const bgPreviewImage = document.getElementById('pov-bg-preview-image');
            const bgRemoveBtn = document.getElementById('pov-bg-remove-btn');

            let uploadedImageData = null;
            let uploadedBackgroundData = null;
            let backgroundStyleDescription = null;
            let generatedImages = [];
            let generatedImageParams = []; // Track parameters for each image
            let selectedPovRatio = '16:9';
            let selectedHandCount = '1'; // Default 1 tangan
            let selectedThemeCategory = 'random'; // Kategori tema background, default acak semua
            let currentThemesToUse = []; // Store current themes for regeneration
            let stopPov = false;
            const povStopBtn = document.getElementById('pov-stop-btn');
            if (povStopBtn) povStopBtn.addEventListener('click', () => { stopPov = true; });

            // Count selection (1-10), default 4
            let selectedCount = 4;
            const countSelectionPov = document.getElementById('pov-count-selection-grid');
            if (countSelectionPov) {
                countSelectionPov.addEventListener('click', (e) => {
                    const btn = e.target.closest('button[data-count]');
                    if (!btn) return;
                    countSelectionPov.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    selectedCount = parseInt(btn.dataset.count, 10);
                });
            }

            // POV Themes per kategori - dipilih acak (shuffle) tiap generate supaya tidak monoton
            const povThemeCategories = {
                feminin: [
                    "POV from above showing a hand holding the product on a soft pink aesthetic study desk with white pegboard wall, cute stationery cups, heart-shaped mirror, soft yellow lamp, tidy layout, cozy lighting, realistic 4k photo, shallow depth of field",
                    "Top-down angle at 40 degrees showing a hand holding the product on a pastel desk setup with tulip vase, soft desk mat, washi tapes, and white lamp, warm daylight, feminine cozy mood, ultra-detailed lifestyle photo",
                    "POV showing a hand delicately holding the product above a pastel pink workspace with white pegboard full of stationery cups, washi tapes, mini photos, heart mirror, pink lamp, tulip vase, natural lighting, cozy feminine tone",
                    "Standing POV of a hand holding the product above a lavender and white aesthetic desk with pegboard full of washi tapes, small artworks, headphones, notebooks, vases with tulips, clean daylight photo",
                    "Overhead hand holding the product above a Gen-Z aesthetic desk with retro Y2K accessories, holographic accents, colorful stickers, playful vibrant energy",
                    "POV hand holding the product above a soft peach vanity with heart-shaped LED mirror, makeup brushes, pink roses in a vase, dreamy soft-focus lighting, romantic feminine aesthetic"
                ],
                minimal: [
                    "Slightly top-view of a hand holding the product over a mint green and white-themed desk filled with plants, pastel organizers, hanging notes, pen holders, bright daylight tone, fresh and cozy look",
                    "Hand holding the product in POV angle above a white marble desk with gold accents, minimalist aesthetic, soft natural window light, elegant and clean composition",
                    "POV hand holding the product above a minimal Japanese wabi-sabi setup with bamboo tray, linen cloth, ceramic cup, zen garden stones, soft neutral tones",
                    "Top-down hand holding the product above a clean white marble surface with fresh flower petals, eucalyptus sprigs, minimal elegant composition, natural daylight",
                    "Top-down hand holding the product above a pure white seamless background with soft natural shadow, clean e-commerce product photography, bright even lighting",
                    "POV hand holding the product above a beige linen tablecloth with one ceramic plate and neutral tones, airy natural window light, calm minimalist flat lay"
                ],
                cafe: [
                    "Elevated angle hand holding the product above a sunlit outdoor cafe table with a latte, croissant, flowers, natural shadow play, warm lifestyle photography",
                    "POV hand holding the product above a wooden coffee shop table with latte art cup and an open book, warm ambient cafe lighting, cozy coffeehouse atmosphere",
                    "POV hand holding the product above a marble bakery counter with fresh croissants and pastries in the background, warm morning light, artisan bakery vibe",
                    "POV hand holding the product above a rustic Indonesian coffee stall table with a glass of traditional kopi tubruk and fried bananas on a plate, warm afternoon light, authentic local warung atmosphere",
                    "Top-down hand holding the product above a bright brunch table with avocado toast, orange juice, fresh flowers, crisp daylight, fresh lifestyle food photography",
                    "POV hand holding the product above a pastel dessert cafe table with a strawberry cake slice and milkshake, playful sweet aesthetic, soft daylight"
                ],
                nature: [
                    "POV of a hand holding the product over a wooden rustic desk with green plants, terracotta pots, natural fiber mat, warm earthy tones, bohemian aesthetic",
                    "POV hand holding the product above a beach-side flat lay with white sand, tropical shells, sunglasses, golden hour warm light, summer lifestyle vibe",
                    "Slightly above angle hand holding the product over a vibrant tropical Bali-inspired setup with tropical leaves, rattan mat, warm sunlight, lush green aesthetic",
                    "POV hand holding the product above a picnic blanket on green grass with a wicker basket, wildflowers, dappled sunlight through trees, relaxed outdoor picnic mood",
                    "POV hand holding the product with a misty mountain valley and pine forest in the background, golden sunrise light, adventurous outdoor travel vibe",
                    "POV hand holding the product surrounded by lush tropical garden leaves with soft bokeh, fresh morning dew, vibrant natural green tones"
                ],
                urban: [
                    "POV hand holding the product on a busy city street with blurred pedestrians and shop signs in the background, natural daylight, candid urban lifestyle photography",
                    "POV hand holding the product at night with colorful neon signs and city lights bokeh in the background, cinematic night street photography, vibrant urban glow",
                    "POV hand holding the product on a rooftop with a city skyline at golden hour in the background, warm sunset tones, modern urban lifestyle",
                    "POV hand holding the product against an industrial brick wall with metal accents and dramatic side lighting, edgy streetwear aesthetic",
                    "POV hand holding the product in a motorcycle garage with tools and a sport bike blurred in the background, workshop lighting, masculine automotive vibe",
                    "POV hand holding the product at an outdoor basketball court with painted asphalt and chain fence background, bright afternoon sun, sporty street style"
                ],
                luxury: [
                    "Top-down hand holding the product above a sleek black and gold modern desk with metallic pen holder, keyboard, neon accent light, dark luxury aesthetic, 4K photo",
                    "POV hand holding the product above a luxury cream and champagne vanity table with perfume bottles, pearls, soft studio lighting, elegant fashion aesthetic",
                    "Top-view hand holding the product above a dark moody library desk with stacked books, candle flame, aged wood surface, vintage sophisticated atmosphere",
                    "POV hand holding the product in a luxury hotel suite with silk bedding and warm golden lamp light, five-star elegant atmosphere, rich cinematic tones",
                    "Top-down hand holding the product above a black marble surface with gold veins, dramatic spotlight, premium dark luxury product photography",
                    "POV hand holding the product above deep emerald velvet fabric with soft glints of jewelry, moody elegant lighting, high-end editorial style"
                ],
                home: [
                    "Overhead POV hand holding the product above a terracotta and beige neutral-toned desk with lit candles, dried flowers arrangement, linen cloth, warm cozy atmosphere, lifestyle product photography",
                    "POV hand holding the product above a cozy bedroom aesthetic setup with fairy lights, soft pastel pillows, knit blanket, warm golden hour lighting, dreamy feminine vibe",
                    "POV hand holding the product above a cozy autumn desk with warm orange and brown tones, mini pumpkins, maple leaves, scented candle, cozy fall atmosphere",
                    "POV hand holding the product above a bright home kitchen counter with a wooden cutting board and fresh herbs, morning light through the window, homey cooking atmosphere",
                    "POV hand holding the product in a cozy living room with a knit blanket sofa, warm string lights, indoor plants, soft evening ambiance",
                    "POV hand holding the product on an apartment balcony table with morning coffee and small plants, soft sunrise light, calm morning routine vibe"
                ],
                tech: [
                    "POV hand holding the product above a gaming desk with RGB mechanical keyboard and monitor glow in the background, purple and blue LED lighting, tech gamer aesthetic",
                    "POV hand holding the product above a clean tech workspace with laptop, wireless earbuds, smartphone, neutral desk mat, bright productive daylight",
                    "POV hand holding the product in a photography studio with softbox lighting and a grey backdrop, professional commercial product shot",
                    "POV hand holding the product in a podcast studio with a microphone and acoustic foam panels in the background, warm accent lighting, content creator vibe",
                    "POV hand holding the product above a content creator desk with ring light, camera, and editing monitor in the background, modern creator studio setup",
                    "Top-down hand holding the product above a dark desk with holographic blue light reflections and sleek gadgets, futuristic tech aesthetic"
                ]
            };
            const povThemes = Object.values(povThemeCategories).flat();

            function shufflePovThemes(arr) {
                const copy = [...arr];
                for (let i = copy.length - 1; i > 0; i--) {
                    const j = Math.floor(Math.random() * (i + 1));
                    [copy[i], copy[j]] = [copy[j], copy[i]];
                }
                return copy;
            }

            // Event Listeners - Using Try-On pattern
            // Main product image upload
            if (uploadArea && imageInput) {
                uploadArea.addEventListener('click', () => imageInput.click());
                uploadArea.addEventListener('dragover', (e) => { e.preventDefault(); uploadArea.style.borderColor = '#764ba2'; });
                uploadArea.addEventListener('dragleave', () => uploadArea.style.borderColor = '');
                uploadArea.addEventListener('drop', (e) => {
                    e.preventDefault();
                    uploadArea.style.borderColor = '';
                    if (e.dataTransfer.files[0]) processImage(e.dataTransfer.files[0]);
                });
                imageInput.addEventListener('change', (e) => { if (e.target.files[0]) processImage(e.target.files[0]); });
            }

            // Background image upload
            if (bgUploadArea && bgInput) {
                bgUploadArea.addEventListener('click', () => bgInput.click());
                bgUploadArea.addEventListener('dragover', (e) => { e.preventDefault(); bgUploadArea.style.borderColor = '#764ba2'; });
                bgUploadArea.addEventListener('dragleave', () => bgUploadArea.style.borderColor = '');
                bgUploadArea.addEventListener('drop', (e) => {
                    e.preventDefault();
                    bgUploadArea.style.borderColor = '';
                    if (e.dataTransfer.files[0]) processBgImage(e.dataTransfer.files[0]);
                });
                bgInput.addEventListener('change', (e) => { if (e.target.files[0]) processBgImage(e.target.files[0]); });
            }

            bgRemoveBtn.addEventListener('click', removeBackgroundImage);
            generateBtn.addEventListener('click', startGeneration);
            retryBtn.addEventListener('click', resetForm);

            // Ratio selection event listener
            const ratioSelection = document.getElementById('pov-ratio-selection');
            if (ratioSelection) {
                ratioSelection.addEventListener('click', (e) => {
                    const button = e.target.closest('.ratio-btn-pov');
                    if (button) {
                        document.querySelectorAll('#pov-ratio-selection .ratio-btn-pov').forEach(btn => btn.classList.remove('selected'));
                        button.classList.add('selected');
                        selectedPovRatio = button.dataset.ratio;
                    }
                });
            }

            // Hand count selection event listener
            const handCountSelection = document.getElementById('pov-hand-count-selection');
            if (handCountSelection) {
                handCountSelection.addEventListener('click', (e) => {
                    e.stopPropagation();
                    const button = e.target.closest('.hand-count-btn-pov');
                    if (button) {
                        document.querySelectorAll('#pov-hand-count-selection .hand-count-btn-pov').forEach(btn => btn.classList.remove('selected'));
                        button.classList.add('selected');
                        selectedHandCount = button.dataset.handCount;
                    }
                });
            }

            // Theme category selection event listener
            const themeCatSelection = document.getElementById('pov-theme-selection');
            if (themeCatSelection) {
                themeCatSelection.addEventListener('click', (e) => {
                    const button = e.target.closest('.theme-cat-btn-pov');
                    if (button) {
                        themeCatSelection.querySelectorAll('.theme-cat-btn-pov').forEach(btn => btn.classList.remove('selected'));
                        button.classList.add('selected');
                        selectedThemeCategory = button.dataset.themeCat;
                    }
                });
            }

            function processImage(file) {
                if (file.size > 10 * 1024 * 1024) {
                    showError('Ukuran file terlalu besar. Maksimal 10MB.');
                    return;
                }

                const reader = new FileReader();
                reader.onload = (e) => {
                    uploadedImageData = e.target.result;
                    previewImage.src = uploadedImageData;
                    uploadPlaceholder.classList.add('hidden');
                    previewContainer.classList.remove('hidden');
                    uploadArea.classList.add('has-image');
                    generateBtn.disabled = false;
                    hideError();
                };
                reader.readAsDataURL(file);
            }

            // Custom Background Handlers (NEW)
            async function processBgImage(file) {
                if (file.size > 10 * 1024 * 1024) {
                    showError('Ukuran file background terlalu besar. Maksimal 10MB.');
                    return;
                }

                const reader = new FileReader();
                reader.onload = (e) => {
                    uploadedBackgroundData = e.target.result;
                    bgPreviewImage.src = uploadedBackgroundData;
                    bgPlaceholder.classList.add('hidden');
                    bgPreviewContainer.classList.remove('hidden');
                    bgUploadArea.classList.add('has-image');

                    // No need to analyze, we'll use the image directly
                    backgroundStyleDescription = 'custom'; // Flag that custom bg is present
                };
                reader.readAsDataURL(file);
            }

            function removeBackgroundImage(e) {
                e.stopPropagation();
                uploadedBackgroundData = null;
                backgroundStyleDescription = null;
                bgPreviewImage.src = '';
                bgPlaceholder.classList.remove('hidden');
                bgPreviewContainer.classList.add('hidden');
                bgUploadArea.classList.remove('has-image');
                bgInput.value = '';
            }

            // Retry mechanism sama seperti Miniature Me
            async function generateImageWithRetry(payload, retries = 3, delay = 1000) {
                for (let i = 0; i < retries; i++) {
                    try {
                        const apiKey = "";
                        const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                        const response = await fetch(apiUrl, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify(payload)
                        });
                        const responseText = await response.text();

                        if (!response.ok) {
                            let errorMsg = response.statusText;
                            try {
                                errorMsg = JSON.parse(responseText).error?.message || errorMsg;
                            } catch(e) {
                                if(responseText) errorMsg = responseText;
                            }
                            throw new Error(`API Error: ${errorMsg}`);
                        }

                        try {
                            return JSON.parse(responseText);
                        } catch (e) {
                            throw new Error("Invalid JSON response.");
                        }
                    } catch (error) {
                        console.error(`Attempt ${i + 1} failed:`, error);
                        if (i === retries - 1) return null;
                        await new Promise(res => setTimeout(res, delay * Math.pow(2, i)));
                    }
                }
                return null;
            }

            async function runPovGeneration(themesToUse, productDesc, base64Image) {
                generatedImages = new Array(selectedCount).fill(null);
                generatedImageParams = new Array(selectedCount).fill(null);

                // Pre-create placeholder cards
                resultsGrid.innerHTML = '';
                for (let i = 0; i < selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `pov-card-${i}`;
                    card.className = 'result-card';
                    card.innerHTML = `<div class="flex flex-col items-center justify-center h-full min-h-[200px]"><div class="loader !border-l-purple-500"></div></div>`;
                    resultsGrid.appendChild(card);
                }
                if (povEmptyState) povEmptyState.classList.add('hidden');
                resultsSection.classList.remove('hidden');
                resultCount.textContent = '...';
                downloadAllBtn.classList.add('hidden');

                if (window.innerWidth < 1024) {
                    setTimeout(() => resultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' }), 300);
                }

                await Promise.allSettled(
                    Array.from({ length: selectedCount }, (_, i) => generateSinglePov(i, themesToUse, productDesc, base64Image))
                );
            }

            async function startGeneration() {
                if (generateBtn.disabled) return;
                if (!uploadedImageData) {
                    showError('Silakan upload foto produk terlebih dahulu.');
                    return;
                }

                stopPov = false;
                hideError();
                loadingSection.classList.add('hidden');
                generateBtn.disabled = true;
                generateBtn.classList.add('hidden');
                if (povStopBtn) povStopBtn.classList.remove('hidden');
                currentThemesToUse = [];

                const productDesc = productDescription.value.trim();
                const base64Image = uploadedImageData.split(',')[1];

                let themesToUse;
                if (backgroundStyleDescription && uploadedBackgroundData) {
                    themesToUse = shufflePovThemes([
                        'POV from above', 'top-down view at 45 degrees', 'elevated overhead angle',
                        'standing POV perspective', 'slightly raised view', 'bird eye perspective',
                        'high angle shot', 'diagonal overhead angle', 'close-up POV angle',
                        'low elevated perspective', 'angled side view from above', 'gentle tilt overhead shot',
                        'wide overhead perspective', 'narrow elevated angle', 'dramatic top-down angle',
                        'relaxed overhead view', 'natural hand POV angle', 'first-person perspective',
                        'over-the-shoulder POV angle', 'mid-range elevated angle'
                    ]);
                } else {
                    const themePool = povThemeCategories[selectedThemeCategory] || povThemes;
                    let shuffledThemes = shufflePovThemes(themePool);
                    while (shuffledThemes.length < selectedCount) {
                        shuffledThemes = shuffledThemes.concat(shufflePovThemes(themePool));
                    }
                    themesToUse = shuffledThemes;
                }
                currentThemesToUse = [...themesToUse];

                try {
                    // First attempt
                    await runPovGeneration(themesToUse, productDesc, base64Image);

                    let successCount = generatedImages.filter(img => img).length;

                    // Auto-retry once if all failed and not stopped
                    if (successCount === 0 && !stopPov) {
                        await runPovGeneration(themesToUse, productDesc, base64Image);
                    }

                    renderSuccessfulPovResults();
                } finally {
                    generateBtn.disabled = false;
                    generateBtn.classList.remove('hidden');
                    if (povStopBtn) povStopBtn.classList.add('hidden');
                }
            }

            function renderSuccessfulPovResults() {
                const successfulImages = [];
                const successfulParams = [];
                for (let i = 0; i < generatedImages.length; i++) {
                    if (generatedImages[i]) {
                        successfulImages.push(generatedImages[i]);
                        successfulParams.push(generatedImageParams[i]);
                    }
                }

                generatedImages = successfulImages;
                generatedImageParams = successfulParams;
                currentThemesToUse = successfulParams.map(p => p?.theme || '');

                resultsGrid.innerHTML = '';

                if (successfulImages.length === 0) {
                    resultsSection.classList.add('hidden');
                    if (povEmptyState) povEmptyState.classList.remove('hidden');
                    showError('Tidak ada gambar yang berhasil digenerate. Silakan coba lagi.');
                    return;
                }

                successfulImages.forEach((imageUrl, idx) => {
                    const params = successfulParams[idx];
                    const safeTheme = (params?.theme || '').replace(/"/g, '&quot;');
                    const card = document.createElement('div');
                    card.className = 'result-card';
                    card.innerHTML = `
                        <img src="${imageUrl}" alt="POV Tangan ${idx + 1}" />
                        <div class="result-card-actions">
                            <button class="btn-preview" data-action="preview" data-image-url="${imageUrl}">
                                <i class="fas fa-search-plus"></i>
                                Preview
                            </button>
                            <button class="btn-edit" data-action="edit" data-index="${idx}" data-theme="${safeTheme}">
                                <i class="fas fa-edit"></i>
                                Edit
                            </button>
                            <button class="btn-regenerate" data-action="regenerate" data-index="${idx}">
                                <i class="fas fa-sync-alt"></i>
                                Regenerate
                            </button>
                            <button class="btn-download" data-action="download" data-image-url="${imageUrl}" data-filename="pov-tangan-${idx + 1}.jpg">
                                <i class="fas fa-download"></i>
                                Download
                            </button>
                        </div>
                        <div class="image-counter">#${idx + 1}</div>
                    `;
                    resultsGrid.appendChild(card);
                });

                resultCount.textContent = successfulImages.length;
                downloadAllBtn.classList.remove('hidden');

                if (window.innerWidth < 1024) {
                    resultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            }

            async function generateSinglePov(index, themesToUse, productDesc, base64Image) {
                if (stopPov) return;
                const card = document.getElementById(`pov-card-${index}`);
                if (!card) return;

                const theme = themesToUse[index];
                let prompt = '';
                let payloadParts = [];

                if (backgroundStyleDescription && uploadedBackgroundData) {
                    const base64Background = uploadedBackgroundData.split(',')[1];
                    const handInstruction = selectedHandCount === '2' ? 'both hands (two hands)' : 'one hand';

                    prompt = `Create a composite product photography image by combining these elements:

IMAGE 1 (Background Environment): Use this as the base scene/location
IMAGE 2 (Product): The product to be placed in the scene

Task: Place the product from IMAGE 2 into the environment from IMAGE 1, with ${handInstruction} naturally holding it.

Requirements:
- Camera angle: ${theme}
- Use EXACTLY ${handInstruction} holding the product naturally${productDesc ? `, product is: ${productDesc}` : ''}
- ${selectedHandCount === '2' ? 'Show BOTH hands clearly visible holding/supporting the product from different sides' : 'Show ONE hand only holding the product'}
- The background/environment MUST be from IMAGE 1 (same location, same setting)
- The product MUST be from IMAGE 2
- Blend lighting and colors so product fits naturally in the environment
- Make it look like a real photo taken in that location
- Hand(s) should match the scene lighting
- Professional 4K quality, realistic composition

IMPORTANT: Use the EXACT environment/location from IMAGE 1 as the background. Do not create a different background.`;

                    payloadParts = [
                        { text: prompt },
                        { inlineData: { mimeType: "image/jpeg", data: base64Background } },
                        { inlineData: { mimeType: "image/jpeg", data: base64Image } }
                    ];
                } else {
                    const handInstruction = selectedHandCount === '2' ? 'both hands (two hands)' : 'one hand';
                    const handDescription = selectedHandCount === '2' ? 'BOTH hands clearly visible holding/supporting the product from different sides' : 'ONE hand only holding the product';

                    prompt = `POV product photography: ${handInstruction} holding product. `;
                    if (productDesc) {
                        prompt += `Background setting: ${productDesc}. `;
                    } else {
                        prompt += `${theme}. `;
                    }
                    prompt += `Use EXACTLY ${handInstruction}. ${handDescription}. Natural hand pose, product clearly visible, professional 4K quality, realistic lighting and composition.`;

                    payloadParts = [
                        { text: prompt },
                        { inlineData: { mimeType: "image/jpeg", data: base64Image } }
                    ];
                }

                const payload = {
                    contents: [{ parts: payloadParts }],
                    generationConfig: {
                        responseModalities: ['IMAGE'],
                        imageConfig: { aspectRatio: selectedPovRatio }
                    }
                };

                const result = await generateImageWithRetry(payload);
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (base64Data) {
                    const imageUrl = `data:image/png;base64,${base64Data}`;
                    generatedImages[index] = imageUrl;
                    generatedImageParams[index] = {
                        theme,
                        productData: uploadedImageData,
                        backgroundData: uploadedBackgroundData,
                        backgroundDesc: backgroundStyleDescription,
                        productDesc,
                        ratio: selectedPovRatio,
                        handCount: selectedHandCount
                    };
                    card.innerHTML = `<img src="${imageUrl}" alt="POV Tangan" style="width:100%;height:100%;object-fit:cover;">`;
                } else {
                    card.innerHTML = '';
                }
            }

            // Function to handle edit POV
            function handleEditPov(index, currentTheme) {
                const modal = document.createElement('div');
                modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full">
                        <h3 class="text-xl font-bold mb-4 text-gray-800">
                            <i class="fas fa-edit text-purple-500 mr-2"></i>Edit POV Theme/Angle
                        </h3>
                        <div class="mb-4">
                            <label class="block text-sm font-semibold text-gray-700 mb-2">Theme/Camera Angle:</label>
                            <textarea id="edit-pov-input" rows="4" class="w-full p-3 border-2 border-gray-300 rounded-lg focus:border-purple-500 focus:outline-none resize-none" placeholder="Contoh: POV from above on pink desk with stationery, top-down view on aesthetic workspace, etc...">${currentTheme}</textarea>
                            <p class="text-xs text-gray-500 mt-2">
                                <i class="fas fa-info-circle mr-1"></i>
                                Deskripsi angle/perspektif dan background yang diinginkan
                            </p>
                        </div>
                        <div class="flex gap-2">
                            <button onclick="this.closest('.fixed').remove()" class="flex-1 bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                                Batal
                            </button>
                            <button id="confirm-edit-pov-btn" class="flex-1 bg-purple-500 hover:bg-purple-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                                <i class="fas fa-check mr-2"></i>Generate Ulang
                            </button>
                        </div>
                    </div>
                `;
                document.body.appendChild(modal);

                const confirmBtn = modal.querySelector('#confirm-edit-pov-btn');
                const themeInput = modal.querySelector('#edit-pov-input');

                confirmBtn.addEventListener('click', async () => {
                    const newTheme = themeInput.value.trim();
                    if (!newTheme) return;

                    modal.remove();

                    // Update the theme for this image
                    generatedImageParams[index].theme = newTheme;
                    currentThemesToUse[index] = newTheme;

                    await handleRegeneratePov(index);
                });
            }

            // Function to handle regenerate POV
            async function handleRegeneratePov(index) {
                const resultCards = resultsGrid.querySelectorAll('.result-card');
                const targetCard = resultCards[index];
                if (!targetCard) return;

                const params = generatedImageParams[index];
                if (!params) return;

                // Show loading state
                targetCard.innerHTML = `
                    <div class="flex flex-col items-center justify-center h-full min-h-[200px]">
                        <div class="loader !border-l-purple-500"></div>
                        <p class="mt-4 text-sm text-gray-600">Regenerating...</p>
                    </div>
                `;

                try {
                    const base64Product = params.productData.split(',')[1];
                    let prompt = '';
                    let payloadParts = [];

                    if (params.backgroundDesc && params.backgroundData) {
                        const base64Background = params.backgroundData.split(',')[1];
                        prompt = `Create a composite product photography image by combining these elements:

IMAGE 1 (Background Environment): Use this as the base scene/location
IMAGE 2 (Product): The product to be placed in the scene

Task: Place the product from IMAGE 2 into the environment from IMAGE 1, with a hand naturally holding it.

Requirements:
- Camera angle: ${params.theme}
- Hand holding the product naturally${params.productDesc ? `, product is: ${params.productDesc}` : ''}
- The background/environment MUST be from IMAGE 1 (same location, same setting)
- The product MUST be from IMAGE 2
- Blend lighting and colors so product fits naturally in the environment
- Make it look like a real photo taken in that location
- Hand should match the scene lighting
- Professional 4K quality, realistic composition

IMPORTANT: Use the EXACT environment/location from IMAGE 1 as the background. Do not create a different background.`;

                        payloadParts = [
                            { text: prompt },
                            { inlineData: { mimeType: "image/jpeg", data: base64Background } },
                            { inlineData: { mimeType: "image/jpeg", data: base64Product } }
                        ];
                    } else {
                        prompt = `POV product photography: hand holding product. `;

                        if (params.productDesc) {
                            prompt += `Background setting: ${params.productDesc}. `;
                        } else {
                            prompt += `${params.theme}. `;
                        }

                        prompt += `Natural hand pose, product clearly visible, professional 4K quality, realistic lighting and composition.`;

                        payloadParts = [
                            { text: prompt },
                            { inlineData: { mimeType: "image/jpeg", data: base64Product } }
                        ];
                    }

                    const payload = {
                        contents: [{ parts: payloadParts }],
                        generationConfig: {
                            responseModalities: ['IMAGE'],
                            imageConfig: { aspectRatio: params.ratio }
                        }
                    };

                    const result = await generateImageWithRetry(payload);
                    const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                    if (base64Data) {
                        const imageUrl = `data:image/png;base64,${base64Data}`;

                        // Update stored image
                        generatedImages[index] = imageUrl;

                        // Redisplay the card
                        const safeTheme = params.theme.replace(/"/g, '&quot;');
                        targetCard.innerHTML = `
                            <img src="${imageUrl}" alt="POV Tangan ${index + 1}" />
                            <div class="result-card-actions">
                                <button class="btn-preview" data-action="preview" data-image-url="${imageUrl}">
                                    <i class="fas fa-search-plus"></i>
                                    Preview
                                </button>
                                <button class="btn-edit" data-action="edit" data-index="${index}" data-theme="${safeTheme}">
                                    <i class="fas fa-edit"></i>
                                    Edit
                                </button>
                                <button class="btn-regenerate" data-action="regenerate" data-index="${index}">
                                    <i class="fas fa-sync-alt"></i>
                                    Regenerate
                                </button>
                                <button class="btn-download" data-action="download" data-image-url="${imageUrl}" data-filename="pov-tangan-${index + 1}.jpg">
                                    <i class="fas fa-download"></i>
                                    Download
                                </button>
                            </div>
                            <div class="image-counter">#${index + 1}</div>
                        `;
                    } else {
                        throw new Error('Failed to generate image');
                    }
                } catch (error) {
                    console.error('Regenerate error:', error);
                    targetCard.innerHTML = `
                        <div class="flex flex-col items-center justify-center h-full min-h-[200px] text-red-600">
                            <i class="fas fa-exclamation-triangle text-3xl mb-2"></i>
                            <p class="text-sm">Gagal regenerate</p>
                            <button onclick="location.reload()" class="mt-2 text-xs bg-red-500 text-white px-3 py-1 rounded-full hover:bg-red-600">
                                Refresh
                            </button>
                        </div>
                    `;
                }
            }

            // Event handling for action buttons (works on desktop & mobile)
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionIcon = e.target.closest('[data-action]');
                if (!actionIcon) return;

                const action = actionIcon.dataset.action;
                const imageUrl = actionIcon.dataset.imageUrl;
                const index = actionIcon.dataset.index;
                const theme = actionIcon.dataset.theme;

                if (action === 'preview') {
                    showPreviewModal(imageUrl);
                } else if (action === 'edit') {
                    handleEditPov(parseInt(index), theme);
                } else if (action === 'regenerate') {
                    await handleRegeneratePov(parseInt(index));
                } else if (action === 'download') {
                    const filename = actionIcon.dataset.filename;
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageUrl, filename);
                    }
                }
            });

            // Download all button event handlers (works on desktop & mobile like Umrah)
            if (downloadAllBtn) {
                downloadAllBtn.addEventListener('click', async () => {
                    if (generatedImages.length === 0) return;

                    downloadAllBtn.disabled = true;
                    downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                    for (let i = 0; i < generatedImages.length; i++) {
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(generatedImages[i], `pov-tangan-${i + 1}.jpg`);
                            // Small delay between downloads for better UX
                            await new Promise(resolve => setTimeout(resolve, 500));
                        }
                    }

                    downloadAllBtn.disabled = false;
                    downloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
                });
            }

            function showPreviewModal(imageUrl) {
                const modal = document.createElement('div');
                modal.className = 'image-preview-modal';
                modal.innerHTML = `
                    <div class="preview-modal-overlay"></div>
                    <div class="preview-modal-content">
                        <button class="preview-close-btn" aria-label="Close">
                            <i class="fas fa-times"></i>
                        </button>
                        <img src="${imageUrl}" alt="Preview" class="preview-modal-image">
                    </div>
                `;
                document.body.appendChild(modal);

                // Close modal handlers
                const closeBtn = modal.querySelector('.preview-close-btn');
                const overlay = modal.querySelector('.preview-modal-overlay');

                const closeModal = () => {
                    modal.classList.add('closing');
                    setTimeout(() => modal.remove(), 300);
                };

                closeBtn.addEventListener('click', closeModal);
                overlay.addEventListener('click', closeModal);

                // ESC key to close
                const handleEsc = (e) => {
                    if (e.key === 'Escape') {
                        closeModal();
                        document.removeEventListener('keydown', handleEsc);
                    }
                };
                document.addEventListener('keydown', handleEsc);

                // Touch feedback for mobile
                closeBtn.addEventListener('touchstart', function() {
                    this.style.transform = 'scale(0.9)';
                }, { passive: true });
                closeBtn.addEventListener('touchend', function() {
                    this.style.transform = '';
                }, { passive: true });

                // Animate in
                setTimeout(() => modal.classList.add('show'), 10);
            }

            function showError(message) {
                errorMessage.textContent = message;
                errorSection.classList.remove('hidden');
            }

            function hideError() {
                errorSection.classList.add('hidden');
            }

            function resetForm() {
                uploadedImageData = null;
                uploadedBackgroundData = null;
                backgroundStyleDescription = null;
                generatedImages = [];
                uploadPlaceholder.classList.remove('hidden');
                previewContainer.classList.add('hidden');
                uploadArea.classList.remove('has-image');
                bgPlaceholder.classList.remove('hidden');
                bgPreviewContainer.classList.add('hidden');
                bgUploadArea.classList.remove('has-image');
                productDescription.value = '';
                generateBtn.disabled = true;
                resultsSection.classList.add('hidden');
                resultsGrid.innerHTML = '';
                if (povEmptyState) povEmptyState.classList.remove('hidden');
                hideError();
            }
        })();

    // --- POV SELFIE TAB LOGIC ---
    (function initPovSelfie() {
            const productInput = document.getElementById('povselfie-product-input');
            const productUploadArea = document.getElementById('povselfie-product-upload-area');
            const productPlaceholder = document.getElementById('povselfie-product-placeholder');
            const productPreviewContainer = document.getElementById('povselfie-product-preview-container');
            const productPreviewImage = document.getElementById('povselfie-product-preview-image');
            const modelInput = document.getElementById('povselfie-model-input');
            const modelUploadArea = document.getElementById('povselfie-model-upload-area');
            const modelPlaceholder = document.getElementById('povselfie-model-placeholder');
            const modelPreviewContainer = document.getElementById('povselfie-model-preview-container');
            const modelPreviewImage = document.getElementById('povselfie-model-preview-image');
            const descInput = document.getElementById('povselfie-desc');
            const generateBtn = document.getElementById('povselfie-generate-btn');
            const stopBtn = document.getElementById('povselfie-stop-btn');
            const resultsSection = document.getElementById('povselfie-results-section');
            const emptyState = document.getElementById('povselfie-empty-state');
            const resultCount = document.getElementById('povselfie-result-count');
            const resultsGrid = document.getElementById('povselfie-results-grid');
            const downloadAllBtn = document.getElementById('povselfie-download-all-btn');
            const errorSection = document.getElementById('povselfie-error-section');
            const errorMessage = document.getElementById('povselfie-error-message');
            const retryBtn = document.getElementById('povselfie-retry-btn');

            let uploadedProductData = null;
            let uploadedModelData = null;
            let generatedImages = [];
            let generatedImageParams = [];
            let selectedRatio = '3:4';
            let selectedCameraType = 'hp';
            let selectedCount = 4;
            let stopSelfie = false;

            // Karakter hasil foto per jenis kamera - biar terlihat natural sesuai alatnya
            const selfieCameraStyles = {
                hp: 'Front-camera smartphone perspective (iPhone 15 Pro): arm-length distance, slight wide-angle look, natural casual framing, realistic phone-camera image quality with authentic everyday feel',
                dslr: 'Shot on a professional DSLR camera (Canon EOS R5, 35mm f/1.8): crisp sharp detail, creamy background bokeh, professional color grading and dynamic range - framed like a natural arm-length selfie',
                mirrorless: 'Shot on a modern mirrorless camera (Sony A7 IV, 24mm f/2): clean modern rendering, soft natural bokeh, accurate skin tones, lifestyle editorial quality - framed like a natural arm-length selfie',
                analog: 'Shot on a 35mm analog film camera: subtle film grain, warm slightly faded tones, soft halation on highlights, nostalgic vintage snapshot look'
            };

            if (stopBtn) stopBtn.addEventListener('click', () => { stopSelfie = true; });

            // Variasi gaya selfie - dipilih acak tiap generate, background mengikuti tema produk
            const selfieStyles = [
                "casual arm-extended selfie, warm genuine smile, product held up next to the face",
                "close-up selfie from a slightly high angle, product held near the cheek, soft flattering light",
                "candid selfie while actively using the product, natural laughing expression",
                "selfie with the product held toward the camera in the foreground, face slightly behind in focus",
                "relaxed sitting-down selfie, product resting in the other hand, cozy vibe",
                "golden hour selfie with warm sunlight on the face, product clearly visible",
                "excited unboxing-style selfie, showing the product to the camera with a happy surprised expression",
                "aesthetic over-the-shoulder angle selfie, product held up beside the face",
                "walking selfie with a slight motion feel, product in hand, cheerful energy",
                "cozy indoor selfie with soft window light, product held at chest level",
                "review-style selfie pointing at the product with one finger, friendly expression",
                "content-creator style selfie, product placed prominently, bright even lighting"
            ];

            function shuffleSelfieStyles(arr) {
                const copy = [...arr];
                for (let i = copy.length - 1; i > 0; i--) {
                    const j = Math.floor(Math.random() * (i + 1));
                    [copy[i], copy[j]] = [copy[j], copy[i]];
                }
                return copy;
            }

            function updateGenerateState() {
                generateBtn.disabled = !(uploadedProductData && uploadedModelData);
            }

            function setupUpload(area, input, onLoaded) {
                if (!area || !input) return;
                area.addEventListener('click', () => input.click());
                area.addEventListener('dragover', (e) => { e.preventDefault(); area.style.borderColor = '#15803d'; });
                area.addEventListener('dragleave', () => area.style.borderColor = '');
                area.addEventListener('drop', (e) => {
                    e.preventDefault();
                    area.style.borderColor = '';
                    if (e.dataTransfer.files[0]) readImage(e.dataTransfer.files[0], onLoaded);
                });
                input.addEventListener('change', (e) => { if (e.target.files[0]) readImage(e.target.files[0], onLoaded); });
            }

            function readImage(file, onLoaded) {
                if (file.size > 10 * 1024 * 1024) {
                    showError('Ukuran file terlalu besar. Maksimal 10MB.');
                    return;
                }
                const reader = new FileReader();
                reader.onload = (e) => { onLoaded(e.target.result); hideError(); };
                reader.readAsDataURL(file);
            }

            setupUpload(productUploadArea, productInput, (data) => {
                uploadedProductData = data;
                productPreviewImage.src = data;
                productPlaceholder.classList.add('hidden');
                productPreviewContainer.classList.remove('hidden');
                productUploadArea.classList.add('has-image');
                updateGenerateState();
            });

            setupUpload(modelUploadArea, modelInput, (data) => {
                uploadedModelData = data;
                modelPreviewImage.src = data;
                modelPlaceholder.classList.add('hidden');
                modelPreviewContainer.classList.remove('hidden');
                modelUploadArea.classList.add('has-image');
                updateGenerateState();
            });

            const ratioSelection = document.getElementById('povselfie-ratio-selection');
            if (ratioSelection) {
                ratioSelection.addEventListener('click', (e) => {
                    const button = e.target.closest('.ratio-btn-povselfie');
                    if (button) {
                        ratioSelection.querySelectorAll('.ratio-btn-povselfie').forEach(btn => btn.classList.remove('selected'));
                        button.classList.add('selected');
                        selectedRatio = button.dataset.ratio;
                    }
                });
            }

            const cameraSelection = document.getElementById('povselfie-camera-selection');
            if (cameraSelection) {
                cameraSelection.addEventListener('click', (e) => {
                    const button = e.target.closest('button[data-camera]');
                    if (button) {
                        cameraSelection.querySelectorAll('button[data-camera]').forEach(btn => btn.classList.remove('selected'));
                        button.classList.add('selected');
                        selectedCameraType = button.dataset.camera;
                    }
                });
            }

            const countSelection = document.getElementById('povselfie-count-selection-grid');
            if (countSelection) {
                countSelection.addEventListener('click', (e) => {
                    const btn = e.target.closest('button[data-count]');
                    if (!btn) return;
                    countSelection.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    selectedCount = parseInt(btn.dataset.count, 10);
                });
            }

            generateBtn.addEventListener('click', startGeneration);
            retryBtn.addEventListener('click', () => { hideError(); });

            async function generateImageWithRetry(payload, retries = 3, delay = 1000) {
                for (let i = 0; i < retries; i++) {
                    try {
                        const apiKey = "";
                        const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                        const response = await fetch(apiUrl, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify(payload)
                        });
                        const responseText = await response.text();
                        if (!response.ok) {
                            let errorMsg = response.statusText;
                            try {
                                errorMsg = JSON.parse(responseText).error?.message || errorMsg;
                            } catch(e) {
                                if (responseText) errorMsg = responseText;
                            }
                            throw new Error(`API Error: ${errorMsg}`);
                        }
                        try {
                            return JSON.parse(responseText);
                        } catch (e) {
                            throw new Error("Invalid JSON response.");
                        }
                    } catch (error) {
                        console.error(`Attempt ${i + 1} failed:`, error);
                        if (i === retries - 1) return null;
                        await new Promise(res => setTimeout(res, delay * Math.pow(2, i)));
                    }
                }
                return null;
            }

            function buildSelfiePrompt(style, extraDesc, cameraType) {
                const cameraDesc = selfieCameraStyles[cameraType] || selfieCameraStyles.hp;
                let prompt = `Create ONE hyper-realistic selfie photo by combining these inputs:

IMAGE 1 (Person): the person taking the selfie
IMAGE 2 (Product): the product being shown

Task: The person from IMAGE 1 is taking a selfie while holding and showing the product from IMAGE 2.

Requirements:
- Keep the EXACT same face, hairstyle, and skin tone as IMAGE 1 - do not change the person's identity
- The product from IMAGE 2 must be clearly visible with the same shape, colors, label, and packaging
- Selfie style: ${style}
- Background/location: analyze the product and choose a realistic setting that MATCHES the product theme (examples: skincare -> bright vanity or bathroom, coffee/food -> cozy cafe, sport gear -> gym or outdoor, fashion -> aesthetic street or mall, gadget -> modern workspace)
- Camera character: ${cameraDesc}
- Realistic lighting that matches the chosen location, photorealistic skin texture, natural authentic look`;
                if (extraDesc) {
                    prompt += `\n- Additional details from user: ${extraDesc}`;
                }
                return prompt;
            }

            async function startGeneration() {
                if (generateBtn.disabled) return;
                if (!uploadedProductData || !uploadedModelData) {
                    showError('Silakan upload foto produk dan foto model terlebih dahulu.');
                    return;
                }

                stopSelfie = false;
                hideError();
                generateBtn.disabled = true;
                generateBtn.classList.add('hidden');
                if (stopBtn) stopBtn.classList.remove('hidden');

                const extraDesc = descInput.value.trim();
                let styles = shuffleSelfieStyles(selfieStyles);
                while (styles.length < selectedCount) {
                    styles = styles.concat(shuffleSelfieStyles(selfieStyles));
                }

                try {
                    await runGeneration(styles, extraDesc);
                    const successCount = generatedImages.filter(img => img).length;
                    if (successCount === 0 && !stopSelfie) {
                        await runGeneration(styles, extraDesc);
                    }
                    renderSuccessfulResults();
                } finally {
                    updateGenerateState();
                    generateBtn.classList.remove('hidden');
                    if (stopBtn) stopBtn.classList.add('hidden');
                }
            }

            async function runGeneration(styles, extraDesc) {
                generatedImages = new Array(selectedCount).fill(null);
                generatedImageParams = new Array(selectedCount).fill(null);

                resultsGrid.innerHTML = '';
                for (let i = 0; i < selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `povselfie-card-${i}`;
                    card.className = 'result-card';
                    card.innerHTML = `<div class="flex flex-col items-center justify-center h-full min-h-[200px]"><div class="loader !border-l-purple-500"></div></div>`;
                    resultsGrid.appendChild(card);
                }
                if (emptyState) emptyState.classList.add('hidden');
                resultsSection.classList.remove('hidden');
                resultCount.textContent = '...';
                downloadAllBtn.classList.add('hidden');

                if (window.innerWidth < 1024) {
                    setTimeout(() => resultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' }), 300);
                }

                await Promise.allSettled(
                    Array.from({ length: selectedCount }, (_, i) => generateSingleSelfie(i, styles, extraDesc))
                );
            }

            async function generateSingleSelfie(index, styles, extraDesc) {
                if (stopSelfie) return;
                const card = document.getElementById(`povselfie-card-${index}`);
                if (!card) return;

                const style = styles[index];
                const payload = {
                    contents: [{ parts: [
                        { text: buildSelfiePrompt(style, extraDesc, selectedCameraType) },
                        { inlineData: { mimeType: "image/jpeg", data: uploadedModelData.split(',')[1] } },
                        { inlineData: { mimeType: "image/jpeg", data: uploadedProductData.split(',')[1] } }
                    ] }],
                    generationConfig: {
                        responseModalities: ['IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const result = await generateImageWithRetry(payload);
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (base64Data) {
                    const imageUrl = `data:image/png;base64,${base64Data}`;
                    generatedImages[index] = imageUrl;
                    generatedImageParams[index] = { style, extraDesc, ratio: selectedRatio, camera: selectedCameraType };
                    card.innerHTML = `<img src="${imageUrl}" alt="POV Selfie" style="width:100%;height:100%;object-fit:cover;">`;
                } else {
                    card.innerHTML = '';
                }
            }

            function renderSuccessfulResults() {
                const successfulImages = [];
                const successfulParams = [];
                for (let i = 0; i < generatedImages.length; i++) {
                    if (generatedImages[i]) {
                        successfulImages.push(generatedImages[i]);
                        successfulParams.push(generatedImageParams[i]);
                    }
                }
                generatedImages = successfulImages;
                generatedImageParams = successfulParams;

                resultsGrid.innerHTML = '';

                if (successfulImages.length === 0) {
                    resultsSection.classList.add('hidden');
                    if (emptyState) emptyState.classList.remove('hidden');
                    showError('Tidak ada gambar yang berhasil digenerate. Silakan coba lagi.');
                    return;
                }

                successfulImages.forEach((imageUrl, idx) => {
                    const card = document.createElement('div');
                    card.className = 'result-card';
                    card.innerHTML = `
                        <img src="${imageUrl}" alt="POV Selfie ${idx + 1}" />
                        <div class="result-card-actions">
                            <button class="btn-preview" data-action="preview" data-image-url="${imageUrl}">
                                <i class="fas fa-search-plus"></i>
                                Preview
                            </button>
                            <button class="btn-regenerate" data-action="regenerate" data-index="${idx}">
                                <i class="fas fa-sync-alt"></i>
                                Regenerate
                            </button>
                            <button class="btn-download" data-action="download" data-image-url="${imageUrl}" data-filename="pov-selfie-${idx + 1}.jpg">
                                <i class="fas fa-download"></i>
                                Download
                            </button>
                        </div>
                        <div class="image-counter">#${idx + 1}</div>
                    `;
                    resultsGrid.appendChild(card);
                });

                resultCount.textContent = successfulImages.length;
                downloadAllBtn.classList.remove('hidden');

                if (window.innerWidth < 1024) {
                    resultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            }

            async function handleRegenerate(index) {
                const resultCards = resultsGrid.querySelectorAll('.result-card');
                const targetCard = resultCards[index];
                const params = generatedImageParams[index];
                if (!targetCard || !params) return;

                targetCard.innerHTML = `
                    <div class="flex flex-col items-center justify-center h-full min-h-[200px]">
                        <div class="loader !border-l-purple-500"></div>
                        <p class="mt-4 text-sm text-gray-600">Regenerating...</p>
                    </div>
                `;

                const payload = {
                    contents: [{ parts: [
                        { text: buildSelfiePrompt(params.style, params.extraDesc, params.camera) },
                        { inlineData: { mimeType: "image/jpeg", data: uploadedModelData.split(',')[1] } },
                        { inlineData: { mimeType: "image/jpeg", data: uploadedProductData.split(',')[1] } }
                    ] }],
                    generationConfig: {
                        responseModalities: ['IMAGE'],
                        imageConfig: { aspectRatio: params.ratio }
                    }
                };

                const result = await generateImageWithRetry(payload);
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (base64Data) {
                    const imageUrl = `data:image/png;base64,${base64Data}`;
                    generatedImages[index] = imageUrl;
                    targetCard.innerHTML = `
                        <img src="${imageUrl}" alt="POV Selfie ${index + 1}" />
                        <div class="result-card-actions">
                            <button class="btn-preview" data-action="preview" data-image-url="${imageUrl}">
                                <i class="fas fa-search-plus"></i>
                                Preview
                            </button>
                            <button class="btn-regenerate" data-action="regenerate" data-index="${index}">
                                <i class="fas fa-sync-alt"></i>
                                Regenerate
                            </button>
                            <button class="btn-download" data-action="download" data-image-url="${imageUrl}" data-filename="pov-selfie-${index + 1}.jpg">
                                <i class="fas fa-download"></i>
                                Download
                            </button>
                        </div>
                        <div class="image-counter">#${index + 1}</div>
                    `;
                } else {
                    targetCard.innerHTML = `
                        <div class="flex flex-col items-center justify-center h-full min-h-[200px] text-red-600">
                            <i class="fas fa-exclamation-triangle text-3xl mb-2"></i>
                            <p class="text-sm">Gagal regenerate</p>
                        </div>
                    `;
                }
            }

            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();
                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                if (action === 'preview') {
                    showSelfiePreviewModal(actionBtn.dataset.imageUrl);
                } else if (action === 'regenerate') {
                    await handleRegenerate(parseInt(actionBtn.dataset.index, 10));
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(actionBtn.dataset.imageUrl, actionBtn.dataset.filename);
                    }
                }
            });

            if (downloadAllBtn) {
                downloadAllBtn.addEventListener('click', async () => {
                    if (generatedImages.length === 0) return;
                    downloadAllBtn.disabled = true;
                    downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';
                    for (let i = 0; i < generatedImages.length; i++) {
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(generatedImages[i], `pov-selfie-${i + 1}.jpg`);
                            await new Promise(resolve => setTimeout(resolve, 500));
                        }
                    }
                    downloadAllBtn.disabled = false;
                    downloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
                });
            }

            function showSelfiePreviewModal(imageUrl) {
                const modal = document.createElement('div');
                modal.className = 'image-preview-modal';
                modal.innerHTML = `
                    <div class="preview-modal-overlay"></div>
                    <div class="preview-modal-content">
                        <button class="preview-close-btn" aria-label="Close">
                            <i class="fas fa-times"></i>
                        </button>
                        <img src="${imageUrl}" alt="Preview" class="preview-modal-image">
                    </div>
                `;
                document.body.appendChild(modal);

                const closeBtn = modal.querySelector('.preview-close-btn');
                const overlay = modal.querySelector('.preview-modal-overlay');

                const closeModal = () => {
                    modal.classList.add('closing');
                    setTimeout(() => modal.remove(), 300);
                };

                closeBtn.addEventListener('click', closeModal);
                overlay.addEventListener('click', closeModal);

                const handleEsc = (e) => {
                    if (e.key === 'Escape') {
                        closeModal();
                        document.removeEventListener('keydown', handleEsc);
                    }
                };
                document.addEventListener('keydown', handleEsc);

                closeBtn.addEventListener('touchstart', function() {
                    this.style.transform = 'scale(0.9)';
                }, { passive: true });
                closeBtn.addEventListener('touchend', function() {
                    this.style.transform = '';
                }, { passive: true });

                // Animate in — tanpa class 'show' modal tetap opacity 0 (bug preview V44)
                setTimeout(() => modal.classList.add('show'), 10);
            }

            function showError(message) {
                errorMessage.textContent = message;
                errorSection.classList.remove('hidden');
            }

            function hideError() {
                errorSection.classList.add('hidden');
            }
    })();

        // === MOCKUP STUDIO INITIALIZATION ===
        // Direct initialization like Try-On (no lazy loading)
        (function initMockupStudio() {
            // DOM Elements
            const designInput = document.getElementById('mockup-design-input');
            const uploadArea = document.getElementById('mockup-upload-area');
            const uploadPlaceholder = document.getElementById('mockup-upload-placeholder');
            const previewContainer = document.getElementById('mockup-preview-container');
            const previewImage = document.getElementById('mockup-preview-image');

            const templateInput = document.getElementById('mockup-template-input');
            const templateUploadArea = document.getElementById('mockup-template-upload-area');
            const templatePlaceholder = document.getElementById('mockup-template-placeholder');
            const templatePreviewContainer = document.getElementById('mockup-template-preview-container');
            const templatePreviewImage = document.getElementById('mockup-template-preview-image');
            const templateRemoveBtn = document.getElementById('mockup-template-remove-btn');

            const themeInput = document.getElementById('mockup-theme-input');
            const generateBtn = document.getElementById('mockup-generate-btn');
            const loadingSection = document.getElementById('mockup-loading-section');
            const progressText = document.getElementById('mockup-progress-text');
            const progressBar = document.getElementById('mockup-progress-bar');
            const resultsSection = document.getElementById('mockup-results-section');
            const mockupEmptyState = document.getElementById('mockup-studio-empty-state');
            const resultCount = document.getElementById('mockup-result-count');
            const resultsGrid = document.getElementById('mockup-results-grid');
            const downloadAllBtn = document.getElementById('mockup-download-all-btn');
            const errorSection = document.getElementById('mockup-error-section');
            const errorMessage = document.getElementById('mockup-error-message');
            const retryBtn = document.getElementById('mockup-retry-btn');

            let uploadedDesignData = null;
            let uploadedTemplateData = null;
            let generatedMockups = [];
            let generatedMockupParams = []; // Track parameters for each mockup
            let selectedMockupRatio = '16:9';
            let currentMockupThemes = []; // Store current themes for regeneration
            let stopMockup = false;
            const stopBtn = document.getElementById('mockup-stop-btn');
            if (stopBtn) stopBtn.addEventListener('click', () => { stopMockup = true; });

            // Count selection (1-10), default 4
            let selectedCount = 4;
            const countSelectionMockup = document.getElementById('mockup-count-selection-grid');
            if (countSelectionMockup) {
                countSelectionMockup.addEventListener('click', (e) => {
                    const btn = e.target.closest('button[data-count]');
                    if (!btn) return;
                    countSelectionMockup.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    selectedCount = parseInt(btn.dataset.count, 10);
                });
            }

            // Default mockup themes
            const defaultMockupThemes = [
                'white ceramic coffee mug on wooden desk',
                't-shirt mockup laid flat on white background',
                'smartphone screen mockup in hand',
                'poster frame mockup on white wall',
                'book cover mockup on wooden table',
                'tote bag mockup on solid background',
                'business card mockup on desk',
                'hoodie mockup flat lay on marble surface',
                'laptop screen mockup on office desk',
                'canvas print on living room wall',
                'pillow mockup on sofa background',
                'tumbler bottle mockup on outdoor setting',
                'sticker mockup on macbook laptop',
                'packaging box mockup on clean white surface',
                'phone case mockup on wooden desk',
                'notebook cover mockup on cafe table',
                'canvas tote bag on beach background',
                'framed art print on gallery wall',
                'ceramic plate with design on kitchen setting',
                'cap hat mockup on clean white background'
            ];

            // Event Listeners - Using Try-On pattern
            // Main design upload
            if (uploadArea && designInput) {
                uploadArea.addEventListener('click', () => designInput.click());
                uploadArea.addEventListener('dragover', (e) => { e.preventDefault(); uploadArea.style.borderColor = '#764ba2'; });
                uploadArea.addEventListener('dragleave', () => uploadArea.style.borderColor = '');
                uploadArea.addEventListener('drop', (e) => {
                    e.preventDefault();
                    uploadArea.style.borderColor = '';
                    if (e.dataTransfer.files[0]) processDesign(e.dataTransfer.files[0]);
                });
                designInput.addEventListener('change', (e) => { if (e.target.files[0]) processDesign(e.target.files[0]); });
            }

            // Template upload
            if (templateUploadArea && templateInput) {
                templateUploadArea.addEventListener('click', () => templateInput.click());
                templateUploadArea.addEventListener('dragover', (e) => { e.preventDefault(); templateUploadArea.style.borderColor = '#764ba2'; });
                templateUploadArea.addEventListener('dragleave', () => templateUploadArea.style.borderColor = '');
                templateUploadArea.addEventListener('drop', (e) => {
                    e.preventDefault();
                    templateUploadArea.style.borderColor = '';
                    if (e.dataTransfer.files[0]) processTemplate(e.dataTransfer.files[0]);
                });
                templateInput.addEventListener('change', (e) => { if (e.target.files[0]) processTemplate(e.target.files[0]); });
            }

            templateRemoveBtn.addEventListener('click', removeTemplate);

            generateBtn.addEventListener('click', startGeneration);
            retryBtn.addEventListener('click', resetForm);

            // Ratio selection event listener
            const ratioSelection = document.getElementById('mockup-ratio-selection');
            if (ratioSelection) {
                ratioSelection.addEventListener('click', (e) => {
                    const button = e.target.closest('.ratio-btn-mockup');
                    if (button) {
                        document.querySelectorAll('#mockup-ratio-selection .ratio-btn-mockup').forEach(btn => btn.classList.remove('selected'));
                        button.classList.add('selected');
                        selectedMockupRatio = button.dataset.ratio;
                    }
                });
            }

            function processDesign(file) {
                if (file.size > 10 * 1024 * 1024) {
                    showError('Ukuran file terlalu besar. Maksimal 10MB.');
                    return;
                }

                const reader = new FileReader();
                reader.onload = (e) => {
                    uploadedDesignData = e.target.result;
                    previewImage.src = uploadedDesignData;
                    uploadPlaceholder.classList.add('hidden');
                    previewContainer.classList.remove('hidden');
                    uploadArea.classList.add('has-image');
                    generateBtn.disabled = false;
                    hideError();
                };
                reader.readAsDataURL(file);
            }

            function processTemplate(file) {
                if (file.size > 10 * 1024 * 1024) {
                    showError('Ukuran file template terlalu besar. Maksimal 10MB.');
                    return;
                }

                const reader = new FileReader();
                reader.onload = (e) => {
                    uploadedTemplateData = e.target.result;
                    templatePreviewImage.src = uploadedTemplateData;
                    templatePlaceholder.classList.add('hidden');
                    templatePreviewContainer.classList.remove('hidden');
                    templateUploadArea.classList.add('has-image');
                };
                reader.readAsDataURL(file);
            }

            function removeTemplate(e) {
                e.stopPropagation();
                uploadedTemplateData = null;
                templatePreviewImage.src = '';
                templatePlaceholder.classList.remove('hidden');
                templatePreviewContainer.classList.add('hidden');
                templateUploadArea.classList.remove('has-image');
                templateInput.value = '';
            }

            async function runGeneration(mockupThemes) {
                generatedMockups = new Array(selectedCount).fill(null);
                generatedMockupParams = new Array(selectedCount).fill(null);

                // Pre-create placeholder cards
                resultsGrid.innerHTML = '';
                for (let i = 0; i < selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `mockup-card-${i}`;
                    card.className = 'result-card';
                    card.innerHTML = `<div class="flex flex-col items-center justify-center h-full min-h-[200px]"><div class="loader !border-l-purple-500"></div></div>`;
                    resultsGrid.appendChild(card);
                }
                if (mockupEmptyState) mockupEmptyState.classList.add('hidden');
                resultsSection.classList.remove('hidden');
                resultCount.textContent = '...';
                downloadAllBtn.classList.add('hidden');

                if (window.innerWidth < 1024) {
                    setTimeout(() => resultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' }), 300);
                }

                await Promise.allSettled(
                    Array.from({ length: selectedCount }, (_, i) => generateAndUpdateCard(i, mockupThemes))
                );
            }

            async function startGeneration() {
                if (!uploadedDesignData) {
                    showError('Silakan upload design terlebih dahulu.');
                    return;
                }

                stopMockup = false;
                hideError();
                loadingSection.classList.add('hidden');
                generateBtn.disabled = true;
                generateBtn.classList.add('hidden');
                if (stopBtn) stopBtn.classList.remove('hidden');
                currentMockupThemes = [];

                const manualTheme = themeInput.value.trim();
                let mockupThemes;
                if (manualTheme) {
                    mockupThemes = Array(selectedCount).fill(manualTheme + ' mockup');
                } else {
                    mockupThemes = defaultMockupThemes;
                }
                currentMockupThemes = [...mockupThemes];

                // First attempt
                await runGeneration(mockupThemes);

                let successCount = generatedMockups.filter(m => m).length;

                // Auto-retry once if all failed and not stopped
                if (successCount === 0 && !stopMockup) {
                    await runGeneration(mockupThemes);
                    successCount = generatedMockups.filter(m => m).length;
                }

                generateBtn.disabled = false;
                generateBtn.classList.remove('hidden');
                if (stopBtn) stopBtn.classList.add('hidden');

                renderSuccessfulMockups();
            }

            function renderSuccessfulMockups() {
                // Filter to only successful results
                const successfulMockups = [];
                const successfulParams = [];
                for (let i = 0; i < generatedMockups.length; i++) {
                    if (generatedMockups[i]) {
                        successfulMockups.push(generatedMockups[i]);
                        successfulParams.push(generatedMockupParams[i]);
                    }
                }

                // Replace arrays with filtered versions (re-indexed from 0)
                generatedMockups = successfulMockups;
                generatedMockupParams = successfulParams;
                currentMockupThemes = successfulParams.map(p => p?.theme || '');

                resultsGrid.innerHTML = '';

                if (successfulMockups.length === 0) {
                    resultsSection.classList.add('hidden');
                    if (mockupEmptyState) mockupEmptyState.classList.remove('hidden');
                    showError('Tidak ada mockup yang berhasil digenerate. Silakan coba lagi.');
                    return;
                }

                successfulMockups.forEach((imageUrl, idx) => {
                    const params = successfulParams[idx];
                    const safeTheme = (params?.theme || '').replace(/"/g, '&quot;');
                    const card = document.createElement('div');
                    card.className = 'result-card';
                    card.innerHTML = `
                        <img src="${imageUrl}" alt="Mockup ${idx + 1}" />
                        <div class="result-card-actions">
                            <button class="btn-preview" data-action="preview" data-image-url="${imageUrl}">
                                <i class="fas fa-search-plus"></i>
                                Preview
                            </button>
                            <button class="btn-edit" data-action="edit" data-index="${idx}" data-theme="${safeTheme}">
                                <i class="fas fa-edit"></i>
                                Edit
                            </button>
                            <button class="btn-regenerate" data-action="regenerate" data-index="${idx}">
                                <i class="fas fa-sync-alt"></i>
                                Regenerate
                            </button>
                            <button class="btn-download" data-action="download" data-image-url="${imageUrl}" data-filename="mockup-${idx + 1}.jpg">
                                <i class="fas fa-download"></i>
                                Download
                            </button>
                        </div>
                        <div class="image-counter">#${idx + 1}</div>
                    `;
                    resultsGrid.appendChild(card);
                });

                resultCount.textContent = successfulMockups.length;
                downloadAllBtn.classList.remove('hidden');

                if (window.innerWidth < 1024) {
                    resultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            }

            async function generateAndUpdateCard(index, mockupThemes) {
                if (stopMockup) return;
                const card = document.getElementById(`mockup-card-${index}`);
                if (!card) return;

                const theme = mockupThemes[index];
                const base64Design = uploadedDesignData.split(',')[1];

                let prompt = '';
                let payloadParts = [];

                if (uploadedTemplateData) {
                    const base64Template = uploadedTemplateData.split(',')[1];
                    prompt = `Create a professional product mockup by combining these elements:

IMAGE 1 (Template/Mockup Base): Use this as the mockup template
IMAGE 2 (Design/Artwork): The design to be applied to the mockup

Task: Apply the design from IMAGE 2 onto the mockup template from IMAGE 1 realistically.

Requirements:
- The base mockup/template MUST be from IMAGE 1
- Apply the design from IMAGE 2 onto the mockup surface
- Match perspective, lighting, and shadows naturally
- Make it look like a professional product mockup photo
- Ensure design fits naturally on the mockup surface
- Professional 4K quality, realistic rendering
- Mockup type: ${theme}

IMPORTANT: Use the template from IMAGE 1 as the base, and apply design from IMAGE 2 onto it.`;

                    payloadParts = [
                        { text: prompt },
                        { inlineData: { mimeType: "image/jpeg", data: base64Template } },
                        { inlineData: { mimeType: "image/jpeg", data: base64Design } }
                    ];
                } else {
                    prompt = `Create a professional product mockup for this design/artwork.

Mockup type: ${theme}

Requirements:
- Apply this design/artwork onto a realistic mockup
- Professional product photography style
- Natural lighting and shadows
- High quality rendering, 4K resolution
- Make it look like a real product photograph
- Clean, professional presentation

The design should be clearly visible and properly placed on the mockup surface.`;

                    payloadParts = [
                        { text: prompt },
                        { inlineData: { mimeType: "image/jpeg", data: base64Design } }
                    ];
                }

                const payload = {
                    contents: [{ parts: payloadParts }],
                    generationConfig: {
                        responseModalities: ['IMAGE'],
                        imageConfig: { aspectRatio: selectedMockupRatio }
                    }
                };

                const result = await generateImageWithRetry(payload);
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (base64Data) {
                    const imageUrl = `data:image/png;base64,${base64Data}`;
                    generatedMockups[index] = imageUrl;
                    generatedMockupParams[index] = {
                        theme,
                        designData: uploadedDesignData,
                        templateData: uploadedTemplateData,
                        ratio: selectedMockupRatio
                    };
                    // Show image inline immediately (buttons added by renderSuccessfulMockups at the end)
                    card.innerHTML = `<img src="${imageUrl}" alt="Mockup" style="width:100%;height:100%;object-fit:cover;">`;
                } else {
                    card.innerHTML = '';
                }
            }

            async function generateImageWithRetry(payload, retries = 3, delay = 1000) {
                for (let i = 0; i < retries; i++) {
                    try {
                        const apiKey = "";
                        const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                        const response = await fetch(apiUrl, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify(payload)
                        });
                        const responseText = await response.text();

                        if (!response.ok) {
                            let errorMsg = response.statusText;
                            try {
                                errorMsg = JSON.parse(responseText).error?.message || errorMsg;
                            } catch(e) {
                                if(responseText) errorMsg = responseText;
                            }
                            throw new Error(`API Error: ${errorMsg}`);
                        }

                        try {
                            return JSON.parse(responseText);
                        } catch (e) {
                            throw new Error("Invalid JSON response.");
                        }
                    } catch (error) {
                        console.error(`Attempt ${i + 1} failed:`, error);
                        if (i === retries - 1) return null;
                        await new Promise(res => setTimeout(res, delay * Math.pow(2, i)));
                    }
                }
                return null;
            }

            // Event handling for action buttons (works on desktop & mobile)
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const imageUrl = actionBtn.dataset.imageUrl;
                const index = actionBtn.dataset.index;
                const theme = actionBtn.dataset.theme;

                if (action === 'preview') {
                    showPreviewModal(imageUrl);
                } else if (action === 'edit') {
                    handleEditMockup(parseInt(index), theme);
                } else if (action === 'regenerate') {
                    await handleRegenerateMockup(parseInt(index));
                } else if (action === 'download') {
                    const filename = actionBtn.dataset.filename;
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageUrl, filename);
                    }
                }
            });

            // Function to handle edit mockup
            function handleEditMockup(index, currentTheme) {
                const modal = document.createElement('div');
                modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full">
                        <h3 class="text-xl font-bold mb-4 text-gray-800">
                            <i class="fas fa-edit text-purple-500 mr-2"></i>Edit Mockup Theme
                        </h3>
                        <div class="mb-4">
                            <label class="block text-sm font-semibold text-gray-700 mb-2">Mockup Theme/Style:</label>
                            <textarea id="edit-mockup-input" rows="4" class="w-full p-3 border-2 border-gray-300 rounded-lg focus:border-purple-500 focus:outline-none resize-none" placeholder="Contoh: white ceramic coffee mug on wooden desk, t-shirt mockup on white background, poster frame on wall, etc...">${currentTheme}</textarea>
                            <p class="text-xs text-gray-500 mt-2">
                                <i class="fas fa-info-circle mr-1"></i>
                                Deskripsi mockup yang diinginkan
                            </p>
                        </div>
                        <div class="flex gap-2">
                            <button onclick="this.closest('.fixed').remove()" class="flex-1 bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                                Batal
                            </button>
                            <button id="confirm-edit-mockup-btn" class="flex-1 bg-purple-500 hover:bg-purple-600 text-white font-bold py-2 px-4 rounded-lg transition-colors">
                                <i class="fas fa-check mr-2"></i>Generate Ulang
                            </button>
                        </div>
                    </div>
                `;
                document.body.appendChild(modal);

                const confirmBtn = modal.querySelector('#confirm-edit-mockup-btn');
                const themeInput = modal.querySelector('#edit-mockup-input');

                confirmBtn.addEventListener('click', async () => {
                    const newTheme = themeInput.value.trim();
                    if (!newTheme) return;

                    modal.remove();

                    // Update the theme for this mockup
                    generatedMockupParams[index].theme = newTheme;
                    currentMockupThemes[index] = newTheme;

                    await handleRegenerateMockup(index);
                });
            }

            // Function to handle regenerate mockup
            async function handleRegenerateMockup(index) {
                const resultCards = resultsGrid.querySelectorAll('.result-card');
                const targetCard = resultCards[index];
                if (!targetCard) return;

                const params = generatedMockupParams[index];
                if (!params) return;

                // Show loading state
                targetCard.innerHTML = `
                    <div class="flex flex-col items-center justify-center h-full min-h-[200px]">
                        <div class="loader !border-l-purple-500"></div>
                        <p class="mt-4 text-sm text-gray-600">Regenerating...</p>
                    </div>
                `;

                try {
                    const base64Design = params.designData.split(',')[1];
                    let prompt = '';
                    let payloadParts = [];

                    if (params.templateData) {
                        const base64Template = params.templateData.split(',')[1];
                        prompt = `Create a professional product mockup by combining these elements:

IMAGE 1 (Template/Mockup Base): Use this as the mockup template
IMAGE 2 (Design/Artwork): The design to be applied to the mockup

Task: Apply the design from IMAGE 2 onto the mockup template from IMAGE 1 realistically.
The final output should look like a professional product photograph.

Requirements:
- Apply artwork properly on mockup surface
- Realistic shadows and lighting
- High-quality professional result
- Natural integration of design into template`;

                        payloadParts = [
                            { text: prompt },
                            { inlineData: { mimeType: "image/jpeg", data: base64Template } },
                            { inlineData: { mimeType: "image/jpeg", data: base64Design } }
                        ];
                    } else {
                        prompt = `Create a professional product mockup with the following design.

Mockup type: ${params.theme}

Requirements:
- Apply this design/artwork onto a realistic mockup
- Professional product photography style
- Natural lighting and shadows
- High quality rendering, 4K resolution
- Make it look like a real product photograph
- Clean, professional presentation

The design should be clearly visible and properly placed on the mockup surface.`;

                        payloadParts = [
                            { text: prompt },
                            { inlineData: { mimeType: "image/jpeg", data: base64Design } }
                        ];
                    }

                    const payload = {
                        contents: [{ parts: payloadParts }],
                        generationConfig: {
                            responseModalities: ['IMAGE'],
                            imageConfig: { aspectRatio: params.ratio }
                        }
                    };

                    const result = await generateImageWithRetry(payload);
                    const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                    if (base64Data) {
                        const imageUrl = `data:image/png;base64,${base64Data}`;

                        // Update stored mockup
                        generatedMockups[index] = imageUrl;

                        // Redisplay the card
                        const safeTheme = params.theme.replace(/"/g, '&quot;');
                        targetCard.innerHTML = `
                            <img src="${imageUrl}" alt="Mockup ${index + 1}" />
                            <div class="result-card-actions">
                                <button class="btn-preview" data-action="preview" data-image-url="${imageUrl}">
                                    <i class="fas fa-search-plus"></i>
                                    Preview
                                </button>
                                <button class="btn-edit" data-action="edit" data-index="${index}" data-theme="${safeTheme}">
                                    <i class="fas fa-edit"></i>
                                    Edit
                                </button>
                                <button class="btn-regenerate" data-action="regenerate" data-index="${index}">
                                    <i class="fas fa-sync-alt"></i>
                                    Regenerate
                                </button>
                                <button class="btn-download" data-action="download" data-image-url="${imageUrl}" data-filename="mockup-${index + 1}.jpg">
                                    <i class="fas fa-download"></i>
                                    Download
                                </button>
                            </div>
                            <div class="image-counter">#${index + 1}</div>
                        `;
                    } else {
                        throw new Error('Failed to generate mockup');
                    }
                } catch (error) {
                    console.error('Regenerate error:', error);
                    targetCard.innerHTML = `
                        <div class="flex flex-col items-center justify-center h-full min-h-[200px] text-red-600">
                            <i class="fas fa-exclamation-triangle text-3xl mb-2"></i>
                            <p class="text-sm">Gagal regenerate</p>
                            <button onclick="location.reload()" class="mt-2 text-xs bg-red-500 text-white px-3 py-1 rounded-full hover:bg-red-600">
                                Refresh
                            </button>
                        </div>
                    `;
                }
            }

            // Download all button event handlers (works on desktop & mobile like Umrah)
            if (downloadAllBtn) {
                downloadAllBtn.addEventListener('click', async () => {
                    if (generatedMockups.length === 0) return;

                    downloadAllBtn.disabled = true;
                    downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                    for (let i = 0; i < generatedMockups.length; i++) {
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(generatedMockups[i], `mockup-${i + 1}.jpg`);
                            // Small delay between downloads for better UX
                            await new Promise(resolve => setTimeout(resolve, 500));
                        }
                    }

                    downloadAllBtn.disabled = false;
                    downloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
                });
            }

            function showPreviewModal(imageUrl) {
                const modal = document.createElement('div');
                modal.className = 'image-preview-modal';
                modal.innerHTML = `
                    <div class="preview-modal-overlay"></div>
                    <div class="preview-modal-content">
                        <button class="preview-close-btn" aria-label="Close">
                            <i class="fas fa-times"></i>
                        </button>
                        <img src="${imageUrl}" alt="Preview" class="preview-modal-image">
                    </div>
                `;
                document.body.appendChild(modal);

                // Close modal handlers
                const closeBtn = modal.querySelector('.preview-close-btn');
                const overlay = modal.querySelector('.preview-modal-overlay');

                const closeModal = () => {
                    modal.classList.add('closing');
                    setTimeout(() => modal.remove(), 300);
                };

                closeBtn.addEventListener('click', closeModal);
                overlay.addEventListener('click', closeModal);

                // ESC key to close
                const handleEsc = (e) => {
                    if (e.key === 'Escape') {
                        closeModal();
                        document.removeEventListener('keydown', handleEsc);
                    }
                };
                document.addEventListener('keydown', handleEsc);

                // Touch feedback for mobile
                closeBtn.addEventListener('touchstart', function() {
                    this.style.transform = 'scale(0.9)';
                }, { passive: true });
                closeBtn.addEventListener('touchend', function() {
                    this.style.transform = '';
                }, { passive: true });

                // Animate in
                setTimeout(() => modal.classList.add('show'), 10);
            }

            function showError(message) {
                errorMessage.textContent = message;
                errorSection.classList.remove('hidden');
            }

            function hideError() {
                errorSection.classList.add('hidden');
            }

            function resetForm() {
                uploadedDesignData = null;
                uploadedTemplateData = null;
                generatedMockups = [];
                uploadPlaceholder.classList.remove('hidden');
                previewContainer.classList.add('hidden');
                uploadArea.classList.remove('has-image');
                templatePlaceholder.classList.remove('hidden');
                templatePreviewContainer.classList.add('hidden');
                templateUploadArea.classList.remove('has-image');
                themeInput.value = '';
                generateBtn.disabled = true;
                resultsSection.classList.add('hidden');
                resultsGrid.innerHTML = '';
                if (mockupEmptyState) mockupEmptyState.classList.remove('hidden');
                hideError();
                progressBar.style.width = '0%';
                progressText.textContent = '0/21';
            }
        })();

    // ==================== POV MIRROR SELFIE ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const productInput = document.getElementById('mirror-product-input');
        const productUploadBox = document.getElementById('mirror-product-upload-box');
        const productPlaceholder = document.getElementById('mirror-product-placeholder');
        const productPreviewContainer = document.getElementById('mirror-product-preview-container');
        const productPreview = document.getElementById('mirror-product-preview');
        const removeProductPreview = document.getElementById('mirror-remove-product-preview');

        const modelInput = document.getElementById('mirror-model-input');
        const modelUploadBox = document.getElementById('mirror-model-upload-box');
        const modelPlaceholder = document.getElementById('mirror-model-placeholder');
        const modelPreviewContainer = document.getElementById('mirror-model-preview-container');
        const modelPreview = document.getElementById('mirror-model-preview');
        const removeModelPreview = document.getElementById('mirror-remove-model-preview');

        const mirrorTypeOptions = document.getElementById('mirror-type-options');
        const customTypeContainer = document.getElementById('mirror-custom-type-container');
        const customTypeInput = document.getElementById('mirror-custom-type-input');
        const roomStyleOptions = document.getElementById('mirror-room-style-options');
        const customStyleContainer = document.getElementById('mirror-custom-style-container');
        const customStyleInput = document.getElementById('mirror-custom-style-input');
        const phoneOptions = document.getElementById('mirror-phone-options');
        const handOptions = document.getElementById('mirror-hand-options');
        const lightingOptions = document.getElementById('mirror-lighting-options');
        const ratioOptions = document.getElementById('mirror-ratio-options');

        const generateBtn = document.getElementById('mirror-generate-btn');
        const resultsHeader = document.getElementById('mirror-results-header');
        const resultsCount = document.getElementById('mirror-results-count');
        const resultsGrid = document.getElementById('mirror-results-grid');
        const emptyState = document.getElementById('mirror-empty-state');
        const loading = document.getElementById('mirror-loading');
        const downloadAllBtn = document.getElementById('mirror-download-all-btn');

        // State
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let productImageData = null;
        let modelImageData = null;
        let selectedMirrorType = 'full-body';
        let selectedRoomStyle = 'modern-minimalist';
        let selectedPhone = 'iphone';
        let selectedHand = 'holding-phone';
        let selectedLighting = 'natural-bright';
        let selectedRatio = '9:16';
        let generatedImages = [];
        let mirrorBasePrompt = '';

        // Count selection (1-10), default 4
        const mirrorCountSel = document.getElementById('mirror-count-selection-grid');
        if (mirrorCountSel) {
            mirrorCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                mirrorCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Mirror type descriptions
        const mirrorTypeDescriptions = {
            'full-body': 'full-length body mirror, standing mirror against wall, person visible from head to toe',
            'bathroom': 'bathroom mirror selfie, sink and bathroom tiles visible in background, modern bathroom aesthetic',
            'vanity': 'vanity mirror with lights, makeup table with mirror, aesthetic bedroom vanity setup',
            'gym': 'gym mirror selfie, fitness center environment, workout area mirror, gym equipment visible in background',
            'bedroom': 'bedroom mirror selfie, cozy bedroom setting, mirror in bedroom with bed visible in background',
            'fitting-room': 'fitting room mirror, clothing store changing room, store fitting room with curtain'
        };

        // Room style descriptions
        const roomStyleDescriptions = {
            'modern-minimalist': 'modern minimalist interior, clean white walls, simple elegant furniture, Scandinavian design aesthetic',
            'aesthetic-korean': 'Korean aesthetic room, pastel colors, aesthetic decorations, soft ambient lighting, K-style interior',
            'luxury-hotel': 'luxury hotel room, 5-star hotel interior, elegant high-end design, premium materials and finishes',
            'vintage-retro': 'vintage retro interior, 70s 80s aesthetic, nostalgic vibes, warm vintage colors',
            'industrial-loft': 'industrial loft style, exposed brick walls, concrete floors, modern industrial design',
            'cozy-apartment': 'cozy apartment interior, comfortable homey feel, warm lived-in aesthetic, personal touches',
            'white-clean': 'pure white clean aesthetic, minimalist white interior, bright clean environment, clinical white room',
            'dark-moody': 'dark moody aesthetic, dramatic dark colors, moody atmospheric lighting, dark interior design'
        };

        // Phone descriptions
        const phoneDescriptions = {
            'iphone': 'holding iPhone to take mirror selfie, modern iPhone visible in hand covering face',
            'samsung': 'holding Samsung Galaxy phone, Android smartphone visible in mirror reflection',
            'aesthetic-case': 'phone with aesthetic cute case, kawaii phone case, decorative phone accessories',
            'no-phone': 'no phone visible, clear face visible, hands down or casual pose without phone'
        };

        // Hand position descriptions
        const handPositionDescriptions = {
            'holding-phone': 'one hand holding phone up to take selfie, phone covering part of face',
            'one-hand-hip': 'one hand on hip in confident pose, sassy pose with hand on waist',
            'peace-sign': 'making peace sign with fingers, V-sign hand gesture, playful peace pose',
            'holding-product': 'holding product prominently in hand, showcasing the product clearly',
            'natural': 'natural relaxed hands position, casual comfortable pose, hands hanging naturally',
            'both-hands-up': 'both hands raised up in fun pose, energetic pose with arms up'
        };

        // Lighting descriptions
        const lightingDescriptions = {
            'natural-bright': 'natural bright daylight, soft window light, bright ambient lighting, well-lit room',
            'warm-cozy': 'warm cozy lighting, golden warm tones, comfortable ambient light, warm yellow glow',
            'neon-aesthetic': 'neon aesthetic lighting, LED strip lights, colorful neon glow, aesthetic RGB lights',
            'soft-diffused': 'soft diffused lighting, gentle even light, no harsh shadows, flattering soft light',
            'golden-hour': 'golden hour sunset lighting, warm afternoon sun, beautiful golden natural light',
            'cool-white': 'cool white LED lighting, bright white light, modern cool-toned lighting'
        };

        // Upload product photo
        if (productInput) {
            productInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    productImageData = reader.result;

                    // Show preview on mobile
                    if (productPreview && productPreviewContainer) {
                        productPreview.src = reader.result;
                        productPreviewContainer.classList.remove('hidden');
                    }

                    // Update placeholder
                    if (productPlaceholder) {
                        productPlaceholder.innerHTML = `
                            <i class="fas fa-check-circle text-cyan-500" style="font-size: 2.5rem;"></i>
                            <p class="mt-2 text-sm font-semibold text-cyan-600">Produk berhasil diupload!</p>
                        `;
                    }

                    // Enable generate button
                    if (generateBtn) generateBtn.disabled = false;
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove preview
        if (removeProductPreview) {
            removeProductPreview.addEventListener('click', () => {
                productImageData = null;
                if (productInput) productInput.value = '';
                if (productPreviewContainer) productPreviewContainer.classList.add('hidden');
                if (productPreview) productPreview.src = '';
                if (productPlaceholder) {
                    productPlaceholder.innerHTML = `
                        <i class="fas fa-box-open transition-transform duration-300" style="font-size: 2.5rem; color: #06b6d4;"></i>
                        <p class="mt-2 text-sm font-semibold">📦 Klik untuk upload produk</p>
                        <p class="mt-1 text-xs text-gray-400">Bisa produk apapun (baju, sepatu, makeup, dll)</p>
                    `;
                }
                if (generateBtn) generateBtn.disabled = true;
            });
        }

        // Upload model photo (optional)
        if (modelInput) {
            modelInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    modelImageData = reader.result;

                    // Show preview on mobile
                    if (modelPreview && modelPreviewContainer) {
                        modelPreview.src = reader.result;
                        modelPreviewContainer.classList.remove('hidden');
                    }

                    // Update placeholder
                    if (modelPlaceholder) {
                        modelPlaceholder.innerHTML = `
                            <i class="fas fa-check-circle text-cyan-500" style="font-size: 2.5rem;"></i>
                            <p class="mt-2 text-sm font-semibold text-cyan-600">Model berhasil diupload!</p>
                        `;
                    }
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove model preview
        if (removeModelPreview) {
            removeModelPreview.addEventListener('click', () => {
                modelImageData = null;
                if (modelInput) modelInput.value = '';
                if (modelPreviewContainer) modelPreviewContainer.classList.add('hidden');
                if (modelPreview) modelPreview.src = '';
                if (modelPlaceholder) {
                    modelPlaceholder.innerHTML = `
                        <i class="fas fa-user transition-transform duration-300" style="font-size: 2.5rem; color: #06b6d4;"></i>
                        <p class="mt-2 text-sm font-semibold">👤 Klik untuk upload foto model</p>
                        <p class="mt-1 text-xs text-gray-400">Opsional - Untuk hasil selfie lebih personal</p>
                    `;
                }
            });
        }

        // Mirror type selection
        if (mirrorTypeOptions) {
            mirrorTypeOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                mirrorTypeOptions.querySelectorAll('.option-btn-mirror').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedMirrorType = button.dataset.value;

                // Show/hide custom input
                if (selectedMirrorType === 'custom') {
                    if (customTypeContainer) customTypeContainer.classList.remove('hidden');
                } else {
                    if (customTypeContainer) customTypeContainer.classList.add('hidden');
                }
            });
        }

        // Room style selection
        if (roomStyleOptions) {
            roomStyleOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                roomStyleOptions.querySelectorAll('.option-btn-mirror').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedRoomStyle = button.dataset.value;

                // Show/hide custom input
                if (selectedRoomStyle === 'custom') {
                    if (customStyleContainer) customStyleContainer.classList.remove('hidden');
                } else {
                    if (customStyleContainer) customStyleContainer.classList.add('hidden');
                }
            });
        }

        // Phone model selection
        if (phoneOptions) {
            phoneOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                phoneOptions.querySelectorAll('.option-btn-mirror').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedPhone = button.dataset.value;
            });
        }

        // Hand position selection
        if (handOptions) {
            handOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                handOptions.querySelectorAll('.option-btn-mirror').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedHand = button.dataset.value;
            });
        }

        // Lighting selection
        if (lightingOptions) {
            lightingOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                lightingOptions.querySelectorAll('.option-btn-mirror').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedLighting = button.dataset.value;
            });
        }

        // Ratio selection
        if (ratioOptions) {
            ratioOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                ratioOptions.querySelectorAll('.option-btn-mirror').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedRatio = button.dataset.value;
            });
        }

        // Generate mirror selfies
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (generateBtn.disabled) return;
                if (!productImageData) {
                    alert('Silakan upload foto produk terlebih dahulu');
                    return;
                }

                generateBtn.disabled = true;
                if (emptyState) emptyState.classList.add('hidden');
                if (resultsHeader) resultsHeader.classList.add('hidden');
                if (loading) loading.classList.remove('hidden');

                // Build base prompt
                let mirrorDesc = selectedMirrorType === 'custom'
                    ? (customTypeInput ? customTypeInput.value.trim() : '') || 'full-length body mirror, standing mirror against wall'
                    : mirrorTypeDescriptions[selectedMirrorType] || selectedMirrorType;

                let roomDesc = selectedRoomStyle === 'custom'
                    ? (customStyleInput ? customStyleInput.value.trim() : '') || 'modern minimalist interior, clean aesthetic'
                    : roomStyleDescriptions[selectedRoomStyle] || selectedRoomStyle;

                mirrorBasePrompt = `Buat foto mirror selfie POV yang aesthetic dan natural dengan produk yang diupload.

KOMPOSISI FOTO:
- ${mirrorDesc}
- ${roomDesc}
- ${phoneDescriptions[selectedPhone] || selectedPhone}
- ${handPositionDescriptions[selectedHand] || selectedHand}
- ${lightingDescriptions[selectedLighting] || selectedLighting}

PRODUK:
- Integrasikan produk yang diupload secara natural ke dalam foto (dipakai/dipegang/visible di foto)
- Produk harus terlihat jelas dan menarik
- Pose dan angle yang memperlihatkan produk dengan optimal${modelImageData ? `

MODEL:
- Gunakan orang dari foto model yang diupload sebagai subjek utama
- Pertahankan identitas, wajah, dan karakteristik fisik model EXACTLY seperti di foto referensi
- Model harus terlihat natural dan sesuai dengan gaya mirror selfie yang diminta` : ''}

STYLE FOTO:
- POV mirror selfie aesthetic seperti di Instagram/TikTok
- Natural dan casual vibe
- High quality DSLR photography
- Sharp details, proper depth of field
- Realistic human proportions and anatomy
- Professional color grading

PENTING:
- Mirror reflection harus realistic (person + room + products visible in mirror)
- Lighting harus consistent antara subjek dan reflection
- No distorted body proportions
- Natural human pose dan gesture
- Aesthetic composition dan framing`;

                let successCount = 0;
                let attempts = 0;
                const MAX_ATTEMPTS = 3;

                try {
                    while (attempts < MAX_ATTEMPTS && successCount === 0) {
                        await runMirrorGeneration();
                        successCount = generatedImages.filter(img => img).length;
                        attempts++;
                    }

                    if (loading) loading.classList.add('hidden');

                    if (successCount === 0) {
                        if (emptyState) emptyState.classList.remove('hidden');
                        alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                        return;
                    }

                    renderSuccessfulMirrorResults();
                } finally {
                    if (loading) loading.classList.add('hidden');
                    generateBtn.disabled = false;
                }
            });
        }

        async function runMirrorGeneration() {
            generatedImages = new Array(selectedCount).fill(null);
            if (resultsGrid) resultsGrid.innerHTML = '';

            const aspectClass = selectedRatio === '1:1' ? 'aspect-square'
                : selectedRatio === '16:9' ? 'aspect-video'
                : selectedRatio === '9:16' ? 'aspect-[9/16]'
                : 'aspect-[3/4]';

            for (let i = 1; i <= selectedCount; i++) {
                const card = document.createElement('div');
                card.id = `mirror-card-${i}`;
                card.className = `relative group rounded-xl overflow-hidden bg-gray-100 ${aspectClass} flex items-center justify-center`;
                card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2" style="border-color:#06b6d4;"></div>';
                if (resultsGrid) resultsGrid.appendChild(card);
            }

            if (resultsHeader) resultsHeader.classList.remove('hidden');
            if (window.innerWidth < 1024) {
                setTimeout(() => { if (resultsGrid) resultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' }); }, 300);
            }

            await Promise.allSettled(
                Array.from({ length: selectedCount }, (_, i) => generateSingleMirrorSelfie(i + 1))
            );
        }

        async function generateSingleMirrorSelfie(index) {
            const card = document.getElementById(`mirror-card-${index}`);
            const finalPrompt = `${mirrorBasePrompt}\n\nVariation ${index}: Add unique variation in pose, angle, or styling while maintaining the mirror selfie aesthetic.`;

            try {
                const base64Data = productImageData.split(',')[1];
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const parts = [
                    { text: finalPrompt },
                    { inlineData: { mimeType: 'image/jpeg', data: base64Data } }
                ];

                if (modelImageData) {
                    parts.push({ inlineData: { mimeType: 'image/jpeg', data: modelImageData.split(',')[1] } });
                }

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                    safetySettings: [
                        { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" }
                    ]
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${imageData}`;
                const filename = `mirror-selfie-${Date.now()}-${index}.png`;
                generatedImages[index - 1] = { url: imageUrl, filename };
                if (card) card.innerHTML = `<img src="${imageUrl}" class="w-full h-full object-cover" alt="Mirror Selfie ${index}">`;
            } catch (error) {
                console.error(`Error generating mirror selfie ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        function renderSuccessfulMirrorResults() {
            const successful = generatedImages.filter(img => img);
            generatedImages = successful;
            if (resultsGrid) resultsGrid.innerHTML = '';

            successful.forEach((imgData, idx) => {
                const card = document.createElement('div');
                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imgData.url}" class="w-full h-full object-cover" alt="Mirror Selfie ${idx + 1}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${idx}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${idx}" class="action-btn bg-gradient-to-r from-cyan-500 to-cyan-600 hover:from-cyan-600 hover:to-cyan-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
                if (resultsGrid) resultsGrid.appendChild(card);
            });

            if (resultsHeader) resultsHeader.classList.remove('hidden');
            if (resultsCount) resultsCount.textContent = successful.length;
            if (emptyState) emptyState.classList.add('hidden');
            if (window.innerWidth < 1024) {
                setTimeout(() => { if (resultsGrid) resultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' }); }, 300);
            }
        }

        // Action buttons (preview/download)
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation(); // Mobile-safe

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all button
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

    })();

    // ==================== WALKING PAD LIFESTYLE ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const modelInput = document.getElementById('walkpad-model-input');
        const modelBox = document.getElementById('walkpad-model-box');
        const modelPlaceholder = document.getElementById('walkpad-model-placeholder');
        const modelPreviewContainer = document.getElementById('walkpad-model-preview-container');
        const modelPreview = document.getElementById('walkpad-model-preview');
        const removeModelBtn = document.getElementById('walkpad-remove-model');
        const productInput = document.getElementById('walkpad-product-input');
        const productBox = document.getElementById('walkpad-product-box');
        const productPlaceholder = document.getElementById('walkpad-product-placeholder');
        const productPreviewContainer = document.getElementById('walkpad-product-preview-container');
        const productCountText = document.getElementById('walkpad-product-count');
        const removeProductBtn = document.getElementById('walkpad-remove-product');
        const roomOptions = document.getElementById('walkpad-room-options');
        const angleOptions = document.getElementById('walkpad-angle-options');
        const poseOptions = document.getElementById('walkpad-pose-options');
        const lightingOptions = document.getElementById('walkpad-lighting-options');
        const ratioOptions = document.getElementById('walkpad-ratio-options');
        const generateBtn = document.getElementById('walkpad-generate-btn');
        const resultsHeader = document.getElementById('walkpad-results-header');
        const resultsCount = document.getElementById('walkpad-results-count');
        const resultsGrid = document.getElementById('walkpad-results-grid');
        const emptyState = document.getElementById('walkpad-empty-state');
        const loadingDiv = document.getElementById('walkpad-loading');
        const loadingText = document.getElementById('walkpad-loading-text');
        const progressBar = document.getElementById('walkpad-progress-bar');
        const progressText = document.getElementById('walkpad-progress-text');
        const downloadAllBtn = document.getElementById('walkpad-download-all-btn');

        // State
        let modelImageData = null;
        let productImagesData = [];
        let selectedRoom = 'aesthetic-pink';
        let selectedAngle = 'diagonal-right';
        let selectedPose = 'walking-motion';
        let selectedLighting = 'natural-bright';
        let selectedRatio = '9:16';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedImages = [];
        let stopWalkpad = false;

        // Count selection (1-10), default 4
        const walkpadCountSel = document.getElementById('walkpad-count-selection-grid');
        if (walkpadCountSel) {
            walkpadCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                walkpadCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Description maps
        const roomDescriptions = {
            'aesthetic-pink': 'aesthetic pink bedroom with fairy lights, white bed with pink pillows, vanity mirror with soft lighting, pink and white decor, feminine cozy atmosphere',
            'korean-minimal': 'Korean minimalist bedroom, beige and white color palette, clean lines, K-style interior, neutral tones, elegant simplicity',
            'boho-natural': 'bohemian bedroom with rattan furniture, macrame wall hanging, potted plants, earth tone textiles, natural warm atmosphere',
            'luxury-white': 'luxury white marble bedroom, crystal chandelier, plush white bedding, gold accents, premium hotel suite aesthetic',
            'pastel-cute': 'pastel kawaii bedroom, colorful pastel decor, aesthetic shelves with collectibles, fairy lights, cute cozy atmosphere',
            'botanical-green': 'botanical bedroom filled with lush indoor plants, wooden furniture, natural light, green living wall, nature-inspired interior',
            'fashion-closet': 'walk-in boutique closet, clothes neatly arranged on racks, full-length mirror, fashion showroom atmosphere, warm spotlights',
            'scandinavian': 'Scandinavian bedroom, white and light wood, hygge cozy atmosphere, minimal Nordic decor, clean and airy',
            'dark-modern': 'dark gray masculine bedroom, sleek modern minimal design, dark walls, sophisticated moody atmosphere, contemporary style',
            'gaming-setup': 'gaming room with RGB LED lighting, multiple monitors on gaming desk, ergonomic gaming chair, dark immersive atmosphere, neon accents',
            'home-gym': 'home gym with professional fitness equipment, floor-to-ceiling mirrors, motivational fitness lifestyle setting, modern workout space',
            'industrial-loft': 'industrial loft interior, exposed brick wall, concrete floors, Edison bulb lighting, dark masculine urban atmosphere',
            'streetwear-room': 'streetwear hypebeast room, sneaker collection display shelves, urban wall art, street culture decor, modern youth aesthetic',
            'smart-home': 'sleek modern smart home interior, minimalist tech aesthetic, cool neutral tones, futuristic clean design, ambient lighting',
            'minimalist-gray': 'Japandi minimalist all-gray room, neutral gray tones throughout, clean simple lines, serene zen calm atmosphere',
            'white-studio': 'pure white seamless professional photography studio, bright even studio lighting, clean white backdrop, photo studio setting',
            'living-room': 'modern living room, stylish comfortable sofa, warm natural lighting, tasteful home interior, cozy lifestyle setting',
            'cafe-office': 'aesthetic home cafe office corner, wooden desk with coffee cup, surrounding plants and books, exposed brick accent wall, warm cozy work atmosphere'
        };

        const angleDescriptions = {
            'diagonal-right': '3/4 diagonal front-right camera angle, flattering full body fashion composition',
            'front-full': 'straight front-facing full body shot, centered symmetrical composition',
            'diagonal-left': '3/4 diagonal front-left camera angle, elegant fashion editorial perspective',
            'side-view': 'pure side profile lateral full body view, showcasing outfit silhouette',
            'low-angle': 'low angle camera looking upward, full body shot making subject appear taller',
            'bust-shot': 'upper body half-body shot from waist up, focusing on outfit top and face details'
        };

        const poseDescriptions = {
            'walking-motion': 'walking forward toward the camera, facing the camera directly, one foot stepping forward, arms swinging naturally at sides, dynamic lifestyle movement, full body facing front',
            'standing-casual': 'standing casually on the treadmill, facing directly toward camera, body facing front, relaxed pose with weight shifted slightly to one hip, hands at sides',
            'standing-confident': 'standing tall and confident, body and face fully facing toward the camera, strong upright posture, arms at sides, looking powerfully and directly into the camera lens'
        };

        const lightingDescriptions = {
            'natural-bright': 'natural bright daylight, soft window light, beautifully well-lit, clear fresh atmosphere',
            'warm-cozy': 'warm amber lighting, golden warm tones, cozy comfortable glow, inviting intimate atmosphere',
            'cool-white': 'cool white LED lighting, crisp clean modern studio lighting, bright and contemporary',
            'soft-diffused': 'soft diffused lighting, gentle even illumination, flattering smooth quality',
            'golden-hour': 'golden hour warm sunlight, late afternoon sunset glow, cinematic warm magical lighting'
        };

        // Read file as base64
        function readFileAsBase64(file) {
            return new Promise((resolve, reject) => {
                const reader = new FileReader();
                reader.onload = (e) => {
                    const base64 = e.target.result.split(',')[1];
                    resolve({ base64, mimeType: file.type || 'image/jpeg' });
                };
                reader.onerror = reject;
                reader.readAsDataURL(file);
            });
        }

        // Enable/disable generate button
        function updateGenerateBtn() {
            if (modelImageData && productImagesData.length > 0) {
                generateBtn.disabled = false;
                generateBtn.classList.remove('opacity-50', 'cursor-not-allowed');
            } else {
                generateBtn.disabled = true;
                generateBtn.classList.add('opacity-50', 'cursor-not-allowed');
            }
        }

        // Handle model photo upload
        async function handleModelUpload(file) {
            if (!file) return;
            try {
                let processedFile = file;
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    if (typeof heic2any !== 'undefined') {
                        const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }
                const { base64, mimeType } = await readFileAsBase64(processedFile);
                modelImageData = { base64, mimeType };
                const dataUrl = 'data:' + mimeType + ';base64,' + base64;
                modelPreview.src = dataUrl;
                modelPlaceholder.classList.add('hidden');
                modelPreviewContainer.classList.remove('hidden');
                updateGenerateBtn();
            } catch (err) {
                console.error('Model upload error:', err);
                alert('Gagal memproses foto model. Coba format JPG/PNG.');
            }
        }

        // Handle product photos upload (up to 5)
        async function handleProductUploads(files) {
            if (!files || !files.length) return;
            productImagesData = [];
            if (productPreviewContainer) { productPreviewContainer.innerHTML = ''; productPreviewContainer.classList.add('hidden'); }
            for (const file of Array.from(files).slice(0, 5)) {
                try {
                    let processedFile = file;
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        if (typeof heic2any !== 'undefined') {
                            const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                        }
                    }
                    const { base64, mimeType } = await readFileAsBase64(processedFile);
                    productImagesData.push({ base64, mimeType });
                    const dataUrl = 'data:' + mimeType + ';base64,' + base64;
                    if (productPreviewContainer) {
                        const img = document.createElement('img');
                        img.src = dataUrl;
                        img.className = 'w-full h-20 object-cover rounded-lg border-2';
                        img.style.borderColor = '#db2777';
                        productPreviewContainer.appendChild(img);
                        productPreviewContainer.classList.remove('hidden');
                    }
                } catch (err) {
                    console.error('Product upload error:', err);
                }
            }
            if (productPlaceholder) productPlaceholder.classList.add('hidden');
            if (productCountText) productCountText.textContent = productImagesData.length + '/5 foto';
            if (removeProductBtn) removeProductBtn.classList.remove('hidden');
            updateGenerateBtn();
        }

        // Model upload events
        if (modelInput) {
            modelInput.addEventListener('change', (e) => {
                if (e.target.files[0]) handleModelUpload(e.target.files[0]);
            });
        }
        if (modelBox) {
            modelBox.addEventListener('click', () => modelInput && modelInput.click());
        }
        if (removeModelBtn) {
            removeModelBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                modelImageData = null;
                if (modelInput) modelInput.value = '';
                modelPreviewContainer.classList.add('hidden');
                modelPlaceholder.classList.remove('hidden');
                updateGenerateBtn();
            });
        }

        // Product upload events
        if (productInput) {
            productInput.addEventListener('change', (e) => {
                if (e.target.files.length) handleProductUploads(e.target.files);
            });
        }
        if (productBox) {
            productBox.addEventListener('click', () => productInput && productInput.click());
        }
        if (removeProductBtn) {
            removeProductBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                productImagesData = [];
                if (productInput) productInput.value = '';
                if (productPreviewContainer) { productPreviewContainer.innerHTML = ''; productPreviewContainer.classList.add('hidden'); }
                if (productPlaceholder) productPlaceholder.classList.remove('hidden');
                if (productCountText) productCountText.textContent = '0/5 foto';
                removeProductBtn.classList.add('hidden');
                updateGenerateBtn();
            });
        }

        // Option buttons setup
        function setupOptions(container, callback) {
            if (!container) return;
            container.querySelectorAll('[data-value]').forEach(btn => {
                btn.addEventListener('click', () => {
                    container.querySelectorAll('[data-value]').forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    callback(btn.dataset.value);
                });
            });
        }

        if (roomOptions) setupOptions(roomOptions, (v) => { selectedRoom = v; });
        if (angleOptions) setupOptions(angleOptions, (v) => { selectedAngle = v; });
        if (poseOptions) setupOptions(poseOptions, (v) => { selectedPose = v; });
        if (lightingOptions) setupOptions(lightingOptions, (v) => { selectedLighting = v; });
        if (ratioOptions) setupOptions(ratioOptions, (v) => { selectedRatio = v; });

        // Step 1: Analyze model photo to get detailed text description (same approach as Foto Produk Affiliate)
        async function analyzeModelPhoto() {
            const textApiUrl = 'https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=' + apiKey;
            const systemPrompt = 'You are an expert at analyzing people in photos. Describe this person in detail for use in an AI image generation prompt. Include: gender, approximate age range, skin tone (use specific descriptors like "warm medium-brown", "light fair", "deep dark"), hair color and style, eye shape, face shape, distinctive facial features, body build, and height impression. Be specific and detailed. Write ONLY the description in English, no preamble. Example: "Indonesian male, mid-20s, warm medium-brown skin tone, short straight black hair, almond-shaped dark brown eyes, oval face, defined jawline, medium build, approximately 170cm height impression."';
            const payload = {
                contents: [{ parts: [{ text: systemPrompt }, { inlineData: { mimeType: modelImageData.mimeType, data: modelImageData.base64 } }] }],
                generationConfig: { maxOutputTokens: 200 }
            };
            try {
                const res = await fetch(textApiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!res.ok) throw new Error('Analysis API error');
                const data = await res.json();
                return data?.candidates?.[0]?.content?.parts?.[0]?.text?.trim() || '';
            } catch (e) {
                console.warn('Model analysis failed, using fallback:', e);
                return '';
            }
        }

        // Step 2: Generate single image — fail-silent pattern (Membuat Model style)
        async function generateSingleWalkpad(index, modelDescription) {
            if (stopWalkpad) return;
            const apiUrl = 'https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=' + apiKey;
            const card = resultsGrid.querySelector('[data-card-index="' + index + '"]');
            try {
                const roomDesc = roomDescriptions[selectedRoom] || selectedRoom;
                const angleDesc = angleDescriptions[selectedAngle] || selectedAngle;
                const poseDesc = poseDescriptions[selectedPose] || selectedPose;
                const lightDesc = lightingDescriptions[selectedLighting] || selectedLighting;

                const productCount = productImagesData.length;
                const outfitRef = productCount > 1
                    ? 'the ' + productCount + ' clothing reference photos (images 2 to ' + (productCount + 1) + '). Study ALL of them to reconstruct the complete outfit'
                    : 'the clothing reference photo (image 2)';

                const identityLine = modelDescription
                    ? 'SUBJECT IDENTITY: ' + modelDescription + '. This is the ONLY person allowed in this image.'
                    : 'SUBJECT IDENTITY: Use the exact person shown in image 1 as the subject.';

                const prompt =
                    identityLine + '\n\n' +
                    '=== NON-NEGOTIABLE FACE & BODY RULES ===\n' +
                    'Image 1 is the model reference. YOU MUST reproduce this EXACT person:\n' +
                    '- FACE: same face shape, same eyes, same nose, same lips, same skin tone — DO NOT alter any facial feature\n' +
                    '- GENDER: keep the EXACT gender shown in image 1 — do not change gender under any circumstances\n' +
                    '- HAIR: same hair color, same hair texture, same hairstyle\n' +
                    '- BODY: same body proportions, same height impression, same skin tone on all exposed skin\n\n' +
                    '=== SCENE ===\n' +
                    'Outfit: recreate EXACTLY the clothing from ' + outfitRef + ' — identical colors, patterns, fabric texture, cut\n' +
                    'Action: the person is ' + poseDesc + ' on a modern slim walking pad treadmill, body facing FORWARD toward the camera\n' +
                    'Walking pad orientation (CRITICAL — must be physically correct for video usage): the FRONT of the walking pad (the end with the LED display / control console / digital readout, AND any vertical handlebars / safety rails / upright support poles if the pad has them) MUST be on the CAMERA SIDE — in the foreground, IN FRONT OF the person, in the same direction the person is facing and walking toward. The running belt extends BEHIND the person, AWAY from the camera. The person walks TOWARD the LED console and TOWARD any handlebars (i.e. toward the camera). NEVER place the LED display, the control panel, or the handlebars BEHIND the person or AT THE BACK END of the pad — this would make the model appear to walk backward off the treadmill, which is physically wrong and unusable for image-to-video.\n' +
                    'Setting: ' + roomDesc + '\n' +
                    'Camera: ' + angleDesc + ', shooting from FRONT so the model\'s face is fully visible\n' +
                    'Lighting: ' + lightDesc + '\n' +
                    'Framing: full body shot, whole walking pad visible, professional fashion lifestyle photography, high resolution, photorealistic';

                const payload = {
                    contents: [{
                        parts: [{ text: prompt }, { inlineData: { mimeType: modelImageData.mimeType, data: modelImageData.base64 } }]
                            .concat(productImagesData.map(function(p) { return { inlineData: { mimeType: p.mimeType, data: p.base64 } }; }))
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                    safetySettings: [
                        { category: 'HARM_CATEGORY_HARASSMENT', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_HATE_SPEECH', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_SEXUALLY_EXPLICIT', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_DANGEROUS_CONTENT', threshold: 'BLOCK_NONE' }
                    ]
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');

                const imageUrl = 'data:image/png;base64,' + imageData;
                const filename = 'walking-pad-' + selectedRoom + '-' + index + '-' + Date.now() + '.jpg';
                generatedImages[index - 1] = { url: imageUrl, filename: filename };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = '<img src="' + imageUrl + '" class="w-full h-auto object-cover" alt="Walking Pad ' + index + '">' +
                        '<div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">' +
                        '<button data-action="preview" data-index="' + (index - 1) + '" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2"><i class="fas fa-eye"></i><span class="hidden sm:inline ml-1">Preview</span></button>' +
                        '<button data-action="download" data-index="' + (index - 1) + '" class="action-btn text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2" style="background:linear-gradient(135deg,#7c3aed 0%,#db2877 100%)"><i class="fas fa-download"></i><span class="hidden sm:inline ml-1">Download</span></button>' +
                        '</div>';
                }
            } catch (error) {
                console.error('Error generating walking pad photo ' + index + ':', error);
                if (card) card.innerHTML = '';
            }
        }

        async function runWalkpadGeneration(modelDescription) {
            generatedImages = new Array(selectedCount).fill(null);
            resultsGrid.innerHTML = '';
            for (let i = 1; i <= selectedCount; i++) {
                const card = document.createElement('div');
                card.dataset.cardIndex = i;
                card.className = 'bg-gray-100 rounded-xl flex items-center justify-center min-h-64 animate-pulse';
                card.innerHTML = '<div class="text-center text-gray-400 p-4"><i class="fas fa-spinner fa-spin text-2xl mb-2"></i><p class="text-xs mt-1">Foto ' + i + '</p></div>';
                resultsGrid.appendChild(card);
            }
            const promises = Array.from({ length: selectedCount }, (_, i) => generateSingleWalkpad(i + 1, modelDescription));
            await Promise.allSettled(promises);
        }

        function renderSuccessfulWalkpadResults() {
            const successful = generatedImages.filter(img => img);
            generatedImages = successful;
            resultsGrid.innerHTML = '';
            successful.forEach((img, idx) => {
                const card = document.createElement('div');
                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = '<img src="' + img.url + '" class="w-full h-auto object-cover" alt="Walking Pad ' + (idx + 1) + '">' +
                    '<div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">' +
                    '<button data-action="preview" data-index="' + idx + '" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2"><i class="fas fa-eye"></i><span class="hidden sm:inline ml-1">Preview</span></button>' +
                    '<button data-action="download" data-index="' + idx + '" class="action-btn text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2" style="background:linear-gradient(135deg,#7c3aed 0%,#db2877 100%)"><i class="fas fa-download"></i><span class="hidden sm:inline ml-1">Download</span></button>' +
                    '</div>';
                resultsGrid.appendChild(card);
            });
            resultsCount.textContent = successful.length;
            downloadAllBtn.classList.remove('hidden');
        }

        // Main generate handler
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!modelImageData || !productImagesData.length) {
                    alert('Upload foto model dan foto produk/baju terlebih dahulu.');
                    return;
                }

                stopWalkpad = false;
                emptyState.classList.add('hidden');
                loadingDiv.classList.remove('hidden');
                resultsHeader.classList.remove('hidden');
                downloadAllBtn.classList.add('hidden');
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Menganalisa model...';
                progressBar.style.width = '0%';
                progressText.textContent = '0 / ' + selectedCount;

                loadingText.textContent = 'Menganalisa foto model...';
                const modelDescription = await analyzeModelPhoto();

                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
                loadingText.textContent = 'Membuat foto walking pad...';

                let successCount = 0;
                let attempts = 0;
                const MAX_ATTEMPTS = 3;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    await runWalkpadGeneration(modelDescription);
                    successCount = generatedImages.filter(img => img).length;
                    attempts++;
                }

                loadingDiv.classList.add('hidden');
                generateBtn.disabled = false;
                generateBtn.innerHTML = '<i class="fas fa-walking mr-3 text-xl"></i>Generate Foto Walking Pad';
                updateGenerateBtn();

                if (successCount === 0) {
                    if (emptyState) emptyState.classList.remove('hidden');
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    return;
                }

                renderSuccessfulWalkpadResults();
            });
        }

        // Results click handler
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();
                const btn = e.target.closest('[data-action]');
                if (!btn) return;
                const index = parseInt(btn.dataset.index);
                const imgData = generatedImages[index];
                if (!imgData) return;
                if (btn.dataset.action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imgData.url);
                    }
                } else if (btn.dataset.action === 'download') {
                    if (window.downloadDataURINew) await window.downloadDataURINew(imgData.url, imgData.filename);
                    else if (window.downloadImage) await window.downloadImage(imgData.url, imgData.filename);
                }
            });
        }

        // Download all handler
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async () => {
                const validImages = generatedImages.filter(function(img) { return img !== null; });
                if (validImages.length === 0) return;
                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;
                downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';
                for (const img of validImages) {
                    if (window.downloadDataURINew) await window.downloadDataURINew(img.url, img.filename);
                    else if (window.downloadImage) await window.downloadImage(img.url, img.filename);
                    await new Promise(function(r) { setTimeout(r, 300); });
                }
                setTimeout(() => {
                    downloadAllBtn.disabled = false;
                    downloadAllBtn.innerHTML = originalHTML;
                }, 2000);
            });
        }

        // Tab initialization
        const tabDesktop = document.querySelector('.main-tab-btn[data-tab="walking-pad"]');
        const tabMobile = document.querySelector('.mobile-tab-btn[data-tab="walking-pad"]');
        if (tabDesktop) tabDesktop.addEventListener('click', function() {});
        if (tabMobile) tabMobile.addEventListener('click', function() {});

    })();

    // ==================== UPSCALE (paid feature, ESRGAN via fal.ai) ====================
    (function() {
        // FAL_KEY: API key dari fal.ai dashboard. Repo private, tapi tetap WAJIB
        // set spending cap di fal.ai dashboard supaya damage ter-cap kalau leak.
        const FAL_KEY = "33808e5d-5e98-4bb9-b3ae-a964eae1a07d:d92050667a4f58031330817634085869";
        const FAL_ESRGAN_ENDPOINT = "https://queue.fal.run/fal-ai/esrgan";

        const uploadBox = document.getElementById('upscale-upload-box');
        const fileInput = document.getElementById('upscale-input');
        const placeholder = document.getElementById('upscale-placeholder');
        const previewContainer = document.getElementById('upscale-preview-container');
        const previewImg = document.getElementById('upscale-preview');
        const filenameLabel = document.getElementById('upscale-filename');
        const removeBtn = document.getElementById('upscale-remove-btn');
        const scaleOptions = document.getElementById('upscale-scale-options');
        const generateBtn = document.getElementById('upscale-generate-btn');
        const emptyState = document.getElementById('upscale-empty-state');
        const loadingDiv = document.getElementById('upscale-loading');
        const resultDiv = document.getElementById('upscale-result');
        const resultImg = document.getElementById('upscale-result-img');
        const downloadBtn = document.getElementById('upscale-download-btn');
        const previewBtn = document.getElementById('upscale-preview-btn');
        if (!uploadBox || !generateBtn) return;

        let sourceImageData = null;
        let sourceFilename = '';
        let selectedScale = 2;
        let resultUrl = null;

        function readAsBase64(file) {
            return new Promise(function(resolve, reject) {
                var reader = new FileReader();
                reader.onload = function(e) { resolve(e.target.result); };
                reader.onerror = reject;
                reader.readAsDataURL(file);
            });
        }

        async function handleUpload(file) {
            if (!file) return;
            if (file.size > 10 * 1024 * 1024) {
                alert('Foto terlalu besar. Maksimal 10 MB.');
                return;
            }
            try {
                let processedFile = file;
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    if (typeof heic2any !== 'undefined') {
                        const converted = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([converted], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }
                sourceImageData = await readAsBase64(processedFile);
                sourceFilename = processedFile.name || 'upscale-source.jpg';
                previewImg.src = sourceImageData;
                filenameLabel.textContent = sourceFilename;
                placeholder.classList.add('hidden');
                previewContainer.classList.remove('hidden');
                generateBtn.disabled = false;
            } catch (err) {
                console.error('Upscale upload error:', err);
                alert('Gagal memproses foto. Coba format JPG/PNG.');
            }
        }

        if (fileInput) {
            fileInput.addEventListener('change', function(e) {
                if (e.target.files[0]) handleUpload(e.target.files[0]);
            });
        }
        if (uploadBox) {
            uploadBox.addEventListener('click', function(e) {
                if (e.target.closest('button')) return;
                fileInput.click();
            });
        }
        if (removeBtn) {
            removeBtn.addEventListener('click', function(e) {
                e.stopPropagation();
                sourceImageData = null;
                sourceFilename = '';
                fileInput.value = '';
                previewContainer.classList.add('hidden');
                placeholder.classList.remove('hidden');
                generateBtn.disabled = true;
            });
        }

        if (scaleOptions) {
            scaleOptions.addEventListener('click', function(e) {
                const btn = e.target.closest('button[data-scale]');
                if (!btn) return;
                Array.prototype.forEach.call(scaleOptions.querySelectorAll('button'), function(b) {
                    b.classList.remove('selected', 'border-purple-500', 'bg-purple-50');
                    b.classList.add('border-gray-200', 'bg-white');
                    var hint = b.querySelector('.text-xs');
                    if (hint) { hint.classList.remove('text-purple-600'); hint.classList.add('text-gray-500'); }
                    var num = b.querySelector('.text-2xl');
                    if (num) { num.classList.remove('text-purple-700'); num.classList.add('text-gray-700'); }
                });
                btn.classList.add('selected', 'border-purple-500', 'bg-purple-50');
                btn.classList.remove('border-gray-200', 'bg-white');
                var hint = btn.querySelector('.text-xs');
                if (hint) { hint.classList.add('text-purple-600'); hint.classList.remove('text-gray-500'); }
                var num = btn.querySelector('.text-2xl');
                if (num) { num.classList.add('text-purple-700'); num.classList.remove('text-gray-700'); }
                selectedScale = parseInt(btn.dataset.scale, 10);
            });
        }

        
        
        );
        }

        async function consumeQuota() {
            const email = localStorage.getItem('affiliatego_email');
            const token = localStorage.getItem('affiliatego_token');
            if (!email || !token) return { ok: false, reason: 'not_logged_in' };
            try {
                const url = SCRIPT_URL + '?action=use_upscale'
                    + '&email=' + encodeURIComponent(email)
                    + '&token=' + encodeURIComponent(token)
                    + '&app_secret=' + encodeURIComponent(APP_SECRET)
                    + '&product=' + encodeURIComponent(PRODUCT_ID);
                const res = await fetch(url);
                const data = await res.json();
                if (data.status === 'SUKSES') return { ok: true };
                if (data.quota_exhausted) return { ok: false, reason: 'exhausted' };
                return { ok: false, reason: 'other', message: data.message };
            } catch (err) {
                console.error('Upscale quota check failed:', err);
                return { ok: false, reason: 'network' };
            }
        }

        async function refundQuota() {
            const email = localStorage.getItem('affiliatego_email');
            const token = localStorage.getItem('affiliatego_token');
            if (!email || !token) return;
            try {
                const url = SCRIPT_URL + '?action=refund_upscale'
                    + '&email=' + encodeURIComponent(email)
                    + '&token=' + encodeURIComponent(token)
                    + '&app_secret=' + encodeURIComponent(APP_SECRET)
                    + '&product=' + encodeURIComponent(PRODUCT_ID);
                await fetch(url);
            } catch (err) {
                console.error('Upscale refund failed:', err);
            }
        }

        async function callFalEsrgan(imageDataUri, scale) {
            if (!FAL_KEY || FAL_KEY === "YOUR_FAL_KEY_HERE") {
                throw new Error('FAL_KEY belum di-set');
            }
            const headers = {
                'Content-Type': 'application/json',
                'Authorization': 'Key ' + FAL_KEY
            };
            const submitRes = await fetch(FAL_ESRGAN_ENDPOINT, {
                method: 'POST',
                headers: headers,
                body: JSON.stringify({
                    image_url: imageDataUri,
                    scale: scale,
                    model: 'RealESRGAN_x4plus',
                    output_format: 'jpeg'
                })
            });
            if (!submitRes.ok) {
                const errText = await submitRes.text().catch(function() { return ''; });
                throw new Error('Submit gagal: HTTP ' + submitRes.status + ' ' + errText.slice(0, 200));
            }
            const submitData = await submitRes.json();
            console.log('[Upscale] Submit response:', submitData);
            const requestId = submitData.request_id;
            if (!requestId) throw new Error('Tidak ada request_id di response submit');

            // Pakai status_url & response_url dari submit response kalau ada (lebih reliable),
            // fallback ke URL pattern manual.
            const statusUrl = submitData.status_url || (FAL_ESRGAN_ENDPOINT + '/requests/' + requestId + '/status');
            const finalUrl = submitData.response_url || (FAL_ESRGAN_ENDPOINT + '/requests/' + requestId);
            const authHeaders = { 'Authorization': 'Key ' + FAL_KEY };
            const maxPolls = 80; // 80 x 1.5s = 120 detik max (cover cold start + queue + 4K)
            let lastStatus = 'UNKNOWN';
            let lastError = null;
            for (let i = 0; i < maxPolls; i++) {
                await new Promise(function(r) { setTimeout(r, 1500); });
                let statusData = null;
                try {
                    const statusRes = await fetch(statusUrl, { headers: authHeaders });
                    if (!statusRes.ok) {
                        lastError = 'HTTP ' + statusRes.status;
                        if (i % 5 === 0) console.log('[Upscale] Poll #' + i + ' failed:', lastError);
                        continue;
                    }
                    statusData = await statusRes.json();
                } catch (netErr) {
                    lastError = netErr.message;
                    if (i % 5 === 0) console.log('[Upscale] Poll #' + i + ' network error:', lastError);
                    continue;
                }
                lastStatus = statusData.status || 'UNKNOWN';
                if (i % 3 === 0) console.log('[Upscale] Poll #' + i + ' status:', lastStatus);
                if (lastStatus === 'COMPLETED') {
                    const finalRes = await fetch(finalUrl, { headers: authHeaders });
                    if (!finalRes.ok) throw new Error('Fetch result gagal: HTTP ' + finalRes.status);
                    const finalData = await finalRes.json();
                    console.log('[Upscale] Final response:', finalData);
                    const imageUrl = finalData.image && finalData.image.url;
                    if (!imageUrl) throw new Error('Tidak ada image.url di response final');
                    return imageUrl;
                }
                if (lastStatus === 'FAILED' || lastStatus === 'ERROR') {
                    throw new Error('Upscale gagal di server fal.ai');
                }
                // IN_QUEUE atau IN_PROGRESS atau status lain → continue polling
            }
            throw new Error('Timeout setelah 120 detik. Last status: ' + lastStatus + (lastError ? ' (last error: ' + lastError + ')' : ''));
        }

        if (generateBtn) {
            generateBtn.addEventListener('click', async function() {
                if (!sourceImageData) return;

                generateBtn.disabled = true;
                emptyState.classList.add('hidden');
                resultDiv.classList.add('hidden');
                loadingDiv.classList.remove('hidden');

                const consume = await consumeQuota();
                if (!consume.ok) {
                    loadingDiv.classList.add('hidden');
                    emptyState.classList.remove('hidden');
                    generateBtn.disabled = false;
                    if (consume.reason === 'exhausted') {
                        alert('Kuota habis. Silakan hubungi admin.');
                    } else if (consume.reason === 'not_logged_in') {
                        alert('Silakan login terlebih dahulu.');
                    } else if (consume.reason === 'network') {
                        alert('Gagal connect ke server. Cek koneksi internet.');
                    } else {
                        alert(consume.message || 'Gagal menggunakan kuota.');
                    }
                    return;
                }

                try {
                    const upscaledUrl = await callFalEsrgan(sourceImageData, selectedScale);
                    resultUrl = upscaledUrl;
                    resultImg.src = upscaledUrl;
                    loadingDiv.classList.add('hidden');
                    resultDiv.classList.remove('hidden');
                } catch (err) {
                    console.error('Upscale error:', err);
                    await refundQuota();
                    loadingDiv.classList.add('hidden');
                    emptyState.classList.remove('hidden');
                    alert('Gagal upscale: ' + err.message + '\n\nKuota dikembalikan, coba lagi.');
                } finally {
                    generateBtn.disabled = false;
                }
            });
        }

        if (downloadBtn) {
            downloadBtn.addEventListener('click', async function() {
                if (!resultUrl) return;
                const filename = 'upscale-' + selectedScale + 'x-' + Date.now() + '.jpg';
                try {
                    if (window.downloadDataURINew) await window.downloadDataURINew(resultUrl, filename);
                    else if (window.downloadImage) await window.downloadImage(resultUrl, filename);
                    else window.open(resultUrl, '_blank');
                } catch (err) {
                    console.error('Upscale download error:', err);
                    window.open(resultUrl, '_blank');
                }
            });
        }

        if (previewBtn) {
            previewBtn.addEventListener('click', function() {
                if (!resultUrl) return;
                if (window.showPreviewModal) window.showPreviewModal(resultUrl);
                else window.open(resultUrl, '_blank');
            });
        }
    })();

    // ==================== SKETSA KE KATALOG PRODUK ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const sketchInput = document.getElementById('sketch-input');
        const sketchUploadBox = document.getElementById('sketch-upload-box');
        const sketchPlaceholder = document.getElementById('sketch-placeholder');
        const sketchPreviewContainer = document.getElementById('sketch-preview-container');
        const sketchPreview = document.getElementById('sketch-preview');
        const removeSketchPreview = document.getElementById('sketch-remove-preview');

        const productTypeOptions = document.getElementById('sketch-product-type-options');
        const catalogStyleOptions = document.getElementById('sketch-catalog-style-options');
        const angleOptions = document.getElementById('sketch-angle-options');
        const backgroundOptions = document.getElementById('sketch-background-options');
        const lightingOptions = document.getElementById('sketch-lighting-options');
        const ratioOptions = document.getElementById('sketch-ratio-options');
        const countSelection = document.getElementById('sketch-count-selection');
        const countText = document.getElementById('sketch-count-text');

        const generateBtn = document.getElementById('sketch-generate-btn');
        const resultsHeader = document.getElementById('sketch-results-header');
        const resultsCount = document.getElementById('sketch-results-count');
        const resultsGrid = document.getElementById('sketch-results-grid');
        const emptyState = document.getElementById('sketch-empty-state');
        const loading = document.getElementById('sketch-loading');
        const loadingText = document.getElementById('sketch-loading-text');
        const downloadAllBtn = document.getElementById('sketch-download-all-btn');

        const progressContainer = document.getElementById('sketch-progress-container');
        const progressBar = document.getElementById('sketch-progress-bar');
        const progressText = document.getElementById('sketch-progress-text');
        const progressPercentage = document.getElementById('sketch-progress-percentage');

        // State
        let sketchImageData = null;
        let selectedProductType = 'dress';
        let selectedCatalogStyle = 'studio-white';
        let selectedAngle = 'front';
        let selectedBackground = 'pure-white';
        let selectedLighting = 'studio-soft';
        let selectedRatio = '1:1';
        let selectedCount = 6;
        let generatedImages = [];

        // Product type descriptions
        const productTypeDescriptions = {
            'dress': 'elegant dress/gown, formal wear, beautiful feminine garment',
            'shirt': 'shirt/t-shirt, casual top wear, comfortable clothing',
            'jersey': 'sports jersey, athletic wear, team uniform, performance apparel',
            'pants': 'pants/trousers, bottom wear, stylish leg wear',
            'jacket': 'jacket/outer wear, layering piece, stylish outerwear',
            'shoes': 'shoes/footwear, stylish kicks, premium sneakers or formal shoes',
            'bag': 'bag/handbag, fashion accessory, stylish carry item',
            'accessories': 'fashion accessories, jewelry, belts, scarves, or other accent pieces'
        };

        // Catalog style descriptions
        const catalogStyleDescriptions = {
            'studio-white': 'professional studio photography with pure white background, clean minimalist aesthetic, e-commerce style, product-focused, high-end catalog look',
            'mannequin': 'mannequin display, department store style presentation, neutral mannequin form, professional retail display',
            'model-pro': 'professional fashion model wearing the product, runway style, editorial fashion photography, model pose and styling',
            'flat-lay': 'flat lay photography, top-down view, product laid flat on surface, Instagram aesthetic, styled composition',
            'lifestyle': 'lifestyle setting, real-life context, natural environment, in-use product photography, relatable scene',
            'hanger': 'product on hanger, retail store display style, hanging presentation, boutique aesthetic',
            'ghost': 'ghost mannequin effect, invisible mannequin technique, 3D product photography, hollow mannequin style',
            'outdoor': 'outdoor scene photography, natural setting, environmental context, lifestyle outdoor shoot',
            'luxury-premium': 'luxury premium catalog style, high-end designer aesthetic, sophisticated elegant presentation, premium materials visible, upscale boutique look, refined and exclusive feel, luxury brand catalog quality',
            'editorial-fashion': 'editorial fashion magazine style, Vogue/Harper\'s Bazaar aesthetic, artistic creative direction, dynamic poses, high-fashion editorial photography, magazine spread quality, avant-garde presentation',
            'street-style': 'street style urban photography, authentic city vibe, cool casual aesthetic, modern streetwear presentation, urban background context, trendy contemporary look, influencer-style catalog',
            'minimal-modern': 'minimal modern aesthetic, clean simple lines, contemporary design, Scandinavian minimalism, monochrome elegance, understated sophistication, less-is-more approach, modern art gallery vibe',
            'vintage-classic': 'vintage classic catalog style, retro nostalgic aesthetic, timeless elegant presentation, heritage brand feel, classic photography techniques, warm vintage tones, traditional catalog look',
            'creative-artistic': 'creative artistic presentation, bold experimental styling, unique unconventional angles, artistic expression, conceptual photography, gallery-worthy aesthetic, innovative creative direction',
            'sports-performance': 'sports performance catalog, athletic dynamic presentation, action-oriented styling, performance apparel showcase, energetic sporty vibe, athletic model in motion, fitness catalog quality',
            'ecommerce-clean': 'e-commerce optimized clean style, Amazon/Shopee catalog standard, clear product visibility, no distractions, conversion-focused presentation, online shopping optimized, marketplace-ready format',
            'boutique-display': 'boutique retail display style, curated sophisticated presentation, high-street fashion aesthetic, premium store window vibe, carefully styled arrangement, boutique shopping experience',
            'fashion-week': 'fashion week runway style, haute couture presentation, designer showcase aesthetic, catwalk ready look, fashion show quality, runway model styling, high-fashion industry standard',
            'magazine-cover': 'magazine cover worthy presentation, glossy editorial quality, cover star aesthetic, bold impactful styling, attention-grabbing composition, newsstand-ready look, professional magazine photography',
            'social-media': 'social media optimized style, Instagram-worthy aesthetic, shareable viral-ready presentation, influencer content quality, thumb-stopping visual, engagement-focused styling, TikTok/IG feed optimized'
        };

        // Angle descriptions
        const angleDescriptions = {
            'front': 'front view, facing camera directly, main product view, frontal perspective',
            'back': 'back view, rear perspective, backside details visible, reverse angle',
            'side': 'side profile view, lateral perspective, product from the side',
            '3-4-view': '3/4 view angle, diagonal perspective, dynamic product angle, professional catalog angle',
            'detail': 'extreme close-up, detail shot, macro photography, showing fabric texture and fine details',
            'full-360': 'multiple angles compilation, 360-degree view, all perspectives shown, comprehensive product view'
        };

        // Background descriptions
        const backgroundDescriptions = {
            'pure-white': 'pure white background #FFFFFF, clean white backdrop, e-commerce white, professional studio white',
            'gradient': 'subtle gradient background, soft color transition, elegant backdrop with depth',
            'studio-gray': 'neutral gray studio background, professional photography backdrop, soft gray tone',
            'natural': 'natural setting background, environmental context, real-world scene, organic backdrop'
        };

        // Lighting descriptions
        const lightingDescriptions = {
            'studio-soft': 'soft studio lighting, diffused light, even illumination, professional photography lighting, no harsh shadows',
            'high-key': 'high-key bright lighting, overexposed bright look, airy feel, minimal shadows, light and fresh',
            'dramatic': 'dramatic lighting, high contrast, strong shadows, moody atmosphere, editorial style lighting',
            'natural-light': 'natural daylight, window light, soft natural illumination, realistic lighting'
        };

        // Upload sketch
        if (sketchInput) {
            sketchInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    sketchImageData = reader.result;

                    // Show preview on mobile
                    if (sketchPreview && sketchPreviewContainer) {
                        sketchPreview.src = reader.result;
                        sketchPreviewContainer.classList.remove('hidden');
                    }

                    // Update placeholder
                    if (sketchPlaceholder) {
                        sketchPlaceholder.innerHTML = `
                            <i class="fas fa-check-circle text-purple-500" style="font-size: 2.5rem;"></i>
                            <p class="mt-2 text-sm font-semibold text-purple-600">Sketsa berhasil diupload!</p>
                        `;
                    }

                    // Enable generate button
                    if (generateBtn) generateBtn.disabled = false;
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove preview
        if (removeSketchPreview) {
            removeSketchPreview.addEventListener('click', () => {
                sketchImageData = null;
                if (sketchInput) sketchInput.value = '';
                if (sketchPreviewContainer) sketchPreviewContainer.classList.add('hidden');
                if (sketchPreview) sketchPreview.src = '';
                if (sketchPlaceholder) {
                    sketchPlaceholder.innerHTML = `
                        <i class="fas fa-pen-nib transition-transform duration-300" style="font-size: 2.5rem; color: #8b5cf6;"></i>
                        <p class="mt-2 text-sm font-semibold">✏️ Klik untuk upload sketsa</p>
                        <p class="mt-1 text-xs text-gray-400">Sketsa gaun, jersey, sepatu, atau produk fashion lainnya</p>
                    `;
                }
                if (generateBtn) generateBtn.disabled = true;
            });
        }

        // Product type selection
        if (productTypeOptions) {
            productTypeOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                productTypeOptions.querySelectorAll('.option-btn-sketch').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedProductType = button.dataset.value;
            });
        }

        // Catalog style selection
        if (catalogStyleOptions) {
            catalogStyleOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                catalogStyleOptions.querySelectorAll('.option-btn-sketch').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedCatalogStyle = button.dataset.value;
            });
        }

        // Angle selection
        if (angleOptions) {
            angleOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                angleOptions.querySelectorAll('.option-btn-sketch').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedAngle = button.dataset.value;
            });
        }

        // Background selection
        if (backgroundOptions) {
            backgroundOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                backgroundOptions.querySelectorAll('.option-btn-sketch').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedBackground = button.dataset.value;
            });
        }

        // Lighting selection
        if (lightingOptions) {
            lightingOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                lightingOptions.querySelectorAll('.option-btn-sketch').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedLighting = button.dataset.value;
            });
        }

        // Ratio selection
        if (ratioOptions) {
            ratioOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                ratioOptions.querySelectorAll('.option-btn-sketch').forEach(btn => {
                    btn.classList.remove('selected');
                });
                button.classList.add('selected');
                selectedRatio = button.dataset.value;
            });
        }

        // Count selection
        if (countSelection) {
            countSelection.addEventListener('click', (e) => {
                const button = e.target.closest('[data-count]');
                if (!button) return;

                // Remove selected state from all buttons
                document.querySelectorAll('.sketch-count-btn').forEach(btn => {
                    btn.classList.remove('sketch-count-selected');
                });

                // Add selected state to clicked button
                button.classList.add('sketch-count-selected');

                // Update count
                selectedCount = parseInt(button.dataset.count, 10);
                if (countText) countText.textContent = selectedCount;
            });
        }

        // Generate catalog photos
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!sketchImageData) {
                    alert('Silakan upload sketsa terlebih dahulu');
                    return;
                }

                // Hide empty state, show loading
                if (emptyState) emptyState.classList.add('hidden');
                if (loading) loading.classList.remove('hidden');
                if (resultsHeader) resultsHeader.classList.add('hidden');
                if (resultsGrid) resultsGrid.innerHTML = '';
                if (progressContainer) progressContainer.classList.remove('hidden');

                generatedImages = [];

                // Build comprehensive prompt
                const productDesc = productTypeDescriptions[selectedProductType] || selectedProductType;
                const styleDesc = catalogStyleDescriptions[selectedCatalogStyle] || selectedCatalogStyle;
                const angleDesc = angleDescriptions[selectedAngle] || selectedAngle;
                const bgDesc = backgroundDescriptions[selectedBackground] || selectedBackground;
                const lightingDesc = lightingDescriptions[selectedLighting] || selectedLighting;

                const basePrompt = `Ubah sketsa produk yang diupload menjadi foto katalog produk profesional berkualitas tinggi.

PRODUK:
- Jenis produk: ${productDesc}
- Pertahankan SEMUA detail design dari sketsa (warna, pattern, cutting, style, details)
- Realisasikan sketch menjadi produk nyata dengan material dan tekstur yang realistic

CATALOG STYLE:
- ${styleDesc}
- ${angleDesc}
- ${bgDesc}
- ${lightingDesc}

KUALITAS FOTO:
- Professional product photography
- E-commerce catalog quality
- High resolution dan sharp details
- Realistic fabric textures dan materials
- Proper shadows dan reflections
- Color-accurate representation
- Commercial photography standard
- DSLR quality output

IMPORTANT:
- Convert sketch to REALISTIC finished product
- Maintain all design elements dari sketch
- Professional commercial photography aesthetic
- Ready for e-commerce catalog use
- No sketch lines, fully rendered realistic product
- Photo-realistic fabric, texture, dan finish`;

                // Pre-create placeholder cards
                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `sketch-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-square';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-purple-600"></div>';
                    if (resultsGrid) resultsGrid.appendChild(card);
                }

                let attempts = 0;
                const MAX_ATTEMPTS = 3;

                while (attempts < MAX_ATTEMPTS) {
                    attempts++;
                    generatedImages = [];
                    await Promise.allSettled(
                        Array.from({ length: selectedCount }, (_, i) => generateSingleCatalogPhoto(i + 1, basePrompt))
                    );
                    const successCount = generatedImages.filter(Boolean).length;
                    if (successCount > 0) break;
                }

                // Remove empty spinner cards
                document.querySelectorAll('[id^="sketch-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });

                if (loading) loading.classList.add('hidden');
                if (progressContainer) progressContainer.classList.add('hidden');

                const finalSuccessSketch = generatedImages.filter(Boolean).length;
                if (finalSuccessSketch === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    if (emptyState) emptyState.classList.remove('hidden');
                } else {
                    if (resultsHeader) resultsHeader.classList.remove('hidden');
                    if (resultsCount) resultsCount.textContent = finalSuccessSketch;
                }
            });
        }

        // Generate single catalog photo
        async function generateSingleCatalogPhoto(index, prompt) {
            const card = document.getElementById(`sketch-card-${index}`);
            const finalPrompt = `${prompt}\n\nVariation ${index}: Create unique variation in pose, styling, or presentation while maintaining professional catalog quality.`;

            try {
                const base64Data = sketchImageData.split(',')[1];

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: finalPrompt },
                            { inlineData: { mimeType: 'image/jpeg', data: base64Data } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data in response');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `catalog-${selectedProductType}-${Date.now()}-${index}.jpg`;

                generatedImages[index - 1] = { url: imageUrl, filename: filename };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Catalog Photo ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye pointer-events-none"></i>
                                <span class="hidden sm:inline pointer-events-none">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-purple-500 to-purple-600 hover:from-purple-600 hover:to-purple-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download pointer-events-none"></i>
                                <span class="hidden sm:inline pointer-events-none">Download</span>
                            </button>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`Error generating catalog photo ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Action buttons (preview/download)
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation(); // Mobile-safe

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all button
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                const validSketchImages = generatedImages.filter(Boolean);
                if (validSketchImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < validSketchImages.length; i++) {
                        const img = validSketchImages[i];
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${validSketchImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

    })();

    // ==================== DESAIN PERNIKAHAN INDONESIA ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const imageInput = document.getElementById('wedding-design-upload');
        const previewContainer = document.getElementById('wedding-design-preview-container');
        const imagePreview = document.getElementById('wedding-design-image-preview');
        const removePreviewBtn = document.getElementById('wedding-design-remove-preview');

        const themeOptions = document.getElementById('wedding-design-theme-options');
        const frameOptions = document.getElementById('wedding-design-frame-options');
        const lightOptions = document.getElementById('wedding-design-light-options');
        const furnitureOptions = document.getElementById('wedding-design-furniture-options');
        const pelaminanOptions = document.getElementById('wedding-design-pelaminan-options');
        const colorOptions = document.getElementById('wedding-design-color-options');
        const lightingOptions = document.getElementById('wedding-design-lighting-options');
        const ratioOptions = document.getElementById('wedding-design-ratio-options');

        const generateBtn = document.getElementById('wedding-design-generate-btn');
        const resultsHeader = document.getElementById('wedding-design-results-header');
        const resultsCount = document.getElementById('wedding-design-results-count');
        const resultsGrid = document.getElementById('wedding-design-results-grid');
        const emptyState = document.getElementById('wedding-design-empty-state');
        const loading = document.getElementById('wedding-design-loading');
        const loadingText = document.getElementById('wedding-design-loading-text');
        const errorMessage = document.getElementById('wedding-design-error-message');
        const downloadAllBtn = document.getElementById('wedding-design-download-all-btn');

        const progressBar = document.getElementById('wedding-design-progress-bar');
        const progressText = document.getElementById('wedding-design-progress-text');

        // State
        let backgroundImageData = null;
        let selectedTheme = 'javanese-traditional';
        let selectedFrame = 'frame-top';
        let selectedLight = 'chandelier-crystal';
        let selectedFurniture = 'sofa-pengantin-mewah';
        let selectedPelaminan = 'pelaminan-gebyok';
        let selectedColor = 'gold-maroon';
        let selectedLighting = 'warm-romantic';
        let selectedRatio = '16:9';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedImages = [];

        // Count selection (1-10), default 4
        const wdesignCountSel = document.getElementById('wedding-design-count-selection-grid');
        if (wdesignCountSel) {
            wdesignCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                wdesignCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Theme descriptions
        const themeDescriptions = {
            'javanese-traditional': 'Traditional Javanese wedding style with ornate Gebyok backdrop, batik fabric elements, traditional Javanese ornaments like wayang motifs, gold accents, rich cultural details, elegant and sophisticated',
            'sundanese-traditional': 'Traditional Sundanese wedding style with Konde decorations, bamboo elements, traditional Sundanese patterns, angklung motifs, natural elegance, refined cultural aesthetics',
            'minang-traditional': 'Traditional Minangkabau wedding style with distinctive Gadang house roof architecture, traditional Minang ornaments, intricate songket patterns, bold cultural identity, majestic appearance',
            'balinese-traditional': 'Traditional Balinese wedding style with temple-inspired decorations, frangipani flowers, traditional Balinese carvings, gold leaf details, spiritual elegance, tropical paradise ambiance',
            'modern-elegant': 'Modern elegant wedding style with sleek contemporary design, minimalist sophistication, geometric patterns, luxurious materials, refined aesthetics, timeless beauty',
            'rustic-garden': 'Rustic garden wedding style with natural wood elements, lush greenery, wildflowers, organic textures, countryside charm, romantic outdoor ambiance',
            'luxury-gold': 'Luxury gold wedding style with opulent gold decorations, crystal chandeliers, premium materials, lavish embellishments, grand and magnificent appearance',
            'islamic-akad': 'Islamic wedding akad style with mosque-inspired decorations, Arabic calligraphy, Islamic geometric patterns, elegant hijab-friendly design, spiritual and sacred atmosphere'
        };

        // Frame descriptions
        const frameDescriptions = {
            'frame-top': 'ornate decorative frame positioned at the top of the stage, elegant arch design with floral arrangements and draping fabrics',
            'frame-middle': 'decorative frame surrounding the middle section, creating an elegant frame around the couple seating area',
            'frame-bottom': 'decorative base frame at the bottom of the stage, elegant foundation with floral and fabric arrangements',
            'frame-jumblak': 'traditional Indonesian Katak Jumblak decorative frames on both sides, ornate cultural pillars with traditional ornaments',
            'frame-full': 'complete decorative frame surrounding the entire stage area, comprehensive elegant border design',
            'frame-minimal': 'minimal clean decorative frame, subtle elegant borders, modern simplicity'
        };

        // Light descriptions
        const lightDescriptions = {
            'chandelier-crystal': 'elegant crystal chandeliers hanging from above, sparkling light reflections, luxurious ambient lighting',
            'fairy-lights': 'warm fairy tumblr lights creating magical atmosphere, soft glowing strings decorating the venue',
            'string-lights': 'decorative string lights creating warm ambiance, romantic lighting design',
            'lantern-gelak': 'traditional Indonesian lanterns (gelak/lentera), cultural lighting elements with warm glow',
            'led-spotlight': 'professional LED spotlights creating dramatic illumination, focused theatrical lighting',
            'candle-ambient': 'ambient candle lighting creating romantic warm atmosphere, elegant candle holders and arrangements'
        };

        // Furniture descriptions
        const furnitureDescriptions = {
            'sofa-pengantin-mewah': 'luxurious wedding sofa for the couple, ornate upholstery with gold accents, premium elegant seating',
            'kursi-pelaminan-jawa': 'traditional Javanese wedding chairs, intricately carved wood with cultural motifs, elegant traditional seating',
            'singgasana-gold': 'royal golden throne chairs, majestic and regal appearance, fit for king and queen',
            'throne-classic': 'classic elegant throne chairs, sophisticated traditional design, premium craftsmanship',
            'loveseat-modern': 'modern contemporary loveseat, sleek elegant design for the couple',
            'bench-minimalis': 'minimalist elegant bench seating, clean modern lines, sophisticated simplicity'
        };

        // Pelaminan style descriptions
        const pelaminanDescriptions = {
            'pelaminan-gebyok': 'traditional Javanese Gebyok backdrop, intricate carved wood panels with traditional motifs, ornate cultural artistry',
            'pelaminan-konde': 'traditional Sundanese Konde style stage decoration, elegant cultural design with traditional patterns',
            'pelaminan-modern-minimalis': 'modern minimalist wedding stage design, clean elegant lines, contemporary sophistication',
            'pelaminan-floral-garden': 'lush floral garden wedding stage, abundant fresh flowers, natural romantic beauty',
            'pelaminan-luxury-draping': 'luxury fabric draping decoration, elegant cascading fabrics with premium textures',
            'pelaminan-arabian-style': 'Arabian-inspired wedding stage, Islamic architectural elements, ornate Middle Eastern elegance'
        };

        // Color scheme descriptions
        const colorDescriptions = {
            'gold-maroon': 'rich gold and deep maroon color palette, luxurious and regal combination',
            'white-gold': 'pristine white with elegant gold accents, classic sophisticated palette',
            'pink-gold': 'soft pink with warm gold tones, romantic and elegant combination',
            'emerald-gold': 'deep emerald green with luxurious gold, rich and opulent palette',
            'navy-silver': 'deep navy blue with silver accents, sophisticated and modern combination',
            'burgundy-cream': 'rich burgundy with soft cream tones, elegant and warm palette'
        };

        // Lighting descriptions
        const lightingDescriptions = {
            'warm-romantic': 'warm romantic lighting with soft golden glow, intimate and cozy atmosphere',
            'bright-elegant': 'bright elegant lighting with clear visibility, professional and refined illumination',
            'soft-dreamy': 'soft dreamy lighting creating ethereal atmosphere, gentle diffused illumination',
            'dramatic-spotlight': 'dramatic spotlight lighting creating theatrical effect, focused intense illumination'
        };

        // Image upload handler with HEIC support
        if (imageInput) {
            imageInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    backgroundImageData = reader.result;

                    // Show preview
                    if (imagePreview && previewContainer) {
                        imagePreview.src = reader.result;
                        previewContainer.classList.remove('hidden');
                    }

                    checkReadyToGenerate();
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove preview handler
        if (removePreviewBtn) {
            removePreviewBtn.addEventListener('click', () => {
                backgroundImageData = null;
                imageInput.value = '';
                if (previewContainer) previewContainer.classList.add('hidden');
                if (imagePreview) imagePreview.src = '';
                checkReadyToGenerate();
            });
        }

        // Option selection handlers
        function setupOptionButtons(container, callback) {
            if (!container) return;

            container.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                // Remove selected state from all buttons
                container.querySelectorAll('.option-btn-wedding').forEach(btn => {
                    btn.classList.remove('selected');
                });

                // Add selected state to clicked button
                button.classList.add('selected');

                // Get selected value and call callback
                const value = button.dataset.value;
                callback(value);
            });
        }

        setupOptionButtons(themeOptions, (value) => selectedTheme = value);
        setupOptionButtons(frameOptions, (value) => selectedFrame = value);
        setupOptionButtons(lightOptions, (value) => selectedLight = value);
        setupOptionButtons(furnitureOptions, (value) => selectedFurniture = value);
        setupOptionButtons(pelaminanOptions, (value) => selectedPelaminan = value);
        setupOptionButtons(colorOptions, (value) => selectedColor = value);
        setupOptionButtons(lightingOptions, (value) => selectedLighting = value);
        setupOptionButtons(ratioOptions, (value) => selectedRatio = value);

        // Check if ready to generate
        function checkReadyToGenerate() {
            if (generateBtn) {
                generateBtn.disabled = !backgroundImageData;
            }
        }

        // Generate button handler
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!backgroundImageData) {
                    showError('Silakan upload background atau sketsa venue terlebih dahulu!');
                    return;
                }

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                if (emptyState) emptyState.classList.add('hidden');
                if (loading) loading.classList.remove('hidden');
                if (resultsHeader) resultsHeader.classList.add('hidden');

                let attempts = 0;
                const MAX_ATTEMPTS = 3;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    await generateWeddingDesigns();

                    // Remove empty cards
                    if (resultsGrid) {
                        resultsGrid.querySelectorAll('[id^="wedding-design-card-"]').forEach(card => {
                            if (!card.querySelector('img')) card.remove();
                        });
                    }
                    successCount = generatedImages.filter(Boolean).length;
                }

                if (loading) loading.classList.add('hidden');

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    if (emptyState) emptyState.classList.remove('hidden');
                } else {
                    if (resultsHeader) resultsHeader.classList.remove('hidden');
                    if (resultsCount) resultsCount.textContent = successCount;
                    if (emptyState) emptyState.classList.add('hidden');
                }

                generateBtn.disabled = false;
                generateBtn.innerHTML = originalHTML;
            });
        }

        // Generate wedding designs function
        async function generateWeddingDesigns() {
            if (resultsGrid) resultsGrid.innerHTML = '';
            generatedImages = [];

            // Build AI prompt
            const themeDesc = themeDescriptions[selectedTheme] || 'elegant wedding decoration';
            const frameDesc = frameDescriptions[selectedFrame] || 'elegant decorative frame';
            const lightDesc = lightDescriptions[selectedLight] || 'elegant ambient lighting';
            const furnitureDesc = furnitureDescriptions[selectedFurniture] || 'elegant wedding seating';
            const pelaminanDesc = pelaminanDescriptions[selectedPelaminan] || 'elegant wedding stage';
            const colorDesc = colorDescriptions[selectedColor] || 'elegant color palette';
            const lightingDesc = lightingDescriptions[selectedLighting] || 'professional lighting';

            const basePrompt = `Buat visualisasi desain pernikahan Indonesia yang sangat profesional dan realistis berdasarkan background/sketsa yang diupload.

TEMA PERNIKAHAN: ${themeDesc}

ELEMEN DEKORASI LENGKAP:
- Pelaminan Style: ${pelaminanDesc}
- Bingkai Dekorasi: ${frameDesc}
- Lampu Hias: ${lightDesc}
- Kursi & Sofa: ${furnitureDesc}
- Warna Tema: ${colorDesc}
- Pencahayaan: ${lightingDesc}

REQUIREMENTS TEKNIS:
✓ Render 3D photorealistic berkualitas sangat tinggi
✓ Lighting professional dengan ambient occlusion
✓ Texture detail realistis untuk semua material (kain, kayu, kristal, bunga)
✓ Komposisi visual seimbang dan harmonis
✓ Perspektif akurat sesuai arsitektur ruangan
✓ Color grading profesional sesuai warna tema
✓ Depth of field yang natural
✓ Refleksi dan bayangan yang akurat
✓ Detail ornamen tradisional Indonesia yang presisi
✓ Kualitas presentasi untuk klien event organizer

OUTPUT STYLE:
- Photorealistic 3D render quality
- Professional architectural visualization
- Event planning presentation standard
- Magazine editorial quality
- Ultra high definition detail

Pastikan semua elemen dekorasi terintegrasi dengan sempurna, lighting dramatis namun elegant, dan hasil akhir terlihat mewah dan profesional untuk presentasi klien event organizer.`;

            // Create placeholder cards
            for (let i = 1; i <= selectedCount; i++) {
                const card = document.createElement('div');
                card.id = `wedding-design-card-${i}`;
                card.className = 'relative rounded-xl overflow-hidden bg-gradient-to-br from-amber-50 to-red-50 flex items-center justify-center aspect-video border-2';
                card.style.borderColor = '#d97706';
                card.innerHTML = `
                    <div class="flex flex-col items-center gap-3">
                        <div class="animate-spin rounded-full h-12 w-12 border-b-3 border-t-3" style="border-color: #d97706;"></div>
                        <p class="text-sm font-medium" style="color: #92400e;">Desain ${i}/${selectedCount}</p>
                    </div>
                `;
                if (resultsGrid) resultsGrid.appendChild(card);
            }

            // Generate all images in parallel
            const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                generateSingleDesign(i + 1, basePrompt)
            );

            await Promise.allSettled(generationPromises);
        }

        // Generate single wedding design
        async function generateSingleDesign(index, basePrompt) {
            const card = document.getElementById(`wedding-design-card-${index}`);

            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const base64Data = backgroundImageData.split(',')[1];
                const mimeType = backgroundImageData.split(';')[0].split(':')[1];

                const payload = {
                    contents: [{
                        parts: [
                            { text: basePrompt },
                            { inlineData: { mimeType: mimeType, data: base64Data } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `wedding-design-${selectedTheme}-${index}-${Date.now()}.jpg`;
                generatedImages[index - 1] = { url: imageUrl, filename };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 aspect-video border-2';
                    card.style.borderColor = '#d97706';
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Wedding Design ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-amber-600 to-red-600 hover:from-amber-700 hover:to-red-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`Error generating design ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Action buttons handler (preview/download)
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);

                if (isNaN(index) || !generatedImages[index]) return;

                const imageData = generatedImages[index];

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    try {
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(imageData.url, imageData.filename);
                        } else if (window.downloadImage) {
                            await window.downloadImage(imageData.url, imageData.filename);
                        }
                    } catch (error) {
                        console.error('Download error:', error);
                    }
                }
            });
        }

        // Download all handler
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        // Show error message
        function showError(message) {
            if (errorMessage) {
                errorMessage.classList.remove('hidden');
                const errorText = errorMessage.querySelector('p');
                if (errorText) errorText.textContent = message;

                setTimeout(() => {
                    errorMessage.classList.add('hidden');
                }, 5000);
            }
        }

    })();

    // ==================== FOTO ULANG TAHUN ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const imageInput = document.getElementById('birthday-photo-upload');
        const previewContainer = document.getElementById('birthday-photo-preview-container');
        const photoCountText = document.getElementById('birthday-photo-count');

        const ageOptions = document.getElementById('birthday-age-options');
        const themeOptions = document.getElementById('birthday-theme-options');
        const decorationOptions = document.getElementById('birthday-decoration-options');
        const cakeOptions = document.getElementById('birthday-cake-options');
        const lightingOptions = document.getElementById('birthday-lighting-options');
        const ratioOptions = document.getElementById('birthday-ratio-options');

        const customThemeInput = document.getElementById('birthday-custom-theme');
        const generateBtn = document.getElementById('birthday-generate-btn');
        const resultsHeader = document.getElementById('birthday-results-header');
        const resultsCount = document.getElementById('birthday-results-count');
        const resultsGrid = document.getElementById('birthday-results-grid');
        const emptyState = document.getElementById('birthday-empty-state');
        const loading = document.getElementById('birthday-loading');
        const loadingText = document.getElementById('birthday-loading-text');
        const errorMessage = document.getElementById('birthday-error-message');
        const downloadAllBtn = document.getElementById('birthday-download-all-btn');

        const progressBar = document.getElementById('birthday-progress-bar');
        const progressText = document.getElementById('birthday-progress-text');

        // State
        let birthdayImages = []; // Array to store multiple images (max 5)
        let selectedMode = 'pesta';
        let selectedMood = 'nostalgic';
        let memoryImageNow = null;     // { mimeType, base64 } — FIRST image (adult)
        let memoryImageChild = null;   // { mimeType, base64 } — SECOND image (child)
        let selectedAge = 'child';
        let selectedTheme = 'rainbow-party';
        let selectedDecoration = 'balloon-arch';
        let selectedCake = 'tiered-cake';
        let selectedLighting = 'bright-cheerful';
        let selectedRatio = '3:4';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedImages = [];

        // Count selection (1-10), default 4
        const birthdayCountSel = document.getElementById('birthday-count-selection-grid');
        if (birthdayCountSel) {
            birthdayCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                birthdayCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Age category descriptions
        const ageDescriptions = {
            'baby': 'adorable baby (0-2 years old), cute infant expression, baby-appropriate props and decorations',
            'toddler': 'cute toddler (3-5 years old), cheerful child expression, playful age-appropriate setting',
            'child': 'happy child (6-12 years old), joyful kid expression, fun age-appropriate celebration',
            'teen': 'teenager (13-17 years old), cool young adult expression, trendy age-appropriate party setting',
            'adult': 'adult person (18-59 years old), elegant sophisticated expression, mature celebration atmosphere',
            'senior': 'senior person (60+ years old), warm dignified expression, respectful elegant celebration'
        };

        // Theme descriptions
        const themeDescriptions = {
            'rainbow-party': 'vibrant rainbow party theme with multicolor decorations, rainbow balloon arch, colorful confetti, bright happy atmosphere, cheerful celebration',
            'unicorn-magic': 'magical unicorn theme with pastel rainbow colors, unicorn decorations, glitter accents, dreamy mystical atmosphere, fantasy celebration',
            'princess-castle': 'elegant princess castle theme with pink and gold colors, royal decorations, crown accents, fairytale castle backdrop, regal atmosphere',
            'superhero': 'action-packed superhero theme with bold primary colors, comic-style decorations, hero emblems, dynamic energetic atmosphere, powerful celebration',
            'dinosaur': 'exciting dinosaur theme with green jungle colors, dinosaur decorations, prehistoric elements, adventurous atmosphere, wild celebration',
            'mermaid': 'enchanting mermaid theme with ocean blue and teal colors, seashell decorations, underwater elements, magical ocean atmosphere',
            'space-astronaut': 'cosmic space theme with dark blue and silver, star decorations, planet elements, rocket accents, galactic atmosphere',
            'safari-jungle': 'wild safari jungle theme with green and brown earth tones, animal decorations, tropical leaves, adventure atmosphere',
            'garden-flower': 'beautiful garden flower theme with soft pastels, fresh flower arrangements, botanical elements, natural outdoor atmosphere',
            'pirate': 'adventurous pirate theme with nautical colors, treasure chest decorations, ship elements, sea adventure atmosphere',
            'circus': 'fun circus theme with bright red and yellow, circus tent decorations, carnival elements, playful entertaining atmosphere',
            'candy-land': 'sweet candy land theme with vibrant pinks and blues, candy decorations, lollipop elements, sugary whimsical atmosphere',
            'elegant-gold': 'sophisticated elegant gold theme with black and gold colors, luxury decorations, metallic accents, glamorous upscale atmosphere',
            'vintage-retro': 'nostalgic vintage retro theme with muted tones, classic decorations, old-fashioned elements, timeless charming atmosphere',
            'hollywood-glam': 'glamorous Hollywood theme with red carpet colors, star decorations, spotlight elements, celebrity VIP atmosphere',
            'garden-tea-party': 'charming garden tea party theme with soft pastels, floral arrangements, vintage tea set, elegant outdoor atmosphere',
            'beach-tropical': 'relaxing beach tropical theme with turquoise and coral, palm decorations, summer elements, sunny vacation atmosphere',
            'winter-wonderland': 'magical winter wonderland theme with white and silver, snowflake decorations, icy elements, enchanting frosty atmosphere',
            'minimalist-modern': 'clean minimalist modern theme with neutral tones, simple geometric decorations, contemporary elements, sophisticated atmosphere',
            'boho-chic': 'trendy boho chic theme with earthy tones, macrame decorations, natural elements, artistic bohemian atmosphere'
        };

        // Decoration descriptions
        const decorationDescriptions = {
            'balloon-arch': 'stunning balloon arch as main decoration, elegant arch design with themed color balloons, professional balloon arrangement',
            'confetti-explosion': 'festive confetti explosion effect, colorful confetti scattered beautifully, celebratory atmosphere with floating confetti',
            'banner-garland': 'decorative banner and garland hanging, festive bunting and streamers, coordinated party banners',
            'giant-numbers': 'eye-catching giant number balloons or decorations displaying age, metallic or themed number display, prominent celebration marker',
            'flower-arrangement': 'beautiful fresh flower arrangements, elegant floral centerpieces and decorations, natural botanical beauty',
            'photo-backdrop': 'professional photo backdrop wall, themed backdrop design, Instagram-worthy photo opportunity area'
        };

        // Cake descriptions
        const cakeDescriptions = {
            'tiered-cake': 'elegant multi-tiered birthday cake, beautifully decorated layers, professional bakery quality, themed frosting and decorations',
            'character-cake': 'creative character-shaped cake matching theme, detailed 3D cake design, fondant character decorations',
            'drip-cake': 'trendy drip cake with colorful ganache drips, modern cake design, Instagram-worthy presentation',
            'cupcake-tower': 'impressive cupcake tower display, multiple decorated cupcakes arranged artistically, variety of flavors and designs',
            'elegant-fondant': 'sophisticated fondant-covered cake, smooth elegant finish, intricate fondant decorations and details',
            'rustic-naked': 'charming rustic naked cake with exposed layers, natural frosting style, fresh fruit and flower decorations'
        };

        // Lighting descriptions
        const lightingDescriptions = {
            'bright-cheerful': 'bright cheerful lighting creating happy energetic atmosphere, clear well-lit scene, vibrant colors enhanced',
            'soft-dreamy': 'soft dreamy lighting with gentle illumination, romantic ethereal atmosphere, pastel colors enhanced, magical glow',
            'party-lights': 'festive party lights with colorful illumination, string lights and LED effects, celebratory dynamic lighting',
            'candle-glow': 'warm candle glow from birthday candles, intimate magical atmosphere, soft flickering light effects'
        };

        // Multiple images upload handler with HEIC support
        if (imageInput) {
            imageInput.addEventListener('change', async (e) => {
                const files = Array.from(e.target.files);
                if (files.length === 0) return;

                // Check if exceeds max limit (5 photos)
                const totalPhotos = birthdayImages.length + files.length;
                if (totalPhotos > 5) {
                    showError(`Maksimal 5 foto! Anda sudah upload ${birthdayImages.length} foto.`);
                    return;
                }

                // Process each file
                for (const file of files) {
                    let processedFile = file;

                    // Convert HEIC to JPEG if needed
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        try {
                            const convertedBlob = await heic2any({
                                blob: file,
                                toType: 'image/jpeg',
                                quality: 0.9
                            });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                                type: 'image/jpeg'
                            });
                        } catch (err) {
                            console.error('HEIC conversion failed:', err);
                            showError(`Gagal mengkonversi ${file.name}`);
                            continue;
                        }
                    }

                    // Read file as base64
                    const reader = new FileReader();
                    reader.onloadend = () => {
                        birthdayImages.push(reader.result);
                        updatePhotoPreview();
                        checkReadyToGenerate();
                    };
                    reader.readAsDataURL(processedFile);
                }

                // Clear input value to allow re-uploading same file
                imageInput.value = '';
            });
        }

        // Update photo preview grid
        function updatePhotoPreview() {
            if (!previewContainer) return;

            // Update photo count
            if (photoCountText) {
                photoCountText.textContent = `${birthdayImages.length}/5 foto`;
            }

            // Show/hide container
            if (birthdayImages.length > 0) {
                previewContainer.classList.remove('hidden');
            } else {
                previewContainer.classList.add('hidden');
            }

            // Clear and rebuild preview grid
            previewContainer.innerHTML = '';
            birthdayImages.forEach((imageData, index) => {
                const photoCard = document.createElement('div');
                photoCard.className = 'relative group';
                photoCard.innerHTML = `
                    <img src="${imageData}" alt="Photo ${index + 1}" class="w-full h-24 object-cover rounded-lg border-2" style="border-color: #ec4899;">
                    <button data-remove-index="${index}" class="absolute top-1 right-1 bg-red-600 hover:bg-red-700 text-white w-6 h-6 rounded-full flex items-center justify-center text-xs opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity">
                        <i class="fas fa-times"></i>
                    </button>
                    <div class="absolute bottom-1 left-1 bg-black/70 text-white px-2 py-0.5 rounded text-xs font-semibold">
                        ${index + 1}
                    </div>
                `;
                previewContainer.appendChild(photoCard);
            });
        }

        // Remove individual photo handler (event delegation)
        if (previewContainer) {
            previewContainer.addEventListener('click', (e) => {
                const removeBtn = e.target.closest('[data-remove-index]');
                if (!removeBtn) return;

                const indexToRemove = parseInt(removeBtn.dataset.removeIndex, 10);
                birthdayImages.splice(indexToRemove, 1);
                updatePhotoPreview();
                checkReadyToGenerate();
            });
        }

        // Option selection handlers
        function setupOptionButtons(container, callback) {
            if (!container) return;

            container.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                // Remove selected state from all buttons
                container.querySelectorAll('.option-btn-birthday').forEach(btn => {
                    btn.classList.remove('selected');
                });

                // Add selected state to clicked button
                button.classList.add('selected');

                // Get selected value and call callback
                const value = button.dataset.value;
                callback(value);
            });
        }

        setupOptionButtons(ageOptions, (value) => selectedAge = value);
        setupOptionButtons(themeOptions, (value) => {
            selectedTheme = value;
            if (customThemeInput) customThemeInput.value = '';
        });
        setupOptionButtons(decorationOptions, (value) => selectedDecoration = value);
        setupOptionButtons(cakeOptions, (value) => selectedCake = value);
        setupOptionButtons(lightingOptions, (value) => selectedLighting = value);
        setupOptionButtons(ratioOptions, (value) => selectedRatio = value);

        // Custom theme input handler
        if (customThemeInput) {
            customThemeInput.addEventListener('input', () => {
                if (customThemeInput.value.trim() !== '') {
                    if (themeOptions) {
                        themeOptions.querySelectorAll('.option-btn-birthday').forEach(btn => {
                            btn.classList.remove('selected');
                        });
                    }
                    selectedTheme = 'custom';
                }
            });
        }

        // Check if ready to generate
        function checkReadyToGenerate() {
            if (generateBtn) {
                generateBtn.disabled = (selectedMode === 'kenangan')
                    ? !(memoryImageNow && memoryImageChild)
                    : !(birthdayImages.length >= 1);
            }
        }

        // Generate button handler
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (selectedMode === 'kenangan') {
                    if (!memoryImageNow || !memoryImageChild) {
                        showError('Mode Kenangan butuh 2 foto: foto sekarang dan foto masa kecil.');
                        return;
                    }
                } else {
                    if (birthdayImages.length === 0) {
                        showError('Silakan upload minimal 1 foto terlebih dahulu!');
                        return;
                    }

                    const customVal = customThemeInput ? customThemeInput.value.trim() : '';
                    if (selectedTheme === 'custom' && !customVal) {
                        showError('Silakan pilih tema atau tulis tema custom terlebih dahulu!');
                        return;
                    }
                }

                await generateBirthdayPhotos();
            });
        }

        // ============================================================================
        // Memory Mode (Past vs Present) — Template Registry
        // ============================================================================
        // Adopsi pattern templateBuilders dari tab studio-profil (line 54300).
        // Setiap builder return prompt dengan FIRST/SECOND image anchor eksplisit.
        // FIRST image = adult subject, SECOND image = childhood reference.
        // Order load-bearing — image dispatch di generateBirthdayPhotos harus match.

        const MEMORY_NEGATIVE_PROMPT = `Negative: no text, no watermark, no distortion, no extra fingers, no blur on face. The child and adult MUST clearly be the same person — same facial features, eyes, smile.`;

        const MEMORY_MOOD_LINES = {
            'nostalgic': 'Mood: nostalgic, emotional, "growing up" concept, past vs present, memory-like feeling. Warm candlelight foreground, soft ambient shadows.',
            'emotional': 'Mood: deeply emotional, intimate close-up, soft tearful glistening eyes, tender moment of remembrance.',
            'joyful': 'Mood: joyful, celebratory, bright vibrant warm tones, wide smile, uplifting tone.',
            'dramatic': 'Mood: cinematic dramatic, high contrast theatrical key light, deep shadows, movie-poster mood.'
        };

        const memoryTemplates = {
            'memory-projection': (ctx) => `IMPORTANT — Two reference images provided:
- FIRST image is the ADULT subject (use this for face, identity, body, current age)
- SECOND image is the CHILDHOOD memory reference (use this as projected memory on wall)

A cinematic, emotional birthday scene combining past and present of the same person.
Foreground: the adult from the FIRST image sitting at a table, holding a round birthday cake with many lit candles, warm candlelight softly illuminating the face and creating cinematic shadows. Calm, nostalgic expression.
Background (key element): on the wall behind, a large projected/painted image of the childhood from the SECOND image, smiling softly, slightly faded like a memory, softly blended into the wall.

${ctx.moodLine}

Style: ultra realistic, 8K, high detail skin texture, cinematic lighting, shallow depth of field. ${MEMORY_NEGATIVE_PROMPT}`,

            'holding-childhood-photo': (ctx) => `IMPORTANT — Two reference images provided:
- FIRST image is the ADULT subject
- SECOND image is the CHILDHOOD photo (will appear inside a framed picture being held)

The adult from the FIRST image holds up a vintage wooden picture frame containing the childhood image from the SECOND image. The framed photo should be clearly visible and recognizable. A birthday cake with lit candles sits on a table in front of them. Warm domestic lighting, soft bokeh background. The adult looks at the childhood photo with a tender, reflective expression.

${ctx.moodLine}

Style: ultra realistic, 8K, cinematic lighting. ${MEMORY_NEGATIVE_PROMPT}`,

            'mirror-reflection': (ctx) => `IMPORTANT — Two reference images provided:
- FIRST image is the ADULT subject standing in front of a mirror
- SECOND image is the CHILDHOOD version that appears in the mirror reflection

A magical-realism birthday scene. The adult from the FIRST image stands in front of a large vintage mirror, holding a birthday cake with lit candles. But in the mirror's reflection, instead of the adult, the childhood version from the SECOND image appears, also holding a smaller cake, blowing out candles. The two figures clearly the same person at different ages.

${ctx.moodLine}

Style: ultra realistic, 8K, cinematic lighting, magical-realism. Subtle dreamlike quality. ${MEMORY_NEGATIVE_PROMPT}`,

            'then-and-now-split': (ctx) => `IMPORTANT — Two reference images provided:
- FIRST image is the ADULT subject (right side of frame)
- SECOND image is the CHILDHOOD version (left side of frame)

A cinematic split-frame diptych composition. LEFT half: the childhood version from the SECOND image, blowing out candles on a small simple birthday cake, in a vintage warm-toned setting (slightly desaturated, film-grain, "memory" feel). RIGHT half: the adult version from the FIRST image, blowing out candles on a more elegant birthday cake, in a modern sharp-toned setting. Both figures in the same pose, clearly the same person grown up. Subtle vertical line/seam separating the two halves.

${ctx.moodLine}

Style: ultra realistic, 8K, cinematic split-frame composition, recommended aspect 16:9. ${MEMORY_NEGATIVE_PROMPT}`,

            'time-portal-ghost': (ctx) => `IMPORTANT — Two reference images provided:
- FIRST image is the ADULT subject (solid, fully present)
- SECOND image is the CHILDHOOD version (semi-transparent, ghostly, holographic)

A magical-realism birthday scene. The adult from the FIRST image sits at a table with a birthday cake, fully solid and present. Beside them, the childhood version from the SECOND image appears as a softly glowing semi-transparent holographic apparition, also blowing out candles together — a "younger self" visiting from the past. Soft ethereal light emanates from the child apparition.

${ctx.moodLine}

Style: ultra realistic with subtle magical-realism, 8K, cinematic lighting, soft glow on the apparition. ${MEMORY_NEGATIVE_PROMPT}`,

            'cake-photo-topper': (ctx) => `IMPORTANT — Two reference images provided:
- FIRST image is the ADULT subject
- SECOND image is the CHILDHOOD photo (will appear AS edible printed cake topper)

The adult from the FIRST image holds a beautifully decorated round birthday cake. The top surface of the cake features an edible printed photo (cake topper) showing the childhood image from the SECOND image — clearly recognizable, with cake decorations, candles, and frosting around the edge. The adult smiles tenderly looking at the cake. Warm intimate lighting.

${ctx.moodLine}

Style: ultra realistic, 8K, cinematic lighting, sharp focus on cake topper detail. ${MEMORY_NEGATIVE_PROMPT}`,
        };

        function buildMemoryPrompt(themeId, moodId) {
            const builder = memoryTemplates[themeId] || memoryTemplates['memory-projection'];
            return builder({ moodLine: MEMORY_MOOD_LINES[moodId] || MEMORY_MOOD_LINES['nostalgic'] });
        }

        // ============================================================================
        // Mode toggle handler
        // ============================================================================
        const modeButtons = document.querySelectorAll('#birthday-mode-options button[data-mode]');
        modeButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                const newMode = btn.dataset.mode;
                if (newMode === selectedMode) return;
                selectedMode = newMode;

                // Update button selected state
                modeButtons.forEach(b => b.classList.toggle('selected', b.dataset.mode === newMode));

                // Toggle body attribute → CSS handles show/hide
                if (newMode === 'kenangan') {
                    document.body.setAttribute('data-birthday-mode', 'kenangan');
                    // Auto-switch ratio ke 16:9 untuk efek cinematic
                    if (typeof selectedRatio !== 'undefined') {
                        selectedRatio = '16:9';
                        const ratioBtns = document.querySelectorAll('#birthday-ratio-options button[data-value]');
                        ratioBtns.forEach(b => b.classList.toggle('selected', b.dataset.value === '16:9'));
                        // bd-mem.ratio-auto-switch — surface toast so user knows ratio was swapped
                        const lang = (typeof localStorage !== 'undefined' && localStorage.getItem('app_language')) || 'id';
                        const ratioMsg = (lang === 'en')
                            ? 'Aspect ratio set to 16:9 for cinematic effect.'
                            : 'Aspect ratio diubah ke 16:9 untuk efek cinematic.';
                        showError(ratioMsg);
                    }
                } else {
                    document.body.removeAttribute('data-birthday-mode');
                }

                // Clear all upload state (cegah image lama dari mode sebelumnya nyangkut)
                memoryImageNow = null;
                memoryImageChild = null;
                const previewNow = document.getElementById('birthday-preview-now');
                const previewChild = document.getElementById('birthday-preview-childhood');
                if (previewNow) previewNow.classList.add('hidden');
                if (previewChild) previewChild.classList.add('hidden');

                // Clear pesta mode upload state too (existing variable: birthdayImages)
                if (typeof birthdayImages !== 'undefined') birthdayImages.length = 0;
                if (previewContainer) {
                    previewContainer.classList.add('hidden');
                    previewContainer.innerHTML = '';
                }
                if (photoCountText) photoCountText.textContent = '0/5 foto';

                // Update generate button state
                if (typeof checkReadyToGenerate === 'function') checkReadyToGenerate();
            });
        });

        // ============================================================================
        // Memory upload handlers (slot now + slot childhood)
        // ============================================================================
        function handleMemoryUpload(slot) {
            const inputId = slot === 'now' ? 'birthday-upload-now' : 'birthday-upload-childhood';
            const previewId = slot === 'now' ? 'birthday-preview-now' : 'birthday-preview-childhood';
            const input = document.getElementById(inputId);
            const previewWrap = document.getElementById(previewId);
            if (!input) return;

            input.addEventListener('change', (e) => {
                const file = e.target.files[0];
                if (!file) return;

                const reader = new FileReader();
                reader.onload = (evt) => {
                    const dataUrl = evt.target.result;
                    const base64 = dataUrl.split(',')[1];
                    const mimeType = (dataUrl.split(';')[0] || '').split(':')[1];

                    if (slot === 'now') {
                        memoryImageNow = { mimeType, base64 };
                    } else {
                        memoryImageChild = { mimeType, base64 };
                    }

                    if (previewWrap) {
                        const img = previewWrap.querySelector('img');
                        if (img) img.src = dataUrl;
                        previewWrap.classList.remove('hidden');
                    }

                    if (typeof checkReadyToGenerate === 'function') checkReadyToGenerate();
                };
                reader.readAsDataURL(file);
            });
        }
        handleMemoryUpload('now');
        handleMemoryUpload('childhood');

        // ============================================================================
        // Mood selector handler
        // ============================================================================
        const moodButtons = document.querySelectorAll('#birthday-mood-options button[data-mood]');
        moodButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                selectedMood = btn.dataset.mood;
                moodButtons.forEach(b => b.classList.toggle('selected', b.dataset.mood === selectedMood));
            });
        });

        // Generate birthday photos function
        async function generateBirthdayPhotos() {
            // Hide empty state, show loading
            if (emptyState) emptyState.classList.add('hidden');
            if (loading) loading.classList.remove('hidden');
            if (resultsHeader) resultsHeader.classList.add('hidden');
            if (errorMessage) errorMessage.classList.add('hidden');

            // Memory mode validation — both photos required
            if (selectedMode === 'kenangan') {
                if (!memoryImageNow || !memoryImageChild) {
                    if (loading) loading.classList.add('hidden');
                    if (emptyState) emptyState.classList.remove('hidden');
                    const lang = (typeof localStorage !== 'undefined' && localStorage.getItem('app_language')) || 'id';
                    const msg = (lang === 'en')
                        ? 'Memory Mode requires 2 photos: now and childhood.'
                        : 'Mode Kenangan butuh 2 foto: foto sekarang dan foto masa kecil.';
                    // bd-mem.error-need-both
                    showError(msg);
                    return;
                }
            }

            // Build AI prompt with multiple people support
            const ageDesc = ageDescriptions[selectedAge] || 'person';
            const customThemeText = customThemeInput ? customThemeInput.value.trim() : '';
            const themeDesc = (selectedTheme === 'custom' && customThemeText)
                ? customThemeText
                : (themeDescriptions[selectedTheme] || 'colorful birthday party theme');
            const decorationDesc = decorationDescriptions[selectedDecoration] || 'festive decorations';
            const cakeDesc = cakeDescriptions[selectedCake] || 'beautiful birthday cake';
            const lightingDesc = lightingDescriptions[selectedLighting] || 'professional lighting';

            // Build subject description based on number of people
            const peopleCount = birthdayImages.length;
            let subjectDesc = '';
            if (peopleCount === 1) {
                subjectDesc = `- ${ageDesc}
- Pertahankan identitas dan fitur wajah PERSIS SAMA seperti foto asli
- Ekspresi bahagia dan antusias merayakan ulang tahun
- Pose natural dan candid untuk foto ulang tahun`;
            } else {
                subjectDesc = `- GROUP OF ${peopleCount} PEOPLE celebrating birthday together
- Pertahankan identitas dan fitur wajah SETIAP ORANG PERSIS SAMA seperti foto asli masing-masing
- Semua orang dengan ekspresi bahagia dan antusias merayakan ulang tahun
- Group pose natural dan candid, interaksi yang warm dan joyful
- Komposisi grup yang balanced (${ageDesc} - sesuaikan dengan kategori umur yang dipilih)`;
            }

            const basePrompt = (selectedMode === 'kenangan')
                ? buildMemoryPrompt(selectedTheme, selectedMood)
                : `Buat foto ulang tahun profesional berkualitas tinggi dengan detail berikut:

SUBJEK:
${subjectDesc}

TEMA & DEKORASI:
- ${themeDesc}
- ${decorationDesc}
- ${cakeDesc}
- Semua elemen dekorasi sesuai tema dan kategori umur

PENCAHAYAAN & ATMOSFER:
- ${lightingDesc}
- Atmosfer celebratory yang ceria dan meriah
- Warna-warna cerah dan vibrant sesuai tema

REQUIREMENTS FOTOGRAFI:
✓ Professional birthday photography quality
✓ Sharp focus pada ${peopleCount > 1 ? 'semua orang' : 'subjek'} dengan bokeh background natural
✓ Komposisi balanced dengan rule of thirds
✓ Color grading sesuai tema (warm/cool tones)
✓ Lighting professional dengan catch light di mata
✓ Depth of field yang tepat untuk ${peopleCount > 1 ? 'group portrait' : 'portrait'}
✓ Background sesuai tema dengan dekorasi lengkap
✓ Cake dan dekorasi terlihat jelas dan menarik
✓ Overall atmosphere joyful dan festive
✓ Instagram-worthy composition
${peopleCount > 1 ? '✓ Semua orang terlihat jelas dan tidak ada yang terpotong' : ''}

STYLE FOTOGRAFI:
- Professional birthday party photography
- Magazine quality ${peopleCount > 1 ? 'group' : ''} portrait
- Candid celebration moment
- Vibrant colorful aesthetic
- Joyful happy atmosphere

Pastikan identitas ${peopleCount > 1 ? 'SETIAP ORANG' : 'subjek'} TETAP SAMA PERSIS seperti foto asli, hanya situasi, tema, dan dekorasi yang berubah. Foto harus terlihat natural, profesional, dan sempurna untuk kenangan ulang tahun yang spesial!`;

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                if (resultsGrid) resultsGrid.innerHTML = '';

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `birthday-card-${i}`;
                    card.className = 'relative rounded-xl overflow-hidden bg-gradient-to-br from-pink-50 to-purple-50 flex items-center justify-center aspect-[3/4] border-2';
                    card.style.borderColor = '#ec4899';
                    card.innerHTML = `
                        <div class="flex flex-col items-center gap-3">
                            <div class="animate-spin rounded-full h-12 w-12 border-b-3 border-t-3" style="border-color: #ec4899;"></div>
                            <p class="text-sm font-medium" style="color: #be185d;">Foto ${i}/${selectedCount}</p>
                        </div>
                    `;
                    if (resultsGrid) resultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSinglePhoto(i + 1, basePrompt)
                );

                await Promise.allSettled(generationPromises);

                if (resultsGrid) {
                    resultsGrid.querySelectorAll('[id^="birthday-card-"]').forEach(card => {
                        if (!card.querySelector('img')) card.remove();
                    });
                }

                successCount = generatedImages.filter(Boolean).length;
            }

            if (loading) loading.classList.add('hidden');

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                if (resultsHeader) resultsHeader.classList.remove('hidden');
                if (resultsCount) resultsCount.textContent = generatedImages.length;
            }
        }

        // Generate single birthday photo
        async function generateSinglePhoto(index, basePrompt) {
            const cardId = `birthday-card-${index}`;
            const card = document.getElementById(cardId);

            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                // Build parts array with prompt and all images
                const parts = [{ text: basePrompt }];

                if (selectedMode === 'kenangan') {
                    // Memory mode: FIRST=memoryImageNow (adult), SECOND=memoryImageChild (childhood)
                    // Order load-bearing — prompt anchors "FIRST image" / "SECOND image"
                    parts.push({ inlineData: { mimeType: memoryImageNow.mimeType,   data: memoryImageNow.base64   } });
                    parts.push({ inlineData: { mimeType: memoryImageChild.mimeType, data: memoryImageChild.base64 } });
                } else {
                    birthdayImages.forEach((imageData) => {
                        const base64Data = imageData.split(',')[1];
                        const mimeType = imageData.split(';')[0].split(':')[1];
                        parts.push({
                            inlineData: { mimeType: mimeType, data: base64Data }
                        });
                    });
                }

                const payload = {
                    contents: [{ parts: parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();

                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data in response');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `birthday-${selectedTheme}-${selectedAge}-${index}-${Date.now()}.jpg`;

                generatedImages[index - 1] = { url: imageUrl, filename: filename };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 aspect-[3/4] border-2';
                    card.style.borderColor = '#ec4899';
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Birthday Photo ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-pink-600 to-purple-600 hover:from-pink-700 hover:to-purple-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }

            } catch (error) {
                console.error(`Error generating photo ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Action buttons handler (preview/download)
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);

                if (isNaN(index) || !generatedImages[index]) return;

                const imageData = generatedImages[index];

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    try {
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(imageData.url, imageData.filename);
                        } else if (window.downloadImage) {
                            await window.downloadImage(imageData.url, imageData.filename);
                        }
                    } catch (error) {
                        console.error('Download error:', error);
                    }
                }
            });
        }

        // Download all handler
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        // Show error message
        function showError(message) {
            if (errorMessage) {
                errorMessage.classList.remove('hidden');
                const errorText = errorMessage.querySelector('p');
                if (errorText) errorText.textContent = message;

                setTimeout(() => {
                    errorMessage.classList.add('hidden');
                }, 5000);
            }
        }

    })();

    // ==================== WATCH TOGETHER (Nonton Bareng) ====================
    (function watchTogetherTab() {
        const apiKey = "";

        // State
        let wtSelectedMode = 'nonton-bola';
        let wtSelectedTheme = 'tv-broadcast';
        let wtSelectedMood = 'excited';
        let wtSelectedAngle = 'wide';
        let wtSelectedRatio = '16:9';
        let wtSelectedCount = 4;
        let wtFaceImages = []; // Array of { mimeType, base64 }, max 5
        let wtJerseyImage = null; // { mimeType, base64 } or null
        let wtLogoImage = null;   // { mimeType, base64 } or null
        let wtGeneratedImages = [];

        // Constants
        const WT_NEGATIVE_PROMPT = `STRICT RULES:
- Subjects MUST naturally face the action they are watching (the TV screen, the pitch, the projector). Their bodies and gaze should be oriented toward the match — DO NOT turn subjects away from the screen/pitch. This is critical: real fans watch the game, they do not pose with their backs to the action. The only exception is the tv-broadcast theme where the broadcast camera naturally captures their faces while they are reacting to the game.
- All text in any overlay, broadcast bar, signage, banner, jersey sponsor, or caption MUST be in Indonesian or English ONLY. NEVER use Malay, Tagalog, Vietnamese, Thai, or other Southeast Asian languages. The user is Indonesian — default to Indonesian for Indonesian league context, English for international leagues.
- Preserve EXACT facial identity of EACH person from the reference photos (no morphing, no chibi-fication, no age/ethnicity change).
- If jersey uploaded: preserve EXACT jersey design, colors, sponsor logos, team name, number, and name on back.
- If team logo uploaded: preserve EXACT logo design, colors, and proportions.
- No watermark, no distortion, no extra fingers, no blur on faces.`;

        const WT_MOOD_LINES = {
            'excited':     'Mood: Excited cheering expressions, raised arms, fist pumps, electrifying energy.',
            'tense':       'Mood: Tense focused expressions, hands clenched, eyes glued to the screen, edge-of-seat moment.',
            'celebrating': 'Mood: Celebrating euphoric expressions, jumping, hugging, screaming in joy after a goal.',
            'relaxed':     'Mood: Relaxed casual expressions, smiling, chatting, light-hearted enjoyment.',
        };

        const WT_ANGLE_LINES = {
            'wide':          'Camera: Wide establishing shot — full scene visible, group composition with environment context.',
            'medium':        'Camera: Medium shot — torso-up framing, balanced subject-and-environment ratio.',
            'close-up':      'Camera: Close-up — tight on face/expression, intimate emotion-focused framing.',
            'low-angle':     'Camera: Low angle — camera positioned below eye-level looking up, dramatic heroic perspective.',
            'high-angle':    'Camera: High angle — camera positioned above looking down, classic TV broadcast camera POV.',
            'over-shoulder': 'Camera: Over-the-shoulder shot — POV behind the subject(s), looking past them toward the TV/match action.',
        };

        // Image-inputs enumeration (variable based on optional jersey/logo)
        function wtImageInputs(ctx) {
            const lines = [`- Images 1 to ${ctx.faceCount}: faces of the ${ctx.faceCount} person(s) in the scene`];
            let next = ctx.faceCount + 1;
            if (ctx.hasJersey) {
                lines.push(`- Image ${next}: jersey/outfit reference — preserve exact design, colors, sponsor logos, team name, number, name on back`);
                next++;
            }
            if (ctx.hasLogo) {
                lines.push(`- Image ${next} (LAST): team logo reference — preserve EXACT logo design and colors; render where appropriate per scene context`);
            }
            return lines.join('\n');
        }

        // Outfit anchor — branches on jersey reference availability
        function wtOutfitLine(ctx) {
            return ctx.hasJersey
                ? `The ${ctx.faceCount === 1 ? 'person' : 'people'} ${ctx.faceCount === 1 ? 'is' : 'are'} wearing the EXACT jersey shown in the jersey reference image (preserve design, colors, sponsor logos, team name, number, name on back).`
                : `The ${ctx.faceCount === 1 ? 'person' : 'people'} ${ctx.faceCount === 1 ? 'is' : 'are'} wearing the official ${ctx.teamSelf} team jersey (use team's actual colors and design).`;
        }

        // Logo anchor — branches on logo reference availability and per-theme rendering context
        function wtLogoLine(ctx, placement) {
            if (!ctx.hasLogo) return '';
            return `Render the EXACT team logo from the logo reference image ${placement} — preserve its design, colors, and proportions.`;
        }

        const bolaThemes = {
            'tv-broadcast': (ctx) => `IMPORTANT — Image inputs:
${wtImageInputs(ctx)}

Ultra-realistic TV broadcast screenshot moment captured during the match "${ctx.teamSelf} vs ${ctx.teamOpponent}". ${ctx.faceCount === 1 ? 'A single fan' : `A group of ${ctx.faceCount} fans`} of ${ctx.teamSelf} in the stadium audience, caught by the broadcast camera. ${wtOutfitLine(ctx)} Sitting in the front rows of a packed stadium, surrounded by passionate supporters in the background (slightly blurred). Stadium lights, motion of cheering crowd. ${ctx.faceCount === 1 ? 'Holding' : 'Some holding'} a typical fan snack/drink (milk tea, coffee, popcorn, or bread) — caught off-guard during the live broadcast.

Broadcast overlay elements:
- Team logo at the top-left corner of the screen. ${wtLogoLine(ctx, 'as the top-left broadcast overlay logo')}
- A broadcast bar at the bottom of the screen reading "LIVE: ${ctx.teamSelf.toUpperCase()} VS ${ctx.teamOpponent.toUpperCase()}"
- Subtle network watermark, slight motion blur on frame edge

${ctx.angleLine}
${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 4K TV broadcast quality, sharp contrast lighting, cinematic depth of field, lens flare from stadium lights. ${WT_NEGATIVE_PROMPT}`,

            'cafe-resto': (ctx) => `IMPORTANT — Image inputs:
${wtImageInputs(ctx)}

A group of ${ctx.faceCount} ${ctx.faceCount === 1 ? 'fan' : 'fans'} of ${ctx.teamSelf} watching the match "${ctx.teamSelf} vs ${ctx.teamOpponent}" at a cozy Indonesian cafe / warung kopi / restoran. ${wtOutfitLine(ctx)} Sitting around a table facing a large TV screen mounted on the wall (TV showing the live match with broadcast bar "LIVE: ${ctx.teamSelf.toUpperCase()} VS ${ctx.teamOpponent.toUpperCase()}"). On the table: coffee cups, kopi susu, snacks, indomie, gorengan, kerupuk. Warm cafe lighting, wooden tables, slight crowd ambiance behind. Eyes on the screen. ${wtLogoLine(ctx, 'on the TV broadcast overlay or as a sticker/banner on the wall')}

${ctx.angleLine}
${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, cozy Indonesian cafe lifestyle photography, warm dim lighting, candid documentary feel. ${WT_NEGATIVE_PROMPT}`,

            'living-room': (ctx) => `IMPORTANT — Image inputs:
${wtImageInputs(ctx)}

A group of ${ctx.faceCount} ${ctx.faceCount === 1 ? 'fan' : 'fans'} of ${ctx.teamSelf} watching the match "${ctx.teamSelf} vs ${ctx.teamOpponent}" together in a cozy living room. ${wtOutfitLine(ctx)} Sitting on a comfortable sofa facing a large flat-screen TV (TV showing the match with broadcast bar "LIVE: ${ctx.teamSelf.toUpperCase()} VS ${ctx.teamOpponent.toUpperCase()}"). Snacks scattered on the coffee table: popcorn, chips, drinks. Warm domestic lighting. ${ctx.faceCount === 1 ? 'Leaning' : 'Some leaning'} forward into the action. ${wtLogoLine(ctx, 'displayed on the TV broadcast overlay')}

${ctx.angleLine}
${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, lifestyle photography, warm cozy home interior, natural lamp lighting. ${WT_NEGATIVE_PROMPT}`,

            'stadion': (ctx) => `IMPORTANT — Image inputs:
${wtImageInputs(ctx)}

A group of ${ctx.faceCount} passionate ${ctx.teamSelf} supporter${ctx.faceCount > 1 ? 's' : ''} at the stadium watching the match "${ctx.teamSelf} vs ${ctx.teamOpponent}" live. ${wtOutfitLine(ctx)} Standing/sitting in the supporters' section, surrounded by a dense packed crowd of fans (slightly blurred), team flags waving, smoke from flares in the air, stadium floodlights, electric atmosphere. Cheering passionately. ${wtLogoLine(ctx, 'on team flags, scarves, or banners held by the crowd in the background')}

${ctx.angleLine}
${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, sports photojournalism, dramatic stadium lighting, lens flare, cinematic crowd. ${WT_NEGATIVE_PROMPT}`,

            'watch-party-outdoor': (ctx) => `IMPORTANT — Image inputs:
${wtImageInputs(ctx)}

A group of ${ctx.faceCount} ${ctx.teamSelf} ${ctx.faceCount === 1 ? 'fan' : 'fans'} at an outdoor watch party for the match "${ctx.teamSelf} vs ${ctx.teamOpponent}". ${wtOutfitLine(ctx)} Gathered in front of a large outdoor projector screen showing the match (broadcast bar "LIVE: ${ctx.teamSelf.toUpperCase()} VS ${ctx.teamOpponent.toUpperCase()}"), sitting on chairs/picnic mats. Open-air venue (could be community field, kampung gathering, hotel garden, or rooftop). String lights overhead, dusk/night ambiance. Food and drinks visible on tables. ${wtLogoLine(ctx, 'on a hanging banner above the screen or on fan-held flags')}

${ctx.angleLine}
${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, lifestyle event photography, warm dusk lighting, festive watch party vibe. ${WT_NEGATIVE_PROMPT}`,

            'sport-bar-pub': (ctx) => `IMPORTANT — Image inputs:
${wtImageInputs(ctx)}

A group of ${ctx.faceCount} ${ctx.teamSelf} ${ctx.faceCount === 1 ? 'fan' : 'fans'} at a sport bar / English-style pub watching the match "${ctx.teamSelf} vs ${ctx.teamOpponent}". ${wtOutfitLine(ctx)} Sitting at wooden bar tables, multiple TV screens visible in the background showing different angles of the match (broadcast bar "LIVE: ${ctx.teamSelf.toUpperCase()} VS ${ctx.teamOpponent.toUpperCase()}"). Dim dramatic lighting with neon signs, beer mugs / drinks on the table, peanuts/wings/snacks. Other patrons cheering in the background (slightly blurred). ${wtLogoLine(ctx, 'on the TV broadcast overlay or as a wall pennant/jersey-frame decoration')}

${ctx.angleLine}
${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, pub atmosphere photography, dramatic dim lighting with neon accents, cinematic depth of field. ${WT_NEGATIVE_PROMPT}`,

            'candid': (ctx) => `IMPORTANT — Image inputs:
${wtImageInputs(ctx)}

A candid unposed photograph capturing ${ctx.faceCount === 1 ? 'a fan' : `${ctx.faceCount} fans`} of ${ctx.teamSelf} AT THE STADIUM watching the match "${ctx.teamSelf} vs ${ctx.teamOpponent}" live. The ${ctx.faceCount === 1 ? 'subject is' : 'subjects are'} NOT looking at the camera — caught in raw natural emotion (mid-cheer, mid-frustration, mid-laugh, mid-gasp, hands gripping head, mouth open in shout). Their bodies and gaze are NATURALLY ORIENTED toward the pitch and the match action — they are watching the game, not posing or facing the camera.

Setting: stadium supporter section — dense crowd of fans visible in the background (slightly blurred), team flags waving, smoke from flares in the air, stadium floodlights overhead, the pitch and match action visible in the background (or implied by the direction of their gaze). ${wtOutfitLine(ctx)} ${wtLogoLine(ctx, 'on team flags, scarves, supporter banners, or jersey worn by surrounding crowd')}

The photographer is positioned naturally (from the side, slightly behind, or in front but the subject is too immersed in the game to notice the camera). Smartphone snapshot quality: natural ambient stadium lighting, slight imperfect framing, soft focus on the surrounding crowd, unposed authentic moment — like a friend in the next seat snapped the photo without warning during a tense or thrilling moment of the match.

${ctx.angleLine}
${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic candid stadium photography, smartphone snapshot quality, natural ambient stadium lighting, slight motion blur on hands/expressions, unposed authentic moment. ${WT_NEGATIVE_PROMPT}`,
        };

        // Registry of all modes (extensibility — future: nonton-f1, nonton-esport, etc.)
        const watchTogetherModes = {
            'nonton-bola': { themes: bolaThemes, defaultRatio: '16:9', maxFaces: 5 },
        };

        function buildWatchTogetherPrompt(mode, themeId, moodId, angleId, faceCount, hasJersey, hasLogo, teamSelf, teamOpponent, customText) {
            const modeConfig = watchTogetherModes[mode] || watchTogetherModes['nonton-bola'];
            const registry = modeConfig.themes;
            const fallbackId = Object.keys(registry)[0];
            const builder = registry[themeId] || registry[fallbackId];
            return builder({
                moodLine: WT_MOOD_LINES[moodId] || WT_MOOD_LINES['excited'],
                angleLine: WT_ANGLE_LINES[angleId] || WT_ANGLE_LINES['wide'],
                customLine: customText ? `Additional user direction: ${customText}` : '',
                faceCount: faceCount,
                hasJersey: !!hasJersey,
                hasLogo: !!hasLogo,
                teamSelf: (teamSelf || 'the team').trim(),
                teamOpponent: (teamOpponent || 'the opponent').trim(),
            });
        }

        // ============================================================================
        // Handlers
        // ============================================================================
        const wtGenerateBtn = document.getElementById('wt-generate-btn');
        const wtFacesInput = document.getElementById('wt-faces-upload');
        const wtFacesPreview = document.getElementById('wt-faces-preview');
        const wtFacesCount = document.getElementById('wt-faces-count');
        const wtJerseyInput = document.getElementById('wt-jersey-upload');
        const wtJerseyPreview = document.getElementById('wt-jersey-preview');
        const wtLogoInput = document.getElementById('wt-logo-upload');
        const wtLogoPreview = document.getElementById('wt-logo-preview');
        const wtTeamSelfInput = document.getElementById('wt-team-self');
        const wtTeamOpponentInput = document.getElementById('wt-team-opponent');
        const wtCustomPromptInput = document.getElementById('wt-custom-prompt');
        const wtEmptyState = document.getElementById('wt-empty-state');
        const wtLoadingEl = document.getElementById('wt-loading');
        const wtResultsHeader = document.getElementById('wt-results-header');
        const wtResultsCountEl = document.getElementById('wt-results-count');
        const wtResultsGrid = document.getElementById('wt-results-grid');
        const wtDownloadAllBtn = document.getElementById('wt-download-all-btn');
        const wtPreviewModal = document.getElementById('wt-preview-modal');
        const wtPreviewImage = document.getElementById('wt-preview-image');
        const wtPreviewClose = document.getElementById('wt-preview-close');

        function checkWTReadyToGenerate() {
            if (!wtGenerateBtn) return;
            const hasFaces = wtFaceImages.length > 0;
            const hasTeamSelf = (wtTeamSelfInput?.value || '').trim().length > 0;
            const hasTeamOpponent = (wtTeamOpponentInput?.value || '').trim().length > 0;
            wtGenerateBtn.disabled = !(hasFaces && hasTeamSelf && hasTeamOpponent);
        }

        function renderWTFacesPreview() {
            if (!wtFacesPreview) return;
            wtFacesPreview.innerHTML = '';
            if (wtFaceImages.length === 0) {
                wtFacesPreview.classList.add('hidden');
                if (wtFacesCount) wtFacesCount.textContent = '0/5 foto';
                return;
            }
            wtFacesPreview.classList.remove('hidden');
            wtFaceImages.forEach((img, idx) => {
                const card = document.createElement('div');
                card.className = 'relative aspect-square rounded-lg overflow-hidden border-2';
                card.style.borderColor = '#0ea5e9';
                card.innerHTML = `
                    <img src="data:${img.mimeType};base64,${img.base64}" class="w-full h-full object-cover" alt="Face ${idx + 1}">
                    <button type="button" data-wt-face-remove="${idx}" class="absolute top-1 right-1 bg-red-500 text-white rounded-full w-6 h-6 flex items-center justify-center text-xs font-bold shadow-md hover:bg-red-600">&times;</button>
                    <div class="absolute bottom-1 left-1 bg-black/60 text-white text-xs px-2 py-0.5 rounded">${idx + 1}</div>
                `;
                wtFacesPreview.appendChild(card);
            });
            if (wtFacesCount) wtFacesCount.textContent = `${wtFaceImages.length}/5 foto`;
        }

        function handleWTFaceUpload() {
            if (!wtFacesInput) return;
            wtFacesInput.addEventListener('change', async (e) => {
                const files = Array.from(e.target.files || []);
                for (const file of files) {
                    if (wtFaceImages.length >= 5) break;
                    await new Promise((resolve) => {
                        const reader = new FileReader();
                        reader.onload = (evt) => {
                            const dataUrl = evt.target.result;
                            const base64 = dataUrl.split(',')[1];
                            const mimeType = (dataUrl.split(';')[0] || '').split(':')[1];
                            wtFaceImages.push({ mimeType, base64 });
                            resolve();
                        };
                        reader.readAsDataURL(file);
                    });
                }
                renderWTFacesPreview();
                checkWTReadyToGenerate();
                wtFacesInput.value = ''; // allow re-selecting same files
            });

            // Remove face by index (event delegation)
            if (wtFacesPreview) {
                wtFacesPreview.addEventListener('click', (e) => {
                    const btn = e.target.closest('[data-wt-face-remove]');
                    if (!btn) return;
                    const idx = parseInt(btn.dataset.wtFaceRemove, 10);
                    if (!isNaN(idx)) {
                        wtFaceImages.splice(idx, 1);
                        renderWTFacesPreview();
                        checkWTReadyToGenerate();
                    }
                });
            }
        }

        function handleWTJerseyUpload() {
            if (!wtJerseyInput) return;
            wtJerseyInput.addEventListener('change', (e) => {
                const file = e.target.files[0];
                if (!file) return;
                const reader = new FileReader();
                reader.onload = (evt) => {
                    const dataUrl = evt.target.result;
                    const base64 = dataUrl.split(',')[1];
                    const mimeType = (dataUrl.split(';')[0] || '').split(':')[1];
                    wtJerseyImage = { mimeType, base64 };

                    if (wtJerseyPreview) {
                        const img = wtJerseyPreview.querySelector('img');
                        if (img) img.src = dataUrl;
                        wtJerseyPreview.classList.remove('hidden');
                    }
                    checkWTReadyToGenerate();
                };
                reader.readAsDataURL(file);
            });
        }

        function handleWTLogoUpload() {
            if (!wtLogoInput) return;
            wtLogoInput.addEventListener('change', (e) => {
                const file = e.target.files[0];
                if (!file) return;
                const reader = new FileReader();
                reader.onload = (evt) => {
                    const dataUrl = evt.target.result;
                    const base64 = dataUrl.split(',')[1];
                    const mimeType = (dataUrl.split(';')[0] || '').split(':')[1];
                    wtLogoImage = { mimeType, base64 };

                    if (wtLogoPreview) {
                        const img = wtLogoPreview.querySelector('img');
                        if (img) img.src = dataUrl;
                        wtLogoPreview.classList.remove('hidden');
                    }
                    checkWTReadyToGenerate();
                };
                reader.readAsDataURL(file);
            });
        }

        handleWTFaceUpload();
        handleWTJerseyUpload();
        handleWTLogoUpload();

        // Theme selector
        document.querySelectorAll('button[data-wt-theme]').forEach(btn => {
            btn.addEventListener('click', () => {
                wtSelectedTheme = btn.dataset.wtTheme;
                document.querySelectorAll('button[data-wt-theme]').forEach(b => {
                    b.classList.toggle('selected', b.dataset.wtTheme === wtSelectedTheme);
                });
            });
        });

        // Mood selector
        document.querySelectorAll('#wt-mood-options button[data-wt-mood]').forEach(btn => {
            btn.addEventListener('click', () => {
                wtSelectedMood = btn.dataset.wtMood;
                document.querySelectorAll('#wt-mood-options button[data-wt-mood]').forEach(b => {
                    b.classList.toggle('selected', b.dataset.wtMood === wtSelectedMood);
                });
            });
        });

        // Camera angle selector
        document.querySelectorAll('#wt-angle-options button[data-wt-angle]').forEach(btn => {
            btn.addEventListener('click', () => {
                wtSelectedAngle = btn.dataset.wtAngle;
                document.querySelectorAll('#wt-angle-options button[data-wt-angle]').forEach(b => {
                    b.classList.toggle('selected', b.dataset.wtAngle === wtSelectedAngle);
                });
            });
        });

        // Ratio selector
        document.querySelectorAll('#wt-ratio-options button[data-wt-ratio]').forEach(btn => {
            btn.addEventListener('click', () => {
                wtSelectedRatio = btn.dataset.wtRatio;
                document.querySelectorAll('#wt-ratio-options button[data-wt-ratio]').forEach(b => {
                    b.classList.toggle('selected', b.dataset.wtRatio === wtSelectedRatio);
                });
            });
        });

        // Count selector
        document.querySelectorAll('#wt-count-options button[data-wt-count]').forEach(btn => {
            btn.addEventListener('click', () => {
                wtSelectedCount = parseInt(btn.dataset.wtCount, 10);
                document.querySelectorAll('#wt-count-options button[data-wt-count]').forEach(b => {
                    b.classList.toggle('selected', parseInt(b.dataset.wtCount, 10) === wtSelectedCount);
                });
            });
        });

        // Team inputs (self + opponent) trigger ready-check
        if (wtTeamSelfInput) wtTeamSelfInput.addEventListener('input', checkWTReadyToGenerate);
        if (wtTeamOpponentInput) wtTeamOpponentInput.addEventListener('input', checkWTReadyToGenerate);

        // Preview modal handlers
        function openWTPreview(imageUrl) {
            if (!wtPreviewModal || !wtPreviewImage) return;
            wtPreviewImage.src = imageUrl;
            wtPreviewModal.classList.remove('hidden');
            document.body.style.overflow = 'hidden';
        }
        function closeWTPreview() {
            if (!wtPreviewModal) return;
            wtPreviewModal.classList.add('hidden');
            document.body.style.overflow = '';
        }
        if (wtPreviewClose) wtPreviewClose.addEventListener('click', closeWTPreview);
        if (wtPreviewModal) {
            wtPreviewModal.addEventListener('click', (e) => {
                if (e.target === wtPreviewModal) closeWTPreview();
            });
        }
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && wtPreviewModal && !wtPreviewModal.classList.contains('hidden')) {
                closeWTPreview();
            }
        });

        // Results grid click delegation — preview button
        if (wtResultsGrid) {
            wtResultsGrid.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-wt-action="preview"]');
                if (!btn) return;
                const idx = parseInt(btn.dataset.wtIndex, 10);
                if (!isNaN(idx) && wtGeneratedImages[idx]) {
                    openWTPreview(wtGeneratedImages[idx].url);
                }
            });
        }

        async function generateSingleWTPhoto(index, basePrompt) {
            const card = document.getElementById(`wt-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const finalPrompt = `${basePrompt}\n\nVariation ${index}: render with slight variation in composition or camera angle while preserving identities and outfit exactly.`;

                // WT DISPATCH: Images 1..N = faces, then jersey (if uploaded), then LAST = logo (if uploaded)
                // Order is load-bearing — prompt enumerates exact position of each ref.
                const parts = [{ text: finalPrompt }];
                wtFaceImages.forEach((img) => {
                    parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
                });
                if (wtJerseyImage) {
                    parts.push({ inlineData: { mimeType: wtJerseyImage.mimeType, data: wtJerseyImage.base64 } });
                }
                if (wtLogoImage) {
                    parts.push({ inlineData: { mimeType: wtLogoImage.mimeType, data: wtLogoImage.base64 } });
                }

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: wtSelectedRatio }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data in response');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `watch-together-${wtSelectedTheme}-${index}-${Date.now()}.jpg`;

                wtGeneratedImages[index - 1] = { url: imageUrl, filename };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = `<img src="${imageUrl}" class="w-full object-contain cursor-pointer" alt="Watch Together ${index}" data-wt-action="preview" data-wt-index="${index - 1}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition flex items-end justify-center pb-3 gap-2">
                            <button type="button" data-wt-action="preview" data-wt-index="${index - 1}" class="bg-white/95 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition flex items-center gap-2">
                                <i class="fas fa-eye"></i><span class="hidden sm:inline" data-i18n="wt.preview">Preview</span>
                            </button>
                            <a href="${imageUrl}" download="${filename}" class="bg-gradient-to-r from-green-600 to-sky-600 hover:from-green-700 hover:to-sky-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition flex items-center gap-2">
                                <i class="fas fa-download"></i><span class="hidden sm:inline" data-i18n="wt.download">Download</span>
                            </a>
                        </div>`;
                }
            } catch (err) {
                console.error(`WT generate error #${index}:`, err);
                if (card) card.innerHTML = '';
            }
        }

        async function generateWTPhotos() {
            const lang = (typeof localStorage !== 'undefined' && localStorage.getItem('app_language')) || 'id';

            if (wtFaceImages.length === 0) {
                // wt.error-need-face
                const msg = (lang === 'en')
                    ? 'Upload at least 1 face photo first to generate.'
                    : 'Upload minimal 1 foto wajah dulu untuk generate.';
                if (typeof showError === 'function') { showError(msg); } else { alert(msg); }
                return;
            }

            const teamSelf = (wtTeamSelfInput?.value || '').trim();
            if (!teamSelf) {
                // wt.error-need-team-self
                const msg = (lang === 'en')
                    ? 'Enter your own team name first.'
                    : 'Tulis nama Tim Sendiri dulu.';
                if (typeof showError === 'function') { showError(msg); } else { alert(msg); }
                return;
            }

            const teamOpponent = (wtTeamOpponentInput?.value || '').trim();
            if (!teamOpponent) {
                // wt.error-need-team-opponent
                const msg = (lang === 'en')
                    ? 'Enter the opponent team name first.'
                    : 'Tulis nama Tim Lawan dulu.';
                if (typeof showError === 'function') { showError(msg); } else { alert(msg); }
                return;
            }

            if (wtEmptyState) wtEmptyState.classList.add('hidden');
            if (wtLoadingEl) wtLoadingEl.classList.remove('hidden');
            if (wtResultsHeader) wtResultsHeader.classList.add('hidden');

            const customText = (wtCustomPromptInput?.value || '').trim();
            const basePrompt = buildWatchTogetherPrompt(
                wtSelectedMode, wtSelectedTheme, wtSelectedMood, wtSelectedAngle,
                wtFaceImages.length, !!wtJerseyImage, !!wtLogoImage,
                teamSelf, teamOpponent, customText
            );

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                wtGeneratedImages = [];
                if (wtResultsGrid) {
                    wtResultsGrid.innerHTML = '';
                    wtResultsGrid.classList.remove('hidden');
                }

                for (let i = 1; i <= wtSelectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `wt-card-${i}`;
                    card.className = 'relative rounded-xl overflow-hidden bg-gradient-to-br from-green-50 to-sky-50 flex items-center justify-center aspect-video border-2';
                    card.style.borderColor = '#0ea5e9';
                    card.innerHTML = `<div class="animate-spin rounded-full h-10 w-10 border-b-3 border-t-3" style="border-color: #0ea5e9;"></div>`;
                    if (wtResultsGrid) wtResultsGrid.appendChild(card);
                }

                const promises = Array.from({ length: wtSelectedCount }, (_, i) =>
                    generateSingleWTPhoto(i + 1, basePrompt)
                );
                await Promise.allSettled(promises);

                if (wtResultsGrid) {
                    wtResultsGrid.querySelectorAll('[id^="wt-card-"]').forEach(card => {
                        if (!card.querySelector('img')) card.remove();
                    });
                }
                successCount = wtGeneratedImages.filter(Boolean).length;
            }

            if (wtLoadingEl) wtLoadingEl.classList.add('hidden');
            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                if (wtResultsHeader) wtResultsHeader.classList.remove('hidden');
                if (wtResultsCountEl) wtResultsCountEl.textContent = wtGeneratedImages.length;
                if (wtDownloadAllBtn) wtDownloadAllBtn.classList.remove('hidden');
            }
        }

        if (wtGenerateBtn) wtGenerateBtn.addEventListener('click', generateWTPhotos);

        if (wtDownloadAllBtn) {
            wtDownloadAllBtn.addEventListener('click', () => {
                wtGeneratedImages.filter(Boolean).forEach(({ url, filename }, i) => {
                    setTimeout(() => {
                        const a = document.createElement('a');
                        a.href = url;
                        a.download = filename;
                        a.click();
                    }, i * 200);
                });
            });
        }

        checkWTReadyToGenerate();
    })();

    // ==================== STUDIO PODCAST ====================
    (function podcastTab() {
        const apiKey = "";

        // State
        let podSelectedTheme = 'studio-pro';
        let podSelectedRatio = '16:9';
        let podSelectedCount = 4;
        let podPhotoImage = null; // { mimeType, base64 }
        let podGeneratedImages = [];

        const POD_NEGATIVE_PROMPT = `STRICT RULES:
- Preserve the EXACT facial identity, hairstyle, and skin tone of the person from the reference photo (no morphing, no chibi-fication, no age/ethnicity change).
- The person is seated naturally at a podcast desk, mid-conversation, with relaxed authentic posture (talking, gesturing, or listening). NOT a stiff studio pose.
- Hands look natural — no extra fingers, no deformed hands.
- Any visible text (signage, LED panel, neon sign, caption) MUST be in Indonesian or English ONLY.
- No watermark, no distortion, no blur on the face.`;

        // Always-on podcast equipment anchor
        const POD_MIC_ANCHOR = `A professional condenser podcast microphone on an adjustable boom arm with a pop filter is positioned in front of the seated person. Studio headphones are present (worn on the head, around the neck, or resting on the desk).`;

        // One reference image: the character/person
        const POD_IMG_ANCHOR = `IMPORTANT — One reference image: the person/character. Preserve their EXACT facial identity, hairstyle, skin tone, and overall appearance from the reference photo. Seat this exact person in the scene as the podcast host.`;

        // Auto-varied camera angle pool — cycled per result card so each generate
        // returns a different angle (deterministic even distribution, no duplicates).
        const POD_ANGLE_POOL = [
            'Camera: Medium shot — waist-up framing, the person and the microphone clearly in frame, eye-level.',
            'Camera: Wide establishing shot — the full podcast set visible, the person seated at the desk with environment context.',
            'Camera: Close-up — tight on the face and microphone, intimate expression-focused framing.',
            'Camera: Three-quarter / side profile — the person seen from the side speaking into the mic, dynamic angle.',
            'Camera: Low angle — camera positioned below eye-level looking up, confident dramatic perspective.',
            'Camera: Over-the-shoulder shot — from behind the person past their shoulder toward the microphone and the set.',
            'Camera: High angle — camera slightly above looking down at the desk and the seated person.',
        ];

        const POD_THEMES = {
            'studio-pro': (ctx) => `${POD_IMG_ANCHOR}

Ultra-realistic photo of the person recording a podcast at a modern professional podcast studio desk. ${POD_MIC_ANCHOR} Background: acoustic foam / sound-treatment panels on the wall, a tidy desk, soft warm key lighting with subtle accent lights, the polished big-production look of a popular YouTube podcast set.

${ctx.customLine}

Style: ultra-realistic, 8K, professional studio photography, cinematic depth of field, sharp focus on the subject. ${POD_NEGATIVE_PROMPT}`,

            'cozy-home': (ctx) => `${POD_IMG_ANCHOR}

Ultra-realistic photo of the person recording a podcast in a warm cozy home studio. ${POD_MIC_ANCHOR} Background: a wooden desk, a bookshelf, indoor plants, soft warm lamp light and gentle fairy lights, a personal intimate at-home atmosphere.

${ctx.customLine}

Style: ultra-realistic, 8K, cozy lifestyle photography, warm dim lighting, natural and inviting. ${POD_NEGATIVE_PROMPT}`,

            'neon-gaming': (ctx) => `${POD_IMG_ANCHOR}

Ultra-realistic photo of the person recording a podcast in a futuristic neon gaming/tech setup. ${POD_MIC_ANCHOR} Background: RGB LED strips and neon accent lighting in purple, blue and magenta, a dark room with glowing tech, gamer/streamer aesthetic.

${ctx.customLine}

Style: ultra-realistic, 8K, cinematic neon lighting, vibrant glow, sharp focus on the subject. ${POD_NEGATIVE_PROMPT}`,

            'dark-moody': (ctx) => `${POD_IMG_ANCHOR}

Ultra-realistic photo of the person recording a podcast in a dark moody cinematic set. ${POD_MIC_ANCHOR} Background: a deep dark backdrop, a single dramatic spotlight on the person, strong chiaroscuro lighting and deep shadows, a serious storytelling atmosphere.

${ctx.customLine}

Style: ultra-realistic, 8K, dramatic cinematic lighting, deep contrast, sharp focus on the subject. ${POD_NEGATIVE_PROMPT}`,

            'custom': (ctx) => `${POD_IMG_ANCHOR}

Ultra-realistic photo of the person recording a podcast. ${POD_MIC_ANCHOR} Setting and atmosphere: ${ctx.customTheme || 'a tasteful podcast set'}.

${ctx.customLine}

Style: ultra-realistic, 8K, cinematic lighting, sharp focus on the subject. ${POD_NEGATIVE_PROMPT}`,
        };

        function buildPodcastPrompt(themeId, customTheme, customText) {
            const builder = POD_THEMES[themeId] || POD_THEMES['studio-pro'];
            return builder({
                customLine: customText ? `Additional user direction: ${customText}` : '',
                customTheme: (customTheme || '').trim(),
            });
        }

        // ============================================================================
        // Handlers
        // ============================================================================
        const podGenerateBtn = document.getElementById('pod-generate-btn');
        const podPhotoInput = document.getElementById('pod-photo-upload');
        const podPhotoPreview = document.getElementById('pod-photo-preview');
        const podCustomThemeWrap = document.getElementById('pod-custom-theme-wrap');
        const podCustomThemeInput = document.getElementById('pod-custom-theme');
        const podCustomPromptInput = document.getElementById('pod-custom-prompt');
        const podEmptyState = document.getElementById('pod-empty-state');
        const podLoadingEl = document.getElementById('pod-loading');
        const podResultsHeader = document.getElementById('pod-results-header');
        const podResultsCountEl = document.getElementById('pod-results-count');
        const podResultsGrid = document.getElementById('pod-results-grid');
        const podDownloadAllBtn = document.getElementById('pod-download-all-btn');
        const podPreviewModal = document.getElementById('pod-preview-modal');
        const podPreviewImage = document.getElementById('pod-preview-image');
        const podPreviewClose = document.getElementById('pod-preview-close');

        function checkPodReadyToGenerate() {
            if (!podGenerateBtn) return;
            podGenerateBtn.disabled = !podPhotoImage;
        }

        if (podPhotoInput) {
            podPhotoInput.addEventListener('change', (e) => {
                const file = e.target.files[0];
                if (!file) return;
                const reader = new FileReader();
                reader.onload = (evt) => {
                    const dataUrl = evt.target.result;
                    const base64 = dataUrl.split(',')[1];
                    const mimeType = (dataUrl.split(';')[0] || '').split(':')[1];
                    podPhotoImage = { mimeType, base64 };
                    if (podPhotoPreview) {
                        const img = podPhotoPreview.querySelector('img');
                        if (img) img.src = dataUrl;
                        podPhotoPreview.classList.remove('hidden');
                    }
                    checkPodReadyToGenerate();
                };
                reader.readAsDataURL(file);
            });
        }

        // Theme selector (Custom reveals free-text setting input)
        document.querySelectorAll('#pod-themes-grid button[data-pod-theme]').forEach(btn => {
            btn.addEventListener('click', () => {
                podSelectedTheme = btn.dataset.podTheme;
                document.querySelectorAll('#pod-themes-grid button[data-pod-theme]').forEach(b => {
                    b.classList.toggle('selected', b.dataset.podTheme === podSelectedTheme);
                });
                if (podCustomThemeWrap) {
                    podCustomThemeWrap.classList.toggle('hidden', podSelectedTheme !== 'custom');
                }
            });
        });

        // Ratio selector
        document.querySelectorAll('#pod-ratio-options button[data-pod-ratio]').forEach(btn => {
            btn.addEventListener('click', () => {
                podSelectedRatio = btn.dataset.podRatio;
                document.querySelectorAll('#pod-ratio-options button[data-pod-ratio]').forEach(b => {
                    b.classList.toggle('selected', b.dataset.podRatio === podSelectedRatio);
                });
            });
        });

        // Count selector
        document.querySelectorAll('#pod-count-options button[data-pod-count]').forEach(btn => {
            btn.addEventListener('click', () => {
                podSelectedCount = parseInt(btn.dataset.podCount, 10);
                document.querySelectorAll('#pod-count-options button[data-pod-count]').forEach(b => {
                    b.classList.toggle('selected', parseInt(b.dataset.podCount, 10) === podSelectedCount);
                });
            });
        });

        // Preview modal handlers
        function openPodPreview(imageUrl) {
            if (!podPreviewModal || !podPreviewImage) return;
            podPreviewImage.src = imageUrl;
            podPreviewModal.classList.remove('hidden');
            document.body.style.overflow = 'hidden';
        }
        function closePodPreview() {
            if (!podPreviewModal) return;
            podPreviewModal.classList.add('hidden');
            document.body.style.overflow = '';
        }
        if (podPreviewClose) podPreviewClose.addEventListener('click', closePodPreview);
        if (podPreviewModal) {
            podPreviewModal.addEventListener('click', (e) => {
                if (e.target === podPreviewModal) closePodPreview();
            });
        }
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && podPreviewModal && !podPreviewModal.classList.contains('hidden')) {
                closePodPreview();
            }
        });

        // Results grid click delegation — preview button
        if (podResultsGrid) {
            podResultsGrid.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-pod-action="preview"]');
                if (!btn) return;
                const idx = parseInt(btn.dataset.podIndex, 10);
                if (!isNaN(idx) && podGeneratedImages[idx]) {
                    openPodPreview(podGeneratedImages[idx].url);
                }
            });
        }

        async function generateSinglePodPhoto(index, basePrompt) {
            const card = document.getElementById(`pod-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const angleLine = POD_ANGLE_POOL[(index - 1) % POD_ANGLE_POOL.length];
                const finalPrompt = `${basePrompt}\n\n${angleLine}\n\nVariation ${index}: slightly vary the person's pose, gesture and expression while keeping the exact identity, outfit, and the podcast microphone setup.`;

                const parts = [
                    { text: finalPrompt },
                    { inlineData: { mimeType: podPhotoImage.mimeType, data: podPhotoImage.base64 } }
                ];

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: podSelectedRatio }
                    },
                    safetySettings: [
                        { category: 'HARM_CATEGORY_HARASSMENT',        threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_HATE_SPEECH',        threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_SEXUALLY_EXPLICIT',  threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_DANGEROUS_CONTENT',  threshold: 'BLOCK_NONE' }
                    ]
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data in response');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `podcast-${podSelectedTheme}-${index}-${Date.now()}.jpg`;

                podGeneratedImages[index - 1] = { url: imageUrl, filename };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = `<img src="${imageUrl}" class="w-full object-contain cursor-pointer" alt="Podcast ${index}" data-pod-action="preview" data-pod-index="${index - 1}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition flex items-end justify-center pb-3 gap-2">
                            <button type="button" data-pod-action="preview" data-pod-index="${index - 1}" class="bg-white/95 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition flex items-center gap-2">
                                <i class="fas fa-eye"></i><span class="hidden sm:inline" data-i18n="pod.preview">Preview</span>
                            </button>
                            <a href="${imageUrl}" download="${filename}" data-action="download" class="bg-gradient-to-r from-violet-600 to-pink-600 hover:from-violet-700 hover:to-pink-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition flex items-center gap-2">
                                <i class="fas fa-download"></i><span class="hidden sm:inline" data-i18n="pod.download">Download</span>
                            </a>
                        </div>`;
                }
            } catch (err) {
                console.error(`Podcast generate error #${index}:`, err);
                if (card) card.innerHTML = '';
            }
        }

        async function generatePodPhotos() {
            const lang = (typeof localStorage !== 'undefined' && localStorage.getItem('app_language')) || 'id';

            if (!podPhotoImage) {
                const msg = (lang === 'en')
                    ? 'Upload a character photo first to generate.'
                    : 'Upload foto karakter dulu untuk generate.';
                if (typeof showError === 'function') { showError(msg); } else { alert(msg); }
                return;
            }

            if (podEmptyState) podEmptyState.classList.add('hidden');
            if (podLoadingEl) podLoadingEl.classList.remove('hidden');
            if (podResultsHeader) podResultsHeader.classList.add('hidden');

            const customTheme = (podCustomThemeInput?.value || '').trim();
            const customText = (podCustomPromptInput?.value || '').trim();
            const basePrompt = buildPodcastPrompt(podSelectedTheme, customTheme, customText);

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                podGeneratedImages = [];
                if (podResultsGrid) {
                    podResultsGrid.innerHTML = '';
                    podResultsGrid.classList.remove('hidden');
                }

                for (let i = 1; i <= podSelectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `pod-card-${i}`;
                    card.className = 'relative rounded-xl overflow-hidden bg-gradient-to-br from-violet-50 to-pink-50 flex items-center justify-center aspect-video border-2';
                    card.style.borderColor = '#a855f7';
                    card.innerHTML = `<div class="animate-spin rounded-full h-10 w-10 border-b-3 border-t-3" style="border-color: #a855f7;"></div>`;
                    if (podResultsGrid) podResultsGrid.appendChild(card);
                }

                const promises = Array.from({ length: podSelectedCount }, (_, i) =>
                    generateSinglePodPhoto(i + 1, basePrompt)
                );
                await Promise.allSettled(promises);

                if (podResultsGrid) {
                    podResultsGrid.querySelectorAll('[id^="pod-card-"]').forEach(card => {
                        if (!card.querySelector('img')) card.remove();
                    });
                }
                successCount = podGeneratedImages.filter(Boolean).length;
            }

            if (podLoadingEl) podLoadingEl.classList.add('hidden');
            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                if (podResultsHeader) podResultsHeader.classList.remove('hidden');
                if (podResultsCountEl) podResultsCountEl.textContent = podGeneratedImages.filter(Boolean).length;
                if (podDownloadAllBtn) podDownloadAllBtn.classList.remove('hidden');
                if (podResultsGrid) podResultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' });
            }
        }

        if (podGenerateBtn) podGenerateBtn.addEventListener('click', async () => {
            if (podGenerateBtn.disabled) return;
            podGenerateBtn.disabled = true;
            try { await generatePodPhotos(); }
            finally { checkPodReadyToGenerate(); }
        });

        if (podDownloadAllBtn) {
            podDownloadAllBtn.addEventListener('click', () => {
                podGeneratedImages.filter(Boolean).forEach(({ url, filename }, i) => {
                    setTimeout(() => {
                        const a = document.createElement('a');
                        a.href = url;
                        a.download = filename;
                        a.click();
                    }, i * 200);
                });
            });
        }

        checkPodReadyToGenerate();
    })();

    // ==================== FOTO WORLD CUP ====================
    (function worldCupTab() {
        const apiKey = "";

        // State
        let wcSelectedTheme = 'stadium-supporter';
        let wcSelectedRatio = '16:9';
        let wcSelectedCount = 4;
        let wcFaceImages = []; // Array of { mimeType, base64 }, max 5
        let wcGeneratedImages = [];

        const WC_NEGATIVE_PROMPT = `STRICT RULES:
- Preserve the EXACT facial identity of EACH person from the reference photos (no morphing, no chibi-fication, no age/ethnicity change).
- Use the official national team jersey colors and flag of the given country accurately.
- All text in any overlay, banner, scoreboard, signage, jersey sponsor, or caption MUST be in Indonesian or English ONLY. NEVER use Malay, Tagalog, Vietnamese, Thai, or other Southeast Asian languages.
- Hands look natural — no extra fingers, no deformed hands.
- No watermark, no distortion, no blur on faces.`;

        // Auto-varied camera angle pool — cycled per result card.
        const WC_ANGLE_POOL = [
            'Camera: Wide establishing shot — the full stadium / scene visible with rich environment context.',
            'Camera: Medium shot — waist-up framing, subjects and surroundings balanced.',
            'Camera: Close-up — tight on faces and raw emotion, intimate framing.',
            'Camera: Low angle — camera below eye-level looking up, heroic dramatic perspective.',
            'Camera: High angle — elevated broadcast camera POV looking down on the scene.',
            'Camera: Dynamic action angle — slight motion blur, energetic sports-photography feel.',
            'Camera: Over-the-shoulder / behind perspective looking toward the action and the crowd.',
        ];

        function wcImageInputs(faceCount) {
            return `- Images 1 to ${faceCount}: faces of the ${faceCount} person(s) to place into the scene (preserve each identity exactly)`;
        }

        const WC_THEMES = {
            'stadium-supporter': (ctx) => `IMPORTANT — Image inputs:
${wcImageInputs(ctx.faceCount)}

Ultra-realistic photo of ${ctx.subj} as passionate ${ctx.country} football supporter(s) in the stadium stands during a World Cup match. Wearing the ${ctx.country} national team jersey and colors, faces painted with the ${ctx.country} flag, holding ${ctx.country} flags and scarves, surrounded by a dense roaring crowd (slightly blurred), confetti drifting, bright stadium floodlights. Festive electric World Cup atmosphere.

${ctx.customLine}

Style: ultra-realistic, 8K, sports photojournalism, dramatic stadium lighting, lens flare. ${WC_NEGATIVE_PROMPT}`,

            'on-pitch-player': (ctx) => `IMPORTANT — Image inputs:
${wcImageInputs(ctx.faceCount)}

Ultra-realistic photo of ${ctx.subj} as professional World Cup football player(s) standing on the pitch of a massive packed stadium. Wearing the official ${ctx.country} national team kit (correct colors and crest), confident athletic posture, stadium floodlights and a huge crowd in the background.

${ctx.customLine}

Style: ultra-realistic, 8K, professional sports photography, cinematic depth of field. ${WC_NEGATIVE_PROMPT}`,

            'lift-trophy': (ctx) => `IMPORTANT — Image inputs:
${wcImageInputs(ctx.faceCount)}

Ultra-realistic photo of ${ctx.subj} as ${ctx.country} football champion(s) lifting the golden World Cup trophy in triumphant celebration. Gold confetti raining down, euphoric expressions, wearing the ${ctx.country} national team kit with gold medals, a packed stadium erupting in the background, dramatic spotlight. Iconic victory moment.

${ctx.customLine}

Style: ultra-realistic, 8K, epic sports celebration photography, golden cinematic lighting. ${WC_NEGATIVE_PROMPT}`,

            'jersey-portrait': (ctx) => `IMPORTANT — Image inputs:
${wcImageInputs(ctx.faceCount)}

Cinematic studio portrait of ${ctx.subj} wearing the ${ctx.country} national team jersey, with ${ctx.country} flag face paint on the cheeks. Dramatic moody lighting, ${ctx.country} flag colors as a background accent, intense proud expression, high-detail editorial sports portrait.

${ctx.customLine}

Style: ultra-realistic, 8K, editorial portrait photography, dramatic studio lighting, sharp detail. ${WC_NEGATIVE_PROMPT}`,

            'goal-celebration': (ctx) => `IMPORTANT — Image inputs:
${wcImageInputs(ctx.faceCount)}

Ultra-realistic photo of ${ctx.subj} as ${ctx.country} player(s) celebrating a goal on the pitch — sliding on the knees, arms wide, screaming in joy — wearing the ${ctx.country} kit. The stadium crowd exploding in the background, floodlights, grass spray flying, peak-emotion sports action shot.

${ctx.customLine}

Style: ultra-realistic, 8K, high-speed sports action photography, motion energy. ${WC_NEGATIVE_PROMPT}`,

            'fan-zone': (ctx) => `IMPORTANT — Image inputs:
${wcImageInputs(ctx.faceCount)}

Ultra-realistic photo of ${ctx.subj} as ${ctx.country} fans at an outdoor World Cup fan zone / public viewing festival. A giant screen showing the match in the background, a crowd of fans waving ${ctx.country} flags, food stalls, warm string lights, festive night atmosphere, wearing ${ctx.country} jerseys.

${ctx.customLine}

Style: ultra-realistic, 8K, lifestyle event photography, warm festive night lighting. ${WC_NEGATIVE_PROMPT}`,

            'player-tunnel': (ctx) => `IMPORTANT — Image inputs:
${wcImageInputs(ctx.faceCount)}

Ultra-realistic photo of ${ctx.subj} as ${ctx.country} national team player(s) walking out of the stadium players' tunnel onto the pitch before a World Cup match, wearing the ${ctx.country} kit. Dramatic tunnel lighting transitioning into the bright stadium, focused game-face expression, photographers' camera flashes.

${ctx.customLine}

Style: ultra-realistic, 8K, cinematic sports photography, dramatic light transition. ${WC_NEGATIVE_PROMPT}`,

            'team-squad': (ctx) => `IMPORTANT — Image inputs:
${wcImageInputs(ctx.faceCount)}

Official ${ctx.country} World Cup team squad photograph of ${ctx.subj} lined up on the pitch before kickoff, wearing the ${ctx.country} national kit, in the classic team-photo pose (arms crossed or hands behind the back), the stadium and crowd behind them. Clean professional sports team photograph.

${ctx.customLine}

Style: ultra-realistic, 8K, official team photography, even professional lighting. ${WC_NEGATIVE_PROMPT}`,

            'press-conference': (ctx) => `IMPORTANT — Image inputs:
${wcImageInputs(ctx.faceCount)}

Ultra-realistic photo of ${ctx.subj} at a World Cup press conference, seated behind a desk with microphones, an official tournament sponsor backdrop behind them, wearing ${ctx.country} team apparel, confident composed expression, press-photography lighting.

${ctx.customLine}

Style: ultra-realistic, 8K, press conference photography, clean professional lighting. ${WC_NEGATIVE_PROMPT}`,

            'trading-card': (ctx) => `IMPORTANT — Image inputs:
${wcImageInputs(ctx.faceCount)}

A premium FIFA-style football trading card featuring ${ctx.subj} as ${ctx.country} player(s). Wearing the ${ctx.country} kit in a dynamic hero pose, the card design shows the ${ctx.country} flag, a player name area, a rating number and stat boxes, framed in a holographic gold premium card frame. Collectible trading-card aesthetic.

${ctx.customLine}

Style: ultra-realistic subject, premium graphic card design, holographic gold accents, 8K. ${WC_NEGATIVE_PROMPT}`,

            'custom': (ctx) => `IMPORTANT — Image inputs:
${wcImageInputs(ctx.faceCount)}

Ultra-realistic World Cup themed photo of ${ctx.subj} supporting ${ctx.country}. Setting and atmosphere: ${ctx.customTheme || 'a festive World Cup celebration'}. Wearing ${ctx.country} national team colors where appropriate.

${ctx.customLine}

Style: ultra-realistic, 8K, cinematic lighting, sharp focus. ${WC_NEGATIVE_PROMPT}`,
        };

        function buildWorldCupPrompt(themeId, faceCount, country, customTheme, customText) {
            const builder = WC_THEMES[themeId] || WC_THEMES['stadium-supporter'];
            const subj = faceCount === 1 ? 'the person' : `the ${faceCount} people`;
            return builder({
                faceCount: faceCount,
                country: (country || 'the national team').trim(),
                subj: subj,
                customTheme: (customTheme || '').trim(),
                customLine: customText ? `Additional user direction: ${customText}` : '',
            });
        }

        // ============================================================================
        // Handlers
        // ============================================================================
        const wcGenerateBtn = document.getElementById('wc-generate-btn');
        const wcFacesInput = document.getElementById('wc-faces-upload');
        const wcFacesPreview = document.getElementById('wc-faces-preview');
        const wcFacesCount = document.getElementById('wc-faces-count');
        const wcCountryInput = document.getElementById('wc-country');
        const wcCustomThemeWrap = document.getElementById('wc-custom-theme-wrap');
        const wcCustomThemeInput = document.getElementById('wc-custom-theme');
        const wcCustomPromptInput = document.getElementById('wc-custom-prompt');
        const wcEmptyState = document.getElementById('wc-empty-state');
        const wcLoadingEl = document.getElementById('wc-loading');
        const wcResultsHeader = document.getElementById('wc-results-header');
        const wcResultsCountEl = document.getElementById('wc-results-count');
        const wcResultsGrid = document.getElementById('wc-results-grid');
        const wcDownloadAllBtn = document.getElementById('wc-download-all-btn');
        const wcPreviewModal = document.getElementById('wc-preview-modal');
        const wcPreviewImage = document.getElementById('wc-preview-image');
        const wcPreviewClose = document.getElementById('wc-preview-close');

        function checkWCReadyToGenerate() {
            if (!wcGenerateBtn) return;
            const hasFaces = wcFaceImages.length > 0;
            const hasCountry = (wcCountryInput?.value || '').trim().length > 0;
            wcGenerateBtn.disabled = !(hasFaces && hasCountry);
        }

        function renderWCFacesPreview() {
            if (!wcFacesPreview) return;
            wcFacesPreview.innerHTML = '';
            if (wcFaceImages.length === 0) {
                wcFacesPreview.classList.add('hidden');
                if (wcFacesCount) wcFacesCount.textContent = '0/5 foto';
                return;
            }
            wcFacesPreview.classList.remove('hidden');
            wcFaceImages.forEach((img, idx) => {
                const card = document.createElement('div');
                card.className = 'relative aspect-square rounded-lg overflow-hidden border-2';
                card.style.borderColor = '#eab308';
                card.innerHTML = `
                    <img src="data:${img.mimeType};base64,${img.base64}" class="w-full h-full object-cover" alt="Face ${idx + 1}">
                    <button type="button" data-wc-face-remove="${idx}" class="absolute top-1 right-1 bg-red-500 text-white rounded-full w-6 h-6 flex items-center justify-center text-xs font-bold shadow-md hover:bg-red-600">&times;</button>
                    <div class="absolute bottom-1 left-1 bg-black/60 text-white text-xs px-2 py-0.5 rounded">${idx + 1}</div>
                `;
                wcFacesPreview.appendChild(card);
            });
            if (wcFacesCount) wcFacesCount.textContent = `${wcFaceImages.length}/5 foto`;
        }

        if (wcFacesInput) {
            wcFacesInput.addEventListener('change', async (e) => {
                const files = Array.from(e.target.files || []);
                for (const file of files) {
                    if (wcFaceImages.length >= 5) break;
                    await new Promise((resolve) => {
                        const reader = new FileReader();
                        reader.onload = (evt) => {
                            const dataUrl = evt.target.result;
                            const base64 = dataUrl.split(',')[1];
                            const mimeType = (dataUrl.split(';')[0] || '').split(':')[1];
                            wcFaceImages.push({ mimeType, base64 });
                            resolve();
                        };
                        reader.readAsDataURL(file);
                    });
                }
                renderWCFacesPreview();
                checkWCReadyToGenerate();
                wcFacesInput.value = '';
            });
        }

        if (wcFacesPreview) {
            wcFacesPreview.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-wc-face-remove]');
                if (!btn) return;
                const idx = parseInt(btn.dataset.wcFaceRemove, 10);
                if (!isNaN(idx)) {
                    wcFaceImages.splice(idx, 1);
                    renderWCFacesPreview();
                    checkWCReadyToGenerate();
                }
            });
        }

        // Theme selector (Custom reveals free-text setting input)
        document.querySelectorAll('#wc-themes-grid button[data-wc-theme]').forEach(btn => {
            btn.addEventListener('click', () => {
                wcSelectedTheme = btn.dataset.wcTheme;
                document.querySelectorAll('#wc-themes-grid button[data-wc-theme]').forEach(b => {
                    b.classList.toggle('selected', b.dataset.wcTheme === wcSelectedTheme);
                });
                if (wcCustomThemeWrap) {
                    wcCustomThemeWrap.classList.toggle('hidden', wcSelectedTheme !== 'custom');
                }
            });
        });

        // Ratio selector
        document.querySelectorAll('#wc-ratio-options button[data-wc-ratio]').forEach(btn => {
            btn.addEventListener('click', () => {
                wcSelectedRatio = btn.dataset.wcRatio;
                document.querySelectorAll('#wc-ratio-options button[data-wc-ratio]').forEach(b => {
                    b.classList.toggle('selected', b.dataset.wcRatio === wcSelectedRatio);
                });
            });
        });

        // Count selector
        document.querySelectorAll('#wc-count-options button[data-wc-count]').forEach(btn => {
            btn.addEventListener('click', () => {
                wcSelectedCount = parseInt(btn.dataset.wcCount, 10);
                document.querySelectorAll('#wc-count-options button[data-wc-count]').forEach(b => {
                    b.classList.toggle('selected', parseInt(b.dataset.wcCount, 10) === wcSelectedCount);
                });
            });
        });

        if (wcCountryInput) wcCountryInput.addEventListener('input', checkWCReadyToGenerate);

        // Preview modal handlers
        function openWCPreview(imageUrl) {
            if (!wcPreviewModal || !wcPreviewImage) return;
            wcPreviewImage.src = imageUrl;
            wcPreviewModal.classList.remove('hidden');
            document.body.style.overflow = 'hidden';
        }
        function closeWCPreview() {
            if (!wcPreviewModal) return;
            wcPreviewModal.classList.add('hidden');
            document.body.style.overflow = '';
        }
        if (wcPreviewClose) wcPreviewClose.addEventListener('click', closeWCPreview);
        if (wcPreviewModal) {
            wcPreviewModal.addEventListener('click', (e) => {
                if (e.target === wcPreviewModal) closeWCPreview();
            });
        }
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && wcPreviewModal && !wcPreviewModal.classList.contains('hidden')) {
                closeWCPreview();
            }
        });

        // Results grid click delegation — preview button
        if (wcResultsGrid) {
            wcResultsGrid.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-wc-action="preview"]');
                if (!btn) return;
                const idx = parseInt(btn.dataset.wcIndex, 10);
                if (!isNaN(idx) && wcGeneratedImages[idx]) {
                    openWCPreview(wcGeneratedImages[idx].url);
                }
            });
        }

        async function generateSingleWCPhoto(index, basePrompt) {
            const card = document.getElementById(`wc-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const angleLine = WC_ANGLE_POOL[(index - 1) % WC_ANGLE_POOL.length];
                const finalPrompt = `${basePrompt}\n\n${angleLine}\n\nVariation ${index}: slightly vary the pose, gesture and composition while preserving each person's exact identity and the national team colors.`;

                const parts = [{ text: finalPrompt }];
                wcFaceImages.forEach((img) => {
                    parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
                });

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: wcSelectedRatio }
                    },
                    safetySettings: [
                        { category: 'HARM_CATEGORY_HARASSMENT',        threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_HATE_SPEECH',        threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_SEXUALLY_EXPLICIT',  threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_DANGEROUS_CONTENT',  threshold: 'BLOCK_NONE' }
                    ]
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data in response');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `worldcup-${wcSelectedTheme}-${index}-${Date.now()}.jpg`;

                wcGeneratedImages[index - 1] = { url: imageUrl, filename };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = `<img src="${imageUrl}" class="w-full object-contain cursor-pointer" alt="World Cup ${index}" data-wc-action="preview" data-wc-index="${index - 1}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition flex items-end justify-center pb-3 gap-2">
                            <button type="button" data-wc-action="preview" data-wc-index="${index - 1}" class="bg-white/95 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition flex items-center gap-2">
                                <i class="fas fa-eye"></i><span class="hidden sm:inline" data-i18n="wc.preview">Preview</span>
                            </button>
                            <a href="${imageUrl}" download="${filename}" data-action="download" class="bg-gradient-to-r from-green-700 to-yellow-500 hover:from-green-800 hover:to-yellow-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition flex items-center gap-2">
                                <i class="fas fa-download"></i><span class="hidden sm:inline" data-i18n="wc.download">Download</span>
                            </a>
                        </div>`;
                }
            } catch (err) {
                console.error(`World Cup generate error #${index}:`, err);
                if (card) card.innerHTML = '';
            }
        }

        async function generateWCPhotos() {
            const lang = (typeof localStorage !== 'undefined' && localStorage.getItem('app_language')) || 'id';

            if (wcFaceImages.length === 0) {
                const msg = (lang === 'en')
                    ? 'Upload at least 1 face photo first to generate.'
                    : 'Upload minimal 1 foto wajah dulu untuk generate.';
                if (typeof showError === 'function') { showError(msg); } else { alert(msg); }
                return;
            }

            const country = (wcCountryInput?.value || '').trim();
            if (!country) {
                const msg = (lang === 'en')
                    ? 'Enter the country/team name first.'
                    : 'Ketik nama negara/tim dulu.';
                if (typeof showError === 'function') { showError(msg); } else { alert(msg); }
                return;
            }

            if (wcEmptyState) wcEmptyState.classList.add('hidden');
            if (wcLoadingEl) wcLoadingEl.classList.remove('hidden');
            if (wcResultsHeader) wcResultsHeader.classList.add('hidden');

            const customTheme = (wcCustomThemeInput?.value || '').trim();
            const customText = (wcCustomPromptInput?.value || '').trim();
            const basePrompt = buildWorldCupPrompt(wcSelectedTheme, wcFaceImages.length, country, customTheme, customText);

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                wcGeneratedImages = [];
                if (wcResultsGrid) {
                    wcResultsGrid.innerHTML = '';
                    wcResultsGrid.classList.remove('hidden');
                }

                for (let i = 1; i <= wcSelectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `wc-card-${i}`;
                    card.className = 'relative rounded-xl overflow-hidden bg-gradient-to-br from-green-50 to-yellow-50 flex items-center justify-center aspect-video border-2';
                    card.style.borderColor = '#eab308';
                    card.innerHTML = `<div class="animate-spin rounded-full h-10 w-10 border-b-3 border-t-3" style="border-color: #eab308;"></div>`;
                    if (wcResultsGrid) wcResultsGrid.appendChild(card);
                }

                const promises = Array.from({ length: wcSelectedCount }, (_, i) =>
                    generateSingleWCPhoto(i + 1, basePrompt)
                );
                await Promise.allSettled(promises);

                if (wcResultsGrid) {
                    wcResultsGrid.querySelectorAll('[id^="wc-card-"]').forEach(card => {
                        if (!card.querySelector('img')) card.remove();
                    });
                }
                successCount = wcGeneratedImages.filter(Boolean).length;
            }

            if (wcLoadingEl) wcLoadingEl.classList.add('hidden');
            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                if (wcResultsHeader) wcResultsHeader.classList.remove('hidden');
                if (wcResultsCountEl) wcResultsCountEl.textContent = wcGeneratedImages.filter(Boolean).length;
                if (wcDownloadAllBtn) wcDownloadAllBtn.classList.remove('hidden');
                if (wcResultsGrid) wcResultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' });
            }
        }

        if (wcGenerateBtn) wcGenerateBtn.addEventListener('click', async () => {
            if (wcGenerateBtn.disabled) return;
            wcGenerateBtn.disabled = true;
            try { await generateWCPhotos(); }
            finally { checkWCReadyToGenerate(); }
        });

        if (wcDownloadAllBtn) {
            wcDownloadAllBtn.addEventListener('click', () => {
                wcGeneratedImages.filter(Boolean).forEach(({ url, filename }, i) => {
                    setTimeout(() => {
                        const a = document.createElement('a');
                        a.href = url;
                        a.download = filename;
                        a.click();
                    }, i * 200);
                });
            });
        }

        checkWCReadyToGenerate();
    })();

    // ==================== PRODUCT PREMIUM (Foto Produk Premium) ====================
    (function productPremiumTab() {
        const apiKey = "";

        // State
        let ppSelectedMode = 'artistic';
        let ppSelectedTheme = 'surreal-nature';
        let ppSelectedMood = 'warm';
        let ppSelectedRatio = '1:1';
        let ppSelectedCount = 4;
        let ppProductImage = null;
        let ppModelImage = null;
        let ppGeneratedImages = [];

        // Constants
        const PP_NEGATIVE_PROMPT = `Negative: no text, no watermark, no distortion, no blur on product, preserve exact product shape, branding, material, and details from the reference image.`;

        const PP_MOOD_LINES = {
            'warm':     'Warm golden tones, sunset/golden hour lighting, cozy and inviting atmosphere.',
            'cool':     'Cool blue/teal tones, modern crisp lighting, sleek contemporary feel.',
            'dramatic': 'High contrast, theatrical key light, deep shadows, cinematic poster mood.',
            'soft':     'Soft diffused lighting, pastel palette, gentle airy aesthetic.',
        };

        // Artistic mode templates (1 image: product)
        const artisticTemplates = {
            'surreal-nature': (ctx) => `IMPORTANT — One reference image: the PRODUCT. Preserve exact shape, branding, material, and details from the reference.

Surreal high-end advertisement of the product. The product is the central focus in a dreamlike composition. Lush natural elements (greenery, plants, environmental textures) organically emerge from and interact with the product, blending seamlessly with its form. Atmospheric haze, falling leaves, light particles transition into symbolic elements (clouds, birds, mists), creating a sense of life and motion. Premium eco-luxury aesthetic.

${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic textures, high detail, cinematic lighting, soft depth of field, premium advertisement style, 8K resolution. ${PP_NEGATIVE_PROMPT}`,

            'surreal-cosmic': (ctx) => `IMPORTANT — One reference image: the PRODUCT. Preserve exact shape, branding, material, and details.

Surreal cosmic advertisement with the product floating in a galaxy backdrop. Swirling nebulas, glowing stardust particles, ethereal cosmic energy radiating from the product. Deep space color palette (purple, blue, magenta) with bright accent highlights. The product hovers as if defying gravity, surrounded by celestial elements.

${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, cinematic lighting, magical-realism, premium advertisement. ${PP_NEGATIVE_PROMPT}`,

            'dark-luxury': (ctx) => `IMPORTANT — One reference image: the PRODUCT. Preserve exactly.

High-end luxury advertisement of the product on a black marble surface with gold light streaks accenting the composition. Dramatic chiaroscuro lighting with deep shadows and gold/copper highlights. Ultra-premium dark aesthetic — the kind of imagery used for luxury fashion or jewelry campaigns.

${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, dramatic cinematic lighting, premium luxury advertisement. ${PP_NEGATIVE_PROMPT}`,

            'hyper-minimal': (ctx) => `IMPORTANT — One reference image: the PRODUCT. Preserve exactly.

Hyper-minimalist product advertisement on a soft pastel background (off-white, beige, or light pink). Single soft directional light creating a clean delicate shadow. Gallery-museum aesthetic with ample negative space. The product is the sole hero, presented with quiet sophistication.

${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, museum-quality minimalist photography, soft natural light. ${PP_NEGATIVE_PROMPT}`,

            'liquid-splash': (ctx) => `IMPORTANT — One reference image: the PRODUCT. Preserve exactly.

Dynamic motion advertisement with water/liquid splashing around the product, frozen mid-action. Droplets suspended in air, ripples cascading, refreshing energetic feel. High-speed photography aesthetic — the kind used for beverage or beauty product campaigns.

${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, frozen motion, crystalline water detail, premium product photography. ${PP_NEGATIVE_PROMPT}`,

            'floating-lifestyle': (ctx) => `IMPORTANT — One reference image: the PRODUCT. Preserve exactly.

Floating product composition with related ingredients/accessories suspended in air around it (e.g., for skincare: fresh leaves, water droplets, flower petals; for food: fresh ingredients; for tech: subtle abstract shapes). Gravity-defying levitation effect. Soft studio light from above creating gentle shadows. Hero composition with the product centered.

${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, levitation photography, premium product hero shot. ${PP_NEGATIVE_PROMPT}`,
        };

        // Lifestyle templates (1-2 images: product + optional model)
        const lifestyleTemplates = {
            'hand-holding': (ctx) => {
                const imgAnchor = ctx.hasModel
                    ? `IMPORTANT — Two reference images: FIRST = product (preserve exactly), SECOND = model (use for the hand/arm and skin tone, preserve identity if face appears).`
                    : `IMPORTANT — One reference image: the PRODUCT. Preserve exactly. Generate a generic hand holding it.`;
                return `${imgAnchor}

Close-up shot of a hand holding the product against a clean blurred background. Natural skin lighting, soft bokeh, lifestyle photography aesthetic. The product is the hero — clearly readable.

${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, lifestyle photography, soft natural light. ${PP_NEGATIVE_PROMPT}`;
            },

            'on-desk-cafe': (ctx) => {
                const imgAnchor = ctx.hasModel
                    ? `IMPORTANT — Two reference images: FIRST = product, SECOND = model (subtly visible in the scene, e.g., hand reaching, partial profile).`
                    : `IMPORTANT — One reference image: the PRODUCT. Preserve exactly.`;
                return `${imgAnchor}

The product placed on a wooden cafe desk surrounded by a coffee cup, open notebook, plants, soft morning light through a window. Warm cozy lifestyle vibe. Shallow depth of field with the product in sharp focus.

${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, lifestyle product photography, natural window light. ${PP_NEGATIVE_PROMPT}`;
            },

            'outdoor-adventure': (ctx) => {
                const imgAnchor = ctx.hasModel
                    ? `IMPORTANT — Two reference images: FIRST = product, SECOND = model (in the outdoor scene, holding or near the product).`
                    : `IMPORTANT — One reference image: the PRODUCT. Preserve exactly.`;
                return `${imgAnchor}

The product placed in an outdoor adventure setting (mountain backdrop, forest trail, beach, etc.). Golden hour lighting, dynamic composition, lifestyle photography style. The product is positioned as a hero element in the natural environment.

${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, outdoor lifestyle photography, golden hour cinematic. ${PP_NEGATIVE_PROMPT}`;
            },

            'kitchen-counter': (ctx) => {
                const imgAnchor = ctx.hasModel
                    ? `IMPORTANT — Two reference images: FIRST = product, SECOND = model (cooking or interacting with the product).`
                    : `IMPORTANT — One reference image: the PRODUCT. Preserve exactly.`;
                return `${imgAnchor}

The product placed on a marble kitchen counter with cooking ingredients/utensils nearby (fresh herbs, wooden cutting board, ceramic bowls). Natural window light, food photography aesthetic, warm domestic feel.

${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, food/lifestyle photography, natural window light. ${PP_NEGATIVE_PROMPT}`;
            },

            'vanity-bathroom': (ctx) => {
                const imgAnchor = ctx.hasModel
                    ? `IMPORTANT — Two reference images: FIRST = product, SECOND = model (in spa/skincare context, partial visibility).`
                    : `IMPORTANT — One reference image: the PRODUCT. Preserve exactly.`;
                return `${imgAnchor}

The product arranged on a clean white bathroom vanity with soft towels, eucalyptus branches, marble surface. Spa-like serene atmosphere. Soft diffused lighting from above.

${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, beauty/skincare lifestyle photography, soft spa lighting. ${PP_NEGATIVE_PROMPT}`;
            },

            'gym-fitness': (ctx) => {
                const imgAnchor = ctx.hasModel
                    ? `IMPORTANT — Two reference images: FIRST = product, SECOND = model (athletic, in workout context).`
                    : `IMPORTANT — One reference image: the PRODUCT. Preserve exactly.`;
                return `${imgAnchor}

The product in a modern gym/fitness setting with weights, water bottle, athletic accessories nearby. Dramatic gym lighting, energetic motivational vibe. The product is positioned as the hero element.

${ctx.moodLine}
${ctx.customLine}

Style: ultra-realistic, 8K, fitness lifestyle photography, dramatic gym lighting. ${PP_NEGATIVE_PROMPT}`;
            },
        };

        function buildProductPremiumPrompt(mode, themeId, moodId, hasModel, customText) {
            const registry = mode === 'lifestyle' ? lifestyleTemplates : artisticTemplates;
            const fallbackId = Object.keys(registry)[0];
            const builder = registry[themeId] || registry[fallbackId];
            return builder({
                moodLine: PP_MOOD_LINES[moodId] || PP_MOOD_LINES['warm'],
                customLine: customText ? `Additional user direction: ${customText}` : '',
                hasModel: !!hasModel,
            });
        }

        // ============================================================================
        // Handlers
        // ============================================================================
        const generateBtn = document.getElementById('pp-generate-btn');
        const productInput = document.getElementById('pp-product-upload');
        const modelInput = document.getElementById('pp-model-upload');
        const productPreview = document.getElementById('pp-product-preview');
        const modelPreview = document.getElementById('pp-model-preview');
        const customPromptInput = document.getElementById('pp-custom-prompt');
        const emptyState = document.getElementById('pp-empty-state');
        const loadingEl = document.getElementById('pp-loading');
        const resultsHeader = document.getElementById('pp-results-header');
        const resultsCount = document.getElementById('pp-results-count');
        const resultsGrid = document.getElementById('pp-results-grid');
        const downloadAllBtn = document.getElementById('pp-download-all-btn');

        function checkPPReadyToGenerate() {
            if (!generateBtn) return;
            generateBtn.disabled = !ppProductImage;
        }

        // Mode toggle
        const modeButtons = document.querySelectorAll('#pp-mode-options button[data-pp-mode]');
        modeButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                const newMode = btn.dataset.ppMode;
                if (newMode === ppSelectedMode) return;
                ppSelectedMode = newMode;
                modeButtons.forEach(b => b.classList.toggle('selected', b.dataset.ppMode === newMode));

                if (newMode === 'lifestyle') {
                    document.body.setAttribute('data-pp-mode', 'lifestyle');
                } else {
                    document.body.removeAttribute('data-pp-mode');
                }

                // Reset theme to first of new mode
                const newRegistry = newMode === 'lifestyle' ? lifestyleTemplates : artisticTemplates;
                ppSelectedTheme = Object.keys(newRegistry)[0];
                document.querySelectorAll('button[data-pp-theme]').forEach(b => {
                    b.classList.toggle('selected', b.dataset.ppTheme === ppSelectedTheme);
                });

                checkPPReadyToGenerate();
            });
        });

        // Theme selector
        document.querySelectorAll('button[data-pp-theme]').forEach(btn => {
            btn.addEventListener('click', () => {
                ppSelectedTheme = btn.dataset.ppTheme;
                const grid = btn.closest('[id^="pp-themes-"]');
                if (grid) {
                    grid.querySelectorAll('button[data-pp-theme]').forEach(b => {
                        b.classList.toggle('selected', b.dataset.ppTheme === ppSelectedTheme);
                    });
                }
            });
        });

        // Mood selector
        document.querySelectorAll('#pp-mood-options button[data-pp-mood]').forEach(btn => {
            btn.addEventListener('click', () => {
                ppSelectedMood = btn.dataset.ppMood;
                document.querySelectorAll('#pp-mood-options button[data-pp-mood]').forEach(b => {
                    b.classList.toggle('selected', b.dataset.ppMood === ppSelectedMood);
                });
            });
        });

        // Ratio selector
        document.querySelectorAll('#pp-ratio-options button[data-pp-ratio]').forEach(btn => {
            btn.addEventListener('click', () => {
                ppSelectedRatio = btn.dataset.ppRatio;
                document.querySelectorAll('#pp-ratio-options button[data-pp-ratio]').forEach(b => {
                    b.classList.toggle('selected', b.dataset.ppRatio === ppSelectedRatio);
                });
            });
        });

        // Count selector
        document.querySelectorAll('#pp-count-options button[data-pp-count]').forEach(btn => {
            btn.addEventListener('click', () => {
                ppSelectedCount = parseInt(btn.dataset.ppCount, 10);
                document.querySelectorAll('#pp-count-options button[data-pp-count]').forEach(b => {
                    b.classList.toggle('selected', parseInt(b.dataset.ppCount, 10) === ppSelectedCount);
                });
            });
        });

        // Upload handlers
        function handlePPUpload(input, previewWrap, slot) {
            if (!input) return;
            input.addEventListener('change', (e) => {
                const file = e.target.files[0];
                if (!file) return;
                const reader = new FileReader();
                reader.onload = (evt) => {
                    const dataUrl = evt.target.result;
                    const base64 = dataUrl.split(',')[1];
                    const mimeType = (dataUrl.split(';')[0] || '').split(':')[1];

                    if (slot === 'product') {
                        ppProductImage = { mimeType, base64 };
                    } else {
                        ppModelImage = { mimeType, base64 };
                    }

                    if (previewWrap) {
                        const img = previewWrap.querySelector('img');
                        if (img) img.src = dataUrl;
                        previewWrap.classList.remove('hidden');
                    }
                    checkPPReadyToGenerate();
                };
                reader.readAsDataURL(file);
            });
        }
        handlePPUpload(productInput, productPreview, 'product');
        handlePPUpload(modelInput, modelPreview, 'model');

        async function generateSinglePPPhoto(index, basePrompt) {
            const card = document.getElementById(`pp-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const finalPrompt = `${basePrompt}\n\nVariation ${index}: render with slight variation in composition or lighting angle while preserving the product exactly.`;

                // PP DISPATCH: FIRST=product (always), SECOND=model (only if lifestyle + uploaded)
                // Order is load-bearing — prompt anchors FIRST/SECOND.
                const parts = [{ text: finalPrompt }];
                parts.push({ inlineData: { mimeType: ppProductImage.mimeType, data: ppProductImage.base64 } });
                if (ppSelectedMode === 'lifestyle' && ppModelImage) {
                    parts.push({ inlineData: { mimeType: ppModelImage.mimeType, data: ppModelImage.base64 } });
                }

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: ppSelectedRatio }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data in response');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `product-premium-${ppSelectedMode}-${ppSelectedTheme}-${index}-${Date.now()}.jpg`;

                ppGeneratedImages[index - 1] = { url: imageUrl, filename };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = `<img src="${imageUrl}" class="w-full object-contain" alt="Product Premium ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent opacity-0 group-hover:opacity-100 transition flex items-end justify-center pb-3">
                            <a href="${imageUrl}" download="${filename}" class="bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg hover:scale-105 transition flex items-center gap-2">
                                <i class="fas fa-download"></i><span>Download</span>
                            </a>
                        </div>`;
                }
            } catch (err) {
                console.error(`PP generate error #${index}:`, err);
                if (card) card.innerHTML = '';
            }
        }

        async function generatePPPhotos() {
            if (!ppProductImage) {
                const lang = (typeof localStorage !== 'undefined' && localStorage.getItem('app_language')) || 'id';
                // pp.error-need-product
                const msg = (lang === 'en')
                    ? 'Upload a product photo first to generate.'
                    : 'Upload foto produk dulu untuk generate.';
                if (typeof showError === 'function') {
                    showError(msg);
                } else {
                    alert(msg);
                }
                return;
            }

            if (emptyState) emptyState.classList.add('hidden');
            if (loadingEl) loadingEl.classList.remove('hidden');
            if (resultsHeader) resultsHeader.classList.add('hidden');

            const customText = (customPromptInput?.value || '').trim();
            const basePrompt = buildProductPremiumPrompt(
                ppSelectedMode, ppSelectedTheme, ppSelectedMood,
                !!ppModelImage, customText
            );

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                ppGeneratedImages = [];
                if (resultsGrid) {
                    resultsGrid.innerHTML = '';
                    resultsGrid.classList.remove('hidden');
                }

                for (let i = 1; i <= ppSelectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `pp-card-${i}`;
                    card.className = 'relative rounded-xl overflow-hidden bg-gradient-to-br from-pink-50 to-purple-50 flex items-center justify-center aspect-square border-2';
                    card.style.borderColor = '#a855f7';
                    card.innerHTML = `<div class="animate-spin rounded-full h-10 w-10 border-b-3 border-t-3" style="border-color: #a855f7;"></div>`;
                    if (resultsGrid) resultsGrid.appendChild(card);
                }

                const promises = Array.from({ length: ppSelectedCount }, (_, i) =>
                    generateSinglePPPhoto(i + 1, basePrompt)
                );
                await Promise.allSettled(promises);

                if (resultsGrid) {
                    resultsGrid.querySelectorAll('[id^="pp-card-"]').forEach(card => {
                        if (!card.querySelector('img')) card.remove();
                    });
                }
                successCount = ppGeneratedImages.filter(Boolean).length;
            }

            if (loadingEl) loadingEl.classList.add('hidden');
            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                if (resultsHeader) resultsHeader.classList.remove('hidden');
                if (resultsCount) resultsCount.textContent = ppGeneratedImages.length;
                if (downloadAllBtn) downloadAllBtn.classList.remove('hidden');
            }
        }

        generateBtn.addEventListener('click', generatePPPhotos);

        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', () => {
                ppGeneratedImages.filter(Boolean).forEach(({ url, filename }, i) => {
                    setTimeout(() => {
                        const a = document.createElement('a');
                        a.href = url;
                        a.download = filename;
                        a.click();
                    }, i * 200);
                });
            });
        }

        checkPPReadyToGenerate();
    })();

    // ==================== KARTU UCAPAN LEBARAN ====================
    (function() {
        const apiKey = "";

        const uploadBox = document.getElementById('lebaran-upload-box');
        const imageInput = document.getElementById('lebaran-photo-upload');
        const previewContainer = document.getElementById('lebaran-photo-preview-container');
        const photoCountText = document.getElementById('lebaran-photo-count');
        const removePhotoBtn = document.getElementById('lebaran-remove-photo');
        const themeOptions = document.getElementById('lebaran-theme-options');
        const greetingOptions = document.getElementById('lebaran-greeting-options');
        const customGreetingInput = document.getElementById('lebaran-custom-greeting');
        const familyNameInput = document.getElementById('lebaran-family-name');
        const ratioOptions = document.getElementById('lebaran-ratio-options');
        const generateBtn = document.getElementById('lebaran-generate-btn');
        const resultsHeader = document.getElementById('lebaran-results-header');
        const resultsCount = document.getElementById('lebaran-results-count');
        const resultsGrid = document.getElementById('lebaran-results-grid');
        const emptyState = document.getElementById('lebaran-empty-state');
        const loading = document.getElementById('lebaran-loading');
        const loadingText = document.getElementById('lebaran-loading-text');
        const downloadAllBtn = document.getElementById('lebaran-download-all-btn');
        const progressBar = document.getElementById('lebaran-progress-bar');
        const progressText = document.getElementById('lebaran-progress-text');

        let uploadedImages = [];
        let selectedTheme = 'ornamen-klasik';
        let selectedGreeting = 'minal-aidin';
        let selectedRatio = '1:1';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedImages = [];

        // Count selection (1-10), default 4
        const lebaranCountSel = document.getElementById('lebaran-count-selection-grid');
        if (lebaranCountSel) {
            lebaranCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                lebaranCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        const themeDescriptions = {
            'ornamen-klasik': 'traditional Islamic Eid greeting card with intricate golden arabesques, deep emerald green background, ketupat diamond patterns, crescent moon and star motifs, elegant golden border with geometric Islamic patterns, luxurious classic Indonesian Lebaran aesthetic',
            'modern-minimalis': 'modern minimalist Eid greeting card with clean white background, subtle gold foil line patterns, simple elegant Islamic geometric motifs, premium typography space, sophisticated contemporary Lebaran design',
            'ketupat-lentera': 'festive Eid greeting card featuring ketupat rice cake decorations, traditional hanging lanterns in warm amber glow, woven palm leaf patterns, warm golden-orange color palette, joyful Idul Fitri celebration atmosphere',
            'masjid-nabawi': 'beautiful Eid greeting card with Masjid Nabawi Madinah backdrop silhouette, iconic green dome and tall minarets at golden hour, soft bokeh lights, night sky with stars and crescent moon, spiritual sacred atmosphere, deep green and gold color scheme',
            'masjid-istiqlal': 'elegant Eid greeting card featuring Masjid Istiqlal Jakarta Indonesia silhouette, Indonesian national mosque backdrop with architectural beauty, dawn sky with soft golden light, patriotic Islamic heritage theme',
            'bunga-tropis': 'vibrant Eid greeting card with tropical Indonesian flowers - hibiscus kembang sepatu, plumeria frangipani, jasmine melati garlands, lush green leaves, fresh bright floral arrangement, tropical paradise Lebaran theme with warm natural colors',
            'batik-nusantara': 'sophisticated Eid greeting card with intricate batik nusantara patterns - parang, kawung, truntum motifs, rich earthy Indonesian colors, traditional Javanese textile art background, cultural heritage Lebaran design with brown and cream tones',
            'watercolor-floral': 'soft romantic Eid greeting card with delicate watercolor floral paintings, pastel pink and mint green tones, hand-painted flowers and leaves, gentle watercolor wash background, dreamy artistic Lebaran aesthetic',
            'gold-luxury': 'opulent luxury Eid greeting card with rich gold foil geometric patterns on deep midnight navy background, premium metallic accents, diamond-like ornaments, high-end glamorous Lebaran design, VIP celebration aesthetic',
            'bintang-bulan': 'enchanting Eid greeting card with starry night sky full of twinkling stars, large golden crescent moon over mosque silhouette, deep blue-purple night sky gradient, magical and mystical Ramadan night atmosphere',
            'kampung-halaman': 'heartwarming mudik homecoming Eid greeting card with traditional Indonesian village scene, rice field terraces, bamboo house rumah panggung, mountain backdrop, warm golden sunset, nostalgic kampung halaman atmosphere',
            'alam-hijau': 'fresh Eid greeting card with lush Indonesian rice paddy fields sawah, tropical coconut palm trees, natural green landscape, morning mist, clear blue sky, refreshing nature-themed Lebaran celebration',
            'langit-senja': 'breathtaking Eid greeting card with dramatic golden sunset sky, silhouette of mosque minarets against vibrant orange and pink sunset clouds, warm glowing atmosphere, beautiful Ramadan golden hour',
            'warisan-jawa': 'cultural Javanese heritage Eid greeting card with traditional Javanese ornaments, wayang shadow puppet motifs, royal Kraton palace architectural elements, earthy gold and deep red Javanese color palette',
            'melayu-tradisional': 'traditional Malay Eid greeting card with beautiful songket fabric woven patterns in gold and crimson, traditional Malay architecture, lush tropical Malay motifs, rich royal Melayu heritage aesthetic',
            'nuansa-biru-tosca': 'elegant teal blue and silver Eid greeting card with turquoise color scheme, intricate geometric Islamic patterns in blue and teal tones, sophisticated cool-toned Lebaran design, serene and refreshing atmosphere',
            'nuansa-ungu': 'royal purple Eid greeting card with deep violet and lavender color palette, mystical purple Islamic ornaments and geometric patterns, elegant purple floral accents, sophisticated and regal Lebaran aesthetic',
            'rose-gold': 'romantic rose gold Eid greeting card with blush pink and champagne gold colors, floral rose motifs, elegant feminine design, soft metallic shimmering accents, modern chic Lebaran aesthetic',
            'kaligrafi-arab': 'artistic Eid greeting card featuring beautiful Arabic calligraphy art of Eid ul Fitr greetings, intricate calligraphic lettering, artistic black and gold ink design on textured parchment background, traditional Islamic art aesthetics',
            'fanoos-lentera': 'magical Eid greeting card with hanging colorful fanoos lanterns and traditional Indonesian lanterns, warm amber and golden glowing bokeh lights, deep warm dark background, festive and celebratory Lebaran night atmosphere'
        };

        const greetingMap = {
            'minal-aidin': 'Minal Aidin Wal Faizin, Mohon Maaf Lahir dan Batin',
            'taqabbalallahu': "Taqabbalallahu Minna Wa Minkum, Wa Ja'alana Allahu Minal 'Aidin Wal Faizin",
            'selamat-idul-fitri': 'Selamat Hari Raya Idul Fitri 1447 H / 2026 M',
            'eid-mubarak': 'Eid Mubarak 1447 H - May Allah Accept Our Good Deeds'
        };

        // Tab init
        const tabDesktop = document.querySelector('.main-tab-btn[data-tab="kartu-lebaran"]');
        const tabMobile = document.querySelector('.mobile-tab-btn[data-tab="kartu-lebaran"]');
        if (tabDesktop) tabDesktop.addEventListener('click', () => {});
        if (tabMobile) tabMobile.addEventListener('click', () => {});

        // Upload box click
        if (uploadBox) uploadBox.addEventListener('click', () => imageInput && imageInput.click());

        if (imageInput) {
            imageInput.addEventListener('change', async (e) => {
                const files = Array.from(e.target.files);
                if (!files.length) return;
                uploadedImages = [];
                if (previewContainer) { previewContainer.innerHTML = ''; previewContainer.classList.remove('hidden'); }
                for (const file of files.slice(0, 5)) {
                    let processedFile = file;
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        try {
                            const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                        } catch (err) { console.error('HEIC conversion error:', err); }
                    }
                    const reader = new FileReader();
                    reader.onload = (ev) => {
                        const base64 = ev.target.result.split(',')[1];
                        uploadedImages.push({ base64, mimeType: processedFile.type || 'image/jpeg' });
                        if (previewContainer) {
                            const img = document.createElement('img');
                            img.src = ev.target.result;
                            img.className = 'w-full h-20 object-cover rounded-lg';
                            previewContainer.appendChild(img);
                        }
                        if (photoCountText) photoCountText.textContent = `${uploadedImages.length}/5 foto`;
                        if (removePhotoBtn) removePhotoBtn.classList.remove('hidden');
                    };
                    reader.readAsDataURL(processedFile);
                }
            });
        }

        if (removePhotoBtn) {
            removePhotoBtn.addEventListener('click', () => {
                uploadedImages = [];
                if (previewContainer) { previewContainer.innerHTML = ''; previewContainer.classList.add('hidden'); }
                if (imageInput) imageInput.value = '';
                if (photoCountText) photoCountText.textContent = '0/5 foto';
                removePhotoBtn.classList.add('hidden');
            });
        }

        function setupOptionBtns(container, callback) {
            if (!container) return;
            container.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;
                container.querySelectorAll('[data-value]').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                callback(btn.dataset.value);
            });
        }

        setupOptionBtns(themeOptions, (v) => { selectedTheme = v; });
        setupOptionBtns(greetingOptions, (v) => {
            selectedGreeting = v;
            if (customGreetingInput) customGreetingInput.value = '';
        });
        setupOptionBtns(ratioOptions, (v) => { selectedRatio = v; });

        if (customGreetingInput) {
            customGreetingInput.addEventListener('input', () => {
                if (customGreetingInput.value.trim()) {
                    if (greetingOptions) greetingOptions.querySelectorAll('[data-value]').forEach(b => b.classList.remove('selected'));
                    selectedGreeting = 'custom';
                }
            });
        }

        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                const familyName = familyNameInput ? familyNameInput.value.trim() : '';
                const greeting = (selectedGreeting === 'custom' && customGreetingInput && customGreetingInput.value.trim())
                    ? customGreetingInput.value.trim()
                    : (greetingMap[selectedGreeting] || greetingMap['minal-aidin']);

                if (emptyState) emptyState.classList.add('hidden');
                if (resultsHeader) resultsHeader.classList.add('hidden');
                if (loading) loading.classList.remove('hidden');
                if (downloadAllBtn) downloadAllBtn.classList.add('hidden');
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i><span>Generating...</span>';

                const aspectClass = selectedRatio === '3:4' ? 'aspect-[3/4]' : (selectedRatio === '9:16' ? 'aspect-[9/16]' : 'aspect-square');

                let attempts = 0;
                const MAX_ATTEMPTS = 3;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedImages = new Array(selectedCount);
                    if (resultsGrid) resultsGrid.innerHTML = '';

                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `lebaran-card-${i}`;
                        card.className = `relative rounded-xl overflow-hidden bg-gradient-to-br from-green-50 to-amber-50 border-2 border-green-200 flex items-center justify-center ${aspectClass}`;
                        card.innerHTML = `<div class="text-center p-4"><div class="animate-spin rounded-full h-10 w-10 border-b-4 border-t-4 mb-3 mx-auto" style="border-color: #16a34a;"></div><p class="text-xs text-gray-500">Membuat kartu ${i}...</p></div>`;
                        if (resultsGrid) resultsGrid.appendChild(card);
                    }

                    const promises = Array.from({ length: selectedCount }, (_, i) => generateSingle(i + 1, greeting, familyName));
                    await Promise.allSettled(promises);

                    if (resultsGrid) {
                        resultsGrid.querySelectorAll('[id^="lebaran-card-"]').forEach(card => {
                            if (!card.querySelector('img')) card.remove();
                        });
                    }

                    successCount = generatedImages.filter(Boolean).length;
                }

                if (loading) loading.classList.add('hidden');
                generateBtn.disabled = false;
                generateBtn.innerHTML = '<i class="fas fa-envelope-open-text mr-3 text-xl relative z-10"></i><span class="relative z-10">Generate Kartu Lebaran</span>';

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                } else {
                    if (resultsHeader) resultsHeader.classList.remove('hidden');
                    if (resultsCount) resultsCount.textContent = successCount;
                    if (downloadAllBtn) downloadAllBtn.classList.remove('hidden');
                }
            });
        }

        async function generateSingle(index, greeting, familyName) {
            const card = document.getElementById(`lebaran-card-${index}`);
            try {
                const themeDesc = themeDescriptions[selectedTheme] || 'beautiful Eid greeting card with Islamic ornaments';
                const familyStr = familyName ? ` Family name displayed on card: "${familyName}".` : '';
                const faceRef = uploadedImages.length > 0
                    ? ' Transform the face(s) in the scene into the faces from the uploaded reference photo(s). Use the facial features, skin tone, face shape, nose shape, lips, eyes, eyebrows, and expressions from the reference face image(s) uploaded. Maintain realistic skin texture, natural pores, sharp facial details, and a photorealistic look.'
                    : '';

                const prompt = `Create a beautiful photorealistic Eid ul Fitr (Idul Fitri 1447 H) greeting card image.

THEME: ${themeDesc}

${uploadedImages.length > 0 ? 'Show a photorealistic portrait of a Muslim family or couple dressed elegantly in traditional Lebaran attire (gamis, baju koko, kebaya), posed warmly and joyfully in front of the themed backdrop.' + faceRef : 'Show a beautiful visual Eid greeting card scene with photorealistic themed backdrop and decorations, without any people.'}

GREETING TEXT (must be displayed prominently and clearly readable on the card): "${greeting}"${familyStr}
YEAR ON CARD: 1447 H / 2026 M

Style: photorealistic, high quality, warm festive Lebaran lighting, elegant decorative frame around edges matching the theme. Suitable for WhatsApp and social media sharing. Professional greeting card quality.`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const parts = [{ text: prompt }];
                if (uploadedImages.length > 0) {
                    uploadedImages.forEach(img => parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } }));
                }

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const data = await response.json();
                const imageData = data?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data in response');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `kartu-lebaran-${selectedTheme}-${index}-${Date.now()}.jpg`;
                generatedImages[index - 1] = { url: imageUrl, filename };

                const aspectCls = selectedRatio === '3:4' ? 'aspect-[3/4]' : (selectedRatio === '9:16' ? 'aspect-[9/16]' : 'aspect-square');
                if (card) {
                    card.className = `relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 ${aspectCls} border-2`;
                    card.style.borderColor = '#16a34a';
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Kartu Lebaran ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2" style="background: linear-gradient(135deg, #16a34a 0%, #d97706 100%);">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }

            } catch (error) {
                console.error(`Error generating lebaran card ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();
                const btn = e.target.closest('[data-action]');
                if (!btn) return;
                const action = btn.dataset.action;
                const idx = parseInt(btn.dataset.index);
                const img = generatedImages[idx];
                if (!img) return;
                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(img.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) await window.downloadDataURINew(img.url, img.filename);
                    else if (window.downloadImage) await window.downloadImage(img.url, img.filename);
                }
            });
        }

        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async () => {
                const validImages = generatedImages.filter(Boolean);
                if (!validImages.length) return;
                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;
                downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading...</span>';
                try {
                    for (let i = 0; i < validImages.length; i++) {
                        try {
                            if (window.downloadDataURINew) await window.downloadDataURINew(validImages[i].url, validImages[i].filename);
                            else if (window.downloadImage) await window.downloadImage(validImages[i].url, validImages[i].filename);
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (err) { console.error(`Download error ${i + 1}:`, err); }
                    }
                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => { downloadAllBtn.disabled = false; downloadAllBtn.innerHTML = originalHTML; }, 2000);
                } catch (err) {
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => { downloadAllBtn.disabled = false; downloadAllBtn.innerHTML = originalHTML; }, 2000);
                }
            });
        }

    })();

    // ==================== IMAGE TO PROMPT ANALYZER ====================
    (function() {
        const apiKey = ""; // Injected by Canvas

        // DOM Elements
        const imageInput = document.getElementById('analyzer-image-upload');
        const imagePreview = document.getElementById('analyzer-image-preview');
        const previewContainer = document.getElementById('analyzer-image-preview-container');
        const removeImageBtn = document.getElementById('analyzer-remove-image');

        const detailLevelContainer = document.getElementById('analyzer-detail-level');
        const promptFormatContainer = document.getElementById('analyzer-prompt-format');
        const languageContainer = document.getElementById('analyzer-language');
        const outputFormatContainer = document.getElementById('analyzer-output-format');

        const analyzeBtn = document.getElementById('analyzer-analyze-btn');
        const loading = document.getElementById('analyzer-loading');
        const loadingText = document.getElementById('analyzer-loading-text');
        const errorMessage = document.getElementById('analyzer-error-message');
        const results = document.getElementById('analyzer-results');
        const emptyState = document.getElementById('analyzer-empty-state');
        const promptOutput = document.getElementById('analyzer-prompt-output');
        const copyBtn = document.getElementById('analyzer-copy-btn');

        // State
        let analyzerImageData = null;
        let selectedDetailLevel = 'detailed';
        let selectedPromptFormat = 'general';
        let selectedLanguage = 'id';
        let selectedOutputFormat = 'text';

        // Image upload handler with HEIC support
        if (imageInput) {
            imageInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                // Read file
                const reader = new FileReader();
                reader.onloadend = () => {
                    analyzerImageData = reader.result;

                    // Show preview
                    if (imagePreview && previewContainer) {
                        imagePreview.src = reader.result;
                        previewContainer.classList.remove('hidden');
                    }

                    checkReadyToAnalyze();
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove image handler
        if (removeImageBtn) {
            removeImageBtn.addEventListener('click', () => {
                analyzerImageData = null;
                if (imageInput) imageInput.value = '';
                if (previewContainer) previewContainer.classList.add('hidden');
                if (imagePreview) imagePreview.src = '';
                checkReadyToAnalyze();
            });
        }

        // Option selection handler
        function setupOptionButtons(container, callback) {
            if (!container) return;

            container.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                // Remove selected state from all buttons in this container
                container.querySelectorAll('.option-btn-analyzer').forEach(btn => {
                    btn.classList.remove('selected');
                });

                // Add selected state to clicked button
                button.classList.add('selected');

                // Get value and call callback
                const value = button.dataset.value;
                callback(value);
            });
        }

        setupOptionButtons(detailLevelContainer, (value) => selectedDetailLevel = value);
        setupOptionButtons(promptFormatContainer, (value) => selectedPromptFormat = value);
        setupOptionButtons(languageContainer, (value) => selectedLanguage = value);
        setupOptionButtons(outputFormatContainer, (value) => selectedOutputFormat = value);

        // Check if ready to analyze
        function checkReadyToAnalyze() {
            if (analyzeBtn) {
                analyzeBtn.disabled = !analyzerImageData;
            }
        }

        // Analyze button handler
        if (analyzeBtn) {
            analyzeBtn.addEventListener('click', async () => {
                if (!analyzerImageData) {
                    showError('Silakan upload gambar terlebih dahulu!');
                    return;
                }

                await analyzeImage();
            });
        }

        // Fallback copy function for mobile compatibility
        function fallbackCopyToClipboard(text) {
            const textArea = document.createElement('textarea');
            textArea.value = text;
            textArea.style.position = 'fixed';
            textArea.style.left = '-9999px';
            textArea.style.top = '0';
            textArea.style.opacity = '0';
            document.body.appendChild(textArea);
            textArea.focus();
            textArea.select();

            try {
                const successful = document.execCommand('copy');
                document.body.removeChild(textArea);
                return successful;
            } catch (err) {
                document.body.removeChild(textArea);
                return false;
            }
        }

        // Copy button handler
        if (copyBtn) {
            copyBtn.addEventListener('click', async (e) => {
                e.preventDefault();
                e.stopPropagation();

                const text = promptOutput?.textContent?.trim();
                if (!text) {
                    console.log('No text to copy');
                    return;
                }

                let copySuccess = false;

                // Try modern Clipboard API first
                if (navigator.clipboard && window.isSecureContext) {
                    try {
                        await navigator.clipboard.writeText(text);
                        copySuccess = true;
                    } catch (error) {
                        console.warn('Clipboard API failed, trying fallback:', error);
                    }
                }

                // Fallback for mobile/older browsers
                if (!copySuccess) {
                    copySuccess = fallbackCopyToClipboard(text);
                }

                if (copySuccess) {
                    // Visual feedback
                    const originalHTML = copyBtn.innerHTML;
                    copyBtn.innerHTML = '<i class="fas fa-check pointer-events-none"></i><span class="hidden sm:inline ml-2 pointer-events-none">Copied!</span>';
                    copyBtn.classList.remove('from-cyan-500', 'to-cyan-600');
                    copyBtn.classList.add('from-green-500', 'to-green-600');

                    setTimeout(() => {
                        copyBtn.innerHTML = originalHTML;
                        copyBtn.classList.remove('from-green-500', 'to-green-600');
                        copyBtn.classList.add('from-cyan-500', 'to-cyan-600');
                    }, 2000);
                } else {
                    console.error('All copy methods failed');
                    showError('Gagal copy ke clipboard');
                }
            });
        }

        // Analyze image function
        async function analyzeImage() {
            // Hide empty state, show loading
            if (emptyState) emptyState.classList.add('hidden');
            if (loading) loading.classList.remove('hidden');
            if (errorMessage) errorMessage.classList.add('hidden');
            if (results) results.classList.add('hidden');

            try {
                // Build analysis prompt based on settings
                const analysisPrompt = buildAnalysisPrompt();

                // Update loading text
                if (loadingText) loadingText.textContent = 'Menganalisis komposisi gambar...';

                // Prepare image data
                const base64Data = analyzerImageData.split(',')[1];
                const mimeType = analyzerImageData.split(';')[0].split(':')[1];

                // API request using gemini-2.0-flash (text model for analysis)
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: analysisPrompt },
                            {
                                inlineData: {
                                    mimeType: mimeType,
                                    data: base64Data
                                }
                            }
                        ]
                    }],
                    generationConfig: {
                        maxOutputTokens: 4096,
                        temperature: 0.7
                    }
                };

                if (loadingText) loadingText.textContent = 'Membuat prompt AI profesional...';

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();

                // Extract text from response
                const promptText = result?.candidates?.[0]?.content?.parts?.[0]?.text;
                if (!promptText) {
                    throw new Error('No text data in response');
                }

                // Display results
                if (promptOutput) {
                    promptOutput.textContent = promptText.trim();
                }

                // Show results
                if (results) results.classList.remove('hidden');

            } catch (error) {
                console.error('Error analyzing image:', error);
                showError('Terjadi kesalahan saat menganalisis gambar. Silakan coba lagi.');
            } finally {
                if (loading) loading.classList.add('hidden');
            }
        }

        // Build analysis prompt based on settings
        function buildAnalysisPrompt() {
            const language = selectedLanguage === 'id' ? 'Bahasa Indonesia' : 'English';

            // If JSON format is selected, use JSON prompt
            if (selectedOutputFormat === 'json') {
                return buildJSONPrompt(language);
            }

            // Detail level descriptions
            const detailLevels = {
                basic: selectedLanguage === 'id' ?
                    'Analisis dasar dengan deskripsi umum' :
                    'Basic analysis with general description',
                detailed: selectedLanguage === 'id' ?
                    'Analisis detail dengan elemen-elemen spesifik' :
                    'Detailed analysis with specific elements',
                expert: selectedLanguage === 'id' ?
                    'Analisis expert dengan semua detail teknis (lighting, composition, colors, style, mood, teknik fotografi/artistic)' :
                    'Expert analysis with all technical details (lighting, composition, colors, style, mood, photography/artistic techniques)'
            };

            // Format-specific instructions
            const formatInstructions = {
                general: selectedLanguage === 'id' ?
                    'Format prompt untuk general AI image generation' :
                    'Format prompt for general AI image generation',
                photography: selectedLanguage === 'id' ?
                    'Format prompt khusus fotografi dengan detail camera settings, lighting setup, lens, aperture, dan teknik photography' :
                    'Format prompt specifically for photography with camera settings, lighting setup, lens, aperture, and photography techniques',
                art: selectedLanguage === 'id' ?
                    'Format prompt untuk art style dengan detail artistic medium, art movement, style references, color palette, brushwork' :
                    'Format prompt for art style with artistic medium, art movement, style references, color palette, brushwork',
                product: selectedLanguage === 'id' ?
                    'Format prompt untuk product photography dengan detail positioning, background, lighting for product showcase, commercial appeal' :
                    'Format prompt for product photography with positioning, background, lighting for product showcase, commercial appeal',
                portrait: selectedLanguage === 'id' ?
                    'Format prompt untuk portrait photography dengan detail ekspresi wajah, pose, framing, background, mood, karakteristik subjek' :
                    'Format prompt for portrait photography with facial expression, pose, framing, background, mood, subject characteristics',
                scene: selectedLanguage === 'id' ?
                    'Format prompt untuk scene/landscape dengan detail environment, atmosphere, weather, time of day, landscape elements, perspective' :
                    'Format prompt for scene/landscape with environment, atmosphere, weather, time of day, landscape elements, perspective'
            };

            if (selectedLanguage === 'id') {
                return `Kamu adalah seorang AI Prompt Engineer profesional. Analisis gambar ini secara ${detailLevels[selectedDetailLevel]} dan buat prompt AI yang profesional dan detail.

INSTRUKSI:
1. Analisis gambar dengan level detail: ${detailLevels[selectedDetailLevel]}
2. ${formatInstructions[selectedPromptFormat]}
3. Output harus dalam ${language}

FORMAT OUTPUT (WAJIB DIIKUTI):

**PROMPT AI:**
[Tulis prompt yang siap digunakan untuk AI image generator. Prompt harus detail, deskriptif, dan mengandung semua elemen visual penting dari gambar]

**ELEMEN KUNCI:**
[List elemen-elemen utama yang ada dalam gambar]

**DETAIL TEKNIS:**
${selectedDetailLevel === 'expert' ? '[Jelaskan detail teknis: lighting, composition, color grading, camera angle, dll]' : '[Jelaskan detail teknis yang relevan]'}

${selectedPromptFormat === 'photography' ? '**PHOTOGRAPHY DETAILS:**\n[Detail spesifik fotografi: lens, aperture, ISO, lighting setup, dll]' : ''}

${selectedPromptFormat === 'art' ? '**ART STYLE DETAILS:**\n[Detail spesifik art style: medium, art movement, technique, dll]' : ''}

**TIPS PENGGUNAAN:**
[Tips singkat untuk menggunakan prompt ini dengan optimal]

PENTING:
- Buat prompt yang DETAIL dan SPESIFIK
- Fokus pada ${formatInstructions[selectedPromptFormat]}
- Gunakan bahasa yang jelas dan deskriptif
- Jangan gunakan referensi nama brand/karakter tertentu
- Output harus dalam ${language}`;
            } else {
                return `You are a professional AI Prompt Engineer. Analyze this image with ${detailLevels[selectedDetailLevel]} and create a professional, detailed AI prompt.

INSTRUCTIONS:
1. Analyze the image with detail level: ${detailLevels[selectedDetailLevel]}
2. ${formatInstructions[selectedPromptFormat]}
3. Output must be in ${language}

OUTPUT FORMAT (MUST FOLLOW):

**AI PROMPT:**
[Write a ready-to-use prompt for AI image generators. The prompt should be detailed, descriptive, and contain all important visual elements from the image]

**KEY ELEMENTS:**
[List the main elements present in the image]

**TECHNICAL DETAILS:**
${selectedDetailLevel === 'expert' ? '[Explain technical details: lighting, composition, color grading, camera angle, etc.]' : '[Explain relevant technical details]'}

${selectedPromptFormat === 'photography' ? '**PHOTOGRAPHY DETAILS:**\n[Specific photography details: lens, aperture, ISO, lighting setup, etc.]' : ''}

${selectedPromptFormat === 'art' ? '**ART STYLE DETAILS:**\n[Specific art style details: medium, art movement, technique, etc.]' : ''}

**USAGE TIPS:**
[Brief tips for using this prompt optimally]

IMPORTANT:
- Create a DETAILED and SPECIFIC prompt
- Focus on ${formatInstructions[selectedPromptFormat]}
- Use clear and descriptive language
- Don't use specific brand/character references
- Output must be in ${language}`;
            }
        }

        // Build JSON format prompt
        function buildJSONPrompt(language) {
            const detailLevel = selectedDetailLevel;
            const promptFormat = selectedPromptFormat;

            if (language === 'Bahasa Indonesia') {
                return `Kamu adalah seorang AI Prompt Engineer profesional. Analisis gambar ini dan buat output dalam format JSON yang valid dan terstruktur.

LEVEL ANALISIS: ${detailLevel} (basic/detailed/expert)
FORMAT PROMPT: ${promptFormat}
BAHASA: ${language}

OUTPUT FORMAT (WAJIB JSON VALID):
Buat JSON dengan struktur berikut:

{
  "prompt": "string - Prompt lengkap yang siap digunakan untuk AI image generator",
  "short_prompt": "string - Versi pendek/summary dari prompt (1-2 kalimat)",
  "main_subject": "string - Subjek utama dalam gambar",
  "style": "string - Style/gaya visual (photography, illustration, art, dll)",
  "composition": {
    "framing": "string - Framing/pembingkaian (close-up, medium shot, wide shot, dll)",
    "angle": "string - Camera angle (eye level, low angle, high angle, dll)",
    "perspective": "string - Perspektif (front view, side view, bird's eye, dll)"
  },
  "lighting": {
    "type": "string - Jenis pencahayaan (natural, studio, golden hour, dll)",
    "mood": "string - Mood pencahayaan (bright, dramatic, soft, moody, dll)",
    "direction": "string - Arah cahaya (front, side, back, rim light, dll)"
  },
  "colors": {
    "palette": "array of strings - Warna dominan dalam gambar",
    "tone": "string - Tone warna keseluruhan (warm, cool, neutral, vibrant, muted, dll)",
    "contrast": "string - Level kontras (high, medium, low)"
  },
  "technical_details": {
    "quality": "string - Kualitas gambar (professional, high-res, DSLR quality, dll)",
    "detail_level": "string - Level detail (ultra detailed, detailed, simple, minimalist)",
    "sharpness": "string - Ketajaman (sharp, soft focus, bokeh, dll)"
  },
  "elements": [
    "array of strings - List elemen-elemen penting dalam gambar"
  ],
  "tags": [
    "array of strings - Tag/keyword relevan untuk gambar ini"
  ],
  "recommended_models": [
    "array of strings - Model AI yang direkomendasikan (Midjourney, DALL-E, Stable Diffusion, dll)"
  ]
}

${promptFormat === 'photography' ? `
TAMBAHAN UNTUK PHOTOGRAPHY:
Tambahkan field berikut dalam JSON:
"photography_details": {
  "lens": "string - Estimasi jenis lens (50mm, wide angle, telephoto, dll)",
  "aperture": "string - Estimasi aperture (f/1.8, f/5.6, dll)",
  "depth_of_field": "string - Depth of field (shallow, deep, bokeh, dll)",
  "technique": "string - Teknik fotografi yang digunakan"
}
` : ''}

${promptFormat === 'art' ? `
TAMBAHAN UNTUK ART:
Tambahkan field berikut dalam JSON:
"art_details": {
  "medium": "string - Medium seni (digital art, oil painting, watercolor, dll)",
  "art_movement": "string - Aliran seni (impressionism, surrealism, modern, dll)",
  "technique": "string - Teknik artistik yang digunakan",
  "inspiration": "string - Inspirasi style (jika teridentifikasi)"
}
` : ''}

PENTING:
- Output HARUS valid JSON (bisa di-parse dengan JSON.parse())
- Tidak boleh ada teks di luar JSON
- Gunakan double quotes untuk string
- Array harus gunakan square brackets []
- Semua nilai harus dalam ${language}
- Jangan gunakan trailing comma
- Jangan gunakan comment dalam JSON`;
            } else {
                return `You are a professional AI Prompt Engineer. Analyze this image and create output in valid, structured JSON format.

ANALYSIS LEVEL: ${detailLevel} (basic/detailed/expert)
PROMPT FORMAT: ${promptFormat}
LANGUAGE: ${language}

OUTPUT FORMAT (MUST BE VALID JSON):
Create JSON with the following structure:

{
  "prompt": "string - Complete prompt ready to use for AI image generator",
  "short_prompt": "string - Short version/summary of the prompt (1-2 sentences)",
  "main_subject": "string - Main subject in the image",
  "style": "string - Visual style (photography, illustration, art, etc.)",
  "composition": {
    "framing": "string - Framing (close-up, medium shot, wide shot, etc.)",
    "angle": "string - Camera angle (eye level, low angle, high angle, etc.)",
    "perspective": "string - Perspective (front view, side view, bird's eye, etc.)"
  },
  "lighting": {
    "type": "string - Lighting type (natural, studio, golden hour, etc.)",
    "mood": "string - Lighting mood (bright, dramatic, soft, moody, etc.)",
    "direction": "string - Light direction (front, side, back, rim light, etc.)"
  },
  "colors": {
    "palette": "array of strings - Dominant colors in the image",
    "tone": "string - Overall color tone (warm, cool, neutral, vibrant, muted, etc.)",
    "contrast": "string - Contrast level (high, medium, low)"
  },
  "technical_details": {
    "quality": "string - Image quality (professional, high-res, DSLR quality, etc.)",
    "detail_level": "string - Level of detail (ultra detailed, detailed, simple, minimalist)",
    "sharpness": "string - Sharpness (sharp, soft focus, bokeh, etc.)"
  },
  "elements": [
    "array of strings - List of important elements in the image"
  ],
  "tags": [
    "array of strings - Relevant tags/keywords for this image"
  ],
  "recommended_models": [
    "array of strings - Recommended AI models (Midjourney, DALL-E, Stable Diffusion, etc.)"
  ]
}

${promptFormat === 'photography' ? `
ADDITIONAL FOR PHOTOGRAPHY:
Add the following field in JSON:
"photography_details": {
  "lens": "string - Estimated lens type (50mm, wide angle, telephoto, etc.)",
  "aperture": "string - Estimated aperture (f/1.8, f/5.6, etc.)",
  "depth_of_field": "string - Depth of field (shallow, deep, bokeh, etc.)",
  "technique": "string - Photography technique used"
}
` : ''}

${promptFormat === 'art' ? `
ADDITIONAL FOR ART:
Add the following field in JSON:
"art_details": {
  "medium": "string - Art medium (digital art, oil painting, watercolor, etc.)",
  "art_movement": "string - Art movement (impressionism, surrealism, modern, etc.)",
  "technique": "string - Artistic technique used",
  "inspiration": "string - Style inspiration (if identifiable)"
}
` : ''}

IMPORTANT:
- Output MUST be valid JSON (parseable with JSON.parse())
- No text outside JSON
- Use double quotes for strings
- Arrays must use square brackets []
- All values must be in ${language}
- No trailing commas
- No comments in JSON`;
            }
        }

        // Show error message
        function showError(message) {
            if (errorMessage) {
                errorMessage.classList.remove('hidden');
                const errorText = errorMessage.querySelector('p');
                if (errorText) errorText.textContent = message;

                setTimeout(() => {
                    errorMessage.classList.add('hidden');
                }, 5000);
            }
        }

    })();

    // ==================== PACKAGING DESIGN GENERATOR ====================
    (function() {
        const apiKey = ""; // Injected by Canvas

        // DOM Elements
        const productInput = document.getElementById('packaging-product-upload');
        const productPreview = document.getElementById('packaging-product-preview');
        const productPreviewContainer = document.getElementById('packaging-product-preview-container');
        const removeProductBtn = document.getElementById('packaging-remove-product');

        const typeOptions = document.getElementById('packaging-type-options');
        const styleOptions = document.getElementById('packaging-style-options');
        const brandNameInput = document.getElementById('packaging-brand-name');
        const brandColorInput = document.getElementById('packaging-brand-color');

        const generateBtn = document.getElementById('packaging-generate-btn');

        const loading = document.getElementById('packaging-loading');
        const loadingText = document.getElementById('packaging-loading-text');
        const progressBar = document.getElementById('packaging-progress-bar');
        const progressText = document.getElementById('packaging-progress-text');

        const errorMessage = document.getElementById('packaging-error-message');
        const resultsHeader = document.getElementById('packaging-results-header');
        const resultsCount = document.getElementById('packaging-results-count');
        const resultsGrid = document.getElementById('packaging-results-grid');
        const emptyState = document.getElementById('packaging-empty-state');
        const downloadAllBtn = document.getElementById('packaging-download-all-btn');

        // State
        let productImageData = null;
        let selectedType = 'box';
        let selectedStyle = 'minimalist';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedImages = [];

        // Count selection (1-10), default 4
        const packagingCountSel = document.getElementById('packaging-count-selection-grid');
        if (packagingCountSel) {
            packagingCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                packagingCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Packaging type descriptions
        const packagingTypes = {
            box: 'Professional product box/cardboard packaging with windows and display features',
            pouch: 'Standing pouch or flexible plastic packaging with zipper closure',
            bottle: 'Glass or plastic bottle packaging with label design',
            can: 'Aluminum can or tin container packaging',
            paperbag: 'Premium paper bag with handle and brand design',
            tube: 'Cosmetic or cream tube packaging with cap'
        };

        // Design style descriptions
        const designStyles = {
            minimalist: 'Clean, simple design with lots of white space, sans-serif typography, minimal colors, modern aesthetic',
            luxury: 'Premium, elegant design with gold/silver accents, sophisticated typography, high-end feel, embossed effects',
            eco: 'Eco-friendly, sustainable design with earthy colors, kraft paper texture, natural elements, green messaging',
            modern: 'Contemporary design with bold colors, geometric shapes, clean lines, trendy aesthetic',
            vintage: 'Retro, classic design with aged textures, ornamental borders, serif fonts, nostalgic appeal',
            playful: 'Fun, colorful design with illustrations, vibrant colors, friendly typography, youthful energy'
        };

        // Product upload handler with HEIC support
        if (productInput) {
            productInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                // Read file
                const reader = new FileReader();
                reader.onloadend = () => {
                    productImageData = reader.result;

                    // Show preview
                    if (productPreview && productPreviewContainer) {
                        productPreview.src = reader.result;
                        productPreviewContainer.classList.remove('hidden');
                    }

                    checkReadyToGenerate();
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove product handler
        if (removeProductBtn) {
            removeProductBtn.addEventListener('click', () => {
                productImageData = null;
                if (productInput) productInput.value = '';
                if (productPreviewContainer) productPreviewContainer.classList.add('hidden');
                if (productPreview) productPreview.src = '';
                checkReadyToGenerate();
            });
        }

        // Option selection handlers
        function setupOptionButtons(container, callback) {
            if (!container) return;

            container.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value], [data-count]');
                if (!button) return;

                // Remove selected state from all buttons in this container
                container.querySelectorAll('.option-btn-packaging').forEach(btn => {
                    btn.classList.remove('selected');
                });

                // Add selected state to clicked button
                button.classList.add('selected');

                // Get value and call callback
                const value = button.dataset.value || button.dataset.count;
                callback(value);
            });
        }

        setupOptionButtons(typeOptions, (value) => selectedType = value);
        setupOptionButtons(styleOptions, (value) => selectedStyle = value);

        // Check if ready to generate
        function checkReadyToGenerate() {
            if (generateBtn) {
                generateBtn.disabled = !productImageData;
            }
        }

        // Generate button handler
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!productImageData) {
                    showError('Silakan upload foto produk terlebih dahulu!');
                    return;
                }

                await generatePackagingDesigns();
            });
        }

        // Main generation function
        async function generatePackagingDesigns() {
            if (emptyState) emptyState.classList.add('hidden');
            if (loading) loading.classList.remove('hidden');
            if (errorMessage) errorMessage.classList.add('hidden');
            if (resultsHeader) resultsHeader.classList.add('hidden');
            if (resultsGrid) {
                resultsGrid.innerHTML = '';
                resultsGrid.classList.add('grid');
            }

            generatedImages = [];

            const brandName = brandNameInput?.value.trim() || '';
            const brandColor = brandColorInput?.value || '#92400e';
            const typeDesc = packagingTypes[selectedType];
            const styleDesc = designStyles[selectedStyle];
            const basePrompt = buildPrompt(typeDesc, styleDesc, brandName, brandColor);

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = new Array(selectedCount).fill(null);
                if (resultsGrid) resultsGrid.innerHTML = '';
                if (progressBar) progressBar.style.width = '0%';
                if (progressText) progressText.textContent = `0/${selectedCount} selesai`;

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `packaging-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gradient-to-br from-amber-100 to-yellow-100 flex items-center justify-center aspect-[3/4]';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-amber-700"></div>';
                    resultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSingleDesign(i, basePrompt)
                );
                await Promise.allSettled(generationPromises);

                resultsGrid.querySelectorAll('[id^="packaging-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                successCount = generatedImages.filter(Boolean).length;
            }

            if (loading) loading.classList.add('hidden');

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                if (resultsHeader) resultsHeader.classList.remove('hidden');
                if (resultsCount) resultsCount.textContent = successCount;
            }
        }

        // Build AI prompt
        function buildPrompt(typeDesc, styleDesc, brandName, brandColor) {
            const brandText = brandName ? `Brand Name: "${brandName}"` : 'No brand name (create generic placeholder)';
            const colorText = `Primary Brand Color: ${brandColor}`;

            return `Create a professional, print-ready packaging design mockup with the following specifications:

PRODUCT IN IMAGE: Analyze and identify the product in the uploaded image.

PACKAGING TYPE: ${typeDesc}

DESIGN STYLE: ${styleDesc}

BRAND DETAILS:
- ${brandText}
- ${colorText}
${brandName ? `- Incorporate "${brandName}" prominently on the packaging` : '- Use elegant placeholder text for brand name'}

DESIGN REQUIREMENTS:
1. REALISTIC MOCKUP: Create a 3D realistic product packaging mockup, not flat design
2. PRODUCT INTEGRATION: Show the actual product from the image integrated naturally on/in the packaging
3. PROFESSIONAL QUALITY: High-resolution, print-ready quality with proper bleed and safe zones
4. BRAND CONSISTENCY: Use the specified brand color as primary accent throughout design
5. TYPOGRAPHY: Choose appropriate fonts that match the ${selectedStyle} style
6. VISUAL HIERARCHY: Clear product name, logo placement, and supporting text
7. FINISHING DETAILS: Include realistic materials, textures, shadows, and lighting
8. FRONT VIEW: Show the main front panel of the packaging clearly

TECHNICAL SPECS:
- High resolution 4K quality
- Proper lighting and shadows for realism
- Print-ready CMYK color profile consideration
- Bleed areas for production
- Professional photography presentation

OUTPUT: Create a stunning, production-ready packaging design that a brand would proudly use for their product. The design should be impressive, professional, and ready for manufacturing.`;
        }

        // Generate single design
        async function generateSingleDesign(index, basePrompt) {
            const cardId = `packaging-card-${index + 1}`;
            const card = document.getElementById(cardId);

            try {
                // Update loading text
                if (loadingText) {
                    const stages = [
                        'Merancang layout kemasan...',
                        'Menambahkan elemen brand...',
                        'Menyesuaikan warna dan tipografi...',
                        'Membuat mockup 3D...',
                        'Menambahkan sentuhan akhir...',
                        'Memfinalisasi desain print-ready...'
                    ];
                    loadingText.textContent = stages[Math.floor(Math.random() * stages.length)];
                }

                // Prepare image data
                const base64Data = productImageData.split(',')[1];
                const mimeType = productImageData.split(';')[0].split(':')[1];

                // API request
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const finalPrompt = `${basePrompt}\n\nVariation ${index + 1}: Create a unique design interpretation with different layout arrangement or visual approach while maintaining the core brand and style requirements.`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: finalPrompt },
                            {
                                inlineData: {
                                    mimeType: mimeType,
                                    data: base64Data
                                }
                            }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: {
                            aspectRatio: '3:4'
                        }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();

                // Extract image from response
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) {
                    throw new Error('No image data in response');
                }

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `packaging_${selectedType}_${selectedStyle}_${index + 1}_${Date.now()}.jpg`;

                // Store generated image (index is 0-based)
                generatedImages[index] = { url: imageUrl, filename: filename };

                // Update card with generated image
                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Packaging Design ${index + 1}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index}" class="action-btn bg-gradient-to-r from-amber-600 to-amber-700 hover:from-amber-700 hover:to-amber-800 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }

            } catch (error) {
                console.error(`Error generating design ${index + 1}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all button
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        // Show error message
        function showError(message) {
            if (errorMessage) {
                errorMessage.classList.remove('hidden');
                const errorText = errorMessage.querySelector('p');
                if (errorText) errorText.textContent = message;

                setTimeout(() => {
                    errorMessage.classList.add('hidden');
                }, 5000);
            }
        }

    })();

    // ==================== BEFORE-AFTER GENERATOR ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const modeUploadBtn = document.getElementById('ba-mode-upload');
        const modeTextBtn = document.getElementById('ba-mode-text');
        const uploadModeContent = document.getElementById('ba-upload-mode-content');
        const textModeContent = document.getElementById('ba-text-mode-content');

        // Upload Mode Elements
        const imageUpload = document.getElementById('ba-before-upload');
        const uploadBox = document.querySelector('.upload-box-before-after');
        const imagePreview = document.getElementById('ba-before-preview');
        const previewContainer = document.getElementById('ba-before-preview-container');
        const removePreviewBtn = document.getElementById('ba-remove-before');
        const transformInput = document.getElementById('ba-transform-prompt');
        const generateAfterBtn = document.getElementById('ba-generate-upload-btn');

        // Text Mode Elements
        const descriptionInput = document.getElementById('ba-text-prompt');
        const aspectRatioButtons = document.querySelectorAll('#ba-ratio-options [data-ratio]');
        const generateBothBtn = document.getElementById('ba-generate-text-btn');

        // Results Elements
        const loading = document.getElementById('ba-loading');
        const loadingText = document.getElementById('ba-loading-text');
        const errorMessage = document.getElementById('ba-error-message');
        const resultsSection = document.getElementById('ba-results');
        const resultsGrid = document.getElementById('ba-results-grid');
        const emptyState = document.getElementById('ba-empty-state');

        // State variables
        let currentMode = 'upload'; // 'upload' or 'text'
        let beforeImageData = null;
        let selectedAspectRatio = '16:9'; // Default matches HTML selected button
        let generatedImages = [];

        // Mode Toggle Handlers
        if (modeUploadBtn) {
            modeUploadBtn.addEventListener('click', () => {
                currentMode = 'upload';
                modeUploadBtn.classList.add('active');
                modeTextBtn.classList.remove('active');
                uploadModeContent.classList.remove('hidden');
                textModeContent.classList.add('hidden');
                resetState();
            });
        }

        if (modeTextBtn) {
            modeTextBtn.addEventListener('click', () => {
                currentMode = 'text';
                modeTextBtn.classList.add('active');
                modeUploadBtn.classList.remove('active');
                textModeContent.classList.remove('hidden');
                uploadModeContent.classList.add('hidden');
                resetState();
            });
        }

        // Aspect Ratio Selection (Text Mode)
        aspectRatioButtons.forEach(button => {
            button.addEventListener('click', () => {
                aspectRatioButtons.forEach(btn => {
                    btn.classList.remove('selected', 'bg-gradient-to-r', 'from-purple-500', 'to-purple-600', 'text-white', 'border-purple-500', 'font-bold', 'shadow-md');
                    btn.classList.add('bg-gray-50', 'hover:bg-gray-100', 'text-gray-700', 'border-gray-300', 'font-medium');
                });

                button.classList.remove('bg-gray-50', 'hover:bg-gray-100', 'text-gray-700', 'border-gray-300', 'font-medium');
                button.classList.add('selected', 'bg-gradient-to-r', 'from-purple-500', 'to-purple-600', 'text-white', 'border-purple-500', 'font-bold', 'shadow-md');

                selectedAspectRatio = button.dataset.ratio;
            });
        });

        // Image Upload Handler (Upload Mode)
        if (imageUpload) {
            imageUpload.addEventListener('change', handleImageUpload);
        }

        // Drag & drop handlers only (click is handled by label for="ba-before-upload")
        if (uploadBox) {
            uploadBox.addEventListener('dragover', (e) => {
                e.preventDefault();
                uploadBox.style.borderColor = '#7c3aed';
                uploadBox.style.background = 'linear-gradient(135deg, #f5f3ff 0%, #ede9fe 100%)';
            });

            uploadBox.addEventListener('dragleave', (e) => {
                e.preventDefault();
                uploadBox.style.borderColor = '';
                uploadBox.style.background = '';
            });

            uploadBox.addEventListener('drop', (e) => {
                e.preventDefault();
                uploadBox.style.borderColor = '';
                uploadBox.style.background = '';

                const file = e.dataTransfer.files[0];
                if (file && file.type.startsWith('image/')) {
                    const dataTransfer = new DataTransfer();
                    dataTransfer.items.add(file);
                    imageUpload.files = dataTransfer.files;
                    handleImageUpload({ target: imageUpload });
                }
            });
        }

        async function handleImageUpload(e) {
            const file = e.target.files[0];
            if (!file) return;

            let processedFile = file;

            // HEIC conversion
            if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                try {
                    const convertedBlob = await heic2any({
                        blob: file,
                        toType: 'image/jpeg',
                        quality: 0.9
                    });
                    processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                        type: 'image/jpeg'
                    });
                } catch (err) {
                    console.error('HEIC conversion failed:', err);
                    showError('Gagal mengkonversi gambar HEIC');
                    return;
                }
            }

            const reader = new FileReader();
            reader.onloadend = () => {
                beforeImageData = reader.result;

                if (imagePreview && previewContainer) {
                    imagePreview.src = reader.result;
                    previewContainer.classList.remove('hidden');
                }

                validateUploadMode();
            };
            reader.readAsDataURL(processedFile);
        }

        // Remove Preview Handler
        if (removePreviewBtn) {
            removePreviewBtn.addEventListener('click', () => {
                beforeImageData = null;
                if (imageUpload) imageUpload.value = '';
                if (previewContainer) previewContainer.classList.add('hidden');
                if (imagePreview) imagePreview.src = '';
                validateUploadMode();
            });
        }

        // Validate Upload Mode
        function validateUploadMode() {
            if (generateAfterBtn) {
                const hasImage = beforeImageData !== null;
                const hasTransform = transformInput && transformInput.value.trim().length > 0;
                generateAfterBtn.disabled = !(hasImage && hasTransform);
            }
        }

        if (transformInput) {
            transformInput.addEventListener('input', validateUploadMode);
        }

        // Validate Text Mode
        function validateTextMode() {
            if (generateBothBtn) {
                const hasDescription = descriptionInput && descriptionInput.value.trim().length > 0;
                generateBothBtn.disabled = !hasDescription;
            }
        }

        if (descriptionInput) {
            descriptionInput.addEventListener('input', validateTextMode);
        }

        // Generate After Image (Upload Mode)
        if (generateAfterBtn) {
            generateAfterBtn.addEventListener('click', async () => {
                if (!beforeImageData || !transformInput.value.trim()) return;

                const originalHTML = generateAfterBtn.innerHTML;
                generateAfterBtn.disabled = true;

                try {
                    // Show loading
                    if (loading) loading.classList.remove('hidden');
                    if (loadingText) loadingText.textContent = 'Generating after image...';
                    if (errorMessage) errorMessage.classList.add('hidden');
                    if (emptyState) emptyState.classList.add('hidden');

                    // Clear previous results
                    generatedImages = [];
                    if (resultsGrid) resultsGrid.innerHTML = '';
                    if (resultsSection) resultsSection.classList.add('hidden');

                    // Build prompt
                    const prompt = buildTransformPrompt(transformInput.value.trim());

                    // Extract base64 data
                    const base64Data = beforeImageData.split(',')[1];
                    const mimeType = beforeImageData.split(';')[0].split(':')[1];

                    // API call
                    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                    const payload = {
                        contents: [{
                            parts: [
                                { text: prompt },
                                { inlineData: { mimeType: mimeType, data: base64Data } }
                            ]
                        }],
                        generationConfig: {
                            responseModalities: ['IMAGE']
                        }
                    };

                    const response = await fetch(apiUrl, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(payload)
                    });

                    if (!response.ok) {
                        throw new Error(`API error: ${response.status}`);
                    }

                    const result = await response.json();
                    const imageData = result?.candidates?.[0]?.content?.parts?.find(part => part.inlineData)?.inlineData;

                    if (!imageData) {
                        throw new Error('No image data in response');
                    }

                    const imageUrl = `data:${imageData.mimeType};base64,${imageData.data}`;
                    const filename = `after_${Date.now()}.png`;

                    generatedImages[index - 1] = { url: imageUrl, filename: filename };

                    // Create result card
                    const card = document.createElement('div');
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="After Image">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="0" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="0" class="action-btn bg-gradient-to-r from-purple-600 to-purple-700 hover:from-purple-700 hover:to-purple-800 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                    if (resultsGrid) resultsGrid.appendChild(card);

                    // Show results
                    if (resultsSection) resultsSection.classList.remove('hidden');

                } catch (error) {
                    console.error('Error generating after image:', error);
                    showError('Terjadi kesalahan saat membuat gambar. Silakan coba lagi.');
                } finally {
                    if (loading) loading.classList.add('hidden');
                    generateAfterBtn.disabled = false;
                    generateAfterBtn.innerHTML = originalHTML;
                }
            });
        }

        // Generate Both Images (Text Mode)
        if (generateBothBtn) {
            generateBothBtn.addEventListener('click', async () => {
                if (!descriptionInput.value.trim()) return;

                const originalHTML = generateBothBtn.innerHTML;
                generateBothBtn.disabled = true;

                try {
                    // Show loading
                    if (loading) loading.classList.remove('hidden');
                    if (loadingText) loadingText.textContent = 'Generating before & after images...';
                    if (errorMessage) errorMessage.classList.add('hidden');
                    if (emptyState) emptyState.classList.add('hidden');

                    // Clear previous results
                    generatedImages = [];
                    if (resultsGrid) resultsGrid.innerHTML = '';
                    if (resultsSection) resultsSection.classList.add('hidden');

                    // Generate Before Image
                    if (loadingText) loadingText.textContent = 'Generating before image...';

                    const beforePrompt = buildBeforePrompt(descriptionInput.value.trim());
                    const beforeImageUrl = await generateImageFromText(beforePrompt);

                    generatedImages.push({ url: beforeImageUrl, filename: `before_${Date.now()}.png` });

                    // Create before card
                    const beforeCard = document.createElement('div');
                    beforeCard.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    beforeCard.innerHTML = `
                        <div class="absolute top-3 left-3 bg-purple-600 text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg z-10">
                            BEFORE
                        </div>
                        <img src="${beforeImageUrl}" class="w-full h-full object-cover" alt="Before Image">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="0" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="0" class="action-btn bg-gradient-to-r from-purple-600 to-purple-700 hover:from-purple-700 hover:to-purple-800 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                    if (resultsGrid) resultsGrid.appendChild(beforeCard);

                    // Generate After Image
                    if (loadingText) loadingText.textContent = 'Generating after image...';

                    const afterPrompt = buildAfterPrompt(descriptionInput.value.trim());
                    const afterImageUrl = await generateImageFromText(afterPrompt);

                    generatedImages.push({ url: afterImageUrl, filename: `after_${Date.now()}.png` });

                    // Create after card
                    const afterCard = document.createElement('div');
                    afterCard.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    afterCard.innerHTML = `
                        <div class="absolute top-3 left-3 bg-green-600 text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg z-10">
                            AFTER
                        </div>
                        <img src="${afterImageUrl}" class="w-full h-full object-cover" alt="After Image">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="1" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="1" class="action-btn bg-gradient-to-r from-green-600 to-green-700 hover:from-green-700 hover:to-green-800 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                    if (resultsGrid) resultsGrid.appendChild(afterCard);

                    // Show results
                    if (resultsSection) resultsSection.classList.remove('hidden');

                } catch (error) {
                    console.error('Error generating images:', error);
                    showError('Terjadi kesalahan saat membuat gambar. Silakan coba lagi.');
                } finally {
                    if (loading) loading.classList.add('hidden');
                    generateBothBtn.disabled = false;
                    generateBothBtn.innerHTML = originalHTML;
                }
            });
        }

        // Generate Image from Text
        async function generateImageFromText(prompt) {
            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

            const payload = {
                contents: [{
                    parts: [{ text: prompt }]
                }],
                generationConfig: {
                    responseModalities: ['IMAGE'],
                    imageConfig: { aspectRatio: selectedAspectRatio }
                }
            };

            const response = await fetch(apiUrl, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            });

            if (!response.ok) {
                throw new Error(`API error: ${response.status}`);
            }

            const result = await response.json();
            const imageData = result?.candidates?.[0]?.content?.parts?.find(part => part.inlineData)?.inlineData;

            if (!imageData) {
                throw new Error('No image data in response');
            }

            return `data:${imageData.mimeType};base64,${imageData.data}`;
        }

        // Build Prompts
        function buildTransformPrompt(transformation) {
            return `Transform the uploaded image based on this instruction: "${transformation}"

TRANSFORMATION REQUIREMENTS:
- Apply the exact transformation described by the user
- Maintain realistic lighting, shadows, and perspective
- Keep the composition and framing consistent
- Ensure photorealistic quality
- Preserve image resolution and quality

OUTPUT: Create a convincing "after" transformation that looks professional and realistic. The transformation should be believable and ready to use in a before-after comparison video.`;
        }

        function buildBeforePrompt(description) {
            return `Create a photorealistic "BEFORE" image based on this description: "${description}"

BEFORE IMAGE REQUIREMENTS:
- Show the initial/original state clearly
- Use realistic lighting and textures
- Create a relatable, everyday scene
- Ensure high quality and professional composition
- Make it suitable for before-after comparison

OUTPUT: A high-quality, photorealistic image showing the "before" state that will be dramatically transformed in the "after" image.`;
        }

        function buildAfterPrompt(description) {
            return `Create a photorealistic "AFTER" image based on this description: "${description}"

AFTER IMAGE REQUIREMENTS:
- Show the final/transformed state clearly
- Emphasize the improvement or transformation
- Use dramatic but realistic lighting
- Create an impressive, aspirational result
- Ensure high quality and professional composition
- Make the transformation obvious and impactful

OUTPUT: A stunning, photorealistic image showing the "after" state that demonstrates a clear and impressive transformation from the "before" state.`;
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    try {
                        actionBtn.disabled = true;
                        const originalHTML = actionBtn.innerHTML;
                        actionBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i>';

                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(imageData.url, imageData.filename);
                        } else if (window.downloadImage) {
                            await window.downloadImage(imageData.url, imageData.filename);
                        }

                        await new Promise(resolve => setTimeout(resolve, 300));
                        actionBtn.innerHTML = originalHTML;
                        actionBtn.disabled = false;
                    } catch (error) {
                        console.error('Download error:', error);
                        actionBtn.disabled = false;
                    }
                }
            });
        }

        // Show error message
        function showError(message) {
            if (errorMessage) {
                errorMessage.classList.remove('hidden');
                const errorText = errorMessage.querySelector('p');
                if (errorText) errorText.textContent = message;

                setTimeout(() => {
                    errorMessage.classList.add('hidden');
                }, 5000);
            }
        }

        // Reset state
        function resetState() {
            beforeImageData = null;
            generatedImages = [];

            if (imageUpload) imageUpload.value = '';
            if (previewContainer) previewContainer.classList.add('hidden');
            if (imagePreview) imagePreview.src = '';
            if (transformInput) transformInput.value = '';
            if (descriptionInput) descriptionInput.value = '';
            if (resultsGrid) resultsGrid.innerHTML = '';
            if (resultsSection) resultsSection.classList.add('hidden');
            if (emptyState) emptyState.classList.remove('hidden');
            if (errorMessage) errorMessage.classList.add('hidden');
            if (loading) loading.classList.add('hidden');

            validateUploadMode();
            validateTextMode();
        }

    })();

    // ========================================
    // VIDEO ANALYZER PRO FUNCTIONALITY
    // ========================================
    (function() {
        // DOM Elements
        const videoUpload = document.getElementById('va-video-upload');
        const fileNameDisplay = document.getElementById('va-file-name');
        const videoContainer = document.getElementById('va-video-container');
        const videoPreview = document.getElementById('video-preview');
        const videoInfo = document.getElementById('va-video-info');
        const duration = document.getElementById('va-duration');
        const resolution = document.getElementById('va-resolution');
        const filesize = document.getElementById('va-filesize');
        const format = document.getElementById('va-format');
        const frameSelector = document.getElementById('va-frame-selector');
        const frameBtns = document.querySelectorAll('.va-frame-btn');
        const analyzeBtn = document.getElementById('va-analyze-btn');
        const outputSection = document.getElementById('va-output-section');
        const loadingIndicator = document.getElementById('va-loading-indicator');
        const loadingText = document.getElementById('va-loading-text');
        const progressBar = document.getElementById('va-progress-bar');
        const progressText = document.getElementById('va-progress-text');
        const framesGallery = document.getElementById('va-frames-gallery');
        const framesGrid = document.getElementById('va-frames-grid');
        const promptContainer = document.getElementById('va-prompt-container');
        const promptOutput = document.getElementById('va-prompt-output');
        const charCount = document.getElementById('va-char-count');
        const copyBtn = document.getElementById('va-copy-btn');
        const copyText = document.getElementById('va-copy-text');
        const downloadPromptBtn = document.getElementById('va-download-prompt-btn');
        const canvas = document.getElementById('va-canvas');
        const errorMessage = document.getElementById('va-error-message');

        let videoFile = null;
        let selectedFrameCount = 5;
        let extractedFramesData = [];

        // Frame count selector
        frameBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                frameBtns.forEach(b => {
                    b.classList.remove('bg-purple-600', 'ring-2', 'ring-purple-400');
                    b.classList.add('bg-gray-700');
                });
                btn.classList.remove('bg-gray-700');
                btn.classList.add('bg-purple-600', 'ring-2', 'ring-purple-400');
                selectedFrameCount = parseInt(btn.dataset.frames);
            });
        });

        // Video upload handler
        videoUpload.addEventListener('change', async (event) => {
            videoFile = event.target.files[0];
            if (videoFile) {
                fileNameDisplay.textContent = videoFile.name;
                fileNameDisplay.classList.add('text-purple-400', 'font-semibold');

                const videoURL = URL.createObjectURL(videoFile);

                // Remove old event listeners by resetting handlers
                videoPreview.onloadedmetadata = null;
                videoPreview.onloadeddata = null;
                videoPreview.oncanplay = null;
                videoPreview.onerror = null;

                // Flag to prevent multiple triggers
                let metadataLoaded = false;

                const handleVideoReady = () => {
                    if (metadataLoaded) return;
                    metadataLoaded = true;

                    const dur = videoPreview.duration;
                    if (dur && !isNaN(dur)) {
                        const mins = Math.floor(dur / 60);
                        const secs = Math.floor(dur % 60);
                        duration.textContent = `${mins}:${secs.toString().padStart(2, '0')}`;
                    } else {
                        duration.textContent = 'N/A';
                    }

                    if (videoPreview.videoWidth && videoPreview.videoHeight) {
                        resolution.textContent = `${videoPreview.videoWidth}x${videoPreview.videoHeight}`;
                    } else {
                        resolution.textContent = 'N/A';
                    }

                    filesize.textContent = formatFileSize(videoFile.size);
                    const formatType = videoFile.type ? videoFile.type.split('/')[1] : videoFile.name.split('.').pop();
                    format.textContent = formatType ? formatType.toUpperCase() : 'VIDEO';

                    videoInfo.classList.remove('hidden');
                    analyzeBtn.disabled = false;
                    console.log('Video ready - analyze button enabled');
                };

                // Multiple event listeners for compatibility
                videoPreview.onloadedmetadata = handleVideoReady;
                videoPreview.onloadeddata = handleVideoReady;
                videoPreview.oncanplay = handleVideoReady;

                // Error handling
                videoPreview.onerror = (e) => {
                    console.error('Video load error:', e);
                    // Still enable button for unsupported preview but valid file
                    filesize.textContent = formatFileSize(videoFile.size);
                    const formatType = videoFile.type ? videoFile.type.split('/')[1] : videoFile.name.split('.').pop();
                    format.textContent = formatType ? formatType.toUpperCase() : 'VIDEO';
                    duration.textContent = 'N/A';
                    resolution.textContent = 'N/A';
                    videoInfo.classList.remove('hidden');
                    analyzeBtn.disabled = false;
                    console.log('Video preview failed but file is valid - analyze button enabled');
                };

                // Set source after attaching handlers
                videoPreview.src = videoURL;
                videoPreview.load(); // Force load
                videoContainer.classList.remove('hidden');
                frameSelector.classList.remove('hidden');

                // Fallback timeout - enable button after 3 seconds if events don't fire
                setTimeout(() => {
                    if (!metadataLoaded && videoFile) {
                        console.log('Fallback timeout - enabling analyze button');
                        handleVideoReady();
                    }
                }, 3000);

            } else {
                resetUI();
            }
        });

        // Analyze button
        analyzeBtn.addEventListener('click', async () => {
            if (!videoFile) return;

            // Reset UI
            analyzeBtn.disabled = true;
            analyzeBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Processing...';
            outputSection.classList.remove('hidden');
            loadingIndicator.classList.remove('hidden');
            promptContainer.classList.add('hidden');
            framesGallery.classList.add('hidden');
            framesGrid.innerHTML = '';
            promptOutput.value = '';
            errorMessage.classList.add('hidden');
            extractedFramesData = [];

            try {
                // Step 1: Extract frames
                loadingText.textContent = `Extracting ${selectedFrameCount} frames...`;
                const frames = await extractFramesWithProgress(videoFile, selectedFrameCount);
                extractedFramesData = frames;

                // Step 2: Display frames gallery
                displayFramesGallery(frames);
                framesGallery.classList.remove('hidden');

                // Step 3: Generate prompt
                loadingText.textContent = 'Generating AI prompt...';
                progressBar.style.width = '100%';
                progressText.textContent = '100%';

                const generatedPrompt = await generatePromptWithGemini(frames.map(f => f.base64));

                // Step 4: Display result
                promptOutput.value = generatedPrompt;
                charCount.textContent = `${generatedPrompt.length} characters`;
                promptContainer.classList.remove('hidden');

            } catch (error) {
                console.error("Error during analysis:", error);
                showError(`Error: ${error.message || 'Terjadi kesalahan saat menganalisis video.'}`);
            } finally {
                loadingIndicator.classList.add('hidden');
                analyzeBtn.disabled = false;
                analyzeBtn.innerHTML = '<i class="fas fa-magic"></i><span>Mulai Analisis</span>';
            }
        });

        // Copy button
        copyBtn.addEventListener('click', async () => {
            try {
                await navigator.clipboard.writeText(promptOutput.value);
                const originalHTML = copyText.innerHTML;
                copyText.innerHTML = '<i class="fas fa-check mr-2"></i>Copied!';
                copyBtn.classList.add('bg-green-600');
                setTimeout(() => {
                    copyText.textContent = 'Copy Prompt';
                    copyBtn.classList.remove('bg-green-600');
                }, 2000);
            } catch (err) {
                promptOutput.select();
                document.execCommand('copy');
            }
        });

        // Download prompt button
        downloadPromptBtn.addEventListener('click', () => {
            const text = promptOutput.value;
            if (!text) return;

            const blob = new Blob([text], { type: 'text/plain' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = `video-prompt-${Date.now()}.txt`;
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);
            URL.revokeObjectURL(url);
        });

        // Helper functions
        function formatFileSize(bytes) {
            if (bytes < 1024) return bytes + ' B';
            else if (bytes < 1048576) return (bytes / 1024).toFixed(1) + ' KB';
            else if (bytes < 1073741824) return (bytes / 1048576).toFixed(1) + ' MB';
            else return (bytes / 1073741824).toFixed(1) + ' GB';
        }

        function resetUI() {
            fileNameDisplay.textContent = 'Belum ada video dipilih';
            fileNameDisplay.classList.remove('text-purple-400', 'font-semibold');
            videoContainer.classList.add('hidden');
            videoInfo.classList.add('hidden');
            frameSelector.classList.add('hidden');
            videoPreview.src = '';
            analyzeBtn.disabled = true;
            videoFile = null;
        }

        function displayFramesGallery(frames) {
            framesGrid.innerHTML = '';
            frames.forEach((frame, index) => {
                const frameCard = document.createElement('div');
                frameCard.className = 'relative group cursor-pointer transform hover:scale-105 transition-all duration-300';
                frameCard.innerHTML = `
                    <img src="data:image/jpeg;base64,${frame.base64}"
                         alt="Frame ${index + 1}"
                         class="w-full h-auto rounded-lg border-2 border-gray-700 hover:border-purple-500 shadow-lg">
                    <div class="absolute top-2 left-2 bg-black/70 text-white text-xs px-2 py-1 rounded-full font-semibold">
                        Frame ${index + 1}
                    </div>
                `;
                frameCard.addEventListener('click', () => {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(`data:image/jpeg;base64,${frame.base64}`);
                    }
                });
                framesGrid.appendChild(frameCard);
            });
        }

        function showError(message) {
            errorMessage.innerHTML = `<i class="fas fa-exclamation-circle mr-2"></i>${message}`;
            errorMessage.classList.remove('hidden');
        }

        /**
         * Extracts frames with progress tracking
         */
        function extractFramesWithProgress(file, numFrames) {
            return new Promise((resolve, reject) => {
                const video = document.createElement('video');
                video.preload = 'metadata';
                video.src = URL.createObjectURL(file);
                const ctx = canvas.getContext('2d');
                const frames = [];

                video.onloadedmetadata = () => {
                    const duration = video.duration;
                    if (duration === 0) {
                       reject(new Error("Video duration is zero or could not be determined."));
                       return;
                    }
                    const interval = duration / (numFrames + 1);
                    let framesExtracted = 0;

                    const captureFrame = (time) => {
                        video.currentTime = time;
                    };

                    video.onseeked = () => {
                        canvas.width = video.videoWidth;
                        canvas.height = video.videoHeight;
                        ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                        const base64Data = canvas.toDataURL('image/jpeg').split(',')[1];
                        frames.push({ base64: base64Data, timestamp: video.currentTime });
                        framesExtracted++;

                        // Update progress
                        const progress = Math.round((framesExtracted / numFrames) * 100);
                        progressBar.style.width = `${progress}%`;
                        progressText.textContent = `${progress}% (${framesExtracted}/${numFrames} frames)`;

                        if (framesExtracted < numFrames) {
                            captureFrame((framesExtracted + 1) * interval);
                        } else {
                            URL.revokeObjectURL(video.src);
                            resolve(frames);
                        }
                    };

                    video.onerror = (e) => {
                        reject(new Error("Error loading video for frame extraction."));
                    };

                    // Start extraction
                    captureFrame(interval);
                    progressBar.style.width = '0%';
                    progressText.textContent = '0%';
                };

                video.onerror = (e) => {
                     reject(new Error("Could not load video metadata. The file might be corrupted or in an unsupported format."));
                };
            });
        }

        /**
         * Generates a descriptive prompt by sending video frames to the Gemini API.
         * @param {string[]} base64Frames - An array of base64-encoded frame data (without the data URL prefix).
         * @returns {Promise<string>} A promise that resolves to the generated text prompt.
         */
        async function generatePromptWithGemini(base64Frames) {
            const apiKey = ""; // API key
            const url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

            const systemInstruction = `You are an expert video analyst and prompt engineer for generative AI. Your task is to analyze a sequence of frames from a video and create a single, highly detailed, and coherent prompt that could be used by a text-to-video AI model to recreate a similar video.

            Combine the information from all frames into one unified description. Describe the scene, subjects, actions, and atmosphere with vivid language.

            Structure your prompt to cover these aspects in order:
            1.  **Core Subject & Action:** What is the main subject and what are they doing?
            2.  **Setting & Environment:** Where does the scene take place? Describe the background and key objects.
            3.  **Visual Style:** What is the overall aesthetic? (e.g., cinematic, realistic, hyperrealistic, anime, vaporwave, grainy vintage footage).
            4.  **Cinematography:** Describe the camera work (e.g., wide shot, close-up, panning shot, drone footage, shaky cam).
            5.  **Lighting:** Describe the lighting (e.g., golden hour, soft studio lighting, neon glow, moody and dark).
            6.  **Color Palette:** What are the dominant colors? (e.g., vibrant and saturated, muted earth tones, monochrome).
            7.  **Mood/Feeling:** What is the overall feeling or emotion of the scene? (e.g., peaceful, energetic, suspenseful, nostalgic).

            Synthesize all observations into a single, compelling paragraph.`;

            const userQuery = "Analyze these video frames and generate a detailed prompt based on your instructions.";

            const imageParts = base64Frames.map(frame => ({
                inlineData: {
                    mimeType: 'image/jpeg',
                    data: frame
                }
            }));

            const payload = {
                systemInstruction: {
                    parts: [{ text: systemInstruction }]
                },
                contents: [{
                    parts: [{ text: userQuery }, ...imageParts]
                }],
            };

            let response;
            let attempts = 0;
            const maxAttempts = 3;
            const initialDelay = 1000;

            while (attempts < maxAttempts) {
                try {
                    response = await fetch(url, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                        body: JSON.stringify(payload),
                    });

                    if (response.ok) {
                        const result = await response.json();
                        if (result.candidates && result.candidates[0].content.parts[0].text) {
                             return result.candidates[0].content.parts[0].text;
                        } else {
                            throw new Error('Invalid response structure from API.');
                        }
                    } else {
                        // Handle non-OK HTTP responses
                        const errorBody = await response.json();
                        console.error('API Error:', errorBody);
                        throw new Error(`API request failed with status ${response.status}: ${errorBody.error?.message || 'Unknown error'}`);
                    }
                } catch (error) {
                    attempts++;
                    if (attempts >= maxAttempts) {
                        throw error;
                    }
                    // Exponential backoff
                    await new Promise(res => setTimeout(res, initialDelay * Math.pow(2, attempts)));
                }
            }
        }
    })();

    // ========================================
    // TAB: PRODUCT REVIEW GENERATOR
    // ========================================
    (() => {
        const reviewImageInput = document.getElementById('review-image-input');
        const reviewImageUploadArea = document.getElementById('review-image-upload-area');
        const reviewImagePreviewContainer = document.getElementById('review-image-preview-container');

        const reviewModelImageInput = document.getElementById('review-model-image-input');
        const reviewModelImageUploadArea = document.getElementById('review-model-image-upload-area');
        const reviewModelImagePreviewContainer = document.getElementById('review-model-image-preview-container');
        const reviewModelImagePreview = document.getElementById('review-model-image-preview');
        const reviewRemoveModelImageBtn = document.getElementById('review-remove-model-image-btn');

        const reviewProductDescInput = document.getElementById('review-product-desc-input');
        const reviewGenerateBtn = document.getElementById('review-generate-btn');
        const reviewGrid = document.getElementById('review-b-roll-grid');
        const reviewGenerateDescBtn = document.getElementById('review-generate-desc-btn');

        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const reviewCountSel = document.getElementById('review-count-selection-grid');
        if (reviewCountSel) {
            reviewCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                reviewCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        let reviewProductImages = [];
        let reviewModelImageBase64 = null;
        let reviewModelImageMimeType = null;

        function updateReviewButtonState() {
            reviewGenerateBtn.disabled = reviewProductImages.length === 0 || !reviewProductDescInput.value.trim();
            reviewGenerateDescBtn.disabled = reviewProductImages.length === 0;
        }

        reviewImageInput.addEventListener('change', (event) => {
            handleReviewProductFiles(event.target.files);
            event.target.value = null;
        });

        function manageAddMoreButton() {
            const existingButton = document.getElementById('review-add-more-button');
            if (existingButton) {
                existingButton.remove();
            }

            const addButtonWrapper = document.createElement('div');
            addButtonWrapper.id = 'review-add-more-button';
            addButtonWrapper.innerHTML = `
                <label for="review-image-input" class="w-full h-24 flex flex-col items-center justify-center rounded-lg border-2 border-dashed border-gray-300 cursor-pointer text-gray-400 hover:bg-gray-100 hover:border-gray-400 transition-colors">
                     <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path></svg>
                </label>
            `;
            reviewImagePreviewContainer.appendChild(addButtonWrapper);
        }

        function handleReviewProductFiles(files) {
            if (files.length === 0) return;

            reviewImageUploadArea.classList.add('hidden');
            reviewImagePreviewContainer.classList.remove('hidden');

            let filesToProcess = files.length;

            Array.from(files).forEach(file => {
                const reader = new FileReader();
                reader.onload = (e) => {
                    const parts = e.target.result.split(',');
                    const mimeType = parts[0].match(/:(.*?);/)[1];
                    const base64 = parts[1];
                    const imageData = { base64, mimeType, id: Date.now() + Math.random() };
                    reviewProductImages.push(imageData);

                    const previewWrapper = document.createElement('div');
                    previewWrapper.className = 'relative group';
                    previewWrapper.innerHTML = `
                        <img src="${e.target.result}" alt="Pratinjau Produk" class="rounded-lg w-full h-24 object-cover">
                        <button data-id="${imageData.id}" class="absolute top-1 right-1 bg-red-500 text-white rounded-full p-1 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity focus:opacity-100">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                        </button>
                    `;

                    reviewImagePreviewContainer.appendChild(previewWrapper);

                    previewWrapper.querySelector('button').addEventListener('click', (btnEvent) => {
                        const idToRemove = parseFloat(btnEvent.currentTarget.getAttribute('data-id'));
                        reviewProductImages = reviewProductImages.filter(img => img.id !== idToRemove);
                        previewWrapper.remove();
                        if (reviewProductImages.length === 0) {
                            reviewImageUploadArea.classList.remove('hidden');
                            reviewImagePreviewContainer.classList.add('hidden');
                            const addButton = document.getElementById('review-add-more-button');
                            if (addButton) addButton.remove();
                        }
                        updateReviewButtonState();
                    });

                    updateReviewButtonState();

                    filesToProcess--;
                    if (filesToProcess === 0) {
                        manageAddMoreButton();
                    }
                };
                reader.readAsDataURL(file);
            });
        }

        function setupReviewSingleImageUpload(input, uploadArea, previewContainer, previewImg, removeBtn, onUpload) {
            input.addEventListener('change', (event) => {
                const file = event.target.files[0];
                if (file) {
                    const reader = new FileReader();
                    reader.onload = (e) => {
                        previewImg.src = e.target.result;
                        const parts = e.target.result.split(',');
                        const mimeType = parts[0].match(/:(.*?);/)[1];
                        const base64 = parts[1];
                        onUpload(base64, mimeType);

                        uploadArea.classList.add('hidden');
                        previewContainer.classList.remove('hidden');
                    };
                    reader.readAsDataURL(file);
                }
            });

            removeBtn.addEventListener('click', () => {
                onUpload(null, null);
                input.value = '';
                uploadArea.classList.remove('hidden');
                previewContainer.classList.add('hidden');
            });
        }

        setupReviewSingleImageUpload(reviewModelImageInput, reviewModelImageUploadArea, reviewModelImagePreviewContainer, reviewModelImagePreview, reviewRemoveModelImageBtn, (base64, mime) => {
            reviewModelImageBase64 = base64;
            reviewModelImageMimeType = mime;
        });

        reviewProductDescInput.addEventListener('input', updateReviewButtonState);

        const createInitialReviewPlaceholders = () => {
            reviewGrid.innerHTML = '';
            const selectedRatio = document.querySelector('.ratio-btn-review.selected')?.dataset.ratio || '16:9';
            let aspectRatioClass = 'aspect-video'; // default 16:9
            if (selectedRatio === '1:1') aspectRatioClass = 'aspect-square';
            else if (selectedRatio === '3:4') aspectRatioClass = 'aspect-[3/4]';
            else if (selectedRatio === '9:16') aspectRatioClass = 'aspect-[9/16]';

            for(let i = 1; i <= selectedCount; i++) {
                const card = document.createElement('div');
                card.className = 'result-card card p-4 flex flex-col justify-between animate-pulse';
                card.innerHTML = `
                    <div><div class="h-6 bg-gray-200 rounded w-3/4 mb-4"></div></div>
                    <div class="mt-4 ${aspectRatioClass} bg-gray-300 rounded-md flex items-center justify-center"></div>
                `;
                reviewGrid.appendChild(card);
            }
        };

        // Call on DOMContentLoaded instead of window load to avoid conflicts
        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', createInitialReviewPlaceholders);
        } else {
            createInitialReviewPlaceholders();
        }

        // Ratio button selection handler
        document.querySelectorAll('.ratio-btn-review').forEach(btn => {
            btn.addEventListener('click', function() {
                // Remove selected class from all buttons
                document.querySelectorAll('.ratio-btn-review').forEach(b => {
                    b.classList.remove('selected');
                    b.style.borderColor = '';
                    b.style.background = '';
                    b.style.color = '';
                    b.style.boxShadow = '';
                });
                // Add selected class to clicked button
                this.classList.add('selected');
                this.style.borderColor = '#6366f1';
                this.style.background = 'linear-gradient(135deg, #6366f1, #8b5cf6)';
                this.style.color = 'white';
                this.style.boxShadow = '0 4px 12px rgba(99, 102, 241, 0.3)';

                // Update placeholders
                createInitialReviewPlaceholders();
            });
        });

        reviewGenerateBtn.addEventListener('click', async () => {
            if (reviewProductImages.length === 0) return;
            reviewGenerateBtn.disabled = true;
            const originalBtnHTML = `<svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg><span>Buat Scene Review</span>`;
            reviewGenerateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Menganalisa...</span>`;
            reviewGrid.innerHTML = `<div class="col-span-1 sm:col-span-2 xl:col-span-3 text-center py-10">
                <div class="loader inline-block"></div>
                <p class="mt-4 text-gray-500">AI sedang menganalisis produk...</p>
            </div>`;

            let reviewIdeas;
            try {
                reviewIdeas = await analyzeAndGetPrompts();
            } catch (error) {
                console.error("Error analyzing product:", error);
                reviewGrid.innerHTML = `<div class="col-span-1 sm:col-span-2 xl:col-span-3 text-center py-10 text-red-500">
                    <p>Terjadi kesalahan saat menganalisis produk: ${error.message}</p>
                </div>`;
                reviewGenerateBtn.disabled = false;
                reviewGenerateBtn.innerHTML = originalBtnHTML;
                return;
            }

            reviewGenerateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Membuat Visual...</span>`;

            const MAX_ATTEMPTS = 3;
            let attempts = 0;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                createReviewDynamicCards(reviewIdeas);
                const generationPromises = reviewIdeas.map((idea, index) =>
                    generateSingleReview(index + 1, idea.title, idea.prompt)
                );
                await Promise.allSettled(generationPromises);
                successCount = reviewGrid.querySelectorAll('.result-card').length > 0
                    ? Array.from(reviewGrid.querySelectorAll('.result-card')).filter(c => c.querySelector('img')).length
                    : 0;
            }

            // Remove failed cards
            reviewGrid.querySelectorAll('.result-card').forEach(card => {
                if (!card.querySelector('img')) card.remove();
            });

            reviewGenerateBtn.disabled = false;
            reviewGenerateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            }
        });

        async function analyzeAndGetPrompts() {
            const apiKey = "";
            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
            const selectedRatio = document.querySelector('.ratio-btn-review.selected')?.dataset.ratio || '16:9';

            let systemPrompt = '';
            if (reviewModelImageBase64) {
                 systemPrompt = `You are an expert product reviewer and affiliate marketer. Your task is to analyze the product, description, and model to create a **${selectedCount}-part visual review story** (storyboard) for the aspect ratio ${selectedRatio}. The story should feel authentic, like a real user's experience.

**CRITICAL VISUAL RULES FOR MODEL CONSISTENCY:**
1.  **Studio/Setting Konsisten:** SELURUH review HARUS berlangsung di setting studio yang sama dan profesional (misalnya: di atas meja bersih, di depan backdrop solid color, setup minimalis). Ini adalah aturan kritis untuk SEMUA ${selectedCount} scene. Setiap scene harus berbagi background yang sama secara konsisten.
2.  **Model + Produk Bersama:** Untuk SEMUA ${selectedCount} scene, model manusia dan produk HARUS terlihat bersama dalam frame yang sama. Model harus secara aktif memegang, menggunakan, atau mendemonstrasikan produk. JANGAN generate close-up *hanya* produk atau shot *hanya* model.
3.  **KONSISTENSI MODEL SANGAT PENTING:** Model harus orang yang **SAMA PERSIS** di **SEMUA ${selectedCount} SCENE**. Gunakan deskripsi yang sangat spesifik dan konsisten untuk model berdasarkan foto yang diupload:
   - Gender, usia, ras/etnis HARUS identik di semua scene
   - Warna rambut, panjang rambut, gaya rambut HARUS identik di semua scene
   - Pakaian (warna, style) HARUS identik di semua scene
   - Fitur wajah (bentuk wajah, mata, hidung, bibir) HARUS konsisten
   - Seolah difilmkan dalam satu sesi berkelanjutan tanpa ganti baju atau gaya rambut
   - Di setiap prompt, SELALU describe model dengan detail yang sama persis

**STRUCTURE PRODUCT REVIEW:**
Create ${selectedCount} scenes forming a complete and sequential review narrative from start to finish. Ensure key stages are covered: Unboxing, Product Details, Features Demo, Usage, Results, and Final Verdict. Scenes must connect to form a coherent story.

Example 4 scenes: Unboxing → Feature Demo → Usage → Final Results
Example 7 scenes: Unboxing → Product Details → Features → Demo → Results → Comparison → Final Verdict
Example 10 scenes: Complete journey from Unboxing to detailed Long-term Results

For each scene, provide a short title (in Indonesian, e.g., 'Scene 1: Model Unboxing Produk') and a CONCISE English prompt for an AI image generator yang SELALU include deskripsi model yang konsisten.
Respond ONLY with a valid JSON array of ${selectedCount} objects, in sequential story order.`;
            } else {
                 systemPrompt = `You are an expert product reviewer. Your primary goal is to generate a **${selectedCount}-part visual review story** (storyboard) for the product for the aspect ratio ${selectedRatio}. **Absolutely no people, models, or human parts.** The review should focus on the product itself. **The entire review MUST take place in a single, professional studio setting (e.g., on a clean tabletop, against a seamless backdrop). Do not generate scenes in outdoor or other locations. This is a critical rule for ALL ${selectedCount} scenes. Every single scene must share the same consistent studio background.**

**STRUCTURE PRODUCT REVIEW (PRODUCT-ONLY):**
Create ${selectedCount} scenes forming a complete and sequential review narrative from start to finish. Ensure key stages: Product Showcase, Details, Features, Benefits, and Call to Action. Scenes must connect to form a coherent story.

Example 4 scenes: Showcase → Features → Benefits → Call to Action
Example 7 scenes: Showcase → Details → Features → Close-up → Benefits → Comparison → CTA
Example 10 scenes: Complete detailed journey showcasing all product aspects

You MUST ensure the ${selectedCount} scenes form a coherent, sequential narrative that showcases the product's features and benefits sequentially for a product review video.
For each concept, provide a short, elegant title in Indonesian (e.g., 'Bagian 1: Detail Produk') and a CONCISE English prompt for the AI image generator.
Respond ONLY with a valid JSON array of ${selectedCount} objects, in sequential story order.`;
            }

            const theme = document.getElementById('review-photo-theme-input').value.trim();
            let userQuery = `Analyze this product. Product Description: "${reviewProductDescInput.value.trim()}". Desired aspect ratio is ${selectedRatio}.`;
            if (theme) {
                userQuery += `\nPhoto Theme: "${theme}"`;
            }
            const parts = [{ text: userQuery }];
            reviewProductImages.forEach(img => {
                parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
            });
            if (reviewModelImageBase64) {
                parts.push({ inlineData: { mimeType: reviewModelImageMimeType, data: reviewModelImageBase64 } });
            }
            const payload = { contents: [{ parts: parts }], systemInstruction: { parts: [{ text: systemPrompt }] }, generationConfig: { responseMimeType: "application/json", responseSchema: { type: "ARRAY", items: { type: "OBJECT", properties: { "title": { "type": "STRING" }, "prompt": { "type": "STRING" } }, required: ["title", "prompt"] } } } };
            const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
            if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
            const result = await response.json();

            if (!result.candidates || !result.candidates[0].content || !result.candidates[0].content.parts || !result.candidates[0].content.parts[0].text) {
                throw new Error("Invalid response structure from API.");
            }

            let rawText = result.candidates[0].content.parts[0].text;
            rawText = rawText.replace(/```json/g, '').replace(/```/g, '').trim();
            const startIndex = rawText.indexOf('[');
            const endIndex = rawText.lastIndexOf(']');

            if (startIndex === -1 || endIndex === -1 || startIndex > endIndex) {
                throw new Error("Could not find a valid JSON array in the API response.");
            }

            const jsonText = rawText.substring(startIndex, endIndex + 1);

            try {
                return JSON.parse(jsonText);
            } catch (e) {
                console.error("Failed to parse JSON:", jsonText, e);
                throw new Error("API returned malformed JSON after cleaning.");
            }
        }

        async function generateSingleReview(id, title, prompt) {
            const card = document.getElementById(`review-card-${id}`);
            if (!card) return;
            const outputContainer = card.querySelector('.review-output-container');

            // Show loading state immediately so user knows something is happening
            outputContainer.innerHTML = `<div class="w-full h-full flex items-center justify-center"><div class="loader"></div></div>`;

            const retries = 3;
            let lastError = null;

            for (let i = 0; i < retries; i++) {
                try {
                    const apiKey = "";
                    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                    const selectedRatio = document.querySelector('.ratio-btn-review.selected')?.dataset.ratio || '16:9';

                    let finalPrompt = '';
                    if (reviewModelImageBase64) {
                        finalPrompt = `${prompt}, elegant, cinematic, professional product photography, dramatic lighting, 8k, photorealistic`;
                    } else {
                        finalPrompt = `${prompt}, professional commercial product photography, hyper-detailed, epic composition, cinematic lighting, no people, 8k, photorealistic`;
                    }

                    const parts = [{ text: finalPrompt }];

                    reviewProductImages.forEach(img => {
                        parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
                    });

                    if (reviewModelImageBase64) { parts.push({ inlineData: { mimeType: reviewModelImageMimeType, data: reviewModelImageBase64 } }); }

                    const payload = {
                        contents: [{ parts: parts }],
                        generationConfig: {
                            responseModalities: ['TEXT', 'IMAGE'],
                            imageConfig: { aspectRatio: selectedRatio }
                        },
                        safetySettings: [
                            { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" },
                            { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" },
                            { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" },
                            { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" }
                        ]
                    };

                    const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                    const result = await response.json();
                    const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                    if (!base64Data) throw new Error('No image data received');

                    if (base64Data) {
                        const imageUrl = `data:image/png;base64,${base64Data}`;
                        const safeTitle = title.replace(/[^a-z0-9]/gi, '_').toLowerCase();
                        outputContainer.innerHTML = `
                            <div class="relative w-full h-full group">
                                <img src="${imageUrl}" class="w-full h-full object-cover rounded-md" alt="Review Scene">
                                <div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/70 via-black/50 to-transparent p-4 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-all duration-300 ease-in-out rounded-b-md">
                                    <div class="flex flex-wrap gap-2 justify-end">
                                        <button data-action="review-preview" data-scene-id="${id}" class="action-btn bg-blue-500 text-white px-3 py-2 rounded-lg hover:bg-blue-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Preview">
                                            <i class="fas fa-search-plus pointer-events-none"></i>
                                            <span class="hidden sm:inline pointer-events-none">Preview</span>
                                        </button>
                                        <button data-action="review-edit-prompt" data-scene-id="${id}" class="action-btn bg-purple-500 text-white px-3 py-2 rounded-lg hover:bg-purple-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Edit Prompt">
                                            <i class="fas fa-edit pointer-events-none"></i>
                                            <span class="hidden sm:inline pointer-events-none">Edit</span>
                                        </button>
                                        <button data-action="review-regenerate" data-scene-id="${id}" class="action-btn bg-green-500 text-white px-3 py-2 rounded-lg hover:bg-green-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Regenerate">
                                            <i class="fas fa-sync-alt pointer-events-none"></i>
                                            <span class="hidden sm:inline pointer-events-none">Regenerate</span>
                                        </button>
                                        <button data-action="review-video-prompt" data-scene-id="${id}" class="action-btn bg-fuchsia-500 text-white px-3 py-2 rounded-lg hover:bg-fuchsia-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Buat Prompt Video">
                                            <i class="fas fa-film pointer-events-none"></i>
                                            <span class="hidden sm:inline pointer-events-none">Video</span>
                                        </button>
                                        <button data-action="review-caption" data-scene-id="${id}" class="action-btn bg-rose-500 text-white px-3 py-2 rounded-lg hover:bg-rose-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Buat Caption">
                                            <i class="fas fa-pencil-alt pointer-events-none"></i>
                                            <span class="hidden sm:inline pointer-events-none">Caption</span>
                                        </button>
                                        <button data-action="review-download" data-scene-id="${id}" data-filename="review_${id}_${safeTitle}.png" class="action-btn bg-cyan-600 text-white px-3 py-2 rounded-lg hover:bg-cyan-700 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Unduh Gambar">
                                            <i class="fas fa-download pointer-events-none"></i>
                                            <span class="hidden sm:inline pointer-events-none">Unduh</span>
                                        </button>
                                    </div>
                                </div>
                            </div>`;

                        // Update card's stored prompt
                        if (card) {
                            card.dataset.prompt = prompt;
                        }
                        return;
                    } else {
                         let reason = "Respon tidak valid dari API.";
                        if (result.promptFeedback && result.promptFeedback.blockReason) {
                            reason = `Diblokir: ${result.promptFeedback.blockReason}`;
                        } else if (result?.candidates?.[0]?.finishReason) {
                            reason = `Selesai dengan alasan: ${result.candidates[0].finishReason}`;
                        }
                        throw new Error(reason);
                    }
                } catch (error) {
                    lastError = error;
                    console.error(`Attempt ${i + 1} for card ${id} failed:`, error);
                    if (i < retries - 1) {
                        const delay = 1000 * Math.pow(2, i);
                        await new Promise(res => setTimeout(res, delay));
                    }
                }
            }

            if (lastError) {
                console.error(`All retries failed for card ${id}:`, lastError);
                outputContainer.innerHTML = '';
            }
        }

        const createReviewDynamicCards = (prompts) => {
            reviewGrid.innerHTML = '';
            const selectedRatio = document.querySelector('.ratio-btn-review.selected')?.dataset.ratio || '16:9';
            let aspectRatioClass = 'aspect-video'; // default 16:9
            if (selectedRatio === '1:1') aspectRatioClass = 'aspect-square';
            else if (selectedRatio === '3:4') aspectRatioClass = 'aspect-[3/4]';
            else if (selectedRatio === '9:16') aspectRatioClass = 'aspect-[9/16]';

            prompts.forEach((p, index) => {
                const card = document.createElement('div');
                card.id = `review-card-${index + 1}`;
                card.className = 'result-card card p-4 flex flex-col justify-between';

                card.dataset.title = p.title;
                card.dataset.prompt = p.prompt;

                card.innerHTML = `
                    <div class="mb-4">
                        <h3 class="text-lg font-semibold text-gray-800">${p.title}</h3>
                    </div>
                    <div class="review-output-container ${aspectRatioClass} bg-gray-100 rounded-md flex items-center justify-center">
                        <div class="loader"></div>
                    </div>
                `;
                reviewGrid.appendChild(card);
            });
        };

        reviewGenerateDescBtn.addEventListener('click', async () => {
            const originalButtonText = reviewGenerateDescBtn.innerHTML;
            reviewGenerateDescBtn.innerHTML = `<div class="loader !w-4 !h-4 !border-2"></div>`;
            reviewGenerateDescBtn.disabled = true;
            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `You are a professional affiliate reviewer. Analyze the product in the image and write a concise, authentic-sounding review intro in Indonesian. Highlight its key features from a user's perspective. Keep it under 500 characters.`;
                const parts = [ { text: "Buatkan deskripsi produk untuk gambar ini." }];
                if (reviewProductImages.length > 0) {
                     parts.push({ inlineData: { mimeType: reviewProductImages[0].mimeType, data: reviewProductImages[0].base64 } });
                }
                const payload = {
                    contents: [{ parts: parts }],
                    systemInstruction: { parts: [{ text: systemPrompt }] }
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
                const result = await response.json();
                const description = result.candidates[0].content.parts[0].text;
                reviewProductDescInput.value = description.trim();
                updateReviewButtonState();
            } catch(error) {
                console.error("Error generating description:", error);
                reviewProductDescInput.value = "Gagal membuat deskripsi. Coba lagi.";
            } finally {
                reviewGenerateDescBtn.innerHTML = originalButtonText;
                reviewGenerateDescBtn.disabled = false;
            }
        });

        window.handleReviewCaptionClick = async function(imageUrl, title) {
            try {
                // Tampilkan loading modal
                const modal = document.createElement('div');
                modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4 overflow-y-auto';
                modal.style.WebkitOverflowScrolling = 'touch';
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full my-8" onclick="event.stopPropagation()">
                        <h3 class="text-xl font-bold mb-4 text-gray-800">
                            <i class="fas fa-pencil-alt text-rose-500 mr-2"></i>Membuat Caption...
                        </h3>
                        <div class="flex items-center justify-center py-8">
                            <div class="loader !border-l-rose-500"></div>
                        </div>
                    </div>
                `;
                document.body.appendChild(modal);

                // Close modal on backdrop click
                modal.addEventListener('click', (e) => {
                    if (e.target === modal) {
                        modal.remove();
                    }
                });

                const parts = imageUrl.split(',');
                const mimeType = parts[0].match(/:(.*?);/)[1];
                const base64Data = parts[1];
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `You are a creative social media manager for an affiliate marketer. Based on the product photo, description, and theme, write an engaging review-style Instagram caption in Indonesian. It must sound authentic (like a real user's experience), be appealing, and end with 3-5 relevant hashtags and a subtle call-to-action (e.g., 'cek link di bio!').`;
                const userQuery = `Product Description: "${reviewProductDescInput.value.trim()}"\nPhoto Theme: "${title}"`;
                const payload = {
                    contents: [{ parts: [ { text: userQuery }, { inlineData: { mimeType: mimeType, data: base64Data } } ] }],
                    systemInstruction: { parts: [{ text: systemPrompt }] },
                    safetySettings: [
                        { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" }
                    ]
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
                const result = await response.json();
                const caption = result?.candidates?.[0]?.content?.parts?.[0]?.text;
                if (!caption) throw new Error('Respons kosong dari API. Coba lagi.');

                // Update modal dengan hasil caption
                const modalContent = modal.querySelector('.bg-white');
                modalContent.innerHTML = `
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-xl font-bold text-gray-800">
                            <i class="fas fa-pencil-alt text-rose-500 mr-2"></i>Caption Instagram
                        </h3>
                        <button data-action="close-modal" class="text-gray-500 hover:text-gray-700 text-2xl leading-none" title="Tutup">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>

                    <div class="max-h-[60vh] overflow-y-auto mb-4" style="-webkit-overflow-scrolling: touch;">
                        <div class="whitespace-pre-wrap bg-gray-100 p-4 rounded-lg text-gray-800 leading-relaxed">${caption.trim()}</div>
                    </div>

                    <div class="flex gap-2">
                        <button data-action="copy-caption" data-caption="${caption.trim().replace(/"/g, '&quot;')}"
                            class="flex-1 bg-rose-500 hover:bg-rose-600 text-white font-bold py-3 px-4 rounded-lg transition-colors">
                            <i class="fas fa-copy mr-2"></i>Salin Caption
                        </button>
                        <button data-action="close-modal"
                            class="bg-gray-500 hover:bg-gray-600 text-white font-bold py-3 px-4 rounded-lg transition-colors">
                            Tutup
                        </button>
                    </div>
                `;

                // Add event delegation for copy and close buttons (works on desktop & mobile)
                modalContent.addEventListener('click', async (e) => {
                    e.stopPropagation();

                    const copyBtn = e.target.closest('[data-action="copy-caption"]');
                    const closeBtn = e.target.closest('[data-action="close-modal"]');

                    if (copyBtn) {
                        const captionText = copyBtn.dataset.caption;
                        try {
                            await navigator.clipboard.writeText(captionText);
                            const originalHTML = copyBtn.innerHTML;
                            copyBtn.innerHTML = '<i class="fas fa-check mr-2"></i>Tersalin!';
                            copyBtn.classList.remove('bg-rose-500');
                            copyBtn.classList.add('bg-green-500');
                            setTimeout(() => {
                                copyBtn.innerHTML = originalHTML;
                                copyBtn.classList.remove('bg-green-500');
                                copyBtn.classList.add('bg-rose-500');
                            }, 2000);
                        } catch (err) {
                            console.error('Failed to copy:', err);
                            alert('Caption berhasil dipilih. Salin dengan Ctrl+C atau Command+C');
                        }
                    } else if (closeBtn) {
                        modal.remove();
                    }
                });
            } catch (error) {
                console.error("Error generating caption:", error);
                showModal('Error', `<p class="text-red-500">Gagal membuat caption. Silakan coba lagi.</p>`);
            }
        }

        window.handleReviewVideoPromptClick = async function(imageUrl, title, sceneIndex) {
            const sparkleEmoji = '\u2728'; // ✨
            showModal(sparkleEmoji + ' Prompt Video Review Berbicara', `<div class="flex flex-col items-center gap-4"><div class="loader"></div> <p class="text-gray-500">AI sedang membuat prompt video dengan dialog dalam Bahasa Indonesia...</p></div>`);
            try {
                const parts = imageUrl.split(',');
                const mimeType = parts[0].match(/:(.*?);/)[1];
                const base64Data = parts[1];
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const productDesc = document.getElementById('review-product-desc-input').value.trim();
                const photoTheme = document.getElementById('review-photo-theme-input').value.trim();

                const systemPrompt = `Kamu adalah ahli pembuatan prompt image-to-video khusus TALKING HEAD / SPEAKING VIDEO. User memiliki storyboard review produk dengan model yang BERBICARA menjelaskan produk. Kamu diberi *satu scene* dari ${selectedCount} scene total.

TUGAS KAMU: Buat prompt image-to-video dalam **BAHASA INDONESIA** yang SANGAT SPESIFIK agar model/reviewer terlihat SEDANG BERBICARA/menjelaskan produk sesuai scene ini.

**FORMAT PROMPT WAJIB:**
1. **Deskripsi Model Berbicara**: "[Deskripsi model] sedang berbicara ke kamera dengan [ekspresi], bibirnya bergerak natural..."
2. **Dialog/Isi Pembicaraan**: "Dia mengatakan: '[Dialog spesifik dalam Bahasa Indonesia tentang scene ini, 1-2 kalimat]'"
3. **Gestur & Aksi**: "sambil [gerakan tangan/gestur yang sesuai], [aksi dengan produk jika relevan]"
4. **Kamera**: "[gerakan kamera yang sesuai]"

**PENTING:**
- Dialog HARUS relevan dengan scene (unboxing, demo tekstur, hasil pemakaian, dll)
- Dialog HARUS dalam Bahasa Indonesia yang natural seperti content creator asli
- Include detail gerakan bibir dan ekspresi wajah saat bicara
- Gestur tangan harus mendukung pembicaraan (pointing, menunjuk produk, dll)
- Tone bicara sesuai scene (antusias, serius, excited, dll)
- JANGAN sebut nama tool/model video tertentu (Sora, Veo, Kling, dll) di dalam prompt maupun judulnya — prompt harus general, bisa dipakai di tool image-to-video apapun

OUTPUT: Satu prompt image-to-video lengkap dalam Bahasa Indonesia yang natural.`;

                let userQuery = `
                --- KONTEKS REVIEW ---
                Deskripsi Produk: "${productDesc}"
                Tema Visual: "${photoTheme || 'Tidak ada'}"
                Judul Scene: "${title}"
                Scene ke-${sceneIndex} dari ${selectedCount}

                --- TUGAS ---
                Analisa gambar ini dan buat prompt video LENGKAP dalam Bahasa Indonesia agar model terlihat SEDANG BERBICARA menjelaskan aspek produk yang sesuai dengan scene "${title}".

                Include:
                1. Dialog spesifik yang diucapkan model (relevan dengan "${title}")
                2. Detail ekspresi dan gerakan bibir saat bicara
                3. Gestur tangan yang natural
                4. Interaksi dengan produk (jika ada)
                5. Camera movement yang cinematic
                `;

                const payload = {
                    contents: [{ parts: [ { text: userQuery }, { inlineData: { mimeType: mimeType, data: base64Data } } ] }],
                    systemInstruction: { parts: [{ text: systemPrompt }] },
                    safetySettings: [
                        { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" }
                    ]
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
                const result = await response.json();
                const prompt = result?.candidates?.[0]?.content?.parts?.[0]?.text;
                if (!prompt) throw new Error('Respons kosong dari API. Coba lagi.');
                const content = `
                    <div class="text-sm text-gray-500 mb-2">Prompt Video Berbicara untuk Scene ${sceneIndex}/${selectedCount} :</div>
                    <div id="modal-text-content" class="whitespace-pre-wrap bg-gray-100 p-4 rounded-lg text-gray-800 mb-4 text-sm leading-relaxed border-l-4 border-indigo-500 shadow-sm max-h-[50vh] overflow-y-auto" style="-webkit-overflow-scrolling: touch;">${prompt.trim()}</div>
                    <div class="text-xs text-gray-500 mb-3 italic">💡 Prompt ini sudah include dialog berbahasa Indonesia dan detail gestur untuk talking head video</div>
                    <button id="copy-modal-btn" class="w-full font-bold py-3 px-4 rounded-lg flex items-center justify-center bg-gradient-to-r from-indigo-500 to-purple-600 hover:from-indigo-600 hover:to-purple-700 text-white shadow-lg active:scale-95 transition-all">
                        <i class="fas fa-copy mr-2 pointer-events-none"></i><span class="pointer-events-none">Salin Prompt ke Clipboard</span>
                    </button>
                `;
                const sparkleEmojiModal = '\u2728'; // ✨
                showModal(sparkleEmojiModal + ' Prompt Video Review Berbicara', content);
                const copyBtn = document.getElementById('copy-modal-btn');
                const handleCopy = (e) => {
                    e.preventDefault();
                    e.stopPropagation();
                    const text = document.getElementById('modal-text-content').innerText;
                    copyToClipboard(text, e.currentTarget);
                };
                copyBtn.addEventListener('click', handleCopy);
                copyBtn.addEventListener('touchend', (e) => {
                    e.preventDefault();
                    e.stopPropagation();
                    handleCopy(e);
                });
            } catch (error) {
                console.error("Error generating video prompt:", error);
                showModal('Error', `<p class="text-red-500">Gagal membuat prompt video. Silakan coba lagi.</p>`);
            }
        }

        window.handleReviewEditPromptClick = function(id, title, originalPrompt) {
            const content = `
                <p class="text-sm text-gray-500 mb-2">Edit prompt untuk scene review:</p>
                <h4 class="text-lg font-semibold text-gray-800 mb-3">${title}</h4>
                <textarea id="modal-edit-prompt-textarea" class="w-full h-32 p-3 bg-gray-100 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition" rows="5">${originalPrompt}</textarea>
                <button id="modal-regenerate-btn" class="w-full btn-primary font-bold py-2 px-4 rounded-lg flex items-center justify-center mt-4">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9M4 12v5h.582m15.356 2A8.001 8.001 0 004.582 17M20 12h-5" /></svg>
                    Regenerate
                </button>
            `;
            const editEmoji = '\u270F\uFE0F'; // ✏️
            showModal(editEmoji + ' Edit & Regenerate Prompt', content);

            document.getElementById('modal-regenerate-btn').addEventListener('click', () => {
                const newPrompt = document.getElementById('modal-edit-prompt-textarea').value;
                universalModal.classList.remove('visible');
                generateSingleReview(id, title, newPrompt);
            });
        }

        // Event delegation for action buttons in review results (works on desktop & mobile)
        reviewGrid.addEventListener('click', (e) => {
            e.stopPropagation();

            // Try to find the button - either clicked directly or child element
            const actionBtn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
            if (!actionBtn) return;

            const action = actionBtn.dataset.action;
            const sceneId = actionBtn.dataset.sceneId;
            const card = document.getElementById(`review-card-${sceneId}`);

            if (!card) {
                console.error('Card not found for scene ID:', sceneId);
                return;
            }

            const img = card.querySelector('.review-output-container img');
            const imageUrl = img ? img.src : '';
            const title = card.dataset.title || '';
            const originalPrompt = card.dataset.prompt || '';

            console.log('Action clicked:', action, 'Scene:', sceneId);

            if (action === 'review-preview') {
                if (!img) return;
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageUrl);
                }
            } else if (action === 'review-edit-prompt') {
                window.handleReviewEditPromptClick(parseInt(sceneId), title, originalPrompt);
            } else if (action === 'review-regenerate') {
                // Regenerate with same prompt
                generateSingleReview(parseInt(sceneId), title, originalPrompt);
            } else if (action === 'review-video-prompt') {
                if (!img) return;
                window.handleReviewVideoPromptClick(imageUrl, title, parseInt(sceneId));
            } else if (action === 'review-caption') {
                if (!img) return;
                window.handleReviewCaptionClick(imageUrl, title);
            } else if (action === 'review-download') {
                if (!img) {
                    console.error('Image not found for download');
                    return;
                }
                const filename = actionBtn.dataset.filename || `review_${sceneId}.png`;
                console.log('Downloading:', filename, imageUrl.substring(0, 50));
                if (window.downloadDataURINew) {
                    window.downloadDataURINew(imageUrl, filename);
                }
            }
        });
    })();

    // ========================================
    // TAB: SKINCARE PRODUCT REVIEW
    // ========================================
    (() => {
        const skincareImageInput = document.getElementById('skincare-image-input');
        const skincareImageUploadArea = document.getElementById('skincare-image-upload-area');
        const skincareImagePreviewContainer = document.getElementById('skincare-image-preview-container');

        const skincareModelImageInput = document.getElementById('skincare-model-image-input');
        const skincareModelImageUploadArea = document.getElementById('skincare-model-image-upload-area');
        const skincareModelImagePreviewContainer = document.getElementById('skincare-model-image-preview-container');
        const skincareModelImagePreview = document.getElementById('skincare-model-image-preview');
        const skincareRemoveModelImageBtn = document.getElementById('skincare-remove-model-image-btn');

        const skincareProductDescInput = document.getElementById('skincare-product-desc-input');
        const skincareGenerateBtn = document.getElementById('skincare-generate-btn');
        const skincareGrid = document.getElementById('skincare-b-roll-grid');
        const skincareGenerateDescBtn = document.getElementById('skincare-generate-desc-btn');

        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const skincareCountSel = document.getElementById('skincare-count-selection-grid');
        if (skincareCountSel) {
            skincareCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                skincareCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        let skincareProductImages = [];
        let skincareModelImageBase64 = null;
        let skincareModelImageMimeType = null;
        let selectedSkincareTheme = 'professional-commercial'; // Default theme

        // Theme descriptions for prompt generation
        const skincareThemeDescriptions = {
            'professional-commercial': {
                name: 'Professional Commercial',
                prompt: 'High-end commercial skincare advertisement style. Luxury product photography with elegant lighting, clean backgrounds, and premium aesthetic. Focus on product quality and brand appeal.',
                structure: 'Buat storyboard iklan komersial mewah: Hero Shot → Brand Story → Key Ingredients → Application Demo → Results → Premium Close-up → Call to Action'
            },
            'problem-solution': {
                name: 'Problem & Solution',
                prompt: 'Before-after transformation storytelling. Show skin problems being solved by the product. Dramatic comparison shots, close-up skin texture improvements, journey from problem to solution.',
                structure: 'Buat storyboard problem-solution: Skin Problem Close-up → Pain Point → Product Introduction → Application Process → Transformation → Before-After Comparison → Happy Result'
            },
            'honest-ugc': {
                name: 'Honest UGC Review',
                prompt: 'Authentic user-generated content style. Natural lighting, real-life settings, genuine expressions. Like a friend sharing their honest skincare experience on social media.',
                structure: 'Buat storyboard UGC review autentik: Unboxing Reaction → First Impressions → Texture Test → Real Application → Daily Use → Honest Results → Final Verdict'
            },
            'the-battle-vs': {
                name: 'The Battle (VS)',
                prompt: 'Product comparison/battle style content. Side-by-side comparison of multiple products. Split-screen shots, competitive analysis visuals, which product wins scenarios.',
                structure: 'Buat storyboard perbandingan produk: Products Intro Side-by-Side → Packaging Battle → Texture Comparison → Application Race → Ingredient Face-off → Performance Test → Winner Reveal'
            },
            'science-routine': {
                name: 'Science & Routine',
                prompt: 'Educational and scientific approach. Focus on ingredients, formulation, skincare routine steps. Lab-like aesthetics, ingredient close-ups, step-by-step tutorial style.',
                structure: 'Buat storyboard edukasi skincare: Ingredient Spotlight → Science Behind It → Routine Order → Application Technique → Layering Demo → Tips & Tricks → Complete Routine'
            },
            'aesthetic-asmr': {
                name: 'Aesthetic / ASMR',
                prompt: 'Visually satisfying and relaxing content. Smooth textures, satisfying product swatches, calming colors, ASMR-style close-ups. Focus on sensory pleasure and visual satisfaction.',
                structure: 'Buat storyboard aesthetic ASMR: Satisfying Unboxing → Texture ASMR → Product Swatch → Slow-Motion Application → Calming Routine → Relaxing Close-ups → Zen Finale'
            }
        };

        function updateSkincareButtonState() {
            skincareGenerateBtn.disabled = skincareProductImages.length === 0 || !skincareProductDescInput.value.trim();
            skincareGenerateDescBtn.disabled = skincareProductImages.length === 0;
        }

        // HEIC conversion function
        async function convertHeicToJpeg(file) {
            if (window.heic2any) {
                try {
                    const convertedBlob = await heic2any({
                        blob: file,
                        toType: 'image/jpeg',
                        quality: 0.9
                    });
                    return new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                        type: 'image/jpeg'
                    });
                } catch (err) {
                    console.error('HEIC conversion failed:', err);
                    throw new Error('Gagal mengkonversi gambar HEIC');
                }
            } else {
                throw new Error('HEIC converter not available');
            }
        }

        skincareImageInput.addEventListener('change', async (event) => {
            await handleSkincareProductFiles(event.target.files);
            event.target.value = null;
        });

        function manageSkincareAddMoreButton() {
            const existingButton = document.getElementById('skincare-add-more-button');
            if (existingButton) {
                existingButton.remove();
            }

            const addButtonWrapper = document.createElement('div');
            addButtonWrapper.id = 'skincare-add-more-button';
            addButtonWrapper.innerHTML = `
                <label for="skincare-image-input" class="w-full h-24 flex flex-col items-center justify-center rounded-lg border-2 border-dashed border-gray-300 cursor-pointer text-gray-400 hover:bg-gray-100 hover:border-gray-400 transition-colors">
                     <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path></svg>
                </label>
            `;
            skincareImagePreviewContainer.appendChild(addButtonWrapper);
        }

        async function handleSkincareProductFiles(files) {
            if (files.length === 0) return;

            skincareImageUploadArea.classList.add('hidden');
            skincareImagePreviewContainer.classList.remove('hidden');

            let filesToProcess = files.length;

            for (const file of Array.from(files)) {
                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        processedFile = await convertHeicToJpeg(file);
                    } catch (err) {
                        console.error('HEIC conversion error:', err);
                        alert('Gagal mengkonversi file HEIC: ' + file.name);
                        filesToProcess--;
                        continue;
                    }
                }

                const reader = new FileReader();
                reader.onload = (e) => {
                    const parts = e.target.result.split(',');
                    const mimeType = parts[0].match(/:(.*?);/)[1];
                    const base64 = parts[1];
                    const imageData = { base64, mimeType, id: Date.now() + Math.random() };
                    skincareProductImages.push(imageData);

                    const previewWrapper = document.createElement('div');
                    previewWrapper.className = 'relative group';
                    previewWrapper.innerHTML = `
                        <img src="${e.target.result}" alt="Pratinjau Produk" class="rounded-lg w-full h-24 object-cover">
                        <button data-id="${imageData.id}" class="absolute top-1 right-1 bg-red-500 text-white rounded-full p-1 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity focus:opacity-100">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                        </button>
                    `;

                    skincareImagePreviewContainer.appendChild(previewWrapper);

                    previewWrapper.querySelector('button').addEventListener('click', (btnEvent) => {
                        const idToRemove = parseFloat(btnEvent.currentTarget.getAttribute('data-id'));
                        skincareProductImages = skincareProductImages.filter(img => img.id !== idToRemove);
                        previewWrapper.remove();
                        if (skincareProductImages.length === 0) {
                            skincareImageUploadArea.classList.remove('hidden');
                            skincareImagePreviewContainer.classList.add('hidden');
                            const addButton = document.getElementById('skincare-add-more-button');
                            if (addButton) addButton.remove();
                        }
                        updateSkincareButtonState();
                    });

                    updateSkincareButtonState();

                    filesToProcess--;
                    if (filesToProcess === 0) {
                        manageSkincareAddMoreButton();
                    }
                };
                reader.readAsDataURL(processedFile);
            }
        }

        async function setupSkincareSingleImageUpload(input, uploadArea, previewContainer, previewImg, removeBtn, onUpload) {
            input.addEventListener('change', async (event) => {
                const file = event.target.files[0];
                if (file) {
                    let processedFile = file;

                    // Convert HEIC to JPEG if needed
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        try {
                            processedFile = await convertHeicToJpeg(file);
                        } catch (err) {
                            console.error('HEIC conversion error:', err);
                            alert('Gagal mengkonversi file HEIC');
                            return;
                        }
                    }

                    const reader = new FileReader();
                    reader.onload = (e) => {
                        previewImg.src = e.target.result;
                        const parts = e.target.result.split(',');
                        const mimeType = parts[0].match(/:(.*?);/)[1];
                        const base64 = parts[1];
                        onUpload(base64, mimeType);

                        uploadArea.classList.add('hidden');
                        previewContainer.classList.remove('hidden');
                    };
                    reader.readAsDataURL(processedFile);
                }
            });

            removeBtn.addEventListener('click', () => {
                onUpload(null, null);
                input.value = '';
                uploadArea.classList.remove('hidden');
                previewContainer.classList.add('hidden');
            });
        }

        setupSkincareSingleImageUpload(skincareModelImageInput, skincareModelImageUploadArea, skincareModelImagePreviewContainer, skincareModelImagePreview, skincareRemoveModelImageBtn, (base64, mime) => {
            skincareModelImageBase64 = base64;
            skincareModelImageMimeType = mime;
        });

        skincareProductDescInput.addEventListener('input', updateSkincareButtonState);

        const createInitialSkincarePlaceholders = () => {
            skincareGrid.innerHTML = '';
            const selectedRatio = document.querySelector('.ratio-btn-skincare.selected')?.dataset.ratio || '16:9';
            let aspectRatioClass = 'aspect-video'; // default 16:9
            if (selectedRatio === '1:1') aspectRatioClass = 'aspect-square';
            else if (selectedRatio === '3:4') aspectRatioClass = 'aspect-[3/4]';
            else if (selectedRatio === '9:16') aspectRatioClass = 'aspect-[9/16]';

            for(let i = 1; i <= selectedCount; i++) {
                const card = document.createElement('div');
                card.className = 'result-card card p-4 flex flex-col justify-between animate-pulse';
                card.innerHTML = `
                    <div><div class="h-6 bg-pink-100 rounded w-3/4 mb-4"></div></div>
                    <div class="mt-4 ${aspectRatioClass} bg-pink-200 rounded-md flex items-center justify-center"></div>
                `;
                skincareGrid.appendChild(card);
            }
        };

        // Call on DOMContentLoaded
        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', createInitialSkincarePlaceholders);
        } else {
            createInitialSkincarePlaceholders();
        }

        // Ratio button selection handler
        document.querySelectorAll('.ratio-btn-skincare').forEach(btn => {
            btn.addEventListener('click', function() {
                // Remove selected class from all buttons
                document.querySelectorAll('.ratio-btn-skincare').forEach(b => {
                    b.classList.remove('selected');
                    b.style.borderColor = '';
                    b.style.background = '';
                    b.style.color = '';
                    b.style.boxShadow = '';
                });
                // Add selected class to clicked button
                this.classList.add('selected');
                this.style.borderColor = '#ec4899';
                this.style.background = 'linear-gradient(135deg, #ec4899, #f43f5e)';
                this.style.color = 'white';
                this.style.boxShadow = '0 4px 12px rgba(236, 72, 153, 0.3)';

                // Update placeholders
                createInitialSkincarePlaceholders();
            });
        });

        // Theme button selection handler
        const skincareThemeSelection = document.getElementById('skincare-theme-selection');
        const skincareCustomThemeContainer = document.getElementById('skincare-custom-theme-container');

        if (skincareThemeSelection) {
            skincareThemeSelection.addEventListener('click', function(e) {
                const button = e.target.closest('.theme-btn-skincare');
                if (!button) return;

                // Remove selected state from all buttons
                document.querySelectorAll('.theme-btn-skincare').forEach(b => {
                    b.classList.remove('selected');
                    b.style.borderColor = '';
                    b.style.background = '';
                    b.style.color = '';
                    b.style.boxShadow = '';
                    // Restore icon colors
                    const icon = b.querySelector('i');
                    if (icon) icon.classList.add('text-pink-500');
                    const title = b.querySelector('.font-semibold');
                    if (title) title.classList.add('text-gray-700');
                    const desc = b.querySelector('.text-xs');
                    if (desc) desc.classList.add('text-gray-500');
                });

                // Add selected state to clicked button
                button.classList.add('selected');
                button.style.borderColor = '#ec4899';
                button.style.background = 'linear-gradient(135deg, #ec4899, #f43f5e)';
                button.style.color = 'white';
                button.style.boxShadow = '0 4px 12px rgba(236, 72, 153, 0.3)';
                // Change text colors for selected
                const icon = button.querySelector('i');
                if (icon) icon.classList.remove('text-pink-500');
                const title = button.querySelector('.font-semibold');
                if (title) title.classList.remove('text-gray-700');
                const desc = button.querySelector('.text-xs');
                if (desc) {
                    desc.classList.remove('text-gray-500');
                    desc.classList.add('opacity-80');
                }

                // Update selected theme
                selectedSkincareTheme = button.dataset.theme;

                // Show/hide custom theme textarea
                if (selectedSkincareTheme === 'custom') {
                    if (skincareCustomThemeContainer) skincareCustomThemeContainer.classList.remove('hidden');
                } else {
                    if (skincareCustomThemeContainer) skincareCustomThemeContainer.classList.add('hidden');
                }
            });
        }

        skincareGenerateBtn.addEventListener('click', async () => {
            if (skincareProductImages.length === 0) return;
            skincareGenerateBtn.disabled = true;
            const originalBtnHTML = `<svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg><span>Buat Scene Skincare Review</span>`;
            skincareGenerateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Menganalisa produk skincare...</span>`;
            skincareGrid.innerHTML = `<div class="col-span-1 sm:grid-cols-2 xl:col-span-3 text-center py-10">
                <div class="loader inline-block"></div>
                <p class="mt-4 text-gray-500">AI sedang menganalisis produk...</p>
            </div>`;

            let reviewIdeas;
            try {
                reviewIdeas = await analyzeAndGetSkincarePrompts();
            } catch (error) {
                console.error("Error analyzing product:", error);
                skincareGrid.innerHTML = `<div class="col-span-1 sm:col-span-2 xl:col-span-3 text-center py-10 text-red-500">
                    <p>Terjadi kesalahan saat menganalisis produk: ${error.message}</p>
                </div>`;
                skincareGenerateBtn.disabled = false;
                skincareGenerateBtn.innerHTML = originalBtnHTML;
                return;
            }

            skincareGenerateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Membuat Visual Skincare...</span>`;

            const MAX_ATTEMPTS = 3;
            let attempts = 0;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                createSkincareDynamicCards(reviewIdeas);
                const generationPromises = reviewIdeas.map((idea, index) =>
                    generateSingleSkincareReview(index + 1, idea.title, idea.prompt)
                );
                await Promise.allSettled(generationPromises);
                successCount = Array.from(skincareGrid.querySelectorAll('.result-card')).filter(c => c.querySelector('img')).length;
            }

            // Remove failed cards
            skincareGrid.querySelectorAll('.result-card').forEach(card => {
                if (!card.querySelector('img')) card.remove();
            });

            skincareGenerateBtn.disabled = false;
            skincareGenerateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            }
        });

        async function analyzeAndGetSkincarePrompts() {
            const apiKey = "";
            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
            const selectedRatio = document.querySelector('.ratio-btn-skincare.selected')?.dataset.ratio || '16:9';

            // Get theme-specific instructions
            const themeInfo = skincareThemeDescriptions[selectedSkincareTheme];
            const customThemeInput = document.getElementById('skincare-photo-theme-input').value.trim();

            let themePromptAddition = '';
            let themeStructure = '';

            if (selectedSkincareTheme === 'custom' && customThemeInput) {
                themePromptAddition = customThemeInput;
                themeStructure = `Buat ${selectedCount} scene berdasarkan tema custom user: "${customThemeInput}". Sesuaikan alur cerita dengan tema tersebut.`;
            } else if (themeInfo) {
                themePromptAddition = themeInfo.prompt;
                themeStructure = themeInfo.structure;
            }

            // Special handling for VS/Battle theme
            const isVsTheme = selectedSkincareTheme === 'the-battle-vs';
            const productCount = skincareProductImages.length;
            let vsInstructions = '';
            if (isVsTheme) {
                vsInstructions = `
**KHUSUS TEMA BATTLE/VS:**
- User mengupload ${productCount} produk skincare untuk dibandingkan
- WAJIB menampilkan SEMUA ${productCount} produk dalam scene perbandingan
- Gunakan split-screen, side-by-side comparison, atau VS layout
- Buat narasi kompetisi: mana yang lebih baik dalam aspek tertentu
- Scene harus menunjukkan perbandingan langsung: tekstur vs tekstur, hasil vs hasil, dll
- Highlight perbedaan dan keunggulan masing-masing produk`;
            }

            let systemPrompt = '';
            if (skincareModelImageBase64) {
                 systemPrompt = `Kamu adalah ahli review skincare dan content creator profesional. Tugas kamu adalah menganalisis produk skincare, deskripsi, dan model untuk membuat **${selectedCount}-part visual review story** (storyboard) khusus produk skincare dengan aspect ratio ${selectedRatio}.

**TEMA KONTEN: ${themeInfo?.name || 'Custom'}**
${themePromptAddition}
${vsInstructions}

**ATURAN VISUAL KRITIS UNTUK SKINCARE:**
1.  **Studio/Setting Konsisten:** SELURUH review HARUS berlangsung di setting studio yang sama dan profesional sesuai tema. Ini adalah aturan kritis untuk SEMUA ${selectedCount} scene.
2.  **Model + Produk Bersama:** Untuk SEMUA ${selectedCount} scene, model manusia dan produk skincare HARUS terlihat bersama dalam frame yang sama. Model harus secara aktif memegang, menggunakan, atau mendemonstrasikan produk.
3.  **Konsistensi Model:** Model harus orang yang **sama persis** di **SEMUA ${selectedCount} SCENE**, berdasarkan foto model yang diupload.

**STRUCTURE REVIEW SKINCARE:**
${themeStructure}

Untuk setiap scene, berikan judul singkat dalam Bahasa Indonesia dan prompt CONCISE dalam Bahasa Indonesia untuk AI image generator yang sesuai dengan tema "${themeInfo?.name || 'Custom'}".
Respond HANYA dengan valid JSON array dari ${selectedCount} objects, dalam urutan sequential story.`;
            } else {
                 systemPrompt = `Kamu adalah ahli review skincare profesional. Tujuan utama kamu adalah membuat **${selectedCount}-part visual review story** (storyboard) khusus produk skincare dengan aspect ratio ${selectedRatio}. **Tidak ada orang, model, atau bagian tubuh manusia sama sekali.**

**TEMA KONTEN: ${themeInfo?.name || 'Custom'}**
${themePromptAddition}
${vsInstructions}

**ATURAN VISUAL KRITIS UNTUK SKINCARE:**
**Setting Studio Konsisten:** SELURUH review HARUS berlangsung di satu setting studio profesional yang sama sesuai tema. Jangan generate scenes di outdoor atau lokasi lain kecuali sesuai tema.

**STRUCTURE REVIEW SKINCARE:**
${themeStructure}

Kamu HARUS memastikan ${selectedCount} scene membentuk narasi sequential yang koheren sesuai tema "${themeInfo?.name || 'Custom'}".
Untuk setiap konsep, berikan judul singkat dalam Bahasa Indonesia dan prompt CONCISE dalam Bahasa Indonesia untuk AI image generator.
Respond HANYA dengan valid JSON array dari ${selectedCount} objects, dalam urutan sequential story.`;
            }

            let userQuery = `Analisa produk skincare ini. Deskripsi Produk: "${skincareProductDescInput.value.trim()}". Aspect ratio: ${selectedRatio}. Tema: ${themeInfo?.name || customThemeInput || 'Professional Commercial'}.`;
            if (isVsTheme) {
                userQuery += `\nINI ADALAH KONTEN PERBANDINGAN/VS dengan ${productCount} produk. Buat scene yang membandingkan produk-produk ini secara langsung.`;
            }
            const parts = [{ text: userQuery }];
            skincareProductImages.forEach(img => {
                parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
            });
            if (skincareModelImageBase64) {
                parts.push({ inlineData: { mimeType: skincareModelImageMimeType, data: skincareModelImageBase64 } });
            }
            const payload = { contents: [{ parts: parts }], systemInstruction: { parts: [{ text: systemPrompt }] }, generationConfig: { responseMimeType: "application/json", responseSchema: { type: "ARRAY", items: { type: "OBJECT", properties: { "title": { "type": "STRING" }, "prompt": { "type": "STRING" } }, required: ["title", "prompt"] } } } };
            const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
            if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
            const result = await response.json();

            if (!result.candidates || !result.candidates[0].content || !result.candidates[0].content.parts || !result.candidates[0].content.parts[0].text) {
                throw new Error("Invalid response structure from API.");
            }

            let rawText = result.candidates[0].content.parts[0].text;
            rawText = rawText.replace(/```json/g, '').replace(/```/g, '').trim();
            const startIndex = rawText.indexOf('[');
            const endIndex = rawText.lastIndexOf(']');

            if (startIndex === -1 || endIndex === -1 || startIndex > endIndex) {
                throw new Error("Could not find a valid JSON array in the API response.");
            }

            const jsonText = rawText.substring(startIndex, endIndex + 1);

            try {
                return JSON.parse(jsonText);
            } catch (e) {
                console.error("Failed to parse JSON:", jsonText, e);
                throw new Error("API returned malformed JSON after cleaning.");
            }
        }

        async function generateSingleSkincareReview(id, title, prompt) {
            const card = document.getElementById(`skincare-card-${id}`);
            if (!card) return;
            const outputContainer = card.querySelector('.skincare-output-container');

            const retries = 3;
            let lastError = null;

            for (let i = 0; i < retries; i++) {
                try {
                    const apiKey = "";
                    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                    const selectedRatio = document.querySelector('.ratio-btn-skincare.selected')?.dataset.ratio || '16:9';

                    // Get theme-specific style additions
                    const themeInfo = skincareThemeDescriptions[selectedSkincareTheme];
                    let themeStyle = '';
                    if (selectedSkincareTheme === 'professional-commercial') {
                        themeStyle = 'luxury commercial advertisement, high-end brand aesthetic, premium lighting';
                    } else if (selectedSkincareTheme === 'problem-solution') {
                        themeStyle = 'before-after transformation style, dramatic comparison, skin improvement focus';
                    } else if (selectedSkincareTheme === 'honest-ugc') {
                        themeStyle = 'authentic user-generated content, natural lighting, real-life setting, genuine expression';
                    } else if (selectedSkincareTheme === 'the-battle-vs') {
                        themeStyle = 'product comparison battle, split-screen VS layout, side-by-side comparison';
                    } else if (selectedSkincareTheme === 'science-routine') {
                        themeStyle = 'educational scientific style, ingredient spotlight, lab-like aesthetic, tutorial format';
                    } else if (selectedSkincareTheme === 'aesthetic-asmr') {
                        themeStyle = 'satisfying ASMR aesthetic, smooth textures, calming colors, visual pleasure focus';
                    }

                    let finalPrompt = '';
                    if (skincareModelImageBase64) {
                        finalPrompt = `${prompt}, ${themeStyle}, skincare product photography, elegant, soft lighting beauty photography, professional, 8k, photorealistic, natural skin tone, clean aesthetic`;
                    } else {
                        finalPrompt = `${prompt}, ${themeStyle}, professional skincare product photography, commercial beauty shot, hyper-detailed, minimalist clean composition, soft natural lighting, no people, 8k, photorealistic`;
                    }

                    const parts = [{ text: finalPrompt }];

                    skincareProductImages.forEach(img => {
                        parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
                    });

                    if (skincareModelImageBase64) { parts.push({ inlineData: { mimeType: skincareModelImageMimeType, data: skincareModelImageBase64 } }); }

                    const payload = {
                        contents: [{ parts: parts }],
                        generationConfig: {
                            responseModalities: ['TEXT', 'IMAGE'],
                            imageConfig: { aspectRatio: selectedRatio }
                        },
                        safetySettings: [
                            { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" },
                            { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" },
                            { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" },
                            { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" }
                        ]
                    };

                    const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                    const result = await response.json();
                    const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                    if (!base64Data) throw new Error('No image data received');

                    if (base64Data) {
                        const imageUrl = `data:image/png;base64,${base64Data}`;
                        const safeTitle = title.replace(/[^a-z0-9]/gi, '_').toLowerCase();
                        outputContainer.innerHTML = `
                            <div class="relative w-full h-full group">
                                <img src="${imageUrl}" class="w-full h-full object-cover rounded-md" alt="Skincare Review Scene">
                                <div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/70 via-black/50 to-transparent p-4 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-all duration-300 ease-in-out rounded-b-md">
                                    <div class="flex flex-wrap gap-2 justify-end">
                                        <button data-action="skincare-preview" data-scene-id="${id}" class="action-btn bg-blue-500 text-white px-3 py-2 rounded-lg hover:bg-blue-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Preview">
                                            <i class="fas fa-search-plus pointer-events-none"></i>
                                            <span class="hidden sm:inline pointer-events-none">Preview</span>
                                        </button>
                                        <button data-action="skincare-edit-prompt" data-scene-id="${id}" class="action-btn bg-purple-500 text-white px-3 py-2 rounded-lg hover:bg-purple-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Edit Prompt">
                                            <i class="fas fa-edit pointer-events-none"></i>
                                            <span class="hidden sm:inline pointer-events-none">Edit</span>
                                        </button>
                                        <button data-action="skincare-regenerate" data-scene-id="${id}" class="action-btn bg-green-500 text-white px-3 py-2 rounded-lg hover:bg-green-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Regenerate">
                                            <i class="fas fa-sync-alt pointer-events-none"></i>
                                            <span class="hidden sm:inline pointer-events-none">Regenerate</span>
                                        </button>
                                        <button data-action="skincare-video-prompt" data-scene-id="${id}" class="action-btn bg-fuchsia-500 text-white px-3 py-2 rounded-lg hover:bg-fuchsia-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Prompt Image-to-Video">
                                            <i class="fas fa-film pointer-events-none"></i>
                                            <span class="hidden sm:inline pointer-events-none">Video</span>
                                        </button>
                                        <button data-action="skincare-caption" data-scene-id="${id}" class="action-btn bg-rose-500 text-white px-3 py-2 rounded-lg hover:bg-rose-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Caption IG">
                                            <i class="fas fa-pencil-alt pointer-events-none"></i>
                                            <span class="hidden sm:inline pointer-events-none">Caption</span>
                                        </button>
                                        <button data-action="skincare-download" data-scene-id="${id}" data-filename="skincare_review_${id}_${safeTitle}.png" class="action-btn bg-cyan-600 text-white px-3 py-2 rounded-lg hover:bg-cyan-700 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Unduh">
                                            <i class="fas fa-download pointer-events-none"></i>
                                            <span class="hidden sm:inline pointer-events-none">Unduh</span>
                                        </button>
                                    </div>
                                </div>
                            </div>`;

                        // Update card's stored prompt
                        if (card) {
                            card.dataset.prompt = prompt;
                        }
                        return;
                    } else {
                         let reason = "Respon tidak valid dari API.";
                        if (result.promptFeedback && result.promptFeedback.blockReason) {
                            reason = `Diblokir: ${result.promptFeedback.blockReason}`;
                        } else if (result?.candidates?.[0]?.finishReason) {
                            reason = `Selesai dengan alasan: ${result.candidates[0].finishReason}`;
                        }
                        throw new Error(reason);
                    }
                } catch (error) {
                    lastError = error;
                    console.error(`Attempt ${i + 1} for card ${id} failed:`, error);
                    if (i < retries - 1) {
                        const delay = 1000 * Math.pow(2, i);
                        await new Promise(res => setTimeout(res, delay));
                    }
                }
            }

            if (lastError) {
                console.error(`All retries failed for card ${id}:`, lastError);
                outputContainer.innerHTML = '';
            }
        }

        const createSkincareDynamicCards = (prompts) => {
            skincareGrid.innerHTML = '';
            const selectedRatio = document.querySelector('.ratio-btn-skincare.selected')?.dataset.ratio || '16:9';
            let aspectRatioClass = 'aspect-video'; // default 16:9
            if (selectedRatio === '1:1') aspectRatioClass = 'aspect-square';
            else if (selectedRatio === '3:4') aspectRatioClass = 'aspect-[3/4]';
            else if (selectedRatio === '9:16') aspectRatioClass = 'aspect-[9/16]';

            prompts.forEach((p, index) => {
                const card = document.createElement('div');
                card.id = `skincare-card-${index + 1}`;
                card.className = 'result-card card p-4 flex flex-col justify-between';

                card.dataset.title = p.title;
                card.dataset.prompt = p.prompt;

                card.innerHTML = `
                    <div class="mb-4">
                        <h3 class="text-lg font-semibold text-gray-800">${p.title}</h3>
                    </div>
                    <div class="skincare-output-container ${aspectRatioClass} bg-pink-50 rounded-md flex items-center justify-center">
                        <div class="loader"></div>
                    </div>
                `;
                skincareGrid.appendChild(card);
            });
        };

        skincareGenerateDescBtn.addEventListener('click', async () => {
            const originalButtonText = skincareGenerateDescBtn.innerHTML;
            skincareGenerateDescBtn.innerHTML = `<div class="loader !w-4 !h-4 !border-2"></div>`;
            skincareGenerateDescBtn.disabled = true;
            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `Kamu adalah seorang beauty enthusiast dan skincare reviewer profesional. Analisa produk skincare di gambar dan buatkan deskripsi singkat tapi autentik dalam Bahasa Indonesia. Highlight fitur utama, bahan aktif, manfaat, dan tekstur dari perspektif user. Maksimal 500 karakter.`;
                const parts = [ { text: "Buatkan deskripsi produk skincare untuk gambar ini dalam Bahasa Indonesia." }];
                if (skincareProductImages.length > 0) {
                     parts.push({ inlineData: { mimeType: skincareProductImages[0].mimeType, data: skincareProductImages[0].base64 } });
                }
                const payload = {
                    contents: [{ parts: parts }],
                    systemInstruction: { parts: [{ text: systemPrompt }] }
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
                const result = await response.json();
                const description = result.candidates[0].content.parts[0].text;
                skincareProductDescInput.value = description.trim();
                updateSkincareButtonState();
            } catch(error) {
                console.error("Error generating description:", error);
                skincareProductDescInput.value = "Gagal membuat deskripsi. Coba lagi.";
            } finally {
                skincareGenerateDescBtn.innerHTML = originalButtonText;
                skincareGenerateDescBtn.disabled = false;
            }
        });

        window.handleSkincareCaptionClick = async function(imageUrl, title) {
            try {
                const modal = document.createElement('div');
                modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4 overflow-y-auto';
                modal.style.WebkitOverflowScrolling = 'touch';
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full my-8" onclick="event.stopPropagation()">
                        <h3 class="text-xl font-bold mb-4 text-gray-800">
                            <i class="fas fa-pencil-alt text-rose-500 mr-2"></i>Membuat Caption Skincare...
                        </h3>
                        <div class="flex items-center justify-center py-8">
                            <div class="loader !border-l-rose-500"></div>
                        </div>
                    </div>
                `;
                document.body.appendChild(modal);

                modal.addEventListener('click', (e) => {
                    if (e.target === modal) {
                        modal.remove();
                    }
                });

                const parts = imageUrl.split(',');
                const mimeType = parts[0].match(/:(.*?);/)[1];
                const base64Data = parts[1];
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `Kamu adalah content creator skincare profesional yang bikin caption Instagram engaging untuk review produk skincare. Berdasarkan foto produk skincare, deskripsi, dan tema, buatkan caption review-style Instagram dalam Bahasa Indonesia yang autentik (seperti pengalaman user asli), menarik, informatif tentang manfaat produk, dan diakhiri dengan 5-7 hashtag skincare relevan plus soft call-to-action (contoh: 'cek di bio untuk link pembelian!', 'swipe untuk info lengkap!')`;
                const userQuery = `Deskripsi Produk Skincare: "${skincareProductDescInput.value.trim()}"\nTema Visual: "${title}"`;
                const payload = {
                    contents: [{ parts: [ { text: userQuery }, { inlineData: { mimeType: mimeType, data: base64Data } } ] }],
                    systemInstruction: { parts: [{ text: systemPrompt }] },
                    safetySettings: [
                        { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" }
                    ]
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const caption = result?.candidates?.[0]?.content?.parts?.[0]?.text;
                if (!caption) throw new Error('Respons kosong dari API. Coba lagi.');

                const modalContent = modal.querySelector('.bg-white');
                modalContent.innerHTML = `
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-xl font-bold text-gray-800">
                            <i class="fas fa-pencil-alt text-rose-500 mr-2"></i>Caption Instagram Skincare
                        </h3>
                        <button data-action="close-modal" class="text-gray-500 hover:text-gray-700 text-2xl leading-none" title="Tutup">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>

                    <div class="max-h-[60vh] overflow-y-auto mb-4" style="-webkit-overflow-scrolling: touch;">
                        <div class="whitespace-pre-wrap bg-gray-100 p-4 rounded-lg text-gray-800 leading-relaxed">${caption.trim()}</div>
                    </div>

                    <div class="flex gap-2">
                        <button data-action="copy-caption" data-caption="${caption.trim().replace(/"/g, '&quot;')}"
                            class="flex-1 bg-rose-500 hover:bg-rose-600 text-white font-bold py-3 px-4 rounded-lg transition-colors">
                            <i class="fas fa-copy mr-2"></i>Salin Caption
                        </button>
                        <button data-action="close-modal"
                            class="bg-gray-500 hover:bg-gray-600 text-white font-bold py-3 px-4 rounded-lg transition-colors">
                            Tutup
                        </button>
                    </div>
                `;

                modalContent.addEventListener('click', async (e) => {
                    e.stopPropagation();

                    const copyBtn = e.target.closest('[data-action="copy-caption"]');
                    const closeBtn = e.target.closest('[data-action="close-modal"]');

                    if (copyBtn) {
                        const captionText = copyBtn.dataset.caption;
                        try {
                            await navigator.clipboard.writeText(captionText);
                            const originalHTML = copyBtn.innerHTML;
                            copyBtn.innerHTML = '<i class="fas fa-check mr-2"></i>Tersalin!';
                            copyBtn.classList.remove('bg-rose-500');
                            copyBtn.classList.add('bg-green-500');
                            setTimeout(() => {
                                copyBtn.innerHTML = originalHTML;
                                copyBtn.classList.remove('bg-green-500');
                                copyBtn.classList.add('bg-rose-500');
                            }, 2000);
                        } catch (err) {
                            console.error('Failed to copy:', err);
                            alert('Caption berhasil dipilih. Salin dengan Ctrl+C atau Command+C');
                        }
                    } else if (closeBtn) {
                        modal.remove();
                    }
                });
            } catch (error) {
                console.error("Error generating caption:", error);
                showModal('Error', `<p class="text-red-500">Gagal membuat caption. Silakan coba lagi.</p>`);
            }
        }

        window.handleSkincareVideoPromptClick = async function(imageUrl, title, sceneIndex) {
            const sparkleEmoji = '\u2728'; // ✨
            showModal(sparkleEmoji + ' Prompt Video Skincare Berbicara', `<div class="flex flex-col items-center gap-4"><div class="loader"></div> <p class="text-gray-500">AI sedang membuat prompt video dengan dialog dalam Bahasa Indonesia...</p></div>`);
            try {
                const parts = imageUrl.split(',');
                const mimeType = parts[0].match(/:(.*?);/)[1];
                const base64Data = parts[1];
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const productDesc = document.getElementById('skincare-product-desc-input').value.trim();
                const photoTheme = document.getElementById('skincare-photo-theme-input').value.trim();

                const systemPrompt = `Kamu adalah ahli pembuatan prompt image-to-video khusus TALKING HEAD / SPEAKING VIDEO untuk skincare review. User memiliki storyboard review skincare dengan beauty influencer yang BERBICARA menjelaskan produk skincare. Kamu diberi *satu scene* dari ${selectedCount} scene total.

TUGAS KAMU: Buat prompt image-to-video dalam **BAHASA INDONESIA** yang SANGAT SPESIFIK agar beauty reviewer terlihat SEDANG BERBICARA/menjelaskan produk skincare sesuai scene ini.

**FORMAT PROMPT WAJIB:**
1. **Deskripsi Reviewer Berbicara**: "[Deskripsi reviewer] sedang berbicara ke kamera dengan [ekspresi], bibirnya bergerak natural..."
2. **Dialog/Isi Pembicaraan**: "Dia mengatakan: '[Dialog spesifik dalam Bahasa Indonesia tentang skincare scene ini, 1-2 kalimat]'"
3. **Gestur & Aksi Skincare**: "sambil [gerakan tangan/gestur], [aksi dengan produk skincare: mengoleskan, menunjukkan tekstur, memijat wajah, dll]"
4. **Kamera**: "[gerakan kamera yang sesuai]"

**PENTING UNTUK SKINCARE REVIEW:**
- Dialog HARUS relevan dengan skincare scene (unboxing, demo tekstur, aplikasi, before-after, hasil, dll)
- Dialog HARUS dalam Bahasa Indonesia yang natural seperti beauty influencer asli
- Include detail gerakan bibir dan ekspresi wajah saat bicara
- Gestur tangan harus mendukung pembicaraan + interaksi dengan produk skincare
- Tone bicara sesuai scene skincare (excited untuk tekstur, serius untuk ingredients, antusias untuk hasil, dll)
- Highlight sentuhan kulit, aplikasi produk, atau hasil di wajah jika relevan
- JANGAN sebut nama tool/model video tertentu (Sora, Veo, Kling, dll) di dalam prompt maupun judulnya — prompt harus general, bisa dipakai di tool image-to-video apapun

OUTPUT: Satu prompt image-to-video lengkap dalam Bahasa Indonesia yang natural.`;

                let userQuery = `
                --- KONTEKS SKINCARE REVIEW ---
                Deskripsi Produk Skincare: "${productDesc}"
                Tema Visual: "${photoTheme || 'Tidak ada'}"
                Judul Scene: "${title}"
                Scene ke-${sceneIndex} dari ${selectedCount}

                --- TUGAS ---
                Analisa gambar ini dan buat prompt video LENGKAP dalam Bahasa Indonesia agar beauty reviewer terlihat SEDANG BERBICARA menjelaskan aspek skincare yang sesuai dengan scene "${title}".

                Include:
                1. Dialog spesifik tentang skincare yang diucapkan reviewer (relevan dengan "${title}")
                2. Detail ekspresi dan gerakan bibir saat bicara
                3. Gestur tangan yang natural + interaksi dengan produk skincare
                4. Detail aplikasi skincare di wajah/tangan jika ada
                5. Camera movement yang cinematic untuk beauty content
                `;

                const payload = {
                    contents: [{ parts: [ { text: userQuery }, { inlineData: { mimeType: mimeType, data: base64Data } } ] }],
                    systemInstruction: { parts: [{ text: systemPrompt }] },
                    safetySettings: [
                        { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" }
                    ]
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
                const result = await response.json();
                const prompt = result?.candidates?.[0]?.content?.parts?.[0]?.text;
                if (!prompt) throw new Error('Respons kosong dari API. Coba lagi.');
                const content = `
                    <div class="text-sm text-gray-500 mb-2">Prompt Video Berbicara untuk Scene ${sceneIndex}/${selectedCount} :</div>
                    <div id="modal-text-content" class="whitespace-pre-wrap bg-gray-100 p-4 rounded-lg text-gray-800 mb-4 text-sm leading-relaxed border-l-4 border-pink-500 shadow-sm max-h-[50vh] overflow-y-auto" style="-webkit-overflow-scrolling: touch;">${prompt.trim()}</div>
                    <div class="text-xs text-gray-500 mb-3 italic">💡 Prompt ini sudah include dialog berbahasa Indonesia dan detail gestur untuk talking head skincare video</div>
                    <button id="copy-modal-btn" class="w-full font-bold py-3 px-4 rounded-lg flex items-center justify-center bg-gradient-to-r from-pink-500 to-rose-600 hover:from-pink-600 hover:to-rose-700 text-white shadow-lg active:scale-95 transition-all">
                        <i class="fas fa-copy mr-2 pointer-events-none"></i><span class="pointer-events-none">Salin Prompt ke Clipboard</span>
                    </button>
                `;
                const sparkleEmojiModal = '\u2728'; // ✨
                showModal(sparkleEmojiModal + ' Prompt Video Skincare Berbicara', content);
                const copyBtn = document.getElementById('copy-modal-btn');
                const handleCopy = (e) => {
                    e.preventDefault();
                    e.stopPropagation();
                    const text = document.getElementById('modal-text-content').innerText;
                    copyToClipboard(text, e.currentTarget);
                };
                copyBtn.addEventListener('click', handleCopy);
                copyBtn.addEventListener('touchend', (e) => {
                    e.preventDefault();
                    e.stopPropagation();
                    handleCopy(e);
                });
            } catch (error) {
                console.error("Error generating video prompt:", error);
                showModal('Error', `<p class="text-red-500">Gagal membuat prompt video. Silakan coba lagi.</p>`);
            }
        }

        window.handleSkincareEditPromptClick = function(id, title, originalPrompt) {
            const content = `
                <p class="text-sm text-gray-500 mb-2">Edit prompt untuk scene skincare review:</p>
                <h4 class="text-lg font-semibold text-gray-800 mb-3">${title}</h4>
                <textarea id="modal-edit-prompt-textarea" class="w-full h-32 p-3 bg-gray-100 border border-gray-300 rounded-lg focus:ring-2 focus:ring-pink-500 focus:border-pink-500 transition" rows="5">${originalPrompt}</textarea>
                <button id="modal-regenerate-btn" class="w-full btn-primary font-bold py-2 px-4 rounded-lg flex items-center justify-center mt-4 bg-gradient-to-r from-pink-500 to-rose-600 hover:from-pink-600 hover:to-rose-700">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9M4 12v5h.582m15.356 2A8.001 8.001 0 004.582 17M20 12h-5" /></svg>
                    Regenerate
                </button>
            `;
            const editEmoji = '\u270F\uFE0F'; // ✏️
            showModal(editEmoji + ' Edit & Regenerate Prompt', content);

            document.getElementById('modal-regenerate-btn').addEventListener('click', () => {
                const newPrompt = document.getElementById('modal-edit-prompt-textarea').value;
                universalModal.classList.remove('visible');
                generateSingleSkincareReview(id, title, newPrompt);
            });
        }

        // Event delegation for action buttons (works on desktop & mobile)
        skincareGrid.addEventListener('click', (e) => {
            e.stopPropagation();

            const actionBtn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
            if (!actionBtn) return;

            const action = actionBtn.dataset.action;
            const sceneId = actionBtn.dataset.sceneId;
            const card = document.getElementById(`skincare-card-${sceneId}`);

            if (!card) {
                console.error('Card not found for scene ID:', sceneId);
                return;
            }

            const img = card.querySelector('.skincare-output-container img');
            const imageUrl = img ? img.src : '';
            const title = card.dataset.title || '';
            const originalPrompt = card.dataset.prompt || '';

            console.log('Action clicked:', action, 'Scene:', sceneId);

            if (action === 'skincare-preview') {
                if (!img) return;
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageUrl);
                }
            } else if (action === 'skincare-edit-prompt') {
                window.handleSkincareEditPromptClick(parseInt(sceneId), title, originalPrompt);
            } else if (action === 'skincare-regenerate') {
                generateSingleSkincareReview(parseInt(sceneId), title, originalPrompt);
            } else if (action === 'skincare-video-prompt') {
                if (!img) return;
                window.handleSkincareVideoPromptClick(imageUrl, title, parseInt(sceneId));
            } else if (action === 'skincare-caption') {
                if (!img) return;
                window.handleSkincareCaptionClick(imageUrl, title);
            } else if (action === 'skincare-download') {
                if (!img) {
                    console.error('Image not found for download');
                    return;
                }
                const filename = actionBtn.dataset.filename || `skincare_review_${sceneId}.png`;
                console.log('Downloading:', filename, imageUrl.substring(0, 50));
                if (window.downloadDataURINew) {
                    window.downloadDataURINew(imageUrl, filename);
                }
            }
        });
    })();

    // ========================================
    // TAB: FOOD REVIEW GENERATOR
    // ========================================
    (() => {
        const foodImageInput = document.getElementById('food-image-input');
        const foodImageUploadArea = document.getElementById('food-image-upload-area');
        const foodImagePreviewContainer = document.getElementById('food-image-preview-container');

        const foodModelImageInput = document.getElementById('food-model-image-input');
        const foodModelImageUploadArea = document.getElementById('food-model-image-upload-area');
        const foodModelImagePreviewContainer = document.getElementById('food-model-image-preview-container');
        const foodModelImagePreview = document.getElementById('food-model-image-preview');
        const foodRemoveModelImageBtn = document.getElementById('food-remove-model-image-btn');

        const foodProductDescInput = document.getElementById('food-product-desc-input');
        const foodGenerateBtn = document.getElementById('food-generate-btn');
        const foodGrid = document.getElementById('food-grid');
        const foodGenerateDescBtn = document.getElementById('food-generate-desc-btn');
        const foodResultsSection = document.getElementById('food-results-section');

        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const foodCountSel = document.getElementById('food-count-selection-grid');
        if (foodCountSel) {
            foodCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                foodCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        let foodProductImages = [];
        let foodModelImageBase64 = null;
        let foodModelImageMimeType = null;

        function updateFoodButtonState() {
            foodGenerateBtn.disabled = foodProductImages.length === 0 || !foodProductDescInput.value.trim();
            foodGenerateDescBtn.disabled = foodProductImages.length === 0;
        }

        foodImageInput.addEventListener('change', (event) => {
            handleFoodProductFiles(event.target.files);
            event.target.value = null;
        });

        foodImageUploadArea.addEventListener('click', () => {
            foodImageInput.click();
        });

        foodModelImageUploadArea.addEventListener('click', () => {
            foodModelImageInput.click();
        });

        function manageFoodAddMoreButton() {
            const existingButton = document.getElementById('food-add-more-button');
            if (existingButton) existingButton.remove();
            if (foodProductImages.length >= 5) return;

            const addButtonWrapper = document.createElement('div');
            addButtonWrapper.id = 'food-add-more-button';
            addButtonWrapper.innerHTML = `<label for="food-image-input" class="w-full h-24 flex flex-col items-center justify-center rounded-lg border-2 border-dashed border-gray-300 cursor-pointer text-gray-400 hover:bg-orange-50 hover:border-orange-400 transition-colors"><svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path></svg></label>`;
            foodImagePreviewContainer.appendChild(addButtonWrapper);
        }

        async function handleFoodProductFiles(files) {
            if (files.length === 0) return;
            if (foodProductImages.length + files.length > 5) { alert('Maksimal 5 foto makanan'); return; }
            foodImageUploadArea.classList.add('hidden');
            foodImagePreviewContainer.classList.remove('hidden');

            for (let file of files) {
                let processedFile = file;
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        continue;
                    }
                }

                const reader = new FileReader();
                reader.onload = (e) => {
                    const parts = e.target.result.split(',');
                    const mimeType = parts[0].match(/:(.*?);/)[1];
                    const base64 = parts[1];
                    const imageData = { base64, mimeType, id: Date.now() + Math.random() };
                    foodProductImages.push(imageData);

                    const previewWrapper = document.createElement('div');
                    previewWrapper.className = 'relative group';
                    previewWrapper.innerHTML = `<img src="${e.target.result}" alt="Pratinjau Makanan" class="rounded-lg w-full h-24 object-cover"><button data-id="${imageData.id}" class="absolute top-1 right-1 bg-red-500 text-white rounded-full p-1 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity focus:opacity-100"><svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg></button>`;

                    const removeButton = document.getElementById('food-add-more-button');
                    if (removeButton) { foodImagePreviewContainer.insertBefore(previewWrapper, removeButton); } else { foodImagePreviewContainer.appendChild(previewWrapper); }

                    previewWrapper.querySelector('button').addEventListener('click', (btnEvent) => {
                        const idToRemove = parseFloat(btnEvent.currentTarget.getAttribute('data-id'));
                        foodProductImages = foodProductImages.filter(img => img.id !== idToRemove);
                        previewWrapper.remove();
                        if (foodProductImages.length === 0) {
                            foodImageUploadArea.classList.remove('hidden');
                            foodImagePreviewContainer.classList.add('hidden');
                            const addButton = document.getElementById('food-add-more-button');
                            if (addButton) addButton.remove();
                        }
                        manageFoodAddMoreButton();
                        updateFoodButtonState();
                    });

                    updateFoodButtonState();
                    manageFoodAddMoreButton();
                };
                reader.readAsDataURL(processedFile);
            }
        }

        foodModelImageInput.addEventListener('change', async (event) => {
            const file = event.target.files[0];
            if (file) {
                let processedFile = file;
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onload = (e) => {
                    foodModelImagePreview.src = e.target.result;
                    const parts = e.target.result.split(',');
                    const mimeType = parts[0].match(/:(.*?);/)[1];
                    const base64 = parts[1];
                    foodModelImageBase64 = base64;
                    foodModelImageMimeType = mimeType;
                    foodModelImageUploadArea.classList.add('hidden');
                    foodModelImagePreviewContainer.classList.remove('hidden');
                };
                reader.readAsDataURL(processedFile);
            }
        });

        foodRemoveModelImageBtn.addEventListener('click', () => {
            foodModelImageBase64 = null;
            foodModelImageMimeType = null;
            foodModelImageInput.value = '';
            foodModelImageUploadArea.classList.remove('hidden');
            foodModelImagePreviewContainer.classList.add('hidden');
        });

        foodProductDescInput.addEventListener('input', updateFoodButtonState);

        const createInitialFoodPlaceholders = () => {
            foodGrid.innerHTML = '';
            const selectedRatio = document.querySelector('.ratio-btn-food.selected')?.dataset.ratio || '16:9';
            const foodAspectMap = { '1:1': 'aspect-square', '4:5': 'aspect-[4/5]', '9:16': 'aspect-[9/16]', '3:4': 'aspect-[3/4]', '2:3': 'aspect-[2/3]', '16:9': 'aspect-video', '3:2': 'aspect-[3/2]', '4:3': 'aspect-[4/3]', '21:9': 'aspect-[21/9]', '5:4': 'aspect-[5/4]' };
            let aspectRatioClass = foodAspectMap[selectedRatio] || 'aspect-video';

            for(let i = 1; i <= selectedCount; i++) {
                const card = document.createElement('div');
                card.className = 'result-card card p-4 flex flex-col justify-between animate-pulse';
                card.innerHTML = `<div><div class="h-6 bg-gray-200 rounded w-3/4 mb-4"></div></div><div class="mt-4 ${aspectRatioClass} bg-gray-300 rounded-md flex items-center justify-center"></div>`;
                foodGrid.appendChild(card);
            }
        };

        document.querySelectorAll('.ratio-btn-food').forEach(btn => {
            btn.addEventListener('click', function() {
                document.querySelectorAll('.ratio-btn-food').forEach(b => {
                    b.classList.remove('selected');
                    b.style.borderColor = '';
                    b.style.background = '';
                    b.style.color = '';
                    b.style.boxShadow = '';
                });
                this.classList.add('selected');
                this.style.borderColor = '#f97316';
                this.style.background = 'linear-gradient(135deg, #f97316, #ea580c)';
                this.style.color = 'white';
                this.style.boxShadow = '0 4px 12px rgba(249, 115, 22, 0.3)';
                createInitialFoodPlaceholders();
            });
        });

        // Preset tema visual - klik chip untuk isi input tema
        const foodThemePresets = document.getElementById('food-theme-presets');
        if (foodThemePresets) {
            foodThemePresets.addEventListener('click', (e) => {
                const chip = e.target.closest('.food-theme-preset');
                if (!chip) return;
                const themeInputEl = document.getElementById('food-photo-theme-input');
                if (chip.classList.contains('selected')) {
                    chip.classList.remove('selected');
                    if (themeInputEl) themeInputEl.value = '';
                } else {
                    foodThemePresets.querySelectorAll('.food-theme-preset').forEach(c => c.classList.remove('selected'));
                    chip.classList.add('selected');
                    if (themeInputEl) themeInputEl.value = chip.dataset.preset;
                }
            });
        }

        foodGenerateBtn.addEventListener('click', async () => {
            if (foodProductImages.length === 0) return;
            foodGenerateBtn.disabled = true;
            const originalBtnHTML = `<svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg><span>Buat Scene Food Review</span>`;
            foodGenerateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Menganalisa makanan...</span>`;
            const frEmptyState = document.getElementById('food-review-empty-state');
            if (frEmptyState) frEmptyState.classList.add('hidden');
            foodResultsSection.classList.remove('hidden');
            foodResultsSection.scrollIntoView({ behavior: 'smooth', block: 'nearest' });

            let reviewIdeas;
            try {
                reviewIdeas = await analyzeAndGetFoodPrompts();
            } catch (error) {
                console.error("Error analyzing food:", error);
                foodGrid.innerHTML = `<div class="col-span-1 sm:col-span-2 xl:col-span-3 text-center py-10 text-red-500"><p>Terjadi kesalahan: ${error.message}</p></div>`;
                foodGenerateBtn.disabled = false;
                foodGenerateBtn.innerHTML = originalBtnHTML;
                return;
            }

            foodGenerateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Membuat Visual Food Review...</span>`;

            const MAX_ATTEMPTS = 3;
            let attempts = 0;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                createFoodDynamicCards(reviewIdeas);
                const generationPromises = reviewIdeas.map((idea, index) => generateSingleFoodReview(index + 1, idea.title, idea.prompt));
                await Promise.allSettled(generationPromises);
                successCount = Array.from(foodGrid.querySelectorAll('.result-card')).filter(c => c.querySelector('img')).length;
            }

            // Remove failed cards
            foodGrid.querySelectorAll('.result-card').forEach(card => {
                if (!card.querySelector('img')) card.remove();
            });

            foodGenerateBtn.disabled = false;
            foodGenerateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                foodResultsSection.classList.add('hidden');
                if (frEmptyState) frEmptyState.classList.remove('hidden');
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            }
        });

        async function analyzeAndGetFoodPrompts() {
            const apiKey = "";
            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
            const selectedRatio = document.querySelector('.ratio-btn-food.selected')?.dataset.ratio || '16:9';
            let systemPrompt = '';
            if (foodModelImageBase64) {
                systemPrompt = `Kamu adalah ahli food review dan content creator kuliner profesional seperti TikToker/Selebgram. Tugas kamu adalah menganalisis makanan dan food reviewer untuk membuat **${selectedCount}-part visual food review story** (storyboard) khusus review makanan dengan aspect ratio ${selectedRatio}.

**ATURAN VISUAL KRITIS UNTUK FOOD REVIEW:**
1. **Studio/Setting Konsisten:** SELURUH review HARUS berlangsung di setting yang sama (cafe, restaurant, street food stall, kitchen, dll). Ini adalah aturan kritis untuk SEMUA ${selectedCount} scene. Background harus konsisten.
2. **Reviewer + Makanan Bersama:** Untuk SEMUA ${selectedCount} scene, food reviewer dan makanan HARUS terlihat bersama dalam frame. Reviewer harus interaksi dengan makanan (memegang, mencicipi, menunjukkan, dll). JANGAN generate close-up *hanya* makanan atau shot *hanya* reviewer.
3. **Konsistensi Reviewer:** Food reviewer harus orang yang **sama persis** di **SEMUA ${selectedCount} SCENE**. Pakaian dan gaya rambut harus tetap identik di semua scene, seolah difilmkan dalam satu sesi berkelanjutan.

**STRUCTURE FOOD REVIEW:**
Buat ${selectedCount} scene yang membentuk narasi food review lengkap dari awal sampai akhir. Scene harus nyambung membentuk cerita koheren.

Contoh 4 scene: Opening & Food Reveal → Close-up Details → Taste Test → Final Verdict
Contoh 7 scene: Opening → Unboxing/Plating → Texture Demo → First Bite → Taste Analysis → Ambience/Price → Final Rating
Contoh 10 scene: Complete food review dari Opening sampai Final Recommendation dengan semua tahapan lengkap (appearance, aroma, taste, texture, value, comparison)

Kamu HARUS membuat ${selectedCount} scene sequential yang cover: opening, food presentation, close-up details, taste test, analysis, dan verdict. Scene harus flow natural seperti content creator sungguhan.

**KUALITAS VISUAL PROFESIONAL (WAJIB di setiap prompt):**
- Sebutkan angle kamera spesifik yang BERBEDA tiap scene (contoh: eye-level medium shot, over-the-shoulder, 45-degree hero angle, macro close-up on texture, top-down)
- Sebutkan teknik lighting yang konsisten dengan setting (contoh: warm cafe window light, street food bulb lighting at dusk, soft ring light)
- Sebutkan detail yang bikin makanan menggugah selera: uap mengepul untuk makanan panas, tekstur renyah/lumer terlihat, saus meleleh, kondensasi di minuman dingin
- Ekspresi reviewer harus autentik dan berbeda tiap scene (penasaran, terkejut enak, menikmati, mikir menilai, puas merekomendasikan)

For each scene, provide:
- title: Judul scene dalam Bahasa Indonesia (contoh: "Scene 1: Opening & First Impression")
- prompt: CONCISE English prompt for AI image generator yang SELALU include deskripsi reviewer konsisten + food interaction + camera angle + lighting

Respond ONLY with valid JSON array of ${selectedCount} objects.`;
            } else {
                systemPrompt = `Kamu adalah ahli food photography dan content creator kuliner. Tugas kamu adalah menganalisis makanan untuk membuat **${selectedCount}-part visual food review story** (storyboard) fokus pada makanan dengan aspect ratio ${selectedRatio}. **TANPA reviewer/orang.**

**STRUCTURE FOOD REVIEW (NO PEOPLE):**
Buat ${selectedCount} scene yang membentuk narasi food presentation lengkap. Scene harus nyambung showing different angles and aspects of the food.

Contoh 4 scene: Full Plate Shot → Close-up Texture → Ingredients Detail → Plating Art
Contoh 7 scene: Establishing Shot → Top View → Side Angle → Macro Texture → Cross-section → Garnish Detail → Final Presentation
Contoh 10 scene: Complete food showcase dari various angles, lighting, details (colors, textures, steam, garnishes, etc.)

**KUALITAS VISUAL PROFESIONAL (WAJIB di setiap prompt):**
- Angle kamera BERBEDA tiap scene (top-down flat lay, 45-degree hero, macro texture, side profile showing layers, wide establishing)
- Teknik lighting profesional yang konsisten (window light, moody low-key, golden hour, high-key studio)
- Detail menggugah selera: uap, tekstur, saus mengalir, garnish segar, kondensasi
- Props pendukung yang cocok dengan makanan dan setting (jangan mencuri fokus)

For each scene, provide:
- title: Judul scene dalam Bahasa Indonesia
- prompt: CONCISE English prompt for professional food photography with camera angle + lighting, NO PEOPLE

Respond ONLY with valid JSON array of ${selectedCount} objects.`;
            }
            const theme = document.getElementById('food-photo-theme-input').value.trim();
            let userQuery = `Analyze this food. Food Description: "${foodProductDescInput.value.trim()}". Desired aspect ratio is ${selectedRatio}.`;
            if (theme) userQuery += `\nPhoto Theme/Ambience: "${theme}"`;
            const parts = [{ text: userQuery }];
            foodProductImages.forEach(img => { parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } }); });
            if (foodModelImageBase64) { parts.push({ inlineData: { mimeType: foodModelImageMimeType, data: foodModelImageBase64 } }); }
            const payload = { contents: [{ parts: parts }], systemInstruction: { parts: [{ text: systemPrompt }] }, generationConfig: { responseMimeType: "application/json", responseSchema: { type: "ARRAY", items: { type: "OBJECT", properties: { "title": { "type": "STRING" }, "prompt": { "type": "STRING" } }, required: ["title", "prompt"] } } } };
            const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
            if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
            const result = await response.json();
            if (!result.candidates?.[0]?.content?.parts?.[0]?.text) { throw new Error("Invalid response structure from API."); }
            let rawText = result.candidates[0].content.parts[0].text;
            rawText = rawText.replace(/```json/g, '').replace(/```/g, '').trim();
            const startIndex = rawText.indexOf('[');
            const endIndex = rawText.lastIndexOf(']');
            if (startIndex === -1 || endIndex === -1 || startIndex > endIndex) { throw new Error("Could not find a valid JSON array in the API response."); }
            const jsonText = rawText.substring(startIndex, endIndex + 1);
            try { return JSON.parse(jsonText); } catch (e) { console.error("Failed to parse JSON:", jsonText, e); throw new Error("API returned malformed JSON after cleaning."); }
        }

        async function generateSingleFoodReview(id, title, prompt) {
            const card = document.getElementById(`food-card-${id}`);
            if (!card) return;
            const outputContainer = card.querySelector('.food-output-container');
            const selectedRatio = document.querySelector('.ratio-btn-food.selected')?.dataset.ratio || '16:9';
            const foodAspectMap = { '1:1': 'aspect-square', '4:5': 'aspect-[4/5]', '9:16': 'aspect-[9/16]', '3:4': 'aspect-[3/4]', '2:3': 'aspect-[2/3]', '16:9': 'aspect-video', '3:2': 'aspect-[3/2]', '4:3': 'aspect-[4/3]', '21:9': 'aspect-[21/9]', '5:4': 'aspect-[5/4]' };
            let aspectRatioClass = foodAspectMap[selectedRatio] || 'aspect-video';
            outputContainer.innerHTML = `<div class="${aspectRatioClass} bg-orange-50 rounded-md flex items-center justify-center"><div class="loader"></div></div>`;
            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                let finalPrompt = '';
                if (foodModelImageBase64) { finalPrompt = `${prompt}. The reviewer must be the EXACT same person from the reference photo (same face, hair, outfit across all scenes). The food must stay identical to the uploaded food photo. Shot like a professional food content creator video still: appetizing styling with visible texture detail, gentle steam for hot dishes, realistic skin texture, sharp focus on food and face, commercial-grade lighting, 8k photorealistic`; } else { finalPrompt = `${prompt}. The food must stay identical to the uploaded food photo (same dish, portion, plating). Professional commercial food photography: mouthwatering presentation with visible texture detail, gentle steam or fresh condensation where appropriate, styled props that complement without stealing focus, controlled highlights and soft shadows, no people, 8k photorealistic, culinary magazine quality`; }
                const parts = [{ text: finalPrompt }];
                foodProductImages.forEach(img => { parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } }); });
                if (foodModelImageBase64) { parts.push({ inlineData: { mimeType: foodModelImageMimeType, data: foodModelImageBase64 } }); }
                const payload = { contents: [{ parts: parts }], generationConfig: { responseModalities: ['TEXT', 'IMAGE'], imageConfig: { aspectRatio: selectedRatio } }, safetySettings: [ { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" }, { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" }, { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" }, { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" } ] };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data received');
                const imageUrl = `data:image/png;base64,${base64Data}`;
                const safeTitle = title.replace(/[^a-z0-9]/gi, '_').toLowerCase();
                outputContainer.innerHTML = `<div class="relative w-full h-full group"><img src="${imageUrl}" class="w-full h-full object-cover rounded-md" alt="Food Review Scene"><div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/70 via-black/50 to-transparent p-4 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-all duration-300 ease-in-out rounded-b-md"><div class="flex flex-wrap gap-2 justify-end"><button data-action="food-preview" data-scene-id="${id}" class="action-btn bg-blue-500 text-white px-3 py-2 rounded-lg hover:bg-blue-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Preview"><i class="fas fa-search-plus pointer-events-none"></i><span class="hidden sm:inline pointer-events-none">Preview</span></button><button data-action="food-edit-prompt" data-scene-id="${id}" class="action-btn bg-purple-500 text-white px-3 py-2 rounded-lg hover:bg-purple-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Edit Prompt"><i class="fas fa-edit pointer-events-none"></i><span class="hidden sm:inline pointer-events-none">Edit</span></button><button data-action="food-regenerate" data-scene-id="${id}" class="action-btn bg-green-500 text-white px-3 py-2 rounded-lg hover:bg-green-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Regenerate"><i class="fas fa-sync-alt pointer-events-none"></i><span class="hidden sm:inline pointer-events-none">Regenerate</span></button><button data-action="food-video-prompt" data-scene-id="${id}" class="action-btn bg-fuchsia-500 text-white px-3 py-2 rounded-lg hover:bg-fuchsia-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Prompt Image-to-Video"><i class="fas fa-film pointer-events-none"></i><span class="hidden sm:inline pointer-events-none">Video</span></button><button data-action="food-caption" data-scene-id="${id}" class="action-btn bg-rose-500 text-white px-3 py-2 rounded-lg hover:bg-rose-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Caption IG"><i class="fas fa-pencil-alt pointer-events-none"></i><span class="hidden sm:inline pointer-events-none">Caption</span></button><button data-action="food-download" data-scene-id="${id}" data-filename="food_review_${id}_${safeTitle}.png" class="action-btn bg-cyan-600 text-white px-3 py-2 rounded-lg hover:bg-cyan-700 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Unduh"><i class="fas fa-download pointer-events-none"></i><span class="hidden sm:inline pointer-events-none">Unduh</span></button></div></div></div>`;
                if (card) { card.dataset.prompt = prompt; }
            } catch (error) {
                console.error(`Error generating food card ${id}:`, error);
                outputContainer.innerHTML = '';
            }
        }

        const createFoodDynamicCards = (prompts) => {
            foodGrid.innerHTML = '';
            const selectedRatio = document.querySelector('.ratio-btn-food.selected')?.dataset.ratio || '16:9';
            const foodAspectMap = { '1:1': 'aspect-square', '4:5': 'aspect-[4/5]', '9:16': 'aspect-[9/16]', '3:4': 'aspect-[3/4]', '2:3': 'aspect-[2/3]', '16:9': 'aspect-video', '3:2': 'aspect-[3/2]', '4:3': 'aspect-[4/3]', '21:9': 'aspect-[21/9]', '5:4': 'aspect-[5/4]' };
            let aspectRatioClass = foodAspectMap[selectedRatio] || 'aspect-video';
            prompts.forEach((p, index) => {
                const card = document.createElement('div');
                card.id = `food-card-${index + 1}`;
                card.className = 'result-card card p-4 flex flex-col justify-between';
                card.dataset.title = p.title;
                card.dataset.prompt = p.prompt;
                card.innerHTML = `<div class="mb-4"><h3 class="text-lg font-semibold text-gray-800">${p.title}</h3></div><div class="food-output-container ${aspectRatioClass} bg-orange-50 rounded-md flex items-center justify-center"><div class="loader"></div></div>`;
                foodGrid.appendChild(card);
            });
        };

        foodGenerateDescBtn.addEventListener('click', async () => {
            const originalButtonText = foodGenerateDescBtn.innerHTML;
            foodGenerateDescBtn.innerHTML = `<div class="loader !w-4 !h-4 !border-2"></div>`;
            foodGenerateDescBtn.disabled = true;
            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `You are a professional food writer. Analyze the food in the image and write a concise, appetizing description in Indonesian. Include: nama makanan, rasa, tekstur, bahan utama, dan kesan keseluruhan. Keep it under 300 characters.`;
                const parts = [ { text: "Buatkan deskripsi makanan untuk gambar ini." }];
                if (foodProductImages.length > 0) { parts.push({ inlineData: { mimeType: foodProductImages[0].mimeType, data: foodProductImages[0].base64 } }); }
                const payload = { contents: [{ parts: parts }], systemInstruction: { parts: [{ text: systemPrompt }] } };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || response.statusText); }
                const result = await response.json();
                const description = result?.candidates?.[0]?.content?.parts?.[0]?.text || "Gagal generate deskripsi";
                foodProductDescInput.value = description.trim();
                updateFoodButtonState();
            } catch (error) {
                console.error("Error generating description:", error);
                alert('Gagal generate deskripsi. Silakan coba lagi.');
            } finally {
                foodGenerateDescBtn.innerHTML = originalButtonText;
                foodGenerateDescBtn.disabled = false;
            }
        });

        window.handleFoodCaptionClick = async function(imageUrl, title) {
            const sparkleEmoji = '\u2728';
            showModal(sparkleEmoji + ' Caption Instagram', `<div class="flex flex-col items-center gap-4"><div class="loader"></div> <p class="text-gray-500">AI sedang membuat caption...</p></div>`);
            try {
                const parts = imageUrl.split(',');
                const mimeType = parts[0].match(/:(.*?);/)[1];
                const base64Data = parts[1];
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const foodDesc = document.getElementById('food-product-desc-input').value.trim();
                const systemPrompt = `Kamu adalah social media expert khusus food content. Buat caption Instagram untuk food review yang engaging, natural, dan relatable. Include: hook menarik, deskripsi makanan yang appetizing, call-to-action, dan hashtags relevan. Output dalam Bahasa Indonesia yang casual tapi profesional.`;
                let userQuery = `Buat caption Instagram untuk food review scene: "${title}"\nFood Description: "${foodDesc}"\n\nFormat:\n- Opening hook yang catchy\n- Review singkat yang appetizing\n- Emoji yang sesuai\n- Hashtags relevan (#kuliner #foodreview dll)`;
                const payload = { contents: [{ parts: [ { text: userQuery }, { inlineData: { mimeType: mimeType, data: base64Data } } ] }], systemInstruction: { parts: [{ text: systemPrompt }] } };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
                const result = await response.json();
                const caption = result.candidates[0].content.parts[0].text;
                const modal = document.getElementById('universal-modal');
                const modalContent = modal.querySelector('.modal-content-inner');
                modalContent.innerHTML = `<div class="max-h-[60vh] overflow-y-auto mb-4" style="-webkit-overflow-scrolling: touch;"><div class="whitespace-pre-wrap bg-gray-100 p-4 rounded-lg text-gray-800 leading-relaxed">${caption.trim()}</div></div><div class="flex gap-2"><button data-action="copy-caption" data-caption="${caption.trim().replace(/"/g, '&quot;')}" class="flex-1 bg-orange-500 hover:bg-orange-600 text-white font-bold py-3 px-4 rounded-lg transition-colors"><i class="fas fa-copy mr-2"></i>Salin Caption</button><button data-action="close-modal" class="bg-gray-500 hover:bg-gray-600 text-white font-bold py-3 px-4 rounded-lg transition-colors">Tutup</button></div>`;
                modalContent.addEventListener('click', async (e) => {
                    e.stopPropagation();
                    const copyBtn = e.target.closest('[data-action="copy-caption"]');
                    const closeBtn = e.target.closest('[data-action="close-modal"]');
                    if (copyBtn) {
                        const captionText = copyBtn.dataset.caption;
                        try {
                            await navigator.clipboard.writeText(captionText);
                            const originalHTML = copyBtn.innerHTML;
                            copyBtn.innerHTML = '<i class="fas fa-check mr-2"></i>Tersalin!';
                            copyBtn.classList.remove('bg-orange-500');
                            copyBtn.classList.add('bg-green-500');
                            setTimeout(() => {
                                copyBtn.innerHTML = originalHTML;
                                copyBtn.classList.remove('bg-green-500');
                                copyBtn.classList.add('bg-orange-500');
                            }, 2000);
                        } catch (err) {
                            console.error('Failed to copy:', err);
                            alert('Caption berhasil dipilih. Salin dengan Ctrl+C atau Command+C');
                        }
                    } else if (closeBtn) {
                        modal.remove();
                    }
                });
            } catch (error) {
                console.error("Error generating caption:", error);
                showModal('Error', `<p class="text-red-500">Gagal membuat caption. Silakan coba lagi.</p>`);
            }
        }

        window.handleFoodVideoPromptClick = async function(imageUrl, title, sceneIndex) {
            const sparkleEmoji = '\u2728';
            showModal(sparkleEmoji + ' Prompt Video Food Review Berbicara', `<div class="flex flex-col items-center gap-4"><div class="loader"></div> <p class="text-gray-500">AI sedang membuat prompt video dengan dialog dalam Bahasa Indonesia...</p></div>`);
            try {
                const parts = imageUrl.split(',');
                const mimeType = parts[0].match(/:(.*?);/)[1];
                const base64Data = parts[1];
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const foodDesc = document.getElementById('food-product-desc-input').value.trim();
                const photoTheme = document.getElementById('food-photo-theme-input').value.trim();
                const systemPrompt = `Kamu adalah ahli pembuatan prompt image-to-video khusus TALKING HEAD / SPEAKING VIDEO untuk food review. User memiliki storyboard food review dengan food reviewer yang BERBICARA menjelaskan makanan. Kamu diberi *satu scene* dari ${selectedCount} scene total.

TUGAS KAMU: Buat prompt image-to-video dalam **BAHASA INDONESIA** yang SANGAT SPESIFIK agar food reviewer terlihat SEDANG BERBICARA/menjelaskan makanan sesuai scene ini.

**FORMAT PROMPT WAJIB:**
1. **Deskripsi Reviewer Berbicara**: "[Deskripsi reviewer] sedang berbicara ke kamera dengan [ekspresi], bibirnya bergerak natural..."
2. **Dialog/Isi Pembicaraan**: "Dia mengatakan: '[Dialog spesifik dalam Bahasa Indonesia tentang food scene ini, 1-2 kalimat]'"
3. **Gestur & Aksi Food**: "sambil [gerakan tangan/gestur], [aksi dengan makanan: memegang, mencicipi, menunjukkan, dll]"
4. **Kamera**: "[gerakan kamera yang sesuai]"

**PENTING UNTUK FOOD REVIEW:**
- Dialog HARUS relevan dengan food scene (opening, close-up, taste test, verdict, dll)
- Dialog HARUS dalam Bahasa Indonesia yang natural seperti food vlogger/TikToker asli
- Include detail gerakan bibir dan ekspresi wajah saat bicara
- Gestur tangan harus mendukung pembicaraan + interaksi dengan makanan
- Tone bicara sesuai scene food (excited untuk first bite, analytical untuk taste, satisfied untuk verdict, dll)
- Highlight interaksi dengan makanan: memegang piring, mencicipi, menunjukkan tekstur, dll
- JANGAN sebut nama tool/model video tertentu (Sora, Veo, Kling, dll) di dalam prompt maupun judulnya — prompt harus general, bisa dipakai di tool image-to-video apapun

OUTPUT: Satu prompt image-to-video lengkap dalam Bahasa Indonesia yang natural.`;
                let userQuery = `--- KONTEKS FOOD REVIEW ---\nDeskripsi Makanan: "${foodDesc}"\nTema Visual: "${photoTheme || 'Tidak ada'}"\nJudul Scene: "${title}"\nScene ke-${sceneIndex} dari ${selectedCount}\n\n--- TUGAS ---\nAnalisa gambar ini dan buat prompt video LENGKAP dalam Bahasa Indonesia agar food reviewer terlihat SEDANG BERBICARA menjelaskan aspek makanan yang sesuai dengan scene "${title}".\n\nInclude:\n1. Dialog spesifik tentang makanan yang diucapkan reviewer (relevan dengan "${title}")\n2. Detail ekspresi dan gerakan bibir saat bicara\n3. Gestur tangan yang natural + interaksi dengan makanan\n4. Detail tasting atau holding makanan jika ada\n5. Camera movement yang cinematic untuk food content`;
                const payload = { contents: [{ parts: [ { text: userQuery }, { inlineData: { mimeType: mimeType, data: base64Data } } ] }], systemInstruction: { parts: [{ text: systemPrompt }] } };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
                const result = await response.json();
                const prompt = result.candidates[0].content.parts[0].text;
                const content = `<div class="text-sm text-gray-500 mb-2">Prompt Video Berbicara untuk Scene ${sceneIndex}/${selectedCount} :</div><div id="modal-text-content" class="whitespace-pre-wrap bg-gray-100 p-4 rounded-lg text-gray-800 mb-4 text-sm leading-relaxed border-l-4 border-orange-500 shadow-sm max-h-[50vh] overflow-y-auto" style="-webkit-overflow-scrolling: touch;">${prompt.trim()}</div><div class="text-xs text-gray-500 mb-3 italic">💡 Prompt ini sudah include dialog berbahasa Indonesia dan detail gestur untuk talking head food video</div><button id="copy-modal-btn" class="w-full font-bold py-3 px-4 rounded-lg flex items-center justify-center bg-gradient-to-r from-orange-500 to-amber-600 hover:from-orange-600 hover:to-amber-700 text-white shadow-lg active:scale-95 transition-all"><i class="fas fa-copy mr-2 pointer-events-none"></i><span class="pointer-events-none">Salin Prompt ke Clipboard</span></button>`;
                const sparkleEmojiModal = '\u2728';
                showModal(sparkleEmojiModal + ' Prompt Video Food Review Berbicara', content);
                const copyBtn = document.getElementById('copy-modal-btn');
                const handleCopy = (e) => {
                    e.preventDefault();
                    e.stopPropagation();
                    const text = document.getElementById('modal-text-content').innerText;
                    copyToClipboard(text, e.currentTarget);
                };
                copyBtn.addEventListener('click', handleCopy);
                copyBtn.addEventListener('touchend', (e) => {
                    e.preventDefault();
                    e.stopPropagation();
                    handleCopy(e);
                });
            } catch (error) {
                console.error("Error generating video prompt:", error);
                showModal('Error', `<p class="text-red-500">Gagal membuat prompt video. Silakan coba lagi.</p>`);
            }
        }

        window.handleFoodEditPromptClick = function(id, title, originalPrompt) {
            const content = `<p class="text-sm text-gray-500 mb-2">Edit prompt untuk scene food review:</p><h4 class="text-lg font-semibold text-gray-800 mb-3">${title}</h4><textarea id="modal-edit-prompt-textarea" class="w-full h-32 p-3 bg-gray-100 border border-gray-300 rounded-lg focus:ring-2 focus:ring-orange-500 focus:border-orange-500 transition" rows="5">${originalPrompt}</textarea><button id="modal-regenerate-btn" class="w-full btn-primary font-bold py-2 px-4 rounded-lg flex items-center justify-center mt-4 bg-gradient-to-r from-orange-500 to-amber-600 hover:from-orange-600 hover:to-amber-700"><svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9M4 12v5h.582m15.356 2A8.001 8.001 0 004.582 17M20 12h-5" /></svg>Regenerate</button>`;
            const editEmoji = '\u270F\uFE0F';
            showModal(editEmoji + ' Edit & Regenerate Prompt', content);
            document.getElementById('modal-regenerate-btn').addEventListener('click', () => {
                const newPrompt = document.getElementById('modal-edit-prompt-textarea').value;
                universalModal.classList.remove('visible');
                generateSingleFoodReview(id, title, newPrompt);
            });
        }

        foodGrid.addEventListener('click', (e) => {
            e.stopPropagation();
            const actionBtn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
            if (!actionBtn) return;
            const action = actionBtn.dataset.action;
            const sceneId = actionBtn.dataset.sceneId;
            const card = document.getElementById(`food-card-${sceneId}`);
            if (!card) { console.error('Card not found for scene ID:', sceneId); return; }
            const img = card.querySelector('.food-output-container img');
            const imageUrl = img ? img.src : '';
            const title = card.dataset.title || '';
            const originalPrompt = card.dataset.prompt || '';
            console.log('Action clicked:', action, 'Scene:', sceneId);
            if (action === 'food-preview') {
                if (!img) return;
                if (window.showPreviewModal) { window.showPreviewModal(imageUrl); }
            } else if (action === 'food-edit-prompt') {
                window.handleFoodEditPromptClick(parseInt(sceneId), title, originalPrompt);
            } else if (action === 'food-regenerate') {
                generateSingleFoodReview(parseInt(sceneId), title, originalPrompt);
            } else if (action === 'food-video-prompt') {
                if (!img) return;
                window.handleFoodVideoPromptClick(imageUrl, title, parseInt(sceneId));
            } else if (action === 'food-caption') {
                if (!img) return;
                window.handleFoodCaptionClick(imageUrl, title);
            } else if (action === 'food-download') {
                if (!img) { console.error('Image not found for download'); return; }
                const filename = actionBtn.dataset.filename || `food_review_${sceneId}.png`;
                console.log('Downloading:', filename, imageUrl.substring(0, 50));
                if (window.downloadDataURINew) {
                    window.downloadDataURINew(imageUrl, filename);
                }
            }
        });
    })();

    // ========================================
    // TAB: PRODUCT ADS GENERATOR
    // ========================================
    (() => {
        const adsImageInput = document.getElementById('ads-image-input');
        const adsImageUploadArea = document.getElementById('ads-image-upload-area');
        const adsImagePreviewContainer = document.getElementById('ads-image-preview-container');

        const adsModelImageInput = document.getElementById('ads-model-image-input');
        const adsModelImageUploadArea = document.getElementById('ads-model-image-upload-area');
        const adsModelImagePreviewContainer = document.getElementById('ads-model-image-preview-container');
        const adsModelImagePreview = document.getElementById('ads-model-image-preview');
        const adsRemoveModelImageBtn = document.getElementById('ads-remove-model-image-btn');

        const adsProductDescInput = document.getElementById('ads-product-desc-input');
        const adsGenerateBtn = document.getElementById('ads-generate-btn');
        const adsGrid = document.getElementById('ads-grid');
        const adsGenerateDescBtn = document.getElementById('ads-generate-desc-btn');

        let adsProductImages = [];
        let adsModelImageBase64 = null;
        let adsModelImageMimeType = null;
        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const adsCountSel = document.getElementById('ads-count-selection-grid');
        if (adsCountSel) {
            adsCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                adsCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        function updateAdsButtonState() {
            adsGenerateBtn.disabled = adsProductImages.length === 0 || !adsProductDescInput.value.trim();
            adsGenerateDescBtn.disabled = adsProductImages.length === 0;
        }

        adsImageInput.addEventListener('change', (event) => {
            handleAdsProductFiles(event.target.files);
            event.target.value = null;
        });

        function manageAdsAddMoreButton() {
            const existingButton = document.getElementById('ads-add-more-button');
            if (existingButton) {
                existingButton.remove();
            }

            const addButtonWrapper = document.createElement('div');
            addButtonWrapper.id = 'ads-add-more-button';
            addButtonWrapper.innerHTML = `
                <label for="ads-image-input" class="w-full h-24 flex flex-col items-center justify-center rounded-lg border-2 border-dashed border-gray-300 cursor-pointer text-gray-400 hover:bg-gray-100 hover:border-gray-400 transition-colors">
                     <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path></svg>
                </label>
            `;
            adsImagePreviewContainer.appendChild(addButtonWrapper);
        }

        function handleAdsProductFiles(files) {
            if (files.length === 0) return;

            adsImageUploadArea.classList.add('hidden');
            adsImagePreviewContainer.classList.remove('hidden');

            let filesToProcess = files.length;

            Array.from(files).forEach(file => {
                const reader = new FileReader();
                reader.onload = (e) => {
                    const parts = e.target.result.split(',');
                    const mimeType = parts[0].match(/:(.*?);/)[1];
                    const base64 = parts[1];
                    const imageData = { base64, mimeType, id: Date.now() + Math.random() };
                    adsProductImages.push(imageData);

                    const previewWrapper = document.createElement('div');
                    previewWrapper.className = 'relative group';
                    previewWrapper.innerHTML = `
                        <img src="${e.target.result}" alt="Pratinjau Produk" class="rounded-lg w-full h-24 object-cover">
                        <button data-id="${imageData.id}" class="absolute top-1 right-1 bg-red-500 text-white rounded-full p-1 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity focus:opacity-100">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                        </button>
                    `;

                    adsImagePreviewContainer.appendChild(previewWrapper);

                    previewWrapper.querySelector('button').addEventListener('click', (btnEvent) => {
                        const idToRemove = parseFloat(btnEvent.currentTarget.getAttribute('data-id'));
                        adsProductImages = adsProductImages.filter(img => img.id !== idToRemove);
                        previewWrapper.remove();
                        if (adsProductImages.length === 0) {
                            adsImageUploadArea.classList.remove('hidden');
                            adsImagePreviewContainer.classList.add('hidden');
                            const addButton = document.getElementById('ads-add-more-button');
                            if (addButton) addButton.remove();
                        }
                        updateAdsButtonState();
                    });

                    updateAdsButtonState();

                    filesToProcess--;
                    if (filesToProcess === 0) {
                        manageAdsAddMoreButton();
                    }
                };
                reader.readAsDataURL(file);
            });
        }

        function setupAdsSingleImageUpload(input, uploadArea, previewContainer, previewImg, removeBtn, onUpload) {
            input.addEventListener('change', (event) => {
                const file = event.target.files[0];
                if (file) {
                    const reader = new FileReader();
                    reader.onload = (e) => {
                        previewImg.src = e.target.result;
                        const parts = e.target.result.split(',');
                        const mimeType = parts[0].match(/:(.*?);/)[1];
                        const base64 = parts[1];
                        onUpload(base64, mimeType);

                        uploadArea.classList.add('hidden');
                        previewContainer.classList.remove('hidden');
                    };
                    reader.readAsDataURL(file);
                }
            });

            removeBtn.addEventListener('click', () => {
                onUpload(null, null);
                input.value = '';
                uploadArea.classList.remove('hidden');
                previewContainer.classList.add('hidden');
            });
        }

        setupAdsSingleImageUpload(adsModelImageInput, adsModelImageUploadArea, adsModelImagePreviewContainer, adsModelImagePreview, adsRemoveModelImageBtn, (base64, mime) => {
            adsModelImageBase64 = base64;
            adsModelImageMimeType = mime;
        });

        adsProductDescInput.addEventListener('input', updateAdsButtonState);

        const createInitialAdsPlaceholders = () => {
            adsGrid.innerHTML = '';
            const selectedRatio = document.querySelector('.ratio-btn-ads.selected')?.dataset.ratio || '16:9';
            let aspectRatioClass = 'aspect-video'; // default 16:9
            if (selectedRatio === '1:1') aspectRatioClass = 'aspect-square';
            else if (selectedRatio === '3:4') aspectRatioClass = 'aspect-[3/4]';
            else if (selectedRatio === '9:16') aspectRatioClass = 'aspect-[9/16]';

            for(let i = 1; i <= selectedCount; i++) {
                const card = document.createElement('div');
                card.className = 'result-card card p-4 flex flex-col justify-between animate-pulse';
                card.innerHTML = `
                    <div><div class="h-6 bg-gray-200 rounded w-3/4 mb-4"></div></div>
                    <div class="mt-4 ${aspectRatioClass} bg-gray-300 rounded-md flex items-center justify-center"></div>
                `;
                adsGrid.appendChild(card);
            }
        };
        window.addEventListener('load', createInitialAdsPlaceholders);

        // Ratio button selection handler
        document.querySelectorAll('.ratio-btn-ads').forEach(btn => {
            btn.addEventListener('click', function() {
                // Remove selected class from all buttons
                document.querySelectorAll('.ratio-btn-ads').forEach(b => {
                    b.classList.remove('selected');
                    b.style.borderColor = '';
                    b.style.background = '';
                    b.style.color = '';
                    b.style.boxShadow = '';
                });
                // Add selected class to clicked button
                this.classList.add('selected');
                this.style.borderColor = '#ec4899';
                this.style.background = 'linear-gradient(135deg, #ec4899, #8b5cf6)';
                this.style.color = 'white';
                this.style.boxShadow = '0 4px 12px rgba(236, 72, 153, 0.3)';

                // Update placeholders
                createInitialAdsPlaceholders();
            });
        });

        adsGenerateBtn.addEventListener('click', async () => {
            if (adsProductImages.length === 0) return;
            adsGenerateBtn.disabled = true;
            const originalBtnHTML = `<svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg><span>Buat Scene Iklan</span>`;
            adsGenerateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Menganalisa produk...</span>`;
            adsGrid.scrollIntoView({ behavior: 'smooth', block: 'nearest' });

            let adsIdeas;
            try {
                adsIdeas = await analyzeAndGetAdsPrompts();
            } catch (error) {
                console.error("Error analyzing product:", error);
                adsGrid.innerHTML = `<div class="col-span-1 sm:col-span-2 xl:col-span-3 text-center py-10 text-red-500"><p>Terjadi kesalahan: ${error.message}</p></div>`;
                adsGenerateBtn.disabled = false;
                adsGenerateBtn.innerHTML = originalBtnHTML;
                return;
            }

            adsGenerateBtn.innerHTML = `<div class="loader"></div><span class="ml-2">Membuat Visual Iklan...</span>`;

            const MAX_ATTEMPTS = 3;
            let attempts = 0;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                createAdsDynamicCards(adsIdeas);
                const generationPromises = adsIdeas.map((idea, index) => generateSingleAd(index + 1, idea.title, idea.prompt));
                await Promise.allSettled(generationPromises);
                successCount = Array.from(adsGrid.querySelectorAll('.result-card')).filter(c => c.querySelector('img')).length;
            }

            adsGrid.querySelectorAll('.result-card').forEach(card => {
                if (!card.querySelector('img')) card.remove();
            });

            adsGenerateBtn.disabled = false;
            adsGenerateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            }
        });

        async function analyzeAndGetAdsPrompts() {
            const apiKey = "";
            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
            const selectedRatio = document.querySelector('.ratio-btn-ads.selected')?.dataset.ratio || '16:9';

            const voiceSelect = document.getElementById('ads-narrator-voice');
            const selectedVoiceName = voiceSelect.value;
            const selectedVoiceDesc = voiceSelect.options[voiceSelect.selectedIndex].dataset.desc;

            const theme = document.getElementById('ads-photo-theme-input').value.trim();
            const settingInstruction = theme
                ? `**SETTING/LATAR:** SELURUH iklan HARUS menggunakan setting yang sesuai dengan tema: "${theme}". Setting ini WAJIB diterapkan konsisten di SEMUA ${selectedCount} scene.`
                : `**SETTING/LATAR:** SELURUH iklan HARUS berlangsung di setting studio yang sama dan profesional (di atas meja bersih, di depan backdrop solid color, setup minimalis). Konsisten di SEMUA ${selectedCount} scene.`;

            let systemPrompt = '';
            if (adsModelImageBase64) {
                 systemPrompt = `You are an expert AI storyboard artist and creative director. Your task is to analyze the product, description, and model to create a **${selectedCount}-part visual advertising story** for the aspect ratio ${selectedRatio}.
The story will be narrated by a voice named **${selectedVoiceName}**, who has a **'${selectedVoiceDesc}'** tone.

**CRITICAL VISUAL RULES FOR MODEL CONSISTENCY:**
1.  ${settingInstruction}
2.  **Model + Produk Bersama:** Untuk SEMUA ${selectedCount} scene, model manusia dan produk HARUS terlihat bersama dalam frame yang sama. Model harus secara aktif memegang, menggunakan, atau mendemonstrasikan produk. JANGAN generate close-up *hanya* produk atau shot *hanya* model.
3.  **KONSISTENSI MODEL SANGAT PENTING:** Model harus orang yang **SAMA PERSIS** di **SEMUA ${selectedCount} SCENE**. Gunakan deskripsi yang sangat spesifik dan konsisten untuk model berdasarkan foto yang diupload:
   - Gender, usia, ras/etnis HARUS identik di semua scene
   - Warna rambut, panjang rambut, gaya rambut HARUS identik di semua scene
   - Pakaian (warna, style) HARUS identik di semua scene
   - Fitur wajah (bentuk wajah, mata, hidung, bibir) HARUS konsisten
   - Seolah difilmkan dalam satu sesi berkelanjutan tanpa ganti baju atau gaya rambut
   - Di setiap prompt, SELALU describe model dengan detail yang sama persis

**STRUCTURE IKLAN:**
Buat ${selectedCount} scene yang membentuk narasi iklan lengkap dan sequential dari awal sampai akhir. Scene harus nyambung satu sama lain membentuk cerita yang koheren.

Contoh 4 scene: Opening Hook → Product Demo → Benefits Showcase → Call to Action
Contoh 7 scene: Opening Hook → Problem Introduction → Product Reveal → Key Feature 1 → Key Feature 2 → Results/Benefits → Call to Action
Contoh 10 scene: Full advertising story dari Opening Hook sampai Final Call to Action dengan semua tahapan lengkap

You MUST ensure the ${selectedCount} scenes form a coherent, sequential narrative (Scene 1, Scene 2... Scene ${selectedCount}) that perfectly matches the narrator's tone.
For each scene, provide a short title (in Indonesian, e.g., 'Scene 1: Awal Mula') and a CONCISE English prompt for an AI image generator yang SELALU include deskripsi model yang konsisten.
Respond ONLY with a valid JSON array of ${selectedCount} objects, in sequential story order.`;
            } else {
                 systemPrompt = `You are an elite-level creative director. Your primary goal is to generate a **${selectedCount}-part visual advertising story** (storyboard) for the product for the aspect ratio ${selectedRatio}. **Absolutely no people, models, or human parts.**
The story will be narrated by a voice named **${selectedVoiceName}**, who has a **'${selectedVoiceDesc}'** tone.

${settingInstruction}

**STRUCTURE IKLAN:**
Buat ${selectedCount} scene yang membentuk narasi iklan lengkap dan sequential dari awal sampai akhir. Scene harus nyambung satu sama lain membentuk cerita produk yang koheren.

Contoh 4 scene: Product Introduction → Feature Highlight → Benefits Demo → Final Appeal
Contoh 7 scene: Opening Shot → Problem Setup → Product Reveal → Feature 1 → Feature 2 → Lifestyle Integration → Closing Shot
Contoh 10 scene: Complete advertising narrative dari Product Introduction sampai Strong Closing dengan semua tahapan lengkap

You MUST ensure the ${selectedCount} scenes form a coherent, sequential narrative (Scene 1, Scene 2... Scene ${selectedCount}) that showcases the product's journey or benefits sequentially, matching the narrator's tone.
For each concept, provide a short, elegant title in Indonesian (e.g., 'Bagian 1: Pengenalan') and a CONCISE English prompt for the AI image generator.
Respond ONLY with a valid JSON array of ${selectedCount} objects, in sequential story order.`;
            }

            let userQuery = `Analyze this product. Product Description: "${adsProductDescInput.value.trim()}". Desired aspect ratio is ${selectedRatio}.`;
            if (theme) {
                userQuery += `\nPhoto Theme/Setting: "${theme}" - WAJIB gunakan setting ini di semua scene.`;
            }
            const parts = [{ text: userQuery }];
            adsProductImages.forEach(img => {
                parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
            });
            if (adsModelImageBase64) {
                parts.push({ inlineData: { mimeType: adsModelImageMimeType, data: adsModelImageBase64 } });
            }
            const payload = { contents: [{ parts: parts }], systemInstruction: { parts: [{ text: systemPrompt + `\n\nFor each scene, also generate a "narration" field: a short, natural-sounding Bahasa Indonesia narration script (2-3 sentences, max 50 words) for the narrator ${selectedVoiceName} to speak. Match the narrator's tone: ${selectedVoiceDesc}.` }] }, generationConfig: { responseMimeType: "application/json", responseSchema: { type: "ARRAY", items: { type: "OBJECT", properties: { "title": { "type": "STRING" }, "prompt": { "type": "STRING" }, "narration": { "type": "STRING" } }, required: ["title", "prompt", "narration"] } } } };
            const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
            if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
            const result = await response.json();

            if (!result.candidates || !result.candidates[0].content || !result.candidates[0].content.parts || !result.candidates[0].content.parts[0].text) {
                throw new Error("Invalid response structure from API.");
            }

            let rawText = result.candidates[0].content.parts[0].text;
            rawText = rawText.replace(/```json/g, '').replace(/```/g, '').trim();
            const startIndex = rawText.indexOf('[');
            const endIndex = rawText.lastIndexOf(']');

            if (startIndex === -1 || endIndex === -1 || startIndex > endIndex) {
                throw new Error("Could not find a valid JSON array in the API response.");
            }

            const jsonText = rawText.substring(startIndex, endIndex + 1);

            try {
                return JSON.parse(jsonText);
            } catch (e) {
                console.error("Failed to parse JSON:", jsonText, e);
                throw new Error("API returned malformed JSON after cleaning.");
            }
        }

        async function generateSingleAd(id, title, prompt) {
            const card = document.getElementById(`ads-card-${id}`);
            if (!card) return;
            const outputContainer = card.querySelector('.ads-output-container');

            const selectedRatio = document.querySelector('.ratio-btn-ads.selected')?.dataset.ratio || '16:9';
            let aspectRatioClass = 'aspect-video'; // default 16:9
            if (selectedRatio === '1:1') aspectRatioClass = 'aspect-square';
            else if (selectedRatio === '3:4') aspectRatioClass = 'aspect-[3/4]';
            else if (selectedRatio === '9:16') aspectRatioClass = 'aspect-[9/16]';

            outputContainer.innerHTML = `<div class="${aspectRatioClass} bg-gray-100 rounded-md flex items-center justify-center"><div class="loader"></div></div>`;

            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const selectedRatio = document.querySelector('.ratio-btn-ads.selected')?.dataset.ratio || '16:9';

                let finalPrompt = '';
                if (adsModelImageBase64) {
                    finalPrompt = `${prompt}, elegant, cinematic, professional product photography, dramatic lighting, 8k, photorealistic`;
                } else {
                    finalPrompt = `${prompt}, professional commercial product photography, hyper-detailed, epic composition, cinematic lighting, no people, 8k, photorealistic`;
                }

                const parts = [{ text: finalPrompt }];

                adsProductImages.forEach(img => {
                    parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
                });

                if (adsModelImageBase64) { parts.push({ inlineData: { mimeType: adsModelImageMimeType, data: adsModelImageBase64 } }); }

                const payload = {
                    contents: [{ parts: parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                    safetySettings: [
                        { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" }
                    ]
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data received');

                {
                        const imageUrl = `data:image/png;base64,${base64Data}`;
                        const safeTitle = title.replace(/[^a-z0-9]/gi, '_').toLowerCase();

                        // Store prompt in card's dataset for edit functionality
                        // Using card variable already declared at line 45283
                        card.dataset.prompt = prompt;

                        outputContainer.innerHTML = `
                            <div class="relative w-full h-full group">
                                <img src="${imageUrl}" class="w-full h-full object-cover rounded-md" alt="Ad Scene">
                                <div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/70 via-black/50 to-transparent p-4 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-all duration-300 ease-in-out rounded-b-md">
                                    <div class="flex flex-wrap gap-2 justify-end">
                                        <button data-action="ads-preview" data-scene-id="${id}" class="action-btn bg-blue-500 text-white px-3 py-2 rounded-lg hover:bg-blue-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Preview">
                                            <i class="fas fa-search-plus"></i>
                                            <span class="hidden sm:inline">Preview</span>
                                        </button>
                                        <button data-action="ads-edit-prompt" data-scene-id="${id}" class="action-btn bg-purple-500 text-white px-3 py-2 rounded-lg hover:bg-purple-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Edit Prompt">
                                            <i class="fas fa-edit"></i>
                                            <span class="hidden sm:inline">Edit</span>
                                        </button>
                                        <button data-action="ads-regenerate" data-scene-id="${id}" class="action-btn bg-green-500 text-white px-3 py-2 rounded-lg hover:bg-green-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Regenerate">
                                            <i class="fas fa-sync-alt"></i>
                                            <span class="hidden sm:inline">Regenerate</span>
                                        </button>
                                        <button data-action="ads-video-prompt" data-scene-id="${id}" class="action-btn bg-fuchsia-500 text-white px-3 py-2 rounded-lg hover:bg-fuchsia-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Buat Prompt Video">
                                            <i class="fas fa-film"></i>
                                            <span class="hidden sm:inline">Video</span>
                                        </button>
                                        <button data-action="ads-caption" data-scene-id="${id}" class="action-btn bg-rose-500 text-white px-3 py-2 rounded-lg hover:bg-rose-600 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Buat Caption">
                                            <i class="fas fa-pencil-alt"></i>
                                            <span class="hidden sm:inline">Caption</span>
                                        </button>
                                        <button data-action="ads-download" data-scene-id="${id}" data-filename="iklan_${id}_${safeTitle}.png" class="action-btn bg-cyan-600 text-white px-3 py-2 rounded-lg hover:bg-cyan-700 active:scale-95 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm font-medium" title="Unduh Gambar">
                                            <i class="fas fa-download"></i>
                                            <span class="hidden sm:inline">Unduh</span>
                                        </button>
                                    </div>
                                </div>
                            </div>`;
                        return;
                }
            } catch (error) {
                console.error(`Error generating ad card ${id}:`, error);
                outputContainer.innerHTML = '';
            }
        }

        const createAdsDynamicCards = (prompts) => {
            adsGrid.innerHTML = '';
            const selectedRatio = document.querySelector('.ratio-btn-ads.selected')?.dataset.ratio || '16:9';
            let aspectRatioClass = 'aspect-video'; // default 16:9
            if (selectedRatio === '1:1') aspectRatioClass = 'aspect-square';
            else if (selectedRatio === '3:4') aspectRatioClass = 'aspect-[3/4]';
            else if (selectedRatio === '9:16') aspectRatioClass = 'aspect-[9/16]';

            prompts.forEach((p, index) => {
                const card = document.createElement('div');
                card.id = `ads-card-${index + 1}`;
                card.className = 'result-card card p-4 flex flex-col justify-between';
                card.dataset.title = p.title;
                card.dataset.prompt = p.prompt;
                card.dataset.narration = p.narration || '';
                card.innerHTML = `<div><h3 class="text-lg font-semibold text-gray-800 mb-4">${p.title}</h3></div> <div class="ads-output-container ${aspectRatioClass} bg-gray-100 rounded-md flex items-center justify-center"><div class="loader"></div></div> ${p.narration ? `<div class="ads-narration-box mt-3 p-3 bg-indigo-50 border border-indigo-200 rounded-lg"><div class="flex items-center gap-2 mb-1"><i class="fas fa-microphone text-indigo-500 text-xs"></i><span class="text-xs font-semibold text-indigo-600">Script Narasi</span></div><p class="text-sm text-gray-700 leading-relaxed">${p.narration}</p><button class="ads-copy-narration mt-2 text-xs text-indigo-500 hover:text-indigo-700 flex items-center gap-1" data-text="${p.narration.replace(/"/g, '&quot;')}"><i class="fas fa-copy"></i> Salin</button></div>` : ''}`;
                adsGrid.appendChild(card);
            });
        };

        adsGenerateDescBtn.addEventListener('click', async () => {
            const originalButtonText = adsGenerateDescBtn.innerHTML;
            adsGenerateDescBtn.innerHTML = `<div class="loader !w-4 !h-4 !border-2"></div>`;
            adsGenerateDescBtn.disabled = true;
            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `You are a professional copywriter. Analyze the product in the image and write a concise, compelling, and SEO-friendly product description in Indonesian. Highlight its key features and potential benefits for customers. Keep it under 500 characters.`;
                const parts = [ { text: "Buatkan deskripsi produk untuk gambar ini." }];
                if (adsProductImages.length > 0) {
                     parts.push({ inlineData: { mimeType: adsProductImages[0].mimeType, data: adsProductImages[0].base64 } });
                }
                const payload = {
                    contents: [{ parts: parts }],
                    systemInstruction: { parts: [{ text: systemPrompt }] }
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
                const result = await response.json();
                const description = result.candidates[0].content.parts[0].text;
                adsProductDescInput.value = description.trim();
                updateAdsButtonState();
            } catch(error) {
                console.error("Error generating description:", error);
                adsProductDescInput.value = "Gagal membuat deskripsi. Coba lagi.";
            } finally {
                adsGenerateDescBtn.innerHTML = originalButtonText;
                adsGenerateDescBtn.disabled = false;
            }
        });

        window.handleAdsCaptionClick = async function(imageUrl, title) {
            try {
                // Tampilkan loading modal
                const modal = document.createElement('div');
                modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4 overflow-y-auto';
                modal.style.WebkitOverflowScrolling = 'touch';
                modal.innerHTML = `
                    <div class="bg-white rounded-xl p-6 max-w-2xl w-full my-8" onclick="event.stopPropagation()">
                        <h3 class="text-xl font-bold mb-4 text-gray-800">
                            <i class="fas fa-pencil-alt text-rose-500 mr-2"></i>Membuat Caption...
                        </h3>
                        <div class="flex items-center justify-center py-8">
                            <div class="loader !border-l-rose-500"></div>
                        </div>
                    </div>
                `;
                document.body.appendChild(modal);

                // Close modal on backdrop click
                modal.addEventListener('click', (e) => {
                    if (e.target === modal) {
                        modal.remove();
                    }
                });

                const parts = imageUrl.split(',');
                const mimeType = parts[0].match(/:(.*?);/)[1];
                const base64Data = parts[1];
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `You are a creative social media manager. Based on the product photo, description, and theme, write an engaging Instagram caption in Indonesian. It should be appealing, concise, and end with 3-5 relevant hashtags.`;
                const userQuery = `Product Description: "${adsProductDescInput.value.trim()}"\nPhoto Theme: "${title}"`;
                const payload = {
                    contents: [{ parts: [ { text: userQuery }, { inlineData: { mimeType: mimeType, data: base64Data } } ] }],
                    systemInstruction: { parts: [{ text: systemPrompt }] }
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
                const result = await response.json();
                const caption = result.candidates[0].content.parts[0].text;

                // Update modal dengan hasil caption
                const modalContent = modal.querySelector('.bg-white');
                modalContent.innerHTML = `
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-xl font-bold text-gray-800">
                            <i class="fas fa-pencil-alt text-rose-500 mr-2"></i>Caption Instagram
                        </h3>
                        <button data-action="close-modal" class="text-gray-500 hover:text-gray-700 text-2xl leading-none" title="Tutup">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>

                    <div class="max-h-[60vh] overflow-y-auto mb-4" style="-webkit-overflow-scrolling: touch;">
                        <div class="whitespace-pre-wrap bg-gray-100 p-4 rounded-lg text-gray-800 leading-relaxed">${caption.trim()}</div>
                    </div>

                    <div class="flex gap-2">
                        <button data-action="copy-caption" data-caption="${caption.trim().replace(/"/g, '&quot;')}"
                            class="flex-1 bg-rose-500 hover:bg-rose-600 text-white font-bold py-3 px-4 rounded-lg transition-colors">
                            <i class="fas fa-copy mr-2"></i>Salin Caption
                        </button>
                        <button data-action="close-modal"
                            class="bg-gray-500 hover:bg-gray-600 text-white font-bold py-3 px-4 rounded-lg transition-colors">
                            Tutup
                        </button>
                    </div>
                `;

                // Add event delegation for copy and close buttons (works on desktop & mobile)
                modalContent.addEventListener('click', async (e) => {
                    e.stopPropagation();

                    const copyBtn = e.target.closest('[data-action="copy-caption"]');
                    const closeBtn = e.target.closest('[data-action="close-modal"]');

                    if (copyBtn) {
                        const captionText = copyBtn.dataset.caption;
                        try {
                            await navigator.clipboard.writeText(captionText);
                            const originalHTML = copyBtn.innerHTML;
                            copyBtn.innerHTML = '<i class="fas fa-check mr-2"></i>Tersalin!';
                            copyBtn.classList.remove('bg-rose-500');
                            copyBtn.classList.add('bg-green-500');
                            setTimeout(() => {
                                copyBtn.innerHTML = originalHTML;
                                copyBtn.classList.remove('bg-green-500');
                                copyBtn.classList.add('bg-rose-500');
                            }, 2000);
                        } catch (err) {
                            console.error('Failed to copy:', err);
                            alert('Caption berhasil dipilih. Salin dengan Ctrl+C atau Command+C');
                        }
                    } else if (closeBtn) {
                        modal.remove();
                    }
                });
            } catch (error) {
                console.error("Error generating caption:", error);
                showModal('Error', `<p class="text-red-500">Gagal membuat caption. Silakan coba lagi.</p>`);
            }
        }

        window.handleAdsEditPromptClick = function(id, title, originalPrompt) {
            const content = `
                <p class="text-sm text-gray-500 mb-2">Edit prompt untuk scene:</p>
                <h4 class="text-lg font-semibold text-gray-800 mb-3">${title}</h4>
                <textarea id="modal-edit-prompt-textarea" class="w-full h-32 p-3 bg-gray-100 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition" rows="5">${originalPrompt}</textarea>
                <button id="modal-regenerate-btn" class="w-full btn-primary font-bold py-2 px-4 rounded-lg flex items-center justify-center mt-4">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9M4 12v5h.582m15.356 2A8.001 8.001 0 004.582 17M20 12h-5" /></svg>
                    Regenerate
                </button>
            `;
            const editEmoji = '\u270F\uFE0F'; // ✏️
            showModal(editEmoji + ' Edit & Regenerate Prompt', content);

            document.getElementById('modal-regenerate-btn').addEventListener('click', () => {
                const newPrompt = document.getElementById('modal-edit-prompt-textarea').value;
                universalModal.classList.remove('visible');
                generateSingleAd(id, title, newPrompt);
            });
        }

        window.handleAdsVideoPromptClick = async function(imageUrl, title, sceneIndex) {
            const sparkleEmoji = '\u2728'; // ✨
            showModal(sparkleEmoji + ' Prompt Image-to-Video', `<div class="flex flex-col items-center gap-4"><div class="loader"></div> <p class="text-gray-500">AI sedang menganalisa gerakan...</p></div>`);
            try {
                const parts = imageUrl.split(',');
                const mimeType = parts[0].match(/:(.*?);/)[1];
                const base64Data = parts[1];
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const voiceSelect = document.getElementById('ads-narrator-voice');
                const selectedVoiceName = voiceSelect.options[voiceSelect.selectedIndex].text;
                const productDesc = document.getElementById('ads-product-desc-input').value.trim();
                const photoTheme = document.getElementById('ads-photo-theme-input').value.trim();

                const systemPrompt = `You are an AI video generation expert. A user has a 10-part image storyboard for a product advertising. You are being given *one scene* from that story. Your task is to generate a concise, effective image-to-video prompt in English that animates this specific scene, *keeping in mind its place in the overall narrative*. The prompt must strictly follow the formula: "subject + what it's doing (action) + background + camera movement". Infer the most logical and visually appealing motion from the static image for the 'action' part, and add a suitable 'camera movement' to make the shot more cinematic (e.g., 'slow zoom in', 'panning from left to right').`;

                let userQuery = `
                --- Context ---
                Product Description: "${productDesc}"
                Photo Theme: "${photoTheme || 'Tidak ada'}"
                Selected Narrator: "${selectedVoiceName}"
                This Scene's Title: "${title}"
                This is Scene: ${sceneIndex} of 10.
                --- Task ---
                Based on all this context, write a short, cinematic video prompt (in English) to bring this specific advertising scene to life.
                `;

                const payload = {
                    contents: [{ parts: [ { text: userQuery }, { inlineData: { mimeType: mimeType, data: base64Data } } ] }],
                    systemInstruction: { parts: [{ text: systemPrompt }] }
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) { const errorData = await response.json(); throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`); }
                const result = await response.json();
                const prompt = result.candidates[0].content.parts[0].text;
                const content = `
                    <div class="text-sm text-gray-500 mb-2">Prompt ini dibuat berdasarkan konteks Scene ${sceneIndex}/10:</div>
                    <div id="modal-text-content" class="whitespace-pre-wrap bg-gray-100 p-4 rounded-lg text-gray-600 mb-4 font-mono">${prompt.trim()}</div>
                    <button id="copy-modal-btn" class="w-full btn-primary font-bold py-2 px-4 rounded-lg flex items-center justify-center">Salin Prompt</button>
                `;
                const sparkleEmojiModal = '\u2728'; // ✨
                showModal(sparkleEmojiModal + ' Prompt Image-to-Video', content);
                const copyBtn = document.getElementById('copy-modal-btn');
                const handleCopy = (e) => {
                    e.preventDefault();
                    e.stopPropagation();
                    const text = document.getElementById('modal-text-content').innerText;
                    copyToClipboard(text, e.currentTarget);
                };
                copyBtn.addEventListener('click', handleCopy);
                copyBtn.addEventListener('touchend', (e) => {
                    e.preventDefault();
                    e.stopPropagation();
                    handleCopy(e);
                });
            } catch (error) {
                console.error("Error generating video prompt:", error);
                showModal('Error', `<p class="text-red-500">Gagal membuat prompt video. Silakan coba lagi.</p>`);
            }
        }

        // Event delegation for action buttons in ads results
        adsGrid.addEventListener('click', (e) => {
            e.stopPropagation();

            // Handle copy narration button
            const copyNarBtn = e.target.closest('.ads-copy-narration');
            if (copyNarBtn) {
                const text = copyNarBtn.dataset.text;
                const orig = copyNarBtn.innerHTML;
                const fallbackCopy = (t) => { const ta = document.createElement('textarea'); ta.value = t; ta.style.position = 'fixed'; ta.style.left = '-9999px'; document.body.appendChild(ta); ta.select(); try { document.execCommand('copy'); } catch(e){} document.body.removeChild(ta); };
                if (navigator.clipboard && window.isSecureContext) { navigator.clipboard.writeText(text).catch(() => fallbackCopy(text)); } else { fallbackCopy(text); }
                copyNarBtn.innerHTML = '<i class="fas fa-check"></i> Tersalin!';
                setTimeout(() => { copyNarBtn.innerHTML = orig; }, 2000);
                return;
            }

            // Try to find the button - either clicked directly or child element
            const actionBtn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
            if (!actionBtn) return;

            const action = actionBtn.dataset.action;
            const sceneId = actionBtn.dataset.sceneId;
            const card = document.getElementById(`ads-card-${sceneId}`);

            if (!card) {
                console.error('Card not found for scene ID:', sceneId);
                return;
            }

            const img = card.querySelector('.ads-output-container img');
            const imageUrl = img ? img.src : '';
            const title = card.querySelector('h3')?.textContent || '';
            const originalPrompt = card.dataset.prompt || '';

            console.log('Action clicked:', action, 'Scene:', sceneId);

            if (action === 'ads-preview') {
                if (!img) return;
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageUrl);
                }
            } else if (action === 'ads-edit-prompt') {
                window.handleAdsEditPromptClick(parseInt(sceneId), title, originalPrompt);
            } else if (action === 'ads-regenerate') {
                // Regenerate with same prompt
                generateSingleAd(parseInt(sceneId), title, originalPrompt);
            } else if (action === 'ads-video-prompt') {
                if (!img) return;
                window.handleAdsVideoPromptClick(imageUrl, title, parseInt(sceneId));
            } else if (action === 'ads-caption') {
                if (!img) return;
                window.handleAdsCaptionClick(imageUrl, title);
            } else if (action === 'ads-download') {
                if (!img) {
                    console.error('Image not found for download');
                    return;
                }
                const filename = actionBtn.dataset.filename || `iklan_${sceneId}.png`;
                console.log('Downloading:', filename, imageUrl.substring(0, 50));
                if (window.downloadDataURINew) {
                    window.downloadDataURINew(imageUrl, filename);
                }
            }
        });
    })();

    // ==================== FOTO UMRAH/HAJI & PAS FOTO WARNA ====================

    const apiKey = ""; // API key will be injected here by the environment

    // Fungsi Utilitas
    async function convertHeicToJpg(file) {
        const isHeic = file.name.toLowerCase().endsWith('.heic') ||
                      file.type.toLowerCase() === 'image/heic' ||
                      file.type.toLowerCase() === 'image/heif';

        if (isHeic && typeof heic2any !== 'undefined') {
            console.log("HEIC file detected, converting...");
            try {
                const conversionResult = await heic2any({
                    blob: file,
                    toType: "image/jpeg",
                    quality: 0.9,
                });
                const convertedBlob = Array.isArray(conversionResult) ? conversionResult[0] : conversionResult;
                return new File([convertedBlob], file.name.replace(/\.heic$/i, ".jpg"), { type: "image/jpeg" });
            } catch (error) {
                console.error("HEIC conversion failed:", error);
                throw new Error("Gagal mengonversi file HEIC. Coba unggah file JPEG/PNG.");
            }
        }
        return file;
    }

    function setupImageUploadNew(input, uploadArea, onFile) {
        async function handleFile(file) {
            if (file) {
                try {
                    const processedFile = await convertHeicToJpg(file);
                    const reader = new FileReader();
                    reader.onload = (e) => {
                        const parts = e.target.result.split(',');
                        const mimeType = parts[0].match(/:([\w\/]+);/)[1];
                        const base64 = parts[1];
                        onFile({ base64, mimeType, dataUrl: e.target.result });
                    };
                    reader.readAsDataURL(processedFile);
                } catch (error) {
                    console.error("Error processing file:", error);
                }
            }
        }

        input.addEventListener('change', (event) => {
            setTimeout(() => {
                if (event.target.files && event.target.files.length > 0) {
                    handleFile(event.target.files[0]);
                }
            }, 100);
        });

        if (uploadArea) {
            ['dragover', 'drop', 'dragleave'].forEach(eventName => {
                uploadArea.addEventListener(eventName, e => { e.preventDefault(); e.stopPropagation(); });
            });
            uploadArea.addEventListener('dragover', () => uploadArea.style.borderColor = '#F59E0B');
            uploadArea.addEventListener('dragleave', () => uploadArea.style.borderColor = '');
            uploadArea.addEventListener('drop', (e) => {
                uploadArea.style.borderColor = '';
                handleFile(e.dataTransfer.files[0]);
            });
        }
    }

    function setupOptionButtonsNew(container, isCheckbox = false) {
        if (!container) return;
        container.addEventListener('click', (e) => {
            const clickedButton = e.target.closest('button');
            if (clickedButton && container.contains(clickedButton)) {
                if (isCheckbox) {
                    clickedButton.classList.toggle('selected');
                } else {
                    Array.from(container.children).forEach(btn => btn.classList.remove('selected'));
                    clickedButton.classList.add('selected');
                }
            }
        });
    }

    function getAspectRatioClassNew(ratio) {
        const mapping = {
            '1:1': 'aspect-square',
            '16:9': 'aspect-video',
            '9:16': 'aspect-[9/16]',
            '4:3': 'aspect-[4/3]',
            '3:4': 'aspect-[3/4]',
            '2:3': 'aspect-[2/3]',
            '4:6': 'aspect-[4/6]',
            '4:5': 'aspect-[4/5]'
        };
        return mapping[ratio] || 'aspect-square';
    }

    window.downloadDataURINew = async function(dataURI, filename) {
        try {
            const response = await fetch(dataURI);
            const blob = await response.blob();

            // iOS path: native share sheet untuk hindari blob URL navigation yang
            // reset Canvas session (user keluar tab → kembali harus login lagi).
            if (window.__isIOS && window.__isIOS()) {
                await window.__iosShareOrSaveImage(blob, filename);
                return;
            }

            if (typeof saveAs !== 'undefined') {
                saveAs(blob, filename);
            } else {
                const url = window.URL.createObjectURL(blob);
                const a = document.createElement('a');
                a.href = url;
                a.download = filename;
                document.body.appendChild(a);
                a.click();
                document.body.removeChild(a);
                window.URL.revokeObjectURL(url);
            }
        } catch (error) {
            console.error("Gagal mengunduh file:", error);
            // iOS: jangan window.open — itu yang nyebabin Canvas keluar + login hilang.
            // Sebaliknya, kalau __iosShareOrSaveImage sukses tadi sudah return; kalau gagal
            // (mis. fetch error sebelum sampai sana), tampilkan modal sebagai last resort.
            if (window.__isIOS && window.__isIOS()) {
                if (window.showUniversalModal) {
                    var isEN = (localStorage.getItem('app_language') || 'id') === 'en';
                    window.showUniversalModal(
                        isEN ? 'Download Failed' : 'Download Gagal',
                        '<p class="text-gray-700">' +
                        (isEN
                            ? 'Could not save the image. Please try again.'
                            : 'Gagal menyimpan gambar. Silakan coba lagi.') +
                        '</p>'
                    );
                }
            } else {
                window.open(dataURI, '_blank');
            }
        }
    };

    async function validateImageContentNew(imageData, contentType, validationElement) {
        if (!imageData) {
            validationElement.textContent = '';
            return false;
        }
        validationElement.innerHTML = '<div class="loader-icon-umrah mx-auto"></div>';
        try {
            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
            const systemPrompt = `Analyze the image. Determine if the main subject is ${contentType}. Respond ONLY with "yes" or "no".`;
            const payload = {
                contents: [{ parts: [{ text: `Is this a photo of ${contentType}?` }, { inlineData: { mimeType: imageData.mimeType, data: imageData.base64 } }] }],
                systemInstruction: { parts: [{ text: systemPrompt }] }
            };
            const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
            if (!response.ok) throw new Error(`Error ${response.status}`);
            const result = await response.json();
            const answer = result.candidates[0].content.parts[0].text.trim().toLowerCase();

            if (answer.includes('yes')) {
                validationElement.textContent = '✓ Foto valid';
                validationElement.className = 'text-center text-sm mt-2 text-orange-600';
                return true;
            } else {
                validationElement.textContent = `✗ Harap unggah foto ${contentType.replace('a ', '')}`;
                validationElement.className = 'text-center text-sm mt-2 text-red-600';
                return false;
            }
        } catch (error) {
            console.error("Validation error:", error);
            validationElement.textContent = `Gagal validasi: ${error.message}`;
            validationElement.className = 'text-center text-sm mt-2 text-red-600';
            return false;
        }
    }

    // ==================== FOTO UMRAH/HAJI ====================
    (function() {
        const fileInputArea = document.getElementById('umrah-file-input-area');
        const addPhotoBtn = document.getElementById('umrah-add-photo-btn');
        const umrahValidation = document.getElementById('umrah-validation');
        const umrahGenerateBtn = document.getElementById('umrah-generate-btn');
        const umrahDownloadAllBtn = document.getElementById('umrah-download-all-btn');

        if (!fileInputArea) return;

        let photos = [null, null]; // Will store base64 strings
        let photoMimeTypes = [null, null]; // Will store mime types
        let generatedImages = []; // Store generated images
        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const umrahCountSel = document.getElementById('umrah-count-selection-grid');
        if (umrahCountSel) {
            umrahCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                umrahCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        function updateFileInputs() {
            fileInputArea.innerHTML = '';
            photos.forEach((photo, i) => {
                const wrapper = document.createElement('div');
                wrapper.className = 'file-input-wrapper';
                if (photo) wrapper.classList.add('has-photo');

                const input = document.createElement('input');
                input.type = 'file';
                input.accept = 'image/png, image/jpeg, image/webp, image/heic, .heic, .HEIC';
                input.className = 'file-input';
                input.dataset.index = i;
                input.addEventListener('change', handleFileChange);

                const preview = document.createElement('img');
                preview.className = 'file-preview';
                if (photo) {
                    preview.src = `data:${photoMimeTypes[i]};base64,${photo}`;
                } else {
                    preview.classList.add('hidden');
                }

                const placeholder = document.createElement('span');
                placeholder.className = 'text-gray-500 text-center text-sm flex items-center justify-center h-full pointer-events-none';
                if (!photo) {
                    placeholder.innerHTML = '<div><i class="fas fa-user text-2xl mb-2" style="color: #F59E0B;"></i><br><span class="text-xs sm:text-sm">Tambah Foto</span></div>';
                }

                const deleteBtn = document.createElement('button');
                deleteBtn.className = 'delete-btn';
                deleteBtn.innerHTML = '×';
                deleteBtn.type = 'button';

                // Mobile-friendly click handler
                deleteBtn.addEventListener('click', (e) => umrahDeletePhoto(e, i));
                deleteBtn.addEventListener('touchstart', (e) => {
                    e.stopPropagation();
                    deleteBtn.style.transform = 'scale(0.9)';
                }, { passive: true });
                deleteBtn.addEventListener('touchend', (e) => {
                    e.stopPropagation();
                    deleteBtn.style.transform = '';
                }, { passive: true });

                wrapper.append(input, preview, placeholder, deleteBtn);
                fileInputArea.appendChild(wrapper);
            });
            updateValidation();
        }

        async function handleFileChange(e) {
            const index = parseInt(e.target.dataset.index, 10);
            const file = e.target.files[0];
            if (!file) return;

            // Show loading state
            const wrapper = e.target.closest('.file-input-wrapper');
            const placeholder = wrapper.querySelector('span');
            if (placeholder && !wrapper.classList.contains('has-photo')) {
                placeholder.innerHTML = '<div><i class="fas fa-spinner fa-spin text-2xl mb-2" style="color: #F59E0B;"></i><br><span class="text-xs">Memproses...</span></div>';
            }

            let processedFile = file;

            // Convert HEIC to JPEG if needed
            if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                try {
                    umrahValidation.textContent = 'Mengkonversi HEIC...';
                    umrahValidation.className = 'text-center text-sm mt-3 font-medium text-blue-600';

                    const convertedBlob = await heic2any({
                        blob: file,
                        toType: 'image/jpeg',
                        quality: 0.9
                    });
                    processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                        type: 'image/jpeg'
                    });
                } catch (err) {
                    console.error('HEIC conversion failed:', err);
                    umrahValidation.textContent = '❌ Gagal mengkonversi gambar HEIC';
                    umrahValidation.className = 'text-center text-sm mt-3 font-medium text-red-600';
                    updateFileInputs(); // Reset UI
                    return;
                }
            }

            const reader = new FileReader();
            reader.onload = (event) => {
                photos[index] = event.target.result.split(',')[1];
                photoMimeTypes[index] = processedFile.type;
                updateFileInputs();
            };
            reader.readAsDataURL(processedFile);
        }

        window.umrahDeletePhoto = (event, index) => {
            event.preventDefault();
            event.stopPropagation();

            // Visual feedback
            const wrapper = event.target.closest('.file-input-wrapper');
            if (wrapper) {
                wrapper.style.transition = 'transform 0.2s, opacity 0.2s';
                wrapper.style.transform = 'scale(0.9)';
                wrapper.style.opacity = '0.5';
            }

            setTimeout(() => {
                photos[index] = null;
                photoMimeTypes[index] = null;
                // Only remove the slot if there are more than 2
                if (photos.length > 2) {
                    photos.splice(index, 1);
                    photoMimeTypes.splice(index, 1);
                }
                updateFileInputs();
            }, 150);
        };

        function updateValidation() {
            const uploadedCount = photos.filter(p => p !== null).length;

            if (uploadedCount === 0) {
                umrahValidation.textContent = 'Silakan upload minimal 1 foto';
                umrahValidation.className = 'text-center text-sm mt-3 font-medium text-gray-500';
                umrahGenerateBtn.disabled = true;
            } else {
                umrahValidation.textContent = `✓ ${uploadedCount} foto siap digunakan`;
                umrahValidation.className = 'text-center text-sm mt-3 font-medium text-orange-600';
                umrahGenerateBtn.disabled = false;
            }
        }

        if (addPhotoBtn) {
            addPhotoBtn.addEventListener('click', () => {
                if (photos.length < 5) {
                    photos.push(null);
                    photoMimeTypes.push(null);
                    updateFileInputs();

                    // Scroll to bottom to show new input on mobile
                    if (window.innerWidth < 640) {
                        setTimeout(() => {
                            const lastWrapper = fileInputArea.lastElementChild;
                            if (lastWrapper) {
                                lastWrapper.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
                            }
                        }, 100);
                    }
                } else {
                    umrahValidation.textContent = '❌ Maksimal 5 foto';
                    umrahValidation.className = 'text-center text-sm mt-3 font-medium text-red-600';
                    setTimeout(() => {
                        updateValidation();
                    }, 2000);
                }
            });

            // Mobile touch feedback
            addPhotoBtn.addEventListener('touchstart', () => {
                addPhotoBtn.style.transform = 'scale(0.95)';
            }, { passive: true });

            addPhotoBtn.addEventListener('touchend', () => {
                addPhotoBtn.style.transform = '';
            }, { passive: true });
        }

        setupOptionButtonsNew(document.getElementById('umrah-attire-options'));
        setupOptionButtonsNew(document.getElementById('umrah-ratio-options'));
        setupOptionButtonsNew(document.getElementById('umrah-theme-options'));

        umrahGenerateBtn.addEventListener('click', async () => {
            const uploadedFiles = photos.map((p, i) => ({ base64: p, mimeType: photoMimeTypes[i] })).filter(f => f.base64);

            if (uploadedFiles.length === 0) {
                umrahValidation.textContent = '❌ Silakan upload minimal 1 foto';
                umrahValidation.className = 'text-center text-sm mt-3 font-medium text-red-600';
                return;
            }

            document.getElementById('umrah-results-placeholder').classList.add('hidden');
            const attire = document.querySelector('#umrah-attire-options .selected').dataset.value;
            const aspectRatio = document.querySelector('#umrah-ratio-options .selected').dataset.value;
            const theme = document.querySelector('#umrah-theme-options .selected').dataset.value;

            let prompt;
            if (uploadedFiles.length === 1) {
                prompt = `Create a professional photo of the person from the image, placed into an Umrah/Hajj setting. CRITICAL: Preserve the person's exact face, features, and gender from the original photo. The person should be wearing ${attire} that is appropriate for their gender. Scene: ${theme}. The final photo should be high-quality, realistic, and respectful.`;
            } else {
                prompt = `IMPORTANT: Create a professional photo showing ALL ${uploadedFiles.length} people from ALL uploaded images together in one Umrah/Hajj scene. CRITICAL: Preserve each person's exact face, features, and gender from their original photos. Each person should be wearing ${attire} that is appropriate for their respective gender (Ihram for men, appropriate covering for women if Ihram is selected). Scene: ${theme}. Compose them naturally in the scene as if they are together. The final photo should be high-quality, realistic, and respectful.`;
            }

            await generatePhotoSet('umrah', prompt, uploadedFiles, aspectRatio);
        });

        async function generatePhotoSet(type, prompt, imageData, aspectRatio, refImageData = null) {
            const resultsContainer = document.getElementById(`${type}-results-container`);
            const resultsGrid = document.getElementById(`${type}-results-grid`);
            const generateBtn = document.getElementById(`${type}-generate-btn`);

            const originalBtnHTML = generateBtn.innerHTML;
            generateBtn.disabled = true;
            generateBtn.innerHTML = `<div class="loader-icon-umrah !border-l-white w-5 h-5"></div><span class="ml-2">Membuat Foto...</span>`;

            const aspectClass = getAspectRatioClassNew(aspectRatio);
            resultsContainer.classList.remove('hidden');

            // selectedCount sudah di-declare di scope luar (count selector)
            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                resultsGrid.innerHTML = '';

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `${type}-card-${i}`;
                    card.className = `card overflow-hidden transition-all ${aspectClass} bg-gray-100 flex items-center justify-center`;
                    card.innerHTML = `<div class="loader-icon-umrah" style="width: 2.5rem; height: 2.5rem;"></div>`;
                    resultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSinglePhoto(i + 1, type, prompt, imageData, aspectRatio, refImageData)
                );
                await Promise.allSettled(generationPromises);

                // Remove empty cards
                resultsGrid.querySelectorAll(`[id^="${type}-card-"]`).forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                successCount = generatedImages.filter(Boolean).length;
            }

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else if (window.innerWidth < 1024) {
                setTimeout(() => {
                    resultsContainer.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }, 300);
            }

            generateBtn.disabled = false;
            generateBtn.innerHTML = originalBtnHTML;
        }

        async function generateSinglePhoto(id, type, prompt, imageData, aspectRatio, refImageData = null) {
            const card = document.getElementById(`${type}-card-${id}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const finalPrompt = `${prompt} This is variation number ${id}.`;

                const parts = [{ text: finalPrompt }];

                // Support both single image (object) and multiple images (array)
                if (Array.isArray(imageData)) {
                    // Multiple images - add all to parts
                    imageData.forEach(img => {
                        parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
                    });
                } else {
                    // Single image - backward compatibility
                    parts.push({ inlineData: { mimeType: imageData.mimeType, data: imageData.base64 } });
                }

                if (refImageData) {
                    parts.push({ inlineData: { mimeType: refImageData.mimeType, data: refImageData.base64 } });
                }

                const payload = {
                    contents: [{ parts: parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: aspectRatio }
                    },
                    safetySettings: [
                        { category: 'HARM_CATEGORY_HARASSMENT', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_HATE_SPEECH', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_SEXUALLY_EXPLICIT', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_DANGEROUS_CONTENT', threshold: 'BLOCK_NONE' }
                    ]
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const filename = `${type}_foto_${id}.png`;
                generatedImages[id - 1] = { url: imageUrl, filename };

                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover">
                    <div class="absolute bottom-2 right-2 flex gap-1">
                        <button class="btn-view-image bg-white hover:bg-gray-100 text-gray-800 p-2 rounded-full shadow-md" title="Lihat Gambar">
                            <i class="fas fa-eye w-4 h-4"></i>
                        </button>
                        <button class="btn-download-image text-white p-2 rounded-full shadow-md" style="background-color: #F59E0B;" title="Unduh Gambar" data-action="download">
                            <i class="fas fa-download w-4 h-4"></i>
                        </button>
                    </div>`;
                card.classList.remove('bg-gray-100', 'flex', 'items-center', 'justify-center');
                card.classList.add('relative');

                const viewBtn = card.querySelector('.btn-view-image');
                const downloadBtn = card.querySelector('.btn-download-image');
                viewBtn.addEventListener('click', (e) => {
                    e.stopPropagation();
                    if (window.showPreviewModal) window.showPreviewModal(imageUrl);
                });
                downloadBtn.addEventListener('click', (e) => {
                    e.stopPropagation();
                    if (window.downloadDataURINew) window.downloadDataURINew(imageUrl, filename);
                });
            } catch (error) {
                console.error(`Error for card ${type}-${id}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Download all functionality for Umrah
        if (umrahDownloadAllBtn) {
            umrahDownloadAllBtn.addEventListener('click', async () => {
                if (generatedImages.length === 0) return;

                umrahDownloadAllBtn.disabled = true;
                umrahDownloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < generatedImages.length; i++) {
                    const img = generatedImages[i];
                    if (!img) continue;
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                        // Small delay between downloads for better UX
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }
                }

                umrahDownloadAllBtn.disabled = false;
                umrahDownloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
            });
        }

        // Initialize file inputs on page load
        updateFileInputs();
    })();

    // ==================== PAS FOTO WARNA ====================
    (function() {
        const passportInput = document.getElementById('passport-input');
        const passportUploadBox = document.getElementById('passport-upload-box');
        const passportPreview = document.getElementById('passport-preview');
        const passportPlaceholder = document.getElementById('passport-placeholder');
        const passportRemoveBtn = document.getElementById('passport-remove-btn');
        const passportValidation = document.getElementById('passport-validation');
        const passportGenerateBtn = document.getElementById('passport-generate-btn');
        const passportCustomAttireContainer = document.getElementById('passport-custom-attire-container');
        const passportCustomAttireRefInput = document.getElementById('passport-custom-attire-ref-input');
        const passportCustomAttireRefUploadBox = document.getElementById('passport-custom-attire-ref-upload-box');
        const passportCustomAttireRefPreview = document.getElementById('passport-custom-attire-ref-preview');
        const passportCustomAttireRefPlaceholder = document.getElementById('passport-custom-attire-ref-placeholder');
        const passportCustomAttireRefRemoveBtn = document.getElementById('passport-custom-attire-ref-remove-btn');
        const passportAttireOptions = document.getElementById('passport-attire-options');
        const passportSchoolContainer = document.getElementById('passport-school-options-container');
        const passportSchoolOptions = document.getElementById('passport-school-options');
        const passportDownloadAllBtn = document.getElementById('passport-download-all-btn');

        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const passportCountSel = document.getElementById('passport-count-selection-grid');
        if (passportCountSel) {
            passportCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                passportCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        if (!passportInput) return;

        let passportData = { data: null, isValid: false };
        let passportCustomRefData = null;
        let generatedImages = []; // Store generated images

        function updatePassportBtn() {
            passportGenerateBtn.disabled = !passportData.isValid;
        }

        setupImageUploadNew(passportInput, passportUploadBox, async (data) => {
            passportData.data = data;
            passportPreview.src = data.dataUrl;
            passportPlaceholder.classList.add('hidden');
            passportPreview.classList.remove('hidden');
            passportRemoveBtn.classList.remove('hidden');
            passportData.isValid = await validateImageContentNew(data, 'a clear, front-facing portrait of a person', passportValidation);
            updatePassportBtn();
        });

        passportRemoveBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            passportData = { data: null, isValid: false };
            passportInput.value = '';
            passportPreview.src = '#';
            passportPreview.classList.add('hidden');
            passportPlaceholder.classList.remove('hidden');
            passportRemoveBtn.classList.add('hidden');
            passportValidation.textContent = '';
            updatePassportBtn();
            document.getElementById('passport-results-placeholder').classList.remove('hidden');
            document.getElementById('passport-results-container').classList.add('hidden');
            document.getElementById('passport-results-grid').innerHTML = '';
        });

        setupOptionButtonsNew(document.getElementById('passport-bg-options'));
        setupOptionButtonsNew(passportAttireOptions);
        setupOptionButtonsNew(passportSchoolOptions);
        setupOptionButtonsNew(document.getElementById('passport-size-options'));

        passportAttireOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            if (button.dataset.value === 'Sekolah') {
                passportSchoolContainer.classList.remove('hidden');
            } else {
                passportSchoolContainer.classList.add('hidden');
            }

            if (button.dataset.value === 'Kustom') {
                passportCustomAttireContainer.classList.remove('hidden');
            } else {
                passportCustomAttireContainer.classList.add('hidden');
            }
        });

        setupImageUploadNew(passportCustomAttireRefInput, passportCustomAttireRefUploadBox, (data) => {
            passportCustomRefData = data;
            passportCustomAttireRefPreview.src = data.dataUrl;
            passportCustomAttireRefPlaceholder.classList.add('hidden');
            passportCustomAttireRefPreview.classList.remove('hidden');
            passportCustomAttireRefRemoveBtn.classList.remove('hidden');
        });

        passportCustomAttireRefRemoveBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            passportCustomRefData = null;
            passportCustomAttireRefInput.value = '';
            passportCustomAttireRefPreview.src = '#';
            passportCustomAttireRefPreview.classList.add('hidden');
            passportCustomAttireRefPlaceholder.classList.remove('hidden');
            passportCustomAttireRefRemoveBtn.classList.add('hidden');
        });

        passportGenerateBtn.addEventListener('click', async () => {
            document.getElementById('passport-results-placeholder').classList.add('hidden');
            const background = document.querySelector('#passport-bg-options .selected').dataset.value;
            let attire = document.querySelector('#passport-attire-options .selected').dataset.value;

            let size = document.querySelector('#passport-size-options .selected').dataset.value;
            const aspectRatioForApi = (size === '2:3' || size === '4:6') ? '3:4' : size;

            let customRefDataForApi = null;

            if (attire === 'Sekolah') {
                const schoolLevel = document.querySelector('#passport-school-options .selected').dataset.value;
                attire = `Ganti dengan seragam sekolah ${schoolLevel} khas Indonesia`;
            } else if (attire === 'Kustom') {
                const customText = document.getElementById('passport-custom-attire-input').value.trim();
                if (passportCustomRefData) {
                    attire = `Ganti dengan pakaian yang sangat mirip dengan gambar referensi pakaian (gambar ketiga).`;
                    if (customText) {
                        attire += ` Deskripsi tambahan: ${customText}.`;
                    }
                    customRefDataForApi = passportCustomRefData;
                } else if (customText) {
                    attire = `Ganti dengan pakaian berikut: ${customText}.`;
                } else {
                    attire = 'Ganti dengan pakaian formal standar (kemeja putih).';
                }
            } else {
                attire = `Ganti dengan ${attire}`;
            }

            let prompt = `Create a formal passport photo using the person from the image. CRITICAL: Do not alter the person's face.
            Background: Replace the background with a solid ${background} color.
            Clothing: ${attire}.
            The final image must be a clear, front-facing headshot with professional lighting.`;

            await generatePassportPhotoSet('passport', prompt, passportData.data, aspectRatioForApi, customRefDataForApi);
        });

        async function generatePassportPhotoSet(type, prompt, imageData, aspectRatio, refImageData = null) {
            const resultsContainer = document.getElementById(`${type}-results-container`);
            const resultsGrid = document.getElementById(`${type}-results-grid`);
            const generateBtn = document.getElementById(`${type}-generate-btn`);

            const originalBtnHTML = generateBtn.innerHTML;
            generateBtn.disabled = true;
            generateBtn.innerHTML = `<div class="loader-icon-passport !border-l-white w-5 h-5"></div><span class="ml-2">Membuat Foto...</span>`;

            const aspectClass = getAspectRatioClassNew(aspectRatio);
            resultsContainer.classList.remove('hidden');

            // selectedCount sudah di-declare di scope luar (count selector)
            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                resultsGrid.innerHTML = '';

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `${type}-card-${i}`;
                    card.className = `card overflow-hidden transition-all ${aspectClass} bg-gray-100 flex items-center justify-center`;
                    card.innerHTML = `<div class="loader-icon-passport" style="width: 2.5rem; height: 2.5rem;"></div>`;
                    resultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSinglePassportPhoto(i + 1, type, prompt, imageData, aspectRatio, refImageData)
                );
                await Promise.allSettled(generationPromises);

                // Remove empty cards
                resultsGrid.querySelectorAll(`[id^="${type}-card-"]`).forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                successCount = generatedImages.filter(Boolean).length;
            }

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            }

            generateBtn.disabled = false;
            generateBtn.innerHTML = originalBtnHTML;
        }

        async function generateSinglePassportPhoto(id, type, prompt, imageData, aspectRatio, refImageData = null) {
            const card = document.getElementById(`${type}-card-${id}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const finalPrompt = `${prompt} This is variation number ${id}.`;

                const parts = [{ text: finalPrompt }, { inlineData: { mimeType: imageData.mimeType, data: imageData.base64 } }];
                if (refImageData) {
                    parts.push({ inlineData: { mimeType: refImageData.mimeType, data: refImageData.base64 } });
                }

                const payload = {
                    contents: [{ parts: parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: aspectRatio }
                    },
                    safetySettings: [
                        { category: 'HARM_CATEGORY_HARASSMENT', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_HATE_SPEECH', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_SEXUALLY_EXPLICIT', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_DANGEROUS_CONTENT', threshold: 'BLOCK_NONE' }
                    ]
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const filename = `${type}_foto_${id}.png`;
                generatedImages[id - 1] = { url: imageUrl, filename };

                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover">
                    <div class="absolute bottom-2 right-2 flex gap-1">
                        <button class="btn-view-passport bg-white hover:bg-gray-100 text-gray-800 p-2 rounded-full shadow-md" title="Lihat Gambar">
                            <i class="fas fa-eye w-4 h-4"></i>
                        </button>
                        <button class="btn-download-passport text-white p-2 rounded-full shadow-md" style="background-color: #7DDCDD;" title="Unduh Gambar" data-action="download">
                            <i class="fas fa-download w-4 h-4"></i>
                        </button>
                    </div>`;
                card.classList.remove('bg-gray-100', 'flex', 'items-center', 'justify-center');
                card.classList.add('relative');

                const viewBtn = card.querySelector('.btn-view-passport');
                const downloadBtn = card.querySelector('.btn-download-passport');
                viewBtn.addEventListener('click', (e) => {
                    e.stopPropagation();
                    if (window.showPreviewModal) window.showPreviewModal(imageUrl);
                });
                downloadBtn.addEventListener('click', (e) => {
                    e.stopPropagation();
                    if (window.downloadDataURINew) window.downloadDataURINew(imageUrl, filename);
                });
            } catch (error) {
                console.error(`Error for card ${type}-${id}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Download all functionality for Passport
        if (passportDownloadAllBtn) {
            passportDownloadAllBtn.addEventListener('click', async () => {
                if (generatedImages.length === 0) return;

                passportDownloadAllBtn.disabled = true;
                passportDownloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < generatedImages.length; i++) {
                    const img = generatedImages[i];
                    if (!img) continue;
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                        // Small delay between downloads for better UX
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }
                }

                passportDownloadAllBtn.disabled = false;
                passportDownloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
            });
        }
    })();

    // ==================== PHOTO BOOTH ====================
    (function() {
        const apiKey = "";

        // Element references
        const photoBoothInput = document.getElementById('photo-booth-input');
        const photoBoothUploadBox = document.getElementById('photo-booth-upload-box');
        const photoBoothPlaceholder = document.getElementById('photo-booth-placeholder');
        const photoBoothPreviewContainer = document.getElementById('photo-booth-preview-container');
        const photoBoothGenerateBtn = document.getElementById('photo-booth-generate-btn');
        const photoBoothFrameOptions = document.getElementById('photo-booth-frame-options');
        const photoBoothCustomThemeContainer = document.getElementById('photo-booth-custom-theme-container');
        const photoBoothCustomThemeInput = document.getElementById('photo-booth-custom-theme-input');
        const photoBoothRatioOptions = document.getElementById('photo-booth-ratio-options');
        const photoBoothResultsPlaceholder = document.getElementById('photo-booth-results-placeholder');
        const photoBoothResultsContainer = document.getElementById('photo-booth-results-container');
        const photoBoothResultsGrid = document.getElementById('photo-booth-results-grid');
        const photoBoothDownloadAllBtn = document.getElementById('photo-booth-download-all-btn');

        if (!photoBoothInput) return;

        let uploadedImages = []; // Array of multiple photos
        let generatedImages = [];
        let selectedFrame = 'Classic Strip';
        let selectedRatio = '3:4';
        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const photoBoothCountSel = document.getElementById('photo-booth-count-selection-grid');
        if (photoBoothCountSel) {
            photoBoothCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                photoBoothCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Setup multiple image upload with HEIC support
        photoBoothInput.addEventListener('change', async (e) => {
            const files = Array.from(e.target.files);
            if (files.length === 0) return;

            // Limit to max 4 photos
            const filesToProcess = files.slice(0, 4);

            photoBoothPlaceholder.classList.add('hidden');
            photoBoothPreviewContainer.classList.remove('hidden');
            photoBoothPreviewContainer.innerHTML = ''; // Clear existing previews
            uploadedImages = []; // Reset

            for (const file of filesToProcess) {
                try {
                    let processedFile = file;

                    // Convert HEIC to JPEG if necessary
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        const heic2any = window.heic2any;
                        if (heic2any) {
                            const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                        }
                    }

                    const reader = new FileReader();
                    reader.onload = (event) => {
                        const dataUrl = event.target.result;
                        const base64 = dataUrl.split(',')[1];

                        uploadedImages.push({
                            base64: base64,
                            mimeType: processedFile.type,
                            dataUrl: dataUrl
                        });

                        // Create preview item (mobile-friendly)
                        const previewItem = document.createElement('div');
                        previewItem.className = 'photo-booth-preview-item relative';
                        previewItem.innerHTML = `
                            <img src="${dataUrl}" class="w-full h-24 object-cover rounded-lg shadow-md" alt="Preview">
                            <button type="button" class="remove-photo-btn absolute -top-2 -right-2 bg-red-600 text-white rounded-full w-6 h-6 flex items-center justify-center hover:bg-red-700 shadow-lg text-xs opacity-0 transition-opacity" data-index="${uploadedImages.length - 1}">
                                <i class="fas fa-times"></i>
                            </button>
                        `;
                        photoBoothPreviewContainer.appendChild(previewItem);

                        // Enable generate button
                        photoBoothGenerateBtn.disabled = false;
                    };
                    reader.readAsDataURL(processedFile);
                } catch (error) {
                    console.error('Error processing image:', error);
                }
            }
        });

        // Remove individual photo handler (event delegation)
        photoBoothPreviewContainer.addEventListener('click', (e) => {
            const removeBtn = e.target.closest('.remove-photo-btn');
            if (!removeBtn) return;

            const index = parseInt(removeBtn.dataset.index);
            uploadedImages.splice(index, 1);

            // Re-render previews
            photoBoothPreviewContainer.innerHTML = '';
            uploadedImages.forEach((img, i) => {
                const previewItem = document.createElement('div');
                previewItem.className = 'relative group';
                previewItem.innerHTML = `
                    <img src="${img.dataUrl}" class="w-full h-24 object-cover rounded-lg shadow-md" alt="Preview">
                    <button type="button" class="remove-photo-btn absolute -top-2 -right-2 bg-red-600 text-white rounded-full w-6 h-6 flex items-center justify-center hover:bg-red-700 shadow-lg text-xs opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity" data-index="${i}">
                        <i class="fas fa-times"></i>
                    </button>
                `;
                photoBoothPreviewContainer.appendChild(previewItem);
            });

            // If no photos left, show placeholder and disable button
            if (uploadedImages.length === 0) {
                photoBoothPlaceholder.classList.remove('hidden');
                photoBoothPreviewContainer.classList.add('hidden');
                photoBoothGenerateBtn.disabled = true;
                photoBoothInput.value = '';

                // Reset results
                photoBoothResultsPlaceholder.classList.remove('hidden');
                photoBoothResultsContainer.classList.add('hidden');
                photoBoothResultsGrid.innerHTML = '';
                generatedImages = [];
            }
        });

        // Frame theme selection handler
        photoBoothFrameOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            photoBoothFrameOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedFrame = button.dataset.value;

            // Show/hide custom theme input
            if (selectedFrame === 'Custom') {
                photoBoothCustomThemeContainer.classList.remove('hidden');
            } else {
                photoBoothCustomThemeContainer.classList.add('hidden');
            }
        });

        // Aspect ratio selection handler
        photoBoothRatioOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            photoBoothRatioOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedRatio = button.dataset.value;
        });

        // Generate button handler
        photoBoothGenerateBtn.addEventListener('click', async () => {
            if (uploadedImages.length === 0) {
                alert('Silakan unggah foto terlebih dahulu');
                return;
            }

            // Determine frame theme
            let frameTheme = selectedFrame;
            if (selectedFrame === 'Custom' && photoBoothCustomThemeInput) {
                const customTheme = photoBoothCustomThemeInput.value.trim();
                frameTheme = customTheme || 'Classic Strip';
            }

            // Create prompt based on frame theme with ALL themes
            const frameDescriptions = {
                'Classic Strip': 'classic black and white photo booth strip style with film-like borders, vintage camera aesthetic, fun and nostalgic feel, multiple photo frames arranged vertically',
                'Foto Box': 'foto box style with multiple photo frames in a grid layout (2x2 or similar), clean borders between photos, professional photo booth aesthetic, perfect for group photos or multiple moments',
                'Vintage Polaroid': 'vintage polaroid instant photo style with white borders, retro 70s aesthetic, warm nostalgic colors, instant camera feel',
                'Colorful Party': 'vibrant colorful party photo booth style with confetti, balloons, festive decorations, fun celebration vibes, energetic atmosphere',
                'Wedding Elegant': 'elegant wedding photo booth style with romantic decorative frames, soft lighting, flowers and elegant patterns, sophisticated and classy',
                'Birthday Fun': 'fun birthday party photo booth with birthday cake, balloons, presents, cheerful decorations, festive celebration theme',
                'Retro 80s': 'retro 1980s style photo booth with neon colors, geometric patterns, vibrant pink and blue tones, nostalgic 80s aesthetic, vintage vibes',
                'Modern Minimal': 'modern minimalist photo booth style with clean lines, simple elegant frames, contemporary aesthetic, sleek design',
                'Neon Cyberpunk': 'neon cyberpunk futuristic photo booth with glowing neon lights, electric blue and pink colors, sci-fi aesthetic, digital futuristic vibe',
                'Tropical Beach': 'tropical beach photo booth theme with palm trees, ocean background, sunny vibes, summer vacation aesthetic, relaxed and breezy',
                'Christmas Holiday': 'Christmas holiday photo booth with snowflakes, santa hats, christmas trees, festive red and green colors, winter wonderland theme',
                'Halloween Spooky': 'Halloween spooky photo booth with pumpkins, ghosts, bats, orange and black colors, fun scary atmosphere, trick or treat vibes',
                'Valentine Love': 'Valentine romantic photo booth with hearts, roses, pink and red colors, love theme, romantic and sweet atmosphere',
                'Graduation': 'graduation ceremony photo booth with cap and gown, diplomas, academic achievement theme, proud and celebratory atmosphere',
                'Baby Shower': 'baby shower photo booth with cute baby decorations, pastel colors, teddy bears, baby bottles, adorable and sweet theme',
                'Festival Concert': 'music festival concert photo booth with stage lights, musical instruments, energetic crowd vibes, rock and roll aesthetic',
                'Movie Cinema': 'movie cinema photo booth with film reels, popcorn, movie tickets, Hollywood glamour, red carpet aesthetic',
                'Travel Adventure': 'travel adventure photo booth with landmarks, suitcases, passports, wanderlust theme, explorer vibes, world traveler aesthetic',
                'Anime Manga': 'anime manga style photo booth with Japanese pop culture elements, vibrant colors, comic book style frames, otaku aesthetic',
                'Gaming Esports': 'gaming esports photo booth with controllers, digital effects, neon gaming lights, competitive gamer aesthetic, tech-savvy theme',
                'Sports Team': 'sports team photo booth with trophies, medals, team colors, athletic energy, champion celebration theme',
                'Korean Photobox': 'korean photobox studio inside a small corner room with 3 walls visible, fisheye lens with barrel distortion effect, ultra wide 0.5 iPhone camera style angle, top down slightly, CCTV aesthetic, plain solid wall colors (light grey, beige, light brown), minimal studio with no busy decorations, tight corner box feel, softbox lighting with diffused light, evenly lit face and body, casual candid poses with peace signs and fun expressions, friends having fun, standing close to camera, full body shot visible, high detail DSLR photography, korean photo booth aesthetic'
            };

            const frameDescription = frameDescriptions[selectedFrame] || frameTheme;

            // Build prompt based on number of uploaded photos
            let photoInstruction = '';
            if (uploadedImages.length === 1) {
                photoInstruction = 'Keep the person\'s face and identity EXACTLY the same as in the uploaded photo';
            } else {
                photoInstruction = `Include ALL ${uploadedImages.length} people from the uploaded photos in the photo booth image. Each person should be clearly visible and their identities preserved. ${selectedFrame === 'Foto Box' ? 'Arrange them in a grid layout with separate frames for each person or group them naturally in one frame' : 'Group them together naturally in the frame'}`;
            }

            const prompt = `Create a professional photo booth image featuring ${uploadedImages.length === 1 ? 'the person' : `all ${uploadedImages.length} people`} from the uploaded ${uploadedImages.length === 1 ? 'photo' : 'photos'}.

FRAME STYLE: Apply a ${frameDescription} theme with decorative frame borders and aesthetic effects.

IMPORTANT REQUIREMENTS:
- ${photoInstruction}
- Create natural, fun, and engaging photo booth poses and expressions
- Add appropriate decorative frame borders that match the ${frameTheme} theme
- Include subtle effects like lighting, filters, or overlays that enhance the photo booth aesthetic
- Make it look like a real photo booth picture with authentic styling
- Ensure high quality and professional appearance
- ${uploadedImages.length > 1 ? 'Make sure all people are clearly visible and well-composed in the frame' : 'Focus on the person with great composition'}

The final result should be a complete photo booth image ready to print or share, with ${uploadedImages.length === 1 ? 'the person' : 'all people'} clearly recognizable and the frame style prominently featured.`;

            await generatePhotoBoothSet(prompt);
        });

        async function generatePhotoBoothSet(prompt) {
            const originalBtnHTML = photoBoothGenerateBtn.innerHTML;
            photoBoothGenerateBtn.disabled = true;
            photoBoothGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Membuat Photo Booth...';

            photoBoothResultsPlaceholder.classList.add('hidden');
            photoBoothResultsContainer.classList.remove('hidden');

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                photoBoothResultsGrid.innerHTML = '';

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `photo-booth-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-[3/4]';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-purple-600"></div>';
                    photoBoothResultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSinglePhotoBoothImage(i + 1, prompt)
                );
                await Promise.allSettled(generationPromises);

                photoBoothResultsGrid.querySelectorAll('[id^="photo-booth-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                successCount = generatedImages.filter(Boolean).length;
            }

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            }

            photoBoothGenerateBtn.disabled = false;
            photoBoothGenerateBtn.innerHTML = originalBtnHTML;
        }

        async function generateSinglePhotoBoothImage(index, basePrompt) {
            const card = document.getElementById(`photo-booth-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const finalPrompt = `${basePrompt}\n\nThis is photo booth variation number ${index}. Make this variation unique with different pose, expression, or slight styling variations while maintaining the same frame theme and ${uploadedImages.length === 1 ? "person's identity" : "all people's identities"}.`;

                // Build parts array: prompt + all uploaded images
                const parts = [{ text: finalPrompt }];

                // Add all uploaded images to the request
                uploadedImages.forEach(img => {
                    parts.push({ inlineData: { mimeType: img.mimeType, data: img.base64 } });
                });

                const payload = {
                    contents: [{ parts: parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                    safetySettings: [
                        { category: 'HARM_CATEGORY_HARASSMENT', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_HATE_SPEECH', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_SEXUALLY_EXPLICIT', threshold: 'BLOCK_NONE' },
                        { category: 'HARM_CATEGORY_DANGEROUS_CONTENT', threshold: 'BLOCK_NONE' }
                    ]
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const filename = `photo_booth_${selectedFrame.replace(/\s+/g, '_')}_${Date.now()}_${index}.png`;
                generatedImages[index - 1] = { url: imageUrl, filename };

                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Photo Booth ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-purple-500 to-pink-500 hover:from-purple-600 hover:to-pink-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
            } catch (error) {
                console.error(`Error generating photo booth ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        photoBoothResultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();

            const actionBtn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
            if (!actionBtn) return;

            const action = actionBtn.dataset.action;
            const index = parseInt(actionBtn.dataset.index, 10);
            const imageData = generatedImages[index];

            if (!imageData) return;

            if (action === 'preview') {
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageData.url);
                }
            } else if (action === 'download') {
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(imageData.url, imageData.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(imageData.url, imageData.filename);
                }
            }
        });

        // Download all functionality
        if (photoBoothDownloadAllBtn) {
            photoBoothDownloadAllBtn.addEventListener('click', async () => {
                if (generatedImages.length === 0) return;

                photoBoothDownloadAllBtn.disabled = true;
                photoBoothDownloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < generatedImages.length; i++) {
                    const img = generatedImages[i];
                    if (window.downloadImage) {
                        await window.downloadImage(img.url, img.filename);
                    } else if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                    }
                    await new Promise(resolve => setTimeout(resolve, 500));
                }

                photoBoothDownloadAllBtn.disabled = false;
                photoBoothDownloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
            });
        }
    })();

    // ==================== STICKER PACK STUDIO ====================
    (function() {
        const apiKey = "";

        const imageInput = document.getElementById('sticker-pack-studio-image-input');
        const imagePreview = document.getElementById('sticker-pack-studio-image-preview');
        const placeholder = document.getElementById('sticker-pack-studio-placeholder');
        const removeBtn = document.getElementById('sticker-pack-studio-remove-btn');

        const templateOptions = document.getElementById('sticker-pack-studio-template-options');
        const expressionOptions = document.getElementById('sticker-pack-studio-expression-options');
        const ratioOptions = document.getElementById('sticker-pack-studio-ratio-options');
        const counterEl = document.getElementById('sticker-pack-studio-counter');
        const selectAllBtn = document.getElementById('sticker-pack-studio-select-all-btn');
        const clearBtn = document.getElementById('sticker-pack-studio-clear-btn');

        const generateBtn = document.getElementById('sticker-pack-studio-generate-btn');
        const downloadAllBtn = document.getElementById('sticker-pack-studio-download-all-btn');
        const resultsGrid = document.getElementById('sticker-pack-studio-results-grid');
        const emptyState = document.getElementById('sticker-pack-studio-empty-state');

        const MAX_SELECTION = 10;

        let uploadedImageData = null;
        let selectedTemplate = 'expression-pack-10';
        let selectedRatio = '1:1';
        let selectedExpressions = []; // ordered list of expression IDs (max 10)
        let generatedImages = [];

        // Template-aware preset registry. Tiap template punya pool preset sendiri dengan:
        // - label_id / label_en: button label per locale
        // - icon: FontAwesome class
        // - prompt: face/expression detail untuk AI
        // - text: text yang muncul di output gambar (locale dipilih per-template)
        // - doodle: motif dekorasi kecil
        const templatePresets = {
            'expression-pack-10': {
                vibe: 'cream',
                presets: {
                    'tertawa': { label_id: 'Tertawa', label_en: 'Laughing', icon: 'fa-face-laugh-squint', prompt: 'big joyful laughing expression with mouth wide open showing teeth, eyes squinted with joy, head tilted slightly back', text: 'haha wow!', doodle: 'star sparkles, motion lines around the head' },
                    'senyum': { label_id: 'Senyum', label_en: 'Smiling', icon: 'fa-face-smile', prompt: 'sweet amused warm smile, eyes soft and crinkled with happiness, friendly approachable look', text: 'duh lucu!', doodle: 'tiny hearts and small star sparkles' },
                    'bingung': { label_id: 'Bingung', label_en: 'Confused', icon: 'fa-face-meh', prompt: 'confused puzzled expression, eyebrows slightly raised and asymmetric, lips pursed to one side, head tilted', text: 'hmm?', doodle: 'question mark and squiggly thinking lines above the head' },
                    'kaget': { label_id: 'Kaget', label_en: 'Surprised', icon: 'fa-face-surprise', prompt: 'genuinely surprised expression, eyes wide open shocked, mouth slightly agape, eyebrows raised high', text: 'loh??', doodle: 'exclamation marks and shock lines bursting out from the head' },
                    'berpikir': { label_id: 'Berpikir', label_en: 'Thinking', icon: 'fa-face-rolling-eyes', prompt: 'thoughtful contemplating expression with finger or hand near chin, eyes looking up slightly, lips closed gently', text: 'eh iya ya?', doodle: 'lightbulb idea icon and small thinking dots above the head' },
                    'percaya-diri': { label_id: 'Percaya Diri', label_en: 'Confident', icon: 'fa-face-grin-stars', prompt: 'confident self-assured expression with a smug satisfied smile, head held high, eyes focused and proud', text: 'mantap!', doodle: 'thumbs up and star burst, sparkle accents' },
                    'cool-kacamata': { label_id: 'Cool Kacamata', label_en: 'Cool Sunglasses', icon: 'fa-glasses', prompt: 'cool stylish expression wearing black sunglasses, slight smirk, head tilted with attitude', text: 'keren banget!', doodle: 'fire emoji and sunglasses sparkle reflection' },
                    'gemas': { label_id: 'Gemas', label_en: 'Adoring', icon: 'fa-face-grin-hearts', prompt: 'adoring squee expression with hands near cheeks or mouth, eyes sparkling with affection, soft excited smile', text: 'gemes!', doodle: 'heart bubbles, pink hearts of varying sizes around the face' },
                    'sedih': { label_id: 'Sedih', label_en: 'Sad', icon: 'fa-face-sad-tear', prompt: 'sad melancholic expression with downturned mouth, eyes glistening as if about to tear up, head slightly bowed', text: 'sedih banget...', doodle: 'tiny tear drop and small rain cloud above the head' },
                    'tidur': { label_id: 'Tidur', label_en: 'Sleeping', icon: 'fa-moon', prompt: 'peaceful sleeping expression with closed eyes, gentle relaxed smile, head tilted to side as if napping', text: 'selamat malam', doodle: 'Zzz floating up from the head, small moon and sparkle stars' },
                    // 6 ekspresi tambahan dari prompt screenshot (16 total now):
                    'makan': { label_id: 'Makan', label_en: 'Eating', icon: 'fa-utensils', prompt: 'eating expression with cheeks slightly puffed, mouth chewing happily, satisfied content look', text: 'nyam nyam', doodle: 'food crumbs and small fork-spoon icons' },
                    'marah': { label_id: 'Marah', label_en: 'Angry', icon: 'fa-face-angry', prompt: 'angry frustrated expression, eyebrows furrowed downward, lips tight in scowl, slight head tilt with intensity', text: 'kesel!', doodle: 'cartoon fire flames and angry vein lines on temple' },
                    'kiss': { label_id: 'Kiss', label_en: 'Kiss', icon: 'fa-heart', prompt: 'blowing a kiss with lips puckered forward and eyes gentle, hand near mouth as if sending kiss', text: 'sayang kamu!', doodle: 'pink hearts floating from mouth, small lipstick mark icon' },
                    'bersin': { label_id: 'Bersin', label_en: 'Sneezing', icon: 'fa-tissue', prompt: 'sneezing expression with eyes squinted shut, mouth open in mid-sneeze, head tilted back slightly', text: 'hachoo!', doodle: 'small splash droplets and tissue icon' },
                    'meminta-maaf': { label_id: 'Minta Maaf', label_en: 'Apologetic', icon: 'fa-hands-praying', prompt: 'apologetic regretful expression with hands clasped together near face, eyes pleading and soft, slight head bow', text: 'maaf ya...', doodle: 'small sweat drop and apologetic question mark' },
                    'semangat': { label_id: 'Semangat', label_en: 'Hyped', icon: 'fa-fire', prompt: 'excited motivated expression with mouth open in cheer, eyes bright, fist pumped or hand raised in hype', text: 'semangat!', doodle: 'flame burst and motion energy lines' }
                }
            },
            'mood-pack': {
                vibe: 'pastel',
                presets: {
                    'happy': { label_id: 'Happy', label_en: 'Happy', icon: 'fa-sun', prompt: 'serene happy expression with soft smile, eyes warm and content, peaceful glow', text: 'feeling good', doodle: 'sun rays and gentle sparkles' },
                    'calm': { label_id: 'Calm', label_en: 'Calm', icon: 'fa-leaf', prompt: 'calm peaceful expression with eyes softly closed or half-closed, relaxed mouth, gentle posture', text: 'deep breath', doodle: 'leaf and gentle wind swirl lines' },
                    'excited': { label_id: 'Excited', label_en: 'Excited', icon: 'fa-bolt', prompt: 'energetic excited expression with bright wide eyes, open smile, leaning forward with anticipation', text: 'lets gooo', doodle: 'lightning bolts and energy bursts' },
                    'chill': { label_id: 'Chill', label_en: 'Chill', icon: 'fa-mug-hot', prompt: 'relaxed chill expression with subtle smile, eyes half-lidded, leaning back slightly cool and casual', text: 'just vibing', doodle: 'coffee cup steam swirls and music notes' },
                    'melancholy': { label_id: 'Melancholy', label_en: 'Melancholy', icon: 'fa-cloud', prompt: 'wistful melancholic expression with eyes looking away into distance, soft mouth, contemplative mood', text: 'lost in thought', doodle: 'small rain cloud and floating dust particles' },
                    'hopeful': { label_id: 'Hopeful', label_en: 'Hopeful', icon: 'fa-star', prompt: 'hopeful optimistic expression looking upward with bright eyes, slight smile of anticipation, chin up', text: 'looking up', doodle: 'rising stars and gentle light beams' },
                    'grateful': { label_id: 'Grateful', label_en: 'Grateful', icon: 'fa-hand-holding-heart', prompt: 'grateful thankful expression with hands near heart, soft warm smile, eyes closed or gentle, peaceful gratitude', text: 'so blessed', doodle: 'small flowers and warm heart pulse' },
                    'peaceful': { label_id: 'Peaceful', label_en: 'Peaceful', icon: 'fa-dove', prompt: 'tranquil peaceful expression with eyes gently closed in meditation, calm relaxed face, balanced posture', text: 'zen mode', doodle: 'lotus petals and soft glow circles' },
                    'nostalgic': { label_id: 'Nostalgic', label_en: 'Nostalgic', icon: 'fa-camera-retro', prompt: 'nostalgic wistful expression with soft gaze into distance, gentle smile of memory, head slightly tilted', text: 'remember when', doodle: 'old polaroid photos and dust sparkles' },
                    'empowered': { label_id: 'Empowered', label_en: 'Empowered', icon: 'fa-crown', prompt: 'empowered confident expression with strong direct gaze, slight smirk, fist raised or hand on hip', text: 'I got this', doodle: 'crown silhouette and power burst rays' },
                    // 6 mood tambahan untuk total 16:
                    'focused': { label_id: 'Focused', label_en: 'Focused', icon: 'fa-bullseye', prompt: 'deeply focused expression with sharp determined gaze, slightly furrowed brow, lips pressed in concentration, head slightly forward', text: 'in the zone', doodle: 'concentric target rings and converging arrows' },
                    'inspired': { label_id: 'Inspired', label_en: 'Inspired', icon: 'fa-lightbulb', prompt: 'inspired creative expression with bright eyes lit up by an idea, parted lips of wonder, head tilted upward with realization', text: 'feeling inspired', doodle: 'glowing lightbulb and gentle radiant lines' },
                    'tired': { label_id: 'Tired', label_en: 'Tired', icon: 'fa-battery-quarter', prompt: 'exhausted weary expression with droopy eyelids, slight pout, head propped or leaning on hand, soft sigh quality', text: 'drained', doodle: 'small wilted leaves and gentle sigh wave lines' },
                    'accomplished': { label_id: 'Accomplished', label_en: 'Accomplished', icon: 'fa-circle-check', prompt: 'satisfied accomplished expression with proud smile, relaxed shoulders, eyes content with finished work, head held confidently', text: 'did the thing', doodle: 'checkmark and gentle confetti drift' },
                    'adventurous': { label_id: 'Adventurous', label_en: 'Adventurous', icon: 'fa-mountain-sun', prompt: 'adventurous curious expression with wide-eyed wonder, slight smile of discovery, head turned as if exploring something new', text: 'exploring', doodle: 'compass icon and small mountain silhouette' },
                    'contemplative': { label_id: 'Contemplative', label_en: 'Contemplative', icon: 'fa-feather', prompt: 'contemplative reflective expression with soft introspective gaze downward, fingers near chin, lips slightly parted in thought', text: 'deep thoughts', doodle: 'floating feather and gentle ripple lines' }
                }
            },
            'reaction-pack': {
                vibe: 'meme',
                presets: {
                    'shock': { label_id: 'Shock', label_en: 'Shock', icon: 'fa-face-surprise', prompt: 'extremely shocked expression with mouth gaping open, eyes wide as saucers, head pulled back in disbelief', text: 'OMG!!', doodle: 'big exclamation marks and shock lines bursting' },
                    'cry': { label_id: 'Cry', label_en: 'Cry', icon: 'fa-droplet', prompt: 'dramatic crying expression with mouth open wide wailing, tears streaming from squinted eyes, head tilted back', text: 'WAAAH', doodle: 'cartoon tear waterfall and dramatic emphasis lines' },
                    'lmao': { label_id: 'LMAO', label_en: 'LMAO', icon: 'fa-face-grin-tears', prompt: 'hysterical laughing expression with mouth wide open showing teeth, eyes squeezed shut with tears, head thrown back', text: 'LMAOO', doodle: 'laughing lines and tears of joy radiating outward' },
                    'wat': { label_id: 'Wat?', label_en: 'Wat?', icon: 'fa-circle-question', prompt: 'extremely confused expression with eyebrows scrunched together asymmetrically, mouth in skeptical pursed line, head tilted', text: 'WAT?', doodle: 'big question marks and squiggle confusion lines' },
                    'rage': { label_id: 'Rage', label_en: 'Rage', icon: 'fa-fire-flame-curved', prompt: 'enraged furious expression with veins on temple, teeth bared in scowl, eyebrows pulled down sharply, intense glare', text: 'FFS!', doodle: 'fire flames bursting and angry vein marks' },
                    'bruh': { label_id: 'Bruh', label_en: 'Bruh', icon: 'fa-hand-back-fist', prompt: 'facepalm expression with hand covering forehead in disappointment, head slightly bowed, exasperated look', text: 'BRUH', doodle: 'sigh wave lines and tired energy droplets' },
                    'yas': { label_id: 'Yas Queen', label_en: 'Yas Queen', icon: 'fa-crown', prompt: 'fierce confident excited expression with sassy smile, eyebrow raised, head tilted with attitude, snap-fingers vibe', text: 'YAS QUEEN', doodle: 'crown emoji and fabulous sparkles' },
                    'bored': { label_id: 'Bored', label_en: 'Bored', icon: 'fa-bed', prompt: 'extremely bored deadpan expression with half-closed eyes, flat unimpressed mouth, head propped lazily on hand', text: 'zzz...', doodle: 'small Zzz floating and dust falling' },
                    'nope': { label_id: 'Nope', label_en: 'Nope', icon: 'fa-ghost', prompt: 'terrified frightened expression with mouth pulled back in horror grimace, eyes wide and worried, hand near mouth', text: 'NOPE NOPE', doodle: 'sweat droplets and trembling lines' },
                    'gasp': { label_id: 'Gasp', label_en: 'Gasp', icon: 'fa-comment', prompt: 'dramatic gasp expression with hand over mouth in shock, eyes wide with surprise, head pulled back theatrically', text: 'GASP!', doodle: 'sparkle stars of revelation and dramatic emphasis lines' },
                    // 6 reaction tambahan untuk total 16:
                    'vibing': { label_id: 'Vibing', label_en: 'Vibing', icon: 'fa-music', prompt: 'cool vibing expression with subtle nod, eyes half-lidded with chill confidence, slight head bob to imaginary music', text: 'VIBES', doodle: 'music notes and bouncing waveform lines' },
                    'dead': { label_id: 'Dead', label_en: 'Dead', icon: 'fa-skull', prompt: 'dramatic laughing-to-death expression, head thrown back wildly, mouth wide open in roaring laugh, hands waving as if cant breathe', text: 'IM DEAD', doodle: 'cartoon X-eyes and floating soul wisp' },
                    'mind-blown': { label_id: 'Mind Blown', label_en: 'Mind Blown', icon: 'fa-explosion', prompt: 'mind-blown expression with hands on either side of head, eyes wide in amazement, mouth open in shock at revelation', text: 'MIND BLOWN', doodle: 'explosion burst and small mushroom cloud' },
                    'sus': { label_id: 'Sus', label_en: 'Sus', icon: 'fa-eye', prompt: 'suspicious side-eye expression with eyes glancing sideways, eyebrow raised skeptically, lips in tight skeptical line', text: 'SUS', doodle: 'magnifying glass and side-eye emphasis arrow' },
                    'lfg': { label_id: 'LFG', label_en: 'LFG', icon: 'fa-rocket', prompt: 'hyped excitement expression with both fists pumped up, mouth wide in cheering yell, eyes blazing with hype energy', text: 'LFGGG', doodle: 'rocket trails and explosive star burst' },
                    'meh': { label_id: 'Meh', label_en: 'Meh', icon: 'fa-face-meh-blank', prompt: 'dismissive unimpressed expression with shoulders shrugged, palms turned upward, flat lips, eyes deadpan', text: 'MEH', doodle: 'shrug emoji style arrows and dust puffs' }
                }
            },
            'pacar-pack': {
                vibe: 'romantic',
                presets: {
                    'flying-kiss': { label_id: 'Flying Kiss', label_en: 'Flying Kiss', icon: 'fa-heart', prompt: 'sweet romantic flying kiss expression with lips puckered, hand near mouth blowing a kiss outward, soft loving eyes', text: 'muach!', doodle: 'pink hearts floating from mouth and lipstick mark' },
                    'love-eyes': { label_id: 'Love Eyes', label_en: 'Love Eyes', icon: 'fa-heart-circle-bolt', prompt: 'lovestruck expression with hearts in eyes, dreamy smile, slight head tilt with adoration, soft blush on cheeks', text: 'cintaku <3', doodle: 'big floating hearts around the head and sparkle dust' },
                    'kangen': { label_id: 'Kangen', label_en: 'Miss You', icon: 'fa-heart-pulse', prompt: 'longing missing expression with soft wistful eyes looking into distance, hand near heart, gentle melancholic smile', text: 'kangen kamu', doodle: 'broken heart with reconnect line and small thought bubble heart' },
                    'good-night': { label_id: 'Good Night', label_en: 'Good Night', icon: 'fa-moon', prompt: 'sleepy peaceful expression with eyes gently closed, soft smile, hands folded near cheek as if about to sleep', text: 'selamat tidur sayang', doodle: 'crescent moon and small floating Zzz with hearts' },
                    'good-morning': { label_id: 'Good Morning', label_en: 'Good Morning', icon: 'fa-sun', prompt: 'fresh morning expression with bright cheerful smile, eyes warm and welcoming, slight stretching gesture, glow of new day', text: 'pagi sayang', doodle: 'sun rays and small coffee cup with heart steam' },
                    'mikirin': { label_id: 'Mikirin Kamu', label_en: 'Thinking of You', icon: 'fa-cloud', prompt: 'thoughtful loving expression with soft gaze upward, finger on chin, gentle smile of fondness, head slightly tilted', text: 'lagi mikirin kamu', doodle: 'thought cloud bubble with heart inside' },
                    'peluk': { label_id: 'Peluk', label_en: 'Hug', icon: 'fa-hand-holding-heart', prompt: 'warm hugging gesture with arms wrapped around self as if sending a hug, eyes closed contentedly, soft loving smile', text: 'peluk!', doodle: 'arms-circle hug motion lines and warm heart glow' },
                    'cute-pout': { label_id: 'Pout Lucu', label_en: 'Cute Pout', icon: 'fa-face-meh', prompt: 'cute playful pout expression with lips pursed in mock upset, cheeks slightly puffed, eyes looking up with playful charm', text: 'ihhh kamu', doodle: 'small puffy cloud and tiny crossed-arms gesture lines' },
                    'malu': { label_id: 'Malu', label_en: 'Shy Blush', icon: 'fa-face-flushed', prompt: 'shy blushing expression with rosy cheeks, hand partially covering smile, eyes glancing down or away with bashful charm', text: 'duuh malu', doodle: 'small blush marks and tiny floating sakura petals' },
                    'wink': { label_id: 'Wink', label_en: 'Wink', icon: 'fa-face-grin-wink', prompt: 'flirty playful wink with one eye closed, charming half-smile, slight head tilt with confident charm', text: 'wink wink', doodle: 'small star at the wink eye and heart sparkle' },
                    'cheering': { label_id: 'Semangatin', label_en: 'Cheering', icon: 'fa-flag', prompt: 'enthusiastic cheering expression with arms raised, bright smile of encouragement, eyes shining with support and pride', text: 'kamu hebat sayang!', doodle: 'small flag waving and heart-shaped pom-poms' },
                    'maafin': { label_id: 'Maafin Yaa', label_en: 'Sorry Cute', icon: 'fa-hands-praying', prompt: 'cute apologetic expression with hands clasped together in pleading prayer pose, sad puppy eyes looking up, tiny pout', text: 'maafin yaa', doodle: 'small heart-shaped tear and tiny prayer hands sparkle' },
                    'dadah': { label_id: 'Dadah', label_en: 'Wave Bye', icon: 'fa-hand', prompt: 'sweet waving goodbye gesture with hand raised mid-wave, gentle longing smile, eyes warm with see-you-soon affection', text: 'daah sayang', doodle: 'small wave motion lines and tiny heart trail' },
                    'sendiri': { label_id: 'Sendirian', label_en: 'Lonely', icon: 'fa-cloud-rain', prompt: 'lonely melancholic expression with soft pout, hugging knees or arms, eyes looking down with longing, slight slump posture', text: 'sendirian nih', doodle: 'small rain cloud and lonely heart with single tear' },
                    'ketemu': { label_id: 'Ketemu Yuk', label_en: 'Meet Up', icon: 'fa-calendar-heart', prompt: 'excited anticipatory expression with hopeful smile, hands clasped together near chest, eyes bright with excitement to see partner', text: 'ketemu yuk!', doodle: 'small calendar with heart and tiny clock spinning fast' },
                    'love-finger': { label_id: 'Love Finger', label_en: 'Finger Heart', icon: 'fa-heart', prompt: 'kpop-style finger heart gesture with thumb and index finger forming small heart shape near face, sweet smile, eyes loving and warm', text: 'love you', doodle: 'tiny heart at finger gesture and floating sparkle hearts' }
                }
            },
            'anak-pack': {
                vibe: 'kids',
                presets: {
                    'menangis': { label_id: 'Menangis', label_en: 'Crying', icon: 'fa-face-sad-tear', prompt: 'crying expression with tears welling up in big eyes, mouth in sad pout, slight head down', text: 'menangis :(', doodle: 'tiny tear drops below the eyes' },
                    'hehehe': { label_id: 'Hehehe', label_en: 'Giggle', icon: 'fa-face-laugh', prompt: 'playful giggling expression with bright eyes squinted in laugh, mouth open in cheerful giggle, head tilted with joy', text: 'hehehe!', doodle: 'tiny stars and giggle motion lines' },
                    'ngantuk': { label_id: 'Ngantuk', label_en: 'Sleepy', icon: 'fa-moon', prompt: 'sleepy drowsy expression with droopy half-closed eyes, slight pout, hand near cheek as if rubbing eye', text: 'ngantuk...', doodle: 'small Zzz floating up' },
                    'terkejut': { label_id: 'Terkejut', label_en: 'Surprised', icon: 'fa-face-surprise', prompt: 'wide-eyed surprised expression with mouth in cute O shape, eyebrows raised, innocent shock', text: 'terkejut!', doodle: 'tiny exclamation marks around the head' },
                    'bingung': { label_id: 'Bingung', label_en: 'Confused', icon: 'fa-face-meh', prompt: 'confused puzzled expression with one finger near lip, eyes looking sideways, mouth slightly pursed', text: 'bingung?', doodle: 'small question mark and squiggly thinking lines' },
                    'lagi-makan': { label_id: 'Lagi Makan', label_en: 'Eating', icon: 'fa-utensils', prompt: 'happy eating expression with cheeks puffed full, mouth chewing happily, satisfied bright eyes', text: 'lagi makan', doodle: 'tiny food crumbs and small spoon icon' },
                    'cemberut': { label_id: 'Cemberut', label_en: 'Pouty', icon: 'fa-face-frown', prompt: 'cute pouty expression with lips pursed forward in pout, slight scowl, arms crossed feel, head turned slightly', text: 'cemberut', doodle: 'small grumpy cloud and tiny crossed-arm lines' },
                    'lucu': { label_id: 'Lucu', label_en: 'Cute', icon: 'fa-face-grin-hearts', prompt: 'irresistibly cute expression with bright sparkling eyes, sweet small smile, hands near cheeks in adorable pose', text: 'lucu yaa~', doodle: 'tiny hearts and sparkle stars around the face' },
                    'ohayoo': { label_id: 'Ohayoo', label_en: 'Good Morning', icon: 'fa-sun', prompt: 'fresh cheerful morning expression with bright smile, eyes wide and happy, slight stretching gesture, sunny glow', text: 'ohayoo!', doodle: 'small sun rays and tiny coffee mug icon' },
                    'kesel': { label_id: 'Kesel', label_en: 'Annoyed', icon: 'fa-face-angry', prompt: 'mildly annoyed cute expression with furrowed brow, lips in slight scowl, hands on hips feel, indignant pout', text: 'lagi kesel!!', doodle: 'tiny steam puffs and squiggle anger marks' },
                    'hmmm': { label_id: 'Hmmm', label_en: 'Thinking', icon: 'fa-face-rolling-eyes', prompt: 'thoughtful pondering expression with finger on chin, eyes looking up slightly, mouth in curious frown', text: 'Hmmm?', doodle: 'small thought bubble and tiny dots above the head' },
                    'love-you': { label_id: 'Love You', label_en: 'Love You', icon: 'fa-heart', prompt: 'sweet loving expression with arms extended forming a big heart shape above head, bright smile, eyes shining with affection', text: 'I love you!', doodle: 'big heart shape outline and small floating hearts' },
                    'cape': { label_id: 'Cape', label_en: 'Tired', icon: 'fa-battery-quarter', prompt: 'exhausted tired expression with droopy eyes, mouth in tired pout, hand wiping forehead, slumped shoulders', text: 'Cape jirrr~', doodle: 'small sweat drop and gentle exhausted sigh lines' },
                    'achoo': { label_id: 'Achoo', label_en: 'Sneeze', icon: 'fa-tissue', prompt: 'mid-sneeze expression with eyes squinted shut, mouth open in cute sneeze, head tilted back slightly with hand near nose', text: 'Achoo!', doodle: 'small splash droplets and tiny tissue icon' },
                    'good-night': { label_id: 'Good Night', label_en: 'Good Night', icon: 'fa-bed', prompt: 'peaceful sleeping expression with eyes gently closed, soft smile, hands folded near cheek, head tilted to side', text: 'malem malem!', doodle: 'crescent moon and small floating Zzz with stars' },
                    'gemes': { label_id: 'Gemes', label_en: 'Adorable', icon: 'fa-heart-circle-bolt', prompt: 'irresistibly adorable expression with both hands pressed to cheeks, mouth in tiny gasp of cuteness, eyes sparkling', text: 'gemes aku!', doodle: 'big floating hearts and many small sparkle stars' }
                }
            }
        };

        // Per-template prompt builders. Tiap return ARRAY of prompts (1 per selected preset).
        // Tiap template punya prompt structure berbeda — beda aesthetic, beda style cue, beda palette.
        const templateBuilders = {
            'expression-pack-10': (selectedList) => {
                const presets = templatePresets['expression-pack-10'].presets;
                return selectedList.map(id => {
                    const e = presets[id]; if (!e) return null;
                    return `Use the uploaded reference photo as the SAME PERSON in this image. Preserve their real facial features, identity, hair, and skin tone exactly as in the reference photo — the face must stay consistent and recognizable.

Render this person with the following expression and pose:
EXPRESSION: ${e.prompt}.

Background: plain cream-colored background with soft studio lighting, clean and minimal aesthetic.

Add hand-written aesthetic Indonesian text reading "${e.text}" placed naturally near the head or bottom of the frame, in casual doodle handwriting font style with imperfect organic strokes (NOT a typed font).

Add small white doodle decorations: ${e.doodle}, drawn in thin white outline hand-drawn style.

Overall style: realistic photography of the person, high detail, natural expression, sticker-pack aesthetic with white outline glow around the subject, soft pastel cream tone, instagrammable kawaii vibe. Do NOT make this a chibi or cartoon — keep it photorealistic.`;
                });
            },
            'mood-pack': (selectedList) => {
                const presets = templatePresets['mood-pack'].presets;
                return selectedList.map(id => {
                    const m = presets[id]; if (!m) return null;
                    return `Use the uploaded reference photo as the SAME PERSON in this image. Preserve their real facial features, identity, hair, and skin tone exactly as in the reference photo — the face must stay consistent and recognizable.

Render this person embodying the following mood:
MOOD: ${m.prompt}.

Background: soft pastel gradient background (gentle pinks, lavenders, peach tones blending), dreamy minimal aesthetic with diffused soft lighting.

Add minimalist hand-written English text reading "${m.text}" placed gracefully in negative space, in elegant cursive doodle font with thin organic strokes.

Add subtle decorative elements: ${m.doodle}, drawn in soft white or pastel-tinted thin lines.

Overall style: realistic photography of the person, soft cinematic atmospheric lighting, high detail, dreamy mood-board aesthetic, Pinterest-worthy soft tones, ethereal sticker-pack vibe with delicate outline. Do NOT make this a chibi or cartoon — keep it photorealistic and atmospheric.`;
                });
            },
            'reaction-pack': (selectedList) => {
                const presets = templatePresets['reaction-pack'].presets;
                return selectedList.map(id => {
                    const r = presets[id]; if (!r) return null;
                    return `Use the uploaded reference photo as the SAME PERSON in this image. Preserve their real facial features, identity, hair, and skin tone exactly as in the reference photo — the face must stay consistent and recognizable.

Render this person with this exaggerated reaction:
REACTION: ${r.prompt}.

Background: bold solid color background (bright yellow, vivid red, electric blue, or similar saturated meme-style color), high contrast with subject.

Add bold all-caps meme text reading "${r.text}" placed prominently near the top or bottom of the frame, in thick comic-style font with strong black outline (Impact-style meme typography).

Add exaggerated cartoon decorations: ${r.doodle}, drawn in bold cartoon outline style with high-contrast black lines.

Overall style: realistic photography of the person but with EXTRA EXAGGERATED expression for comedic effect, meme-pack aesthetic, comic book / reaction-image vibe, sharp contrast, viral-ready format. Subject stays photorealistic but expression is amplified for maximum reaction-image energy.`;
                });
            },
            'pacar-pack': (selectedList) => {
                const presets = templatePresets['pacar-pack'].presets;
                return selectedList.map(id => {
                    const p = presets[id]; if (!p) return null;
                    return `Use the uploaded reference photo as the SAME PERSON in this image. Preserve their real facial features, identity, hair, and skin tone exactly as in the reference photo — the face must stay consistent and recognizable.

Render this person with the following romantic expression and pose for sending to a partner:
ROMANTIC GESTURE: ${p.prompt}.

Background: soft romantic gradient background (gentle blush pink, peachy rose, warm cream, or dreamy lavender tones blending), warm sweet lighting with subtle glow that flatters the face.

Add hand-written aesthetic Indonesian text reading "${p.text}" placed naturally near the head or in negative space, in casual cursive sweetheart handwriting font with small organic strokes (NOT a typed font).

Add cute romantic doodle decorations: ${p.doodle}, drawn in thin pink/rose hand-drawn outline style with sparkle accents.

Overall style: realistic photography of the person with sweet romantic vibe, high detail, intimate warm sticker-pack aesthetic, white/pink outline glow around the subject, soft pastel rose tones, perfect for sending as personalized sticker to a partner. Do NOT make this a chibi or cartoon — keep it photorealistic with romantic warmth.`;
                });
            },
            'anak-pack': (selectedList) => {
                const presets = templatePresets['anak-pack'].presets;
                return selectedList.map(id => {
                    const a = presets[id]; if (!a) return null;
                    return `Use the uploaded reference photo as the SAME PERSON in this image. Preserve their real facial features, identity, hair, skin tone EXACTLY as in the reference photo — the face must NOT be changed even slightly. Keep the original face fully intact and recognizable.

Render this person with the following cute kid-style expression:
EXPRESSION: ${a.prompt}.

Background: clean simple solid background — soft white, light pastel cream, or gentle pale pink — minimal and clutter-free. The face should pop against the clean backdrop.

TEXT PLACEMENT (IMPORTANT): Add cute Indonesian hand-written text reading "${a.text}" placed at the BOTTOM-CENTER of the frame, directly below the photo, centered horizontally. The text must be in a designated text area below the subject (NOT floating around the head). Use casual cute kawaii kid-friendly handwriting font with playful organic strokes (NOT a typed font), in a warm color like soft brown, dusty pink, or playful blue.

Add small subtle decorations: ${a.doodle}, drawn in thin hand-drawn outline style around the bottom text area.

Overall style: realistic photography of the person (face unchanged from reference), clean kid-friendly sticker-pack aesthetic, white/soft outline around the subject, bright cheerful tones, perfect for cute baby/child sticker pack. Do NOT make this a chibi, cartoon, anime, or stylized illustration — keep it 100% photorealistic with the real face from the reference photo, only the expression and pose changes.`;
                });
            }
        };

        function updateCounter() {
            counterEl.textContent = selectedExpressions.length;
            generateBtn.disabled = !(uploadedImageData && selectedExpressions.length > 0);
        }

        function checkReady() {
            generateBtn.disabled = !(uploadedImageData && selectedExpressions.length > 0);
        }

        // Render preset buttons untuk template aktif. Locale-aware via app_language.
        // Reset selectedExpressions saat switch template karena preset IDs beda per template.
        function renderExpressionGrid() {
            const tpl = templatePresets[selectedTemplate] || templatePresets['expression-pack-10'];
            const lang = (typeof localStorage !== 'undefined' && localStorage.getItem('app_language')) || 'id';
            selectedExpressions = [];
            expressionOptions.innerHTML = '';
            for (const [id, info] of Object.entries(tpl.presets)) {
                const label = (lang === 'en' ? info.label_en : info.label_id) || info.label_en || id;
                const btn = document.createElement('button');
                btn.type = 'button';
                btn.dataset.value = id;
                btn.className = 'option-btn-sticker-pack-studio';
                btn.innerHTML = `<i class="fas ${info.icon} mr-1"></i><span>${label}</span>`;
                expressionOptions.appendChild(btn);
            }
            updateCounter();
        }

        templateOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('button[data-value]:not([disabled])');
            if (!btn) return;
            templateOptions.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
            btn.classList.add('selected');
            selectedTemplate = btn.dataset.value;
            renderExpressionGrid();
        });

        // Initial render dengan default template (expression-pack-10) on load
        renderExpressionGrid();

        // Multi-select expression with max 10. Toggle on/off + visual feedback.
        expressionOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('button[data-value]');
            if (!btn) return;
            const exprId = btn.dataset.value;
            const idx = selectedExpressions.indexOf(exprId);
            if (idx >= 0) {
                // Already selected — toggle off
                selectedExpressions.splice(idx, 1);
                btn.classList.remove('selected');
            } else {
                // Try to add — block kalau sudah max
                if (selectedExpressions.length >= MAX_SELECTION) {
                    // Visual feedback shake on counter
                    counterEl.parentElement.style.transition = 'transform 0.15s';
                    counterEl.parentElement.style.transform = 'scale(1.15)';
                    setTimeout(() => { counterEl.parentElement.style.transform = ''; }, 150);
                    return;
                }
                selectedExpressions.push(exprId);
                btn.classList.add('selected');
            }
            updateCounter();
        });

        if (selectAllBtn) {
            // "Pilih Semua" = pilih sampai MAX_SELECTION (10) dari preset pool template aktif.
            // Kalau pool > 10 (T1 punya 16), auto-cap ke first 10.
            selectAllBtn.addEventListener('click', () => {
                const tpl = templatePresets[selectedTemplate] || templatePresets['expression-pack-10'];
                const allIds = Object.keys(tpl.presets);
                selectedExpressions = allIds.slice(0, MAX_SELECTION);
                expressionOptions.querySelectorAll('button[data-value]').forEach(b => {
                    if (selectedExpressions.includes(b.dataset.value)) {
                        b.classList.add('selected');
                    } else {
                        b.classList.remove('selected');
                    }
                });
                updateCounter();
            });
        }

        if (clearBtn) {
            clearBtn.addEventListener('click', () => {
                selectedExpressions = [];
                expressionOptions.querySelectorAll('button[data-value]').forEach(b => b.classList.remove('selected'));
                updateCounter();
            });
        }

        ratioOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('button[data-value]');
            if (!btn) return;
            ratioOptions.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
            btn.classList.add('selected');
            selectedRatio = btn.dataset.value;
        });

        imageInput.addEventListener('change', async (e) => {
            const file = e.target.files[0];
            if (!file) return;
            try {
                let processedFile = file;
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    if (window.heic2any) {
                        const blob = await window.heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([blob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }
                const reader = new FileReader();
                reader.onload = (ev) => {
                    const dataUrl = ev.target.result;
                    uploadedImageData = {
                        base64: dataUrl.split(',')[1],
                        mimeType: processedFile.type,
                        dataUrl: dataUrl
                    };
                    imagePreview.src = dataUrl;
                    imagePreview.classList.remove('hidden');
                    placeholder.classList.add('hidden');
                    removeBtn.classList.remove('hidden');
                    checkReady();
                };
                reader.readAsDataURL(processedFile);
            } catch (err) {
                console.error('Sticker Pack image processing failed:', err);
                alert('Gagal memproses gambar. Silakan coba lagi.');
            }
        });

        removeBtn.addEventListener('click', () => {
            uploadedImageData = null;
            imageInput.value = '';
            imagePreview.src = '';
            imagePreview.classList.add('hidden');
            placeholder.classList.remove('hidden');
            removeBtn.classList.add('hidden');
            checkReady();
        });

        generateBtn.addEventListener('click', async () => {
            if (!uploadedImageData) {
                alert('Silakan upload foto wajah dulu.');
                return;
            }
            if (selectedExpressions.length === 0) {
                alert('Silakan pilih minimal 1 ekspresi.');
                return;
            }
            const builder = templateBuilders[selectedTemplate] || templateBuilders['expression-pack-10'];
            const prompts = builder(selectedExpressions);
            const validPrompts = prompts.filter(Boolean);
            if (validPrompts.length === 0) return;
            await generateStickerPackSet(validPrompts);
        });

        // Multi-prompt parallel generation: tiap output pakai prompt BERBEDA (per ekspresi).
        // Beda dari single-prompt-N-variations di tab lain.
        async function generateStickerPackSet(prompts) {
            const originalBtnHTML = generateBtn.innerHTML;
            generateBtn.disabled = true;
            generateBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i>Generating ${prompts.length} stickers...`;

            emptyState.classList.add('hidden');
            resultsGrid.classList.remove('hidden');
            downloadAllBtn.classList.add('hidden');

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                resultsGrid.innerHTML = '';

                for (let i = 1; i <= prompts.length; i++) {
                    const card = document.createElement('div');
                    card.id = `sticker-pack-studio-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gradient-to-br from-amber-100 to-orange-100 flex items-center justify-center aspect-square';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-orange-500"></div>';
                    resultsGrid.appendChild(card);
                }

                const promises = prompts.map((prompt, i) =>
                    generateSingleStickerPack(i + 1, prompt, selectedExpressions[i])
                );
                await Promise.allSettled(promises);

                resultsGrid.querySelectorAll('[id^="sticker-pack-studio-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });

                successCount = generatedImages.filter(Boolean).length;
            }

            generateBtn.disabled = false;
            generateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                downloadAllBtn.classList.remove('hidden');
            }
        }

        async function generateSingleStickerPack(index, prompt, exprId) {
            const card = document.getElementById(`sticker-pack-studio-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: uploadedImageData.mimeType, data: uploadedImageData.base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data in response');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const safeExpr = (exprId || `expr${index}`).replace(/[^a-z0-9-]/gi, '_').toLowerCase();
                const filename = `sticker_pack_${safeExpr}_${index}.png`;

                generatedImages[index - 1] = { url: imageUrl, filename: filename, exprId: exprId };

                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Sticker ${exprId}">
                    <div class="absolute top-2 left-2 bg-black/60 text-white text-xs font-semibold px-2 py-1 rounded-full backdrop-blur-sm">${exprId}</div>
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2 text-sm">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="action-btn text-white px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2 text-sm" style="background: linear-gradient(135deg, #fbbf24 0%, #f97316 100%);">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
            } catch (error) {
                console.error(`Error generating sticker pack ${index} (${exprId}):`, error);
                if (card) card.innerHTML = '';
            }
        }

        resultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();
            const btn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
            if (!btn) return;
            const action = btn.dataset.action;
            const idx = parseInt(btn.dataset.index, 10);
            const img = generatedImages[idx];
            if (!img) return;

            if (action === 'preview' && window.showPreviewModal) {
                window.showPreviewModal(img.url);
            } else if (action === 'download') {
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(img.url, img.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(img.url, img.filename);
                }
            }
        });

        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();
                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;
                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;
                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-1"></i>Downloading ${i + 1}/${generatedImages.length}...`;
                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(r => setTimeout(r, 500));
                        } catch (err) {
                            console.error(`Download ${i + 1} failed:`, err);
                        }
                    }
                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-1"></i>Selesai!';
                    setTimeout(() => { downloadAllBtn.disabled = false; downloadAllBtn.innerHTML = originalHTML; }, 2000);
                } catch (err) {
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-1"></i>Error';
                    setTimeout(() => { downloadAllBtn.disabled = false; downloadAllBtn.innerHTML = originalHTML; }, 2000);
                }
            });
        }
    })();

    // ==================== EXPRESSION CHANGER ====================
    (function() {
        const apiKey = "";

        // Element references
        const expressionInput = document.getElementById('expression-input');
        const expressionUploadBox = document.getElementById('expression-upload-box');
        const expressionPreview = document.getElementById('expression-preview');
        const expressionPlaceholder = document.getElementById('expression-placeholder');
        const expressionRemoveBtn = document.getElementById('expression-remove-btn');
        const expressionGenerateBtn = document.getElementById('expression-generate-btn');
        const expressionTypeOptions = document.getElementById('expression-type-options');
        const expressionCustomContainer = document.getElementById('expression-custom-container');
        const expressionCustomInput = document.getElementById('expression-custom-input');
        const expressionIntensityOptions = document.getElementById('expression-intensity-options');
        const expressionResultsPlaceholder = document.getElementById('expression-results-placeholder');
        const expressionResultsContainer = document.getElementById('expression-results-container');
        const expressionResultsGrid = document.getElementById('expression-results-grid');
        const expressionDownloadAllBtn = document.getElementById('expression-download-all-btn');

        if (!expressionInput) return;

        let uploadedImageData = null;
        let generatedImages = [];
        let selectedExpression = 'Happy Smiling';
        let selectedIntensity = 'Moderate';
        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const expressionCountSel = document.getElementById('expression-count-selection-grid');
        if (expressionCountSel) {
            expressionCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                expressionCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Setup image upload with HEIC support
        expressionInput.addEventListener('change', async (e) => {
            const file = e.target.files[0];
            if (!file) return;

            try {
                let processedFile = file;

                // Convert HEIC to JPEG if necessary
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    const heic2any = window.heic2any;
                    if (heic2any) {
                        const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }

                const reader = new FileReader();
                reader.onload = (event) => {
                    const dataUrl = event.target.result;
                    const base64 = dataUrl.split(',')[1];

                    uploadedImageData = {
                        base64: base64,
                        mimeType: processedFile.type,
                        dataUrl: dataUrl
                    };

                    expressionPreview.src = dataUrl;
                    expressionPlaceholder.classList.add('hidden');
                    expressionPreview.classList.remove('hidden');
                    expressionRemoveBtn.classList.remove('hidden');
                    expressionGenerateBtn.disabled = false;
                };
                reader.readAsDataURL(processedFile);
            } catch (error) {
                console.error('Error processing image:', error);
                alert('Gagal memproses gambar. Silakan coba lagi.');
            }
        });

        // Remove image handler
        expressionRemoveBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            uploadedImageData = null;
            expressionInput.value = '';
            expressionPreview.src = '#';
            expressionPreview.classList.add('hidden');
            expressionPlaceholder.classList.remove('hidden');
            expressionRemoveBtn.classList.add('hidden');
            expressionGenerateBtn.disabled = true;

            // Reset results
            expressionResultsPlaceholder.classList.remove('hidden');
            expressionResultsContainer.classList.add('hidden');
            expressionResultsGrid.innerHTML = '';
            generatedImages = [];
        });

        // Expression type selection handler
        expressionTypeOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            expressionTypeOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedExpression = button.dataset.value;

            // Show/hide custom expression input
            if (selectedExpression === 'Custom') {
                expressionCustomContainer.classList.remove('hidden');
            } else {
                expressionCustomContainer.classList.add('hidden');
            }
        });

        // Intensity selection handler
        expressionIntensityOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            expressionIntensityOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedIntensity = button.dataset.value;
        });

        // Generate button handler
        expressionGenerateBtn.addEventListener('click', async () => {
            if (!uploadedImageData) {
                alert('Silakan unggah foto terlebih dahulu');
                return;
            }

            // Determine expression
            let targetExpression = selectedExpression;
            if (selectedExpression === 'Custom' && expressionCustomInput) {
                const customExpr = expressionCustomInput.value.trim();
                targetExpression = customExpr || 'Happy Smiling';
            }

            // Map intensity to description
            const intensityMap = {
                'Subtle': 'subtle and gentle',
                'Moderate': 'natural and balanced',
                'Strong': 'strong and pronounced'
            };
            const intensityDescription = intensityMap[selectedIntensity];

            // Create detailed prompt for expression change
            const prompt = `Transform the facial expression of the person in this photo to show a ${intensityDescription} "${targetExpression}" expression.

CRITICAL REQUIREMENTS:
- Keep the person's identity, face structure, and all facial features EXACTLY the same
- ONLY change the facial expression (eyes, mouth, eyebrows positioning)
- Do NOT change: skin tone, face shape, hair, clothing, background, or any other aspect
- The expression should look completely natural and realistic, as if the person naturally made this expression
- Maintain the same lighting, angle, and photo quality
- The person must be clearly recognizable as the same individual
- Expression intensity: ${intensityDescription}

The result should be a natural-looking photo where only the facial expression has changed to "${targetExpression}" while everything else remains identical to the original.`;

            await generateExpressionSet(prompt);
        });

        async function generateExpressionSet(prompt) {
            const originalBtnHTML = expressionGenerateBtn.innerHTML;
            expressionGenerateBtn.disabled = true;
            expressionGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Mengubah Ekspresi...';

            expressionResultsPlaceholder.classList.add('hidden');

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                expressionResultsGrid.innerHTML = '';
                expressionResultsContainer.classList.remove('hidden');

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `expression-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-[3/4]';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-blue-600"></div>';
                    expressionResultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSingleExpression(i + 1, prompt)
                );
                await Promise.allSettled(generationPromises);

                expressionResultsGrid.querySelectorAll('[id^="expression-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                successCount = generatedImages.filter(Boolean).length;
            }

            expressionGenerateBtn.disabled = false;
            expressionGenerateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                expressionResultsContainer.classList.add('hidden');
                expressionResultsPlaceholder.classList.remove('hidden');
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            }
        }

        async function generateSingleExpression(index, basePrompt) {
            const card = document.getElementById(`expression-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const finalPrompt = `${basePrompt}\n\nThis is variation ${index}. Create a slightly different natural variation of the same expression (e.g., different smile intensity, eye openness, etc.) while maintaining the target expression type.`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: finalPrompt },
                            { inlineData: { mimeType: uploadedImageData.mimeType, data: uploadedImageData.base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: '3:4' }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (!base64Data) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const filename = `expression_${selectedExpression.replace(/\s+/g, '_').toLowerCase()}_${index}.png`;

                generatedImages[index - 1] = { url: imageUrl, filename: filename };

                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Expression ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-blue-500 to-cyan-500 hover:from-blue-600 hover:to-cyan-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
            } catch (error) {
                console.error(`Error generating expression ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        expressionResultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();

            const actionBtn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
            if (!actionBtn) return;

            const action = actionBtn.dataset.action;
            const index = parseInt(actionBtn.dataset.index, 10);
            const imageData = generatedImages[index];

            if (!imageData) return;

            if (action === 'preview') {
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageData.url);
                }
            } else if (action === 'download') {
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(imageData.url, imageData.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(imageData.url, imageData.filename);
                }
            }
        });

        // Download all functionality
        if (expressionDownloadAllBtn) {
            expressionDownloadAllBtn.addEventListener('click', async () => {
                if (generatedImages.length === 0) return;

                expressionDownloadAllBtn.disabled = true;
                expressionDownloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < generatedImages.length; i++) {
                    const img = generatedImages[i];
                    if (!img) continue;
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(img.url, img.filename);
                    }
                    await new Promise(resolve => setTimeout(resolve, 500));
                }

                expressionDownloadAllBtn.disabled = false;
                expressionDownloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
            });
        }
    })();

    // ==================== KARIKATUR GENERATOR ====================
    (function() {
        const apiKey = "";

        // Element references
        const karikaturInput = document.getElementById('karikatur-image-input');
        const karikaturImagePreview = document.getElementById('karikatur-image-preview');
        const karikaturUploadPrompt = document.getElementById('karikatur-upload-prompt');
        const karikaturMobilePreview = document.getElementById('karikatur-mobile-preview');
        const karikaturPreviewContainer = document.getElementById('karikatur-preview-container');
        const karikaturRemovePreview = document.getElementById('karikatur-remove-preview');
        const karikaturStyleOptions = document.getElementById('karikatur-style-options');
        const karikaturCustomStyleContainer = document.getElementById('karikatur-custom-style-container');
        const karikaturCustomStyle = document.getElementById('karikatur-custom-style');
        const karikaturExaggerationOptions = document.getElementById('karikatur-exaggeration-options');
        const karikaturBackgroundOptions = document.getElementById('karikatur-background-options');
        const karikaturBackgroundDetailContainer = document.getElementById('karikatur-background-detail-container');
        const karikaturBackgroundDetail = document.getElementById('karikatur-background-detail');
        const karikaturRatioOptions = document.getElementById('karikatur-ratio-options');
        const karikaturGenerateBtn = document.getElementById('karikatur-generate-btn');
        const karikaturPlaceholder = document.getElementById('karikatur-placeholder');
        const karikaturResultsGrid = document.getElementById('karikatur-results-grid');
        const karikaturDownloadAllBtn = document.getElementById('karikatur-download-all-btn');

        if (!karikaturInput) return;

        let uploadedImageData = null;
        let generatedImages = [];
        let selectedStyle = 'anime';
        let selectedExaggeration = 'moderate';
        let selectedBackground = 'transparent';
        let selectedRatio = '3:4';
        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const karikaturCountSel = document.getElementById('karikatur-count-selection-grid');
        if (karikaturCountSel) {
            karikaturCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                karikaturCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Style name mapping for prompts
        const styleDescriptions = {
            // Classic Animation Styles
            'anime': 'Japanese anime/manga style with large expressive eyes, clean linework, and vibrant colors',
            'cartoon': 'Western cartoon style with exaggerated features, bold outlines, and vibrant colors like Disney or Looney Tunes',
            'pixar': 'Pixar 3D animation style with smooth textures, appealing character design, and expressive features',
            'disney': 'Classic Disney animation style with elegant proportions, expressive eyes, and timeless charm',
            'ghibli': 'Studio Ghibli style with soft colors, dreamy atmosphere, hand-painted aesthetic, and gentle character design',
            'comic': 'Comic book art style with bold lines, dramatic shading, halftone dots, and dynamic poses',
            'marvel': 'Marvel superhero comic style with dynamic action poses, bold inking, and dramatic heroic proportions',
            'chibi': 'Cute chibi/kawaii style with oversized head, tiny body, and adorable proportions',

            // Art Styles
            'watercolor': 'Soft watercolor caricature with flowing colors, delicate brushstrokes, and artistic texture',
            'oilpainting': 'Oil painting style with rich textures, bold brushstrokes, and classical fine art aesthetic',
            'sketch': 'Hand-drawn sketch style with pencil lines, cross-hatching, and artistic imperfections',
            'lineart': 'Clean line art style with precise black outlines, minimal shading, and elegant simplicity',
            'popart': 'Pop art style with bright colors, bold outlines, Ben-Day dots, and high contrast like Andy Warhol',
            'graffiti': 'Urban graffiti street art style with bold colors, spray paint textures, and edgy hip-hop aesthetic',
            'crayon-anak': 'Turn the entire image into a cute crayon drawing illustration, as if drawn by a 10-year-old child on plain white paper. Simplify the details and shapes with imperfect hand-drawn lines, slightly wavy proportions, and innocent childlike creativity. Do not use the original image colors literally — replace them with soft pastel and colorful crayon tones. The artwork should feel handmade, textured, and naturally messy like real wax crayon strokes on paper. Add adorable childlike doodles such as flowers, stars, hearts, candy, clouds, rainbows, smiley suns, and tiny sparkles around the composition. Keep the overall aesthetic warm, whimsical, innocent, and nostalgic, with a cozy kindergarten art feeling. Visible crayon texture, minimal shading, cute and dreamy atmosphere, naive storytelling energy, ultra charming and playful.',

            // Digital Styles
            'digital': 'Modern digital art caricature with clean rendering, smooth gradients, and polished finish',
            'vector': 'Clean vector art style with smooth shapes, flat colors, and geometric precision',
            'minimalist': 'Minimalist style with simple shapes, limited color palette, and essential features only',
            'geometric': 'Geometric abstract style with angular shapes, polygonal forms, and modern design',
            'neon': 'Neon glow style with vibrant luminous colors, electric outlines, and futuristic cyberpunk aesthetic',
            'cyberpunk': 'Cyberpunk futuristic style with neon accents, tech elements, and sci-fi urban atmosphere',

            // Modern Social Media Styles
            'instagram': 'Instagram-optimized style with trendy filters, perfect lighting, aesthetic colors, and social media appeal',
            'tiktok': 'TikTok viral style with energetic colors, fun elements, Gen Z aesthetics, and shareable appeal',
            'sticker': 'Cute sticker style with thick outlines, bright colors, and fun decorative elements perfect for messaging apps',
            'emoji': 'Emoji-like style with simple rounded shapes, bright cheerful colors, and expressive minimalist features',

            // Classic Art Styles
            'retro': 'Retro vintage style with aged colors, classic 50s-80s aesthetics, and nostalgic charm',
            'noir': 'Film noir black and white style with dramatic shadows, high contrast, and classic Hollywood mystery atmosphere',
            'pastel': 'Soft pastel style with gentle colors, dreamy atmosphere, and delicate romantic aesthetic',
            'monochrome': 'Monochrome black and white style with strong contrast, dramatic shadows, and timeless elegance',

            // Fantasy & Special
            'fantasy': 'Fantasy art style with magical elements, ethereal lighting, and epic illustration quality',
            'gothic': 'Gothic dark style with dramatic shadows, mysterious atmosphere, and elegant dark aesthetic',
            'kawaii': 'Kawaii Japanese cute style with pastel colors, adorable features, and irresistibly charming appeal',
            'realistic': 'Realistic caricature maintaining photographic quality while exaggerating distinctive features',

            // Portrait Styles
            'portrait': 'Professional portrait art style with refined features, elegant composition, and gallery-quality finish',
            'editorial': 'Editorial magazine style with sophisticated lighting, fashion-forward aesthetic, and publication quality',
            'vogue': 'Vogue fashion style with high-end glamour, dramatic posing, and luxury magazine aesthetic',

            // Indonesian & Cultural Styles
            'wayang': 'Indonesian Wayang puppet style with traditional ornate patterns, elegant silhouettes, and cultural heritage aesthetic',
            'batik': 'Indonesian Batik pattern style with intricate traditional motifs, rich cultural details, and artistic fabric design elements',

            // Special Request
            'flatcartoon': 'Transform this image into a flat vector digital illustration style, precisely maintaining the original subjects, poses, and composition. The new style must feature bold clean black outlines around everything, solid flat color fills with no gradients or textures, and simplified cartoon faces with dot eyes and distinct rosy pink blush marks on the cheeks.'
        };

        // Setup image upload with HEIC support
        karikaturInput.addEventListener('change', async (e) => {
            const file = e.target.files[0];
            if (!file) return;

            try {
                let processedFile = file;

                // Convert HEIC to JPEG if necessary
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    const heic2any = window.heic2any;
                    if (heic2any) {
                        const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }

                const reader = new FileReader();
                reader.onload = (event) => {
                    const dataUrl = event.target.result;
                    const base64 = dataUrl.split(',')[1];

                    uploadedImageData = {
                        base64: base64,
                        mimeType: processedFile.type,
                        dataUrl: dataUrl
                    };

                    // Show desktop preview
                    karikaturImagePreview.src = dataUrl;
                    karikaturUploadPrompt.classList.add('hidden');
                    karikaturImagePreview.classList.remove('hidden');

                    // Show mobile preview
                    if (karikaturMobilePreview && karikaturPreviewContainer) {
                        karikaturMobilePreview.src = dataUrl;
                        karikaturPreviewContainer.classList.remove('hidden');
                    }

                    karikaturGenerateBtn.disabled = false;
                };
                reader.readAsDataURL(processedFile);
            } catch (error) {
                console.error('Error processing image:', error);
                alert('Gagal memproses gambar. Silakan coba lagi.');
            }
        });

        // Remove image handler
        if (karikaturRemovePreview) {
            karikaturRemovePreview.addEventListener('click', () => {
                uploadedImageData = null;
                karikaturInput.value = '';
                karikaturImagePreview.src = '';
                karikaturImagePreview.classList.add('hidden');
                karikaturUploadPrompt.classList.remove('hidden');

                if (karikaturPreviewContainer) {
                    karikaturPreviewContainer.classList.add('hidden');
                }
                if (karikaturMobilePreview) {
                    karikaturMobilePreview.src = '';
                }

                karikaturGenerateBtn.disabled = true;

                // Reset results
                karikaturPlaceholder.classList.remove('hidden');
                karikaturResultsGrid.classList.add('hidden');
                karikaturResultsGrid.innerHTML = '';
                karikaturDownloadAllBtn.classList.add('hidden');
                generatedImages = [];
            });
        }

        // Style selection handler
        karikaturStyleOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            karikaturStyleOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedStyle = button.dataset.style;

            // Show/hide custom style input
            if (selectedStyle === 'custom') {
                karikaturCustomStyleContainer.classList.remove('hidden');
            } else {
                karikaturCustomStyleContainer.classList.add('hidden');
            }
        });

        // Exaggeration level selection handler
        karikaturExaggerationOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            karikaturExaggerationOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedExaggeration = button.dataset.exaggeration;
        });

        // Background selection handler
        karikaturBackgroundOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            karikaturBackgroundOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedBackground = button.dataset.background;

            // Show/hide background detail input
            if (selectedBackground !== 'transparent') {
                karikaturBackgroundDetailContainer.classList.remove('hidden');
            } else {
                karikaturBackgroundDetailContainer.classList.add('hidden');
            }
        });

        // Aspect ratio selection handler
        karikaturRatioOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            karikaturRatioOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedRatio = button.dataset.ratio;
        });

        // Generate button handler
        karikaturGenerateBtn.addEventListener('click', async () => {
            if (!uploadedImageData) {
                alert('Silakan unggah foto terlebih dahulu');
                return;
            }

            // Determine style description
            let styleDesc = styleDescriptions[selectedStyle];
            if (selectedStyle === 'custom' && karikaturCustomStyle) {
                const customStyleText = karikaturCustomStyle.value.trim();
                styleDesc = customStyleText || styleDescriptions['anime'];
            }

            // Map exaggeration level to description
            const exaggerationMap = {
                'subtle': 'subtle exaggeration maintaining realistic proportions with slight enhancement of distinctive features',
                'moderate': 'balanced exaggeration that clearly emphasizes unique facial features while remaining appealing',
                'extreme': 'strong, dramatic exaggeration with highly emphasized features for maximum caricature effect'
            };
            const exaggerationDesc = exaggerationMap[selectedExaggeration];

            // Background instructions
            let backgroundInstructions = '';
            if (selectedBackground === 'transparent') {
                backgroundInstructions = 'transparent or simple plain background';
            } else if (selectedBackground === 'solid') {
                const bgDetail = karikaturBackgroundDetail?.value.trim() || 'solid vibrant color';
                backgroundInstructions = `solid background with ${bgDetail}`;
            } else if (selectedBackground === 'gradient') {
                const bgDetail = karikaturBackgroundDetail?.value.trim() || 'smooth colorful gradient';
                backgroundInstructions = `gradient background with ${bgDetail}`;
            } else if (selectedBackground === 'scene') {
                const bgDetail = karikaturBackgroundDetail?.value.trim() || 'simple contextual scene';
                backgroundInstructions = `background scene featuring ${bgDetail}`;
            }

            // Create comprehensive prompt
            // crayon-anak bypasses caricature wrapper: wrapper demands "polished/clean lines/identity preservation"
            // which directly contradicts childlike crayon's "imperfect/wavy/naive" aesthetic. Use standalone prompt.
            const prompt = selectedStyle === 'crayon-anak'
                ? styleDesc
                : `Create a professional caricature of the person in this photo with the following specifications:

STYLE REQUIREMENT:
${styleDesc}

EXAGGERATION LEVEL:
${exaggerationDesc}
- Emphasize the most distinctive facial features (nose, eyes, mouth, chin, forehead, etc.)
- Maintain recognizability - the person should still be clearly identifiable
- Create an appealing, flattering interpretation even with exaggeration

BACKGROUND:
${backgroundInstructions}

CRITICAL QUALITY REQUIREMENTS:
1. PROFESSIONAL QUALITY: High-resolution, polished, publication-ready artwork
2. IDENTITY PRESERVATION: Person must be clearly recognizable despite stylization
3. APPEALING RESULT: Create a flattering, attractive caricature that subjects would love
4. STYLE CONSISTENCY: Maintain consistent ${selectedStyle} artistic style throughout
5. NATURAL PROPORTIONS: Even with exaggeration, maintain overall balance and appeal
6. CLEAN EXECUTION: Sharp details, clean lines, no artifacts or distortions
7. EXPRESSIVE CHARACTER: Capture the person's personality and unique characteristics
8. COLOR HARMONY: Use appealing, well-balanced color palette appropriate for the style

TECHNICAL SPECIFICATIONS:
- Keep the person's identity recognizable
- Emphasize distinctive features through ${selectedExaggeration} exaggeration
- Apply ${selectedStyle} artistic treatment consistently
- Ensure professional quality suitable for portfolio or commercial use

Generate a stunning, professional caricature that the subject would be proud to share and display.`;

            await generateKarikaturSet(prompt);
        });

        async function generateKarikaturSet(prompt) {
            const originalBtnHTML = karikaturGenerateBtn.innerHTML;
            karikaturGenerateBtn.disabled = true;
            karikaturGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Membuat Karikatur...';

            karikaturPlaceholder.classList.add('hidden');
            karikaturResultsGrid.classList.remove('hidden');
            karikaturDownloadAllBtn.classList.add('hidden');

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                karikaturResultsGrid.innerHTML = '';

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `karikatur-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gradient-to-br from-orange-100 to-yellow-100 flex items-center justify-center aspect-[3/4]';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-orange-600"></div>';
                    karikaturResultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSingleKarikatur(i + 1, prompt)
                );
                await Promise.allSettled(generationPromises);

                karikaturResultsGrid.querySelectorAll('[id^="karikatur-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });

                successCount = generatedImages.filter(Boolean).length;
            }

            karikaturGenerateBtn.disabled = false;
            karikaturGenerateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                karikaturDownloadAllBtn.classList.remove('hidden');
            }
        }

        async function generateSingleKarikatur(index, basePrompt) {
            const card = document.getElementById(`karikatur-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const finalPrompt = `${basePrompt}\n\nVariation ${index}: Create a unique interpretation with slightly different exaggeration emphasis or styling details while maintaining the core style and requirements.`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: finalPrompt },
                            { inlineData: { mimeType: uploadedImageData.mimeType, data: uploadedImageData.base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data in response');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const filename = `karikatur_${selectedStyle}_${selectedExaggeration}_${index}.png`;

                generatedImages[index - 1] = { url: imageUrl, filename: filename };

                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Karikatur ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-orange-500 to-yellow-500 hover:from-orange-600 hover:to-yellow-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
            } catch (error) {
                console.error(`Error generating karikatur ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons - Mobile friendly
        karikaturResultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();

            const actionBtn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
            if (!actionBtn) return;

            const action = actionBtn.dataset.action;
            const index = parseInt(actionBtn.dataset.index, 10);
            const imageData = generatedImages[index];

            if (!imageData) return;

            if (action === 'preview') {
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageData.url);
                }
            } else if (action === 'download') {
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(imageData.url, imageData.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(imageData.url, imageData.filename);
                }
            }
        });

        // Download all functionality with proper error handling
        if (karikaturDownloadAllBtn) {
            karikaturDownloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || karikaturDownloadAllBtn.disabled) return;

                const originalHTML = karikaturDownloadAllBtn.innerHTML;
                karikaturDownloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        karikaturDownloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    karikaturDownloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        karikaturDownloadAllBtn.disabled = false;
                        karikaturDownloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    karikaturDownloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        karikaturDownloadAllBtn.disabled = false;
                        karikaturDownloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }
    })();

    // ==================== STUDIO PROFIL SOSMED ====================
    (function() {
        const apiKey = "";

        const imageInput = document.getElementById('studio-profil-image-input');
        const imagePreview = document.getElementById('studio-profil-image-preview');
        const placeholder = document.getElementById('studio-profil-placeholder');
        const removeBtn = document.getElementById('studio-profil-remove-btn');

        const screenshotInput = document.getElementById('studio-profil-screenshot-input');
        const screenshotPreview = document.getElementById('studio-profil-screenshot-preview');
        const screenshotPlaceholder = document.getElementById('studio-profil-screenshot-placeholder');
        const screenshotRemoveBtn = document.getElementById('studio-profil-screenshot-remove-btn');

        const platformOptions = document.getElementById('studio-profil-platform-options');
        const ratioOptions = document.getElementById('studio-profil-ratio-options');
        const countSelGrid = document.getElementById('studio-profil-count-selection-grid');

        const characterNameInput = document.getElementById('studio-profil-character-name-input');
        const bioInput = document.getElementById('studio-profil-bio-input');
        const followersInput = document.getElementById('studio-profil-followers-input');

        const templateOptions = document.getElementById('studio-profil-template-options');

        const generateBtn = document.getElementById('studio-profil-generate-btn');
        const downloadAllBtn = document.getElementById('studio-profil-download-all-btn');
        const resultsGrid = document.getElementById('studio-profil-results-grid');
        const emptyState = document.getElementById('studio-profil-empty-state');

        let uploadedImageData = null;
        let uploadedScreenshotData = null;
        let selectedTemplate = 'torn-paper-breakout';
        let selectedPlatform = 'instagram';
        let selectedRatio = '9:16';
        let selectedCount = 4;
        let generatedImages = [];

        const platformDescriptions = {
            'instagram': 'Instagram profile interface with circular profile picture frame, gradient pink-purple-orange Instagram brand colors, "Follow" button, Instagram heart and message icons, 3-column grid of post thumbnails partially visible behind the torn paper, recognizable Instagram UI design language',
            'tiktok': 'TikTok profile interface with circular profile picture, dark black background with cyan and pink neon accents, "Follow" button styled like TikTok, music note icons and video play counters, vertical short-video thumbnails partially visible behind the torn paper, recognizable TikTok UI design language',
            'youtube': 'YouTube channel page interface with circular profile picture, white background with red YouTube brand accents, large red "Subscribe" button with bell icon, video thumbnails with red play buttons arranged horizontally behind the torn paper, recognizable YouTube UI design language',
            'twitter': 'X (formerly Twitter) profile interface with circular profile picture, dark mode black background with white text, black "Follow" button, X logo, tweet timeline with reply/retweet/like icons partially visible behind the torn paper, recognizable X/Twitter UI design language',
            'facebook': 'Facebook profile interface with circular profile picture, white background with blue Facebook brand accents, blue "Add Friend" and "Message" buttons, posts feed with like/comment/share icons partially visible behind the torn paper, recognizable Facebook UI design language'
        };

        function checkReady() {
            const nameOk = characterNameInput.value.trim().length > 0;
            generateBtn.disabled = !(uploadedImageData && nameOk);
        }

        if (countSelGrid) {
            countSelGrid.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                countSelGrid.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        platformOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('button[data-value]');
            if (!btn) return;
            platformOptions.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
            btn.classList.add('selected');
            selectedPlatform = btn.dataset.value;
        });

        // Template selector — disabled buttons (Coming Soon) ignored via .closest filtering on :not([disabled]) below
        templateOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('button[data-value]:not([disabled])');
            if (!btn) return;
            templateOptions.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
            btn.classList.add('selected');
            selectedTemplate = btn.dataset.value;
        });

        ratioOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('button[data-value]');
            if (!btn) return;
            ratioOptions.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
            btn.classList.add('selected');
            selectedRatio = btn.dataset.value;
        });

        characterNameInput.addEventListener('input', checkReady);

        imageInput.addEventListener('change', async (e) => {
            const file = e.target.files[0];
            if (!file) return;
            try {
                let processedFile = file;
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    if (window.heic2any) {
                        const blob = await window.heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([blob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }
                const reader = new FileReader();
                reader.onload = (ev) => {
                    const dataUrl = ev.target.result;
                    uploadedImageData = {
                        base64: dataUrl.split(',')[1],
                        mimeType: processedFile.type,
                        dataUrl: dataUrl
                    };
                    imagePreview.src = dataUrl;
                    imagePreview.classList.remove('hidden');
                    placeholder.classList.add('hidden');
                    removeBtn.classList.remove('hidden');
                    checkReady();
                };
                reader.readAsDataURL(processedFile);
            } catch (err) {
                console.error('Studio Profil image processing failed:', err);
                alert('Gagal memproses gambar. Silakan coba lagi.');
            }
        });

        removeBtn.addEventListener('click', () => {
            uploadedImageData = null;
            imageInput.value = '';
            imagePreview.src = '';
            imagePreview.classList.add('hidden');
            placeholder.classList.remove('hidden');
            removeBtn.classList.add('hidden');
            checkReady();
        });

        // Optional reference profile screenshot — when provided, AI uses real layout instead of imagining from platformDescriptions
        screenshotInput.addEventListener('change', async (e) => {
            const file = e.target.files[0];
            if (!file) return;
            try {
                let processedFile = file;
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    if (window.heic2any) {
                        const blob = await window.heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([blob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }
                const reader = new FileReader();
                reader.onload = (ev) => {
                    const dataUrl = ev.target.result;
                    uploadedScreenshotData = {
                        base64: dataUrl.split(',')[1],
                        mimeType: processedFile.type,
                        dataUrl: dataUrl
                    };
                    screenshotPreview.src = dataUrl;
                    screenshotPreview.classList.remove('hidden');
                    screenshotPlaceholder.classList.add('hidden');
                    screenshotRemoveBtn.classList.remove('hidden');
                };
                reader.readAsDataURL(processedFile);
            } catch (err) {
                console.error('Studio Profil screenshot processing failed:', err);
                alert('Gagal memproses screenshot. Silakan coba lagi.');
            }
        });

        screenshotRemoveBtn.addEventListener('click', () => {
            uploadedScreenshotData = null;
            screenshotInput.value = '';
            screenshotPreview.src = '';
            screenshotPreview.classList.add('hidden');
            screenshotPlaceholder.classList.remove('hidden');
            screenshotRemoveBtn.classList.add('hidden');
        });

        // Template registry — each template builder takes a context object and returns a full prompt.
        // Adding new template: implement function here, register in `templateBuilders`, enable button in HTML.
        const templateBuilders = {
            'torn-paper-breakout': (ctx) => {
                const { characterName, bio, followers, platformDesc, hasScreenshot, selectedPlatform } = ctx;
                const bioLine = bio ? `, with bio text reading "${bio}"` : '';
                const followersLine = followers ? `, displaying "${followers} followers" prominently` : ', displaying realistic follower count statistics';

                // Two-image mode: image #1 = character subject, image #2 = real profile screenshot reference.
                // Anchor must be EXPLICIT — without it AI may swap roles or merge images into one chimera.
                const subjectAnchor = hasScreenshot
                    ? `IMPORTANT — Two reference images provided:
- FIRST image is the CHARACTER SUBJECT (use this for face, identity, body)
- SECOND image is the SOCIAL MEDIA PROFILE INTERFACE (use this as the EXACT layout being torn through, including username display, follower count, posts grid, banner image — preserve real text and layout from this screenshot as faithfully as possible)

Medium cinematic shot of the character from the FIRST image, a highly detailed and recognizable figure with accurate facial features matching the FIRST reference photo, styled to match their real-life persona.`
                    : `Medium cinematic shot, ${characterName}, a highly detailed and recognizable figure with accurate facial features matching the uploaded reference photo, styled to match their real-life persona.`;

                const interfaceLine = hasScreenshot
                    ? `The character is breaking through a torn paper effect from the ${selectedPlatform} profile shown in the SECOND reference image — render the SECOND image as the actual UI being torn, preserving its exact username, profile picture, follower numbers, posts/videos thumbnails, and overall layout. The character grips the ripped edges of the torn paper with both hands as if physically tearing through the screen interface. The torn opening reveals a deep dark background behind them, creating strong depth and dimensional contrast between the bright interface in the foreground and the dark void behind.`
                    : `The character is breaking through a torn paper effect from a ${platformDesc}, gripping the ripped edges of the torn paper with both hands as if physically tearing through the screen interface. The torn opening reveals a deep dark background behind them, creating strong depth and dimensional contrast between the bright interface in the foreground and the dark void behind.

The interface displays a clean minimal social media profile layout including profile picture, username "${characterName}", subscribe/follow button, bio text${bioLine}${followersLine} and follower statistics. The top banner features a black-and-white close-up of eyes, adding a dramatic visual hook.`;

                return `${subjectAnchor} Expression is surprised and expressive, with wide eyes and slightly open mouth, capturing a spontaneous breakout moment.

${interfaceLine}

Soft cinematic lighting with realistic shadows, subtle glow from the UI illuminating the character's face and hands. Depth-of-field, ultra-realistic textures, sharp focus on the subject, smooth background blur.

Cinematic composition, vibrant color contrast between subject and interface, trending AI art style, 3D photo manipulation, hyper-realistic rendering, 4K resolution, highly detailed.`;
            }
            // Future templates registered here:
            //   'phone-screen-popout': (ctx) => `...`,
            //   'cover-hero-portrait': (ctx) => `...`,
        };

        function buildPrompt() {
            const ctx = {
                characterName: characterNameInput.value.trim() || 'Character',
                bio: bioInput.value.trim(),
                followers: followersInput.value.trim(),
                platformDesc: platformDescriptions[selectedPlatform] || platformDescriptions['instagram'],
                hasScreenshot: !!uploadedScreenshotData,
                selectedPlatform: selectedPlatform
            };
            const builder = templateBuilders[selectedTemplate] || templateBuilders['torn-paper-breakout'];
            return builder(ctx);
        }

        generateBtn.addEventListener('click', async () => {
            if (!uploadedImageData) {
                alert('Silakan upload foto karakter dulu.');
                return;
            }
            if (!characterNameInput.value.trim()) {
                alert('Silakan isi nama karakter / username dulu.');
                return;
            }
            const prompt = buildPrompt();
            await generateStudioProfilSet(prompt);
        });

        async function generateStudioProfilSet(prompt) {
            const originalBtnHTML = generateBtn.innerHTML;
            generateBtn.disabled = true;
            generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i><span data-i18n="sp.generate-progress">Membuat foto...</span>';

            emptyState.classList.add('hidden');
            resultsGrid.classList.remove('hidden');
            downloadAllBtn.classList.add('hidden');

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                resultsGrid.innerHTML = '';

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `studio-profil-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gradient-to-br from-indigo-100 to-purple-100 flex items-center justify-center aspect-[3/4]';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-indigo-600"></div>';
                    resultsGrid.appendChild(card);
                }

                const promises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSingleStudioProfil(i + 1, prompt)
                );
                await Promise.allSettled(promises);

                resultsGrid.querySelectorAll('[id^="studio-profil-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });

                successCount = generatedImages.filter(Boolean).length;
            }

            generateBtn.disabled = false;
            generateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                downloadAllBtn.classList.remove('hidden');
            }
        }

        async function generateSingleStudioProfil(index, basePrompt) {
            const card = document.getElementById(`studio-profil-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const finalPrompt = `${basePrompt}\n\nVariation ${index}: Create a unique interpretation with slightly different camera angle, hand position on torn edges, or expression intensity while keeping the breaking-through-profile composition.`;

                // Multi-image input: when screenshot uploaded, send character photo (FIRST) + profile screenshot (SECOND).
                // Order matters — prompt anchors "FIRST image" / "SECOND image" so AI knows which role each plays.
                const parts = [{ text: finalPrompt }];
                parts.push({ inlineData: { mimeType: uploadedImageData.mimeType, data: uploadedImageData.base64 } });
                if (uploadedScreenshotData) {
                    parts.push({ inlineData: { mimeType: uploadedScreenshotData.mimeType, data: uploadedScreenshotData.base64 } });
                }

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data in response');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const safeName = (characterNameInput.value.trim() || 'character').replace(/[^a-z0-9]/gi, '_').toLowerCase();
                const filename = `studio_profil_${selectedPlatform}_${safeName}_${index}.png`;

                generatedImages[index - 1] = { url: imageUrl, filename: filename };

                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Studio Profil ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="action-btn text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2" style="background: linear-gradient(135deg, #6366f1 0%, #a855f7 100%);">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
            } catch (error) {
                console.error(`Error generating studio profil ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        resultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();
            const btn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
            if (!btn) return;
            const action = btn.dataset.action;
            const idx = parseInt(btn.dataset.index, 10);
            const img = generatedImages[idx];
            if (!img) return;

            if (action === 'preview' && window.showPreviewModal) {
                window.showPreviewModal(img.url);
            } else if (action === 'download') {
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(img.url, img.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(img.url, img.filename);
                }
            }
        });

        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();
                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;
                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;
                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-1"></i>Downloading ${i + 1}/${generatedImages.length}...`;
                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(r => setTimeout(r, 500));
                        } catch (err) {
                            console.error(`Download ${i + 1} failed:`, err);
                        }
                    }
                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-1"></i>Selesai!';
                    setTimeout(() => { downloadAllBtn.disabled = false; downloadAllBtn.innerHTML = originalHTML; }, 2000);
                } catch (err) {
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-1"></i>Error';
                    setTimeout(() => { downloadAllBtn.disabled = false; downloadAllBtn.innerHTML = originalHTML; }, 2000);
                }
            });
        }
    })();

    // ==================== PHOTO EDITOR PRO ====================
    (function() {
        // Element references
        const photoeditorInput = document.getElementById('photoeditor-image-input');
        const photoeditorImagePreview = document.getElementById('photoeditor-image-preview');
        const photoeditorUploadPrompt = document.getElementById('photoeditor-upload-prompt');
        const photoeditorMobilePreview = document.getElementById('photoeditor-mobile-preview');
        const photoeditorPreviewContainer = document.getElementById('photoeditor-preview-container');
        const photoeditorRemovePreview = document.getElementById('photoeditor-remove-preview');
        const photoeditorPresetOptions = document.getElementById('photoeditor-preset-options');
        const photoeditorPlaceholder = document.getElementById('photoeditor-placeholder');
        const photoeditorPreviewArea = document.getElementById('photoeditor-preview-area');
        const photoeditorCanvas = document.getElementById('photoeditor-canvas');
        const photoeditorResetBtn = document.getElementById('photoeditor-reset-btn');
        const photoeditorDownloadBtn = document.getElementById('photoeditor-download-btn');

        // Adjustment sliders
        const brightnessSlider = document.getElementById('photoeditor-brightness');
        const contrastSlider = document.getElementById('photoeditor-contrast');
        const saturationSlider = document.getElementById('photoeditor-saturation');
        const warmthSlider = document.getElementById('photoeditor-warmth');
        const highlightsSlider = document.getElementById('photoeditor-highlights');
        const shadowsSlider = document.getElementById('photoeditor-shadows');
        const sharpnessSlider = document.getElementById('photoeditor-sharpness');
        const vignetteSlider = document.getElementById('photoeditor-vignette');

        // Value displays
        const brightnessValue = document.getElementById('photoeditor-brightness-value');
        const contrastValue = document.getElementById('photoeditor-contrast-value');
        const saturationValue = document.getElementById('photoeditor-saturation-value');
        const warmthValue = document.getElementById('photoeditor-warmth-value');
        const highlightsValue = document.getElementById('photoeditor-highlights-value');
        const shadowsValue = document.getElementById('photoeditor-shadows-value');
        const sharpnessValue = document.getElementById('photoeditor-sharpness-value');
        const vignetteValue = document.getElementById('photoeditor-vignette-value');

        if (!photoeditorInput) return;

        let originalImage = null;
        let ctx = null;
        let selectedPreset = 'none';
        let adjustments = {
            brightness: 0,
            contrast: 0,
            saturation: 0,
            warmth: 0,
            highlights: 0,
            shadows: 0,
            sharpness: 0,
            vignette: 0
        };

        // Preset configurations
        const presets = {
            'none': { brightness: 0, contrast: 0, saturation: 0, warmth: 0, highlights: 0, shadows: 0, sharpness: 0, vignette: 0 },
            'cinematic': { brightness: -10, contrast: 15, saturation: -10, warmth: 5, highlights: -15, shadows: 10, sharpness: 10, vignette: 20 },
            'vintage': { brightness: 5, contrast: -10, saturation: -20, warmth: 25, highlights: -10, shadows: -5, sharpness: -10, vignette: 30 },
            'moody': { brightness: -15, contrast: 20, saturation: -15, warmth: -10, highlights: -20, shadows: 15, sharpness: 5, vignette: 35 },
            'vibrant': { brightness: 10, contrast: 15, saturation: 30, warmth: 0, highlights: 5, shadows: -5, sharpness: 15, vignette: 0 },
            'warm': { brightness: 8, contrast: 5, saturation: 10, warmth: 30, highlights: 0, shadows: 0, sharpness: 5, vignette: 15 },
            'cool': { brightness: 5, contrast: 10, saturation: 5, warmth: -30, highlights: 5, shadows: 0, sharpness: 10, vignette: 10 },
            'bw': { brightness: 0, contrast: 20, saturation: -100, warmth: 0, highlights: -10, shadows: 10, sharpness: 15, vignette: 20 },
            'sepia': { brightness: 5, contrast: 10, saturation: -30, warmth: 40, highlights: 0, shadows: 5, sharpness: 0, vignette: 25 },
            'dramatic': { brightness: -10, contrast: 30, saturation: 10, warmth: 0, highlights: -25, shadows: 20, sharpness: 20, vignette: 40 },
            'soft': { brightness: 10, contrast: -15, saturation: -10, warmth: 10, highlights: 10, shadows: -10, sharpness: -20, vignette: 15 },
            'hdr': { brightness: 5, contrast: 25, saturation: 20, warmth: 0, highlights: -30, shadows: 30, sharpness: 25, vignette: 0 },
            'golden': { brightness: 10, contrast: 10, saturation: 15, warmth: 35, highlights: 5, shadows: -5, sharpness: 10, vignette: 20 },
            'film': { brightness: -5, contrast: 15, saturation: -5, warmth: 10, highlights: -5, shadows: 5, sharpness: -5, vignette: 25 },
            'neon': { brightness: 15, contrast: 25, saturation: 50, warmth: -15, highlights: 10, shadows: -10, sharpness: 20, vignette: 0 },
            'pastel': { brightness: 15, contrast: -10, saturation: -15, warmth: 5, highlights: 15, shadows: -15, sharpness: -10, vignette: 10 },
            'urban': { brightness: -5, contrast: 20, saturation: 5, warmth: -5, highlights: -10, shadows: 15, sharpness: 15, vignette: 25 },
            'sunset': { brightness: 5, contrast: 15, saturation: 20, warmth: 40, highlights: -10, shadows: 5, sharpness: 10, vignette: 30 },
            'portrait': { brightness: 10, contrast: 10, saturation: 5, warmth: 15, highlights: -5, shadows: -10, sharpness: 20, vignette: 20 },
            'landscape': { brightness: 5, contrast: 15, saturation: 15, warmth: 5, highlights: -10, shadows: 10, sharpness: 25, vignette: 10 }
        };

        // Setup image upload with HEIC support
        photoeditorInput.addEventListener('change', async (e) => {
            const file = e.target.files[0];
            if (!file) return;

            try {
                let processedFile = file;

                // Convert HEIC to JPEG if necessary
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    const heic2any = window.heic2any;
                    if (heic2any) {
                        const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }

                const reader = new FileReader();
                reader.onload = (event) => {
                    const img = new Image();
                    img.onload = () => {
                        originalImage = img;
                        setupCanvas();
                        applyEdits();

                        // Show desktop preview
                        photoeditorImagePreview.src = event.target.result;
                        photoeditorUploadPrompt.classList.add('hidden');
                        photoeditorImagePreview.classList.remove('hidden');

                        // Show mobile preview
                        if (photoeditorMobilePreview && photoeditorPreviewContainer) {
                            photoeditorMobilePreview.src = event.target.result;
                            photoeditorPreviewContainer.classList.remove('hidden');
                        }

                        // Show preview area
                        photoeditorPlaceholder.classList.add('hidden');
                        photoeditorPreviewArea.classList.remove('hidden');
                    };
                    img.src = event.target.result;
                };
                reader.readAsDataURL(processedFile);
            } catch (error) {
                console.error('Error processing image:', error);
                alert('Gagal memproses gambar. Silakan coba lagi.');
            }
        });

        // Remove image handler
        if (photoeditorRemovePreview) {
            photoeditorRemovePreview.addEventListener('click', () => {
                originalImage = null;
                photoeditorInput.value = '';
                photoeditorImagePreview.src = '';
                photoeditorImagePreview.classList.add('hidden');
                photoeditorUploadPrompt.classList.remove('hidden');

                if (photoeditorPreviewContainer) {
                    photoeditorPreviewContainer.classList.add('hidden');
                }
                if (photoeditorMobilePreview) {
                    photoeditorMobilePreview.src = '';
                }

                photoeditorPlaceholder.classList.remove('hidden');
                photoeditorPreviewArea.classList.add('hidden');
            });
        }

        // Setup canvas
        function setupCanvas() {
            if (!originalImage) return;

            // Use ORIGINAL image dimensions (no scaling)
            // Canvas internal resolution = original photo resolution
            photoeditorCanvas.width = originalImage.width;
            photoeditorCanvas.height = originalImage.height;
            ctx = photoeditorCanvas.getContext('2d', { willReadFrequently: true });

            // CSS handles display scaling automatically (defined in stylesheet)
            // Canvas will display scaled down to fit, but data remains full resolution
        }

        // Apply all edits
        function applyEdits() {
            if (!originalImage || !ctx) return;

            // Clear canvas
            ctx.clearRect(0, 0, photoeditorCanvas.width, photoeditorCanvas.height);

            // Draw original image at FULL RESOLUTION
            ctx.drawImage(originalImage, 0, 0, originalImage.width, originalImage.height);

            // Get image data
            const imageData = ctx.getImageData(0, 0, photoeditorCanvas.width, photoeditorCanvas.height);
            const data = imageData.data;

            // Apply adjustments
            for (let i = 0; i < data.length; i += 4) {
                let r = data[i];
                let g = data[i + 1];
                let b = data[i + 2];

                // Brightness
                if (adjustments.brightness !== 0) {
                    const brightness = adjustments.brightness * 2.55;
                    r += brightness;
                    g += brightness;
                    b += brightness;
                }

                // Contrast
                if (adjustments.contrast !== 0) {
                    const factor = (259 * (adjustments.contrast + 255)) / (255 * (259 - adjustments.contrast));
                    r = factor * (r - 128) + 128;
                    g = factor * (g - 128) + 128;
                    b = factor * (b - 128) + 128;
                }

                // Saturation
                if (adjustments.saturation !== 0) {
                    const gray = 0.2989 * r + 0.587 * g + 0.114 * b;
                    const satFactor = 1 + adjustments.saturation / 100;
                    r = gray + satFactor * (r - gray);
                    g = gray + satFactor * (g - gray);
                    b = gray + satFactor * (b - gray);
                }

                // Warmth (Temperature)
                if (adjustments.warmth !== 0) {
                    const warmth = adjustments.warmth / 100;
                    r += warmth * 50;
                    b -= warmth * 30;
                }

                // Highlights & Shadows
                const luminance = 0.2126 * r + 0.7152 * g + 0.0722 * b;
                if (adjustments.highlights !== 0 && luminance > 127) {
                    const factor = 1 + (adjustments.highlights / 100) * ((luminance - 127) / 128);
                    r *= factor;
                    g *= factor;
                    b *= factor;
                }
                if (adjustments.shadows !== 0 && luminance < 128) {
                    const factor = 1 + (adjustments.shadows / 100) * (1 - luminance / 128);
                    r *= factor;
                    g *= factor;
                    b *= factor;
                }

                // Clamp values
                data[i] = Math.max(0, Math.min(255, r));
                data[i + 1] = Math.max(0, Math.min(255, g));
                data[i + 2] = Math.max(0, Math.min(255, b));
            }

            ctx.putImageData(imageData, 0, 0);

            // Apply sharpness
            if (adjustments.sharpness > 0) {
                applySharpness(adjustments.sharpness / 100);
            }

            // Apply vignette
            if (adjustments.vignette > 0) {
                applyVignette(adjustments.vignette / 100);
            }
        }

        // Sharpness filter
        function applySharpness(amount) {
            const imageData = ctx.getImageData(0, 0, photoeditorCanvas.width, photoeditorCanvas.height);
            const data = imageData.data;
            const w = photoeditorCanvas.width;
            const h = photoeditorCanvas.height;
            const weight = amount * 0.5;

            const sharpenKernel = [
                0, -weight, 0,
                -weight, 1 + 4 * weight, -weight,
                0, -weight, 0
            ];

            const output = new Uint8ClampedArray(data);

            for (let y = 1; y < h - 1; y++) {
                for (let x = 1; x < w - 1; x++) {
                    for (let c = 0; c < 3; c++) {
                        let sum = 0;
                        for (let ky = -1; ky <= 1; ky++) {
                            for (let kx = -1; kx <= 1; kx++) {
                                const idx = ((y + ky) * w + (x + kx)) * 4 + c;
                                sum += data[idx] * sharpenKernel[(ky + 1) * 3 + (kx + 1)];
                            }
                        }
                        output[(y * w + x) * 4 + c] = sum;
                    }
                }
            }

            ctx.putImageData(new ImageData(output, w, h), 0, 0);
        }

        // Vignette effect
        function applyVignette(amount) {
            const imageData = ctx.getImageData(0, 0, photoeditorCanvas.width, photoeditorCanvas.height);
            const data = imageData.data;
            const w = photoeditorCanvas.width;
            const h = photoeditorCanvas.height;
            const centerX = w / 2;
            const centerY = h / 2;
            const maxDist = Math.sqrt(centerX * centerX + centerY * centerY);

            for (let y = 0; y < h; y++) {
                for (let x = 0; x < w; x++) {
                    const dist = Math.sqrt((x - centerX) ** 2 + (y - centerY) ** 2);
                    const vignette = 1 - (dist / maxDist) * amount;
                    const idx = (y * w + x) * 4;

                    data[idx] *= vignette;
                    data[idx + 1] *= vignette;
                    data[idx + 2] *= vignette;
                }
            }

            ctx.putImageData(imageData, 0, 0);
        }

        // Preset selection handler
        photoeditorPresetOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            photoeditorPresetOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedPreset = button.dataset.preset;

            // Apply preset values
            const preset = presets[selectedPreset];
            if (preset) {
                adjustments = { ...preset };
                updateSliders();
                applyEdits();
            }
        });

        // Update sliders to match preset
        function updateSliders() {
            brightnessSlider.value = adjustments.brightness;
            contrastSlider.value = adjustments.contrast;
            saturationSlider.value = adjustments.saturation;
            warmthSlider.value = adjustments.warmth;
            highlightsSlider.value = adjustments.highlights;
            shadowsSlider.value = adjustments.shadows;
            sharpnessSlider.value = adjustments.sharpness;
            vignetteSlider.value = adjustments.vignette;

            brightnessValue.textContent = adjustments.brightness;
            contrastValue.textContent = adjustments.contrast;
            saturationValue.textContent = adjustments.saturation;
            warmthValue.textContent = adjustments.warmth;
            highlightsValue.textContent = adjustments.highlights;
            shadowsValue.textContent = adjustments.shadows;
            sharpnessValue.textContent = adjustments.sharpness;
            vignetteValue.textContent = adjustments.vignette;
        }

        // Manual adjustment handlers
        [brightnessSlider, contrastSlider, saturationSlider, warmthSlider, highlightsSlider, shadowsSlider, sharpnessSlider, vignetteSlider].forEach(slider => {
            if (!slider) return;
            slider.addEventListener('input', (e) => {
                const name = e.target.id.replace('photoeditor-', '');
                adjustments[name] = parseInt(e.target.value);
                document.getElementById(`photoeditor-${name}-value`).textContent = e.target.value;
                applyEdits();
            });
        });

        // Reset button
        if (photoeditorResetBtn) {
            photoeditorResetBtn.addEventListener('click', () => {
                selectedPreset = 'none';
                adjustments = { brightness: 0, contrast: 0, saturation: 0, warmth: 0, highlights: 0, shadows: 0, sharpness: 0, vignette: 0 };
                updateSliders();
                applyEdits();

                photoeditorPresetOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
                photoeditorPresetOptions.querySelector('[data-preset="none"]').classList.add('selected');
            });
        }

        // Download button
        if (photoeditorDownloadBtn) {
            photoeditorDownloadBtn.addEventListener('click', async () => {
                if (!photoeditorCanvas) return;

                // Download at ORIGINAL resolution
                console.log(`Downloading edited photo at ORIGINAL size: ${photoeditorCanvas.width}x${photoeditorCanvas.height}px`);

                const dataUrl = photoeditorCanvas.toDataURL('image/png');
                const filename = `edited_photo_${Date.now()}.png`;

                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(dataUrl, filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(dataUrl, filename);
                } else {
                    // Fallback
                    const link = document.createElement('a');
                    link.href = dataUrl;
                    link.download = filename;
                    link.click();
                }
            });
        }
    })();

    // ==================== AI BEAUTY EDITOR ====================
    (function() {
        const apiKey = "";

        // Element references
        const beautyInput = document.getElementById('beauty-image-input');
        const beautyImagePreview = document.getElementById('beauty-image-preview');
        const beautyUploadPrompt = document.getElementById('beauty-upload-prompt');
        const beautyMobilePreview = document.getElementById('beauty-mobile-preview');
        const beautyPreviewContainer = document.getElementById('beauty-preview-container');
        const beautyRemovePreview = document.getElementById('beauty-remove-preview');
        const beautyFeatureOptions = document.getElementById('beauty-feature-options');
        const beautyCustomRequest = document.getElementById('beauty-custom-request');
        const beautyIntensityOptions = document.getElementById('beauty-intensity-options');
        const beautyRatioOptions = document.getElementById('beauty-ratio-options');
        const beautyGenerateBtn = document.getElementById('beauty-generate-btn');
        const beautyPlaceholder = document.getElementById('beauty-placeholder');
        const beautyResultsGrid = document.getElementById('beauty-results-grid');
        const beautyDownloadAllBtn = document.getElementById('beauty-download-all-btn');

        if (!beautyInput) return;

        let uploadedImageData = null;
        let generatedImages = [];
        let selectedFeatures = [];
        let selectedIntensity = 'moderate';
        let selectedRatio = '3:4';
        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const beautyCountSel = document.getElementById('beauty-count-selection-grid');
        if (beautyCountSel) {
            beautyCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                beautyCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Feature descriptions for AI prompts
        const featureDescriptions = {
            'smooth-skin': 'Smooth, flawless skin texture with reduced pores, even skin tone, no blemishes or imperfections, professional skin smoothing like in magazine photos',
            'brighten-face': 'Brightened, luminous face with enhanced radiance, glowing complexion, improved skin clarity, lifted shadows under eyes, overall brighter and more youthful appearance',
            'big-eyes': 'Enlarged, more prominent eyes with enhanced eye definition, bigger iris and pupils, more open and expressive eye appearance while maintaining natural proportions',
            'slim-face': 'Slimmer, more contoured V-line face shape, reduced facial width, defined cheekbones, more elegant facial proportions, oval or heart-shaped face structure',
            'sharp-nose': 'Refined, sharper nose bridge, more defined nose tip, straighter nose profile, higher nose bridge, sculpted nose structure',
            'white-teeth': 'Brightened, whiter teeth with enhanced dental brightness, natural-looking pearly white smile, improved teeth color',
            'slim-body': 'Create a noticeably slimmer, more toned physique with reduced body fat appearance, narrower waist, flatter stomach, slimmer arms and legs, more athletic and fit body shape, hourglass or athletic figure, toned muscles visible, reduced overall body width while maintaining healthy proportions - professional body slimming transformation similar to fitness magazine covers',
            'defined-jawline': 'More defined, chiseled jawline with enhanced jaw contour, sharper jaw definition, more prominent jaw structure, reduced under-chin area',
            'remove-blemish': 'Removed all skin blemishes, acne, spots, scars, and imperfections, completely clear flawless skin, professional skin retouching',
            'natural-makeup': 'Natural, subtle makeup look with enhanced features - light foundation, soft blush, natural eye definition, nude lip color, fresh and effortless beauty',
            'glam-makeup': 'Glamorous, dramatic makeup with bold eyes, defined eyebrows, contouring, highlighted cheekbones, smokey eyes or bold eyeliner, statement lip color, red carpet ready look',
            'full-beauty': 'Complete professional beauty transformation combining: smooth flawless skin, brightened glowing complexion, bigger expressive eyes, slim V-line face, sharp defined nose, white teeth, defined jawline, and glamorous makeup - total magazine-quality beauty enhancement'
        };

        // Intensity level descriptions
        const intensityLevels = {
            'subtle': 'very subtle and natural enhancement, barely noticeable changes, maintaining original appearance',
            'moderate': 'balanced and noticeable enhancement, clear improvements while keeping natural look',
            'strong': 'dramatic and bold enhancement, significant transformation, professional magazine-level editing'
        };

        // Setup image upload with HEIC support
        beautyInput.addEventListener('change', async (e) => {
            const file = e.target.files[0];
            if (!file) return;

            try {
                let processedFile = file;

                // Convert HEIC to JPEG if necessary
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    const heic2any = window.heic2any;
                    if (heic2any) {
                        const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }

                const reader = new FileReader();
                reader.onload = (event) => {
                    const dataUrl = event.target.result;
                    const base64 = dataUrl.split(',')[1];

                    uploadedImageData = {
                        base64: base64,
                        mimeType: processedFile.type,
                        dataUrl: dataUrl
                    };

                    // Show desktop preview
                    beautyImagePreview.src = dataUrl;
                    beautyUploadPrompt.classList.add('hidden');
                    beautyImagePreview.classList.remove('hidden');

                    // Show mobile preview
                    if (beautyMobilePreview && beautyPreviewContainer) {
                        beautyMobilePreview.src = dataUrl;
                        beautyPreviewContainer.classList.remove('hidden');
                    }

                    // Enable generate button if features selected
                    updateGenerateButton();
                };
                reader.readAsDataURL(processedFile);
            } catch (error) {
                console.error('Error processing image:', error);
                alert('Gagal memproses gambar. Silakan coba lagi.');
            }
        });

        // Remove image handler
        if (beautyRemovePreview) {
            beautyRemovePreview.addEventListener('click', () => {
                uploadedImageData = null;
                beautyInput.value = '';
                beautyImagePreview.src = '';
                beautyImagePreview.classList.add('hidden');
                beautyUploadPrompt.classList.remove('hidden');

                if (beautyPreviewContainer) {
                    beautyPreviewContainer.classList.add('hidden');
                }
                if (beautyMobilePreview) {
                    beautyMobilePreview.src = '';
                }

                beautyGenerateBtn.disabled = true;

                // Reset results
                beautyPlaceholder.classList.remove('hidden');
                beautyResultsGrid.classList.add('hidden');
                beautyResultsGrid.innerHTML = '';
                beautyDownloadAllBtn.classList.add('hidden');
                generatedImages = [];
            });
        }

        // Feature selection handler (multi-select)
        beautyFeatureOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            const feature = button.dataset.feature;

            if (button.classList.contains('selected')) {
                button.classList.remove('selected');
                selectedFeatures = selectedFeatures.filter(f => f !== feature);
            } else {
                button.classList.add('selected');
                selectedFeatures.push(feature);
            }

            updateGenerateButton();
        });

        // Intensity selection handler
        beautyIntensityOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            beautyIntensityOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedIntensity = button.dataset.intensity;
        });

        // Aspect ratio selection handler
        beautyRatioOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            beautyRatioOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedRatio = button.dataset.ratio;
        });

        // Update generate button state
        function updateGenerateButton() {
            const hasCustomRequest = beautyCustomRequest && beautyCustomRequest.value.trim().length > 0;
            beautyGenerateBtn.disabled = !uploadedImageData || (selectedFeatures.length === 0 && !hasCustomRequest);
        }

        // Listen to custom request input
        if (beautyCustomRequest) {
            beautyCustomRequest.addEventListener('input', updateGenerateButton);
        }

        // Generate button handler
        beautyGenerateBtn.addEventListener('click', async () => {
            const hasCustomRequest = beautyCustomRequest && beautyCustomRequest.value.trim().length > 0;

            if (!uploadedImageData || (selectedFeatures.length === 0 && !hasCustomRequest)) {
                alert('Silakan unggah foto dan pilih minimal 1 fitur beauty atau isi custom request');
                return;
            }

            // Build comprehensive prompt
            let featuresList = '';
            if (selectedFeatures.length > 0) {
                featuresList = selectedFeatures.map(f => featureDescriptions[f]).join(', ');
            }

            // Add custom request if provided
            const customRequest = beautyCustomRequest ? beautyCustomRequest.value.trim() : '';
            if (customRequest) {
                if (featuresList) {
                    featuresList += `, PLUS custom enhancements: ${customRequest}`;
                } else {
                    featuresList = `Custom beauty enhancements: ${customRequest}`;
                }
            }

            const intensityDesc = intensityLevels[selectedIntensity];

            const prompt = `Apply professional AI beauty enhancement to this person's photo with the following specifications:

BEAUTY ENHANCEMENTS TO APPLY:
${featuresList}

INTENSITY LEVEL:
Apply ${intensityDesc}

CRITICAL REQUIREMENTS:
1. IDENTITY PRESERVATION: Keep the person's identity EXACTLY the same - same facial structure, same person recognizable
2. NATURAL RESULTS: All enhancements must look natural and realistic, not artificial or over-processed
3. PROFESSIONAL QUALITY: Magazine-quality beauty editing, professional retouching standards
4. SKIN TEXTURE: Maintain natural skin texture even when smoothing, avoid plastic or overly smoothed appearance
5. FACIAL HARMONY: All enhancements must work together harmoniously
6. CONSISTENT LIGHTING: Maintain consistent lighting and shadows appropriate to the original photo
7. NO DISTORTIONS: Avoid any unnatural distortions or warping effects
8. PRESERVE BACKGROUND: Keep background unchanged, only enhance the person
9. HIGH RESOLUTION: Maintain image quality and sharpness
10. REALISTIC PROPORTIONS: All adjustments must maintain realistic human proportions

TECHNICAL EXECUTION:
- Apply enhancements with ${selectedIntensity} intensity level
- Blend all changes seamlessly with the original photo
- Ensure professional, believable results that the person would be happy to use
- Focus on enhancement, not complete transformation

Generate a stunning, professionally enhanced photo that maintains the person's authentic identity while applying the selected beauty improvements.`;

            await generateBeautySet(prompt);
        });

        async function generateBeautySet(prompt) {
            const originalBtnHTML = beautyGenerateBtn.innerHTML;
            beautyGenerateBtn.disabled = true;
            beautyGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Applying Beauty Effect...';

            beautyPlaceholder.classList.add('hidden');
            beautyDownloadAllBtn.classList.add('hidden');

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                beautyResultsGrid.innerHTML = '';
                beautyResultsGrid.classList.remove('hidden');

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `beauty-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gradient-to-br from-pink-100 to-rose-100 flex items-center justify-center aspect-[3/4]';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-pink-600"></div>';
                    beautyResultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSingleBeauty(i + 1, prompt)
                );
                await Promise.allSettled(generationPromises);

                beautyResultsGrid.querySelectorAll('[id^="beauty-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                successCount = generatedImages.filter(Boolean).length;
            }

            beautyGenerateBtn.disabled = false;
            beautyGenerateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                beautyDownloadAllBtn.classList.remove('hidden');
            }
        }

        async function generateSingleBeauty(index, basePrompt) {
            const card = document.getElementById(`beauty-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const finalPrompt = `${basePrompt}\n\nVariation ${index}: Create a unique professional result with slightly different beauty enhancement emphasis while maintaining all requirements.`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: finalPrompt },
                            { inlineData: { mimeType: uploadedImageData.mimeType, data: uploadedImageData.base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (!base64Data) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const featureNames = selectedFeatures.join('_');
                const filename = `beauty_${featureNames}_${selectedIntensity}_${index}.png`;

                generatedImages[index - 1] = { url: imageUrl, filename: filename };

                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Beauty ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-pink-500 to-rose-500 hover:from-pink-600 hover:to-rose-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
            } catch (error) {
                console.error(`Error generating beauty ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons - Mobile friendly
        beautyResultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();

            const actionBtn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
            if (!actionBtn) return;

            const action = actionBtn.dataset.action;
            const index = parseInt(actionBtn.dataset.index, 10);
            const imageData = generatedImages[index];

            if (!imageData) return;

            if (action === 'preview') {
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageData.url);
                }
            } else if (action === 'download') {
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(imageData.url, imageData.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(imageData.url, imageData.filename);
                }
            }
        });

        // Download all functionality with proper error handling
        if (beautyDownloadAllBtn) {
            beautyDownloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || beautyDownloadAllBtn.disabled) return;

                const originalHTML = beautyDownloadAllBtn.innerHTML;
                beautyDownloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        beautyDownloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    beautyDownloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        beautyDownloadAllBtn.disabled = false;
                        beautyDownloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    beautyDownloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        beautyDownloadAllBtn.disabled = false;
                        beautyDownloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }
    })();

    // ==================== AFFILIATE AGAMA & BUDAYA ====================
    (function() {
        const apiKey = "";

        // Element references
        const islamiProductInput = document.getElementById('islami-product-input');
        const islamiProductUploadBox = document.getElementById('islami-product-upload-box');
        const islamiProductPreview = document.getElementById('islami-product-preview');
        const islamiProductPlaceholder = document.getElementById('islami-product-placeholder');
        const islamiProductRemoveBtn = document.getElementById('islami-product-remove-btn');

        const categoryFilterContainer = document.getElementById('islami-category-filter');
        const islamiModelOptions = document.getElementById('islami-model-options');
        const islamiAgeContainer = document.getElementById('islami-age-container');
        const islamiAgeOptions = document.getElementById('islami-age-options');
        const islamiThemeOptions = document.getElementById('islami-theme-options');
        const islamiRatioOptions = document.getElementById('islami-ratio-options');
        const themeCountDisplay = document.getElementById('islami-theme-count');

        const islamiGenerateBtn = document.getElementById('islami-generate-btn');
        const islamiResultsPlaceholder = document.getElementById('islami-results-placeholder');
        const islamiResultsContainer = document.getElementById('islami-results-container');
        const islamiResultsGrid = document.getElementById('islami-results-grid');
        const islamiDownloadAllBtn = document.getElementById('islami-download-all-btn');

        if (!islamiProductInput) return;

        let productImageData = null;
        let generatedImages = [];
        let selectedModel = 'mannequin';
        let selectedAge = 'dewasa';
        let selectedTheme = 'ramadan';
        let selectedRatio = '3:4';
        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const islamiCountSel = document.getElementById('islami-count-selection-grid');
        if (islamiCountSel) {
            islamiCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                islamiCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // === CATEGORY FILTERING SYSTEM ===
        if (categoryFilterContainer) {
            const categoryButtons = categoryFilterContainer.querySelectorAll('.category-filter-btn');
            const themeButtons = islamiThemeOptions.querySelectorAll('[data-category]');

            categoryButtons.forEach(btn => {
                btn.addEventListener('click', () => {
                    // Remove selected state from all category buttons
                    categoryButtons.forEach(b => b.classList.remove('selected'));
                    // Add selected state to clicked button
                    btn.classList.add('selected');

                    const category = btn.dataset.category;

                    // Filter theme buttons
                    let visibleCount = 0;
                    themeButtons.forEach(themeBtn => {
                        if (category === 'all' || themeBtn.dataset.category === category) {
                            themeBtn.style.display = 'flex';
                            visibleCount++;
                        } else {
                            themeBtn.style.display = 'none';
                        }
                    });

                    // Update count display
                    if (themeCountDisplay) {
                        themeCountDisplay.textContent = `(${visibleCount} themes)`;
                    }

                    // Auto-select first visible theme if current theme is hidden
                    const currentThemeBtn = islamiThemeOptions.querySelector(`[data-value="${selectedTheme}"]`);
                    if (currentThemeBtn && currentThemeBtn.style.display === 'none') {
                        const firstVisible = islamiThemeOptions.querySelector('[data-category]:not([style*="display: none"])');
                        if (firstVisible) {
                            // Remove selected from all
                            themeButtons.forEach(b => b.classList.remove('selected'));
                            // Select first visible
                            firstVisible.classList.add('selected');
                            selectedTheme = firstVisible.dataset.value;
                        }
                    }
                });
            });
        }

        // Setup image upload with HEIC support
        setupImageUploadNew(islamiProductInput, islamiProductUploadBox, (data) => {
            productImageData = data;
            islamiProductPreview.src = data.dataUrl;
            islamiProductPlaceholder.classList.add('hidden');
            islamiProductPreview.classList.remove('hidden');
            islamiProductRemoveBtn.classList.remove('hidden');
            islamiGenerateBtn.disabled = false;
        });

        // Remove image
        if (islamiProductRemoveBtn) {
            islamiProductRemoveBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                productImageData = null;
                islamiProductInput.value = '';
                islamiProductPreview.src = '';
                islamiProductPreview.classList.add('hidden');
                islamiProductPlaceholder.classList.remove('hidden');
                islamiProductRemoveBtn.classList.add('hidden');
                islamiGenerateBtn.disabled = true;
            });
        }

        // Setup option buttons
        setupOptionButtonsNew(islamiModelOptions);
        setupOptionButtonsNew(islamiAgeOptions);
        setupOptionButtonsNew(islamiThemeOptions);
        setupOptionButtonsNew(islamiRatioOptions);

        // Listen to model type changes (show/hide age category)
        islamiModelOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) {
                selectedModel = btn.dataset.value;

                // Show age category when model-modest or model-regular selected
                if (selectedModel === 'model-modest' || selectedModel === 'model-regular') {
                    islamiAgeContainer.classList.remove('hidden');
                } else {
                    islamiAgeContainer.classList.add('hidden');
                }
            }
        });

        // Listen to option changes
        islamiAgeOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) selectedAge = btn.dataset.value;
        });

        islamiThemeOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) selectedTheme = btn.dataset.value;
        });

        islamiRatioOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) selectedRatio = btn.dataset.value;
        });

        // Generate button click handler
        islamiGenerateBtn.addEventListener('click', async () => {
            if (!productImageData) {
                alert('Mohon upload foto produk terlebih dahulu!');
                return;
            }

            const originalBtnHTML = islamiGenerateBtn.innerHTML;
            islamiGenerateBtn.disabled = true;
            islamiGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
            islamiDownloadAllBtn.classList.add('hidden');
            islamiResultsPlaceholder.classList.add('hidden');
            islamiResultsContainer.classList.remove('hidden');

            let successCount = 0;
            let attempts = 0;
            const MAX_ATTEMPTS = 3;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                generatedImages = new Array(selectedCount).fill(null);
                islamiResultsGrid.innerHTML = '';
                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `islami-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 shadow-lg aspect-[3/4]';
                    card.innerHTML = `<div class="w-full h-full flex items-center justify-center"><div class="animate-spin rounded-full h-10 w-10 border-b-2 border-emerald-600"></div></div>`;
                    islamiResultsGrid.appendChild(card);
                }
                await Promise.allSettled(Array.from({ length: selectedCount }, (_, i) => generateIslamiPhoto(i + 1)));
                successCount = generatedImages.filter(img => img).length;
                attempts++;
            }

            islamiGenerateBtn.disabled = false;
            islamiGenerateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                islamiResultsPlaceholder.classList.remove('hidden');
                islamiResultsContainer.classList.add('hidden');
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                return;
            }

            renderSuccessfulIslamiResults();
            islamiDownloadAllBtn.classList.remove('hidden');
        });

        // Generate single Islamic photo
        async function generateIslamiPhoto(index) {
            try {
                // Build theme description - 50 THEMES (Islamic, Christian, Hindu, Buddhist)
                const themeDescriptions = {
                    // === ISLAMIC THEMES (24) ===
                    'ramadan': 'suasana Ramadan dengan dekorasi bulan bintang, lampu lentera, sajadah, dan nuansa islami yang hangat',
                    'sholat': 'suasana sholat dengan sajadah, mukena/sarung, tasbih, dan pencahayaan lembut yang khusyuk',
                    'lebaran': 'suasana Lebaran dengan ketupat, ornamen islami, warna hijau emas, dan dekorasi Idul Fitri yang meriah',
                    'haji': 'suasana Haji/Umroh dengan Ka\'bah di background, ihram, tasbih, dan nuansa spiritual Mekkah',
                    'kajian': 'suasana kajian/pengajian dengan Al-Qur\'an, buku islami, jilbab, dan pencahayaan yang tenang',
                    'sedekah': 'suasana sedekah/charity dengan kotak amal, uang, makanan, dan nuansa berbagi yang hangat',
                    'berbuka': 'suasana berbuka puasa dengan kurma, kolak, es buah, takjil, dan hidangan berbuka yang menggugah selera',
                    'tarawih': 'suasana tarawih di masjid dengan sajadah berjajar, lampu masjid, dan suasana ibadah malam yang khusyuk',
                    'sahur': 'suasana sahur pagi hari dengan makanan sahur, air putih, kurma, dan pencahayaan remang-remang sebelum subuh',
                    'zakat': 'suasana pembayaran zakat dengan uang, beras, kotak zakat, dan nuansa berbagi kepada yang membutuhkan',
                    'qurban': 'suasana Idul Adha dengan hewan qurban (kambing/sapi), ornamen takbir, dan nuansa berbagi daging qurban',
                    'muharram': 'suasana tahun baru Islam dengan kalender Hijriyah, ornamen islami, dan nuansa refleksi spiritual awal tahun',
                    'maulid': 'suasana Maulid Nabi dengan dekorasi hijau emas, kaligrafi Muhammad SAW, dan nuansa cinta kepada Rasulullah',
                    'isra-miraj': 'suasana Isra Mi\'raj dengan ornamen buraq, masjidil aqsa, sidratul muntaha, dan nuansa perjalanan spiritual',
                    'nuzulul-quran': 'suasana turunnya Al-Qur\'an dengan mushaf indah, cahaya pencerahan, dan nuansa spiritual Ramadan',
                    'hijrah': 'suasana hijrah dengan unta, padang pasir, kaligrafi bismillah, dan nuansa perjalanan hijrah Nabi',
                    'aqiqah': 'suasana aqiqah bayi dengan dekorasi baby islami, undangan aqiqah, dan nuansa syukuran kelahiran',
                    'walimah': 'suasana walimah pernikahan islami dengan dekorasi nikah syar\'i, pelaminan sederhana, dan nuansa barakah',
                    'pengajian': 'suasana pengajian dengan jamaah, Al-Qur\'an, jilbab, sarung, dan nuansa berkumpul untuk ilmu agama',
                    'pesantren': 'suasana pesantren dengan santri, kitab kuning, asrama, musholla, dan nuansa pendidikan islami',
                    'takbiran': 'suasana takbiran malam Idul Fitri/Adha dengan bedug, drum band islami, dan nuansa semangat takbir keliling',
                    'tadarus': 'suasana tadarus Al-Qur\'an dengan mushaf terbuka, jamaah membaca, dan pencahayaan lembut yang khusyuk',
                    'masjid': 'suasana di dalam masjid dengan mihrab, sajadah, kaligrafi, kubah, dan arsitektur islami yang indah',
                    'dzikir': 'suasana dzikir bersama dengan tasbih, tangan berdoa, jamaah berdzikir, dan nuansa ketenangan spiritual',

                    // === CHRISTIAN THEMES (12) ===
                    'christmas': 'festive Christmas atmosphere with decorated Christmas tree, red and green colors, Santa Claus elements, gift wrapping, snowflakes, warm cozy lights, holiday spirit, wreaths, and joyful celebration',
                    'easter': 'Easter celebration with spring flowers, pastel colors (pink, light blue, yellow), Easter eggs, bunny rabbits, resurrection symbolism, cross elements, joyful Christian holiday atmosphere, and renewal theme',
                    'church-wedding': 'church wedding ceremony with beautiful church interior, white dress, wedding veil, flowers bouquet, pews, stained glass windows, altar, romantic and sacred Christian wedding atmosphere',
                    'baptism': 'baptism ceremony with water symbolism, white baptismal gown, Bible, candles, church setting, peaceful and sacred atmosphere, family gathering for Christian sacrament',
                    'first-communion': 'first communion celebration with white communion dress/suit, bread and wine elements, cross, Bible, church interior, solemn and joyful Christian sacrament atmosphere',
                    'confirmation': 'confirmation ceremony with dove symbolism, Bible, white robe, church setting, Holy Spirit imagery, sacred Christian coming-of-age celebration',
                    'sunday-service': 'Sunday church service with hymn books, Bible, congregation, church pews, worship atmosphere, peaceful Christian gathering, prayer elements',
                    'advent': 'Advent season with advent wreath, four candles, purple and pink colors, countdown to Christmas, anticipation atmosphere, Christian preparation theme',
                    'lent': 'Lenten season with cross, ashes symbolism, purple colors, prayer and fasting elements, solemn reflection atmosphere, Christian spiritual preparation',
                    'pentecost': 'Pentecost celebration with fire and flame symbolism, dove representing Holy Spirit, red colors, church gathering, spiritual energy and renewal atmosphere',
                    'good-friday': 'Good Friday solemnity with cross symbolism, crown of thorns, somber colors, candles, prayer atmosphere, Christian sacrifice and redemption theme',
                    'christian-charity': 'Christian charity and giving with helping hands, donation boxes, community service, food bank, loving kindness, compassion and generosity atmosphere',

                    // === HINDU THEMES (8) ===
                    'diwali': 'Diwali Festival of Lights with oil lamps (diyas), rangoli colorful patterns, fireworks, marigold flowers, gold and vibrant colors, prosperity and victory of light atmosphere',
                    'holi': 'Holi Festival of Colors with colorful powder (gulal), water balloons, vibrant splashes, spring celebration, joyful playful atmosphere, unity and love theme',
                    'navratri': 'Navratri celebration with dandiya sticks, colorful traditional attire, goddess Durga imagery, nine nights festival, energetic dance and devotion atmosphere',
                    'ganesh-chaturthi': 'Ganesh Chaturthi with Ganesha idol, modak sweets, flowers, red and yellow colors, elephant god celebration, removal of obstacles theme, festive atmosphere',
                    'hindu-wedding': 'Hindu wedding with mandap decoration, marigold garlands, sacred fire (havan), red and gold colors, traditional bridal attire, sacred marriage rituals atmosphere',
                    'puja': 'Hindu puja ceremony with altar, incense, flowers, diya lamps, bell, tilak, offerings, devotional worship atmosphere, spiritual connection theme',
                    'raksha-bandhan': 'Raksha Bandhan with decorative rakhi threads, sweets, brother-sister bond symbolism, colorful festive atmosphere, love and protection theme',
                    'temple-visit': 'Hindu temple visit with temple architecture, bells, flowers, incense, devotees, spiritual peaceful atmosphere, darshan and prayer theme',

                    // === BUDDHIST THEMES (6) ===
                    'vesak': 'Vesak celebration with lotus flowers, Buddha statue, lanterns, incense, peaceful meditation atmosphere, Buddha\'s birthday, enlightenment and spiritual awakening theme',
                    'meditation': 'Buddhist meditation with serene Buddha statue, lotus position, incense, peaceful zen atmosphere, mindfulness, inner peace and tranquility theme',
                    'buddhist-temple': 'Buddhist temple ceremony with monks, prayer flags, incense, temple architecture, bells, peaceful devotional atmosphere, dharma teaching theme',
                    'buddha-day': 'Buddha Day with golden Buddha statue, lotus flowers, offering bowls, candles, reverent peaceful atmosphere, enlightenment celebration',
                    'mindfulness': 'Mindfulness practice with zen elements, peaceful nature, meditation cushion, simple serene setting, present moment awareness, inner calm atmosphere',
                    'buddhist-offering': 'Buddhist offering ceremony with flowers, incense, fruits, candles, peaceful devotional atmosphere, merit-making and gratitude theme'
                };

                // === DETERMINE THEME CATEGORY ===
                const themeCategories = {
                    // Islamic themes
                    'ramadan': 'islamic', 'sholat': 'islamic', 'lebaran': 'islamic', 'haji': 'islamic',
                    'kajian': 'islamic', 'sedekah': 'islamic', 'berbuka': 'islamic', 'tarawih': 'islamic',
                    'sahur': 'islamic', 'zakat': 'islamic', 'qurban': 'islamic', 'muharram': 'islamic',
                    'maulid': 'islamic', 'isra-miraj': 'islamic', 'nuzulul-quran': 'islamic', 'hijrah': 'islamic',
                    'aqiqah': 'islamic', 'walimah': 'islamic', 'pengajian': 'islamic', 'pesantren': 'islamic',
                    'takbiran': 'islamic', 'tadarus': 'islamic', 'masjid': 'islamic', 'dzikir': 'islamic',
                    // Christian themes
                    'christmas': 'christian', 'easter': 'christian', 'church-wedding': 'christian', 'baptism': 'christian',
                    'first-communion': 'christian', 'confirmation': 'christian', 'sunday-service': 'christian', 'advent': 'christian',
                    'lent': 'christian', 'pentecost': 'christian', 'good-friday': 'christian', 'christian-charity': 'christian',
                    // Hindu themes
                    'diwali': 'hindu', 'holi': 'hindu', 'navratri': 'hindu', 'ganesh-chaturthi': 'hindu',
                    'hindu-wedding': 'hindu', 'puja': 'hindu', 'raksha-bandhan': 'hindu', 'temple-visit': 'hindu',
                    // Buddhist themes
                    'vesak': 'buddhist', 'meditation': 'buddhist', 'buddhist-temple': 'buddhist',
                    'buddha-day': 'buddhist', 'mindfulness': 'buddhist', 'buddhist-offering': 'buddhist'
                };

                const category = themeCategories[selectedTheme] || 'islamic';

                // === BUILD MODEL DESCRIPTION BASED ON CATEGORY ===
                let modelDescription = '';

                if (selectedModel === 'mannequin') {
                    if (category === 'islamic') {
                        modelDescription = 'tampilkan produk pada manekin/mannequin putih polos tanpa wajah';
                    } else {
                        modelDescription = 'display product on plain white mannequin without face';
                    }
                } else if (selectedModel === 'model-modest') {
                    // Modest model - culturally appropriate for each religion
                    const ageDescriptions = {
                        islamic: {
                            'dewasa': 'model dewasa',
                            'anak': 'anak-anak umur 7-12 tahun',
                            'balita': 'balita umur 2-5 tahun',
                            'bayi': 'bayi umur 0-1 tahun'
                        },
                        default: {
                            'dewasa': 'adult model',
                            'anak': 'child aged 7-12 years',
                            'balita': 'toddler aged 2-5 years',
                            'bayi': 'baby aged 0-1 year'
                        }
                    };

                    const ageDesc = category === 'islamic' ? ageDescriptions.islamic : ageDescriptions.default;

                    if (category === 'islamic') {
                        modelDescription = `tampilkan produk pada ${ageDesc[selectedAge]} dengan wajah yang DI-BLUR/BURAM untuk privasi dan kesopanan (PENTING: wajah HARUS blur/buram/tidak terlihat jelas), berpakaian islami (hijab/gamis untuk wanita, koko/sarung untuk pria)`;
                    } else if (category === 'christian') {
                        modelDescription = `display product on ${ageDesc[selectedAge]} with face slightly softened/blurred for modesty, dressed in modest conservative Christian attire`;
                    } else if (category === 'hindu') {
                        modelDescription = `display product on ${ageDesc[selectedAge]} with face gently blurred for modesty, dressed in traditional modest Hindu attire`;
                    } else if (category === 'buddhist') {
                        modelDescription = `display product on ${ageDesc[selectedAge]} with face softly blurred for modesty, dressed in simple modest Buddhist attire`;
                    } else {
                        modelDescription = `display product on ${ageDesc[selectedAge]} with face blurred for privacy, dressed in modest conservative attire appropriate for religious/cultural context`;
                    }
                } else if (selectedModel === 'model-regular') {
                    // Regular model with clear face
                    const ageDescriptions = {
                        islamic: {
                            'dewasa': 'model dewasa',
                            'anak': 'anak-anak umur 7-12 tahun',
                            'balita': 'balita umur 2-5 tahun',
                            'bayi': 'bayi umur 0-1 tahun'
                        },
                        default: {
                            'dewasa': 'adult model',
                            'anak': 'child aged 7-12 years',
                            'balita': 'toddler aged 2-5 years',
                            'bayi': 'baby aged 0-1 year'
                        }
                    };

                    const ageDesc = category === 'islamic' ? ageDescriptions.islamic : ageDescriptions.default;

                    if (category === 'islamic') {
                        modelDescription = `tampilkan produk pada ${ageDesc[selectedAge]} dengan wajah yang terlihat JELAS dan natural (wajah harus terlihat dengan jelas, ekspresif, dan menarik)`;
                    } else {
                        modelDescription = `display product on ${ageDesc[selectedAge]} with CLEAR and natural face visible (face must be clearly visible, expressive, and engaging)`;
                    }
                }

                // === BUILD CULTURALLY APPROPRIATE PROMPT ===
                let prompt = '';

                if (category === 'islamic') {
                    // Indonesian prompt for Islamic themes
                    prompt = `Buat foto produk affiliate dengan nuansa islami untuk ${themeDescriptions[selectedTheme]}.

PRODUK:
- Tampilkan produk dari foto yang di-upload dengan jelas dan menarik
- ${modelDescription}

KOMPOSISI FOTO:
- Produk menjadi fokus utama, terlihat jelas dan detail
- Background islami yang mendukung tema ${selectedTheme}
- Pencahayaan natural dan profesional
- Komposisi seperti foto affiliate/e-commerce yang menarik

ELEMEN ISLAMI:
- ${themeDescriptions[selectedTheme]}
- Warna dominan: hijau emerald, emas, putih, cream
- Ornamen islami: kaligrafi, bulan bintang, motif masjid
- Suasana yang hangat, damai, dan berkah

STYLE FOTO:
- Realistic, high quality, professional photography
- Cocok untuk konten affiliate marketing di media sosial
- Eye-catching tapi tetap sopan dan islami
- Variasi ${index} dengan sudut atau komposisi sedikit berbeda

CRITICAL: Foto harus terlihat natural, profesional, dan menarik untuk affiliate marketing dengan nuansa islami yang kuat!`;

                } else if (category === 'christian') {
                    // English prompt for Christian themes
                    prompt = `Create professional affiliate product photo with Christian/church theme for ${themeDescriptions[selectedTheme]}.

PRODUCT:
- Display the uploaded product clearly and attractively
- ${modelDescription}

PHOTO COMPOSITION:
- Product is the main focus, clearly visible and detailed
- Christian-themed background supporting ${selectedTheme}
- Natural and professional lighting
- Composition suitable for affiliate/e-commerce marketing

CHRISTIAN ELEMENTS:
- ${themeDescriptions[selectedTheme]}
- Dominant colors: warm tones, gold, white, cream, soft pastels
- Christian symbols: crosses, church elements, religious ornaments, stained glass effects
- Atmosphere: warm, peaceful, blessed, joyful celebration

PHOTO STYLE:
- Realistic, high quality, professional photography
- Suitable for social media affiliate marketing content
- Eye-catching yet respectful and appropriate for Christian audience
- Variation ${index} with slightly different angle or composition

CRITICAL: Photo must look natural, professional, and appealing for affiliate marketing with strong Christian celebration theme!`;

                } else if (category === 'hindu') {
                    // English prompt for Hindu themes
                    prompt = `Create professional affiliate product photo with Hindu festival theme for ${themeDescriptions[selectedTheme]}.

PRODUCT:
- Display the uploaded product clearly and attractively
- ${modelDescription}

PHOTO COMPOSITION:
- Product is the main focus, clearly visible and detailed
- Hindu-themed background supporting ${selectedTheme}
- Natural and professional lighting
- Composition suitable for affiliate/e-commerce marketing

HINDU ELEMENTS:
- ${themeDescriptions[selectedTheme]}
- Dominant colors: vibrant orange, red, yellow, gold, saffron
- Hindu symbols: Om symbol, lotus flowers, diyas, rangoli patterns, marigolds
- Atmosphere: vibrant, colorful, blessed, festive celebration

PHOTO STYLE:
- Realistic, high quality, professional photography
- Suitable for social media affiliate marketing content
- Eye-catching yet respectful and appropriate for Hindu audience
- Variation ${index} with slightly different angle or composition

CRITICAL: Photo must look natural, professional, and appealing for affiliate marketing with strong Hindu festival theme!`;

                } else if (category === 'buddhist') {
                    // English prompt for Buddhist themes
                    prompt = `Create professional affiliate product photo with Buddhist spiritual theme for ${themeDescriptions[selectedTheme]}.

PRODUCT:
- Display the uploaded product clearly and attractively
- ${modelDescription}

PHOTO COMPOSITION:
- Product is the main focus, clearly visible and detailed
- Buddhist-themed background supporting ${selectedTheme}
- Natural and professional lighting
- Composition suitable for affiliate/e-commerce marketing

BUDDHIST ELEMENTS:
- ${themeDescriptions[selectedTheme]}
- Dominant colors: saffron, gold, white, earth tones
- Buddhist symbols: lotus flowers, dharma wheel, Buddha statues, incense, prayer flags
- Atmosphere: peaceful, serene, mindful, spiritual tranquility

PHOTO STYLE:
- Realistic, high quality, professional photography
- Suitable for social media affiliate marketing content
- Eye-catching yet respectful and appropriate for Buddhist audience
- Variation ${index} with slightly different angle or composition

CRITICAL: Photo must look natural, professional, and appealing for affiliate marketing with strong Buddhist spiritual theme!`;

                }

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: productImageData.mimeType, data: productImageData.base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data received from API');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const filename = `cultural_marketing_${selectedTheme}_${index}_${Date.now()}.png`;
                generatedImages[index - 1] = { url: imageUrl, filename };

                const card = document.getElementById(`islami-card-${index}`);
                if (card) {
                    card.innerHTML = `<img src="${imageUrl}" class="w-full h-full object-cover" alt="Cultural ${index}"><div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2"><button data-action="preview" data-index="${index - 1}" class="bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2 text-sm" style="touch-action:manipulation;-webkit-tap-highlight-color:transparent;"><i class="fas fa-eye" style="pointer-events:none;"></i><span class="hidden sm:inline" style="pointer-events:none;">Preview</span></button><button data-action="download" data-index="${index - 1}" class="bg-gradient-to-r from-violet-500 to-indigo-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2 text-sm" style="touch-action:manipulation;-webkit-tap-highlight-color:transparent;"><i class="fas fa-download" style="pointer-events:none;"></i><span class="hidden sm:inline" style="pointer-events:none;">Download</span></button></div>`;
                }
            } catch (error) {
                console.error(`Error generating cultural photo ${index}:`, error);
                const card = document.getElementById(`islami-card-${index}`);
                if (card) card.innerHTML = '';
            }
        }

        function renderSuccessfulIslamiResults() {
            const successful = generatedImages.filter(img => img);
            generatedImages = successful;
            islamiResultsGrid.innerHTML = '';
            successful.forEach((img, idx) => {
                const card = document.createElement('div');
                card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 shadow-lg aspect-[3/4]';
                card.innerHTML = `<img src="${img.url}" class="w-full h-full object-cover" alt="Cultural ${idx + 1}"><div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2"><button data-action="preview" data-index="${idx}" class="bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2 text-sm" style="touch-action:manipulation;-webkit-tap-highlight-color:transparent;"><i class="fas fa-eye" style="pointer-events:none;"></i><span class="hidden sm:inline" style="pointer-events:none;">Preview</span></button><button data-action="download" data-index="${idx}" class="bg-gradient-to-r from-violet-500 to-indigo-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2 text-sm" style="touch-action:manipulation;-webkit-tap-highlight-color:transparent;"><i class="fas fa-download" style="pointer-events:none;"></i><span class="hidden sm:inline" style="pointer-events:none;">Download</span></button></div>`;
                islamiResultsGrid.appendChild(card);
            });
        }

        // Event delegation for action buttons
        islamiResultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();

            const actionBtn = e.target.closest('[data-action]');
            if (!actionBtn) return;

            if (actionBtn.disabled) return;

            const action = actionBtn.dataset.action;
            const index = parseInt(actionBtn.dataset.index);
            const imageData = generatedImages[index];

            if (!imageData) return;

            if (action === 'preview') {
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageData.url);
                } else {
                    window.open(imageData.url, '_blank');
                }
            } else if (action === 'download') {
                actionBtn.disabled = true;
                const originalHTML = actionBtn.innerHTML;
                actionBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i>';

                try {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }

                    actionBtn.innerHTML = '<i class="fas fa-check"></i>';
                    setTimeout(() => {
                        actionBtn.disabled = false;
                        actionBtn.innerHTML = originalHTML;
                    }, 1000);
                } catch (error) {
                    console.error('Download failed:', error);
                    actionBtn.innerHTML = '<i class="fas fa-times"></i>';
                    setTimeout(() => {
                        actionBtn.disabled = false;
                        actionBtn.innerHTML = originalHTML;
                    }, 1000);
                }
            }
        });

        // Download all button
        islamiDownloadAllBtn.addEventListener('click', async (e) => {
            e.stopPropagation();

            if (generatedImages.length === 0 || islamiDownloadAllBtn.disabled) return;

            const originalHTML = islamiDownloadAllBtn.innerHTML;
            islamiDownloadAllBtn.disabled = true;

            try {
                for (let i = 0; i < generatedImages.length; i++) {
                    const img = generatedImages[i];
                    islamiDownloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                    try {
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(img.url, img.filename);
                        } else if (window.downloadImage) {
                            await window.downloadImage(img.url, img.filename);
                        }
                        await new Promise(resolve => setTimeout(resolve, 500));
                    } catch (error) {
                        console.error(`Error downloading image ${i + 1}:`, error);
                    }
                }

                islamiDownloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                setTimeout(() => {
                    islamiDownloadAllBtn.disabled = false;
                    islamiDownloadAllBtn.innerHTML = originalHTML;
                }, 2000);
            } catch (error) {
                console.error('Download all error:', error);
                islamiDownloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                setTimeout(() => {
                    islamiDownloadAllBtn.disabled = false;
                    islamiDownloadAllBtn.innerHTML = originalHTML;
                }, 2000);
            }
        });
    })();

    // ==================== BANNER GENERATOR ====================
    (function() {
        const apiKey = "";

        // Element references
        const bannerBgInput = document.getElementById('banner-bg-input');
        const bannerBgUploadBox = document.getElementById('banner-bg-upload-box');
        const bannerBgPreview = document.getElementById('banner-bg-preview');
        const bannerBgPlaceholder = document.getElementById('banner-bg-placeholder');
        const bannerBgRemoveBtn = document.getElementById('banner-bg-remove-btn');

        const bannerHeadlineInput = document.getElementById('banner-headline-input');
        const bannerSubheadlineInput = document.getElementById('banner-subheadline-input');
        const bannerCtaInput = document.getElementById('banner-cta-input');

        const bannerTypeOptions = document.getElementById('banner-type-options');
        const bannerStyleOptions = document.getElementById('banner-style-options');
        const bannerColorOptions = document.getElementById('banner-color-options');
        const bannerSizeOptions = document.getElementById('banner-size-options');
        const bannerCountOptions = document.getElementById('banner-count-options');

        const bannerGenerateBtn = document.getElementById('banner-generate-btn');
        const bannerResultsPlaceholder = document.getElementById('banner-results-placeholder');
        const bannerResultsContainer = document.getElementById('banner-results-container');
        const bannerResultsGrid = document.getElementById('banner-results-grid');
        const bannerDownloadAllBtn = document.getElementById('banner-download-all-btn');

        if (!bannerHeadlineInput) return;

        let backgroundImageData = null;
        let generatedImages = [];
        let selectedType = 'website';
        let selectedStyle = 'modern-minimal';
        let selectedColor = 'blue';
        let selectedSize = '16:9';
        let selectedCount = 6;

        // Enable button when headline is filled
        function checkFormValidity() {
            const hasHeadline = bannerHeadlineInput.value.trim().length > 0;
            bannerGenerateBtn.disabled = !hasHeadline;
        }

        bannerHeadlineInput.addEventListener('input', checkFormValidity);

        // Setup background upload with HEIC support (optional)
        setupImageUploadNew(bannerBgInput, bannerBgUploadBox, (data) => {
            backgroundImageData = data;
            bannerBgPreview.src = data.dataUrl;
            bannerBgPlaceholder.classList.add('hidden');
            bannerBgPreview.classList.remove('hidden');
            bannerBgRemoveBtn.classList.remove('hidden');
        });

        // Remove background image
        if (bannerBgRemoveBtn) {
            bannerBgRemoveBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                backgroundImageData = null;
                bannerBgInput.value = '';
                bannerBgPreview.src = '';
                bannerBgPreview.classList.add('hidden');
                bannerBgPlaceholder.classList.remove('hidden');
                bannerBgRemoveBtn.classList.add('hidden');
            });
        }

        // Setup option buttons
        setupOptionButtonsNew(bannerTypeOptions);
        setupOptionButtonsNew(bannerStyleOptions);
        setupOptionButtonsNew(bannerColorOptions);
        setupOptionButtonsNew(bannerSizeOptions);
        setupOptionButtonsNew(bannerCountOptions);

        // Listen to banner type changes (show/hide size options based on type)
        bannerTypeOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) {
                selectedType = btn.dataset.value;

                // Show/hide size options based on type
                const allSizeButtons = bannerSizeOptions.querySelectorAll('.option-btn-banner');
                allSizeButtons.forEach(sizeBtn => {
                    const category = sizeBtn.dataset.category;
                    if (category === selectedType) {
                        sizeBtn.classList.remove('hidden');
                    } else {
                        sizeBtn.classList.add('hidden');
                    }
                });

                // Auto-select first visible button
                const firstVisible = bannerSizeOptions.querySelector(`.option-btn-banner[data-category="${selectedType}"]`);
                if (firstVisible) {
                    allSizeButtons.forEach(b => b.classList.remove('selected'));
                    firstVisible.classList.add('selected');
                    selectedSize = firstVisible.dataset.value;
                }
            }
        });

        // Listen to option changes
        bannerStyleOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) selectedStyle = btn.dataset.value;
        });

        bannerColorOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) selectedColor = btn.dataset.value;
        });

        bannerSizeOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) selectedSize = btn.dataset.value;
        });

        bannerCountOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) selectedCount = parseInt(btn.dataset.value);
        });

        // Generate button click handler
        bannerGenerateBtn.addEventListener('click', async () => {
            const headline = bannerHeadlineInput.value.trim();
            if (!headline) {
                alert('Mohon masukkan teks headline!');
                return;
            }

            const originalBtnHTML = bannerGenerateBtn.innerHTML;
            bannerGenerateBtn.disabled = true;
            bannerGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

            // Clear previous results
            generatedImages = [];
            bannerDownloadAllBtn.classList.add('hidden');

            // Hide placeholder, show results
            bannerResultsPlaceholder.classList.add('hidden');
            bannerResultsContainer.classList.remove('hidden');
            bannerResultsGrid.innerHTML = '';

            // Create placeholder cards
            for (let i = 1; i <= selectedCount; i++) {
                const card = document.createElement('div');
                card.id = `banner-card-${i}`;
                card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 shadow-lg aspect-[16/9]';
                card.innerHTML = `
                    <div class="w-full h-full flex items-center justify-center">
                        <div class="animate-spin rounded-full h-10 w-10 border-b-2 border-violet-600"></div>
                    </div>
                    <div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/70 to-transparent p-2">
                        <p class="text-white text-xs text-center">Banner ${i}</p>
                    </div>
                `;
                bannerResultsGrid.appendChild(card);
            }

            // Generate banners in parallel with retry
            let attempts = 0;
            const MAX_ATTEMPTS = 3;

            while (attempts < MAX_ATTEMPTS) {
                attempts++;
                generatedImages = [];
                await Promise.allSettled(
                    Array.from({ length: selectedCount }, (_, i) => generateBanner(i + 1))
                );
                const successCount = generatedImages.filter(Boolean).length;
                if (successCount > 0) break;
            }

            // Remove empty spinner cards
            document.querySelectorAll('[id^="banner-card-"]').forEach(card => {
                if (!card.querySelector('img')) card.remove();
            });

            if (generatedImages.filter(Boolean).length === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                bannerResultsPlaceholder.classList.remove('hidden');
                bannerResultsContainer.classList.add('hidden');
            } else {
                bannerDownloadAllBtn.classList.remove('hidden');
            }

            bannerGenerateBtn.disabled = false;
            bannerGenerateBtn.innerHTML = originalBtnHTML;
        });

        // Generate single banner
        async function generateBanner(index) {
            try {
                const headline = bannerHeadlineInput.value.trim();
                const subheadline = bannerSubheadlineInput.value.trim();
                const cta = bannerCtaInput.value.trim();

                // Build style descriptions
                const styleDescriptions = {
                    'modern-minimal': 'modern minimal design dengan clean lines, whitespace, simple elegant, professional look',
                    'bold-vibrant': 'bold dan vibrant dengan warna-warna berani, energetic, eye-catching, high contrast',
                    'elegant-luxury': 'elegant luxury dengan sophisticated styling, premium feel, gold accents, high-end aesthetic',
                    'professional-corporate': 'professional corporate design, business-like, trustworthy, formal dan clean',
                    'playful-fun': 'playful dan fun dengan warna cerah, illustrations, quirky elements, friendly vibe',
                    'dark-moody': 'dark moody atmosphere dengan dramatic lighting, mysterious, cinematic feel',
                    'gradient-modern': 'modern gradient design dengan smooth color transitions, contemporary, trendy',
                    'retro-vintage': 'retro vintage style dengan classic aesthetics, nostalgic feel, vintage colors'
                };

                // Build color descriptions
                const colorDescriptions = {
                    'blue': 'warna dominan biru (#3b82f6) dengan accent colors yang matching',
                    'red': 'warna dominan merah (#ef4444) dengan accent colors yang matching',
                    'green': 'warna dominan hijau (#10b981) dengan accent colors yang matching',
                    'purple': 'warna dominan ungu/violet (#8b5cf6) dengan accent colors yang matching',
                    'orange': 'warna dominan orange (#f97316) dengan accent colors yang matching',
                    'pink': 'warna dominan pink (#ec4899) dengan accent colors yang matching'
                };

                // Build type descriptions
                const typeDescriptions = {
                    'website': 'untuk website hero banner atau header, professional web design aesthetic',
                    'social-media': 'untuk social media post, Instagram-friendly, engaging dan shareable',
                    'ads': 'untuk digital advertising, attention-grabbing, conversion-focused',
                    'ecommerce': 'untuk e-commerce promo banner, product-focused, sales-oriented'
                };

                // Build prompt
                let prompt = `Buat banner profesional ${typeDescriptions[selectedType]} dengan ${styleDescriptions[selectedStyle]}.

TEKS BANNER:
- Headline (BESAR & BOLD): "${headline}"`;

                if (subheadline) {
                    prompt += `\n- Subheadline: "${subheadline}"`;
                }

                if (cta) {
                    prompt += `\n- CTA Button: "${cta}"`;
                }

                prompt += `

DESIGN REQUIREMENTS:
- ${colorDescriptions[selectedColor]}
- Typography: Headline harus SANGAT BESAR, BOLD, dan mudah dibaca
- Layout: Balanced composition, clear hierarchy, professional spacing
- Background: ${backgroundImageData ? 'Gunakan foto background yang di-upload sebagai base, overlay dengan gradients/shapes' : 'Create creative background dengan gradients, shapes, atau patterns'}
- CTA: ${cta ? 'Buat button CTA yang prominent dan clickable' : 'Tidak perlu CTA button'}

STYLE:
- ${styleDescriptions[selectedStyle]}
- Modern, professional, eye-catching
- High quality, print-ready design
- Perfect for ${selectedType} purposes

VARIASI ${index}:
- Gunakan layout atau komposisi yang sedikit berbeda
- Maintain brand consistency tapi dengan creative variation
- Eksplorasi different text placements atau graphic elements

CRITICAL: Banner harus PROFESSIONAL, eye-catching, dan optimized untuk ${selectedType}. Typography harus JELAS dan mudah dibaca!`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                // Build payload with or without background image
                const parts = [{ text: prompt }];
                if (backgroundImageData) {
                    parts.push({ inlineData: { mimeType: backgroundImageData.mimeType, data: backgroundImageData.base64 } });
                }

                const payload = {
                    contents: [{
                        parts: parts
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedSize }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (!base64Data) {
                    throw new Error('No image data in response');
                }

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const filename = `banner_${selectedType}_${Date.now()}_${index}.png`;

                // Store generated image with prompt
                generatedImages[index - 1] = { url: imageUrl, filename, prompt };

                // Update card with generated image
                const card = document.getElementById(`banner-card-${index}`);
                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 aspect-[16/9]';
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Banner ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-1.5">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="edit" data-index="${index - 1}" class="action-btn bg-yellow-500 hover:bg-yellow-600 text-white px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-1.5">
                                <i class="fas fa-edit"></i>
                                <span class="hidden sm:inline">Edit</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-violet-500 to-violet-600 hover:from-violet-600 hover:to-violet-700 text-white px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-1.5">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }

                console.log(`Banner ${index} generated successfully`);

            } catch (error) {
                console.error(`Error generating banner ${index}:`, error);
                const card = document.getElementById(`banner-card-${index}`);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        bannerResultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();

            const actionBtn = e.target.closest('[data-action]');
            if (!actionBtn) return;

            if (actionBtn.disabled) return;

            const action = actionBtn.dataset.action;
            const index = parseInt(actionBtn.dataset.index);
            const imageData = generatedImages[index];

            if (!imageData) return;

            if (action === 'preview') {
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageData.url);
                } else {
                    window.open(imageData.url, '_blank');
                }
            } else if (action === 'edit') {
                showBannerEditModal(index, imageData.prompt || '');
            } else if (action === 'download') {
                actionBtn.disabled = true;
                const originalHTML = actionBtn.innerHTML;
                actionBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i>';

                try {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }

                    actionBtn.innerHTML = '<i class="fas fa-check"></i>';
                    setTimeout(() => {
                        actionBtn.disabled = false;
                        actionBtn.innerHTML = originalHTML;
                    }, 1000);
                } catch (error) {
                    console.error('Download failed:', error);
                    actionBtn.innerHTML = '<i class="fas fa-times"></i>';
                    setTimeout(() => {
                        actionBtn.disabled = false;
                        actionBtn.innerHTML = originalHTML;
                    }, 1000);
                }
            }
        });

        // Banner edit modal + regenerate
        function showBannerEditModal(cardIndex, currentPrompt) {
            let modal = document.getElementById('banner-edit-modal');
            if (!modal) {
                modal = document.createElement('div');
                modal.id = 'banner-edit-modal';
                modal.className = 'fixed inset-0 z-[9999] flex items-center justify-center bg-black/60 p-4';
                modal.innerHTML = `
                    <div class="bg-white rounded-2xl shadow-2xl w-full max-w-2xl max-h-[90vh] flex flex-col">
                        <div class="flex justify-between items-center p-5 border-b">
                            <h3 class="text-xl font-bold text-gray-800"><i class="fas fa-edit mr-2 text-yellow-500"></i>Edit Prompt Banner</h3>
                            <button id="banner-edit-modal-close" class="text-gray-400 hover:text-gray-600 text-2xl font-bold leading-none">&times;</button>
                        </div>
                        <div class="p-5 flex-1 overflow-y-auto">
                            <p class="text-sm text-gray-500 mb-3" id="banner-edit-card-label"></p>
                            <label class="block text-sm font-semibold text-gray-700 mb-2">Prompt AI (edit sesuai kebutuhan):</label>
                            <textarea id="banner-edit-prompt-textarea" class="w-full h-56 p-3 border-2 border-yellow-200 rounded-xl text-sm focus:border-yellow-500 resize-y" placeholder="Edit prompt di sini..."></textarea>
                        </div>
                        <div class="p-5 border-t flex gap-3 justify-end">
                            <button id="banner-edit-modal-cancel" class="px-5 py-2.5 bg-gray-100 text-gray-700 rounded-xl font-semibold hover:bg-gray-200 transition-all">Batal</button>
                            <button id="banner-edit-modal-regen" class="px-5 py-2.5 bg-gradient-to-r from-yellow-500 to-orange-500 text-white rounded-xl font-semibold hover:from-yellow-600 hover:to-orange-600 transition-all flex items-center gap-2">
                                <i class="fas fa-redo"></i> Generate Ulang Banner
                            </button>
                        </div>
                    </div>`;
                document.body.appendChild(modal);
                document.getElementById('banner-edit-modal-close').addEventListener('click', () => modal.classList.add('hidden'));
                document.getElementById('banner-edit-modal-cancel').addEventListener('click', () => modal.classList.add('hidden'));
            }

            document.getElementById('banner-edit-card-label').textContent = 'Banner ' + (cardIndex + 1);
            document.getElementById('banner-edit-prompt-textarea').value = currentPrompt;
            modal.classList.remove('hidden');

            const oldBtn = document.getElementById('banner-edit-modal-regen');
            const newBtn = oldBtn.cloneNode(true);
            oldBtn.parentNode.replaceChild(newBtn, oldBtn);
            newBtn.addEventListener('click', async () => {
                const newPrompt = document.getElementById('banner-edit-prompt-textarea').value.trim();
                if (!newPrompt) return;
                modal.classList.add('hidden');
                await regenerateSingleBanner(cardIndex, newPrompt);
            });
        }

        async function regenerateSingleBanner(cardIndex, newPrompt) {
            const cardNumber = cardIndex + 1;
            const card = document.getElementById('banner-card-' + cardNumber);
            if (!card) return;

            card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 shadow-lg aspect-[16/9]';
            card.innerHTML = `
                <div class="w-full h-full flex items-center justify-center">
                    <div class="animate-spin rounded-full h-10 w-10 border-b-2 border-violet-600"></div>
                </div>
                <div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/70 to-transparent p-2">
                    <p class="text-white text-xs text-center">Generating ulang...</p>
                </div>`;

            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const parts = [{ text: newPrompt }];
                if (backgroundImageData) {
                    parts.push({ inlineData: { mimeType: backgroundImageData.mimeType, data: backgroundImageData.base64 } });
                }
                const payload = {
                    contents: [{ parts }],
                    generationConfig: { responseModalities: ['TEXT', 'IMAGE'], imageConfig: { aspectRatio: selectedSize } }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const filename = `banner_${selectedType}_${Date.now()}_regen.png`;
                generatedImages[cardIndex] = { url: imageUrl, filename, prompt: newPrompt };

                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 aspect-[16/9]';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Banner ${cardNumber}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${cardIndex}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-1.5">
                            <i class="fas fa-eye"></i><span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="edit" data-index="${cardIndex}" class="action-btn bg-yellow-500 hover:bg-yellow-600 text-white px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-1.5">
                            <i class="fas fa-edit"></i><span class="hidden sm:inline">Edit</span>
                        </button>
                        <button data-action="download" data-index="${cardIndex}" class="action-btn bg-gradient-to-r from-violet-500 to-violet-600 hover:from-violet-600 hover:to-violet-700 text-white px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-1.5">
                            <i class="fas fa-download"></i><span class="hidden sm:inline">Download</span>
                        </button>
                    </div>`;

            } catch (error) {
                console.error('Regen banner error:', error);
                card.innerHTML = `
                    <div class="w-full h-full flex flex-col items-center justify-center bg-red-50 p-4">
                        <i class="fas fa-exclamation-triangle text-red-500 text-3xl mb-2"></i>
                        <p class="text-sm text-red-600">Gagal generate ulang</p>
                        <p class="text-xs text-red-400 mt-1">${error.message}</p>
                    </div>`;
            }
        }

        // Download all button
        bannerDownloadAllBtn.addEventListener('click', async (e) => {
            e.stopPropagation();

            const validBanners = generatedImages.filter(Boolean);
            if (validBanners.length === 0 || bannerDownloadAllBtn.disabled) return;

            const originalHTML = bannerDownloadAllBtn.innerHTML;
            bannerDownloadAllBtn.disabled = true;

            try {
                for (let i = 0; i < validBanners.length; i++) {
                    const img = validBanners[i];
                    bannerDownloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${validBanners.length}...</span>`;

                    try {
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(img.url, img.filename);
                        } else if (window.downloadImage) {
                            await window.downloadImage(img.url, img.filename);
                        }
                        await new Promise(resolve => setTimeout(resolve, 500));
                    } catch (error) {
                        console.error(`Error downloading image ${i + 1}:`, error);
                    }
                }

                bannerDownloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                setTimeout(() => {
                    bannerDownloadAllBtn.disabled = false;
                    bannerDownloadAllBtn.innerHTML = originalHTML;
                }, 2000);
            } catch (error) {
                console.error('Download all error:', error);
                bannerDownloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                setTimeout(() => {
                    bannerDownloadAllBtn.disabled = false;
                    bannerDownloadAllBtn.innerHTML = originalHTML;
                }, 2000);
            }
        });
    })();

    // ==================== BACKGROUND REMOVER ====================
    (function() {
        const apiKey = "";

        // Element references
        const bgremoverInput = document.getElementById('bgremover-input');
        const bgremoverUploadBox = document.getElementById('bgremover-upload-box');
        const bgremoverPreview = document.getElementById('bgremover-preview');
        const bgremoverPlaceholder = document.getElementById('bgremover-placeholder');
        const bgremoverRemoveBtn = document.getElementById('bgremover-remove-btn');
        const bgremoverGenerateBtn = document.getElementById('bgremover-generate-btn');
        const bgremoverBgOptions = document.getElementById('bgremover-bg-options');
        const bgremoverCustomContainer = document.getElementById('bgremover-custom-container');
        const bgremoverCustomInput = document.getElementById('bgremover-custom-input');
        const bgremoverRatioOptions = document.getElementById('bgremover-ratio-options');
        const bgremoverResultsPlaceholder = document.getElementById('bgremover-results-placeholder');
        const bgremoverResultsContainer = document.getElementById('bgremover-results-container');
        const bgremoverResultsGrid = document.getElementById('bgremover-results-grid');
        const bgremoverDownloadAllBtn = document.getElementById('bgremover-download-all-btn');
        const bgremoverRefBgInput = document.getElementById('bgremover-refbg-input');
        const bgremoverRefBgUploadBox = document.getElementById('bgremover-refbg-upload-box');
        const bgremoverRefBgPreview = document.getElementById('bgremover-refbg-preview');
        const bgremoverRefBgPlaceholder = document.getElementById('bgremover-refbg-placeholder');
        const bgremoverRefBgRemoveBtn = document.getElementById('bgremover-refbg-remove-btn');
        const bgremoverRefBgContainer = document.getElementById('bgremover-refbg-container');

        if (!bgremoverInput) return;

        let uploadedImageData = null;
        let uploadedBgRefData = null;
        let generatedImages = [];
        let selectedBgType = 'transparent';
        let selectedRatio = '1:1';
        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const bgremoverCountSel = document.getElementById('bgremover-count-selection-grid');
        if (bgremoverCountSel) {
            bgremoverCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                bgremoverCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Setup image upload with HEIC support
        setupImageUploadNew(bgremoverInput, bgremoverUploadBox, (data) => {
            uploadedImageData = data;
            bgremoverPreview.src = data.dataUrl;
            bgremoverPlaceholder.classList.add('hidden');
            bgremoverPreview.classList.remove('hidden');
            bgremoverRemoveBtn.classList.remove('hidden');
            bgremoverGenerateBtn.disabled = false;
        });

        // Remove image
        if (bgremoverRemoveBtn) {
            bgremoverRemoveBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                uploadedImageData = null;
                bgremoverInput.value = '';
                bgremoverPreview.src = '';
                bgremoverPreview.classList.add('hidden');
                bgremoverPlaceholder.classList.remove('hidden');
                bgremoverRemoveBtn.classList.add('hidden');
                bgremoverGenerateBtn.disabled = true;
            });
        }

        // Setup option buttons
        setupOptionButtonsNew(bgremoverBgOptions);
        setupOptionButtonsNew(bgremoverRatioOptions);

        // Reference background upload handler
        setupImageUploadNew(bgremoverRefBgInput, bgremoverRefBgUploadBox, (data) => {
            uploadedBgRefData = data;
            bgremoverRefBgPreview.src = data.dataUrl;
            bgremoverRefBgPlaceholder.classList.add('hidden');
            bgremoverRefBgPreview.classList.remove('hidden');
            bgremoverRefBgRemoveBtn.classList.remove('hidden');
        });

        if (bgremoverRefBgRemoveBtn) {
            bgremoverRefBgRemoveBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                uploadedBgRefData = null;
                bgremoverRefBgInput.value = '';
                bgremoverRefBgPreview.src = '';
                bgremoverRefBgPreview.classList.add('hidden');
                bgremoverRefBgPlaceholder.classList.remove('hidden');
                bgremoverRefBgRemoveBtn.classList.add('hidden');
            });
        }

        // Listen to background type changes
        bgremoverBgOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) {
                selectedBgType = btn.dataset.value;
                bgremoverCustomContainer.classList.add('hidden');
                bgremoverRefBgContainer.classList.add('hidden');
                if (selectedBgType === 'custom') {
                    bgremoverCustomContainer.classList.remove('hidden');
                    bgremoverCustomInput.focus();
                } else if (selectedBgType === 'reference-bg') {
                    bgremoverRefBgContainer.classList.remove('hidden');
                }
            }
        });

        // Listen to ratio changes
        bgremoverRatioOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) selectedRatio = btn.dataset.value;
        });

        // Generate button click handler
        bgremoverGenerateBtn.addEventListener('click', async () => {
            if (!uploadedImageData) {
                alert('Mohon upload foto terlebih dahulu!');
                return;
            }
            if (selectedBgType === 'reference-bg' && !uploadedBgRefData) {
                alert('Mohon upload foto referensi background terlebih dahulu!');
                return;
            }

            const originalBtnHTML = bgremoverGenerateBtn.innerHTML;
            bgremoverGenerateBtn.disabled = true;
            bgremoverGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Menghapus Background...';

            bgremoverDownloadAllBtn.classList.add('hidden');
            bgremoverResultsPlaceholder.classList.add('hidden');

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                bgremoverResultsGrid.innerHTML = '';
                bgremoverResultsContainer.classList.remove('hidden');

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `bgremover-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 shadow-lg aspect-square';
                    card.innerHTML = `<div class="w-full h-full flex items-center justify-center"><div class="animate-spin rounded-full h-10 w-10 border-b-2 border-emerald-600"></div></div>`;
                    bgremoverResultsGrid.appendChild(card);
                }

                const generatePromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateBackgroundRemoval(i + 1)
                );
                await Promise.allSettled(generatePromises);

                bgremoverResultsGrid.querySelectorAll('[id^="bgremover-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                successCount = generatedImages.filter(Boolean).length;
            }

            bgremoverGenerateBtn.disabled = false;
            bgremoverGenerateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                bgremoverResultsContainer.classList.add('hidden');
                bgremoverResultsPlaceholder.classList.remove('hidden');
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                bgremoverDownloadAllBtn.classList.remove('hidden');
            }
        });

        // Generate single background removal
        async function generateBackgroundRemoval(index) {
            try {
                // Build background description
                let backgroundDesc = '';
                let isBlurMode = false;
                let isTransparent = false;
                let isReferenceBg = false;

                if (selectedBgType === 'transparent') {
                    backgroundDesc = 'PURE WHITE background (#FFFFFF) - make it look like a professional cutout on white';
                    isTransparent = true;
                } else if (selectedBgType === 'blur') {
                    backgroundDesc = 'beautifully blurred background with professional bokeh effect';
                    isBlurMode = true;
                } else if (selectedBgType === 'white') {
                    backgroundDesc = 'clean solid pure white background (#FFFFFF)';
                } else if (selectedBgType === 'black') {
                    backgroundDesc = 'clean solid pure black background (#000000)';
                } else if (selectedBgType === 'gradient-blue') {
                    backgroundDesc = 'smooth gradient background from light blue to deep blue';
                } else if (selectedBgType === 'gradient-purple') {
                    backgroundDesc = 'smooth gradient background from light purple to deep purple';
                } else if (selectedBgType === 'custom') {
                    backgroundDesc = bgremoverCustomInput.value.trim() || 'professional studio background';
                } else if (selectedBgType === 'reference-bg') {
                    isReferenceBg = true;
                }

                let prompt = '';
                let parts = [];

                if (isBlurMode) {
                    // Special prompt for blur background
                    prompt = `CRITICAL TASK - BACKGROUND BLUR (BOKEH EFFECT):

You MUST create a professional portrait-style photo with beautifully blurred background.

MANDATORY REQUIREMENTS:
\u2713 Keep main subject PERFECTLY SHARP - maintain ALL details, texture, clarity
\u2713 Apply STRONG, NATURAL blur (bokeh) to ENTIRE background
\u2713 Blur should be smooth and gradual, like professional f/1.4-f/2.8 lens
\u2713 Preserve original background colors, just heavily blurred
\u2713 Create realistic bokeh with smooth circular blur if lights present
\u2713 Subject MUST stand out dramatically from soft, dreamy background
\u2713 Maintain natural lighting, shadows, edge transitions on subject
\u2713 VARIATION ${index}: ${index === 1 ? 'Standard bokeh blur' : 'Enhanced bokeh with different blur intensity'}

The result MUST look like professional portrait with tack-sharp subject and beautiful creamy bokeh background.`;
                    parts = [
                        { text: prompt },
                        { inlineData: { mimeType: uploadedImageData.mimeType, data: uploadedImageData.base64 } }
                    ];
                } else if (isTransparent) {
                    // Transparent background mode (use pure white as workaround)
                    prompt = `CRITICAL TASK - BACKGROUND REMOVAL FOR TRANSPARENT CUTOUT:

You MUST remove the background completely and replace with PURE SOLID WHITE.

MANDATORY REQUIREMENTS:
\u2713 PERFECTLY cut out the main subject with CLEAN, SMOOTH edges
\u2713 REMOVE ALL original background elements - delete everything except subject
\u2713 Replace with ABSOLUTE PURE WHITE background (#FFFFFF, RGB 255,255,255)
\u2713 NO shadows, NO gradients, NO traces - just SOLID WHITE
\u2713 Subject edges MUST be perfectly clean - no halos, fringes, or artifacts
\u2713 Maintain subject's quality, details, lighting, colors EXACTLY as original
\u2713 Subject should look like professional cutout on white paper
\u2713 VARIATION ${index}: ${index === 1 ? 'Standard clean cutout' : 'Enhanced edge refinement'}

CRITICAL: The background MUST be completely uniform pure white - this will be used as a transparency mask.
Generate a perfect cutout with subject on solid white background.`;
                    parts = [
                        { text: prompt },
                        { inlineData: { mimeType: uploadedImageData.mimeType, data: uploadedImageData.base64 } }
                    ];
                } else if (isReferenceBg) {
                    // Reference background mode — use both images
                    if (!uploadedBgRefData) throw new Error('No reference background uploaded');
                    prompt = `CRITICAL TASK - BACKGROUND REPLACEMENT USING REFERENCE PHOTO:

IMAGE 1 is the SUBJECT PHOTO. IMAGE 2 is the REFERENCE BACKGROUND.

Your task:
\u2713 Keep the main subject from IMAGE 1 EXACTLY as-is (face, body, outfit, lighting, colors)
\u2713 REMOVE the original background from IMAGE 1 completely
\u2713 REPLACE the background with the scenery/environment from IMAGE 2
\u2713 Match the lighting and color tone of IMAGE 2 naturally onto the subject
\u2713 Blend edges perfectly — NO halos, artifacts, or rough cutouts
\u2713 The result must look like the subject was actually photographed in the IMAGE 2 location
\u2713 Output in ${selectedRatio} aspect ratio
\u2713 VARIATION ${index}: ${index === 1 ? 'Natural seamless blend' : 'Slightly different lighting adaptation'}

The final result must look completely realistic and professional.`;
                    parts = [
                        { text: prompt },
                        { text: 'IMAGE 1 - Subject photo:' },
                        { inlineData: { mimeType: uploadedImageData.mimeType, data: uploadedImageData.base64 } },
                        { text: 'IMAGE 2 - Reference background:' },
                        { inlineData: { mimeType: uploadedBgRefData.mimeType, data: uploadedBgRefData.base64 } }
                    ];
                } else {
                    // Standard background replacement
                    prompt = `CRITICAL TASK - BACKGROUND REMOVAL & REPLACEMENT:

You MUST remove the original background and replace it with: ${backgroundDesc}

MANDATORY REQUIREMENTS:
\u2713 PERFECTLY cut out the main subject with CLEAN, SMOOTH edges
\u2713 REMOVE ALL original background elements completely
\u2713 Replace with ONLY the specified background: ${backgroundDesc}
\u2713 Maintain subject's quality, details, lighting, colors EXACTLY as original
\u2713 Ensure perfect edge detection - NO halos, artifacts, or rough cutouts
\u2713 Keep subject's natural shadows and highlights
\u2713 Make the new background look natural and professional
\u2713 VARIATION ${index}: ${index === 1 ? 'Standard approach' : 'Enhanced edge refinement or lighting'}

The result MUST show complete background replacement with professional quality separation.`;
                    parts = [
                        { text: prompt },
                        { inlineData: { mimeType: uploadedImageData.mimeType, data: uploadedImageData.base64 } }
                    ];
                }

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                    safetySettings: [
                        { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" }
                    ]
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (!base64Data) throw new Error('No image data received from API');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                const bgTypeName = selectedBgType === 'custom' ? 'custom' : selectedBgType;
                const filename = `bg_removed_${bgTypeName}_${index}_${Date.now()}.png`;

                generatedImages[index - 1] = { url: imageUrl, filename: filename };

                const card = document.getElementById(`bgremover-card-${index}`);
                if (card) {
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Background Removed ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2 text-sm">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="bg-gradient-to-r from-emerald-500 to-emerald-600 hover:from-emerald-600 hover:to-emerald-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2 text-sm">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`Error generating background removal ${index}:`, error);
                const card = document.getElementById(`bgremover-card-${index}`);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        bgremoverResultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();

            const actionBtn = e.target.closest('[data-action]');
            if (!actionBtn) return;

            const action = actionBtn.dataset.action;
            const index = parseInt(actionBtn.dataset.index);
            const imageData = generatedImages[index];

            if (!imageData) return;

            if (action === 'preview') {
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageData.url);
                } else {
                    window.open(imageData.url, '_blank');
                }
            } else if (action === 'download') {
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(imageData.url, imageData.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(imageData.url, imageData.filename);
                }
            }
        });

        // Download all button
        bgremoverDownloadAllBtn.addEventListener('click', async (e) => {
            e.stopPropagation();

            if (generatedImages.length === 0) return;

            bgremoverDownloadAllBtn.disabled = true;
            const originalHTML = bgremoverDownloadAllBtn.innerHTML;
            bgremoverDownloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading...</span>';

            for (let i = 0; i < generatedImages.length; i++) {
                const img = generatedImages[i];
                if (!img) continue;
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(img.url, img.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(img.url, img.filename);
                }
                await new Promise(resolve => setTimeout(resolve, 500));
            }

            bgremoverDownloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
            setTimeout(() => {
                bgremoverDownloadAllBtn.disabled = false;
                bgremoverDownloadAllBtn.innerHTML = originalHTML;
            }, 2000);
        });
    })();

    // ==================== PHOTO EXTENDER ====================
    (function() {
        const apiKey = "";

        // Element references
        const extenderInput = document.getElementById('extender-input');
        const extenderUploadBox = document.getElementById('extender-upload-box');
        const extenderPreview = document.getElementById('extender-preview');
        const extenderPlaceholder = document.getElementById('extender-placeholder');
        const extenderRemoveBtn = document.getElementById('extender-remove-btn');
        const extenderGenerateBtn = document.getElementById('extender-generate-btn');
        const extenderResultsGrid = document.getElementById('extender-results-grid');
        const extenderDownloadAllBtn = document.getElementById('extender-download-all-btn');

        // Mobile preview elements
        const extenderPreviewContainer = document.getElementById('extender-preview-container');
        const extenderImagePreview = document.getElementById('extender-image-preview');
        const extenderRemovePreview = document.getElementById('extender-remove-preview');

        let originalImageData = null;
        let selectedDirection = 'all';
        let selectedAspectRatio = '16:9';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedImages = [];

        // Count selection (1-10), default 4
        const extenderCountSel = document.getElementById('extender-count-selection-grid');
        if (extenderCountSel) {
            extenderCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                extenderCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Setup option buttons using helper function for mobile compatibility
        const directionOptions = document.getElementById('extender-direction-options');
        const ratioOptions = document.getElementById('extender-ratio-options');

        setupOptionButtonsNew(directionOptions);
        setupOptionButtonsNew(ratioOptions);

        // Listen to direction changes
        directionOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) {
                selectedDirection = btn.dataset.value;
                console.log('Direction selected:', selectedDirection);
            }
        });

        // Listen to ratio changes
        ratioOptions.addEventListener('click', (e) => {
            const btn = e.target.closest('[data-value]');
            if (btn) {
                selectedAspectRatio = btn.dataset.value;
                console.log('Aspect ratio selected:', selectedAspectRatio);
            }
        });

        // Setup image upload with HEIC support (using helper function for mobile compatibility)
        setupImageUploadNew(extenderInput, extenderUploadBox, (data) => {
            originalImageData = data.dataUrl;
            extenderPreview.src = data.dataUrl;
            extenderPreview.classList.remove('hidden');
            extenderPlaceholder.classList.add('hidden');
            extenderRemoveBtn.classList.remove('hidden');
            extenderGenerateBtn.disabled = false;

            // Show mobile preview
            if (extenderImagePreview && extenderPreviewContainer) {
                extenderImagePreview.src = data.dataUrl;
                extenderPreviewContainer.classList.remove('hidden');
            }
        });

        // Remove button
        extenderRemoveBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            originalImageData = null;
            extenderInput.value = '';
            extenderPreview.src = '';
            extenderPreview.classList.add('hidden');
            extenderPlaceholder.classList.remove('hidden');
            extenderRemoveBtn.classList.add('hidden');
            extenderGenerateBtn.disabled = true;
            extenderResultsGrid.innerHTML = '';
            generatedImages = [];
            extenderDownloadAllBtn.classList.add('hidden');

            // Hide mobile preview
            if (extenderPreviewContainer && extenderImagePreview) {
                extenderPreviewContainer.classList.add('hidden');
                extenderImagePreview.src = '';
            }
        });

        // Mobile remove preview button
        if (extenderRemovePreview) {
            extenderRemovePreview.addEventListener('click', (e) => {
                e.stopPropagation();
                originalImageData = null;
                extenderInput.value = '';
                extenderPreview.src = '';
                extenderPreview.classList.add('hidden');
                extenderPlaceholder.classList.remove('hidden');
                extenderRemoveBtn.classList.add('hidden');
                extenderGenerateBtn.disabled = true;
                extenderResultsGrid.innerHTML = '';
                generatedImages = [];
                extenderDownloadAllBtn.classList.add('hidden');

                // Hide mobile preview
                if (extenderPreviewContainer && extenderImagePreview) {
                    extenderPreviewContainer.classList.add('hidden');
                    extenderImagePreview.src = '';
                }
            });
        }

        // Generate button
        extenderGenerateBtn.addEventListener('click', async () => {
            if (!originalImageData) {
                alert('Silakan upload foto terlebih dahulu');
                return;
            }

            extenderGenerateBtn.disabled = true;
            extenderGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Membuat...';
            extenderDownloadAllBtn.classList.add('hidden');

            const resultsPlaceholder = document.getElementById('extender-results-placeholder');
            const resultsContainer = document.getElementById('extender-results-container');
            if (resultsPlaceholder) resultsPlaceholder.classList.add('hidden');

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                extenderResultsGrid.innerHTML = '';
                if (resultsContainer) resultsContainer.classList.remove('hidden');

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `extender-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-video';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-sky-600"></div>';
                    extenderResultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSingleImage(i + 1)
                );
                await Promise.allSettled(generationPromises);

                extenderResultsGrid.querySelectorAll('[id^="extender-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                successCount = generatedImages.filter(Boolean).length;
            }

            extenderGenerateBtn.disabled = false;
            extenderGenerateBtn.innerHTML = '<i class="fas fa-expand-arrows-alt mr-2"></i>Perluas Foto';

            if (successCount === 0) {
                if (resultsContainer) resultsContainer.classList.add('hidden');
                if (resultsPlaceholder) resultsPlaceholder.classList.remove('hidden');
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                extenderDownloadAllBtn.classList.remove('hidden');
            }
        });

        async function generateSingleImage(index) {
            try {
                const base64Data = originalImageData.split(',')[1];

                // Build direction instruction
                let directionInstruction = '';
                switch(selectedDirection) {
                    case 'all':
                        directionInstruction = 'Extend the image in ALL directions (top, bottom, left, right) to expand the scene comprehensively';
                        break;
                    case 'horizontal':
                        directionInstruction = 'Extend the image HORIZONTALLY (left and right sides) to widen the scene';
                        break;
                    case 'vertical':
                        directionInstruction = 'Extend the image VERTICALLY (top and bottom) to make the scene taller';
                        break;
                    case 'left':
                        directionInstruction = 'Extend the image to the LEFT side only, adding more content on the left';
                        break;
                    case 'right':
                        directionInstruction = 'Extend the image to the RIGHT side only, adding more content on the right';
                        break;
                    case 'top':
                        directionInstruction = 'Extend the image to the TOP only, adding more content above';
                        break;
                    case 'bottom':
                        directionInstruction = 'Extend the image to the BOTTOM only, adding more content below';
                        break;
                }

                const prompt = `Create a photorealistic image that expands this scene to a ${selectedAspectRatio} aspect ratio.

DIRECTION: ${directionInstruction}

CRITICAL REQUIREMENTS:
1. Keep ALL visible content from the original photo EXACTLY as shown - preserve faces, objects, colors, details
2. The original scene must remain the focal point and stay perfectly recognizable
3. Expand the scene naturally by adding complementary content in the specified direction
4. Match the original photo's style, lighting, colors, and atmosphere PERFECTLY
5. Ensure seamless blending - no visible borders, seams, or discontinuities
6. Maintain consistent perspective, depth, and 3D spatial relationships
7. Continue any patterns, textures, or architectural elements logically
8. If objects/people are partially visible at edges, complete them naturally
9. Keep the same level of photorealism and detail throughout

STYLE & QUALITY:
- Exact color palette match (temperature, saturation, contrast)
- Same lighting direction and intensity
- Identical photographic quality and sharpness
- Consistent artistic style (maintain realism level)
- Continue weather/atmosphere (indoor/outdoor, lighting conditions)

CONTENT GUIDELINES:
- Landscapes: naturally extend terrain, sky, vegetation, water, horizon
- Interiors: logically expand room space, walls, floors, furniture, decorations
- Portraits: appropriately expand background setting and environment
- Products/Objects: show more surrounding context and environment
- Architecture: complete building structures and surroundings realistically
- Urban scenes: add consistent cityscape elements (buildings, streets, vehicles)

OUTPUT: Generate in ${selectedAspectRatio} aspect ratio with the original content preserved in its natural position, expanded seamlessly in the specified direction.

Important: This should look like ONE cohesive photograph, not a composite. The expansion should be so natural that viewers cannot identify where the original ends.`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: "image/png", data: base64Data } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: {
                            aspectRatio: selectedAspectRatio
                        }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${imageData}`;
                const filename = `photo-extended-${Date.now()}-${index}.png`;

                generatedImages[index - 1] = { url: imageUrl, filename: filename };

                const card = document.getElementById(`extender-card-${index}`);
                if (card) {
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Extended Photo ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-sky-500 to-sky-600 hover:from-sky-600 hover:to-sky-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }
            } catch (error) {
                console.error('Error generating extended image:', error);
                const card = document.getElementById(`extender-card-${index}`);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        extenderResultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();

            const actionBtn = e.target.closest('[data-action]');
            if (!actionBtn) return;

            const action = actionBtn.dataset.action;
            const index = parseInt(actionBtn.dataset.index, 10);
            const imageData = generatedImages[index];

            if (action === 'preview') {
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageData.url);
                } else {
                    window.open(imageData.url, '_blank');
                }
            } else if (action === 'download') {
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(imageData.url, imageData.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(imageData.url, imageData.filename);
                }
            }
        });

        // Download All button
        extenderDownloadAllBtn.addEventListener('click', async (e) => {
            e.stopPropagation();

            // Prevent multiple clicks
            if (generatedImages.length === 0 || extenderDownloadAllBtn.disabled) return;

            const originalHTML = extenderDownloadAllBtn.innerHTML;
            extenderDownloadAllBtn.disabled = true;

            try {
                for (let i = 0; i < generatedImages.length; i++) {
                    const img = generatedImages[i];
                    extenderDownloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                    try {
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(img.url, img.filename);
                        } else if (window.downloadImage) {
                            await window.downloadImage(img.url, img.filename);
                        }
                        await new Promise(resolve => setTimeout(resolve, 500));
                    } catch (error) {
                        console.error(`Error downloading image ${i + 1}:`, error);
                        // Continue with next image even if one fails
                    }
                }

                extenderDownloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                setTimeout(() => {
                    extenderDownloadAllBtn.disabled = false;
                    extenderDownloadAllBtn.innerHTML = originalHTML;
                }, 2000);
            } catch (error) {
                console.error('Download all error:', error);
                extenderDownloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                setTimeout(() => {
                    extenderDownloadAllBtn.disabled = false;
                    extenderDownloadAllBtn.innerHTML = originalHTML;
                }, 2000);
            }
        });
    })();

    // ==================== STORY BOARD ====================
    (function() {
        const apiKey = "";

        // Element references
        const topicInput = document.getElementById('storyboard-topic');
        const generateBtn = document.getElementById('storyboard-generate-btn');
        const resultsGrid = document.getElementById('storyboard-results-grid');
        const downloadAllBtn = document.getElementById('storyboard-download-all-btn');

        // Modal elements
        const regenerateModal = document.getElementById('storyboard-regenerate-modal');
        const modalClose = document.getElementById('storyboard-modal-close');
        const modalCancel = document.getElementById('storyboard-modal-cancel');
        const modalRegenerateBtn = document.getElementById('storyboard-modal-regenerate');
        const modalPrompt = document.getElementById('storyboard-modal-prompt');
        const modalFrameNumber = document.getElementById('storyboard-modal-frame-number');
        const modalFrameText = document.getElementById('storyboard-modal-frame-text');
        const modalPreviewContainer = document.getElementById('storyboard-modal-preview-container');
        const modalPreviewImg = document.getElementById('storyboard-modal-preview-img');

        let selectedStoryType = 'social-media';
        let selectedFrameCount = 5;
        let selectedVisualStyle = 'sketch';
        let selectedLanguage = 'indonesian';
        let selectedAspectRatio = '16:9';
        let generatedImages = [];

        function getAspectClass() {
            if (selectedAspectRatio === '9:16') return 'aspect-[9/16]';
            if (selectedAspectRatio === '1:1') return 'aspect-square';
            return 'aspect-video';
        }
        let sceneDescriptions = []; // Store scene descriptions for regeneration
        let currentRegenerateIndex = -1; // Track which frame is being regenerated

        // Story type buttons
        const storyTypeButtons = document.querySelectorAll('#content-story-board .option-btn-storyboard[data-value]');
        storyTypeButtons.forEach(btn => {
            // Check if it's a story type button (in step 2)
            if (btn.closest('div').previousElementSibling?.textContent?.includes('Tipe Story')) {
                btn.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();

                    const parent = this.closest('div');
                    parent.querySelectorAll('.option-btn-storyboard').forEach(b => b.classList.remove('selected'));
                    this.classList.add('selected');
                    selectedStoryType = this.dataset.value;
                });
            }
            // Check if it's a frame count button (in step 3)
            else if (btn.closest('div').previousElementSibling?.textContent?.includes('Jumlah Frame')) {
                btn.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();

                    const parent = this.closest('div');
                    parent.querySelectorAll('.option-btn-storyboard').forEach(b => b.classList.remove('selected'));
                    this.classList.add('selected');
                    selectedFrameCount = parseInt(this.dataset.value, 10);
                });
            }
            // Check if it's a visual style button (in step 4)
            else if (btn.closest('div').previousElementSibling?.textContent?.includes('Style Visual')) {
                btn.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();

                    const parent = this.closest('div');
                    parent.querySelectorAll('.option-btn-storyboard').forEach(b => b.classList.remove('selected'));
                    this.classList.add('selected');
                    selectedVisualStyle = this.dataset.value;
                });
            }
            // Check if it's a language button (in step 5)
            else if (btn.closest('div').previousElementSibling?.textContent?.includes('Bahasa Deskripsi')) {
                btn.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();

                    const parent = this.closest('div');
                    parent.querySelectorAll('.option-btn-storyboard').forEach(b => b.classList.remove('selected'));
                    this.classList.add('selected');
                    selectedLanguage = this.dataset.value;
                });
            }
            // Check if it's a frame size button (in step 6)
            else if (btn.closest('div').previousElementSibling?.textContent?.includes('Ukuran Frame')) {
                btn.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();

                    const parent = this.closest('div');
                    parent.querySelectorAll('.option-btn-storyboard').forEach(b => b.classList.remove('selected'));
                    this.classList.add('selected');
                    selectedAspectRatio = this.dataset.value;
                });
            }
        });

        // Generate button
        generateBtn.addEventListener('click', async () => {
            const topic = topicInput.value.trim();

            if (!topic) {
                alert('Silakan masukkan topik story terlebih dahulu');
                return;
            }

            generateBtn.disabled = true;
            generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Membuat Storyboard...';
            resultsGrid.innerHTML = '';
            generatedImages = [];
            sceneDescriptions = [];
            downloadAllBtn.classList.add('hidden');

            // Generate scene descriptions once
            sceneDescriptions = await generateSceneDescriptions(topic);
            if (!sceneDescriptions || sceneDescriptions.length === 0) {
                alert('Gagal membuat deskripsi scene. Silakan coba lagi.');
                generateBtn.disabled = false;
                generateBtn.innerHTML = '<i class="fas fa-film mr-2"></i>Generate Story Board';
                return;
            }

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                resultsGrid.innerHTML = '';

                for (let i = 0; i < selectedFrameCount; i++) {
                    const card = document.createElement('div');
                    card.id = `storyboard-card-${i + 1}`;
                    card.className = 'relative rounded-xl overflow-hidden bg-gradient-to-br from-purple-50 to-violet-50 border-2 border-purple-200';
                    card.innerHTML = `
                        <div class="${getAspectClass()} bg-gray-100 flex items-center justify-center">
                            <div class="animate-spin rounded-full h-10 w-10 border-b-2 border-purple-600"></div>
                        </div>
                        <div class="p-3 bg-white border-t-2 border-purple-100">
                            <div class="flex items-center gap-2 mb-2">
                                <div class="w-6 h-6 rounded-full bg-gradient-to-br from-purple-500 to-violet-600 text-white text-xs font-bold flex items-center justify-center">${i + 1}</div>
                                <span class="text-xs font-semibold text-gray-700">Frame ${i + 1}</span>
                            </div>
                            <p class="text-xs text-gray-600 line-clamp-2">${sceneDescriptions[i] || 'Generating...'}</p>
                        </div>
                    `;
                    resultsGrid.appendChild(card);
                }

                const generationPromises = sceneDescriptions.map((description, index) =>
                    generateFrameImage(index + 1, description)
                );
                await Promise.allSettled(generationPromises);

                resultsGrid.querySelectorAll('[id^="storyboard-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                successCount = generatedImages.filter(Boolean).length;
            }

            generateBtn.disabled = false;
            generateBtn.innerHTML = '<i class="fas fa-film mr-2"></i>Generate Story Board';

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            } else {
                downloadAllBtn.classList.remove('hidden');
            }
        });

        async function generateSceneDescriptions(topic) {
            try {
                // Map story types to descriptions
                const storyTypeDescriptions = {
                    'social-media': 'Social media story (Instagram/TikTok format, engaging and trendy)',
                    'video-content': 'Video content (YouTube/Vlog style, informative and entertaining)',
                    'film-scene': 'Film scene (Cinematic, dramatic, with proper shot composition)',
                    'tutorial': 'Tutorial video (Step-by-step, educational, clear instructions)',
                    'marketing': 'Marketing campaign (Persuasive, brand-focused, call-to-action)',
                    'product-demo': 'Product demonstration (Feature showcase, benefits-focused)',
                    'event': 'Event coverage (Documentary style, capturing key moments)',
                    'animation': 'Animation sequence (Creative, expressive, story-driven)'
                };

                const languageInstruction = selectedLanguage === 'indonesian'
                    ? 'Write all descriptions in Bahasa Indonesia (Indonesian language).'
                    : 'Write all descriptions in English language.';

                const prompt = `You are creating a visual storyboard about: "${topic}"

MAIN TOPIC: "${topic}" - Every frame MUST directly relate to this topic!

Story Type: ${storyTypeDescriptions[selectedStoryType]}
Visual Style: ${selectedVisualStyle}
Number of Frames: ${selectedFrameCount}
${languageInstruction}

TASK:
Create ${selectedFrameCount} connected visual scenes that tell ONE complete story about "${topic}".
The story should flow naturally from beginning to end, with each frame continuing from the previous one.

REQUIREMENTS:
1. ALL frames must be about the SAME topic: "${topic}"
2. Create EXACTLY ${selectedFrameCount} scenes (no more, no less)
3. Each scene: 2-3 sentences with visual details (camera angle, subject, action, background, mood)
4. Scenes must connect: Frame 2 continues from Frame 1, Frame 3 from Frame 2, etc.
5. Story structure: Beginning (frames 1-${Math.ceil(selectedFrameCount/3)}) → Middle (frames ${Math.ceil(selectedFrameCount/3)+1}-${Math.ceil(selectedFrameCount*2/3)}) → End (frames ${Math.ceil(selectedFrameCount*2/3)+1}-${selectedFrameCount})

FORMAT (copy this exactly):
${Array.from({ length: selectedFrameCount }, (_, i) => {
    const frameNum = i + 1;
    return frameNum === 1
        ? `Frame 1: [Your description here - 2-3 sentences about ${topic}]`
        : `Frame ${frameNum}: [Your description here - 2-3 sentences continuing the ${topic} story]`;
}).join('\n')}

Remember: EVERY frame must be directly about "${topic}". Start creating the ${selectedFrameCount} frames now:`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const tokensPerFrame = 200;
                const minTokens = selectedFrameCount * tokensPerFrame;
                const maxOutputTokens = Math.max(2048, Math.min(minTokens + 500, 8192));

                const payload = {
                    contents: [{
                        parts: [{ text: prompt }]
                    }],
                    generationConfig: {
                        temperature: 0.9,
                        maxOutputTokens: maxOutputTokens
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const text = result?.candidates?.[0]?.content?.parts?.[0]?.text;

                if (!text) {
                    throw new Error('No text in response');
                }

                console.log('Raw API response:', text);

                // Parse frame descriptions with improved multi-line handling
                const frames = [];
                const frameRegex = /Frame (\d+):\s*(.*?)(?=\n\s*Frame \d+:|$)/gis;
                const matches = [...text.matchAll(frameRegex)];

                console.log(`Found ${matches.length} frame matches in response`);

                for (const match of matches) {
                    const frameNum = parseInt(match[1]);
                    let description = match[2].trim();

                    // Clean up the description
                    description = description
                        .replace(/\n+/g, ' ') // Replace newlines with spaces
                        .replace(/\s+/g, ' ')  // Normalize whitespace
                        .trim();

                    if (description) {
                        frames.push(description);
                        console.log(`Frame ${frameNum}: ${description.substring(0, 100)}...`);
                    }
                }

                // Fallback: if regex parsing fails, try line-by-line parsing
                if (frames.length === 0) {
                    console.log('Regex parsing failed, trying line-by-line...');
                    const lines = text.split('\n');
                    let currentDescription = '';

                    for (const line of lines) {
                        if (line.match(/^Frame \d+:/i)) {
                            if (currentDescription) {
                                frames.push(currentDescription.trim());
                            }
                            currentDescription = line.replace(/^Frame \d+:/i, '').trim();
                        } else if (line.trim() && currentDescription) {
                            currentDescription += ' ' + line.trim();
                        }
                    }

                    if (currentDescription) {
                        frames.push(currentDescription.trim());
                    }
                }

                console.log(`Total frames parsed: ${frames.length}, Expected: ${selectedFrameCount}`);

                // Validate and ensure we have exactly the right number of frames
                if (frames.length < selectedFrameCount) {
                    console.warn(`Warning: Only ${frames.length} frames parsed, expected ${selectedFrameCount}`);
                    // Fill missing frames with placeholder
                    while (frames.length < selectedFrameCount) {
                        frames.push(`Continuation of scene ${frames.length} - Scene ${frames.length + 1} of the story`);
                    }
                } else if (frames.length > selectedFrameCount) {
                    console.log(`Trimming ${frames.length - selectedFrameCount} extra frames`);
                }

                return frames.slice(0, selectedFrameCount);
            } catch (error) {
                console.error('Error generating scene descriptions:', error);
                // Return default descriptions as fallback
                return Array.from({ length: selectedFrameCount }, (_, i) =>
                    `Scene ${i + 1}: Visual representation of the story`
                );
            }
        }

        async function generateFrameImage(frameNumber, sceneDescription) {
            try {
                // Map visual styles to style descriptions
                const styleDescriptions = {
                    'sketch': 'hand-drawn sketch style, pencil lines, storyboard sketch, black and white or minimal color',
                    'realistic': 'photorealistic style, detailed and lifelike, professional photography quality',
                    'cartoon': 'cartoon illustration style, vibrant colors, expressive and fun',
                    'minimalist': 'minimalist design, simple shapes, clean lines, limited color palette',
                    'cinematic': 'cinematic film still, dramatic lighting, movie-quality composition',
                    'comic': 'comic book style, bold outlines, dynamic panels, graphic novel aesthetic'
                };

                const prompt = `Create a storyboard frame in ${styleDescriptions[selectedVisualStyle]}.

Scene Description: ${sceneDescription}

CRITICAL REQUIREMENTS FOR STORYBOARD FRAME:
1. Visualize the scene exactly as described
2. Apply ${selectedVisualStyle} style consistently
3. Clear composition suitable for storyboarding
4. Include visual storytelling elements
5. Frame should be self-contained but part of a sequence
6. Professional storyboard quality
7. Aspect ratio: ${selectedAspectRatio}${selectedAspectRatio === '9:16' ? ' (vertical portrait format for TikTok/Reels/Stories)' : selectedAspectRatio === '1:1' ? ' (square format for Instagram/Square video)' : ' (landscape format for video/YouTube)'}

${selectedVisualStyle === 'sketch' ?
    'Create in hand-drawn storyboard sketch style with clear pencil lines, annotations, and professional storyboard appearance.' :
    'Create in the specified visual style with high quality and clarity.'}`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [{ text: prompt }]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: {
                            aspectRatio: selectedAspectRatio
                        }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(part => part.inlineData)?.inlineData?.data;

                if (!imageData) {
                    throw new Error('No image data in response');
                }

                const imageUrl = `data:image/png;base64,${imageData}`;
                const filename = `storyboard-frame-${frameNumber}-${Date.now()}.png`;

                // Store at correct index (not push) to maintain order in parallel execution
                generatedImages[frameNumber - 1] = { url: imageUrl, filename: filename, description: sceneDescription };

                // Update card with image
                const card = document.getElementById(`storyboard-card-${frameNumber}`);
                if (card) {
                    card.innerHTML = `
                        <div class="relative group">
                            <img src="${imageUrl}" class="w-full ${getAspectClass()} object-cover" alt="Frame ${frameNumber}">
                            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex flex-col items-center justify-center gap-2 p-3">
                                <div class="flex gap-2">
                                    <button data-action="preview" data-index="${frameNumber - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2 text-sm">
                                        <i class="fas fa-eye"></i>
                                        <span class="hidden sm:inline">Preview</span>
                                    </button>
                                    <button data-action="download" data-index="${frameNumber - 1}" class="action-btn bg-gradient-to-r from-purple-500 to-violet-600 hover:from-purple-600 hover:to-violet-700 text-white px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2 text-sm">
                                        <i class="fas fa-download"></i>
                                        <span class="hidden sm:inline">Download</span>
                                    </button>
                                </div>
                                <button data-action="regenerate" data-index="${frameNumber - 1}" data-frame="${frameNumber}" class="action-btn bg-gradient-to-r from-orange-500 to-amber-600 hover:from-orange-600 hover:to-amber-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2 text-sm w-full sm:w-auto">
                                    <i class="fas fa-sync-alt"></i>
                                    <span>Regenerate</span>
                                </button>
                            </div>
                        </div>
                        <div class="p-3 bg-white border-t-2 border-purple-100">
                            <div class="flex items-center justify-between gap-2 mb-2">
                                <div class="flex items-center gap-2">
                                    <div class="w-6 h-6 rounded-full bg-gradient-to-br from-purple-500 to-violet-600 text-white text-xs font-bold flex items-center justify-center">${frameNumber}</div>
                                    <span class="text-xs font-semibold text-gray-700">Frame ${frameNumber}</span>
                                </div>
                                <button data-action="edit-prompt" data-index="${frameNumber - 1}" data-frame="${frameNumber}" class="action-btn text-purple-600 hover:text-purple-800 transition-colors" title="Edit Prompt">
                                    <i class="fas fa-edit text-sm"></i>
                                </button>
                            </div>
                            <p class="text-xs text-gray-600 line-clamp-3">${sceneDescription}</p>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`Error generating frame ${frameNumber}:`, error);
                const card = document.getElementById(`storyboard-card-${frameNumber}`);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        resultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();

            const actionBtn = e.target.closest('[data-action]');
            if (!actionBtn) return;

            const action = actionBtn.dataset.action;
            const index = parseInt(actionBtn.dataset.index, 10);
            const imageData = generatedImages[index];

            if (action === 'preview') {
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageData.url);
                } else {
                    window.open(imageData.url, '_blank');
                }
            } else if (action === 'download') {
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(imageData.url, imageData.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(imageData.url, imageData.filename);
                }
            } else if (action === 'regenerate' || action === 'edit-prompt') {
                // Open modal for editing prompt and regenerating
                const frameNumber = parseInt(actionBtn.dataset.frame, 10);
                currentRegenerateIndex = frameNumber - 1; // Store 0-based index

                // Set modal content
                modalFrameNumber.textContent = frameNumber;
                modalFrameText.textContent = frameNumber;
                modalPrompt.value = sceneDescriptions[currentRegenerateIndex] || '';

                // Show current image preview if exists
                if (imageData && imageData.url) {
                    modalPreviewImg.src = imageData.url;
                    modalPreviewContainer.classList.remove('hidden');
                } else {
                    modalPreviewContainer.classList.add('hidden');
                }

                // Show modal
                regenerateModal.classList.remove('hidden');
                modalPrompt.focus();
            }
        });

        // Download All button
        downloadAllBtn.addEventListener('click', async (e) => {
            e.stopPropagation();

            // Prevent multiple clicks
            if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

            const originalHTML = downloadAllBtn.innerHTML;
            downloadAllBtn.disabled = true;

            try {
                for (let i = 0; i < generatedImages.length; i++) {
                    const img = generatedImages[i];
                    downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                    try {
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(img.url, img.filename);
                        } else if (window.downloadImage) {
                            await window.downloadImage(img.url, img.filename);
                        }
                        await new Promise(resolve => setTimeout(resolve, 500));
                    } catch (error) {
                        console.error(`Error downloading image ${i + 1}:`, error);
                        // Continue with next image even if one fails
                    }
                }

                downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                setTimeout(() => {
                    downloadAllBtn.disabled = false;
                    downloadAllBtn.innerHTML = originalHTML;
                }, 2000);
            } catch (error) {
                console.error('Download all error:', error);
                downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                setTimeout(() => {
                    downloadAllBtn.disabled = false;
                    downloadAllBtn.innerHTML = originalHTML;
                }, 2000);
            }
        });

        // Modal close handlers
        function closeModal() {
            regenerateModal.classList.add('hidden');
            currentRegenerateIndex = -1;
            modalPrompt.value = '';
            modalPreviewContainer.classList.add('hidden');
        }

        modalClose.addEventListener('click', closeModal);
        modalCancel.addEventListener('click', closeModal);

        // Close modal when clicking outside
        regenerateModal.addEventListener('click', (e) => {
            if (e.target === regenerateModal) {
                closeModal();
            }
        });

        // Close modal with ESC key
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && !regenerateModal.classList.contains('hidden')) {
                closeModal();
            }
        });

        // Modal regenerate button
        modalRegenerateBtn.addEventListener('click', async () => {
            if (currentRegenerateIndex < 0 || currentRegenerateIndex >= selectedFrameCount) {
                alert('Invalid frame index');
                return;
            }

            const customPrompt = modalPrompt.value.trim();
            if (!customPrompt) {
                alert('Deskripsi scene tidak boleh kosong');
                modalPrompt.focus();
                return;
            }

            // Prevent double-click
            if (modalRegenerateBtn.disabled) return;

            // Show loading state
            const originalHTML = modalRegenerateBtn.innerHTML;
            modalRegenerateBtn.disabled = true;
            modalRegenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Processing...';

            // Update scene description
            sceneDescriptions[currentRegenerateIndex] = customPrompt;

            // Close modal after a brief delay to show feedback
            setTimeout(() => {
                closeModal();
                modalRegenerateBtn.disabled = false;
                modalRegenerateBtn.innerHTML = originalHTML;
            }, 300);

            // Show loading state on the specific card
            const frameNumber = currentRegenerateIndex + 1;
            const card = document.getElementById(`storyboard-card-${frameNumber}`);
            if (card) {
                card.innerHTML = `
                    <div class="${getAspectClass()} bg-gradient-to-br from-purple-50 to-violet-50 flex items-center justify-center">
                        <div class="text-center">
                            <div class="animate-spin rounded-full h-10 w-10 border-b-2 border-purple-600 mx-auto mb-3"></div>
                            <p class="text-sm text-gray-600 font-semibold">Regenerating Frame ${frameNumber}...</p>
                        </div>
                    </div>
                    <div class="p-3 bg-white border-t-2 border-purple-100">
                        <div class="flex items-center gap-2 mb-2">
                            <div class="w-6 h-6 rounded-full bg-gradient-to-br from-purple-500 to-violet-600 text-white text-xs font-bold flex items-center justify-center">${frameNumber}</div>
                            <span class="text-xs font-semibold text-gray-700">Frame ${frameNumber}</span>
                        </div>
                        <p class="text-xs text-gray-600 line-clamp-2">${customPrompt}</p>
                    </div>
                `;
            }

            // Remove old image from generatedImages array
            const oldIndex = generatedImages.findIndex(img => img.filename.includes(`frame-${frameNumber}-`));
            if (oldIndex !== -1) {
                generatedImages.splice(oldIndex, 1);
            }

            // Regenerate the frame
            try {
                await generateFrameImage(frameNumber, customPrompt);
            } catch (error) {
                console.error('Error regenerating frame:', error);
                alert('Gagal regenerate frame. Silakan coba lagi.');
            }
        });
    })();

    // ==================== TIMELAPSE RENOVASI ====================
    (function() {
        const apiKey = "";

        // DOM references
        const beforeBox = document.getElementById('timelapse-before-box');
        const beforeInput = document.getElementById('timelapse-before-input');
        const beforePlaceholder = document.getElementById('timelapse-before-placeholder');
        const beforePreviewContainer = document.getElementById('timelapse-before-preview-container');
        const beforePreviewImg = document.getElementById('timelapse-before-preview');
        const removeBeforeBtn = document.getElementById('timelapse-remove-before');

        const afterBox = document.getElementById('timelapse-after-box');
        const afterInput = document.getElementById('timelapse-after-input');
        const afterPlaceholder = document.getElementById('timelapse-after-placeholder');
        const afterPreviewContainer = document.getElementById('timelapse-after-preview-container');
        const afterPreviewImg = document.getElementById('timelapse-after-preview');
        const removeAfterBtn = document.getElementById('timelapse-remove-after');

        const themeOptions = document.getElementById('timelapse-theme-options');
        const frameCountContainer = document.getElementById('timelapse-frame-count');
        const ratioOptions = document.getElementById('timelapse-ratio-options');
        const generateBtn = document.getElementById('timelapse-generate-btn');
        const loadingDiv = document.getElementById('timelapse-loading');
        const loadingText = document.getElementById('timelapse-loading-text');
        const progressBar = document.getElementById('timelapse-progress-bar');
        const progressText = document.getElementById('timelapse-progress-text');
        const resultsHeader = document.getElementById('timelapse-results-header');
        const resultsCount = document.getElementById('timelapse-results-count');
        const resultsGrid = document.getElementById('timelapse-results-grid');
        const emptyState = document.getElementById('timelapse-empty-state');
        const downloadAllBtn = document.getElementById('timelapse-download-all-btn');

        if (!beforeBox) return;

        // State
        let beforeImageData = null;
        let afterImageData = null;
        let selectedTheme = 'modern-minimalis';
        let selectedFrameCount = 3;
        let selectedRatio = '9:16';
        let generatedImages = [];

        const themeDescriptions = {
            'modern-minimalis': 'clean modern minimalist architecture, white walls, flat roof, minimal ornamentation, large windows, open floor plan, contemporary design',
            'scandinavian': 'Scandinavian design, white and light wood tones, clean lines, hygge aesthetics, warm neutral palette, cozy minimalism',
            'industrial-loft': 'industrial architecture, exposed brick walls, steel beams, large factory windows, concrete surfaces, dark moody tones',
            'tropis-modern': 'tropical modern architecture, open natural ventilation, rattan and bamboo accents, lush tropical plants, indoor-outdoor living space',
            'japanese-zen': 'Japanese minimalist architecture, wabi-sabi philosophy, natural materials, bamboo elements, shoji sliding panels, peaceful zen garden',
            'mediterranean': 'Mediterranean villa style, terracotta clay roof tiles, whitewashed walls, arched doorways, warm stone textures, blue shutters',
            'art-deco-mewah': 'Art Deco luxury architecture, bold geometric patterns, gold metallic accents, polished marble surfaces, opulent glamorous details',
            'rustic-farmhouse': 'rustic farmhouse architecture, reclaimed weathered wood, barn aesthetic, stone chimney, corrugated metal roof, warm rural character',
            'urban-kontemporer': 'urban contemporary architecture, bold dark exterior finish, full glass facade, metal accents, dramatic strong architectural lines',
            'bohemian': 'bohemian eclectic architecture, colorful vibrant facade, mixed textures, artistic murals, mosaic tiles, free-spirited creative character',
            'classic-elegan': 'classical elegant architecture, grand columns, symmetrical facade, traditional ornamental details, timeless refined beauty',
            'smart-futuristik': 'smart futuristic architecture, ultra-modern tech design, integrated solar panels, automated smart home, sleek minimal geometry',
            'eco-green': 'eco-sustainable green architecture, living roof covered in plants, vertical garden walls, natural bamboo construction, solar panels',
            'coastal-beach': 'coastal beach house architecture, white and blue color scheme, driftwood natural details, large ocean-facing windows, airy tropical feel',
            'retro-vintage': 'retro vintage aesthetic architecture, warm 1970s-inspired color palette, period detailing, nostalgic charming character',
            'luxury-mansion': 'luxury grand mansion architecture, palatial grand entrance, soaring high ceilings, premium marble surfaces, gold accents, estate grounds',
            'container-house': 'shipping container home conversion, modern industrial container architecture, bold powder-coated colors, stacked container modules',
            'ruko-modern': 'modern Indonesian shophouse renovation, contemporary ruko facade, glass storefront display, clean commercial signage, professional aesthetic',
            'cafe-resto': 'commercial renovation to cafe or restaurant, warm welcoming entrance, aesthetic signage, exposed brick, industrial chic cafe design',
            'heritage-restoration': 'heritage building restoration, carefully preserved original colonial architecture, restored antique details, classic historical beauty'
        };

        function getStageDescriptions(total) {
            if (total === 2) return [
                'initial state before any renovation work, original untouched condition',
                'fully completed renovation, beautiful finished result'
            ];
            if (total === 3) return [
                'initial state before renovation, original untouched condition',
                'mid-construction renovation phase, walls up, work in progress',
                'fully completed renovation, beautiful finished result'
            ];
            if (total === 4) return [
                'initial state before renovation, original untouched condition',
                'early construction phase, foundation work and demolition started',
                'near-completion phase, exterior almost finished, final touches',
                'fully completed renovation, beautiful finished result'
            ];
            if (total === 5) return [
                'initial state before renovation, original untouched condition',
                'demolition and site clearing phase, materials removed',
                'foundation and structural framework phase, skeleton visible',
                'exterior nearly complete, painting and finishing in progress',
                'fully completed renovation, beautiful finished result'
            ];
            // 6 frames
            return [
                'initial state before renovation, original untouched condition',
                'site clearing and demolition phase',
                'foundation and concrete structure phase',
                'structural framework complete, roofing in progress',
                'exterior finishing touches, painting and glazing',
                'fully completed renovation with landscaping, beautiful finished result'
            ];
        }

        function readFileAsBase64(file) {
            return new Promise((resolve, reject) => {
                const reader = new FileReader();
                reader.onload = (e) => {
                    const base64 = e.target.result.split(',')[1];
                    resolve({ base64, mimeType: file.type || 'image/jpeg' });
                };
                reader.onerror = reject;
                reader.readAsDataURL(file);
            });
        }

        function updateGenerateBtn() {
            generateBtn.disabled = false;
        }

        async function handleBeforeUpload(file) {
            try {
                let processedFile = file;
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    if (typeof heic2any !== 'undefined') {
                        const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }
                beforeImageData = await readFileAsBase64(processedFile);
                const objectUrl = URL.createObjectURL(processedFile);
                beforePreviewImg.src = objectUrl;
                beforePlaceholder.classList.add('hidden');
                beforePreviewContainer.classList.remove('hidden');
                updateGenerateBtn();
            } catch (e) {
                console.error('Error loading before image:', e);
                alert('Gagal memuat foto. Silakan coba lagi.');
            }
        }

        async function handleAfterUpload(file) {
            try {
                let processedFile = file;
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    if (typeof heic2any !== 'undefined') {
                        const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }
                afterImageData = await readFileAsBase64(processedFile);
                const objectUrl = URL.createObjectURL(processedFile);
                afterPreviewImg.src = objectUrl;
                afterPlaceholder.classList.add('hidden');
                afterPreviewContainer.classList.remove('hidden');
            } catch (e) {
                console.error('Error loading after reference image:', e);
                alert('Gagal memuat foto referensi. Silakan coba lagi.');
            }
        }

        // Upload: before
        beforeBox.addEventListener('click', () => beforeInput.click());
        beforeInput.addEventListener('change', (e) => {
            if (e.target.files && e.target.files[0]) handleBeforeUpload(e.target.files[0]);
        });
        beforeBox.addEventListener('dragover', (e) => { e.preventDefault(); beforeBox.style.borderColor = '#f59e0b'; });
        beforeBox.addEventListener('dragleave', () => { beforeBox.style.borderColor = ''; });
        beforeBox.addEventListener('drop', (e) => {
            e.preventDefault();
            beforeBox.style.borderColor = '';
            if (e.dataTransfer.files && e.dataTransfer.files[0]) handleBeforeUpload(e.dataTransfer.files[0]);
        });
        removeBeforeBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            beforeImageData = null;
            beforeInput.value = '';
            beforePreviewImg.src = '';
            beforePreviewContainer.classList.add('hidden');
            beforePlaceholder.classList.remove('hidden');
            updateGenerateBtn();
        });

        // Upload: after (optional)
        afterBox.addEventListener('click', () => afterInput.click());
        afterInput.addEventListener('change', (e) => {
            if (e.target.files && e.target.files[0]) handleAfterUpload(e.target.files[0]);
        });
        afterBox.addEventListener('dragover', (e) => { e.preventDefault(); afterBox.style.borderColor = '#0d9488'; });
        afterBox.addEventListener('dragleave', () => { afterBox.style.borderColor = ''; });
        afterBox.addEventListener('drop', (e) => {
            e.preventDefault();
            afterBox.style.borderColor = '';
            if (e.dataTransfer.files && e.dataTransfer.files[0]) handleAfterUpload(e.dataTransfer.files[0]);
        });
        removeAfterBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            afterImageData = null;
            afterInput.value = '';
            afterPreviewImg.src = '';
            afterPreviewContainer.classList.add('hidden');
            afterPlaceholder.classList.remove('hidden');
        });

        // Theme selection
        if (themeOptions) {
            themeOptions.querySelectorAll('.option-btn-timelapse').forEach(btn => {
                btn.addEventListener('click', () => {
                    themeOptions.querySelectorAll('.option-btn-timelapse').forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    selectedTheme = btn.dataset.value;
                });
            });
        }

        // Frame count selection
        if (frameCountContainer) {
            frameCountContainer.querySelectorAll('.timelapse-frame-btn').forEach(btn => {
                btn.addEventListener('click', () => {
                    frameCountContainer.querySelectorAll('.timelapse-frame-btn').forEach(b => b.classList.remove('timelapse-frame-selected'));
                    btn.classList.add('timelapse-frame-selected');
                    selectedFrameCount = parseInt(btn.dataset.count);
                });
            });
        }

        // Ratio selection
        if (ratioOptions) {
            ratioOptions.querySelectorAll('.option-btn-timelapse').forEach(btn => {
                btn.addEventListener('click', () => {
                    ratioOptions.querySelectorAll('.option-btn-timelapse').forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    selectedRatio = btn.dataset.value;
                });
            });
        }

        function getAspectClass() {
            if (selectedRatio === '9:16') return 'aspect-[9/16]';
            if (selectedRatio === '3:4') return 'aspect-[3/4]';
            if (selectedRatio === '1:1') return 'aspect-square';
            return 'aspect-video';
        }

        async function generateSingleFrame(frameIndex, stageDesc, cameraAnchor) {
            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
            const themeDesc = themeDescriptions[selectedTheme] || 'modern architecture';

            const promptText = `You are generating frame ${frameIndex} of ${selectedFrameCount} in a TIMELAPSE RENOVATION photo sequence.

CRITICAL CAMERA RULE: Every frame in this sequence MUST use EXACTLY the same fixed camera position and angle. The viewpoint is locked and must NEVER change between frames. Use this anchor: ${cameraAnchor}

CURRENT STAGE (frame ${frameIndex}/${selectedFrameCount}): ${stageDesc}

RENOVATION STYLE: ${themeDesc}

REQUIREMENTS:
- FIXED CAMERA: same viewpoint, same focal length, same height, same horizontal angle as ALL other frames — this is a timelapse so the camera DOES NOT MOVE
- Photorealistic architectural photography quality
- Show ONLY the current stage: ${stageDesc}
- Apply renovation style: ${themeDesc}
- Natural daytime lighting, professional composition
- High detail, sharp focus, vibrant realistic colors

Generate a single high-quality architectural photo for this timelapse frame.`;

            const parts = [{ text: promptText }];
            if (beforeImageData) {
                parts.push({ inlineData: { mimeType: beforeImageData.mimeType, data: beforeImageData.base64 } });
            }
            if (afterImageData) {
                parts.push({ inlineData: { mimeType: afterImageData.mimeType, data: afterImageData.base64 } });
            }

            const payload = {
                contents: [{ parts }],
                generationConfig: {
                    responseModalities: ['IMAGE'],
                    imageConfig: { aspectRatio: selectedRatio }
                }
            };

            const response = await fetch(apiUrl, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            });

            if (!response.ok) {
                const errText = await response.text();
                throw new Error(`API error ${response.status}: ${errText}`);
            }

            const data = await response.json();
            const part = data?.candidates?.[0]?.content?.parts?.find(p => p.inlineData);
            if (!part) throw new Error('No image in response');

            const imageUrl = 'data:' + part.inlineData.mimeType + ';base64,' + part.inlineData.data;
            return imageUrl;
        }

        async function runGeneration() {
            generatedImages = [];
            resultsGrid.innerHTML = '';
            emptyState.classList.add('hidden');
            resultsHeader.classList.add('hidden');
            downloadAllBtn.classList.add('hidden');
            loadingDiv.classList.remove('hidden');
            generateBtn.disabled = true;

            const stages = getStageDescriptions(selectedFrameCount);
            const themeLabel = themeOptions ? (themeOptions.querySelector('.option-btn-timelapse.selected')?.textContent?.trim() || selectedTheme) : selectedTheme;

            // Generate a shared camera anchor once — all frames reuse it to stay consistent
            const angles = ['straight-on frontal view', 'slight left diagonal view', 'slight right diagonal view', 'low-angle frontal view'];
            const distances = ['medium distance', 'medium-close distance', 'medium-wide distance'];
            const heights = ['eye-level height', 'slightly elevated height'];
            const randomAngle = angles[Math.floor(Math.random() * angles.length)];
            const randomDist = distances[Math.floor(Math.random() * distances.length)];
            const randomHeight = heights[Math.floor(Math.random() * heights.length)];
            const sessionId = Math.floor(Math.random() * 999999);
            const cameraAnchor = `${randomAngle}, ${randomDist}, ${randomHeight} [session:${sessionId}]`;

            loadingText.textContent = `Membuat ${selectedFrameCount} frame timelapse renovasi...`;
            progressBar.style.width = '0%';
            progressText.textContent = `0 / ${selectedFrameCount} frame selesai`;

            // Create placeholder cards
            for (let i = 0; i < selectedFrameCount; i++) {
                const card = document.createElement('div');
                card.id = `timelapse-card-${i}`;
                card.className = 'relative group rounded-xl overflow-hidden shadow-lg bg-gray-100 flex items-center justify-center ' + getAspectClass();
                card.innerHTML = '<div class="flex flex-col items-center gap-2 text-gray-400"><i class="fas fa-spinner fa-spin text-2xl"></i><span class="text-xs font-medium">Frame ' + (i + 1) + '</span></div>';
                resultsGrid.appendChild(card);
            }

            let completed = 0;

            const promises = stages.map((stageDesc, idx) => {
                return generateSingleFrame(idx + 1, stageDesc, cameraAnchor).then(imageUrl => {
                    const frameNum = idx + 1;
                    const filename = `timelapse-renovasi-frame${frameNum}-${selectedTheme}-${Date.now()}.jpg`;
                    generatedImages[idx] = { url: imageUrl, filename };

                    const card = document.getElementById(`timelapse-card-${idx}`);
                    if (card) {
                        card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                        card.innerHTML =
                            '<img src="' + imageUrl + '" class="w-full h-auto object-cover" alt="Timelapse Frame ' + frameNum + '">' +
                            '<div class="absolute top-2 left-2 bg-black/70 text-white text-xs font-bold px-2 py-1 rounded-full">Frame ' + frameNum + '</div>' +
                            '<div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">' +
                            '<button data-action="preview" data-index="' + idx + '" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-1 text-xs"><i class="fas fa-eye"></i><span class="hidden sm:inline">Preview</span></button>' +
                            '<button data-action="download" data-index="' + idx + '" class="action-btn text-white px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-1 text-xs" style="background:linear-gradient(135deg,#f59e0b 0%,#0d9488 100%)"><i class="fas fa-download"></i><span class="hidden sm:inline">Download</span></button>' +
                            '</div>';
                    }

                    completed++;
                    const pct = Math.round((completed / selectedFrameCount) * 100);
                    progressBar.style.width = pct + '%';
                    progressText.textContent = completed + ' / ' + selectedFrameCount + ' frame selesai';
                    loadingText.textContent = `Membuat frame ${completed + 1 <= selectedFrameCount ? (completed + 1) : completed} dari ${selectedFrameCount}...`;

                }).catch(err => {
                    console.error('Frame ' + (idx + 1) + ' error:', err);
                    generatedImages[idx] = null;
                    completed++;
                    const pct = Math.round((completed / selectedFrameCount) * 100);
                    progressBar.style.width = pct + '%';
                    progressText.textContent = completed + ' / ' + selectedFrameCount + ' frame selesai';

                    const card = document.getElementById(`timelapse-card-${idx}`);
                    if (card) {
                        card.className = 'relative group rounded-xl overflow-hidden shadow-lg bg-red-50 border border-red-200 flex items-center justify-center ' + getAspectClass();
                        card.innerHTML = '<div class="flex flex-col items-center gap-2 text-red-400 p-4 text-center"><i class="fas fa-exclamation-triangle text-2xl"></i><span class="text-xs font-medium">Frame ' + (idx + 1) + ' gagal</span></div>';
                    }
                });
            });

            await Promise.allSettled(promises);

            loadingDiv.classList.add('hidden');
            generateBtn.disabled = false;
            updateGenerateBtn();

            const successCount = generatedImages.filter(g => g !== null && g !== undefined).length;
            if (successCount > 0) {
                resultsHeader.classList.remove('hidden');
                resultsCount.textContent = successCount + ' dari ' + selectedFrameCount + ' frame timelapse berhasil dibuat';
                downloadAllBtn.classList.remove('hidden');
            } else {
                emptyState.classList.remove('hidden');
            }
        }

        generateBtn.addEventListener('click', runGeneration);

        // Results click handler
        resultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();
            const btn = e.target.closest('[data-action]');
            if (!btn) return;

            const idx = parseInt(btn.dataset.index);
            const imgData = generatedImages[idx];
            if (!imgData) return;

            const action = btn.dataset.action;
            if (action === 'preview') {
                if (window.showPreviewModal) window.showPreviewModal(imgData.url);
            } else if (action === 'download') {
                if (window.downloadDataURINew) await window.downloadDataURINew(imgData.url, imgData.filename);
                else if (window.downloadImage) await window.downloadImage(imgData.url, imgData.filename);
            }
        });

        // Download all
        downloadAllBtn.addEventListener('click', async () => {
            const validImages = generatedImages.filter(g => g);
            if (!validImages.length) return;
            downloadAllBtn.disabled = true;
            downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Mengunduh semua...';
            for (let i = 0; i < validImages.length; i++) {
                const img = validImages[i];
                try {
                    if (window.downloadDataURINew) await window.downloadDataURINew(img.url, img.filename);
                    else if (window.downloadImage) await window.downloadImage(img.url, img.filename);
                    await new Promise(r => setTimeout(r, 400));
                } catch (err) { console.error('Download error:', err); }
            }
            downloadAllBtn.disabled = false;
            downloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua Frame';
        });

        // Tab initialization
        function initTab() {}
        const tabDesktop = document.querySelector('.main-tab-btn[data-tab="timelapse-renovasi"]');
        const tabMobile = document.querySelector('.mobile-tab-btn[data-tab="timelapse-renovasi"]');
        if (tabDesktop) tabDesktop.addEventListener('click', initTab);
        if (tabMobile) tabMobile.addEventListener('click', initTab);

    })();

    // ==================== TWIBON ====================
    (function() {
        const apiKey = "";

        // Element references
        const photoInput = document.getElementById('twibon-input');
        const photoUploadBox = document.getElementById('twibon-upload-box');
        const photoPreview = document.getElementById('twibon-preview');
        const photoPlaceholder = document.getElementById('twibon-placeholder');
        const photoRemoveBtn = document.getElementById('twibon-remove-btn');

        const frameInput = document.getElementById('twibon-frame-input');
        const frameUploadBox = document.getElementById('twibon-frame-upload-box');
        const framePreview = document.getElementById('twibon-frame-preview');
        const framePlaceholder = document.getElementById('twibon-frame-placeholder');

        const descriptionInput = document.getElementById('twibon-description');
        const aiSection = document.getElementById('twibon-ai-section');
        const customSection = document.getElementById('twibon-custom-section');

        const generateBtn = document.getElementById('twibon-generate-btn');
        const downloadBtn = document.getElementById('twibon-download-btn');
        const resetBtn = document.getElementById('twibon-reset-btn');

        const canvas = document.getElementById('twibon-canvas');
        const ctx = canvas.getContext('2d');

        const previewContainer = document.getElementById('twibon-preview-container');
        const emptyState = document.getElementById('twibon-empty-state');

        const resultContainer = document.getElementById('twibon-result-container');
        const photoDisplay = document.getElementById('twibon-photo-display');
        const frameWrapper = document.getElementById('twibon-frame-wrapper');
        const frameDisplay = document.getElementById('twibon-frame-display');

        // Position controls
        const positionControls = document.getElementById('twibon-position-controls');
        const zoomSlider = document.getElementById('twibon-zoom-slider');
        const zoomValue = document.getElementById('twibon-zoom-value');
        const moveUpBtn = document.getElementById('twibon-move-up');
        const moveDownBtn = document.getElementById('twibon-move-down');
        const moveLeftBtn = document.getElementById('twibon-move-left');
        const moveRightBtn = document.getElementById('twibon-move-right');
        const resetPositionBtn = document.getElementById('twibon-reset-position');

        let userPhotoData = null;
        let customFrameData = null;
        let generatedFrameData = null;
        let selectedCategory = 'independence';
        let selectedStyle = 'elegant';
        let selectedAspectRatio = '1:1';

        // Photo positioning variables
        let photoScale = 1.0;
        let photoOffsetX = 0;
        let photoOffsetY = 0;
        let currentUserPhoto = null;  // Store loaded image
        let currentFrameImage = null; // Store loaded frame

        // Category buttons
        document.querySelectorAll('#content-twibon .option-btn-twibon[data-value]').forEach(btn => {
            if (btn.closest('div').previousElementSibling?.textContent?.includes('Kategori Frame')) {
                btn.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();

                    const parent = this.closest('div');
                    parent.querySelectorAll('.option-btn-twibon').forEach(b => b.classList.remove('selected'));
                    this.classList.add('selected');
                    selectedCategory = this.dataset.value;

                    // Toggle AI/Custom sections
                    if (selectedCategory === 'custom') {
                        aiSection.classList.add('hidden');
                        customSection.classList.remove('hidden');
                    } else {
                        aiSection.classList.remove('hidden');
                        customSection.classList.add('hidden');
                    }
                });
            }
            // Style buttons
            else if (btn.closest('div').previousElementSibling?.textContent?.includes('Style Frame')) {
                btn.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();

                    const parent = this.closest('div');
                    parent.querySelectorAll('.option-btn-twibon').forEach(b => b.classList.remove('selected'));
                    this.classList.add('selected');
                    selectedStyle = this.dataset.value;
                });
            }
            // Aspect ratio buttons
            else if (btn.closest('div').previousElementSibling?.textContent?.includes('Ukuran Frame')) {
                btn.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();

                    const parent = this.closest('div');
                    parent.querySelectorAll('.option-btn-twibon').forEach(b => b.classList.remove('selected'));
                    this.classList.add('selected');
                    selectedAspectRatio = this.dataset.value;

                    // Update result container aspect ratio
                    updateResultContainerAspectRatio();
                });
            }
        });

        function updateResultContainerAspectRatio() {
            if (!resultContainer) return;

            const aspectRatioMap = {
                '1:1': '1/1',
                '4:5': '4/5',
                '9:16': '9/16',
                '16:9': '16/9'
            };

            resultContainer.style.aspectRatio = aspectRatioMap[selectedAspectRatio] || '1/1';
        }

        // Upload photo
        photoUploadBox.addEventListener('click', () => {
            photoInput.click();
        });

        photoInput.addEventListener('change', async (e) => {
            const file = e.target.files[0];
            if (!file) return;

            let processedFile = file;

            // Convert HEIC to JPEG if needed
            if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                try {
                    const convertedBlob = await heic2any({
                        blob: file,
                        toType: 'image/jpeg',
                        quality: 0.9
                    });
                    processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                        type: 'image/jpeg'
                    });
                } catch (err) {
                    console.error('HEIC conversion failed:', err);
                    alert('Gagal mengkonversi gambar HEIC. Silakan coba format lain.');
                    return;
                }
            }

            const reader = new FileReader();
            reader.onloadend = () => {
                userPhotoData = reader.result;
                photoPreview.src = reader.result;
                photoPreview.classList.remove('hidden');
                photoPlaceholder.classList.add('hidden');
                photoRemoveBtn.classList.remove('hidden');
                updateGenerateButtonState();
            };
            reader.readAsDataURL(processedFile);
        });

        // Remove photo
        photoRemoveBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            userPhotoData = null;
            photoInput.value = '';
            photoPreview.src = '';
            photoPreview.classList.add('hidden');
            photoPlaceholder.classList.remove('hidden');
            photoRemoveBtn.classList.add('hidden');

            // Hide position controls
            if (positionControls) {
                positionControls.classList.add('hidden');
            }

            // Reset position
            photoScale = 1.0;
            photoOffsetX = 0;
            photoOffsetY = 0;
            currentUserPhoto = null;
            currentFrameImage = null;

            updateGenerateButtonState();
        });

        // Upload custom frame
        frameUploadBox.addEventListener('click', () => {
            frameInput.click();
        });

        frameInput.addEventListener('change', async (e) => {
            const file = e.target.files[0];
            if (!file) return;

            const reader = new FileReader();
            reader.onloadend = () => {
                customFrameData = reader.result;
                framePreview.src = reader.result;
                framePreview.classList.remove('hidden');
                framePlaceholder.classList.add('hidden');
            };
            reader.readAsDataURL(file);
        });

        // Position controls event handlers
        if (zoomSlider && zoomValue) {
            zoomSlider.addEventListener('input', (e) => {
                photoScale = parseInt(e.target.value) / 100;
                zoomValue.textContent = `${e.target.value}%`;
                updatePhotoTransform();
            });
        }

        if (moveUpBtn) {
            moveUpBtn.addEventListener('click', () => {
                photoOffsetY -= 20;
                updatePhotoTransform();
            });
        }

        if (moveDownBtn) {
            moveDownBtn.addEventListener('click', () => {
                photoOffsetY += 20;
                updatePhotoTransform();
            });
        }

        if (moveLeftBtn) {
            moveLeftBtn.addEventListener('click', () => {
                photoOffsetX -= 20;
                updatePhotoTransform();
            });
        }

        if (moveRightBtn) {
            moveRightBtn.addEventListener('click', () => {
                photoOffsetX += 20;
                updatePhotoTransform();
            });
        }

        if (resetPositionBtn) {
            resetPositionBtn.addEventListener('click', () => {
                photoScale = 1.0;
                photoOffsetX = 0;
                photoOffsetY = 0;
                if (zoomSlider) zoomSlider.value = 100;
                if (zoomValue) zoomValue.textContent = '100%';
                updatePhotoTransform();
            });
        }

        function updatePhotoTransform() {
            if (photoDisplay) {
                photoDisplay.style.transform = `scale(${photoScale}) translate(${photoOffsetX}px, ${photoOffsetY}px)`;
                photoDisplay.style.transformOrigin = 'center';
            }
        }

        function updateGenerateButtonState() {
            if (userPhotoData) {
                generateBtn.disabled = false;
            } else {
                generateBtn.disabled = true;
            }
        }

        // Generate button
        generateBtn.addEventListener('click', async () => {
            if (!userPhotoData) {
                alert('Silakan upload foto terlebih dahulu');
                return;
            }

            generateBtn.disabled = true;
            generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let frameImage = null;

            while (attempts < MAX_ATTEMPTS && !frameImage) {
                attempts++;
                try {
                    if (selectedCategory === 'custom' && customFrameData) {
                        frameImage = await loadImage(customFrameData);
                    } else {
                        const frameData = await generateAIFrame();
                        if (frameData) {
                            generatedFrameData = `data:image/png;base64,${frameData}`;
                            frameImage = await loadImage(generatedFrameData);
                        }
                    }
                } catch (err) {
                    console.error(`Twibon attempt ${attempts} failed:`, err);
                }
            }

            if (!frameImage) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                generateBtn.disabled = false;
                generateBtn.innerHTML = '<i class="fas fa-magic mr-2"></i>Generate Twibon';
                return;
            }

            try {
                // Step 2: Display photo and frame
                console.log('Frame image loaded successfully');

                // Update container aspect ratio
                updateResultContainerAspectRatio();

                // Set photo display
                photoDisplay.src = userPhotoData;
                photoDisplay.onload = () => {
                    console.log('Photo displayed:', photoDisplay.naturalWidth, 'x', photoDisplay.naturalHeight);
                };

                // Set frame display with green screen removal
                if (selectedCategory === 'custom' && customFrameData) {
                    frameDisplay.src = customFrameData;
                    frameDisplay.onload = () => {
                        console.log('Custom frame displayed:', frameDisplay.naturalWidth, 'x', frameDisplay.naturalHeight);
                    };
                } else {
                    // Generate frame has green screen - need to remove it
                    console.log('Removing green screen from generated frame...');
                    removeGreenScreen(generatedFrameData).then(processedFrameData => {
                        console.log('Green screen removal complete, displaying frame...');
                        frameDisplay.src = processedFrameData;
                        currentFrameImage = processedFrameData; // Update stored frame

                        frameDisplay.onload = () => {
                            console.log('Frame displayed (green screen removed):', frameDisplay.naturalWidth, 'x', frameDisplay.naturalHeight);
                            // Show frame wrapper after frame loads
                            frameWrapper.classList.remove('hidden');
                        };
                    }).catch(err => {
                        console.error('Error removing green screen:', err);
                        // Fallback: use original frame
                        frameDisplay.src = generatedFrameData;
                        currentFrameImage = generatedFrameData;
                        frameWrapper.classList.remove('hidden');
                    });
                }

                // Store for download
                currentUserPhoto = userPhotoData;
                // currentFrameImage will be updated after green screen removal
                if (selectedCategory === 'custom' && customFrameData) {
                    currentFrameImage = customFrameData;
                }
                // For generated frame, currentFrameImage is set in removeGreenScreen callback above

                // Reset position/scale on new generation
                photoScale = 1.0;
                photoOffsetX = 0;
                photoOffsetY = 0;
                if (zoomSlider) zoomSlider.value = 100;
                if (zoomValue) zoomValue.textContent = '100%';
                updatePhotoTransform();

                // Show preview and position controls
                emptyState.classList.add('hidden');
                previewContainer.classList.remove('hidden');
                // frameWrapper will be shown after green screen removal or immediately for custom frame
                if (selectedCategory === 'custom' && customFrameData) {
                    frameWrapper.classList.remove('hidden');
                }
                if (positionControls) {
                    positionControls.classList.remove('hidden');
                }

                generateBtn.disabled = false;
                generateBtn.innerHTML = '<i class="fas fa-magic mr-2"></i>Generate Twibon';
            } catch (error) {
                console.error('Error generating twibon:', error);
                alert('Terjadi kesalahan saat membuat twibon. Silakan coba lagi.');
                generateBtn.disabled = false;
                generateBtn.innerHTML = '<i class="fas fa-magic mr-2"></i>Generate Twibon';
            }
        });

        async function generateAIFrame() {
            try {
                // Map categories to descriptions
                const categoryDescriptions = {
                    'independence': 'Indonesian Independence Day (HUT RI) frame with red and white colors, patriotic theme, Indonesian flag elements',
                    'holiday': 'Holiday celebration frame with festive decorations, joyful colors, celebration elements',
                    'birthday': 'Birthday celebration frame with balloons, cake, confetti, festive and joyful theme',
                    'wedding': 'Wedding frame with elegant floral decorations, romantic theme, soft colors, love elements',
                    'graduation': 'Graduation celebration frame with academic cap, diploma, achievement theme, proud moment',
                    'campaign': 'Campaign or promotional frame with bold text area, attention-grabbing design, professional look',
                    'corporate': 'Corporate or business frame with professional design, company branding elements, clean and modern'
                };

                const styleDescriptions = {
                    'elegant': 'elegant and sophisticated style with refined decorative elements',
                    'modern': 'modern and contemporary style with clean geometric shapes',
                    'minimalist': 'minimalist style with simple clean lines and subtle details',
                    'colorful': 'colorful and vibrant style with bold colors and dynamic elements'
                };

                const description = descriptionInput.value.trim() || categoryDescriptions[selectedCategory];

                const prompt = `Create a professional Twibon (photo frame/border overlay) for: ${description}

Style: ${styleDescriptions[selectedStyle]}

CRITICAL INSTRUCTION - GREEN SCREEN BACKGROUND:
The CENTER 60% of the image MUST be filled with a SOLID, VIBRANT GREEN SCREEN (#00FF00 or bright lime green).
This green area is where the user's photo will show through - DO NOT put any decorations in this area.

FRAME ELEMENTS (edges only):
1. Top edge (15% of height): Decorative banner with theme elements, text, ornaments
2. Bottom edge (15% of height): Decorative border with theme elements
3. Four corners: Ornamental corner designs
4. Left/right edges: Thin decorative borders (10% width each)

REQUIREMENTS:
- Theme: ${description}
- Style: ${selectedStyle}
- Aspect ratio: ${selectedAspectRatio}
- Center green screen area: EXACTLY #00FF00 bright green (no gradients, no shadows on green)
- Decorations: ONLY at edges (top 15%, bottom 15%, corners, sides)
- The green screen must be clean and uniform for easy removal

LAYOUT EXAMPLE:
┌──────────────────────┐
│  TOP BANNER (15%)    │ ← Decorations
├──────────────────────┤
│░                    ░│ ← Green screen center (60%)
│░   #00FF00 GREEN   ░│ ← This will be transparent
│░                    ░│
├──────────────────────┤
│ BOTTOM BORDER (15%)  │ ← Decorations
└──────────────────────┘

Create a professional Twibon frame with clean green screen center now.`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [{ text: prompt }]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: {
                            aspectRatio: selectedAspectRatio
                        }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(part => part.inlineData)?.inlineData?.data;

                if (!imageData) {
                    throw new Error('No image data in response');
                }

                return imageData;
            } catch (error) {
                console.error('Error generating AI frame:', error);
                return null;
            }
        }

        function loadImage(src) {
            return new Promise((resolve, reject) => {
                const img = new Image();
                img.crossOrigin = 'anonymous';
                img.onload = () => resolve(img);
                img.onerror = reject;
                img.src = src;
            });
        }

        async function removeGreenScreen(imageDataUrl, tolerance = 80) {
            return new Promise((resolve, reject) => {
                const img = new Image();
                img.crossOrigin = 'anonymous';

                img.onload = () => {
                    // Create temporary canvas for processing
                    const tempCanvas = document.createElement('canvas');
                    const tempCtx = tempCanvas.getContext('2d');

                    tempCanvas.width = img.width;
                    tempCanvas.height = img.height;

                    // Draw image
                    tempCtx.drawImage(img, 0, 0);

                    // Get image data
                    const imageData = tempCtx.getImageData(0, 0, tempCanvas.width, tempCanvas.height);
                    const data = imageData.data;

                    let removedPixels = 0;
                    const totalPixels = data.length / 4;

                    // Process pixels - remove green
                    for (let i = 0; i < data.length; i += 4) {
                        const r = data[i];
                        const g = data[i + 1];
                        const b = data[i + 2];

                        // Method 1: Simple green detection
                        // Green channel high, red and blue low
                        const isSimpleGreen = (g > 180) && (r < 120) && (b < 120);

                        // Method 2: Color distance from pure green (#00FF00)
                        const greenDiff = Math.abs(g - 255);
                        const redDiff = Math.abs(r - 0);
                        const blueDiff = Math.abs(b - 0);
                        const colorDistance = Math.sqrt(redDiff * redDiff + greenDiff * greenDiff + blueDiff * blueDiff);

                        // Method 3: Green dominance (green channel significantly higher than others)
                        const greenDominance = g > (r * 1.5) && g > (b * 1.5) && g > 150;

                        // Remove pixel if any method detects green
                        if (isSimpleGreen || colorDistance < tolerance || greenDominance) {
                            // Make pixel transparent
                            data[i + 3] = 0;
                            removedPixels++;
                        }
                    }

                    // Put processed image data back
                    tempCtx.putImageData(imageData, 0, 0);

                    // Log statistics
                    const percentageRemoved = ((removedPixels / totalPixels) * 100).toFixed(2);
                    console.log(`Green screen removed: ${removedPixels} pixels (${percentageRemoved}% of image)`);

                    // Return as data URL
                    const processedDataUrl = tempCanvas.toDataURL('image/png');
                    resolve(processedDataUrl);
                };

                img.onerror = () => {
                    reject(new Error('Failed to load image for green screen removal'));
                };

                img.src = imageDataUrl;
            });
        }

        // Download button
        downloadBtn.addEventListener('click', async () => {
            try {
                if (!currentUserPhoto || !currentFrameImage) {
                    alert('Tidak ada twibon untuk didownload');
                    return;
                }

                // Combine on canvas for download
                await combineForDownload(currentUserPhoto, currentFrameImage);

                const dataUrl = canvas.toDataURL('image/png');
                const filename = `twibon-${selectedCategory}-${Date.now()}.png`;

                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(dataUrl, filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(dataUrl, filename);
                } else {
                    // Fallback download
                    const link = document.createElement('a');
                    link.download = filename;
                    link.href = dataUrl;
                    link.click();
                }
            } catch (error) {
                console.error('Error downloading twibon:', error);
                alert('Terjadi kesalahan saat download. Silakan coba lagi.');
            }
        });

        async function combineForDownload(photoDataUrl, frameDataUrl) {
            // Load images
            const photo = await loadImage(photoDataUrl);
            const frame = await loadImage(frameDataUrl);

            // Calculate canvas size
            let canvasWidth, canvasHeight;
            const maxSize = 1080;

            switch(selectedAspectRatio) {
                case '1:1':
                    canvasWidth = canvasHeight = maxSize;
                    break;
                case '4:5':
                    canvasWidth = maxSize * 0.8;
                    canvasHeight = maxSize;
                    break;
                case '9:16':
                    canvasWidth = maxSize * 0.5625;
                    canvasHeight = maxSize;
                    break;
                case '16:9':
                    canvasWidth = maxSize;
                    canvasHeight = maxSize * 0.5625;
                    break;
                default:
                    canvasWidth = canvasHeight = maxSize;
            }

            canvas.width = canvasWidth;
            canvas.height = canvasHeight;

            // Clear canvas
            ctx.clearRect(0, 0, canvasWidth, canvasHeight);

            // Calculate photo dimensions
            const photoAspect = photo.width / photo.height;
            const canvasAspect = canvasWidth / canvasHeight;

            let baseWidth, baseHeight, baseOffsetX, baseOffsetY;

            if (photoAspect > canvasAspect) {
                baseHeight = canvasHeight;
                baseWidth = baseHeight * photoAspect;
                baseOffsetX = (canvasWidth - baseWidth) / 2;
                baseOffsetY = 0;
            } else {
                baseWidth = canvasWidth;
                baseHeight = baseWidth / photoAspect;
                baseOffsetX = 0;
                baseOffsetY = (canvasHeight - baseHeight) / 2;
            }

            // Apply scale and position
            const scaledWidth = baseWidth * photoScale;
            const scaledHeight = baseHeight * photoScale;

            const scaleDiff = photoScale - 1.0;
            const centerAdjustX = (baseWidth * scaleDiff) / 2;
            const centerAdjustY = (baseHeight * scaleDiff) / 2;

            const finalX = baseOffsetX - centerAdjustX + photoOffsetX;
            const finalY = baseOffsetY - centerAdjustY + photoOffsetY;

            // Draw photo
            ctx.drawImage(photo, finalX, finalY, scaledWidth, scaledHeight);

            // Draw frame
            ctx.drawImage(frame, 0, 0, canvasWidth, canvasHeight);
        }

        // Reset button
        resetBtn.addEventListener('click', () => {
            // Clear everything
            userPhotoData = null;
            customFrameData = null;
            generatedFrameData = null;

            photoInput.value = '';
            frameInput.value = '';
            descriptionInput.value = '';

            photoPreview.src = '';
            photoPreview.classList.add('hidden');
            photoPlaceholder.classList.remove('hidden');
            photoRemoveBtn.classList.add('hidden');

            framePreview.src = '';
            framePreview.classList.add('hidden');
            framePlaceholder.classList.remove('hidden');

            // Clear displays
            photoDisplay.src = '';
            frameDisplay.src = '';
            frameWrapper.classList.add('hidden');

            ctx.clearRect(0, 0, canvas.width, canvas.height);

            previewContainer.classList.add('hidden');
            emptyState.classList.remove('hidden');

            // Hide position controls and reset position
            if (positionControls) {
                positionControls.classList.add('hidden');
            }
            photoScale = 1.0;
            photoOffsetX = 0;
            photoOffsetY = 0;
            currentUserPhoto = null;
            currentFrameImage = null;
            if (zoomSlider) zoomSlider.value = 100;
            if (zoomValue) zoomValue.textContent = '100%';

            updateGenerateButtonState();
        });
    })();

    // ==================== PROFESSIONAL HEADSHOT ====================
    (function() {
        const apiKey = "";

        // Element references
        const headshotInput = document.getElementById('headshot-input');
        const headshotUploadBox = document.getElementById('headshot-upload-box');
        const headshotPreview = document.getElementById('headshot-preview');
        const headshotPlaceholder = document.getElementById('headshot-placeholder');
        const headshotRemoveBtn = document.getElementById('headshot-remove-btn');
        const headshotGenerateBtn = document.getElementById('headshot-generate-btn');
        const headshotThemeOptions = document.getElementById('headshot-theme-options');
        const headshotCustomThemeContainer = document.getElementById('headshot-custom-theme-container');
        const headshotCustomThemeInput = document.getElementById('headshot-custom-theme-input');
        const headshotBgOptions = document.getElementById('headshot-bg-options');
        const headshotAttireOptions = document.getElementById('headshot-attire-options');
        const headshotStyleOptions = document.getElementById('headshot-style-options');
        const headshotRatioOptions = document.getElementById('headshot-ratio-options');
        const headshotResultsPlaceholder = document.getElementById('headshot-results-placeholder');
        const headshotResultsContainer = document.getElementById('headshot-results-container');
        const headshotResultsGrid = document.getElementById('headshot-results-grid');
        const headshotDownloadAllBtn = document.getElementById('headshot-download-all-btn');

        if (!headshotInput) return;

        let uploadedImageData = null;
        let generatedImages = [];
        let selectedTheme = 'LinkedIn Professional';
        let selectedBackground = 'Studio White';
        let selectedAttire = 'Business Suit';
        let selectedStyle = 'Classic';
        let selectedRatio = '3:4';
        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const headshotCountSel = document.getElementById('headshot-count-selection-grid');
        if (headshotCountSel) {
            headshotCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                headshotCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Setup image upload with HEIC support
        headshotInput.addEventListener('change', async (e) => {
            const file = e.target.files[0];
            if (!file) return;

            try {
                let processedFile = file;

                // Convert HEIC to JPEG if necessary
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    const heic2any = window.heic2any;
                    if (heic2any) {
                        const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                    }
                }

                const reader = new FileReader();
                reader.onload = (event) => {
                    const dataUrl = event.target.result;
                    const base64 = dataUrl.split(',')[1];

                    uploadedImageData = {
                        base64: base64,
                        mimeType: processedFile.type,
                        dataUrl: dataUrl
                    };

                    headshotPreview.src = dataUrl;
                    headshotPlaceholder.classList.add('hidden');
                    headshotPreview.classList.remove('hidden');
                    headshotRemoveBtn.classList.remove('hidden');
                    headshotGenerateBtn.disabled = false;
                };
                reader.readAsDataURL(processedFile);
            } catch (error) {
                console.error('Error processing image:', error);
                alert('Gagal memproses gambar. Silakan coba lagi.');
            }
        });

        // Remove image handler
        headshotRemoveBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            uploadedImageData = null;
            headshotInput.value = '';
            headshotPreview.src = '#';
            headshotPreview.classList.add('hidden');
            headshotPlaceholder.classList.remove('hidden');
            headshotRemoveBtn.classList.add('hidden');
            headshotGenerateBtn.disabled = true;

            // Reset results
            headshotResultsPlaceholder.classList.remove('hidden');
            headshotResultsContainer.classList.add('hidden');
            headshotResultsGrid.innerHTML = '';
            generatedImages = [];
        });

        // Theme selection handler
        headshotThemeOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            headshotThemeOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedTheme = button.dataset.value;

            // Show/hide custom theme input
            if (selectedTheme === 'Custom') {
                headshotCustomThemeContainer.classList.remove('hidden');
            } else {
                headshotCustomThemeContainer.classList.add('hidden');
            }
        });

        // Background selection handler
        headshotBgOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            headshotBgOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedBackground = button.dataset.value;
        });

        // Attire selection handler
        headshotAttireOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            headshotAttireOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedAttire = button.dataset.value;
        });

        // Style selection handler
        headshotStyleOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            headshotStyleOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedStyle = button.dataset.value;
        });

        // Ratio selection handler
        headshotRatioOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (!button) return;

            headshotRatioOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
            button.classList.add('selected');
            selectedRatio = button.dataset.value;
        });

        // Generate button handler
        headshotGenerateBtn.addEventListener('click', async () => {
            if (!uploadedImageData) {
                alert('Silakan unggah foto terlebih dahulu');
                return;
            }

            // Determine theme
            let professionalTheme = selectedTheme;
            if (selectedTheme === 'Custom' && headshotCustomThemeInput) {
                const customTheme = headshotCustomThemeInput.value.trim();
                professionalTheme = customTheme || 'LinkedIn Professional';
            }

            // Theme descriptions for better AI prompts
            const themeDescriptions = {
                'LinkedIn Professional': 'professional LinkedIn headshot with confident smile, approachable and trustworthy appearance, corporate professional look',
                'Business Executive': 'executive business headshot, authoritative yet approachable, leadership presence, sophisticated corporate style',
                'Corporate Formal': 'formal corporate headshot, serious professional demeanor, conservative business appearance',
                'Creative Professional': 'creative professional headshot, artistic and modern look, innovative and forward-thinking appearance',
                'Tech Professional': 'tech industry professional headshot, smart casual tech style, innovative and contemporary look',
                'Medical Professional': 'medical healthcare professional headshot, caring and trustworthy appearance, clean clinical professional look',
                'Legal Professional': 'legal professional headshot, trustworthy authoritative look, sophisticated law firm appearance',
                'Entrepreneur': 'entrepreneur headshot, confident dynamic look, innovative business leader appearance',
                'Academic': 'academic professional headshot, intellectual scholarly appearance, knowledgeable and approachable look',
                'Smart Casual': 'smart casual professional headshot, modern relaxed professional look, approachable business casual style',
                'Consultant': 'consultant professional headshot, expert advisor appearance, trusted professional consultant look'
            };

            const themeDescription = themeDescriptions[selectedTheme] || professionalTheme;

            // Attire descriptions
            const attireDescriptions = {
                'Business Suit': 'wearing a professional business suit with tie or formal business attire',
                'Smart Casual Blazer': 'wearing a smart casual blazer over professional shirt',
                'Professional Shirt': 'wearing a crisp professional dress shirt or blouse',
                'Industry Specific': `wearing appropriate professional attire for ${professionalTheme} industry`
            };

            const attireDescription = attireDescriptions[selectedAttire];

            // Background descriptions
            const backgroundDescriptions = {
                'Studio White': 'clean studio white background, professional photography studio lighting',
                'Professional Gray': 'professional neutral gray background with gradient lighting',
                'Navy Blue': 'professional navy blue background with elegant lighting',
                'Gradient Blue': 'professional blue gradient background with sophisticated lighting',
                'Office Interior': 'professional modern office interior background with soft bokeh effect',
                'Outdoor Professional': 'professional outdoor background with natural professional setting, blurred bokeh'
            };

            const backgroundDescription = backgroundDescriptions[selectedBackground];

            // Style descriptions
            const styleDescriptions = {
                'Classic': 'classic professional photography style with traditional lighting and composition',
                'Modern': 'modern contemporary photography style with clean lines and bright lighting',
                'Cinematic': 'cinematic professional style with dramatic depth and artistic lighting'
            };

            const styleDescription = styleDescriptions[selectedStyle];

            // Create comprehensive prompt
            const prompt = `Create a professional headshot photograph for ${themeDescription}.

CRITICAL REQUIREMENTS:
- Keep the person's identity, face, and all facial features EXACTLY the same as in the original photo
- Transform the photo into a professional headshot with the following specifications:

THEME & PURPOSE: ${professionalTheme}
- ${themeDescription}

ATTIRE: ${attireDescription}

BACKGROUND: ${backgroundDescription}

PHOTOGRAPHY STYLE: ${styleDescription}

EXPRESSION & POSE:
- Confident, professional facial expression
- Direct eye contact with camera
- Shoulders squared towards camera
- Professional headshot composition (head and shoulders)
- Natural, relaxed posture

LIGHTING & QUALITY:
- Professional studio-quality lighting
- Soft, flattering light on face
- No harsh shadows
- High-resolution professional photography quality
- Color-corrected and professionally edited look

The final result must be a high-quality professional headshot suitable for LinkedIn, CV, business profiles, and professional use. The person must be clearly recognizable as the same individual from the original photo.`;

            await generateHeadshotSet(prompt);
        });

        async function generateHeadshotSet(prompt) {
            const originalBtnHTML = headshotGenerateBtn.innerHTML;
            headshotGenerateBtn.disabled = true;
            headshotGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Membuat Headshot...';

            headshotResultsPlaceholder.classList.add('hidden');
            headshotResultsContainer.classList.remove('hidden');

            const MAX_ATTEMPTS = 3;
            let attempts = 0;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                headshotResultsGrid.innerHTML = '';
                generatedImages = [];

                // Create placeholder cards
                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `headshot-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-[3/4]';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-indigo-600"></div>';
                    headshotResultsGrid.appendChild(card);
                }

                // Generate all headshots in parallel
                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSingleHeadshot(i + 1, prompt)
                );
                await Promise.allSettled(generationPromises);
                successCount = headshotResultsGrid.querySelectorAll('img').length;
            }

            // Remove failed cards
            headshotResultsGrid.querySelectorAll('[id^="headshot-card-"]').forEach(card => {
                if (!card.querySelector('img')) card.remove();
            });

            headshotGenerateBtn.disabled = false;
            headshotGenerateBtn.innerHTML = originalBtnHTML;

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
            }
        }

        async function generateSingleHeadshot(index, basePrompt) {
            const card = document.getElementById(`headshot-card-${index}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const finalPrompt = `${basePrompt}\n\nThis is professional headshot variation ${index}. Create a slightly different professional pose or angle while maintaining the same professional quality and style. Ensure natural variation in pose (slight head tilt, different shoulder angle, etc.) while keeping the professional headshot standard.`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: finalPrompt },
                            { inlineData: { mimeType: uploadedImageData.mimeType, data: uploadedImageData.base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                    safetySettings: [
                        { "category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE" },
                        { "category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE" }
                    ]
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data received');

                {
                    const imageUrl = `data:image/png;base64,${base64Data}`;
                    const filename = `professional_headshot_${selectedTheme.replace(/\s+/g, '_').toLowerCase()}_${index}.png`;

                    // Store at correct index (not push) to maintain order in parallel execution
                    generatedImages[index - 1] = { url: imageUrl, filename: filename };

                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Professional Headshot ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-indigo-600 to-blue-600 hover:from-indigo-700 hover:to-blue-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`Error generating headshot ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons (works on desktop & mobile)
        headshotResultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();

            const actionBtn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
            if (!actionBtn) return;

            const action = actionBtn.dataset.action;
            const index = parseInt(actionBtn.dataset.index, 10);
            const imageData = generatedImages[index];

            if (!imageData) return;

            if (action === 'preview') {
                if (window.showPreviewModal) {
                    window.showPreviewModal(imageData.url);
                }
            } else if (action === 'download') {
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(imageData.url, imageData.filename);
                }
            }
        });

        // Download all functionality (works on desktop & mobile like Umrah)
        if (headshotDownloadAllBtn) {
            headshotDownloadAllBtn.addEventListener('click', async () => {
                if (generatedImages.length === 0) return;

                headshotDownloadAllBtn.disabled = true;
                headshotDownloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                const validImages = generatedImages.filter(Boolean);
                for (let i = 0; i < validImages.length; i++) {
                    const img = validImages[i];
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }
                }

                headshotDownloadAllBtn.disabled = false;
                headshotDownloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
            });
        }
    })();

    // ==================== FOTO MATERNITY ====================
    (function() {
        const apiKey = "";

        // Element references
        const fileInputArea = document.getElementById('maternity-file-input-area');
        const addPhotoBtn = document.getElementById('maternity-add-photo-btn');
        const themeOptions = document.getElementById('maternity-theme-options');
        const customThemeContainer = document.getElementById('maternity-custom-theme-container');
        const customThemeInput = document.getElementById('maternity-custom-theme');
        const styleOptions = document.getElementById('maternity-style-options');
        const ratioOptions = document.getElementById('maternity-ratio-options');
        const generateBtn = document.getElementById('maternity-generate-btn');
        const loadingIndicator = document.getElementById('maternity-loading-indicator');
        const loadingText = document.getElementById('maternity-loading-text');
        const progressBar = document.getElementById('maternity-progress-bar');
        const progressText = document.getElementById('maternity-progress-text');
        const resultsHeader = document.getElementById('maternity-results-header');
        const resultsCount = document.getElementById('maternity-results-count');
        const resultsGrid = document.getElementById('maternity-results-grid');
        const downloadAllBtn = document.getElementById('maternity-download-all-btn');
        const errorMessage = document.getElementById('maternity-error-message');
        const errorText = document.getElementById('maternity-error-text');

        if (!fileInputArea) return;

        // State variables - Support 1-2 photos
        let photos = [null, null];
        let photoMimeTypes = [null, null];
        let generatedImages = [];
        let selectedTheme = 'elegant-indoor';
        let selectedStyle = 'soft-dreamy';
        let selectedRatio = '3:4';
        let selectedCount = 4; // 1-10, default 4 (count selector)

        // Count selection (1-10), default 4
        const maternityCountSel = document.getElementById('maternity-count-selection-grid');
        if (maternityCountSel) {
            maternityCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                maternityCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Theme descriptions in Bahasa Indonesia for better results
        const themeDescriptions = {
            'elegant-indoor': 'setting indoor yang elegan dengan dekorasi mewah, pencahayaan lembut, dan furnitur klasik. Suasana hangat dan intim di dalam ruangan yang tertata indah',
            'outdoor-nature': 'setting outdoor di tengah alam dengan pepohonan hijau, taman bunga, atau padang rumput. Natural lighting dan suasana segar',
            'studio-classic': 'studio foto profesional dengan backdrop solid atau bertekstur, lighting setup yang sempurna, dan komposisi klasik',
            'bohemian': 'gaya bohemian dengan dekorasi boho chic, dreamcatcher, bunga kering, kain-kain natural, dan nuansa earthy',
            'modern-minimalist': 'setting modern minimalis dengan garis bersih, warna netral, dan fokus pada subjek dengan background sederhana',
            'romantic-sunset': 'setting outdoor saat golden hour atau sunset, dengan cahaya matahari yang warm dan romantis, siluet yang indah',
            'beach-casual': 'setting pantai yang casual dan santai, dengan pasir pantai, ombak, dan suasana lepas yang natural'
        };

        // Style descriptions
        const styleDescriptions = {
            'soft-dreamy': 'style soft dan dreamy dengan tone warna pastel, lighting lembut, efek ethereal dan romantic',
            'bold-dramatic': 'style bold dan dramatic dengan kontras tinggi, warna yang kuat, dan pencahayaan yang striking',
            'natural-warm': 'style natural dan warm dengan tone warna hangat, pencahayaan natural, dan suasana yang nyaman',
            'artistic-creative': 'style artistic dan creative dengan komposisi unik, angle yang menarik, dan sentuhan seni yang kuat'
        };

        // Multiple file upload handler
        function updateFileInputs() {
            if (!fileInputArea) return;
            fileInputArea.innerHTML = '';

            photos.forEach((photo, i) => {
                const wrapper = document.createElement('div');
                wrapper.className = 'file-input-wrapper';
                if (photo) wrapper.classList.add('has-photo');

                if (photo) {
                    wrapper.innerHTML = `
                        <img src="${photo}" alt="Photo ${i + 1}" class="photo-preview">
                        <button type="button" class="delete-btn" data-index="${i}">
                            <i class="fas fa-times" style="pointer-events: none;"></i>
                        </button>
                    `;
                } else {
                    wrapper.innerHTML = `
                        <div class="upload-placeholder">
                            <i class="fas fa-user"></i>
                            <p>Foto ${i + 1}</p>
                        </div>
                    `;
                }

                if (!photo) {
                    const input = document.createElement('input');
                    input.type = 'file';
                    input.accept = 'image/png, image/jpeg, image/webp, image/heic, .heic';
                    input.className = 'hidden';
                    input.dataset.index = i;
                    wrapper.appendChild(input);

                    wrapper.addEventListener('click', (e) => {
                        if (!e.target.classList.contains('delete-btn')) {
                            input.click();
                        }
                    });

                    input.addEventListener('change', (e) => handleFileChange(e));
                }

                fileInputArea.appendChild(wrapper);
            });

            updateValidation();
        }

        async function handleFileChange(e) {
            const index = parseInt(e.target.dataset.index, 10);
            const file = e.target.files[0];
            if (!file) return;

            try {
                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    const heic2any = window.heic2any;
                    if (heic2any) {
                        showError('Converting HEIC image...');
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File(
                            [convertedBlob],
                            file.name.replace(/\.heic$/i, '.jpg'),
                            { type: 'image/jpeg' }
                        );
                        hideError();
                    } else {
                        showError('HEIC format not supported. Please use JPG or PNG.');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    photos[index] = reader.result;
                    photoMimeTypes[index] = processedFile.type;
                    updateFileInputs();
                    hideError();

                    // Auto-scroll to next input on mobile
                    setTimeout(() => {
                        if (window.innerWidth < 768) {
                            const nextWrapper = fileInputArea.children[index + 1];
                            if (nextWrapper && !photos[index + 1]) {
                                nextWrapper.scrollIntoView({ behavior: 'smooth', block: 'center' });
                            } else if (themeOptions) {
                                themeOptions.scrollIntoView({ behavior: 'smooth', block: 'center' });
                            }
                        }
                    }, 300);
                };
                reader.readAsDataURL(processedFile);
            } catch (error) {
                console.error('Error processing image:', error);
                showError('Gagal memproses gambar. Silakan coba lagi.');
            }
        }

        // Delete photo handler
        fileInputArea.addEventListener('click', (e) => {
            if (e.target.classList.contains('delete-btn')) {
                e.stopPropagation();
                const index = parseInt(e.target.dataset.index, 10);
                photos[index] = null;
                photoMimeTypes[index] = null;
                updateFileInputs();
            }
        });

        // Add photo button
        if (addPhotoBtn) {
            addPhotoBtn.addEventListener('click', () => {
                const emptyIndex = photos.findIndex(p => p === null);
                if (emptyIndex !== -1 && emptyIndex < 2) {
                    const wrapper = fileInputArea.children[emptyIndex];
                    const input = wrapper.querySelector('input[type="file"]');
                    if (input) input.click();
                }
            });
        }

        function updateValidation() {
            const hasAtLeastOne = photos.some(p => p !== null);
            if (generateBtn) {
                generateBtn.disabled = !hasAtLeastOne;
            }
            if (addPhotoBtn) {
                const filledCount = photos.filter(p => p !== null).length;
                addPhotoBtn.disabled = filledCount >= 2;
                addPhotoBtn.style.opacity = filledCount >= 2 ? '0.5' : '1';
            }
        }

        // Initialize file inputs
        updateFileInputs();

        // Theme selection handler
        if (themeOptions) {
            themeOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                themeOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
                button.classList.add('selected');
                selectedTheme = button.dataset.value;

                // Show/hide custom theme input
                if (selectedTheme === 'custom' && customThemeContainer) {
                    customThemeContainer.classList.remove('hidden');
                } else if (customThemeContainer) {
                    customThemeContainer.classList.add('hidden');
                }
            });
        }

        // Style selection handler
        if (styleOptions) {
            styleOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                styleOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
                button.classList.add('selected');
                selectedStyle = button.dataset.value;
            });
        }

        // Ratio selection handler
        if (ratioOptions) {
            ratioOptions.addEventListener('click', (e) => {
                const button = e.target.closest('[data-value]');
                if (!button) return;

                ratioOptions.querySelectorAll('button').forEach(btn => btn.classList.remove('selected'));
                button.classList.add('selected');
                selectedRatio = button.dataset.value;
            });
        }

        // Generate button handler
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                const hasPhoto = photos.some(p => p !== null);
                if (!hasPhoto) {
                    showError('Silakan upload minimal 1 foto terlebih dahulu');
                    return;
                }

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
                hideError();
                resultsHeader.classList.add('hidden');
                loadingIndicator.classList.remove('hidden');

                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    await generateMaternityPhotos();
                    successCount = resultsGrid.querySelectorAll('img').length;
                }

                resultsGrid.querySelectorAll('[id^="maternity-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });

                loadingIndicator.classList.add('hidden');

                const validMaternityCount = generatedImages.filter(Boolean).length;
                if (validMaternityCount > 0) {
                    resultsHeader.classList.remove('hidden');
                    if (resultsCount) resultsCount.textContent = validMaternityCount;
                    setTimeout(() => {
                        if (window.innerWidth < 768 && resultsHeader) {
                            resultsHeader.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
                        }
                    }, 300);
                }

                generateBtn.disabled = false;
                generateBtn.innerHTML = originalHTML;

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                }
            });
        }

        // Generate maternity photos function
        async function generateMaternityPhotos() {
            generatedImages = [];
            resultsGrid.innerHTML = '';

            // Get custom theme if selected
            let themeDescription = '';
            if (selectedTheme === 'custom' && customThemeInput && customThemeInput.value.trim()) {
                themeDescription = customThemeInput.value.trim();
            } else {
                themeDescription = themeDescriptions[selectedTheme] || themeDescriptions['elegant-indoor'];
            }

            const styleDescription = styleDescriptions[selectedStyle] || styleDescriptions['soft-dreamy'];

            // Create placeholder cards
            for (let i = 1; i <= selectedCount; i++) {
                const card = document.createElement('div');
                card.id = `maternity-card-${i}`;
                card.className = 'relative rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-[3/4]';
                card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-rose-500"></div>';
                resultsGrid.appendChild(card);
            }

            // Generate all images in parallel
            const promises = [];
            for (let i = 1; i <= selectedCount; i++) {
                promises.push(generateSinglePhoto(i, themeDescription, styleDescription));
            }

            await Promise.allSettled(promises);
        }

        // Generate single photo
        async function generateSinglePhoto(index, themeDescription, styleDescription) {
            const card = document.getElementById(`maternity-card-${index}`);
            if (!card) return;

            try {
                // Get uploaded photos
                const uploadedFiles = photos
                    .map((photo, i) => photo ? { base64: photo.split(',')[1], mimeType: photoMimeTypes[i] || 'image/jpeg' } : null)
                    .filter(f => f !== null);

                const isCouple = uploadedFiles.length === 2;

                // Build comprehensive prompt in Bahasa Indonesia
                let prompt;
                if (isCouple) {
                    prompt = `PENTING: Buat foto maternity couple profesional yang menampilkan KEDUA ORANG dari kedua foto referensi BERSAMA-SAMA dalam satu foto yang SANGAT REALISTIS dan natural.

SUBJEK (CRITICAL - MUST SHOW BOTH PEOPLE):
- WANITA HAMIL (dari foto 1) dengan perut hamil yang jelas terlihat
- SUAMI/PASANGAN (dari foto 2) bersama wanita hamil
- KEDUA ORANG HARUS TERLIHAT JELAS dalam satu frame
- Keep identity EXACTLY the same untuk KEDUA orang (wajah, bentuk wajah, ekspresi, dan karakteristik fisik HARUS identik dengan foto referensi masing-masing)
- Pose couple maternity yang romantis dan intimate - pasangan saling berinteraksi
- Suami bisa: memeluk dari belakang, memegang perut hamil bersama istri, berdiri di samping dengan tangan di bahu/pinggang, atau pose couple lainnya
- Ekspresi yang lembut, bahagia, dan penuh kasih sayang pada KEDUA orang

TEMA & SETTING:
${themeDescription}

STYLE & MOOD:
${styleDescription}

DETAIL TEKNIS:
- Pencahayaan profesional yang flattering untuk KEDUA subjek
- Komposisi couple yang balance dan aesthetic
- Depth of field yang tepat - fokus pada couple
- Wardrobe yang sesuai dengan tema untuk KEDUA orang (wanita: maternity dress/flowing fabric, pria: matching outfit dengan tema)
- Foto berkualitas tinggi, tajam, dan detail

PENTING:
- HARUS sangat realistis dan natural, BUKAN ilustrasi atau kartun
- Keep BOTH people's identity EXACTLY the same as reference photos
- Fokus pada keindahan momen kehamilan sebagai couple
- Professional couple maternity photography quality
- Wajah dan karakteristik fisik HARUS sama persis dengan foto asli untuk KEDUA orang
- KEDUA orang HARUS ada dalam satu foto (bukan hanya wanita hamil saja)

Generate foto couple maternity yang indah, romantis, bermakna, dan profesional dengan KEDUA pasangan terlihat jelas.`;
                } else {
                    prompt = `Buat foto maternity profesional yang SANGAT REALISTIS dan natural dengan karakteristik berikut:

SUBJEK:
- Wanita hamil dengan perut hamil yang jelas terlihat
- Keep identity EXACTLY the same (wajah, bentuk wajah, ekspresi, dan karakteristik fisik HARUS identik dengan foto referensi)
- Pose maternity yang anggun dan menonjolkan perut hamil
- Ekspresi yang lembut, bahagia, dan penuh kasih sayang

TEMA & SETTING:
${themeDescription}

STYLE & MOOD:
${styleDescription}

DETAIL TEKNIS:
- Pencahayaan profesional yang flattering untuk subjek
- Komposisi yang balance dan aesthetic
- Depth of field yang tepat
- Wardrobe yang sesuai dengan tema (maternity dress, flowing fabric, atau outfit yang menonjolkan perut hamil)
- Foto berkualitas tinggi, tajam, dan detail

PENTING:
- HARUS sangat realistis dan natural, BUKAN ilustrasi atau kartun
- Keep person's identity EXACTLY the same as reference photo
- Fokus pada keindahan momen kehamilan
- Professional maternity photography quality
- Wajah dan karakteristik fisik HARUS sama persis dengan foto asli

Generate foto maternity yang indah, bermakna, dan profesional.`;
                }

                // Prepare API request
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                // Build payload parts
                const parts = [{ text: prompt }];
                uploadedFiles.forEach(file => {
                    parts.push({
                        inlineData: {
                            mimeType: file.mimeType,
                            data: file.base64
                        }
                    });
                });

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: {
                            aspectRatio: selectedRatio
                        }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${imageData}`;
                const filename = `maternity_photo_${selectedTheme}_${index}_${Date.now()}.png`;

                generatedImages[index - 1] = { url: imageUrl, filename: filename };

                // Update card with image and action buttons
                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Maternity Photo ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-rose-500 to-pink-500 hover:from-rose-600 hover:to-pink-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;
            } catch (error) {
                console.error(`Error generating maternity photo ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons (mobile-friendly)
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.classList.contains('action-btn') ? e.target : e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all functionality (mobile-friendly with progress)
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        // Helper functions
        function showError(message) {
            if (errorMessage && errorText) {
                errorText.textContent = message;
                errorMessage.classList.remove('hidden');
            }
        }

        function hideError() {
            if (errorMessage) {
                errorMessage.classList.add('hidden');
            }
        }
    })();

    // ==================== DESAIN RUMAH ====================
    (function() {
        const drTabInterior = document.getElementById('dr-tab-interior');
        const drTabEksterior = document.getElementById('dr-tab-eksterior');
        const drContentInterior = document.getElementById('dr-content-interior');
        const drContentEksterior = document.getElementById('dr-content-eksterior');

        if (!drTabInterior) return;

        function switchDesainRumahTab(tabName) {
            const isInteriorActive = tabName === 'interior';
            drTabInterior.classList.toggle('selected', isInteriorActive);
            drTabEksterior.classList.toggle('selected', !isInteriorActive);
            drContentInterior.classList.toggle('hidden', !isInteriorActive);
            drContentEksterior.classList.toggle('hidden', isInteriorActive);
        }

        drTabInterior.addEventListener('click', () => switchDesainRumahTab('interior'));
        drTabEksterior.addEventListener('click', () => switchDesainRumahTab('eksterior'));

        // --- INTERIOR ---
        const drInteriorRoomInput = document.getElementById('dr-interior-room-input');
        const drInteriorRoomUploadBox = document.getElementById('dr-interior-room-upload-box');
        const drInteriorRoomPreview = document.getElementById('dr-interior-room-preview');
        const drInteriorRoomPlaceholder = document.getElementById('dr-interior-room-placeholder');
        const drInteriorRemoveRoomBtn = document.getElementById('dr-interior-remove-room-btn');
        const drInteriorFurniturePrompt = document.getElementById('dr-interior-furniture-prompt');
        const drInteriorAddFurnitureBtn = document.getElementById('dr-interior-add-furniture-btn');
        const drInteriorAiFurnitureContainer = document.getElementById('dr-interior-ai-furniture-container');
        const drInteriorAiFurniturePlaceholder = document.getElementById('dr-interior-ai-furniture-placeholder');
        const drInteriorManualFurnitureInput = document.getElementById('dr-interior-manual-furniture-input');
        const drInteriorManualFurnitureContainer = document.getElementById('dr-interior-manual-furniture-container');
        const drInteriorInstructionInput = document.getElementById('dr-interior-instruction-input');
        const drInteriorMagicInstructionBtn = document.getElementById('dr-interior-magic-instruction-btn');
        const drInteriorRatioOptions = document.getElementById('dr-interior-ratio-options');
        const drInteriorGenerateBtn = document.getElementById('dr-interior-generate-btn');
        const drInteriorResultsContainer = document.getElementById('dr-interior-results-container');
        const drInteriorResultsGrid = document.getElementById('dr-interior-results-grid');

        let drInteriorRoomData = null;
        let drInteriorAiFurniture = [];
        let drInteriorManualFurniture = [];

        function drInteriorUpdateGenerateButtonState() {
            const hasRoom = !!drInteriorRoomData;
            const hasFurniture = drInteriorAiFurniture.length > 0 || drInteriorManualFurniture.length > 0;
            const hasInstruction = drInteriorInstructionInput.value.trim() !== '';

            drInteriorGenerateBtn.disabled = !hasRoom || !hasFurniture || !hasInstruction;
            drInteriorAddFurnitureBtn.disabled = !hasRoom || drInteriorFurniturePrompt.value.trim() === '';
            drInteriorMagicInstructionBtn.disabled = !hasRoom || !hasFurniture;
        }

        drInteriorInstructionInput.addEventListener('input', drInteriorUpdateGenerateButtonState);

        setupImageUploadNew(drInteriorRoomInput, drInteriorRoomUploadBox, (data) => {
            drInteriorRoomData = data;
            drInteriorRoomPreview.src = data.dataUrl;
            drInteriorRoomPlaceholder.classList.add('hidden');
            drInteriorRoomPreview.classList.remove('hidden');
            drInteriorRemoveRoomBtn.classList.remove('hidden');
            drInteriorUpdateGenerateButtonState();
        });

        drInteriorRemoveRoomBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            drInteriorRoomData = null;
            drInteriorRoomInput.value = '';
            drInteriorRoomPreview.src = '#';
            drInteriorRoomPreview.classList.add('hidden');
            drInteriorRoomPlaceholder.classList.remove('hidden');
            drInteriorRemoveRoomBtn.classList.add('hidden');

            document.getElementById('dr-interior-results-placeholder').classList.remove('hidden');
            drInteriorResultsContainer.classList.add('hidden');
            drInteriorResultsGrid.innerHTML = '';

            drInteriorUpdateGenerateButtonState();
        });

        drInteriorFurniturePrompt.addEventListener('input', drInteriorUpdateGenerateButtonState);

        drInteriorAddFurnitureBtn.addEventListener('click', async () => {
            const prompt = drInteriorFurniturePrompt.value.trim();
            if (!prompt) return;

            const originalBtnHTML = drInteriorAddFurnitureBtn.innerHTML;
            drInteriorAddFurnitureBtn.disabled = true;
            drInteriorAddFurnitureBtn.innerHTML = `<div class="loader-icon-umrah !border-l-white w-5 h-5"></div>`;
            drInteriorAiFurniturePlaceholder.classList.add('hidden');

            const loaderItem = document.createElement('div');
            loaderItem.className = 'aspect-square bg-gray-200 rounded-md flex items-center justify-center';
            loaderItem.innerHTML = `<div class="loader-icon-umrah w-6 h-6"></div>`;
            drInteriorAiFurnitureContainer.appendChild(loaderItem);

            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const finalPrompt = `A photorealistic image of a single piece of furniture: ${prompt}. The furniture should be on a plain white background, front view, no shadows.`;
                const payload = {
                    contents: [{ parts: [{ text: finalPrompt }] }],
                    generationConfig: { responseModalities: ['IMAGE'], imageConfig: { aspectRatio: '1:1' } }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) throw new Error(`Error ${response.status}`);

                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error("No image data from AI.");

                const furnitureData = {
                    base64: base64Data,
                    mimeType: 'image/png',
                    dataUrl: `data:image/png;base64,${base64Data}`
                };
                drInteriorAiFurniture.push(furnitureData);
                drInteriorRenderAiFurniture();
                drInteriorFurniturePrompt.value = '';
            } catch (error) {
                console.error("Error generating AI furniture:", error);
                alert(`Gagal membuat furnitur: ${error.message}`);
                if (drInteriorAiFurniture.length === 0) drInteriorAiFurniturePlaceholder.classList.remove('hidden');
            } finally {
                loaderItem.remove();
                drInteriorAddFurnitureBtn.innerHTML = originalBtnHTML;
                drInteriorUpdateGenerateButtonState();
            }
        });

        function drInteriorRenderAiFurniture() {
            drInteriorAiFurnitureContainer.innerHTML = '';
            if (drInteriorAiFurniture.length === 0) {
                drInteriorAiFurnitureContainer.appendChild(drInteriorAiFurniturePlaceholder);
                drInteriorAiFurniturePlaceholder.classList.remove('hidden');
            } else {
                drInteriorAiFurniturePlaceholder.classList.add('hidden');
            }

            drInteriorAiFurniture.forEach((item, index) => {
                const div = document.createElement('div');
                div.className = 'relative aspect-square';
                div.innerHTML = `
                    <img src="${item.dataUrl}" class="w-full h-full object-contain rounded-md border">
                    <button data-index="${index}" class="dr-interior-remove-ai-furniture absolute top-1 right-1 bg-red-600 text-white rounded-full p-0.5 hover:bg-red-700">
                        <i class="fas fa-times"></i>
                    </button>
                `;
                drInteriorAiFurnitureContainer.appendChild(div);
            });
            drInteriorUpdateGenerateButtonState();
        }

        drInteriorAiFurnitureContainer.addEventListener('click', (e) => {
            const removeBtn = e.target.closest('.dr-interior-remove-ai-furniture');
            if (removeBtn) {
                const index = parseInt(removeBtn.dataset.index);
                drInteriorAiFurniture.splice(index, 1);
                drInteriorRenderAiFurniture();
            }
        });

        setupImageUploadNew(drInteriorManualFurnitureInput, null, (data) => {
            drInteriorManualFurniture.push(data);
            drInteriorRenderManualFurniture();
        });

        function drInteriorRenderManualFurniture() {
            drInteriorManualFurnitureContainer.innerHTML = '';
            drInteriorManualFurniture.forEach((item, index) => {
                const div = document.createElement('div');
                div.className = 'relative aspect-square';
                div.innerHTML = `
                    <img src="${item.dataUrl}" class="w-full h-full object-contain rounded-md border">
                    <button data-index="${index}" class="dr-interior-remove-manual-furniture absolute top-1 right-1 bg-red-600 text-white rounded-full p-0.5 hover:bg-red-700">
                        <i class="fas fa-times"></i>
                    </button>
                `;
                drInteriorManualFurnitureContainer.appendChild(div);
            });

            drInteriorManualFurnitureContainer.querySelectorAll('.dr-interior-remove-manual-furniture').forEach(btn => {
                btn.addEventListener('click', (e) => {
                    const index = parseInt(e.currentTarget.dataset.index);
                    drInteriorManualFurniture.splice(index, 1);
                    drInteriorRenderManualFurniture();
                });
            });

            drInteriorUpdateGenerateButtonState();
        }

        drInteriorMagicInstructionBtn.addEventListener('click', async () => {
            const originalBtnHTML = drInteriorMagicInstructionBtn.innerHTML;
            drInteriorMagicInstructionBtn.disabled = true;
            drInteriorMagicInstructionBtn.innerHTML = `<div class="loader-icon-umrah w-5 h-5"></div>`;

            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `You are a professional interior designer. Analyze the provided room image (the first one) and the furniture images (all subsequent ones). Based on the room's style and the available furniture, generate a concise layout instruction in Indonesian. For example: "Letakkan sofa di depan jendela, dengan meja kopi di depannya. Tambahkan lukisan di dinding di atas sofa." Respond ONLY with the instruction text.`;

                const parts = [{ text: "Buatkan instruksi desain untuk ruangan ini dengan furnitur berikut:" }, { inlineData: { mimeType: drInteriorRoomData.mimeType, data: drInteriorRoomData.base64 } }];
                [...drInteriorAiFurniture, ...drInteriorManualFurniture].forEach(furniture => {
                    parts.push({ inlineData: { mimeType: furniture.mimeType, data: furniture.base64 } });
                });
                const payload = { contents: [{ parts }], systemInstruction: { parts: [{ text: systemPrompt }] } };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) throw new Error(`Error ${response.status}`);

                const result = await response.json();
                drInteriorInstructionInput.value = result.candidates[0].content.parts[0].text.trim();
            } catch (error) {
                console.error("Error generating magic instruction:", error);
                drInteriorInstructionInput.value = `Gagal membuat instruksi: ${error.message}`;
            } finally {
                drInteriorMagicInstructionBtn.innerHTML = originalBtnHTML;
                drInteriorUpdateGenerateButtonState();
            }
        });

        setupOptionButtonsNew(drInteriorRatioOptions);

        drInteriorGenerateBtn.addEventListener('click', async () => {
            const originalBtnHTML = drInteriorGenerateBtn.innerHTML;
            drInteriorGenerateBtn.disabled = true;
            drInteriorGenerateBtn.innerHTML = `<div class="loader-icon-umrah !border-l-white w-5 h-5"></div><span class="ml-2">Mendesain Ruangan...</span>`;

            document.getElementById('dr-interior-results-placeholder').classList.add('hidden');
            drInteriorResultsContainer.classList.remove('hidden');

            const aspectRatio = drInteriorRatioOptions.querySelector('.selected').dataset.value;
            const aspectClass = getAspectRatioClassNew(aspectRatio);
            const TOTAL_INTERIOR_COUNT = 15;
            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;
            let drInteriorResults = [];

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                drInteriorResults = [];
                drInteriorResultsGrid.innerHTML = '';

                for (let i = 1; i <= TOTAL_INTERIOR_COUNT; i++) {
                    const card = document.createElement('div');
                    card.id = `dr-interior-card-${i}`;
                    card.className = `card overflow-hidden bg-gray-100 flex items-center justify-center ${aspectClass}`;
                    card.innerHTML = `<div class="loader-icon-umrah w-8 h-8"></div>`;
                    drInteriorResultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: TOTAL_INTERIOR_COUNT }, (_, i) =>
                    generateSingleInteriorDesign(i + 1, aspectRatio, drInteriorResults)
                );
                await Promise.allSettled(generationPromises);

                drInteriorResultsGrid.querySelectorAll('[id^="dr-interior-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });

                successCount = drInteriorResults.filter(Boolean).length;
            }

            if (successCount === 0) alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');

            drInteriorGenerateBtn.disabled = false;
            drInteriorGenerateBtn.innerHTML = originalBtnHTML;
        });

        async function generateSingleInteriorDesign(id, aspectRatio, drInteriorResults) {
            const card = document.getElementById(`dr-interior-card-${id}`);
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const instruction = drInteriorInstructionInput.value.trim();
                let prompt = `Your task is to photorealistically place furniture items into an existing room photo. You are given one primary image (the room) and multiple secondary images (the furniture).
                ABSOLUTE RULES: 1. DO NOT CHANGE THE ROOM's structure, walls, floor, or windows. 2. ONLY ADD the provided furniture items. 3. USE ALL furniture items provided. 4. INTEGRATE REALISTICALLY with correct scale, perspective, lighting, and shadows.
                USER'S PLACEMENT GUIDE: "${instruction}".
                FINAL OUTPUT: A single, photorealistic composite image showing the original room with the new furniture added. This is design variation number ${id}.`;

                const parts = [{ text: prompt }, { inlineData: { mimeType: drInteriorRoomData.mimeType, data: drInteriorRoomData.base64 } }];
                [...drInteriorAiFurniture, ...drInteriorManualFurniture].forEach(furniture => {
                    parts.push({ inlineData: { mimeType: furniture.mimeType, data: furniture.base64 } });
                });

                const payload = {
                    contents: [{ parts: parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: aspectRatio }
                    }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error("No image data from AI for this design.");

                const imageUrl = `data:image/png;base64,${base64Data}`;
                drInteriorResults[id - 1] = imageUrl;
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover cursor-pointer" onclick="window.open('${imageUrl}', '_blank')">
                    <div class="absolute bottom-2 right-2 flex gap-1">
                        <button onclick="window.open('${imageUrl}', '_blank')" class="bg-white hover:bg-gray-100 text-gray-800 p-2 rounded-full shadow-md" title="Lihat Gambar">
                            <i class="fas fa-eye"></i>
                        </button>
                        <button onclick="downloadDataURINew('${imageUrl}', 'desain_interior_${id}.png'); event.stopPropagation();" class="text-white p-2 rounded-full shadow-md" style="background-color: #0EA5E9;" title="Unduh Gambar" data-action="download">
                            <i class="fas fa-download"></i>
                        </button>
                    </div>`;
                card.classList.remove('bg-gray-100', 'flex', 'items-center', 'justify-center');
                card.classList.add('relative');
            } catch (error) {
                console.error(`Error for interior design card ${id}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // --- EKSTERIOR ---
        const drEksteriorHouseInput = document.getElementById('dr-eksterior-house-input');
        const drEksteriorHouseUploadBox = document.getElementById('dr-eksterior-house-upload-box');
        const drEksteriorHousePreview = document.getElementById('dr-eksterior-house-preview');
        const drEksteriorHousePlaceholder = document.getElementById('dr-eksterior-house-placeholder');
        const drEksteriorRemoveHouseBtn = document.getElementById('dr-eksterior-remove-house-btn');
        const drEksteriorStyleOptions = document.getElementById('dr-eksterior-style-options');
        const drEksteriorKustomContainer = document.getElementById('dr-eksterior-kustom-container');
        const drEksteriorKustomInput = document.getElementById('dr-eksterior-kustom-input');
        const drEksteriorInstructionInput = document.getElementById('dr-eksterior-instruction-input');
        const drEksteriorMagicInstructionBtn = document.getElementById('dr-eksterior-magic-instruction-btn');
        const drEksteriorRatioOptions = document.getElementById('dr-eksterior-ratio-options');
        const drEksteriorGenerateBtn = document.getElementById('dr-eksterior-generate-btn');
        const drEksteriorResultsContainer = document.getElementById('dr-eksterior-results-container');
        const drEksteriorResultsGrid = document.getElementById('dr-eksterior-results-grid');

        let drEksteriorHouseData = null;

        function drEksteriorUpdateGenerateButtonState() {
            const hasImage = !!drEksteriorHouseData;
            const hasStyle = drEksteriorStyleOptions.querySelector('.selected') !== null;
            const hasInstruction = drEksteriorInstructionInput.value.trim() !== '';

            drEksteriorGenerateBtn.disabled = !hasImage || !hasStyle || !hasInstruction;
            drEksteriorMagicInstructionBtn.disabled = !hasImage || !hasStyle;
        }

        setupImageUploadNew(drEksteriorHouseInput, drEksteriorHouseUploadBox, (data) => {
            drEksteriorHouseData = data;
            drEksteriorHousePreview.src = data.dataUrl;
            drEksteriorHousePlaceholder.classList.add('hidden');
            drEksteriorHousePreview.classList.remove('hidden');
            drEksteriorRemoveHouseBtn.classList.remove('hidden');
            drEksteriorUpdateGenerateButtonState();
        });

        drEksteriorRemoveHouseBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            drEksteriorHouseData = null;
            drEksteriorHouseInput.value = '';
            drEksteriorHousePreview.src = '#';
            drEksteriorHousePreview.classList.add('hidden');
            drEksteriorHousePlaceholder.classList.remove('hidden');
            drEksteriorRemoveHouseBtn.classList.add('hidden');

            document.getElementById('dr-eksterior-results-placeholder').classList.remove('hidden');
            drEksteriorResultsContainer.classList.add('hidden');
            drEksteriorResultsGrid.innerHTML = '';

            drEksteriorUpdateGenerateButtonState();
        });

        setupOptionButtonsNew(drEksteriorStyleOptions, true);
        drEksteriorStyleOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (button && button.dataset.value === 'Kustom') {
                drEksteriorKustomContainer.classList.toggle('hidden', !button.classList.contains('selected'));
            }
            drEksteriorUpdateGenerateButtonState();
        });

        drEksteriorInstructionInput.addEventListener('input', drEksteriorUpdateGenerateButtonState);
        setupOptionButtonsNew(drEksteriorRatioOptions);

        drEksteriorMagicInstructionBtn.addEventListener('click', async () => {
            const originalBtnHTML = drEksteriorMagicInstructionBtn.innerHTML;
            drEksteriorMagicInstructionBtn.disabled = true;
            drEksteriorMagicInstructionBtn.innerHTML = `<div class="loader-icon-umrah w-5 h-5"></div>`;

            try {
                const selectedStyles = Array.from(drEksteriorStyleOptions.querySelectorAll('.selected')).map(btn => {
                    if (btn.dataset.value === 'Kustom') {
                        return drEksteriorKustomInput.value.trim();
                    }
                    return btn.dataset.value;
                }).filter(Boolean);

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `You are a professional exterior architect. Analyze the provided house photo and the user's selected renovation themes. Based on this, generate a concise design instruction in Indonesian for an AI image generator. For example: "Ganti warna cat rumah menjadi abu-abu modern, tambahkan taman minimalis di halaman depan, dan ganti pagar dengan model besi tempa hitam." Respond ONLY with the instruction text.`;
                const userQuery = `House photo provided. Renovation themes: ${selectedStyles.join(', ')}. Generate an instruction.`;

                const payload = {
                    contents: [{ parts: [{ text: userQuery }, { inlineData: { mimeType: drEksteriorHouseData.mimeType, data: drEksteriorHouseData.base64 } }] }],
                    systemInstruction: { parts: [{ text: systemPrompt }] }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) throw new Error(`Error ${response.status}`);

                const result = await response.json();
                drEksteriorInstructionInput.value = result.candidates[0].content.parts[0].text.trim();
                drEksteriorUpdateGenerateButtonState();

            } catch (error) {
                console.error("Error generating magic instruction:", error);
                drEksteriorInstructionInput.value = `Gagal membuat instruksi: ${error.message}`;
            } finally {
                drEksteriorMagicInstructionBtn.innerHTML = originalBtnHTML;
                drEksteriorUpdateGenerateButtonState();
            }
        });

        drEksteriorGenerateBtn.addEventListener('click', async () => {
            const originalBtnHTML = drEksteriorGenerateBtn.innerHTML;
            drEksteriorGenerateBtn.disabled = true;
            drEksteriorGenerateBtn.innerHTML = `<div class="loader-icon-umrah !border-l-white w-5 h-5"></div><span class="ml-2">Mendesain Ulang...</span>`;

            document.getElementById('dr-eksterior-results-placeholder').classList.add('hidden');
            drEksteriorResultsContainer.classList.remove('hidden');

            const aspectRatio = drEksteriorRatioOptions.querySelector('.selected').dataset.value;
            const aspectClass = getAspectRatioClassNew(aspectRatio);
            const TOTAL_EKSTERIOR_COUNT = 15;
            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;
            let drEksteriorResults = [];

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                drEksteriorResults = [];
                drEksteriorResultsGrid.innerHTML = '';

                for (let i = 1; i <= TOTAL_EKSTERIOR_COUNT; i++) {
                    const card = document.createElement('div');
                    card.id = `dr-eksterior-card-${i}`;
                    card.className = `card overflow-hidden bg-gray-100 flex items-center justify-center ${aspectClass}`;
                    card.innerHTML = `<div class="loader-icon-umrah w-8 h-8"></div>`;
                    drEksteriorResultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: TOTAL_EKSTERIOR_COUNT }, (_, i) =>
                    generateSingleEksteriorDesign(i + 1, aspectRatio, drEksteriorResults)
                );
                await Promise.allSettled(generationPromises);

                drEksteriorResultsGrid.querySelectorAll('[id^="dr-eksterior-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });

                successCount = drEksteriorResults.filter(Boolean).length;
            }

            if (successCount === 0) alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');

            drEksteriorGenerateBtn.disabled = false;
            drEksteriorGenerateBtn.innerHTML = originalBtnHTML;
        });

        async function generateSingleEksteriorDesign(id, aspectRatio, drEksteriorResults) {
            const card = document.getElementById(`dr-eksterior-card-${id}`);
            try {
                const selectedStyles = Array.from(drEksteriorStyleOptions.querySelectorAll('.selected')).map(btn => {
                    if (btn.dataset.value === 'Kustom') return drEksteriorKustomInput.value.trim();
                    return btn.dataset.value;
                }).filter(Boolean);

                const instruction = drEksteriorInstructionInput.value.trim();

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const prompt = `Redesign the exterior of the house in the provided image. CRITICAL: Maintain the core structure and shape of the original house. Focus only on changing the facade, colors, materials, and landscaping as instructed.
                - Renovation Themes: ${selectedStyles.join(', ')}.
                - Detailed Instructions: "${instruction}".
                The final result must be a photorealistic image of the redesigned house exterior. This is design variation number ${id}.`;

                const payload = {
                    contents: [{ parts: [{ text: prompt }, { inlineData: { mimeType: drEksteriorHouseData.mimeType, data: drEksteriorHouseData.base64 } }] }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: aspectRatio }
                    }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error("No image data from AI for this design.");

                const imageUrl = `data:image/png;base64,${base64Data}`;
                drEksteriorResults[id - 1] = imageUrl;
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover cursor-pointer" onclick="window.open('${imageUrl}', '_blank')">
                    <div class="absolute bottom-2 right-2 flex gap-1">
                        <button onclick="window.open('${imageUrl}', '_blank')" class="bg-white hover:bg-gray-100 text-gray-800 p-2 rounded-full shadow-md" title="Lihat Gambar">
                            <i class="fas fa-eye"></i>
                        </button>
                        <button onclick="downloadDataURINew('${imageUrl}', 'desain_eksterior_${id}.png'); event.stopPropagation();" class="text-white p-2 rounded-full shadow-md" style="background-color: #0EA5E9;" title="Unduh Gambar" data-action="download">
                            <i class="fas fa-download"></i>
                        </button>
                    </div>`;
                card.classList.remove('bg-gray-100', 'flex', 'items-center', 'justify-center');
                card.classList.add('relative');
            } catch (error) {
                console.error(`Error for eksterior design card ${id}:`, error);
                if (card) card.innerHTML = '';
            }
        }
    })();

    // ==================== SKETSA GAMBAR ====================
    (function() {
        const stiImageInput = document.getElementById('sti-image-input');
        const stiUploadBox = document.getElementById('sti-upload-box');
        const stiPreview = document.getElementById('sti-preview');
        const stiPlaceholder = document.getElementById('sti-placeholder');
        const stiRemoveBtn = document.getElementById('sti-remove-btn');
        const stiPurposeOptions = document.getElementById('sti-purpose-options');
        const stiCustomPurposeContainer = document.getElementById('sti-custom-purpose-container');
        const stiCustomPurposeInput = document.getElementById('sti-custom-purpose-input');
        const stiPromptInput = document.getElementById('sti-prompt-input');
        const stiMagicPromptBtn = document.getElementById('sti-magic-prompt-btn');
        const stiRatioOptions = document.getElementById('sti-ratio-options');
        const stiGenerateBtn = document.getElementById('sti-generate-btn');
        const stiResultsContainer = document.getElementById('sti-results-container');
        const stiResultsGrid = document.getElementById('sti-results-grid');

        if (!stiImageInput) return;

        // Count selection (1-10), default 4
        let selectedCount = 4;
        const stiCountSel = document.getElementById('sti-count-selection-grid');
        if (stiCountSel) {
            stiCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                stiCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        let stiImageData = null;

        function stiUpdateButtons() {
            const hasImage = !!stiImageData;
            const hasPurpose = stiPurposeOptions.querySelector('.selected') !== null;
            const hasPrompt = stiPromptInput.value.trim() !== '';

            stiGenerateBtn.disabled = !hasImage || !hasPurpose || !hasPrompt;
            stiMagicPromptBtn.disabled = !hasImage || !hasPurpose;
        }

        setupImageUploadNew(stiImageInput, stiUploadBox, (data) => {
            stiImageData = data;
            stiPreview.src = data.dataUrl;
            stiPlaceholder.classList.add('hidden');
            stiPreview.classList.remove('hidden');
            stiRemoveBtn.classList.remove('hidden');
            stiUpdateButtons();
        });

        stiRemoveBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            stiImageData = null;
            stiImageInput.value = '';
            stiPreview.src = '#';
            stiPreview.classList.add('hidden');
            stiPlaceholder.classList.remove('hidden');
            stiRemoveBtn.classList.add('hidden');

            document.getElementById('sti-results-placeholder').classList.remove('hidden');
            stiResultsContainer.classList.add('hidden');
            stiResultsGrid.innerHTML = '';

            stiUpdateButtons();
        });

        setupOptionButtonsNew(stiPurposeOptions);
        stiPurposeOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (button && button.dataset.value === 'Kustom') {
                stiCustomPurposeContainer.classList.toggle('hidden', !button.classList.contains('selected'));
            }
            stiUpdateButtons();
        });

        stiPromptInput.addEventListener('input', stiUpdateButtons);
        setupOptionButtonsNew(stiRatioOptions);

        stiMagicPromptBtn.addEventListener('click', async () => {
            const originalBtnHTML = stiMagicPromptBtn.innerHTML;
            stiMagicPromptBtn.disabled = true;
            stiMagicPromptBtn.innerHTML = `<div class="loader-icon-umrah w-5 h-5"></div>`;

            try {
                let purpose = document.querySelector('#sti-purpose-options .selected').dataset.value;
                if (purpose === 'Kustom') {
                    purpose = stiCustomPurposeInput.value.trim() || 'Gambar Digital';
                }

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `You are a professional designer assistant. Analyze the provided sketch image and the user's intended purpose. Generate a concise, detailed prompt in Indonesian for an AI image generator to convert this sketch into a polished digital image. The prompt should specify the style, colors, details, and quality appropriate for the purpose. Respond ONLY with the prompt text, nothing else.`;
                const userQuery = `Sketch image provided. Purpose: ${purpose}. Generate a detailed conversion prompt.`;

                const payload = {
                    contents: [{ parts: [{ text: userQuery }, { inlineData: { mimeType: stiImageData.mimeType, data: stiImageData.base64 } }] }],
                    systemInstruction: { parts: [{ text: systemPrompt }] }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) throw new Error(`Error ${response.status}`);

                const result = await response.json();
                stiPromptInput.value = result.candidates[0].content.parts[0].text.trim();
                stiUpdateButtons();

            } catch (error) {
                console.error("Error generating magic prompt:", error);
                stiPromptInput.value = `Gagal membuat instruksi: ${error.message}`;
            } finally {
                stiMagicPromptBtn.innerHTML = originalBtnHTML;
                stiUpdateButtons();
            }
        });

        stiGenerateBtn.addEventListener('click', async () => {
            const originalBtnHTML = stiGenerateBtn.innerHTML;
            stiGenerateBtn.disabled = true;
            stiGenerateBtn.innerHTML = `<div class="loader-icon-umrah !border-l-white w-5 h-5"></div><span class="ml-2">Membuat Gambar...</span>`;

            document.getElementById('sti-results-placeholder').classList.add('hidden');
            stiResultsContainer.classList.remove('hidden');

            const aspectRatio = stiRatioOptions.querySelector('.selected').dataset.value;
            const aspectClass = getAspectRatioClassNew(aspectRatio);
            // selectedCount sudah di-declare di scope luar (count selector)
            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;
            let stiResults = [];

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                stiResults = [];
                stiResultsGrid.innerHTML = '';

                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `sti-card-${i}`;
                    card.className = `card overflow-hidden bg-gray-100 flex items-center justify-center ${aspectClass}`;
                    card.innerHTML = `<div class="loader-icon-umrah w-8 h-8"></div>`;
                    stiResultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSingleSketchImage(i + 1, aspectRatio, stiResults)
                );
                await Promise.allSettled(generationPromises);

                stiResultsGrid.querySelectorAll('[id^="sti-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });

                successCount = stiResults.filter(Boolean).length;
            }

            if (successCount === 0) alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');

            stiGenerateBtn.disabled = false;
            stiGenerateBtn.innerHTML = originalBtnHTML;
        });

        async function generateSingleSketchImage(id, aspectRatio, stiResults) {
            const card = document.getElementById(`sti-card-${id}`);
            try {
                let purpose = document.querySelector('#sti-purpose-options .selected').dataset.value;
                if (purpose === 'Kustom') {
                    purpose = stiCustomPurposeInput.value.trim() || 'Gambar Digital';
                }

                const instruction = stiPromptInput.value.trim();

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const prompt = `Transform the provided sketch into a polished, professional digital image.
                Purpose: ${purpose}.
                Detailed Instructions: "${instruction}".
                This is variation number ${id}. Create a high-quality, finished artwork based on the sketch. The final result must be clean, professional, and suitable for its intended purpose.`;

                const payload = {
                    contents: [{ parts: [{ text: prompt }, { inlineData: { mimeType: stiImageData.mimeType, data: stiImageData.base64 } }] }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: aspectRatio }
                    }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error("No image data from AI.");

                const imageUrl = `data:image/png;base64,${base64Data}`;
                stiResults[id - 1] = imageUrl;
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover cursor-pointer" onclick="window.open('${imageUrl}', '_blank')">
                    <div class="absolute bottom-2 right-2 flex gap-1">
                        <button onclick="window.open('${imageUrl}', '_blank')" class="bg-white hover:bg-gray-100 text-gray-800 p-2 rounded-full shadow-md" title="Lihat Gambar">
                            <i class="fas fa-eye"></i>
                        </button>
                        <button onclick="downloadDataURINew('${imageUrl}', 'sketsa_gambar_${id}.png'); event.stopPropagation();" class="text-white p-2 rounded-full shadow-md" style="background-color: #8B5CF6;" title="Unduh Gambar" data-action="download">
                            <i class="fas fa-download"></i>
                        </button>
                    </div>`;
                card.classList.remove('bg-gray-100', 'flex', 'items-center', 'justify-center');
                card.classList.add('relative');
            } catch (error) {
                console.error(`Error for sketch card ${id}:`, error);
                if (card) card.innerHTML = '';
            }
        }
    })();

    // ==================== BIKIN CAROUSEL ====================
    (() => {
        const apiKey = "";

        const crslImageInput = document.getElementById('crsl-image-input');
        const crslUploadBox = document.getElementById('crsl-upload-box');
        const crslPreview = document.getElementById('crsl-preview');
        const crslPlaceholder = document.getElementById('crsl-placeholder');
        const crslRemoveBtn = document.getElementById('crsl-remove-btn');
        const crslNameInput = document.getElementById('crsl-name-input');
        const crslDescInput = document.getElementById('crsl-desc-input');
        const crslAutoBtn = document.getElementById('crsl-auto-btn');
        const crslAutoScriptBtn = document.getElementById('crsl-auto-script-btn');
        const crslSlidesOptions = document.getElementById('crsl-slides-options');
        const crslRatioOptions = document.getElementById('crsl-ratio-options');
        const crslStyleOptions = document.getElementById('crsl-style-options');
        const crslScriptTypeOptions = document.getElementById('crsl-script-type-options');
        const crslScriptEditorContainer = document.getElementById('crsl-script-editor-container');
        const crslGenerateBtn = document.getElementById('crsl-generate-btn');
        const crslResultsContainer = document.getElementById('crsl-results-container');
        const crslResultsGrid = document.getElementById('crsl-results-grid');
        const crslKustomScriptContainer = document.getElementById('crsl-kustom-script-container');
        const crslKustomScriptInput = document.getElementById('crsl-kustom-script-input');
        const crslAutoNameTooltip = document.getElementById('crsl-auto-name-tooltip');
        const crslAutoScriptTooltip = document.getElementById('crsl-auto-script-tooltip');
        const crslTextModeToggle = document.getElementById('crsl-text-mode-toggle');
        const crslScriptSection = document.getElementById('crsl-script-section');
        const crslPositionOptions = document.getElementById('crsl-position-options');
        const crslFontOptions = document.getElementById('crsl-font-options');
        const crslFontColorOptions = document.getElementById('crsl-font-color-options');
        const crslTextSettingsSection = document.getElementById('crsl-text-settings-section');
        const crslAutoColorPreviewContainer = document.getElementById('crsl-auto-color-preview-container');
        const crslAutoColorPreview = document.getElementById('crsl-auto-color-preview');

        if (!crslImageInput) return;

        let crslImageData = null;
        let crslAutoFontColor = '#FFFFFF';
        const generatedCarousels = {};

        async function analyzeImageForFontColor(imageData) {
            return new Promise((resolve) => {
                const img = new Image();
                img.onload = () => {
                    const canvas = document.createElement('canvas');
                    const ctx = canvas.getContext('2d');
                    const sampleSize = 100;
                    canvas.width = sampleSize;
                    canvas.height = sampleSize;
                    ctx.drawImage(img, img.width * 0.25, img.height * 0.7, img.width * 0.5, img.height * 0.3, 0, 0, sampleSize, sampleSize);

                    const pixelData = ctx.getImageData(0, 0, sampleSize, sampleSize).data;
                    let totalBrightness = 0;
                    for (let i = 0; i < pixelData.length; i += 4) {
                        const r = pixelData[i];
                        const g = pixelData[i+1];
                        const b = pixelData[i+2];
                        const brightness = (0.299 * r + 0.587 * g + 0.114 * b) / 255;
                        totalBrightness += brightness;
                    }
                    const avgBrightness = totalBrightness / (pixelData.length / 4);
                    resolve(avgBrightness > 0.5 ? '#1f2937' : '#FFFFFF');
                };
                img.onerror = () => resolve('#FFFFFF');
                img.src = imageData.dataUrl;
            });
        }

        function showTooltip(tooltipElement) {
            tooltipElement.classList.add('visible');
            setTimeout(() => {
                tooltipElement.classList.remove('visible');
            }, 5000);
        }

        function crslUpdateButtons() {
            const hasImage = !!crslImageData;
            const hasName = crslNameInput.value.trim() !== '';
            const hasDesc = crslDescInput.value.trim() !== '';

            let scriptOk = true;
            if (crslTextModeToggle.checked) {
                const scriptTextareas = crslScriptEditorContainer.querySelectorAll('textarea');
                scriptOk = scriptTextareas.length > 0 && Array.from(scriptTextareas).every(ta => ta.value.trim() !== '');
            }

            crslGenerateBtn.disabled = !hasImage || !hasName || !hasDesc || !scriptOk;
            crslAutoBtn.disabled = !hasImage;
            crslAutoScriptBtn.disabled = !hasImage || !hasName || !hasDesc;

            if (hasImage && !hasName && !hasDesc) {
                showTooltip(crslAutoNameTooltip);
            } else if (hasImage && hasName && hasDesc) {
                if (crslTextModeToggle.checked) {
                    const scriptTextareas = crslScriptEditorContainer.querySelectorAll('textarea');
                    if (scriptTextareas.length > 0 && Array.from(scriptTextareas).every(ta => ta.value.trim() === '')) {
                        showTooltip(crslAutoScriptTooltip);
                    }
                }
            }
        }

        function crslDisplayScriptEditor() {
            if (!crslTextModeToggle.checked) {
                crslScriptEditorContainer.innerHTML = '';
                crslScriptEditorContainer.classList.add('hidden');
                crslUpdateButtons();
                return;
            }

            const slideCount = parseInt(crslSlidesOptions.querySelector('.selected').dataset.value);
            crslScriptEditorContainer.innerHTML = '';
            for (let i = 0; i < slideCount; i++) {
                const slideEl = document.createElement('div');
                slideEl.className = 'flex flex-col gap-1';
                slideEl.innerHTML = `
                    <label for="crsl-slide-text-${i}" class="text-sm font-semibold text-gray-700">Slide ${i + 1}</label>
                    <textarea id="crsl-slide-text-${i}" class="w-full p-2 text-sm rounded-lg border focus:ring-2 focus:outline-none" rows="2" placeholder="Klik 'Buat Script Otomatis' atau isi manual..."></textarea>
                `;
                crslScriptEditorContainer.appendChild(slideEl);
            }
            crslScriptEditorContainer.querySelectorAll('textarea').forEach(ta => ta.addEventListener('input', crslUpdateButtons));
            crslScriptEditorContainer.classList.remove('hidden');
            crslUpdateButtons();
        }

        crslTextModeToggle.addEventListener('change', () => {
            const isTextMode = crslTextModeToggle.checked;
            crslScriptSection.classList.toggle('hidden', !isTextMode);
            crslTextSettingsSection.classList.toggle('hidden', !isTextMode);
            if (isTextMode) {
                crslDisplayScriptEditor();
            }
            crslUpdateButtons();
        });

        setupOptionButtonsNew(crslPositionOptions);
        setupOptionButtonsNew(crslFontOptions);
        setupOptionButtonsNew(crslFontColorOptions);
        setupOptionButtonsNew(crslSlidesOptions);
        setupOptionButtonsNew(crslRatioOptions);
        setupOptionButtonsNew(crslStyleOptions);
        setupOptionButtonsNew(crslScriptTypeOptions);

        crslDisplayScriptEditor();

        setupImageUploadNew(crslImageInput, crslUploadBox, async (data) => {
            // Compress before storing — large product photos cause API timeout
            const MAX = 1024;
            const img = new Image();
            img.onload = async () => {
                let w = img.width, h = img.height;
                if (w > MAX || h > MAX) {
                    if (w > h) { h = Math.round(h * MAX / w); w = MAX; }
                    else       { w = Math.round(w * MAX / h); h = MAX; }
                }
                const canvas = document.createElement('canvas');
                canvas.width = w; canvas.height = h;
                canvas.getContext('2d').drawImage(img, 0, 0, w, h);
                const compressedDataUrl = canvas.toDataURL('image/jpeg', 0.85);
                crslImageData = { base64: compressedDataUrl.split(',')[1], mimeType: 'image/jpeg', dataUrl: compressedDataUrl };
                crslPreview.src = compressedDataUrl;
                crslPlaceholder.classList.add('hidden');
                crslPreview.classList.remove('hidden');
                crslRemoveBtn.classList.remove('hidden');
                crslDisplayScriptEditor();
                crslUpdateButtons();
                crslAutoColorPreviewContainer.style.display = 'flex';
                crslAutoColorPreview.style.backgroundColor = 'transparent';
                crslAutoFontColor = await analyzeImageForFontColor(crslImageData);
                crslAutoColorPreview.style.backgroundColor = crslAutoFontColor;
            };
            img.src = data.dataUrl;
        });

        crslRemoveBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            crslImageData = null;
            crslImageInput.value = '';
            crslNameInput.value = '';
            crslDescInput.value = '';
            crslPreview.src = '#';
            crslPreview.classList.add('hidden');
            crslPlaceholder.classList.remove('hidden');
            crslRemoveBtn.classList.add('hidden');
            crslScriptEditorContainer.innerHTML = '';
            crslScriptEditorContainer.classList.add('hidden');

            document.getElementById('crsl-results-placeholder').classList.remove('hidden');
            crslResultsContainer.classList.add('hidden');
            crslResultsGrid.innerHTML = '';

            crslUpdateButtons();
        });

        [crslNameInput, crslDescInput].forEach(el => el.addEventListener('input', crslUpdateButtons));

        crslSlidesOptions.addEventListener('click', () => {
            if(crslImageData) crslDisplayScriptEditor();
        });

        crslScriptTypeOptions.addEventListener('click', (e) => {
            const button = e.target.closest('button');
            if (button) {
                const isKustom = button.dataset.type === 'kustom';
                crslKustomScriptContainer.classList.toggle('hidden', !isKustom);
                if (isKustom) {
                    crslKustomScriptInput.focus();
                }
            }
            if(crslImageData) crslDisplayScriptEditor();
        });

        crslAutoBtn.addEventListener('click', async () => {
            if (!crslImageData) return;
            crslAutoNameTooltip.classList.remove('visible');
            const originalBtnHTML = crslAutoBtn.innerHTML;
            crslAutoBtn.disabled = true;
            crslAutoBtn.innerHTML = `<div class="loader-icon-umrah w-4 h-4"></div><span class="ml-2">Memproses...</span>`;
            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;
                const systemPrompt = `Analyze the product image. Respond with a JSON object containing two keys: "productName" (a suitable name for the product) and "shortDescription" (a brief, appealing description, max 15 words). Both must be in Indonesian.`;
                const payload = {
                    contents: [{ parts: [ { text: "Analyze this product." }, { inlineData: { mimeType: crslImageData.mimeType, data: crslImageData.base64 } } ] }],
                    systemInstruction: { parts: [{ text: systemPrompt }] },
                    generationConfig: { responseMimeType: "application/json" }
                };
                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) {
                    const errorText = await response.text();
                    throw new Error(`Error ${response.status}: ${errorText.substring(0, 200) || 'empty response'}`);
                }

                const result = await response.json();
                const textPart = result?.candidates?.[0]?.content?.parts?.[0]?.text;
                if (!textPart) throw new Error('Respons AI kosong. Coba lagi.');
                const data = JSON.parse(textPart);
                crslNameInput.value = data.productName || '';
                crslDescInput.value = data.shortDescription || '';
                crslUpdateButtons();
            } catch(e) {
                console.error("Failed to auto-generate details:", e);
                alert(`Gagal membuat detail otomatis: ${e.message}`);
            } finally {
                crslAutoBtn.innerHTML = originalBtnHTML;
                crslAutoBtn.disabled = false;
            }
        });

        crslAutoScriptBtn.addEventListener('click', async () => {
            if (!crslImageData || !crslNameInput.value || !crslDescInput.value) return;
            crslAutoScriptTooltip.classList.remove('visible');
            const originalBtnHTML = crslAutoScriptBtn.innerHTML;
            crslAutoScriptBtn.disabled = true;
            crslAutoScriptBtn.innerHTML = `<div class="loader-icon-umrah w-4 h-4"></div><span class="ml-2">Menulis...</span>`;

            const textareas = crslScriptEditorContainer.querySelectorAll('textarea');
            textareas.forEach(ta => {
                ta.value = "AI sedang menulis...";
                ta.disabled = true;
            });

            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const slideCount = parseInt(crslSlidesOptions.querySelector('.selected').dataset.value);
                let scriptType = crslScriptTypeOptions.querySelector('.selected').dataset.type;
                if (scriptType === 'kustom') {
                    scriptType = crslKustomScriptInput.value.trim() || 'Showcase';
                }
                const productName = crslNameInput.value;
                const productDesc = crslDescInput.value;

                const systemPrompt = `You are an expert social media copywriter. Generate a script for a product carousel with exactly ${slideCount} slides in Indonesian. The response MUST be a JSON array of strings, with each string representing the text for one slide.
CRITICAL: The text for each individual slide MUST NOT exceed 20 words.
The script must follow this structure:
1. The first slide MUST be a compelling hook to grab attention.
2. The middle ${slideCount - 2 > 0 ? slideCount - 2 : 0} slides are the main content, providing value.
3. The last slide MUST be a clear Call to Action (CTA).
The script's tone and content must be tailored to the chosen script type.`;

                const userQuery = `Product Name: ${productName}\nProduct Description: ${productDesc}\nScript Type: ${scriptType}\nNumber of Slides: ${slideCount}`;

                const payload = {
                    contents: [{ parts: [{ text: userQuery }] }],
                    systemInstruction: { parts: [{ text: systemPrompt }] },
                    generationConfig: {
                        responseMimeType: "application/json",
                        responseSchema: { type: "ARRAY", items: { "type": "STRING" } }
                    }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) {
                    const errorText = await response.text();
                    throw new Error(`Error ${response.status}: ${errorText.substring(0, 200) || 'empty response'}`);
                }

                const result = await response.json();
                const textPart = result?.candidates?.[0]?.content?.parts?.[0]?.text;
                if (!textPart) throw new Error('Respons AI kosong. Coba lagi.');
                const scriptArray = JSON.parse(textPart);

                if (scriptArray && scriptArray.length > 0) {
                    textareas.forEach((ta, index) => {
                        ta.value = scriptArray[index] || '';
                    });
                } else {
                    throw new Error("AI returned an empty or invalid script.");
                }
                crslUpdateButtons();
            } catch(e) {
                console.error("Failed to auto-generate script:", e);
                textareas.forEach(ta => ta.value = "Gagal membuat script. Coba lagi.");
                alert(`Gagal membuat script otomatis: ${e.message}`);
            } finally {
                crslAutoScriptBtn.innerHTML = originalBtnHTML;
                textareas.forEach(ta => ta.disabled = false);
                crslUpdateButtons();
            }
        });

        async function generateSingleCarouselSlide(slideNumber, slideText, style, aspectRatio, isTextMode) {
            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

            let finalPrompt = `Create a professional and visually appealing product marketing image for a carousel slide.
- Product: Use the provided image as the central product.
- Style: The overall aesthetic must be '${style}'.
- Task: Create a unique background or scene that showcases the product. For this slide (number ${slideNumber}), create a slightly different composition or background to ensure variety.`;

            if (isTextMode) {
                 finalPrompt += `
- CRITICAL: The composition must leave ample, clean space where text could be placed over it later. The main product must not be obstructed.
- ABSOLUTE RULE: DO NOT add any text, words, or numbers to the image. The image must be purely visual, as text will be added separately.`;
            } else {
                finalPrompt += `
- This is a visual-only slide. Create an engaging and dynamic shot of the product without any text overlay space consideration.`;
            }

            finalPrompt += `
- The final image must have an aspect ratio of ${aspectRatio}.`;

            const payload = {
                contents: [{ parts: [{ text: finalPrompt }, { inlineData: { mimeType: crslImageData.mimeType, data: crslImageData.base64 } }] }],
                generationConfig: {
                    responseModalities: ['TEXT', 'IMAGE'],
                    imageConfig: { aspectRatio: aspectRatio }
                }
            };

            const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
            if (!response.ok) {
                const errText = await response.text();
                throw new Error(`API error ${response.status}: ${errText.substring(0, 200)}`);
            }
            const result = await response.json();
            const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
            if (!base64Data) throw new Error("No image data from AI for this slide.");

            return { base64Data, prompt: finalPrompt };
        }

        function setupCarousel(carouselId) {
            const carousel = document.getElementById(carouselId);
            if (!carousel) return;

            const slides = carousel.querySelector('.carousel-slides');
            const dots = carousel.querySelectorAll('.carousel-dot');
            const totalSlides = slides.children.length;
            let currentIndex = 0;

            function goToSlide(index) {
                currentIndex = (index + totalSlides) % totalSlides;
                slides.style.transform = `translateX(-${currentIndex * 100}%)`;
                dots.forEach((dot, i) => dot.classList.toggle('active', i === currentIndex));
            }

            carousel.querySelector('.prev').addEventListener('click', () => goToSlide(currentIndex - 1));
            carousel.querySelector('.next').addEventListener('click', () => goToSlide(currentIndex + 1));
            dots.forEach(dot => dot.addEventListener('click', () => goToSlide(parseInt(dot.dataset.slideTo))));
        }

        function renderCarouselResult(cardId, images, style, aspectRatio) {
            const card = document.getElementById(cardId);
            if (!card) return;

            function getPositionClasses(position) {
                let classes = '';
                if (position.includes('top')) classes += 'items-start ';
                else if (position.includes('middle')) classes += 'items-center ';
                else classes += 'items-end ';
                classes += 'justify-center text-center';
                return classes;
            }

            const slidesHTML = images.map((img, slideIdx) => {
                const positionClass = getPositionClasses(img.position);
                const textOverlay = img.text ?
                    `<div class="absolute inset-0 flex p-6 md:p-8 lg:p-10 pointer-events-none ${positionClass}">
                        <p class="text-lg lg:text-xl font-bold max-w-[90%]" style="color: ${img.fontColor}; font-family: ${img.font}; text-shadow: 2px 2px 5px rgba(0,0,0,0.9);">${img.text}</p>
                    </div>` : '';

                return `<div class="carousel-slide relative bg-black">
                            <img src="${img.src}" alt="${img.title}" class="w-full h-full object-contain">
                            ${textOverlay}
                            <button class="crsl-edit-slide-btn absolute top-2 right-2 z-20 bg-black/60 hover:bg-pink-600 text-white px-2 py-1 rounded-lg text-xs flex items-center gap-1 transition-all duration-200"
                                    data-carousel-id="${cardId}"
                                    data-slide-index="${slideIdx}"
                                    title="Edit &amp; Generate Ulang Slide Ini">
                                <i class="fas fa-edit"></i><span>Edit</span>
                            </button>
                        </div>`;
            }).join('');

            const dotsHTML = images.map((_, i) => `<div class="carousel-dot ${i === 0 ? 'active' : ''}" data-slide-to="${i}"></div>`).join('');

            const carouselContainer = card.querySelector('.carousel-container');
            carouselContainer.innerHTML = `
                <div class="carousel-slides">${slidesHTML}</div>
                <button class="carousel-nav prev"><i class="fas fa-chevron-left"></i></button>
                <button class="carousel-nav next"><i class="fas fa-chevron-right"></i></button>
                <div class="carousel-dots">${dotsHTML}</div>
            `;
            setupCarousel(cardId);
        }

        crslGenerateBtn.addEventListener('click', async () => {
            const originalBtnHTML = crslGenerateBtn.innerHTML;
            crslGenerateBtn.disabled = true;
            crslGenerateBtn.innerHTML = `<div class="loader-icon-umrah !border-l-white w-5 h-5"></div><span class="ml-2">Membuat Carousel...</span>`;

            document.getElementById('crsl-results-placeholder').classList.add('hidden');

            const slideCount = parseInt(crslSlidesOptions.querySelector('.selected').dataset.value);
            const aspectRatio = crslRatioOptions.querySelector('.selected').dataset.value;
            const style = crslStyleOptions.querySelector('.selected').dataset.value;
            const isTextMode = crslTextModeToggle.checked;
            const position = crslPositionOptions.querySelector('.selected').dataset.value;
            const font = crslFontOptions.querySelector('.selected').dataset.value;
            const selectedFontColor = crslFontColorOptions.querySelector('.selected').dataset.value;
            const fontColor = selectedFontColor === 'auto' ? crslAutoFontColor : selectedFontColor;
            const productName = crslNameInput.value.trim();

            const aspectClass = getAspectRatioClassNew(aspectRatio);

            crslResultsContainer.classList.remove('hidden');
            crslResultsGrid.innerHTML = '';

            const cardId = `crsl-result-${Date.now()}`;
            const card = document.createElement('div');
            card.id = cardId;
            card.className = 'desktop-carousel-frame';
            card.innerHTML = `
                <div class="carousel-container ${aspectClass} bg-gray-200 flex items-center justify-center">
                    <div class="loader-icon-umrah w-12 h-12"></div>
                </div>
                <div class="mt-3 text-center text-sm text-gray-600">Sedang membuat ${slideCount} slide...</div>
            `;
            crslResultsGrid.appendChild(card);

            try {
                const textareas = isTextMode ? crslScriptEditorContainer.querySelectorAll('textarea') : [];
                const totalGenerate = slideCount * 3;

                // Fire all requests simultaneously (e.g. 5 slides → 15 requests at once)
                const allPromises = Array.from({ length: totalGenerate }, (_, i) => {
                    const slotIdx = i % slideCount;
                    const slideText = isTextMode ? textareas[slotIdx].value.trim() : '';
                    return generateSingleCarouselSlide(i + 1, slideText, style, aspectRatio, isTextMode)
                        .then(r => ({ ok: true, result: r, slotIdx, slideText }))
                        .catch(() => ({ ok: false }));
                });

                const allResults = await Promise.allSettled(allPromises);

                // For text mode: collect first success per slot, preserve text order
                // For visual mode: collect first slideCount successes in order
                const images = [];
                if (isTextMode) {
                    const slotWinner = {};
                    for (const r of allResults) {
                        if (r.status === 'fulfilled' && r.value.ok) {
                            const { result, slotIdx, slideText } = r.value;
                            if (!slotWinner[slotIdx]) slotWinner[slotIdx] = { result, slideText };
                        }
                    }
                    for (let i = 0; i < slideCount; i++) {
                        if (slotWinner[i]) {
                            images.push({
                                src: `data:image/png;base64,${slotWinner[i].result.base64Data}`,
                                title: `Slide ${images.length + 1}`,
                                text: slotWinner[i].slideText,
                                position, font, fontColor,
                                prompt: slotWinner[i].result.prompt
                            });
                        }
                    }
                } else {
                    for (const r of allResults) {
                        if (r.status === 'fulfilled' && r.value.ok && images.length < slideCount) {
                            images.push({
                                src: `data:image/png;base64,${r.value.result.base64Data}`,
                                title: `Slide ${images.length + 1}`,
                                text: '',
                                position, font, fontColor,
                                prompt: r.value.result.prompt
                            });
                        }
                    }
                }

                if (images.length === 0) throw new Error('Semua slide gagal. Coba generate ulang.');

                generatedCarousels[cardId] = { productName, images, style, aspectRatio };
                renderCarouselResult(cardId, images, style, aspectRatio);

                card.querySelector('.text-sm').innerHTML = `
                    <button class="crsl-open-download-btn font-semibold"
                            style="color: #EC4899;"
                            data-carousel-id="${cardId}"
                            data-action="download">
                        <i class="fas fa-download mr-1"></i> Unduh Semua Slide
                    </button>
                `;

            } catch (error) {
                console.error("Error generating carousel:", error);
                card.innerHTML = `<div class="text-xs text-red-500 p-4 text-center">Gagal: ${error.message}</div>`;
            } finally {
                crslGenerateBtn.disabled = false;
                crslGenerateBtn.innerHTML = originalBtnHTML;
            }
        });

        // Function to merge text overlay onto image using Canvas
        async function mergeTextToImage(imageData) {
            return new Promise((resolve, reject) => {
                const img = new Image();
                img.crossOrigin = 'anonymous';

                img.onload = () => {
                    const canvas = document.createElement('canvas');
                    const ctx = canvas.getContext('2d');

                    canvas.width = img.width;
                    canvas.height = img.height;

                    // Draw the base image
                    ctx.drawImage(img, 0, 0);

                    // Add text overlay if exists
                    if (imageData.text && imageData.text.trim()) {
                        // Calculate position
                        let y;
                        const padding = canvas.height * 0.08; // 8% padding

                        if (imageData.position.includes('top')) {
                            y = padding;
                        } else if (imageData.position.includes('middle')) {
                            y = canvas.height / 2;
                        } else {
                            y = canvas.height - padding;
                        }

                        // Set font
                        const fontSize = Math.floor(canvas.width * 0.05); // 5% of width
                        ctx.font = `bold ${fontSize}px ${imageData.font || 'Arial'}`;
                        ctx.fillStyle = imageData.fontColor || '#FFFFFF';
                        ctx.textAlign = 'center';
                        ctx.textBaseline = imageData.position.includes('top') ? 'top' :
                                          imageData.position.includes('middle') ? 'middle' : 'bottom';

                        // Add text shadow for better readability
                        ctx.shadowColor = 'rgba(0, 0, 0, 0.9)';
                        ctx.shadowBlur = 5;
                        ctx.shadowOffsetX = 2;
                        ctx.shadowOffsetY = 2;

                        // Word wrap text
                        const maxWidth = canvas.width * 0.9; // 90% of canvas width
                        const words = imageData.text.split(' ');
                        let line = '';
                        let lines = [];

                        for (let n = 0; n < words.length; n++) {
                            const testLine = line + words[n] + ' ';
                            const metrics = ctx.measureText(testLine);
                            const testWidth = metrics.width;

                            if (testWidth > maxWidth && n > 0) {
                                lines.push(line);
                                line = words[n] + ' ';
                            } else {
                                line = testLine;
                            }
                        }
                        lines.push(line);

                        // Draw each line
                        const lineHeight = fontSize * 1.2;
                        const totalHeight = lines.length * lineHeight;
                        let startY = y;

                        if (imageData.position.includes('middle')) {
                            startY = y - (totalHeight / 2) + (lineHeight / 2);
                        }

                        lines.forEach((line, index) => {
                            ctx.fillText(line.trim(), canvas.width / 2, startY + (index * lineHeight));
                        });
                    }

                    // Convert canvas to data URL
                    const finalImageUrl = canvas.toDataURL('image/png');
                    resolve(finalImageUrl);
                };

                img.onerror = () => reject(new Error('Failed to load image'));
                img.src = imageData.src;
            });
        }

        document.body.addEventListener('click', async (e) => {
            const openDownloadBtn = e.target.closest('.crsl-open-download-btn');
            if (openDownloadBtn) {
                e.stopPropagation();
                const carouselId = openDownloadBtn.dataset.carouselId;
                const carouselData = generatedCarousels[carouselId];
                if (!carouselData) return;

                // Download all carousel slides
                openDownloadBtn.disabled = true;
                openDownloadBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-1"></i> Mengunduh...';

                try {
                    for (let i = 0; i < carouselData.images.length; i++) {
                        const img = carouselData.images[i];
                        const filename = `carousel_${carouselData.productName}_slide_${i + 1}.png`.replace(/[^a-z0-9_]/gi, '_');

                        // Merge text to image before download
                        const mergedImageUrl = await mergeTextToImage(img);

                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(mergedImageUrl, filename);
                        } else if (window.downloadImage) {
                            await window.downloadImage(mergedImageUrl, filename);
                        }

                        // Add delay between downloads to prevent browser blocking
                        await new Promise(resolve => setTimeout(resolve, 300));
                    }

                    openDownloadBtn.disabled = false;
                    openDownloadBtn.innerHTML = '<i class="fas fa-check mr-1"></i> Selesai!';
                    setTimeout(() => {
                        openDownloadBtn.innerHTML = '<i class="fas fa-download mr-1"></i> Unduh Semua Slide';
                    }, 2000);
                } catch (error) {
                    console.error('Download error:', error);
                    openDownloadBtn.disabled = false;
                    openDownloadBtn.innerHTML = '<i class="fas fa-times mr-1"></i> Gagal!';
                    setTimeout(() => {
                        openDownloadBtn.innerHTML = '<i class="fas fa-download mr-1"></i> Unduh Semua Slide';
                    }, 2000);
                }
            }
        });

        // Edit Slide button delegation
        if (crslResultsGrid) {
            crslResultsGrid.addEventListener('click', (e) => {
                const editBtn = e.target.closest('.crsl-edit-slide-btn');
                if (!editBtn) return;
                e.stopPropagation();
                const cid = editBtn.dataset.carouselId;
                const sidx = parseInt(editBtn.dataset.slideIndex);
                const cdata = generatedCarousels[cid];
                if (!cdata) return;
                showCarouselEditModal(cid, sidx, cdata.images[sidx].prompt || '');
            });
        }

        function showCarouselEditModal(carouselId, slideIndex, currentPrompt) {
            let modal = document.getElementById('crsl-edit-modal');
            if (!modal) {
                modal = document.createElement('div');
                modal.id = 'crsl-edit-modal';
                modal.className = 'fixed inset-0 z-[9999] flex items-center justify-center bg-black/60 p-4';
                modal.innerHTML = `
                    <div class="bg-white rounded-2xl shadow-2xl w-full max-w-2xl max-h-[90vh] flex flex-col">
                        <div class="flex justify-between items-center p-5 border-b">
                            <h3 class="text-xl font-bold text-gray-800"><i class="fas fa-edit mr-2 text-pink-500"></i>Edit Prompt Slide</h3>
                            <button id="crsl-edit-modal-close" class="text-gray-400 hover:text-gray-600 text-2xl font-bold leading-none">&times;</button>
                        </div>
                        <div class="p-5 flex-1 overflow-y-auto">
                            <p class="text-sm text-gray-500 mb-3" id="crsl-edit-slide-label"></p>
                            <label class="block text-sm font-semibold text-gray-700 mb-2">Prompt AI (edit sesuai kebutuhan):</label>
                            <textarea id="crsl-edit-prompt-textarea" class="w-full h-56 p-3 border-2 border-pink-200 rounded-xl text-sm focus:border-pink-500 resize-y" placeholder="Edit prompt di sini..."></textarea>
                        </div>
                        <div class="p-5 border-t flex gap-3 justify-end">
                            <button id="crsl-edit-modal-cancel" class="px-5 py-2.5 bg-gray-100 text-gray-700 rounded-xl font-semibold hover:bg-gray-200 transition-all">Batal</button>
                            <button id="crsl-edit-modal-regen" class="px-5 py-2.5 bg-gradient-to-r from-pink-500 to-rose-500 text-white rounded-xl font-semibold hover:from-pink-600 hover:to-rose-600 transition-all flex items-center gap-2">
                                <i class="fas fa-redo"></i> Generate Ulang Slide
                            </button>
                        </div>
                    </div>`;
                document.body.appendChild(modal);
                document.getElementById('crsl-edit-modal-close').addEventListener('click', () => modal.classList.add('hidden'));
                document.getElementById('crsl-edit-modal-cancel').addEventListener('click', () => modal.classList.add('hidden'));
            }

            const cdata = generatedCarousels[carouselId];
            document.getElementById('crsl-edit-slide-label').textContent =
                (cdata?.productName ? cdata.productName + ' \u2014 ' : '') + 'Slide ' + (slideIndex + 1);
            document.getElementById('crsl-edit-prompt-textarea').value = currentPrompt;
            modal.classList.remove('hidden');

            const oldBtn = document.getElementById('crsl-edit-modal-regen');
            const newBtn = oldBtn.cloneNode(true);
            oldBtn.parentNode.replaceChild(newBtn, oldBtn);
            newBtn.addEventListener('click', async () => {
                const newPrompt = document.getElementById('crsl-edit-prompt-textarea').value.trim();
                if (!newPrompt) return;
                modal.classList.add('hidden');
                await regenerateSingleCarouselSlide(carouselId, slideIndex, newPrompt);
            });
        }

        async function regenerateSingleCarouselSlide(carouselId, slideIndex, newPrompt) {
            const cdata = generatedCarousels[carouselId];
            if (!cdata) return;

            const card = document.getElementById(carouselId);
            if (!card) return;

            // Show loading on the slide
            const slides = card.querySelectorAll('.carousel-slide');
            if (slides[slideIndex]) {
                slides[slideIndex].innerHTML = `
                    <div class="w-full h-full flex items-center justify-center bg-gray-900 min-h-[200px]">
                        <div class="text-center">
                            <div class="animate-spin rounded-full h-10 w-10 border-b-2 border-pink-500 mx-auto mb-2"></div>
                            <p class="text-white text-xs">Generating slide ${slideIndex + 1}...</p>
                        </div>
                    </div>`;
            }

            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;
                const payload = {
                    contents: [{ parts: [
                        { text: newPrompt },
                        { inlineData: { mimeType: crslImageData.mimeType, data: crslImageData.base64 } }
                    ]}],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: cdata.aspectRatio || '1:1' }
                    }
                };

                const response = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                if (!response.ok) throw new Error('Error ' + response.status);
                const result = await response.json();
                const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!base64Data) throw new Error('No image data');

                const imageUrl = `data:image/png;base64,${base64Data}`;
                cdata.images[slideIndex].src = imageUrl;
                cdata.images[slideIndex].prompt = newPrompt;

                renderCarouselResult(carouselId, cdata.images, cdata.style, cdata.aspectRatio);

            } catch (error) {
                console.error('Regen carousel slide error:', error);
                renderCarouselResult(carouselId, cdata.images, cdata.style, cdata.aspectRatio);
                alert('Gagal generate ulang slide: ' + error.message);
            }
        }

    })();

    // ==================== LOGO GENERATOR ====================
    (() => {
        const logoBrandName = document.getElementById('logo-brand-name');
        const logoTagline = document.getElementById('logo-tagline');
        const logoBusinessDesc = document.getElementById('logo-business-desc');
        const logoIndustryOptions = document.getElementById('logo-industry-options');
        const logoTypeOptions = document.getElementById('logo-type-options');
        const logoStyleOptions = document.getElementById('logo-style-options');
        const logoColorSchemeOptions = document.getElementById('logo-color-scheme-options');
        const logoColorPaletteOptions = document.getElementById('logo-color-palette-options');
        const logoRefInput = document.getElementById('logo-ref-input');
        const logoRefUploadBox = document.getElementById('logo-ref-upload-box');
        const logoRefPreview = document.getElementById('logo-ref-preview');
        const logoRefPlaceholder = document.getElementById('logo-ref-placeholder');
        const logoRefRemoveBtn = document.getElementById('logo-ref-remove-btn');
        const logoGenerateBtn = document.getElementById('logo-generate-btn');
        const logoResultsPlaceholder = document.getElementById('logo-results-placeholder');
        const logoResultsContainer = document.getElementById('logo-results-container');
        const logoResultsGrid = document.getElementById('logo-results-grid');
        const logoDownloadAllBtn = document.getElementById('logo-download-all-btn');

        if (!logoBrandName) return;

        const apiKey = "";
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let logoRefImageData = null;
        let generatedLogos = [];

        // Count selection (1-10), default 4
        const logoCountSel = document.getElementById('logo-count-selection-grid');
        if (logoCountSel) {
            logoCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                logoCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Setup option buttons
        setupOptionButtonsNew(logoIndustryOptions);
        setupOptionButtonsNew(logoTypeOptions);
        setupOptionButtonsNew(logoColorSchemeOptions);

        // Setup checkbox buttons for styles (multi-select)
        if (logoStyleOptions) {
            logoStyleOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('.checkbox-btn-logo');
                if (btn) {
                    btn.classList.toggle('active');
                    updateLogoGenerateButton();
                }
            });
        }

        // Setup color palette selection
        if (logoColorPaletteOptions) {
            logoColorPaletteOptions.addEventListener('click', (e) => {
                const chip = e.target.closest('.logo-color-chip');
                if (chip) {
                    // Toggle selection
                    const wasSelected = chip.classList.contains('selected');
                    logoColorPaletteOptions.querySelectorAll('.logo-color-chip').forEach(c => c.classList.remove('selected'));
                    if (!wasSelected) {
                        chip.classList.add('selected');
                    }
                }
            });
        }

        // Reference image upload
        setupImageUploadNew(logoRefInput, logoRefUploadBox, (data) => {
            logoRefImageData = data;
            logoRefPreview.src = data.dataUrl;
            logoRefPlaceholder.classList.add('hidden');
            logoRefPreview.classList.remove('hidden');
            logoRefRemoveBtn.classList.remove('hidden');
        });

        if (logoRefRemoveBtn) {
            logoRefRemoveBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                logoRefImageData = null;
                logoRefInput.value = '';
                logoRefPreview.src = '#';
                logoRefPreview.classList.add('hidden');
                logoRefPlaceholder.classList.remove('hidden');
                logoRefRemoveBtn.classList.add('hidden');
            });
        }

        // Update generate button state
        function updateLogoGenerateButton() {
            const hasName = logoBrandName.value.trim() !== '';
            const hasDesc = logoBusinessDesc.value.trim() !== '';
            logoGenerateBtn.disabled = !hasName || !hasDesc;
        }

        [logoBrandName, logoBusinessDesc].forEach(el => {
            if (el) el.addEventListener('input', updateLogoGenerateButton);
        });

        // Generate logos
        if (logoGenerateBtn) {
            logoGenerateBtn.addEventListener('click', async () => {
                const brandName = logoBrandName.value.trim();
                const tagline = logoTagline.value.trim();
                const businessDesc = logoBusinessDesc.value.trim();

                if (!brandName || !businessDesc) {
                    alert('Mohon isi Nama Brand dan Deskripsi Bisnis!');
                    return;
                }

                // Get selected options
                const industry = logoIndustryOptions.querySelector('.selected')?.dataset.value || 'General Business';
                const logoType = logoTypeOptions.querySelector('.selected')?.dataset.value || 'combination';
                const colorScheme = logoColorSchemeOptions.querySelector('.selected')?.dataset.value || 'colorful';

                // Get selected styles (multi-select)
                const selectedStyles = Array.from(logoStyleOptions.querySelectorAll('.active')).map(btn => btn.dataset.value);
                const styleString = selectedStyles.length > 0 ? selectedStyles.join(', ') : 'Modern Professional';

                // Get selected color palette
                const selectedColorChip = logoColorPaletteOptions.querySelector('.logo-color-chip.selected');
                const selectedColors = selectedColorChip ? selectedColorChip.dataset.colors : null;

                const originalBtnHTML = logoGenerateBtn.innerHTML;
                logoGenerateBtn.disabled = true;
                logoGenerateBtn.innerHTML = `<div class="loader-icon-logo mr-2"></div><span>Generating Logos...</span>`;

                generatedLogos = new Array(selectedCount).fill(null);
                if (logoDownloadAllBtn) logoDownloadAllBtn.classList.add('hidden');
                logoResultsPlaceholder.classList.add('hidden');
                logoResultsContainer.classList.remove('hidden');

                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedLogos = new Array(selectedCount).fill(null);
                    logoResultsGrid.innerHTML = '';
                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `logo-card-${i}`;
                        card.className = 'logo-preview-card p-4';
                        card.innerHTML = `
                            <div class="aspect-square bg-gray-100 rounded-lg flex items-center justify-center relative">
                                <div class="loader-icon-logo"></div>
                                <span class="logo-variation-badge">V${i}</span>
                            </div>
                        `;
                        logoResultsGrid.appendChild(card);
                    }
                    await Promise.allSettled(
                        Array.from({ length: selectedCount }, (_, i) =>
                            generateSingleLogoCard(i + 1, brandName, tagline, businessDesc, industry, logoType, styleString, colorScheme, selectedColors)
                        )
                    );
                    successCount = generatedLogos.filter(Boolean).length;
                }

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    logoResultsPlaceholder.classList.remove('hidden');
                    logoResultsContainer.classList.add('hidden');
                    logoGenerateBtn.disabled = false;
                    logoGenerateBtn.innerHTML = originalBtnHTML;
                    return;
                }

                document.querySelectorAll('[id^="logo-card-"]').forEach(card => { if (!card.querySelector('img')) card.remove(); });
                generatedLogos = generatedLogos.filter(Boolean);
                if (generatedLogos.length > 0 && logoDownloadAllBtn) logoDownloadAllBtn.classList.remove('hidden');
                const sparkleEmojiSuccess = '\u2728';
                showModal('Logo Generator', `${sparkleEmojiSuccess} Berhasil! ${brandName} memiliki ${generatedLogos.length} variasi logo profesional. Download logo favorit Anda!`, 'success');
                logoGenerateBtn.disabled = false;
                logoGenerateBtn.innerHTML = originalBtnHTML;
            });
        }

        // Build prompt for logo generation
        function buildLogoPrompt(brandName, tagline, businessDesc, industry, logoType, style, colorScheme, colors, variationNumber) {
            let prompt = `Create a professional logo design for "${brandName}".\n\n`;

            prompt += `BUSINESS CONTEXT:\n`;
            prompt += `- Industry: ${industry}\n`;
            prompt += `- Description: ${businessDesc}\n`;
            if (tagline) prompt += `- Tagline: "${tagline}"\n`;

            prompt += `\nLOGO SPECIFICATIONS:\n`;
            prompt += `- Logo Type: ${logoType.charAt(0).toUpperCase() + logoType.slice(1)}\n`;

            if (logoType === 'wordmark') {
                prompt += `  * Create a text-only logo using the brand name "${brandName}"\n`;
                prompt += `  * Focus on unique typography and letterforms\n`;
                prompt += `  * The logo must be purely typographic without any icons or symbols\n`;
            } else if (logoType === 'symbol') {
                prompt += `  * Create an icon/symbol-only logo without any text\n`;
                prompt += `  * The symbol should represent the brand essence\n`;
                prompt += `  * Must be recognizable and work at small sizes\n`;
            } else if (logoType === 'combination') {
                prompt += `  * Combine both "${brandName}" text and a symbolic icon\n`;
                prompt += `  * Balance the text and icon harmoniously\n`;
                prompt += `  * Create a cohesive design that works together\n`;
            } else if (logoType === 'emblem') {
                prompt += `  * Create a badge/emblem style logo\n`;
                prompt += `  * Place "${brandName}" within a shape or frame\n`;
                prompt += `  * Design should have a classic, trustworthy feel\n`;
            }

            prompt += `\nDESIGN STYLE:\n`;
            prompt += `- Style: ${style}\n`;
            prompt += `- Color Scheme: ${colorScheme}\n`;

            if (colors) {
                prompt += `- REQUIRED COLOR PALETTE: Use these specific colors: ${colors}\n`;
                prompt += `- CRITICAL: The logo MUST incorporate these colors prominently\n`;
            } else {
                prompt += `- Choose colors that best represent ${industry} and convey ${style}\n`;
            }

            prompt += `\nVARIATION ${variationNumber} REQUIREMENTS:\n`;
            if (variationNumber === 1) prompt += `- Create a bold, impactful version with strong visual weight\n`;
            else if (variationNumber === 2) prompt += `- Create a minimalist, clean version with lots of negative space\n`;
            else if (variationNumber === 3) prompt += `- Create a dynamic version with movement or energy\n`;
            else if (variationNumber === 4) prompt += `- Create an elegant, refined version with sophisticated details\n`;
            else if (variationNumber === 5) prompt += `- Create a geometric version using shapes and structured forms\n`;
            else if (variationNumber === 6) prompt += `- Create a creative version pushing boundaries while staying professional\n`;
            else prompt += `- Create a unique variation with a distinctive layout or composition approach\n`;

            prompt += `\nTECHNICAL REQUIREMENTS:\n`;
            prompt += `- Design on a clean white background\n`;
            prompt += `- Ensure the logo is centered and prominent\n`;
            prompt += `- Logo should be scalable and work at any size\n`;
            prompt += `- Avoid any decorative borders, frames, or backgrounds unless it's part of the emblem design\n`;
            prompt += `- CRITICAL: Do NOT add any mockups, devices, or presentation elements\n`;
            prompt += `- Output only the logo itself\n\n`;

            prompt += `Create a unique, memorable, and professional logo that will make ${brandName} stand out in the ${industry} industry.`;

            return prompt;
        }

        async function generateSingleLogoCard(index, brandName, tagline, businessDesc, industry, logoType, styleString, colorScheme, selectedColors) {
            const card = document.getElementById(`logo-card-${index}`);
            try {
                const variationPrompt = buildLogoPrompt(brandName, tagline, businessDesc, industry, logoType, styleString, colorScheme, selectedColors, index);
                const logoImage = await generateSingleLogo(variationPrompt, logoRefImageData);
                const filename = `${brandName.replace(/\s+/g, '_')}_logo_v${index}.png`;
                generatedLogos[index - 1] = { url: logoImage, filename };
                if (card) card.innerHTML = `
                    <div class="aspect-square bg-white rounded-lg flex items-center justify-center relative overflow-hidden border-2 border-gray-200">
                        <img src="${logoImage}" alt="Logo Variation ${index}" class="w-full h-full object-contain p-4">
                        <span class="logo-variation-badge">V${index}</span>
                    </div>
                    <div class="mt-3 flex gap-2">
                        <button class="logo-action-btn flex-1 text-sm" data-action="logo-preview" data-image="${logoImage}">
                            <i class="fas fa-eye mr-1"></i> Preview
                        </button>
                        <button class="logo-action-btn flex-1 text-sm" data-action="logo-download" data-image="${logoImage}" data-filename="${filename}">
                            <i class="fas fa-download mr-1"></i> Download
                        </button>
                    </div>
                    <p class="text-xs text-center text-gray-500 mt-2">Style: ${styleString.split(',')[0]}</p>
                `;
            } catch (err) {
                console.error(`Error generating logo ${index}:`, err);
                if (card) card.innerHTML = '';
            }
        }

        // Generate single logo
        async function generateSingleLogo(prompt, referenceImageData) {
            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

            const parts = [{ text: prompt }];

            if (referenceImageData) {
                parts.push({
                    inlineData: {
                        mimeType: referenceImageData.mimeType,
                        data: referenceImageData.base64
                    }
                });
                parts[0].text += `\n\nA reference logo image is provided. Draw inspiration from its style, composition, and aesthetic qualities, but create a completely original logo for "${logoBrandName.value}". Do not copy or replicate the reference.`;
            }

            const payload = {
                contents: [{ parts }],
                generationConfig: {
                    responseModalities: ['TEXT', 'IMAGE'],
                    imageConfig: { aspectRatio: '1:1' }
                }
            };

            const response = await fetch(apiUrl, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            });

            const result = await response.json();
            const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

            if (!base64Data) {
                throw new Error('No logo image data received from API');
            }

            return `data:image/png;base64,${base64Data}`;
        }

        // Event delegation for logo action buttons
        if (logoResultsGrid) {
            logoResultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action^="logo-"]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const imageUrl = actionBtn.dataset.image;

                if (action === 'logo-preview') {
                    // Use preview modal for better mobile UX
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageUrl);
                    } else {
                        // Fallback to new tab
                        window.open(imageUrl, '_blank');
                    }
                } else if (action === 'logo-download') {
                    const filename = actionBtn.dataset.filename;
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageUrl, filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageUrl, filename);
                    }
                }
            });
        }

        // Download All button
        if (logoDownloadAllBtn) {
            logoDownloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedLogos.length === 0) return;

                logoDownloadAllBtn.disabled = true;
                const originalHTML = logoDownloadAllBtn.innerHTML;
                logoDownloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading...</span>';

                for (let i = 0; i < generatedLogos.length; i++) {
                    const logo = generatedLogos[i];
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(logo.url, logo.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(logo.url, logo.filename);
                    }
                    // Add delay between downloads for better UX
                    await new Promise(resolve => setTimeout(resolve, 500));
                }

                logoDownloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                setTimeout(() => {
                    logoDownloadAllBtn.disabled = false;
                    logoDownloadAllBtn.innerHTML = originalHTML;
                }, 2000);
            });
        }

        // Helper function to show modal
        function showModal(title, message, type = 'info') {
            const modal = document.createElement('div');
            modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';

            const bgColor = type === 'success' ? 'bg-green-50' : type === 'error' ? 'bg-red-50' : 'bg-purple-50';
            const iconColor = type === 'success' ? 'text-green-600' : type === 'error' ? 'text-red-600' : 'text-purple-600';
            const icon = type === 'success' ? 'fa-check-circle' : type === 'error' ? 'fa-exclamation-circle' : 'fa-info-circle';

            modal.innerHTML = `
                <div class="${bgColor} rounded-2xl p-6 max-w-md w-full shadow-2xl">
                    <div class="text-center">
                        <i class="fas ${icon} ${iconColor} text-5xl mb-4"></i>
                        <h3 class="text-2xl font-bold text-gray-800 mb-3">${title}</h3>
                        <p class="text-gray-600 mb-6">${message}</p>
                        <button onclick="this.closest('.fixed').remove()" class="logo-action-btn">
                            <i class="fas fa-times mr-2"></i> Tutup
                        </button>
                    </div>
                </div>
            `;

            document.body.appendChild(modal);
            modal.addEventListener('click', (e) => {
                if (e.target === modal) modal.remove();
            });
        }

    })();

    // ==================== AI BRAND KIT GENERATOR ====================
    (() => {
        // Element References
        const form = document.getElementById('brandkit-form');
        const brandName = document.getElementById('brandkit-name');
        const brandDescription = document.getElementById('brandkit-description');
        const industryOptions = document.getElementById('brandkit-industry-options');
        const audienceOptions = document.getElementById('brandkit-audience-options');
        const personalityOptions = document.getElementById('brandkit-personality-options');
        const colorOptions = document.getElementById('brandkit-color-options');
        const generateBtn = document.getElementById('brandkit-generate-btn');

        // Count selection (1-10), default 4
        let selectedCount = 4;
        const brandkitCountSel = document.getElementById('brandkit-count-selection-grid');
        if (brandkitCountSel) {
            brandkitCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                brandkitCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        const placeholder = document.getElementById('brandkit-placeholder');
        const loading = document.getElementById('brandkit-loading');
        const loadingText = document.getElementById('brandkit-loading-text');
        const loadingDetail = document.getElementById('brandkit-loading-detail');
        const progressBar = document.getElementById('brandkit-progress-bar');
        const progressText = document.getElementById('brandkit-progress-text');

        const resultsTabs = document.getElementById('brandkit-results-tabs');
        const resultsContent = document.getElementById('brandkit-results-content');
        const downloadSection = document.getElementById('brandkit-download-section');
        const downloadAllBtn = document.getElementById('brandkit-download-all-btn');

        const logosGrid = document.getElementById('brandkit-logos-grid');
        const colorsContent = document.getElementById('brandkit-colors-content');
        const typographyContent = document.getElementById('brandkit-typography-content');
        const voiceContent = document.getElementById('brandkit-voice-content');
        const visualContent = document.getElementById('brandkit-visual-content');

        if (!form) return;

        const apiKey = ""; // Injected by Canvas
        let selectedIndustry = '';
        let selectedAudiences = [];
        let selectedPersonalities = [];
        let selectedColors = [];
        let generatedLogos = [];
        let brandKitData = {};

        // Setup Option Buttons (Single Selection)
        function setupSingleSelect(container) {
            if (!container) return;
            container.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                container.querySelectorAll('.option-btn-brandkit').forEach(b =>
                    b.classList.remove('selected')
                );
                btn.classList.add('selected');

                if (container === industryOptions) {
                    selectedIndustry = btn.dataset.value;
                }

                validateForm();
            });
        }

        // Setup Checkbox Buttons (Multiple Selection)
        function setupMultiSelect(container, array) {
            if (!container) return;
            container.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                btn.classList.toggle('active');
                const checkIcon = btn.querySelector('.fa-check');
                if (checkIcon) {
                    checkIcon.classList.toggle('hidden');
                }

                const value = btn.dataset.value;

                if (btn.classList.contains('active')) {
                    if (!array.includes(value)) array.push(value);
                } else {
                    const index = array.indexOf(value);
                    if (index > -1) array.splice(index, 1);
                }

                // Update personality counter
                if (container === personalityOptions) {
                    const countSpan = document.getElementById('brandkit-personality-count');
                    const selectedSpan = document.getElementById('brandkit-personality-selected');
                    const numSpan = document.getElementById('brandkit-personality-num');

                    if (numSpan) numSpan.textContent = array.length;

                    if (array.length >= 3) {
                        if (countSpan) countSpan.classList.add('hidden');
                        if (selectedSpan) selectedSpan.classList.remove('hidden');
                    } else {
                        if (countSpan) countSpan.classList.remove('hidden');
                        if (selectedSpan) selectedSpan.classList.add('hidden');
                    }
                }

                validateForm();
            });
        }

        // Setup all option buttons
        setupSingleSelect(industryOptions);
        setupMultiSelect(audienceOptions, selectedAudiences);
        setupMultiSelect(personalityOptions, selectedPersonalities);
        setupMultiSelect(colorOptions, selectedColors);

        // Setup Results Tabs
        const tabButtons = document.querySelectorAll('.brandkit-tab-btn');
        const contentSections = document.querySelectorAll('.brandkit-content-section');

        tabButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                const tabName = btn.dataset.tab;

                // Update active tab
                tabButtons.forEach(b => b.classList.remove('active'));
                btn.classList.add('active');

                // Show corresponding section
                contentSections.forEach(section => {
                    if (section.id === `brandkit-section-${tabName}`) {
                        section.classList.remove('hidden');
                    } else {
                        section.classList.add('hidden');
                    }
                });
            });
        });

        // Form Validation
        function validateForm() {
            const isValid = brandName.value.trim() !== '' &&
                           brandDescription.value.trim() !== '' &&
                           selectedIndustry !== '' &&
                           selectedPersonalities.length >= 3;

            generateBtn.disabled = !isValid;
            if (isValid) {
                generateBtn.style.opacity = '1';
                generateBtn.style.cursor = 'pointer';
            } else {
                generateBtn.style.opacity = '0.5';
                generateBtn.style.cursor = 'not-allowed';
            }
        }

        brandName.addEventListener('input', validateForm);
        brandDescription.addEventListener('input', validateForm);

        // Update Progress
        function updateProgress(step, totalSteps, message, detail) {
            const percent = Math.round((step / totalSteps) * 100);
            progressBar.style.width = `${percent}%`;
            progressText.textContent = `${percent}% complete`;
            loadingText.textContent = message;
            loadingDetail.textContent = detail;
        }

        // Generate Color Palette (Smart Algorithm)
        function generateColorPalette(brandData) {
            // Base colors from user selection or auto-generate
            const baseColors = {
                'Blue': { primary: '#3b82f6', secondary: '#60a5fa', accent: '#1d4ed8' },
                'Red': { primary: '#ef4444', secondary: '#f87171', accent: '#b91c1c' },
                'Green': { primary: '#10b981', secondary: '#34d399', accent: '#047857' },
                'Purple': { primary: '#8b5cf6', secondary: '#a78bfa', accent: '#6d28d9' },
                'Orange': { primary: '#f97316', secondary: '#fb923c', accent: '#c2410c' },
                'Yellow': { primary: '#eab308', secondary: '#fbbf24', accent: '#a16207' },
                'Pink': { primary: '#ec4899', secondary: '#f472b6', accent: '#be185d' },
                'Teal': { primary: '#14b8a6', secondary: '#2dd4bf', accent: '#0f766e' },
                'Black/Gray': { primary: '#1f2937', secondary: '#4b5563', accent: '#111827' }
            };

            let palette;
            if (selectedColors.length > 0) {
                const firstColor = selectedColors[0].split(' ')[0];
                palette = baseColors[firstColor] || baseColors['Blue'];
            } else {
                // Auto-generate based on industry
                const industryColors = {
                    'Technology': baseColors['Blue'],
                    'Food': baseColors['Orange'],
                    'Fashion': baseColors['Pink'],
                    'Health': baseColors['Green'],
                    'Finance': baseColors['Blue'],
                    'Creative': baseColors['Purple']
                };
                const key = Object.keys(industryColors).find(k => selectedIndustry.includes(k));
                palette = industryColors[key] || baseColors['Blue'];
            }

            return {
                primary: palette.primary,
                secondary: palette.secondary,
                accent: palette.accent,
                neutral: ['#f8fafc', '#e2e8f0', '#cbd5e1', '#94a3b8', '#64748b'],
                gradients: [
                    `linear-gradient(135deg, ${palette.primary}, ${palette.secondary})`,
                    `linear-gradient(135deg, ${palette.secondary}, ${palette.accent})`
                ]
            };
        }

        // Generate Typography Recommendations
        function generateTypography(brandData) {
            const typeSystems = {
                'Modern': { heading: 'Inter', body: 'Roboto', style: 'Clean, geometric sans-serif' },
                'Elegant': { heading: 'Playfair Display', body: 'Lora', style: 'Sophisticated serif' },
                'Playful': { heading: 'Poppins', body: 'Nunito', style: 'Rounded, friendly' },
                'Bold': { heading: 'Montserrat', body: 'Open Sans', style: 'Strong, impactful' },
                'Minimalist': { heading: 'Helvetica Neue', body: 'Arial', style: 'Simple, timeless' },
                'Corporate': { heading: 'Roboto', body: 'Source Sans Pro', style: 'Professional, neutral' }
            };

            const personality = selectedPersonalities[0] || 'Modern';
            const key = Object.keys(typeSystems).find(k => personality.includes(k)) || 'Modern';
            const system = typeSystems[key];

            return {
                heading: {
                    font: system.heading,
                    weights: [700, 600, 500],
                    sizes: ['48px', '36px', '24px', '20px']
                },
                body: {
                    font: system.body,
                    weights: [400, 500],
                    sizes: ['16px', '14px', '12px']
                },
                description: system.style,
                hierarchy: 'H1: Bold 48px | H2: Semibold 36px | H3: Medium 24px | Body: Regular 16px'
            };
        }

        // Generate Brand Voice Guidelines
        function generateBrandVoice(brandData) {
            const tones = {
                'Modern': 'innovative, forward-thinking, tech-savvy',
                'Elegant': 'sophisticated, refined, premium',
                'Playful': 'fun, energetic, approachable',
                'Professional': 'trustworthy, reliable, expert',
                'Friendly': 'warm, welcoming, conversational',
                'Bold': 'confident, daring, powerful'
            };

            const personality = selectedPersonalities.join(', ');
            const tone = tones[Object.keys(tones).find(k => personality.includes(k))] || 'professional, innovative';

            return {
                tone: tone,
                dos: [
                    `Use ${tone} language`,
                    'Be authentic and transparent',
                    'Focus on customer benefits',
                    'Use active voice',
                    'Keep it concise and clear'
                ],
                donts: [
                    'Avoid jargon and complex terms',
                    "Don't use overly sales-y language",
                    'Avoid negative framing',
                    "Don't make unrealistic promises",
                    'Avoid inconsistent messaging'
                ],
                examples: [
                    `"We're ${brandName.value} - ${brandDescription.value.substring(0, 100)}..."`,
                    `"Discover how ${brandName.value} transforms your ${selectedIndustry.toLowerCase()} experience"`,
                    `"Join thousands who trust ${brandName.value} for ${selectedIndustry.toLowerCase()} excellence"`
                ]
            };
        }

        // Generate Visual Style Guide
        function generateVisualStyle(brandData) {
            const styles = {
                'Modern': { photography: 'Clean, minimalist, bright lighting', graphics: 'Geometric shapes, flat design' },
                'Elegant': { photography: 'Sophisticated, moody, dramatic', graphics: 'Curved lines, gold accents' },
                'Playful': { photography: 'Vibrant, colorful, energetic', graphics: 'Rounded shapes, illustrations' },
                'Bold': { photography: 'High contrast, dramatic angles', graphics: 'Strong shapes, bold patterns' }
            };

            const personality = selectedPersonalities[0] || 'Modern';
            const key = Object.keys(styles).find(k => personality.includes(k)) || 'Modern';
            const style = styles[key];

            return {
                photography: style.photography,
                graphics: style.graphics,
                icons: 'Line icons with rounded corners, 2px stroke weight',
                imagery: 'High-quality, authentic photos. Avoid stock photos when possible.',
                patterns: 'Subtle gradients, minimal patterns, focus on whitespace'
            };
        }

        // Generate Single Logo
        async function generateLogo(index, logoType, brandData) {
            const logoTypes = {
                1: 'Wordmark - Typography-based logo with creative lettering',
                2: 'Symbol - Icon/symbol representation without text',
                3: 'Combination Mark - Icon + brand name integrated',
                4: 'Emblem - Badge-style logo with text inside a symbol',
                5: 'Monogram - Stylized initials of the brand'
            };

            const type = logoTypes[index];
            const personality = selectedPersonalities.slice(0, 3).join(', ');
            const colors = selectedColors.length > 0 ? selectedColors.slice(0, 2).join(', ') : 'industry-appropriate colors';

            const prompt = `Create a professional logo design for "${brandName.value}".

LOGO TYPE: ${type}
INDUSTRY: ${selectedIndustry}
BRAND PERSONALITY: ${personality}
COLOR PREFERENCE: ${colors}
TARGET AUDIENCE: ${selectedAudiences.slice(0, 2).join(', ')}

DESIGN REQUIREMENTS:
- Clean, scalable vector-style design
- ${type}
- Professional and memorable
- Suitable for ${selectedIndustry} industry
- Reflects ${personality} personality
- Modern design trends
- Works in both color and black & white
- High contrast and legibility

OUTPUT: High-quality logo design, clean background, professional presentation, PNG format with transparency support.`;

            try {
                const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        contents: [{
                            parts: [{ text: prompt }]
                        }],
                        generationConfig: {
                            responseModalities: ['TEXT', 'IMAGE'],
                            imageConfig: { aspectRatio: '1:1' }
                        }
                    })
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (!imageData) throw new Error('No image data in response');

                const imageUrl = `data:image/png;base64,${imageData}`;
                const filename = `${brandName.value.replace(/\s+/g, '_')}_Logo_${index}_${Date.now()}.png`;

                return { url: imageUrl, filename, type: type.split(' - ')[0] };
            } catch (error) {
                console.error(`Error generating logo ${index}:`, error);
                return null;
            }
        }

        // selectedCount sudah di-declare di scope luar (count selector untuk logos)

        // Generate All Logos
        async function generateAllLogos(brandData) {
            updateProgress(1, 5, 'Generating logo variations...', 'Creating logo designs...');

            const MAX_ATTEMPTS = 3;
            let attempts = 0;
            let successCount = 0;
            let logoResults = [];

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                const logoPromises = Array.from({ length: selectedCount }, (_, i) => generateLogo(i + 1, i + 1, brandData));
                const results = await Promise.allSettled(logoPromises);
                logoResults = results
                    .filter(r => r.status === 'fulfilled' && r.value !== null)
                    .map(r => r.value);
                successCount = logoResults.length;
            }

            if (successCount === 0) {
                throw new Error('logo_limit_reached');
            }

            generatedLogos = logoResults;

            logosGrid.innerHTML = '';
            generatedLogos.forEach((logo, index) => {
                const card = document.createElement('div');
                card.className = 'brandkit-logo-card';
                card.innerHTML = `
                    <div class="aspect-square bg-gray-50 flex items-center justify-center p-4">
                        <img src="${logo.url}" alt="${logo.type} Logo" class="w-full h-full object-contain">
                    </div>
                    <div class="p-4 bg-white">
                        <h4 class="font-bold text-gray-800 mb-1">${logo.type}</h4>
                        <p class="text-xs text-gray-500 mb-3">Variation ${index + 1}</p>
                        <button onclick="window.downloadDataURINew ? window.downloadDataURINew('${logo.url}', '${logo.filename}') : window.downloadImage('${logo.url}', '${logo.filename}')"
                                class="w-full py-2 px-4 rounded-lg font-semibold text-white text-sm transition-all"
                                style="background: linear-gradient(135deg, #6366f1, #8b5cf6);"
                                data-action="download">
                            <i class="fas fa-download mr-1"></i> Download
                        </button>
                    </div>
                `;
                logosGrid.appendChild(card);
            });
        }

        // Display Color Palette
        function displayColorPalette(palette) {
            updateProgress(2, 5, 'Generating color palette...', 'Creating brand color system...');

            colorsContent.innerHTML = `
                <!-- Primary Colors -->
                <div>
                    <h4 class="font-bold text-gray-800 mb-3">Primary Colors</h4>
                    <div class="grid grid-cols-3 gap-4">
                        <div class="text-center">
                            <div class="color-swatch mb-2" style="background-color: ${palette.primary};"
                                 onclick="navigator.clipboard.writeText('${palette.primary}')"></div>
                            <p class="text-sm font-semibold text-gray-700">Primary</p>
                            <p class="text-xs text-gray-500 font-mono">${palette.primary}</p>
                            <button onclick="navigator.clipboard.writeText('${palette.primary}')"
                                    class="mt-1 text-xs text-indigo-600 hover:text-indigo-700">
                                <i class="fas fa-copy mr-1"></i>Copy
                            </button>
                        </div>
                        <div class="text-center">
                            <div class="color-swatch mb-2" style="background-color: ${palette.secondary};"
                                 onclick="navigator.clipboard.writeText('${palette.secondary}')"></div>
                            <p class="text-sm font-semibold text-gray-700">Secondary</p>
                            <p class="text-xs text-gray-500 font-mono">${palette.secondary}</p>
                            <button onclick="navigator.clipboard.writeText('${palette.secondary}')"
                                    class="mt-1 text-xs text-indigo-600 hover:text-indigo-700">
                                <i class="fas fa-copy mr-1"></i>Copy
                            </button>
                        </div>
                        <div class="text-center">
                            <div class="color-swatch mb-2" style="background-color: ${palette.accent};"
                                 onclick="navigator.clipboard.writeText('${palette.accent}')"></div>
                            <p class="text-sm font-semibold text-gray-700">Accent</p>
                            <p class="text-xs text-gray-500 font-mono">${palette.accent}</p>
                            <button onclick="navigator.clipboard.writeText('${palette.accent}')"
                                    class="mt-1 text-xs text-indigo-600 hover:text-indigo-700">
                                <i class="fas fa-copy mr-1"></i>Copy
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Neutral Colors -->
                <div>
                    <h4 class="font-bold text-gray-800 mb-3">Neutral Colors</h4>
                    <div class="grid grid-cols-5 gap-3">
                        ${palette.neutral.map((color, i) => `
                            <div class="text-center">
                                <div class="color-swatch mb-2" style="background-color: ${color};"
                                     onclick="navigator.clipboard.writeText('${color}')"></div>
                                <p class="text-xs text-gray-500 font-mono">${color}</p>
                            </div>
                        `).join('')}
                    </div>
                </div>

                <!-- Gradients -->
                <div>
                    <h4 class="font-bold text-gray-800 mb-3">Brand Gradients</h4>
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                        ${palette.gradients.map((gradient, i) => `
                            <div>
                                <div class="w-full h-24 rounded-lg mb-2" style="background: ${gradient};"></div>
                                <p class="text-xs text-gray-500 font-mono">${gradient}</p>
                                <button onclick="navigator.clipboard.writeText('${gradient}')"
                                        class="mt-1 text-xs text-indigo-600 hover:text-indigo-700">
                                    <i class="fas fa-copy mr-1"></i>Copy CSS
                                </button>
                            </div>
                        `).join('')}
                    </div>
                </div>

                <!-- Color Usage Guidelines -->
                <div class="bg-indigo-50 border-2 border-indigo-200 rounded-xl p-4">
                    <h4 class="font-bold text-gray-800 mb-2"><i class="fas fa-lightbulb text-indigo-600 mr-2"></i>Usage Guidelines</h4>
                    <ul class="text-sm text-gray-700 space-y-1">
                        <li>• <strong>Primary:</strong> Main brand color - buttons, links, key elements</li>
                        <li>• <strong>Secondary:</strong> Supporting color - hover states, highlights</li>
                        <li>• <strong>Accent:</strong> Call-to-action, important notifications</li>
                        <li>• <strong>Neutrals:</strong> Text, backgrounds, borders, shadows</li>
                        <li>• <strong>Gradients:</strong> Hero sections, premium features</li>
                    </ul>
                </div>
            `;

            brandKitData.colorPalette = palette;
        }

        // Display Typography
        function displayTypography(typo) {
            updateProgress(3, 5, 'Generating typography system...', 'Creating font pairings and hierarchy...');

            typographyContent.innerHTML = `
                <!-- Heading Font -->
                <div class="font-preview-card">
                    <h4 class="font-bold text-gray-800 mb-3">Heading Font</h4>
                    <div class="mb-4">
                        <p class="text-5xl font-bold mb-2" style="font-family: '${typo.heading.font}', sans-serif;">${brandName.value}</p>
                        <p class="text-sm text-gray-500">Font: ${typo.heading.font}</p>
                        <p class="text-sm text-gray-500">Weights: ${typo.heading.weights.join(', ')}</p>
                    </div>
                    <div class="space-y-2">
                        ${typo.heading.sizes.map((size, i) => `
                            <p style="font-family: '${typo.heading.font}', sans-serif; font-size: ${size}; font-weight: ${typo.heading.weights[0]};">
                                Heading ${i + 1} - ${size}
                            </p>
                        `).join('')}
                    </div>
                </div>

                <!-- Body Font -->
                <div class="font-preview-card">
                    <h4 class="font-bold text-gray-800 mb-3">Body Font</h4>
                    <div class="mb-4">
                        <p class="text-lg mb-2" style="font-family: '${typo.body.font}', sans-serif;">
                            The quick brown fox jumps over the lazy dog. 1234567890
                        </p>
                        <p class="text-sm text-gray-500">Font: ${typo.body.font}</p>
                        <p class="text-sm text-gray-500">Weights: ${typo.body.weights.join(', ')}</p>
                    </div>
                    <div class="space-y-2">
                        ${typo.body.sizes.map(size => `
                            <p style="font-family: '${typo.body.font}', sans-serif; font-size: ${size};">
                                Body text - ${size}: Lorem ipsum dolor sit amet, consectetur adipiscing elit.
                            </p>
                        `).join('')}
                    </div>
                </div>

                <!-- Typography Hierarchy -->
                <div class="bg-indigo-50 border-2 border-indigo-200 rounded-xl p-4">
                    <h4 class="font-bold text-gray-800 mb-2"><i class="fas fa-layer-group text-indigo-600 mr-2"></i>Typography Hierarchy</h4>
                    <p class="text-sm text-gray-700 font-mono whitespace-pre-wrap">${typo.hierarchy}</p>
                    <p class="text-xs text-gray-500 mt-2">Style: ${typo.description}</p>
                </div>
            `;

            brandKitData.typography = typo;
        }

        // Display Brand Voice
        function displayBrandVoice(voice) {
            updateProgress(4, 5, 'Generating brand voice guidelines...', 'Creating communication framework...');

            voiceContent.innerHTML = `
                <!-- Tone of Voice -->
                <div class="bg-gradient-to-br from-indigo-50 to-purple-50 border-2 border-indigo-200 rounded-xl p-6">
                    <h4 class="font-bold text-gray-800 mb-2"><i class="fas fa-volume-up text-indigo-600 mr-2"></i>Brand Tone</h4>
                    <p class="text-lg font-semibold text-gray-700">${voice.tone}</p>
                </div>

                <!-- Do's and Don'ts -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div class="border-2 border-green-200 bg-green-50 rounded-xl p-4">
                        <h4 class="font-bold text-green-800 mb-3"><i class="fas fa-check-circle mr-2"></i>Do's</h4>
                        <ul class="space-y-2">
                            ${voice.dos.map(item => `
                                <li class="text-sm text-gray-700 flex items-start">
                                    <i class="fas fa-check text-green-600 mr-2 mt-1"></i>
                                    <span>${item}</span>
                                </li>
                            `).join('')}
                        </ul>
                    </div>
                    <div class="border-2 border-red-200 bg-red-50 rounded-xl p-4">
                        <h4 class="font-bold text-red-800 mb-3"><i class="fas fa-times-circle mr-2"></i>Don'ts</h4>
                        <ul class="space-y-2">
                            ${voice.donts.map(item => `
                                <li class="text-sm text-gray-700 flex items-start">
                                    <i class="fas fa-times text-red-600 mr-2 mt-1"></i>
                                    <span>${item}</span>
                                </li>
                            `).join('')}
                        </ul>
                    </div>
                </div>

                <!-- Example Copy -->
                <div class="border-2 border-indigo-200 rounded-xl p-4">
                    <h4 class="font-bold text-gray-800 mb-3"><i class="fas fa-pen-fancy text-indigo-600 mr-2"></i>Example Copy</h4>
                    <div class="space-y-3">
                        ${voice.examples.map((example, i) => `
                            <div class="bg-white border border-gray-200 rounded-lg p-3">
                                <p class="text-sm text-gray-700 italic">"${example}"</p>
                            </div>
                        `).join('')}
                    </div>
                </div>
            `;

            brandKitData.brandVoice = voice;
        }

        // Display Visual Style
        function displayVisualStyle(visual) {
            updateProgress(5, 5, 'Generating visual style guide...', 'Finalizing brand identity...');

            visualContent.innerHTML = `
                <!-- Photography Style -->
                <div class="font-preview-card">
                    <h4 class="font-bold text-gray-800 mb-2"><i class="fas fa-camera text-indigo-600 mr-2"></i>Photography Style</h4>
                    <p class="text-sm text-gray-700">${visual.photography}</p>
                </div>

                <!-- Graphic Elements -->
                <div class="font-preview-card">
                    <h4 class="font-bold text-gray-800 mb-2"><i class="fas fa-shapes text-indigo-600 mr-2"></i>Graphic Elements</h4>
                    <p class="text-sm text-gray-700">${visual.graphics}</p>
                </div>

                <!-- Icon Style -->
                <div class="font-preview-card">
                    <h4 class="font-bold text-gray-800 mb-2"><i class="fas fa-icons text-indigo-600 mr-2"></i>Icon Style</h4>
                    <p class="text-sm text-gray-700">${visual.icons}</p>
                </div>

                <!-- Imagery Guidelines -->
                <div class="font-preview-card">
                    <h4 class="font-bold text-gray-800 mb-2"><i class="fas fa-image text-indigo-600 mr-2"></i>Imagery Guidelines</h4>
                    <p class="text-sm text-gray-700">${visual.imagery}</p>
                </div>

                <!-- Pattern Usage -->
                <div class="font-preview-card">
                    <h4 class="font-bold text-gray-800 mb-2"><i class="fas fa-fill-drip text-indigo-600 mr-2"></i>Patterns & Textures</h4>
                    <p class="text-sm text-gray-700">${visual.patterns}</p>
                </div>

                <!-- Summary -->
                <div class="bg-gradient-to-br from-indigo-50 to-purple-50 border-2 border-indigo-200 rounded-xl p-6">
                    <h4 class="font-bold text-gray-800 mb-3"><i class="fas fa-star text-indigo-600 mr-2"></i>Visual Identity Summary</h4>
                    <p class="text-sm text-gray-700 leading-relaxed">
                        ${brandName.value} combines ${selectedPersonalities.slice(0, 2).join(' and ')} design elements
                        to create a distinctive ${selectedIndustry.toLowerCase()} brand identity.
                        The visual system is designed to resonate with ${selectedAudiences.slice(0, 2).join(' and ')}
                        through consistent use of photography, graphics, and patterns that reflect the brand's core values.
                    </p>
                </div>
            `;

            brandKitData.visualStyle = visual;
        }

        // Main Generation Function
        async function generateBrandKit(e) {
            e.preventDefault();

            if (generateBtn.disabled) return;

            try {
                // Hide placeholder, show loading
                placeholder.classList.add('hidden');
                loading.classList.remove('hidden');
                resultsTabs.classList.add('hidden');
                resultsContent.classList.add('hidden');
                downloadSection.classList.add('hidden');
                generateBtn.disabled = true;

                const brandData = {
                    name: brandName.value.trim(),
                    description: brandDescription.value.trim(),
                    industry: selectedIndustry,
                    audiences: selectedAudiences,
                    personalities: selectedPersonalities,
                    colors: selectedColors
                };

                // Step 1: Generate Logos (20%)
                await generateAllLogos(brandData);

                // Step 2: Generate Color Palette (40%)
                const colorPalette = generateColorPalette(brandData);
                displayColorPalette(colorPalette);

                // Step 3: Generate Typography (60%)
                const typography = generateTypography(brandData);
                displayTypography(typography);

                // Step 4: Generate Brand Voice (80%)
                const brandVoice = generateBrandVoice(brandData);
                displayBrandVoice(brandVoice);

                // Step 5: Generate Visual Style (100%)
                const visualStyle = generateVisualStyle(brandData);
                displayVisualStyle(visualStyle);

                // Show results
                setTimeout(() => {
                    loading.classList.add('hidden');
                    resultsTabs.classList.remove('hidden');
                    resultsContent.classList.remove('hidden');
                    downloadSection.classList.remove('hidden');
                    generateBtn.disabled = false;

                    // Activate first tab
                    document.querySelector('.brandkit-tab-btn[data-tab="logos"]').click();

                    // Scroll to results on mobile
                    if (window.innerWidth < 1024) {
                        resultsTabs.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }
                }, 500);

            } catch (error) {
                console.error('Brand Kit Generation Error:', error);
                if (error.message === 'logo_limit_reached') {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                } else {
                    alert('Failed to generate brand kit. Please try again.');
                }
                loading.classList.add('hidden');
                placeholder.classList.remove('hidden');
                generateBtn.disabled = false;
            }
        }

        // Download All (ZIP would require additional library, so download individually)
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedLogos.length === 0) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedLogos.length; i++) {
                        const logo = generatedLogos[i];
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedLogos.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(logo.url, logo.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(logo.url, logo.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading logo ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Download Complete!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        // Generate Button Click Handler (button has type="button" — form submit won't fire on click)
        generateBtn.addEventListener('click', generateBrandKit);

        // Initialize validation
        validateForm();

    })();

    // ==================== CONTENT CALENDAR PLANNER ====================
    (() => {
        // Element References
        const form = document.getElementById('calendar-form');
        const brandName = document.getElementById('calendar-brand-name');
        const niche = document.getElementById('calendar-niche');
        const description = document.getElementById('calendar-description');
        const platformOptions = document.getElementById('calendar-platform-options');
        const frequencyOptions = document.getElementById('calendar-frequency-options');
        const contentTypes = document.getElementById('calendar-content-types');
        const toneOptions = document.getElementById('calendar-tone-options');
        const generateBtn = document.getElementById('calendar-generate-btn');

        const placeholder = document.getElementById('calendar-placeholder');
        const loading = document.getElementById('calendar-loading');
        const loadingText = document.getElementById('calendar-loading-text');
        const progressBar = document.getElementById('calendar-progress-bar');
        const progressText = document.getElementById('calendar-progress-text');

        const controls = document.getElementById('calendar-controls');
        const calendarView = document.getElementById('calendar-view');
        const listView = document.getElementById('list-view');
        const downloadBtn = document.getElementById('calendar-download-btn');

        const calendarGridContainer = document.getElementById('calendar-grid-container');
        const calendarMonthTitle = document.getElementById('calendar-month-title');
        const dayDetail = document.getElementById('calendar-day-detail');
        const listContainer = document.getElementById('list-container');
        const listCount = document.getElementById('list-count');

        if (!form) return;

        const apiKey = ""; // Injected by Canvas
        let selectedPlatforms = [];
        let selectedFrequency = '3x-week';
        let selectedContentTypes = ['Educational', 'Entertainment', 'Promotional'];
        let selectedTone = 'Casual & Friendly';
        let calendarData = [];
        let currentView = 'calendar';

        // Setup Checkbox Buttons
        function setupCheckboxes(container, array) {
            if (!container) return;
            container.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                btn.classList.toggle('active');
                const value = btn.dataset.value;
                const icon = btn.querySelector('i');

                if (btn.classList.contains('active')) {
                    if (!array.includes(value)) array.push(value);
                    if (icon) icon.classList.remove('hidden');
                } else {
                    const index = array.indexOf(value);
                    if (index > -1) array.splice(index, 1);
                    if (icon) icon.classList.add('hidden');
                }

                // Update platform counter
                if (container === platformOptions) {
                    const countSpan = document.getElementById('calendar-platform-count');
                    const selectedSpan = document.getElementById('calendar-platform-selected');
                    const numSpan = document.getElementById('calendar-platform-num');

                    if (numSpan) numSpan.textContent = array.length;

                    if (array.length >= 1) {
                        if (countSpan) countSpan.classList.add('hidden');
                        if (selectedSpan) selectedSpan.classList.remove('hidden');
                    } else {
                        if (countSpan) countSpan.classList.remove('hidden');
                        if (selectedSpan) selectedSpan.classList.add('hidden');
                    }
                }

                validateForm();
            });
        }

        // Setup Option Buttons (Single Selection)
        function setupSingleSelect(container, callback) {
            if (!container) return;
            container.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                container.querySelectorAll('.option-btn-calendar').forEach(b =>
                    b.classList.remove('selected')
                );
                btn.classList.add('selected');

                if (callback) callback(btn.dataset.value);
                validateForm();
            });
        }

        // Setup all option buttons
        setupCheckboxes(platformOptions, selectedPlatforms);
        setupCheckboxes(contentTypes, selectedContentTypes);
        setupSingleSelect(frequencyOptions, (val) => { selectedFrequency = val; });
        setupSingleSelect(toneOptions, (val) => { selectedTone = val; });

        // Setup View Toggle
        document.querySelectorAll('.view-toggle-btn').forEach(btn => {
            btn.addEventListener('click', () => {
                const view = btn.dataset.view;
                currentView = view;

                // Update active button
                document.querySelectorAll('.view-toggle-btn').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');

                // Toggle views
                if (view === 'calendar') {
                    calendarView.classList.remove('hidden');
                    listView.classList.add('hidden');
                } else {
                    calendarView.classList.add('hidden');
                    listView.classList.remove('hidden');
                }
            });
        });

        // Form Validation
        function validateForm() {
            const isValid = brandName.value.trim() !== '' &&
                           niche.value.trim() !== '' &&
                           selectedPlatforms.length > 0;

            generateBtn.disabled = !isValid;
            if (isValid) {
                generateBtn.style.opacity = '1';
                generateBtn.style.cursor = 'pointer';
            } else {
                generateBtn.style.opacity = '0.5';
                generateBtn.style.cursor = 'not-allowed';
            }
        }

        brandName.addEventListener('input', validateForm);
        niche.addEventListener('input', validateForm);

        // Update Progress
        function updateProgress(percent, message) {
            progressBar.style.width = `${percent}%`;
            progressText.textContent = `${percent}% complete`;
            loadingText.textContent = message;
        }

        // Get Best Posting Times by Platform
        function getBestPostingTime(platform, dayIndex) {
            const times = {
                'Instagram': ['09:00 AM', '12:00 PM', '05:00 PM', '07:00 PM'],
                'TikTok': ['07:00 AM', '12:00 PM', '06:00 PM', '09:00 PM'],
                'YouTube': ['02:00 PM', '05:00 PM', '08:00 PM'],
                'Facebook': ['01:00 PM', '03:00 PM', '07:00 PM'],
                'LinkedIn': ['08:00 AM', '12:00 PM', '05:00 PM'],
                'Twitter': ['09:00 AM', '12:00 PM', '03:00 PM', '06:00 PM']
            };

            const platformTimes = times[platform] || ['12:00 PM'];
            return platformTimes[dayIndex % platformTimes.length];
        }

        // Get Content Format by Platform
        function getContentFormat(platform) {
            const formats = {
                'Instagram': ['Reel', 'Carousel', 'Story', 'Static Post'],
                'TikTok': ['Short Video', 'Duet', 'Stitch', 'Photo Montage'],
                'YouTube': ['Short', 'Video', 'Community Post'],
                'Facebook': ['Video', 'Photo Post', 'Story'],
                'LinkedIn': ['Article', 'Carousel', 'Poll', 'Document'],
                'Twitter': ['Thread', 'Image Post', 'Poll', 'Video']
            };

            const platformFormats = formats[platform] || ['Post'];
            return platformFormats[Math.floor(Math.random() * platformFormats.length)];
        }

        // Generate Content Calendar via AI
        async function generateContentCalendar() {
            updateProgress(10, 'Analyzing your brand and niche...');

            const frequencyMap = {
                'daily': 30,
                '5x-week': 22,
                '3x-week': 13,
                '2x-week': 9
            };

            const totalPosts = frequencyMap[selectedFrequency];
            const platforms = selectedPlatforms.join(', ');
            const contentMix = selectedContentTypes.join(', ');

            const prompt = `Generate a 30-day content calendar for "${brandName.value}" in the ${niche.value} niche.

BRAND INFO:
- Name: ${brandName.value}
- Niche: ${niche.value}
- Description: ${description.value || 'Not provided'}
- Platforms: ${platforms}
- Posting Frequency: ${selectedFrequency} (${totalPosts} posts total)
- Content Types: ${contentMix}
- Brand Tone: ${selectedTone}

REQUIREMENTS:
1. Generate EXACTLY ${totalPosts} content ideas distributed across 30 days
2. Ensure 80/20 balance: 80% value content (Educational/Entertainment), 20% promotional
3. Vary content types and topics to avoid repetition
4. Each post must include:
   - Date (Day 1-30)
   - Platform (rotate among: ${platforms})
   - Content Type (${contentMix})
   - Topic/Theme (specific and actionable)
   - Hook/Caption Idea (engaging, ${selectedTone} tone)
   - 5-10 relevant hashtags (trending + niche-specific)
   - Content Format (Reel, Carousel, Video, etc - platform-appropriate)

5. Platform-specific optimization:
   - Instagram: Visual-first, trending audio mentions
   - TikTok: Viral hooks, trending sounds
   - YouTube: SEO titles, thumbnail ideas
   - Facebook: Community engagement
   - LinkedIn: Professional insights
   - Twitter: Concise threads

6. Content variety:
   - Mix tutorials, tips, behind-the-scenes, testimonials, FAQs, myths, trends, challenges
   - Include seasonal/trending topics when relevant
   - Vary hook styles: question, shocking stat, storytelling, controversy

OUTPUT FORMAT (CRITICAL - USE EXACTLY THIS STRUCTURE):
For each post, use this exact format with line separators:

━━━━━━━━━━━━━━━━
DAY [number]
Platform: [platform name]
Format: [content format]
Type: [content type]
━━━━━━━━━━━━━━━━

📌 TOPIC: [specific topic/theme]

🎯 HOOK/CAPTION:
[engaging caption/hook with ${selectedTone} tone]

#️⃣ HASHTAGS:
[list 5-10 relevant hashtags separated by spaces]

━━━━━━━━━━━━━━━━

Repeat this format for all ${totalPosts} posts. Make each post unique and valuable!`;

            try {
                updateProgress(30, 'Generating 30-day content strategy...');

                const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        contents: [{
                            parts: [{ text: prompt }]
                        }],
                        generationConfig: {
                            temperature: 0.9,
                            topK: 40,
                            topP: 0.95,
                            maxOutputTokens: 8192
                        }
                    })
                });

                if (!response.ok) throw new Error(`API error: ${response.status}`);

                updateProgress(60, 'Processing content ideas...');

                const result = await response.json();
                const content = result?.candidates?.[0]?.content?.parts?.[0]?.text;

                if (!content) throw new Error('No content generated');

                // Parse the AI response
                const posts = parseCalendarContent(content, totalPosts);

                updateProgress(90, 'Finalizing calendar...');

                return posts;

            } catch (error) {
                console.error('Calendar generation error:', error);
                throw error;
            }
        }

        // Parse AI Response into Structured Data
        function parseCalendarContent(content, totalPosts) {
            const posts = [];
            const lines = content.split('\n');
            let currentPost = null;
            let section = null;

            for (let i = 0; i < lines.length; i++) {
                const line = lines[i].trim();

                // Detect day start
                if (line.match(/^DAY\s+(\d+)/i)) {
                    if (currentPost) posts.push(currentPost);
                    const dayMatch = line.match(/^DAY\s+(\d+)/i);
                    currentPost = {
                        day: parseInt(dayMatch[1]),
                        platform: '',
                        format: '',
                        type: '',
                        topic: '',
                        hook: '',
                        hashtags: [],
                        postingTime: ''
                    };
                    section = 'header';
                    continue;
                }

                if (!currentPost) continue;

                // Parse header info
                if (section === 'header') {
                    if (line.match(/^Platform:\s*(.+)/i)) {
                        const platformMatch = line.match(/^Platform:\s*(.+)/i);
                        currentPost.platform = platformMatch[1].trim();
                        currentPost.postingTime = getBestPostingTime(currentPost.platform, currentPost.day);
                    } else if (line.match(/^Format:\s*(.+)/i)) {
                        const formatMatch = line.match(/^Format:\s*(.+)/i);
                        currentPost.format = formatMatch[1].trim();
                    } else if (line.match(/^Type:\s*(.+)/i)) {
                        const typeMatch = line.match(/^Type:\s*(.+)/i);
                        currentPost.type = typeMatch[1].trim();
                    } else if (line.includes('━━━')) {
                        section = 'content';
                    }
                    continue;
                }

                // Parse content sections
                if (line.match(/📌\s*TOPIC:/i)) {
                    section = 'topic';
                    const topicMatch = line.match(/📌\s*TOPIC:\s*(.+)/i);
                    if (topicMatch) currentPost.topic = topicMatch[1].trim();
                    continue;
                } else if (line.match(/🎯\s*HOOK/i)) {
                    section = 'hook';
                    continue;
                } else if (line.match(/#️⃣\s*HASHTAG/i)) {
                    section = 'hashtags';
                    continue;
                }

                // Collect content
                if (section === 'topic' && line && !line.includes('━━━')) {
                    currentPost.topic += (currentPost.topic ? ' ' : '') + line;
                } else if (section === 'hook' && line && !line.includes('━━━') && !line.match(/#️⃣/)) {
                    currentPost.hook += (currentPost.hook ? '\n' : '') + line;
                } else if (section === 'hashtags' && line && !line.includes('━━━')) {
                    const tags = line.split(/\s+/).filter(tag => tag.startsWith('#') || tag.match(/^[a-zA-Z]/));
                    currentPost.hashtags.push(...tags.map(tag => tag.startsWith('#') ? tag : '#' + tag));
                }
            }

            // Add last post
            if (currentPost) posts.push(currentPost);

            // Ensure we have enough posts, fill with fallbacks if needed
            while (posts.length < totalPosts) {
                posts.push(createFallbackPost(posts.length + 1));
            }

            return posts.slice(0, totalPosts);
        }

        // Create Fallback Post (if AI parsing fails)
        function createFallbackPost(day) {
            const platform = selectedPlatforms[day % selectedPlatforms.length];
            const type = selectedContentTypes[day % selectedContentTypes.length];

            return {
                day: day,
                platform: platform,
                format: getContentFormat(platform),
                type: type,
                topic: `${type} content for Day ${day}`,
                hook: `Engaging ${type.toLowerCase()} post for ${brandName.value}`,
                hashtags: [`#${niche.value.replace(/\s+/g, '')}`, `#${brandName.value.replace(/\s+/g, '')}`, '#contentmarketing'],
                postingTime: getBestPostingTime(platform, day)
            };
        }

        // Render Calendar View
        function renderCalendarView(posts) {
            calendarGridContainer.innerHTML = '';
            calendarMonthTitle.textContent = 'Your 30-Day Content Calendar';

            // Create 30 day cards
            for (let day = 1; day <= 30; day++) {
                const dayPost = posts.find(p => p.day === day);
                const card = document.createElement('div');
                card.className = `calendar-day ${dayPost ? 'has-content' : ''}`;
                card.dataset.day = day;

                if (dayPost) {
                    const platformClass = dayPost.platform.toLowerCase().replace(/\s+/g, '');
                    card.innerHTML = `
                        <div>
                            <div class="text-xs font-bold text-gray-500">Day ${day}</div>
                            <div class="platform-badge ${platformClass} mt-1 text-xs">
                                ${dayPost.platform}
                            </div>
                        </div>
                        <div class="text-xs text-gray-600 line-clamp-2 mt-1">
                            ${dayPost.topic.substring(0, 40)}...
                        </div>
                    `;

                    // Click to show details
                    card.addEventListener('click', () => showDayDetail(dayPost));
                } else {
                    card.innerHTML = `
                        <div class="text-xs font-bold text-gray-400">Day ${day}</div>
                        <div class="text-xs text-gray-300">Rest day</div>
                    `;
                }

                calendarGridContainer.appendChild(card);
            }
        }

        // Show Day Detail
        function showDayDetail(post) {
            const platformClass = post.platform.toLowerCase().replace(/\s+/g, '');

            dayDetail.classList.remove('hidden');
            dayDetail.innerHTML = `
                <div class="flex justify-between items-start mb-3">
                    <div>
                        <h4 class="text-lg font-bold text-gray-800">Day ${post.day}</h4>
                        <div class="platform-badge ${platformClass} mt-1">
                            <i class="fas fa-${platformClass === 'instagram' ? 'instagram' : platformClass === 'tiktok' ? 'video' : platformClass === 'youtube' ? 'youtube' : platformClass === 'facebook' ? 'facebook' : platformClass === 'linkedin' ? 'linkedin' : 'twitter'} mr-1"></i>
                            ${post.platform}
                        </div>
                    </div>
                    <button onclick="this.closest('#calendar-day-detail').classList.add('hidden')" class="text-gray-500 hover:text-gray-700">
                        <i class="fas fa-times"></i>
                    </button>
                </div>

                <div class="space-y-3 text-sm">
                    <div>
                        <p class="font-semibold text-gray-700">📅 Posting Time:</p>
                        <p class="text-gray-600">${post.postingTime}</p>
                    </div>
                    <div>
                        <p class="font-semibold text-gray-700">📱 Format:</p>
                        <p class="text-gray-600">${post.format}</p>
                    </div>
                    <div>
                        <p class="font-semibold text-gray-700">🎨 Content Type:</p>
                        <p class="text-gray-600">${post.type}</p>
                    </div>
                    <div>
                        <p class="font-semibold text-gray-700">📌 Topic:</p>
                        <p class="text-gray-600">${post.topic}</p>
                    </div>
                    <div>
                        <p class="font-semibold text-gray-700">🎯 Hook/Caption:</p>
                        <p class="text-gray-600 whitespace-pre-wrap">${post.hook}</p>
                    </div>
                    <div>
                        <p class="font-semibold text-gray-700">#️⃣ Hashtags:</p>
                        <div class="flex flex-wrap gap-1 mt-1">
                            ${post.hashtags.slice(0, 10).map(tag =>
                                `<span class="text-xs bg-green-100 text-green-700 px-2 py-1 rounded-full">${tag}</span>`
                            ).join('')}
                        </div>
                    </div>
                </div>
            `;

            // Scroll to detail on mobile
            if (window.innerWidth < 1024) {
                dayDetail.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
            }
        }

        // Render List View
        function renderListView(posts) {
            listContainer.innerHTML = '';
            listCount.textContent = posts.length;

            posts.forEach((post, index) => {
                const platformClass = post.platform.toLowerCase().replace(/\s+/g, '');
                const card = document.createElement('div');
                card.className = 'content-card';
                card.dataset.platform = post.platform.toLowerCase();
                card.innerHTML = `
                    <div class="flex flex-col sm:flex-row gap-3">
                        <div class="flex-shrink-0">
                            <div class="w-12 h-12 rounded-lg flex items-center justify-center font-bold text-white" style="background: linear-gradient(135deg, #10b981, #059669);">
                                ${post.day}
                            </div>
                        </div>
                        <div class="flex-1">
                            <div class="flex flex-wrap gap-2 items-center mb-2">
                                <div class="platform-badge ${platformClass}">
                                    ${post.platform}
                                </div>
                                <span class="text-xs text-gray-500">${post.format}</span>
                                <span class="text-xs text-gray-400">•</span>
                                <span class="text-xs text-gray-500">${post.postingTime}</span>
                            </div>
                            <h4 class="font-bold text-gray-800 mb-1">${post.topic}</h4>
                            <p class="text-sm text-gray-600 mb-2 line-clamp-2">${post.hook}</p>
                            <div class="flex flex-wrap gap-1">
                                ${post.hashtags.slice(0, 5).map(tag =>
                                    `<span class="text-xs text-green-600">${tag}</span>`
                                ).join('')}
                            </div>
                        </div>
                    </div>
                `;
                listContainer.appendChild(card);
            });

            // Add platform filters
            addPlatformFilters(posts);
        }

        // Add Platform Filters
        function addPlatformFilters(posts) {
            const filterContainer = listView.querySelector('.mb-4');
            const platforms = [...new Set(posts.map(p => p.platform))];

            // Remove old filters except "All"
            const oldFilters = filterContainer.querySelectorAll('[data-filter]:not([data-filter="all"])');
            oldFilters.forEach(f => f.remove());

            // Add platform filters
            platforms.forEach(platform => {
                const btn = document.createElement('button');
                btn.type = 'button';
                btn.dataset.filter = platform.toLowerCase();
                btn.className = 'filter-btn text-xs px-3 py-1.5 rounded-full border-2 border-gray-300 bg-white text-gray-700 font-semibold hover:border-green-500 hover:text-green-600 transition-all';
                btn.textContent = platform;

                btn.addEventListener('click', () => filterListView(platform.toLowerCase()));
                filterContainer.appendChild(btn);
            });

            // All filter handler
            filterContainer.querySelector('[data-filter="all"]').addEventListener('click', () => filterListView('all'));
        }

        // Filter List View
        function filterListView(platform) {
            const cards = listContainer.querySelectorAll('.content-card');
            const filterButtons = document.querySelectorAll('.filter-btn');

            // Update active filter
            filterButtons.forEach(btn => {
                if (btn.dataset.filter === platform) {
                    btn.classList.remove('border-gray-300', 'bg-white', 'text-gray-700');
                    btn.classList.add('border-green-500', 'bg-green-500', 'text-white');
                } else {
                    btn.classList.remove('border-green-500', 'bg-green-500', 'text-white');
                    btn.classList.add('border-gray-300', 'bg-white', 'text-gray-700');
                }
            });

            // Filter cards
            cards.forEach(card => {
                if (platform === 'all' || card.dataset.platform === platform) {
                    card.style.display = '';
                } else {
                    card.style.display = 'none';
                }
            });
        }

        // Download CSV
        function downloadCSV(posts) {
            const headers = ['Day', 'Date', 'Platform', 'Format', 'Type', 'Topic', 'Hook/Caption', 'Hashtags', 'Posting Time'];
            const today = new Date();

            const rows = posts.map(post => {
                const postDate = new Date(today);
                postDate.setDate(today.getDate() + post.day - 1);
                const dateStr = postDate.toLocaleDateString('id-ID');

                return [
                    post.day,
                    dateStr,
                    post.platform,
                    post.format,
                    post.type,
                    post.topic,
                    `"${post.hook.replace(/"/g, '""')}"`,
                    post.hashtags.join(' '),
                    post.postingTime
                ];
            });

            const csv = [headers.join(','), ...rows.map(row => row.join(','))].join('\n');
            const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });
            const link = document.createElement('a');
            link.href = URL.createObjectURL(blob);
            link.download = `${brandName.value.replace(/\s+/g, '_')}_Content_Calendar_${Date.now()}.csv`;
            link.click();
        }

        // Main Generation Function
        async function handleGenerate(e) {
            e.preventDefault();

            if (generateBtn.disabled) return;

            try {
                // Hide placeholder, show loading
                placeholder.classList.add('hidden');
                loading.classList.remove('hidden');
                controls.classList.add('hidden');
                calendarView.classList.add('hidden');
                listView.classList.add('hidden');
                generateBtn.disabled = true;

                // Generate calendar
                const posts = await generateContentCalendar();
                calendarData = posts;

                updateProgress(100, 'Calendar ready!');

                // Show results
                setTimeout(() => {
                    loading.classList.add('hidden');
                    controls.classList.remove('hidden');
                    calendarView.classList.remove('hidden');

                    renderCalendarView(posts);
                    renderListView(posts);

                    generateBtn.disabled = false;

                    // Scroll to results on mobile
                    if (window.innerWidth < 1024) {
                        controls.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }
                }, 500);

            } catch (error) {
                console.error('Calendar Generation Error:', error);
                alert('Failed to generate calendar. Please try again.');
                loading.classList.add('hidden');
                placeholder.classList.remove('hidden');
                generateBtn.disabled = false;
            }
        }

        // Download Button Handler
        if (downloadBtn) {
            downloadBtn.addEventListener('click', () => {
                if (calendarData.length > 0) {
                    downloadCSV(calendarData);
                }
            });
        }

        // Generate Button Click Handler (button has type="button" — form submit won't fire on click)
        generateBtn.addEventListener('click', handleGenerate);

        // Initialize validation
        validateForm();

    })();

    // ==================== AI A/B TESTING RECOMMENDER ====================
    (() => {
        const apiKey = "";

        // Form Elements
        const form = document.getElementById('abtest-form');
        const analyzeBtn = document.getElementById('abtest-analyze-btn');

        // Upload Elements
        const variantInputs = {
            a: document.getElementById('abtest-variant-a'),
            b: document.getElementById('abtest-variant-b'),
            c: document.getElementById('abtest-variant-c'),
            d: document.getElementById('abtest-variant-d')
        };

        const variantPreviews = {
            a: document.getElementById('abtest-preview-a'),
            b: document.getElementById('abtest-preview-b'),
            c: document.getElementById('abtest-preview-c'),
            d: document.getElementById('abtest-preview-d')
        };

        // Option Elements
        const platformOptions = document.getElementById('abtest-platform-options');
        const goalOptions = document.getElementById('abtest-goal-options');
        const genderOptions = document.getElementById('abtest-gender-options');
        const criteriaOptions = document.getElementById('abtest-criteria-options');
        const ageSelect = document.getElementById('abtest-age');

        // Result Elements
        const placeholder = document.getElementById('abtest-placeholder');
        const loading = document.getElementById('abtest-loading');
        const loadingText = document.getElementById('abtest-loading-text');
        const progressBar = document.getElementById('abtest-progress-bar');
        const progressText = document.getElementById('abtest-progress-text');
        const results = document.getElementById('abtest-results');
        const variantsGrid = document.getElementById('abtest-variants-grid');
        const winnerName = document.getElementById('abtest-winner-name');
        const winnerScore = document.getElementById('abtest-winner-score');
        const winnerConfidence = document.getElementById('abtest-winner-confidence');
        const insightsContainer = document.getElementById('abtest-insights-container');
        const resetBtn = document.getElementById('abtest-reset-btn');

        // State
        let variantImages = { a: null, b: null, c: null, d: null };
        let selectedPlatform = 'Instagram';
        let selectedGoal = 'Engagement';
        let selectedGender = 'All';
        let selectedCriteria = ['Visual-Appeal', 'Color-Psychology', 'Composition', 'Attention-Grabbing', 'Emotional-Impact', 'Call-to-Action'];

        // Upload Handlers with HEIC Support
        Object.keys(variantInputs).forEach(variant => {
            const input = variantInputs[variant];
            const preview = variantPreviews[variant];

            if (!input || !preview) return;

            input.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    variantImages[variant] = reader.result;
                    const img = preview.querySelector('img');
                    if (img) img.src = reader.result;
                    preview.classList.remove('hidden');
                    validateForm();
                };
                reader.readAsDataURL(processedFile);
            });
        });

        // Remove Button Handlers
        document.querySelectorAll('.abtest-remove-btn').forEach(btn => {
            btn.addEventListener('click', (e) => {
                e.preventDefault();
                e.stopPropagation();
                const variant = btn.dataset.variant;
                if (variantInputs[variant]) {
                    variantInputs[variant].value = '';
                    variantImages[variant] = null;
                    if (variantPreviews[variant]) {
                        variantPreviews[variant].classList.add('hidden');
                    }
                    validateForm();
                }
            });
        });

        // Platform Selection
        if (platformOptions) {
            platformOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                platformOptions.querySelectorAll('.option-btn-abtest').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedPlatform = btn.dataset.value;
            });
        }

        // Goal Selection
        if (goalOptions) {
            goalOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                goalOptions.querySelectorAll('.option-btn-abtest').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedGoal = btn.dataset.value;
            });
        }

        // Gender Selection
        if (genderOptions) {
            genderOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                genderOptions.querySelectorAll('.option-btn-abtest').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedGender = btn.dataset.value;
            });
        }

        // Criteria Selection (Multi-select with checkboxes)
        if (criteriaOptions) {
            criteriaOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                const value = btn.dataset.value;
                const isActive = btn.classList.contains('active');
                const icon = btn.querySelector('.fas');

                if (isActive) {
                    btn.classList.remove('active');
                    if (icon) icon.classList.add('hidden');
                    selectedCriteria = selectedCriteria.filter(c => c !== value);
                } else {
                    btn.classList.add('active');
                    if (icon) icon.classList.remove('hidden');
                    selectedCriteria.push(value);
                }
            });
        }

        // Form Validation
        function validateForm() {
            const hasMinVariants = variantImages.a && variantImages.b;
            if (analyzeBtn) {
                analyzeBtn.disabled = !hasMinVariants;
            }
        }

        // Analyze Variants
        async function analyzeVariants(e) {
            e.preventDefault();

            const uploadedVariants = Object.keys(variantImages).filter(k => variantImages[k]);
            if (uploadedVariants.length < 2) {
                alert('Upload minimal 2 variants untuk A/B testing!');
                return;
            }

            // Show loading
            if (placeholder) placeholder.classList.add('hidden');
            if (results) results.classList.add('hidden');
            if (loading) loading.classList.remove('hidden');
            if (analyzeBtn) analyzeBtn.disabled = true;

            try {
                const variantAnalysis = [];
                let progress = 0;
                const totalSteps = uploadedVariants.length;

                // Analyze each variant
                for (let i = 0; i < uploadedVariants.length; i++) {
                    const variantKey = uploadedVariants[i];
                    const variantLabel = variantKey.toUpperCase();

                    if (loadingText) loadingText.textContent = `Analyzing Variant ${variantLabel}...`;

                    const analysis = await analyzeVariant(variantKey, variantLabel);
                    variantAnalysis.push(analysis);

                    progress = Math.round(((i + 1) / totalSteps) * 100);
                    if (progressBar) progressBar.style.width = `${progress}%`;
                    if (progressText) progressText.textContent = `${progress}% complete`;
                }

                // Display results
                displayResults(variantAnalysis, uploadedVariants);

            } catch (error) {
                console.error('Analysis error:', error);
                alert('Terjadi kesalahan saat menganalisis variants. Silakan coba lagi.');

                if (loading) loading.classList.add('hidden');
                if (placeholder) placeholder.classList.remove('hidden');
            } finally {
                if (analyzeBtn) analyzeBtn.disabled = false;
                if (progressBar) progressBar.style.width = '0%';
                if (progressText) progressText.textContent = '0% complete';
            }
        }

        // Analyze Single Variant
        async function analyzeVariant(variantKey, variantLabel) {
            const imageData = variantImages[variantKey].split(',')[1];

            const prompt = `Analyze this content variant for ${selectedPlatform} platform.

TARGET AUDIENCE:
- Age: ${ageSelect ? ageSelect.value : '18-24'}
- Gender: ${selectedGender}

CONTENT GOAL: ${selectedGoal}

ANALYSIS CRITERIA:
${selectedCriteria.map(c => `- ${c.replace(/-/g, ' ')}`).join('\n')}

Provide a detailed analysis in the following format:

SCORE: [Overall score 0-100]

STRENGTHS:
- [Strength 1]
- [Strength 2]
- [Strength 3]

WEAKNESSES:
- [Weakness 1]
- [Weakness 2]

METRICS PREDICTION:
- Engagement Rate: [percentage]%
- Click-Through Rate: [percentage]%
- Reach Potential: [Low/Medium/High/Very High]
- Viral Probability: [percentage]%

KEY INSIGHT: [One sentence insight tentang mengapa variant ini akan perform well atau tidak]

IMPROVEMENT TIPS:
- [Tip 1]
- [Tip 2]`;

            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

            const response = await fetch(apiUrl, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: 'image/jpeg', data: imageData } }
                        ]
                    }],
                    generationConfig: {
                        temperature: 0.7,
                        maxOutputTokens: 2048
                    }
                })
            });

            if (!response.ok) {
                throw new Error(`API error: ${response.status}`);
            }

            const result = await response.json();
            const analysisText = result?.candidates?.[0]?.content?.parts?.[0]?.text || '';

            // Parse analysis
            const scoreMatch = analysisText.match(/SCORE:\s*(\d+)/i);
            const score = scoreMatch ? parseInt(scoreMatch[1]) : 75;

            const strengthsMatch = analysisText.match(/STRENGTHS:([\s\S]*?)(?=WEAKNESSES:|METRICS|$)/i);
            const strengths = strengthsMatch ? strengthsMatch[1].trim().split('\n').filter(s => s.trim().startsWith('-')).map(s => s.replace(/^-\s*/, '')) : [];

            const weaknessesMatch = analysisText.match(/WEAKNESSES:([\s\S]*?)(?=METRICS|KEY INSIGHT|$)/i);
            const weaknesses = weaknessesMatch ? weaknessesMatch[1].trim().split('\n').filter(s => s.trim().startsWith('-')).map(s => s.replace(/^-\s*/, '')) : [];

            const engagementMatch = analysisText.match(/Engagement Rate:\s*([\d.]+)%/i);
            const engagement = engagementMatch ? engagementMatch[1] : '0';

            const ctrMatch = analysisText.match(/Click-Through Rate:\s*([\d.]+)%/i);
            const ctr = ctrMatch ? ctrMatch[1] : '0';

            const reachMatch = analysisText.match(/Reach Potential:\s*([\w\s]+)/i);
            const reach = reachMatch ? reachMatch[1].trim() : 'Medium';

            const viralMatch = analysisText.match(/Viral Probability:\s*([\d.]+)%/i);
            const viral = viralMatch ? viralMatch[1] : '0';

            const insightMatch = analysisText.match(/KEY INSIGHT:\s*(.+)/i);
            const insight = insightMatch ? insightMatch[1].trim() : analysisText.substring(0, 150);

            const tipsMatch = analysisText.match(/IMPROVEMENT TIPS:([\s\S]*?)$/i);
            const tips = tipsMatch ? tipsMatch[1].trim().split('\n').filter(s => s.trim().startsWith('-')).map(s => s.replace(/^-\s*/, '')) : [];

            return {
                variant: variantKey,
                label: variantLabel,
                score,
                strengths,
                weaknesses,
                metrics: {
                    engagement,
                    ctr,
                    reach,
                    viral
                },
                insight,
                tips,
                rawAnalysis: analysisText
            };
        }

        // Display Results
        function displayResults(analyses, uploadedVariants) {
            // Find winner (highest score)
            const winner = analyses.reduce((prev, curr) => curr.score > prev.score ? curr : prev);
            const scores = analyses.map(a => a.score);
            const avgScore = scores.reduce((a, b) => a + b, 0) / scores.length;
            const winConfidence = Math.round(((winner.score - avgScore) / winner.score) * 100);

            // Update winner announcement
            if (winnerName) winnerName.textContent = `Variant ${winner.label}`;
            if (winnerScore) winnerScore.textContent = `${winner.score}/100`;
            if (winnerConfidence) winnerConfidence.textContent = `${winConfidence}%`;

            // Create variant cards
            if (variantsGrid) {
                variantsGrid.innerHTML = '';

                analyses.forEach(analysis => {
                    const isWinner = analysis.variant === winner.variant;
                    const card = document.createElement('div');
                    card.className = `variant-card p-4 rounded-xl border-2 ${isWinner ? 'winner border-green-400' : 'border-gray-300'} bg-white`;

                    card.innerHTML = `
                        <div class="flex justify-between items-center mb-3">
                            <h5 class="font-bold text-lg">Variant ${analysis.label}</h5>
                            ${isWinner ? '<span class="text-xs font-bold text-green-600 bg-green-100 px-2 py-1 rounded-full">WINNER</span>' : ''}
                        </div>

                        <div class="mb-3 rounded-lg overflow-hidden">
                            <img src="${variantImages[analysis.variant]}" alt="Variant ${analysis.label}" class="w-full h-48 object-cover">
                        </div>

                        <div class="flex justify-center mb-4">
                            <div class="score-circle flex items-center justify-center ${isWinner ? 'bg-gradient-to-br from-green-400 to-emerald-500' : 'bg-gradient-to-br from-blue-400 to-purple-500'} text-white">
                                <div class="text-center">
                                    <div class="text-4xl font-bold">${analysis.score}</div>
                                    <div class="text-sm opacity-90">/ 100</div>
                                </div>
                            </div>
                        </div>

                        <div class="space-y-3 text-sm">
                            <div>
                                <h6 class="font-bold text-gray-700 mb-1">📊 Predicted Metrics:</h6>
                                <div class="grid grid-cols-2 gap-2 text-xs">
                                    <div class="bg-gray-50 p-2 rounded">
                                        <div class="text-gray-600">Engagement</div>
                                        <div class="font-bold text-blue-600">${analysis.metrics.engagement}%</div>
                                    </div>
                                    <div class="bg-gray-50 p-2 rounded">
                                        <div class="text-gray-600">CTR</div>
                                        <div class="font-bold text-purple-600">${analysis.metrics.ctr}%</div>
                                    </div>
                                    <div class="bg-gray-50 p-2 rounded">
                                        <div class="text-gray-600">Reach</div>
                                        <div class="font-bold text-green-600">${analysis.metrics.reach}</div>
                                    </div>
                                    <div class="bg-gray-50 p-2 rounded">
                                        <div class="text-gray-600">Viral</div>
                                        <div class="font-bold text-orange-600">${analysis.metrics.viral}%</div>
                                    </div>
                                </div>
                            </div>

                            <div>
                                <h6 class="font-bold text-green-700 mb-1">✅ Strengths:</h6>
                                <ul class="text-xs text-gray-600 space-y-1">
                                    ${analysis.strengths.slice(0, 3).map(s => `<li>• ${s}</li>`).join('')}
                                </ul>
                            </div>

                            <div>
                                <h6 class="font-bold text-red-700 mb-1">⚠️ Weaknesses:</h6>
                                <ul class="text-xs text-gray-600 space-y-1">
                                    ${analysis.weaknesses.slice(0, 2).map(w => `<li>• ${w}</li>`).join('')}
                                </ul>
                            </div>
                        </div>
                    `;

                    variantsGrid.appendChild(card);
                });
            }

            // Create insights
            if (insightsContainer) {
                insightsContainer.innerHTML = '';

                // Winner insight
                const winnerInsight = document.createElement('div');
                winnerInsight.className = 'p-4 rounded-lg bg-green-50 border-2 border-green-200';
                winnerInsight.innerHTML = `
                    <h5 class="font-bold text-green-800 mb-2">🏆 Why Variant ${winner.label} Wins:</h5>
                    <p class="text-sm text-gray-700">${winner.insight}</p>
                `;
                insightsContainer.appendChild(winnerInsight);

                // Improvement tips for winner
                if (winner.tips.length > 0) {
                    const tipsCard = document.createElement('div');
                    tipsCard.className = 'p-4 rounded-lg bg-blue-50 border-2 border-blue-200';
                    tipsCard.innerHTML = `
                        <h5 class="font-bold text-blue-800 mb-2">💡 How to Make Variant ${winner.label} Even Better:</h5>
                        <ul class="text-sm text-gray-700 space-y-1">
                            ${winner.tips.map(tip => `<li>• ${tip}</li>`).join('')}
                        </ul>
                    `;
                    insightsContainer.appendChild(tipsCard);
                }

                // Platform-specific tip
                const platformTip = getPlatformTip(selectedPlatform, selectedGoal);
                if (platformTip) {
                    const platformCard = document.createElement('div');
                    platformCard.className = 'p-4 rounded-lg bg-purple-50 border-2 border-purple-200';
                    platformCard.innerHTML = `
                        <h5 class="font-bold text-purple-800 mb-2">🎯 ${selectedPlatform} Pro Tip:</h5>
                        <p class="text-sm text-gray-700">${platformTip}</p>
                    `;
                    insightsContainer.appendChild(platformCard);
                }
            }

            // Show results
            if (loading) loading.classList.add('hidden');
            if (results) results.classList.remove('hidden');
        }

        // Get Platform-Specific Tips
        function getPlatformTip(platform, goal) {
            const tips = {
                'Instagram': {
                    'Engagement': 'Gunakan carousel format untuk engagement 2-3x lebih tinggi. Tambahkan pertanyaan di caption untuk boost comments.',
                    'Reach': 'Post pada jam 6-9 pagi atau 5-8 malam. Gunakan 20-30 hashtags dengan mix populer dan niche.',
                    'Conversion': 'Tambahkan link in bio CTA yang jelas. Gunakan story highlights untuk showcase produk.'
                },
                'TikTok': {
                    'Engagement': 'Hook 3 detik pertama crucial! Gunakan trending sounds dan effects untuk FYP.',
                    'Reach': 'Post 3-5x per hari untuk maximize reach. Durasi ideal 7-15 detik untuk completion rate tinggi.',
                    'Conversion': 'Gunakan TikTok Shop integration. Showcase before/after atau unboxing untuk conversions.'
                },
                'YouTube': {
                    'Engagement': 'CTR thumbnail harus >10%. Pertanyaan di akhir video boost comments 40%.',
                    'Reach': 'SEO title dengan keywords! Upload saat subscriber online (check analytics).',
                    'Retention': 'Hook 15 detik pertama. Pattern interrupt setiap 2-3 menit untuk retention.'
                }
            };

            return tips[platform]?.[goal] || tips[platform]?.['Engagement'] || '';
        }

        // Reset Handler
        if (resetBtn) {
            resetBtn.addEventListener('click', () => {
                // Reset images
                Object.keys(variantImages).forEach(key => {
                    variantImages[key] = null;
                    if (variantInputs[key]) variantInputs[key].value = '';
                    if (variantPreviews[key]) variantPreviews[key].classList.add('hidden');
                });

                // Reset UI
                if (results) results.classList.add('hidden');
                if (placeholder) placeholder.classList.remove('hidden');
                if (variantsGrid) variantsGrid.innerHTML = '';
                if (insightsContainer) insightsContainer.innerHTML = '';

                validateForm();
            });
        }

        // Analyze Button Click Handler (button has type="button" — form submit won't fire on click)
        if (analyzeBtn) {
            analyzeBtn.addEventListener('click', analyzeVariants);
        }

        // Initialize
        validateForm();

    })();

    // ==================== ROI CALCULATOR ====================
    (() => {
        const apiKey = "";

        // Form Elements
        const form = document.getElementById('roi-form');
        const calculateBtn = document.getElementById('roi-calculate-btn');

        // Input Elements
        const pricePerUnit = document.getElementById('roi-price-per-unit');
        const expectedSales = document.getElementById('roi-expected-sales');
        const commission = document.getElementById('roi-commission');
        const marketingCost = document.getElementById('roi-marketing-cost');
        const contentCost = document.getElementById('roi-content-cost');
        const otherCost = document.getElementById('roi-other-cost');

        // Option Elements
        const campaignTypeOptions = document.getElementById('roi-campaign-type-options');
        const timeframeOptions = document.getElementById('roi-timeframe-options');
        const growthOptions = document.getElementById('roi-growth-options');

        // Result Elements
        const placeholder = document.getElementById('roi-placeholder');
        const loading = document.getElementById('roi-loading');
        const progressBar = document.getElementById('roi-progress-bar');
        const progressText = document.getElementById('roi-progress-text');
        const results = document.getElementById('roi-results');
        const metricsGrid = document.getElementById('roi-metrics-grid');
        const roiPercentage = document.getElementById('roi-percentage');
        const timelineContainer = document.getElementById('roi-timeline-container');
        const insightsContainer = document.getElementById('roi-insights-container');
        const resetBtn = document.getElementById('roi-reset-btn');

        // State
        let selectedCampaignType = 'Affiliate Marketing';
        let selectedTimeframe = 3;
        let selectedGrowth = 'Moderate';

        // Option Selection Handlers
        if (campaignTypeOptions) {
            campaignTypeOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;
                campaignTypeOptions.querySelectorAll('.option-btn-roi').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCampaignType = btn.dataset.value;
            });
        }

        if (timeframeOptions) {
            timeframeOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;
                timeframeOptions.querySelectorAll('.option-btn-roi').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedTimeframe = parseInt(btn.dataset.value);
            });
        }

        if (growthOptions) {
            growthOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;
                growthOptions.querySelectorAll('.option-btn-roi').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedGrowth = btn.dataset.value;
            });
        }

        // Form Validation
        function validateForm() {
            const isValid = pricePerUnit?.value &&
                           expectedSales?.value &&
                           commission?.value &&
                           marketingCost?.value;
            if (calculateBtn) {
                calculateBtn.disabled = !isValid;
            }
        }

        // Input change listeners
        [pricePerUnit, expectedSales, commission, marketingCost, contentCost, otherCost].forEach(input => {
            if (input) {
                input.addEventListener('input', validateForm);
            }
        });

        // Calculate ROI
        async function calculateROI(e) {
            e.preventDefault();

            // Get values
            const price = parseFloat(pricePerUnit.value) || 0;
            const sales = parseFloat(expectedSales.value) || 0;
            const commissionPercent = parseFloat(commission.value) || 0;
            const marketingBudget = parseFloat(marketingCost.value) || 0;
            const contentBudget = parseFloat(contentCost.value) || 0;
            const otherBudget = parseFloat(otherCost.value) || 0;

            // Calculate basic metrics
            const monthlyRevenue = price * sales * (commissionPercent / 100);
            const monthlyCosts = marketingBudget + contentBudget + otherBudget;
            const monthlyProfit = monthlyRevenue - monthlyCosts;
            const roi = monthlyCosts > 0 ? ((monthlyProfit / monthlyCosts) * 100) : 0;
            const breakEvenMonths = monthlyProfit > 0 ? Math.ceil(monthlyCosts / monthlyProfit) : 0;

            // Show loading
            if (placeholder) placeholder.classList.add('hidden');
            if (results) results.classList.add('hidden');
            if (loading) loading.classList.remove('hidden');
            if (calculateBtn) calculateBtn.disabled = true;

            try {
                // Simulate progress
                if (progressBar) progressBar.style.width = '30%';
                if (progressText) progressText.textContent = '30% complete';

                // Get AI insights
                const aiInsights = await getAIInsights({
                    campaignType: selectedCampaignType,
                    monthlyRevenue,
                    monthlyCosts,
                    monthlyProfit,
                    roi,
                    growthScenario: selectedGrowth,
                    timeframe: selectedTimeframe
                });

                if (progressBar) progressBar.style.width = '70%';
                if (progressText) progressText.textContent = '70% complete';

                // Calculate projections
                const projections = calculateProjections(monthlyProfit, selectedTimeframe, selectedGrowth);

                if (progressBar) progressBar.style.width = '100%';
                if (progressText) progressText.textContent = '100% complete';

                // Display results
                displayResults({
                    monthlyRevenue,
                    monthlyCosts,
                    monthlyProfit,
                    roi,
                    breakEvenMonths,
                    projections,
                    aiInsights
                });

            } catch (error) {
                console.error('Calculation error:', error);
                alert('Terjadi kesalahan saat menghitung ROI. Silakan coba lagi.');

                if (loading) loading.classList.add('hidden');
                if (placeholder) placeholder.classList.remove('hidden');
            } finally {
                if (calculateBtn) calculateBtn.disabled = false;
                if (progressBar) progressBar.style.width = '0%';
                if (progressText) progressText.textContent = '0% complete';
            }
        }

        // Get AI Insights
        async function getAIInsights(data) {
            const prompt = `Analyze this ${data.campaignType} campaign ROI:

REVENUE & COSTS:
- Monthly Revenue: Rp ${data.monthlyRevenue.toLocaleString('id-ID')}
- Monthly Costs: Rp ${data.monthlyCosts.toLocaleString('id-ID')}
- Monthly Profit: Rp ${data.monthlyProfit.toLocaleString('id-ID')}
- ROI: ${data.roi.toFixed(1)}%

SCENARIO: ${data.growthScenario} growth over ${data.timeframe} months

Provide analysis in this format:

PROFITABILITY ASSESSMENT: [1-2 sentences on whether this is good/bad ROI and why]

KEY RISKS (3 items):
- [Risk 1 with mitigation]
- [Risk 2 with mitigation]
- [Risk 3 with mitigation]

OPTIMIZATION RECOMMENDATIONS (5 specific tips):
1. [Actionable tip to increase revenue]
2. [Actionable tip to reduce costs]
3. [Actionable tip for better targeting]
4. [Actionable tip for scaling]
5. [Actionable tip for retention]

GROWTH STRATEGY: [2-3 sentences on recommended next steps to scale profitably]

Be specific to ${data.campaignType} industry. Fokus pada insights yang actionable untuk affiliate marketers!`;

            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

            const response = await fetch(apiUrl, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    contents: [{ parts: [{ text: prompt }] }],
                    generationConfig: {
                        temperature: 0.8,
                        maxOutputTokens: 2048
                    }
                })
            });

            if (!response.ok) {
                throw new Error(`API error: ${response.status}`);
            }

            const result = await response.json();
            const analysisText = result?.candidates?.[0]?.content?.parts?.[0]?.text || '';

            // Parse insights
            const assessmentMatch = analysisText.match(/PROFITABILITY ASSESSMENT:\s*(.+?)(?=\n\n|KEY RISKS|$)/is);
            const assessment = assessmentMatch ? assessmentMatch[1].trim() : '';

            const risksMatch = analysisText.match(/KEY RISKS[^\n]*:\s*([\s\S]*?)(?=\n\nOPTIMIZATION|$)/i);
            const risks = risksMatch ? risksMatch[1].trim().split('\n').filter(s => s.trim().startsWith('-')).map(s => s.replace(/^-\s*/, '')) : [];

            const recsMatch = analysisText.match(/OPTIMIZATION RECOMMENDATIONS[^\n]*:\s*([\s\S]*?)(?=\n\nGROWTH STRATEGY|$)/i);
            const recommendations = recsMatch ? recsMatch[1].trim().split('\n').filter(s => /^\d+\./.test(s.trim())).map(s => s.replace(/^\d+\.\s*/, '')) : [];

            const strategyMatch = analysisText.match(/GROWTH STRATEGY:\s*(.+?)$/is);
            const strategy = strategyMatch ? strategyMatch[1].trim() : '';

            return {
                assessment,
                risks,
                recommendations,
                strategy,
                rawAnalysis: analysisText
            };
        }

        // Calculate Projections
        function calculateProjections(monthlyProfit, months, growthScenario) {
            const growthRates = {
                'Conservative': 0.03,  // 3% monthly growth
                'Moderate': 0.07,      // 7% monthly growth
                'Aggressive': 0.12     // 12% monthly growth
            };

            const growthRate = growthRates[growthScenario] || 0.07;
            const projections = [];
            let cumulativeProfit = 0;

            for (let month = 1; month <= months; month++) {
                const monthlyGrowth = Math.pow(1 + growthRate, month - 1);
                const projectedProfit = monthlyProfit * monthlyGrowth;
                cumulativeProfit += projectedProfit;

                projections.push({
                    month,
                    monthlyProfit: projectedProfit,
                    cumulativeProfit
                });
            }

            return projections;
        }

        // Display Results
        function displayResults(data) {
            // Update ROI Percentage
            if (roiPercentage) {
                roiPercentage.textContent = `${data.roi >= 0 ? '+' : ''}${data.roi.toFixed(1)}%`;
            }

            // Create Metrics Cards
            if (metricsGrid) {
                metricsGrid.innerHTML = '';

                const metrics = [
                    {
                        label: 'Monthly Revenue',
                        value: `Rp ${data.monthlyRevenue.toLocaleString('id-ID')}`,
                        icon: 'fa-money-bill-trend-up',
                        type: 'neutral'
                    },
                    {
                        label: 'Monthly Costs',
                        value: `Rp ${data.monthlyCosts.toLocaleString('id-ID')}`,
                        icon: 'fa-coins',
                        type: 'neutral'
                    },
                    {
                        label: 'Monthly Profit',
                        value: `Rp ${data.monthlyProfit.toLocaleString('id-ID')}`,
                        icon: 'fa-sack-dollar',
                        type: data.monthlyProfit > 0 ? 'positive' : 'negative'
                    },
                    {
                        label: 'Break-Even Point',
                        value: data.breakEvenMonths > 0 ? `${data.breakEvenMonths} Month${data.breakEvenMonths > 1 ? 's' : ''}` : 'Already Profitable',
                        icon: 'fa-chart-line',
                        type: data.breakEvenMonths <= 3 ? 'positive' : data.breakEvenMonths <= 6 ? 'neutral' : 'negative'
                    }
                ];

                metrics.forEach(metric => {
                    const card = document.createElement('div');
                    card.className = `roi-metric-card ${metric.type}`;
                    card.innerHTML = `
                        <div class="flex items-center justify-between mb-2">
                            <span class="text-sm font-semibold text-gray-600">${metric.label}</span>
                            <i class="fas ${metric.icon} text-xl opacity-50"></i>
                        </div>
                        <div class="text-2xl font-bold text-gray-800">${metric.value}</div>
                    `;
                    metricsGrid.appendChild(card);
                });
            }

            // Create Timeline
            if (timelineContainer) {
                timelineContainer.innerHTML = '';

                data.projections.forEach((proj, index) => {
                    const item = document.createElement('div');
                    item.className = 'roi-timeline-item';
                    item.innerHTML = `
                        <div class="roi-timeline-dot"></div>
                        <div>
                            <div class="flex justify-between items-center mb-1">
                                <h5 class="font-bold text-gray-800">Month ${proj.month}</h5>
                                <span class="text-sm font-semibold text-orange-600">+Rp ${proj.monthlyProfit.toLocaleString('id-ID', {maximumFractionDigits: 0})}</span>
                            </div>
                            <div class="text-xs text-gray-600">Cumulative: Rp ${proj.cumulativeProfit.toLocaleString('id-ID', {maximumFractionDigits: 0})}</div>
                            <div class="mt-2 bg-gray-200 rounded-full h-2">
                                <div class="roi-chart-bar" style="width: ${Math.min((proj.cumulativeProfit / data.projections[data.projections.length - 1].cumulativeProfit) * 100, 100)}%"></div>
                            </div>
                        </div>
                    `;
                    timelineContainer.appendChild(item);
                });
            }

            // Create Insights
            if (insightsContainer) {
                insightsContainer.innerHTML = '';

                // Assessment
                if (data.aiInsights.assessment) {
                    const assessmentCard = document.createElement('div');
                    assessmentCard.className = `roi-insight-card ${data.roi >= 100 ? 'success' : data.roi >= 50 ? '' : 'warning'}`;
                    assessmentCard.innerHTML = `
                        <h5 class="font-bold text-gray-800 mb-2">
                            <i class="fas fa-chart-pie mr-2"></i>
                            Profitability Assessment
                        </h5>
                        <p class="text-sm text-gray-700">${data.aiInsights.assessment}</p>
                    `;
                    insightsContainer.appendChild(assessmentCard);
                }

                // Risks
                if (data.aiInsights.risks.length > 0) {
                    const risksCard = document.createElement('div');
                    risksCard.className = 'roi-insight-card warning';
                    risksCard.innerHTML = `
                        <h5 class="font-bold text-gray-800 mb-2">
                            <i class="fas fa-triangle-exclamation mr-2"></i>
                            Key Risks & Mitigations
                        </h5>
                        <ul class="text-sm text-gray-700 space-y-1">
                            ${data.aiInsights.risks.slice(0, 3).map(risk => `<li>• ${risk}</li>`).join('')}
                        </ul>
                    `;
                    insightsContainer.appendChild(risksCard);
                }

                // Recommendations
                if (data.aiInsights.recommendations.length > 0) {
                    const recsCard = document.createElement('div');
                    recsCard.className = 'roi-insight-card success';
                    recsCard.innerHTML = `
                        <h5 class="font-bold text-gray-800 mb-2">
                            <i class="fas fa-lightbulb mr-2"></i>
                            Optimization Recommendations
                        </h5>
                        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
                            ${data.aiInsights.recommendations.slice(0, 5).map(rec => `<li>${rec}</li>`).join('')}
                        </ol>
                    `;
                    insightsContainer.appendChild(recsCard);
                }

                // Growth Strategy
                if (data.aiInsights.strategy) {
                    const strategyCard = document.createElement('div');
                    strategyCard.className = 'roi-insight-card';
                    strategyCard.innerHTML = `
                        <h5 class="font-bold text-gray-800 mb-2">
                            <i class="fas fa-rocket mr-2"></i>
                            Growth Strategy
                        </h5>
                        <p class="text-sm text-gray-700">${data.aiInsights.strategy}</p>
                    `;
                    insightsContainer.appendChild(strategyCard);
                }

                // Priority Tags
                const tagsContainer = document.createElement('div');
                tagsContainer.className = 'flex flex-wrap gap-2 mt-4';
                const tags = [
                    { label: data.roi >= 100 ? 'High ROI' : data.roi >= 50 ? 'Medium ROI' : 'Low ROI', priority: data.roi >= 100 ? 'low' : data.roi >= 50 ? 'medium' : 'high' },
                    { label: `${selectedGrowth} Growth`, priority: 'medium' },
                    { label: selectedCampaignType, priority: 'low' }
                ];
                tags.forEach(tag => {
                    const span = document.createElement('span');
                    span.className = `roi-tag ${tag.priority}-priority`;
                    span.textContent = tag.label;
                    tagsContainer.appendChild(span);
                });
                insightsContainer.appendChild(tagsContainer);
            }

            // Show results
            if (loading) loading.classList.add('hidden');
            if (results) results.classList.remove('hidden');
        }

        // Reset Handler
        if (resetBtn) {
            resetBtn.addEventListener('click', () => {
                // Reset form
                if (form) form.reset();

                // Reset options to defaults
                if (campaignTypeOptions) {
                    campaignTypeOptions.querySelectorAll('.option-btn-roi').forEach(btn => {
                        btn.classList.toggle('selected', btn.dataset.value === 'Affiliate Marketing');
                    });
                }
                if (timeframeOptions) {
                    timeframeOptions.querySelectorAll('.option-btn-roi').forEach(btn => {
                        btn.classList.toggle('selected', btn.dataset.value === '3');
                    });
                }
                if (growthOptions) {
                    growthOptions.querySelectorAll('.option-btn-roi').forEach(btn => {
                        btn.classList.toggle('selected', btn.dataset.value === 'Moderate');
                    });
                }

                // Reset state
                selectedCampaignType = 'Affiliate Marketing';
                selectedTimeframe = 3;
                selectedGrowth = 'Moderate';

                // Reset UI
                if (results) results.classList.add('hidden');
                if (placeholder) placeholder.classList.remove('hidden');
                if (metricsGrid) metricsGrid.innerHTML = '';
                if (timelineContainer) timelineContainer.innerHTML = '';
                if (insightsContainer) insightsContainer.innerHTML = '';
                if (roiPercentage) roiPercentage.textContent = '0%';

                validateForm();
            });
        }

        // Calculate Button Click Handler (button has type="button" — form submit won't fire on click)
        if (calculateBtn) {
            calculateBtn.addEventListener('click', calculateROI);
        }

        // Initialize
        validateForm();

    })();

    // ==================== GRADUATION PHOTO ====================
    (() => {
        const apiKey = "";

        // Element References
        const fileInputArea = document.getElementById('graduation-file-input-area');
        const addPhotoBtn = document.getElementById('graduation-add-photo-btn');
        const graduationValidation = document.getElementById('graduation-validation');

        const togaStyleOptions = document.getElementById('graduation-toga-style-options');
        const backgroundOptions = document.getElementById('graduation-background-options');
        const poseOptions = document.getElementById('graduation-pose-options');
        const accessoriesOptions = document.getElementById('graduation-accessories-options');
        const aspectOptions = document.getElementById('graduation-aspect-options');

        // Background Upload Elements
        const backgroundInput = document.getElementById('graduation-background-input');
        const backgroundUploadArea = document.getElementById('graduation-background-upload-area');
        const backgroundPreview = document.getElementById('graduation-background-preview');
        const backgroundPreviewContainer = document.getElementById('graduation-background-preview-container');
        const removeBackgroundBtn = document.getElementById('graduation-remove-background');

        // Medal Upload Elements
        const medalInput = document.getElementById('graduation-medal-input');
        const medalUploadArea = document.getElementById('graduation-medal-upload-area');
        const medalPreview = document.getElementById('graduation-medal-preview');
        const medalPreviewContainer = document.getElementById('graduation-medal-preview-container');
        const removeMedalBtn = document.getElementById('graduation-remove-medal');

        // Certificate Upload Elements
        const certificateInput = document.getElementById('graduation-certificate-input');
        const certificateUploadArea = document.getElementById('graduation-certificate-upload-area');
        const certificatePreview = document.getElementById('graduation-certificate-preview');
        const certificatePreviewContainer = document.getElementById('graduation-certificate-preview-container');
        const removeCertificateBtn = document.getElementById('graduation-remove-certificate');

        const generateBtn = document.getElementById('graduation-generate-btn');

        const placeholder = document.getElementById('graduation-placeholder');
        const loading = document.getElementById('graduation-loading');
        const loadingText = document.getElementById('graduation-loading-text');
        const progressBar = document.getElementById('graduation-progress-bar');
        const progressText = document.getElementById('graduation-progress-text');
        const resultsContainer = document.getElementById('graduation-results-container');
        const resultsCount = document.getElementById('graduation-results-count');
        const resultsGrid = document.getElementById('graduation-results-grid');
        const downloadAllBtn = document.getElementById('graduation-download-all-btn');

        // State
        let photos = [null, null]; // Will store base64 strings
        let photoMimeTypes = [null, null]; // Will store mime types
        let customBackgroundData = null;
        let customMedalData = null;
        let customCertificateData = null;
        let selectedTogaStyle = 'Formal Indonesian';
        let selectedBackground = 'University Campus';
        let selectedPose = 'Formal Portrait';
        let selectedAccessories = ['Medal', 'Certificate'];
        let selectedAspect = '3:4';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedImages = [];

        // Count selection (1-10), default 4
        const graduationCountSel = document.getElementById('graduation-count-selection-grid');
        if (graduationCountSel) {
            graduationCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                graduationCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Multiple File Input Handler
        function updateFileInputs() {
            if (!fileInputArea) return;
            fileInputArea.innerHTML = '';
            photos.forEach((photo, i) => {
                const wrapper = document.createElement('div');
                wrapper.className = 'file-input-wrapper';
                if (photo) wrapper.classList.add('has-photo');

                const input = document.createElement('input');
                input.type = 'file';
                input.accept = 'image/png, image/jpeg, image/webp, image/heic, .heic, .HEIC';
                input.className = 'file-input';
                input.dataset.index = i;
                input.addEventListener('change', handleFileChange);

                const preview = document.createElement('img');
                preview.className = 'file-preview';
                if (photo) {
                    preview.src = `data:${photoMimeTypes[i]};base64,${photo}`;
                } else {
                    preview.classList.add('hidden');
                }

                const placeholder = document.createElement('span');
                placeholder.className = 'text-gray-500 text-center text-sm flex items-center justify-center h-full pointer-events-none';
                if (!photo) {
                    placeholder.innerHTML = '<div><i class="fas fa-user-graduate text-2xl mb-2" style="color: #a855f7;"></i><br><span class="text-xs sm:text-sm">Tambah Foto</span></div>';
                }

                const deleteBtn = document.createElement('button');
                deleteBtn.className = 'delete-btn';
                deleteBtn.innerHTML = '×';
                deleteBtn.type = 'button';

                // Mobile-friendly click handler
                deleteBtn.addEventListener('click', (e) => graduationDeletePhoto(e, i));
                deleteBtn.addEventListener('touchstart', (e) => {
                    e.stopPropagation();
                    deleteBtn.style.transform = 'scale(0.9)';
                }, { passive: true });
                deleteBtn.addEventListener('touchend', (e) => {
                    e.stopPropagation();
                    deleteBtn.style.transform = '';
                }, { passive: true });

                wrapper.append(input, preview, placeholder, deleteBtn);
                fileInputArea.appendChild(wrapper);
            });
            updateValidation();
        }

        async function handleFileChange(e) {
            const index = parseInt(e.target.dataset.index, 10);
            const file = e.target.files[0];
            if (!file) return;

            // Show loading state
            const wrapper = e.target.closest('.file-input-wrapper');
            const placeholder = wrapper.querySelector('span');
            if (placeholder && !wrapper.classList.contains('has-photo')) {
                placeholder.innerHTML = '<div><i class="fas fa-spinner fa-spin text-2xl mb-2" style="color: #a855f7;"></i><br><span class="text-xs">Memproses...</span></div>';
            }

            let processedFile = file;

            // Convert HEIC to JPEG if needed
            if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                try {
                    if (graduationValidation) {
                        graduationValidation.textContent = 'Mengkonversi HEIC...';
                        graduationValidation.className = 'text-center text-sm mt-3 font-medium text-blue-600';
                    }

                    const convertedBlob = await heic2any({
                        blob: file,
                        toType: 'image/jpeg',
                        quality: 0.9
                    });
                    processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                        type: 'image/jpeg'
                    });
                } catch (err) {
                    console.error('HEIC conversion failed:', err);
                    if (graduationValidation) {
                        graduationValidation.textContent = '❌ Gagal mengkonversi gambar HEIC';
                        graduationValidation.className = 'text-center text-sm mt-3 font-medium text-red-600';
                    }
                    updateFileInputs(); // Reset UI
                    return;
                }
            }

            const reader = new FileReader();
            reader.onload = (event) => {
                photos[index] = event.target.result.split(',')[1];
                photoMimeTypes[index] = processedFile.type;
                updateFileInputs();
            };
            reader.readAsDataURL(processedFile);
        }

        window.graduationDeletePhoto = (event, index) => {
            event.preventDefault();
            event.stopPropagation();

            // Visual feedback
            const wrapper = event.target.closest('.file-input-wrapper');
            if (wrapper) {
                wrapper.style.transition = 'transform 0.2s, opacity 0.2s';
                wrapper.style.transform = 'scale(0.9)';
                wrapper.style.opacity = '0.5';
            }

            setTimeout(() => {
                photos[index] = null;
                photoMimeTypes[index] = null;
                // Only remove the slot if there are more than 2
                if (photos.length > 2) {
                    photos.splice(index, 1);
                    photoMimeTypes.splice(index, 1);
                }
                updateFileInputs();
            }, 150);
        };

        function updateValidation() {
            if (!graduationValidation) return;
            const uploadedCount = photos.filter(p => p !== null).length;

            if (uploadedCount === 0) {
                graduationValidation.textContent = 'Silakan upload minimal 1 foto';
                graduationValidation.className = 'text-center text-sm mt-3 font-medium text-gray-500';
                if (generateBtn) generateBtn.disabled = true;
            } else {
                graduationValidation.textContent = `✓ ${uploadedCount} foto siap digunakan`;
                graduationValidation.className = 'text-center text-sm mt-3 font-medium text-purple-600';
                if (generateBtn) generateBtn.disabled = false;
            }
        }

        if (addPhotoBtn) {
            addPhotoBtn.addEventListener('click', () => {
                if (photos.length < 5) {
                    photos.push(null);
                    photoMimeTypes.push(null);
                    updateFileInputs();

                    // Scroll to bottom to show new input on mobile
                    if (window.innerWidth < 640) {
                        setTimeout(() => {
                            const lastWrapper = fileInputArea.lastElementChild;
                            if (lastWrapper) {
                                lastWrapper.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
                            }
                        }, 100);
                    }
                } else {
                    if (graduationValidation) {
                        graduationValidation.textContent = '❌ Maksimal 5 foto';
                        graduationValidation.className = 'text-center text-sm mt-3 font-medium text-red-600';
                        setTimeout(() => {
                            updateValidation();
                        }, 2000);
                    }
                }
            });

            // Mobile touch feedback
            addPhotoBtn.addEventListener('touchstart', () => {
                addPhotoBtn.style.transform = 'scale(0.95)';
            }, { passive: true });

            addPhotoBtn.addEventListener('touchend', () => {
                addPhotoBtn.style.transform = '';
            }, { passive: true });
        }

        // Background Upload Handler with HEIC Support
        if (backgroundInput) {
            backgroundInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    customBackgroundData = reader.result;

                    // Show background preview
                    if (backgroundPreview && backgroundPreviewContainer && backgroundUploadArea) {
                        backgroundPreview.src = reader.result;
                        backgroundUploadArea.classList.add('hidden');
                        backgroundPreviewContainer.classList.remove('hidden');
                    }
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove Background Handler
        if (removeBackgroundBtn) {
            removeBackgroundBtn.addEventListener('click', () => {
                customBackgroundData = null;
                if (backgroundInput) backgroundInput.value = '';
                if (backgroundUploadArea) backgroundUploadArea.classList.remove('hidden');
                if (backgroundPreviewContainer) backgroundPreviewContainer.classList.add('hidden');
                if (backgroundPreview) backgroundPreview.src = '';
            });
        }

        // Medal Upload Handler
        if (medalInput) {
            medalInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                const reader = new FileReader();
                reader.onloadend = () => {
                    customMedalData = reader.result;

                    // Show medal preview
                    if (medalPreview && medalPreviewContainer && medalUploadArea) {
                        medalPreview.src = reader.result;
                        medalUploadArea.classList.add('hidden');
                        medalPreviewContainer.classList.remove('hidden');
                    }
                };
                reader.readAsDataURL(file);
            });
        }

        // Remove Medal Handler
        if (removeMedalBtn) {
            removeMedalBtn.addEventListener('click', () => {
                customMedalData = null;
                if (medalInput) medalInput.value = '';
                if (medalUploadArea) medalUploadArea.classList.remove('hidden');
                if (medalPreviewContainer) medalPreviewContainer.classList.add('hidden');
                if (medalPreview) medalPreview.src = '';
            });
        }

        // Certificate Upload Handler
        if (certificateInput) {
            certificateInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                const reader = new FileReader();
                reader.onloadend = () => {
                    customCertificateData = reader.result;

                    // Show certificate preview
                    if (certificatePreview && certificatePreviewContainer && certificateUploadArea) {
                        certificatePreview.src = reader.result;
                        certificateUploadArea.classList.add('hidden');
                        certificatePreviewContainer.classList.remove('hidden');
                    }
                };
                reader.readAsDataURL(file);
            });
        }

        // Remove Certificate Handler
        if (removeCertificateBtn) {
            removeCertificateBtn.addEventListener('click', () => {
                customCertificateData = null;
                if (certificateInput) certificateInput.value = '';
                if (certificateUploadArea) certificateUploadArea.classList.remove('hidden');
                if (certificatePreviewContainer) certificatePreviewContainer.classList.add('hidden');
                if (certificatePreview) certificatePreview.src = '';
            });
        }

        // Toga Style Selection
        if (togaStyleOptions) {
            togaStyleOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                togaStyleOptions.querySelectorAll('.option-btn-graduation').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedTogaStyle = btn.dataset.value;
            });
        }

        // Background Selection
        if (backgroundOptions) {
            backgroundOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                backgroundOptions.querySelectorAll('.option-btn-graduation').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedBackground = btn.dataset.value;
            });
        }

        // Pose Selection
        if (poseOptions) {
            poseOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                poseOptions.querySelectorAll('.option-btn-graduation').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedPose = btn.dataset.value;
            });
        }

        // Accessories Selection (Multi-select)
        if (accessoriesOptions) {
            accessoriesOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                const value = btn.dataset.value;
                const isActive = btn.classList.contains('active');
                const icon = btn.querySelector('.fas');

                if (isActive) {
                    btn.classList.remove('active');
                    if (icon) icon.classList.add('hidden');
                    selectedAccessories = selectedAccessories.filter(a => a !== value);
                } else {
                    btn.classList.add('active');
                    if (icon) icon.classList.remove('hidden');
                    selectedAccessories.push(value);
                }
            });
        }

        // Aspect Ratio Selection
        if (aspectOptions) {
            aspectOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn) return;

                aspectOptions.querySelectorAll('.option-btn-graduation').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAspect = btn.dataset.value;
            });
        }

        // Generate Graduation Photos
        async function generateGraduationPhotos() {
            const uploadedFiles = photos.map((p, i) => ({ base64: p, mimeType: photoMimeTypes[i] })).filter(f => f.base64);

            if (uploadedFiles.length === 0) {
                alert('Silakan upload minimal 1 foto terlebih dahulu!');
                return;
            }

            if (placeholder) placeholder.classList.add('hidden');
            if (resultsContainer) resultsContainer.classList.add('hidden');
            if (loading) loading.classList.remove('hidden');
            if (generateBtn) generateBtn.disabled = true;

            let attempts = 0;
            const MAX_ATTEMPTS = 3;
            let successCount = 0;

            while (attempts < MAX_ATTEMPTS && successCount === 0) {
                attempts++;
                generatedImages = [];
                if (resultsGrid) resultsGrid.innerHTML = '';

                // Pre-create placeholder cards
                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `graduation-card-${i}`;
                    card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-[3/4]';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2" style="border-color: #a855f7;"></div>';
                    if (resultsGrid) resultsGrid.appendChild(card);
                }

                const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                    generateSingleGraduationPhoto(i + 1, uploadedFiles)
                );
                await Promise.allSettled(generationPromises);

                // Remove empty cards
                if (resultsGrid) {
                    resultsGrid.querySelectorAll('[id^="graduation-card-"]').forEach(card => {
                        if (!card.querySelector('img')) card.remove();
                    });
                }
                successCount = generatedImages.filter(Boolean).length;
            }

            if (loading) loading.classList.add('hidden');

            if (successCount === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                if (placeholder) placeholder.classList.remove('hidden');
            } else {
                if (resultsCount) resultsCount.textContent = successCount;
                if (resultsContainer) resultsContainer.classList.remove('hidden');
                if (window.innerWidth < 1024) {
                    setTimeout(() => {
                        if (resultsContainer) resultsContainer.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }, 300);
                }
            }

            if (generateBtn) generateBtn.disabled = false;
        }

        // Generate Single Graduation Photo
        async function generateSingleGraduationPhoto(index, uploadedFiles) {
            const card = document.getElementById(`graduation-card-${index}`);

            try {
                // Build accessories description and collect uploaded accessories
                let accessoriesDesc = '';
                let uploadedAccessories = [];

                if (customMedalData) {
                    uploadedAccessories.push('medal');
                }
                if (customCertificateData) {
                    uploadedAccessories.push('certificate');
                }

                // Build text description for non-uploaded accessories
                const textAccessories = selectedAccessories.filter(acc => {
                    if (acc === 'Medal' && customMedalData) return false;
                    if (acc === 'Certificate' && customCertificateData) return false;
                    return true;
                });

                if (textAccessories.length > 0 || uploadedAccessories.length > 0) {
                    let parts = [];
                    if (uploadedAccessories.length > 0) {
                        parts.push(`Use the uploaded ${uploadedAccessories.join(' and ')} images in the photo`);
                    }
                    if (textAccessories.length > 0) {
                        parts.push(`Include these AI-generated accessories: ${textAccessories.join(', ')}`);
                    }
                    accessoriesDesc = parts.join('. ') + '.';
                }

                let prompt = '';
                let payloadParts = [];

                // Count total images to attach
                let imageIndex = 1;
                const imageDescriptions = [];

                // Check if custom background is uploaded
                if (customBackgroundData) {
                    // With custom background - composite mode
                    const base64Background = customBackgroundData.split(',')[1];
                    imageDescriptions.push(`IMAGE ${imageIndex} = CUSTOM BACKGROUND (use this as the location/environment)`);
                    imageIndex++;

                    imageDescriptions.push(`IMAGE ${imageIndex} = PERSON'S FACE (dress this person in graduation attire)`);
                    imageIndex++;

                    if (customMedalData) {
                        imageDescriptions.push(`IMAGE ${imageIndex} = MEDAL (place this medal on the person)`);
                        imageIndex++;
                    }
                    if (customCertificateData) {
                        imageDescriptions.push(`IMAGE ${imageIndex} = CERTIFICATE/IJAZAH (the person should hold this certificate)`);
                        imageIndex++;
                    }

                    const numPeople = uploadedFiles.length;
                    const peopleText = numPeople === 1 ? '1 PERSON' : `ALL ${numPeople} PEOPLE`;
                    prompt = `Create a professional graduation photo using these ${imageIndex - 1 + numPeople - 1} separate images:

${imageDescriptions.join('\n')}
${numPeople > 1 ? `IMAGES ${imageIndex}-${imageIndex + numPeople - 1} = ${numPeople} PEOPLE'S FACES (show ALL people together in graduation attire)` : ''}

IMPORTANT: This photo should show ${peopleText}${numPeople > 1 ? ' TOGETHER in one scene' : ''}.

TOGA STYLE: ${selectedTogaStyle}
- High-quality graduation gown/toga with proper academic regalia
- Mortarboard cap (topi wisuda) positioned correctly
- Professional and formal appearance
- Authentic Indonesian university graduation style

POSE & EXPRESSION: ${selectedPose}
- Natural and confident posture
- Professional facial expression
- Looking at camera with pride and happiness
${selectedPose === 'Happy Smile' ? '- Warm genuine smile showing joy and achievement' : ''}
${selectedPose === 'Proud Confident' ? '- Confident and proud expression of accomplishment' : ''}
${selectedPose === 'Joyful Celebratory' ? '- Celebratory and joyful expression' : ''}

ACCESSORIES:
${accessoriesDesc}

COMPOSITING REQUIREMENTS:
- Background/environment MUST be from CUSTOM BACKGROUND image
- Person/face MUST be from PERSON image (keep identity EXACTLY the same!)
- Ignore background from PERSON image completely
${customMedalData ? '- Use the EXACT medal from MEDAL image (place on chest/around neck naturally)' : ''}
${customCertificateData ? '- Use the EXACT certificate from CERTIFICATE image (person should hold it naturally)' : ''}
- Blend lighting naturally so everything fits together
- Match lighting, shadows, and colors for realistic composition
- Make it look like a real photo taken in that location

PHOTO QUALITY:
- Professional photography quality
- Sharp focus on face, toga, and accessories
- Natural skin tones
- High resolution and detail
- Suitable for printing and framing
- Realistic and professional result! 🎓✨`;

                    payloadParts = [
                        { text: prompt },
                        { inlineData: { mimeType: 'image/jpeg', data: base64Background } }
                    ];

                    // Add all uploaded person photos
                    uploadedFiles.forEach(file => {
                        payloadParts.push({ inlineData: { mimeType: file.mimeType, data: file.base64 } });
                    });

                    // Add medal if uploaded
                    if (customMedalData) {
                        const base64Medal = customMedalData.split(',')[1];
                        payloadParts.push({ inlineData: { mimeType: 'image/jpeg', data: base64Medal } });
                    }

                    // Add certificate if uploaded
                    if (customCertificateData) {
                        const base64Certificate = customCertificateData.split(',')[1];
                        payloadParts.push({ inlineData: { mimeType: 'image/jpeg', data: base64Certificate } });
                    }

                } else {
                    // Without custom background - use AI generated background

                    // Build image descriptions for non-background mode
                    const noBackgroundImageDescriptions = [];
                    let noBackgroundImageIndex = 1;
                    const numPeople = uploadedFiles.length;

                    if (numPeople === 1) {
                        noBackgroundImageDescriptions.push(`IMAGE ${noBackgroundImageIndex} = PERSON'S FACE (this is the person to dress in graduation attire)`);
                        noBackgroundImageIndex++;
                    } else {
                        noBackgroundImageDescriptions.push(`IMAGES ${noBackgroundImageIndex}-${noBackgroundImageIndex + numPeople - 1} = ${numPeople} PEOPLE'S FACES (ALL people to dress in graduation attire TOGETHER)`);
                        noBackgroundImageIndex += numPeople;
                    }

                    if (customMedalData) {
                        noBackgroundImageDescriptions.push(`IMAGE ${noBackgroundImageIndex} = MEDAL (place this specific medal on the person)`);
                        noBackgroundImageIndex++;
                    }
                    if (customCertificateData) {
                        noBackgroundImageDescriptions.push(`IMAGE ${noBackgroundImageIndex} = CERTIFICATE/IJAZAH (the person should hold this specific certificate)`);
                        noBackgroundImageIndex++;
                    }

                    prompt = `Create a professional graduation photo${customMedalData || customCertificateData ? ` using these ${noBackgroundImageIndex - 1} images:\n\n${noBackgroundImageDescriptions.join('\n')}\n\n` : ' with the following specifications:\n\n'}TOGA STYLE: ${selectedTogaStyle}
- High-quality graduation gown/toga with proper academic regalia
- Mortarboard cap (topi wisuda) positioned correctly
- Professional and formal appearance
- Authentic Indonesian university graduation style

BACKGROUND: ${selectedBackground}
- Professional and prestigious setting
- Well-lit and high quality
- Appropriate for formal graduation portrait
${selectedBackground === 'University Campus' ? '- Show iconic university building architecture' : ''}
${selectedBackground === 'Studio Professional' ? '- Clean studio backdrop with soft lighting' : ''}
${selectedBackground === 'Outdoor Garden' ? '- Beautiful garden with greenery' : ''}
${selectedBackground === 'Library Academic' ? '- Classic library interior with books' : ''}

POSE & EXPRESSION: ${selectedPose}
- Natural and confident posture
- Professional facial expression
- Looking at camera with pride and happiness
${selectedPose === 'Happy Smile' ? '- Warm genuine smile showing joy and achievement' : ''}
${selectedPose === 'Proud Confident' ? '- Confident and proud expression of accomplishment' : ''}
${selectedPose === 'Joyful Celebratory' ? '- Celebratory and joyful expression' : ''}

ACCESSORIES:
${accessoriesDesc}
${customMedalData ? '- IMPORTANT: Use the EXACT medal from the MEDAL image (place on chest/around neck naturally)' : ''}
${customCertificateData ? '- IMPORTANT: Use the EXACT certificate from the CERTIFICATE image (person should hold it naturally)' : ''}

PHOTO QUALITY:
- Professional photography quality
- Sharp focus on face, toga, and accessories
- Proper lighting (soft, flattering light)
- Natural skin tones
- High resolution and detail
- Suitable for printing and framing
- Perfect composition and framing

CRITICAL: Keep the person's face and identity EXACTLY the same from the PERSON image! Only add graduation attire, pose, background, and accessories. The final photo must look REALISTIC and PROFESSIONAL - perfect for graduation album, social media, and family memories! 🎓✨`;

                    payloadParts = [
                        { text: prompt }
                    ];

                    // Add all uploaded person photos
                    uploadedFiles.forEach(file => {
                        payloadParts.push({ inlineData: { mimeType: file.mimeType, data: file.base64 } });
                    });

                    // Add medal if uploaded
                    if (customMedalData) {
                        const base64Medal = customMedalData.split(',')[1];
                        payloadParts.push({ inlineData: { mimeType: 'image/jpeg', data: base64Medal } });
                    }

                    // Add certificate if uploaded
                    if (customCertificateData) {
                        const base64Certificate = customCertificateData.split(',')[1];
                        payloadParts.push({ inlineData: { mimeType: 'image/jpeg', data: base64Certificate } });
                    }
                }

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{ parts: payloadParts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedAspect }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });
                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/png;base64,${imageData}`;
                const filename = `graduation-photo-${Date.now()}-${index}.png`;
                generatedImages[index - 1] = { url: imageUrl, filename };

                if (card) {
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Graduation Photo ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="graduation-action-btn bg-white/90 hover:bg-white text-gray-800">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline ml-1">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="graduation-action-btn text-white" style="background: linear-gradient(135deg, #a855f7, #ec4899);">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline ml-1">Download</span>
                            </button>
                        </div>
                    `;
                }
            } catch (error) {
                console.error(`Error generating photo ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event Delegation for Action Buttons (Mobile-Friendly)
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download All Handler
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        // Generate Button Click Handler
        if (generateBtn) {
            generateBtn.addEventListener('click', generateGraduationPhotos);
        }

        // Initialize file inputs on page load
        updateFileInputs();

    })();

    // ==================== MASCOT GENERATOR ====================
    (() => {
        const mascotRefInput = document.getElementById('mascot-ref-input');
        const mascotRefUploadBox = document.getElementById('mascot-ref-upload-box');
        const mascotRefPreview = document.getElementById('mascot-ref-preview');
        const mascotRefPlaceholder = document.getElementById('mascot-ref-placeholder');
        const mascotRefRemoveBtn = document.getElementById('mascot-ref-remove-btn');
        const mascotName = document.getElementById('mascot-name');
        const mascotDescription = document.getElementById('mascot-description');
        const mascotTypeOptions = document.getElementById('mascot-type-options');
        const mascotStyleOptions = document.getElementById('mascot-style-options');
        const mascotPersonalityOptions = document.getElementById('mascot-personality-options');
        const mascotPoseOptions = document.getElementById('mascot-pose-options');
        const mascotBgOptions = document.getElementById('mascot-bg-options');
        const mascotGenerateBtn = document.getElementById('mascot-generate-btn');
        const mascotResultsPlaceholder = document.getElementById('mascot-results-placeholder');
        const mascotResultsContainer = document.getElementById('mascot-results-container');
        const mascotResultsGrid = document.getElementById('mascot-results-grid');
        const mascotRefDisplayImg = document.getElementById('mascot-ref-display-img');

        if (!mascotRefInput) return;

        const apiKey = "";
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let mascotRefImageData = null;
        let generatedMascots = [];

        // Count selection (1-10), default 4
        const mascotCountSel = document.getElementById('mascot-count-selection-grid');
        if (mascotCountSel) {
            mascotCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                mascotCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Setup option buttons
        setupOptionButtonsNew(mascotTypeOptions);
        setupOptionButtonsNew(mascotStyleOptions);
        setupOptionButtonsNew(mascotPoseOptions);
        setupOptionButtonsNew(mascotBgOptions);

        // Setup checkbox buttons for personality (multi-select)
        if (mascotPersonalityOptions) {
            mascotPersonalityOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('.checkbox-btn-mascot');
                if (btn) {
                    btn.classList.toggle('active');
                }
            });
        }

        // Reference image upload (REQUIRED)
        setupImageUploadNew(mascotRefInput, mascotRefUploadBox, (data) => {
            mascotRefImageData = data;
            mascotRefPreview.src = data.dataUrl;
            mascotRefPlaceholder.classList.add('hidden');
            mascotRefPreview.classList.remove('hidden');
            mascotRefRemoveBtn.classList.remove('hidden');
            updateMascotGenerateButton();
        });

        if (mascotRefRemoveBtn) {
            mascotRefRemoveBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                mascotRefImageData = null;
                mascotRefInput.value = '';
                mascotRefPreview.src = '#';
                mascotRefPreview.classList.add('hidden');
                mascotRefPlaceholder.classList.remove('hidden');
                mascotRefRemoveBtn.classList.add('hidden');
                updateMascotGenerateButton();
            });
        }

        // Update generate button state
        function updateMascotGenerateButton() {
            const hasRef = !!mascotRefImageData;
            const hasName = mascotName.value.trim() !== '';
            const hasDesc = mascotDescription.value.trim() !== '';
            mascotGenerateBtn.disabled = !hasRef || !hasName || !hasDesc;
        }

        [mascotName, mascotDescription].forEach(el => {
            if (el) el.addEventListener('input', updateMascotGenerateButton);
        });

        // Generate mascots
        if (mascotGenerateBtn) {
            mascotGenerateBtn.addEventListener('click', async () => {
                if (!mascotRefImageData) {
                    alert('Upload gambar referensi mascot terlebih dahulu!');
                    return;
                }

                const name = mascotName.value.trim();
                const description = mascotDescription.value.trim();

                if (!name || !description) {
                    alert('Mohon isi Nama Mascot dan Deskripsi Karakter!');
                    return;
                }

                // Get selected options
                const type = mascotTypeOptions.querySelector('.selected')?.dataset.value || 'Character';
                const style = mascotStyleOptions.querySelector('.selected')?.dataset.value || 'Cute Kawaii';
                const pose = mascotPoseOptions.querySelector('.selected')?.dataset.value || 'Standing';
                const background = mascotBgOptions.querySelector('.selected')?.dataset.value || 'transparent';

                // Get selected personalities (multi-select)
                const personalities = Array.from(mascotPersonalityOptions.querySelectorAll('.active'))
                    .map(btn => btn.dataset.value);
                const personalityString = personalities.length > 0 ? personalities.join(', ') : 'Friendly';

                // Save original button state
                const originalBtnHTML = mascotGenerateBtn.innerHTML;
                mascotGenerateBtn.disabled = true;
                mascotGenerateBtn.innerHTML = `<div class="loader-icon-mascot mr-2"></div><span>Generating Mascots...</span>`;

                // Hide placeholder, show results
                mascotResultsPlaceholder.classList.add('hidden');
                mascotResultsContainer.classList.remove('hidden');
                mascotResultsGrid.innerHTML = '';

                // Set reference display
                mascotRefDisplayImg.src = mascotRefImageData.dataUrl;

                // Pre-create placeholder cards
                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `mascot-card-${i}`;
                    card.className = 'mascot-preview-card p-4';
                    card.innerHTML = `
                        <div class="aspect-square bg-gray-100 rounded-lg flex items-center justify-center relative">
                            <div class="loader-icon-mascot"></div>
                            <span class="mascot-variation-badge">Variation ${i}</span>
                        </div>
                        <div class="mt-3 text-center">
                            <p class="text-sm text-gray-600">Generating variation ${i}...</p>
                        </div>
                    `;
                    mascotResultsGrid.appendChild(card);
                }

                generatedMascots = [];
                let attempts = 0;
                const MAX_ATTEMPTS = 3;

                while (attempts < MAX_ATTEMPTS) {
                    attempts++;
                    await Promise.allSettled(
                        Array.from({ length: selectedCount }, (_, i) =>
                            generateSingleMascotCard(i + 1, name, description, type, style, pose, background, personalityString)
                        )
                    );
                    const successCount = generatedMascots.filter(Boolean).length;
                    if (successCount > 0) break;
                }

                // Remove empty spinner cards
                document.querySelectorAll('[id^="mascot-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                generatedMascots = generatedMascots.filter(Boolean);

                if (generatedMascots.length === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    mascotResultsPlaceholder.classList.remove('hidden');
                    mascotResultsContainer.classList.add('hidden');
                } else {
                    showMascotModal('Mascot Generator', `\uD83C\uDF89 Berhasil! ${name} punya ${generatedMascots.length} variasi mascot keren!`, 'success');
                }

                mascotGenerateBtn.disabled = false;
                mascotGenerateBtn.innerHTML = originalBtnHTML;
            });
        }

        // Generate a single mascot card (fail-silent wrapper)
        async function generateSingleMascotCard(index, name, description, type, style, pose, background, personalityString) {
            const card = document.getElementById(`mascot-card-${index}`);
            try {
                const variationPrompt = buildMascotPrompt(name, description, type, style, pose, background, personalityString, index);
                const mascotImage = await generateSingleMascot(variationPrompt, mascotRefImageData);
                generatedMascots[index - 1] = mascotImage;
                if (card) {
                    card.innerHTML = `
                        <div class="aspect-square bg-white rounded-lg flex items-center justify-center relative overflow-hidden border-2 border-gray-200 p-2">
                            <img src="${mascotImage}" alt="Mascot Variation ${index}" class="w-full h-full object-contain">
                            <span class="mascot-variation-badge">V${index}</span>
                        </div>
                        <div class="mt-3 space-y-2">
                            <div class="flex gap-2">
                                <button class="mascot-action-btn flex-1 text-sm" data-action="mascot-preview" data-image="${mascotImage}">
                                    <i class="fas fa-eye mr-1"></i> View
                                </button>
                                <button class="mascot-action-btn flex-1 text-sm" data-action="mascot-download" data-image="${mascotImage}" data-filename="${name.replace(/\s+/g, '_')}_mascot_v${index}.png">
                                    <i class="fas fa-download mr-1"></i> Download
                                </button>
                            </div>
                            <p class="text-xs text-center text-gray-600">${pose} pose</p>
                        </div>
                    `;
                }
            } catch (err) {
                console.error(`Error generating mascot variation ${index}:`, err);
                if (card) card.innerHTML = '';
            }
        }

        // Build prompt for mascot generation with reference image
        function buildMascotPrompt(name, description, type, style, pose, background, personality, variationNumber) {
            let prompt = `CRITICAL INSTRUCTION: Analyze the provided reference image carefully and create a NEW mascot character that follows the EXACT SAME visual style, design approach, and artistic characteristics.\n\n`;

            prompt += `MASCOT SPECIFICATIONS:\n`;
            prompt += `- Character Name: "${name}"\n`;
            prompt += `- Character Type: ${type}\n`;
            prompt += `- Description: ${description}\n`;
            prompt += `- Personality Traits: ${personality}\n\n`;

            prompt += `REFERENCE IMAGE ANALYSIS:\n`;
            prompt += `You are provided with a reference image showing a mascot/character. Study it carefully and identify:\n`;
            prompt += `1. The overall art style (line work, shading, rendering technique)\n`;
            prompt += `2. The proportions and body structure\n`;
            prompt += `3. The level of detail and complexity\n`;
            prompt += `4. The color palette and color usage style\n`;
            prompt += `5. The character design principles (cute factors, exaggeration, simplification)\n`;
            prompt += `6. The visual weight and balance\n`;
            prompt += `7. The drawing/rendering technique (2D flat, 3D render, sketch, etc.)\n\n`;

            prompt += `YOUR TASK:\n`;
            prompt += `Create a COMPLETELY NEW ${type} mascot character named "${name}" that:\n`;
            prompt += `- Uses the EXACT SAME art style as the reference\n`;
            prompt += `- Has similar proportions and body structure principles\n`;
            prompt += `- Matches the level of detail and rendering quality\n`;
            prompt += `- Follows the same design philosophy and aesthetic\n`;
            prompt += `- BUT is a unique character with its own identity based on: ${description}\n\n`;

            prompt += `VISUAL STYLE REQUIREMENTS:\n`;
            prompt += `- Primary Style: ${style}\n`;
            prompt += `- The character MUST look like it was designed by the same artist as the reference\n`;
            prompt += `- Match the color harmony approach from the reference\n`;
            prompt += `- Use similar line weights and outlines as the reference\n`;
            prompt += `- Apply the same level of shading/highlights as seen in reference\n\n`;

            prompt += `POSE & COMPOSITION:\n`;
            prompt += `- Pose: ${pose}\n`;
            prompt += `- The pose should be natural and fit the character's personality: ${personality}\n`;
            prompt += `- Center the character in the frame\n`;
            prompt += `- Ensure the full character is visible\n\n`;

            prompt += `BACKGROUND:\n`;
            prompt += `- Background type: ${background}\n`;
            if (background === 'transparent') {
                prompt += `- Use a clean transparent or solid white background\n`;
                prompt += `- No environment elements, just the character\n`;
            } else if (background === 'solid color') {
                prompt += `- Use a simple solid color background that complements the character\n`;
            } else {
                prompt += `- Use a subtle gradient background that doesn't distract from the character\n`;
            }

            prompt += `\nVARIATION ${variationNumber} SPECIFIC INSTRUCTIONS:\n`;
            if (variationNumber === 1) {
                prompt += `- This is the most faithful interpretation of the reference style\n`;
                prompt += `- Stay closest to the reference's design principles\n`;
            } else if (variationNumber === 2) {
                prompt += `- Add slight personality variations while keeping the core style\n`;
                prompt += `- Emphasize the ${personality} traits more\n`;
            } else if (variationNumber === 3) {
                prompt += `- Introduce minor style evolution while maintaining consistency\n`;
                prompt += `- Adjust proportions slightly but keep the reference aesthetic\n`;
            } else if (variationNumber === 4) {
                prompt += `- Create a more expressive version with enhanced personality\n`;
                prompt += `- Push the ${personality} characteristics further\n`;
            } else {
                prompt += `- Create a unique variation with a distinctive pose or expression approach\n`;
                prompt += `- Explore a fresh visual angle while keeping the core style consistent\n`;
            }

            prompt += `\nFINAL REMINDERS:\n`;
            prompt += `- The mascot should look professionally designed\n`;
            prompt += `- Perfect for brand representation and marketing\n`;
            prompt += `- Memorable, unique, but clearly inspired by the reference style\n`;
            prompt += `- High quality, clean artwork suitable for commercial use\n`;
            prompt += `- Make sure it truly embodies: ${description}\n`;

            return prompt;
        }

        // Generate single mascot with reference image
        async function generateSingleMascot(prompt, referenceImageData) {
            const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

            const parts = [
                { text: prompt },
                {
                    inlineData: {
                        mimeType: referenceImageData.mimeType,
                        data: referenceImageData.base64
                    }
                }
            ];

            const payload = {
                contents: [{ parts }],
                generationConfig: {
                    responseModalities: ['TEXT', 'IMAGE'],
                    imageConfig: { aspectRatio: '1:1' }
                }
            };

            const response = await fetch(apiUrl, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            });

            const result = await response.json();
            const base64Data = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

            if (!base64Data) {
                throw new Error('No mascot image data received from API');
            }

            return `data:image/png;base64,${base64Data}`;
        }

        // Event delegation for mascot action buttons
        if (mascotResultsGrid) {
            mascotResultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action^="mascot-"]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const imageUrl = actionBtn.dataset.image;

                if (action === 'mascot-preview') {
                    window.open(imageUrl, '_blank');
                } else if (action === 'mascot-download') {
                    const filename = actionBtn.dataset.filename;
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageUrl, filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageUrl, filename);
                    }
                }
            });
        }

        // Helper function to show modal
        function showMascotModal(title, message, type = 'info') {
            const modal = document.createElement('div');
            modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';

            const bgColor = type === 'success' ? 'bg-green-50' : type === 'error' ? 'bg-red-50' : 'bg-amber-50';
            const iconColor = type === 'success' ? 'text-green-600' : type === 'error' ? 'text-red-600' : 'text-amber-600';
            const icon = type === 'success' ? 'fa-check-circle' : type === 'error' ? 'fa-exclamation-circle' : 'fa-info-circle';

            modal.innerHTML = `
                <div class="${bgColor} rounded-2xl p-6 max-w-md w-full shadow-2xl">
                    <div class="text-center">
                        <i class="fas ${icon} ${iconColor} text-5xl mb-4"></i>
                        <h3 class="text-2xl font-bold text-gray-800 mb-3">${title}</h3>
                        <p class="text-gray-600 mb-6">${message}</p>
                        <button onclick="this.closest('.fixed').remove()" class="mascot-action-btn">
                            <i class="fas fa-times mr-2"></i> Tutup
                        </button>
                    </div>
                </div>
            `;

            document.body.appendChild(modal);
            modal.addEventListener('click', (e) => {
                if (e.target === modal) modal.remove();
            });
        }

    })();

    // ============================================
    // AFFILIATE & E-COMMERCE FEATURES
    // ============================================

    // 1. GENERATOR DESKRIPSI PRODUK SEO
    setupToggleButtons('#content-deskripsi-produk', 'desc-platform-btn');
    setupToggleButtons('#content-deskripsi-produk', 'desc-length-btn');

    const descGenerateBtn = document.getElementById('desc-generate-btn');
    if (descGenerateBtn) {
        descGenerateBtn.addEventListener('click', async () => {
            const productName = document.getElementById('desc-product-name').value.trim();
            const productLink = document.getElementById('desc-product-link').value.trim();
            const category = document.getElementById('desc-product-category').value;
            const features = document.getElementById('desc-product-features').value.trim();
            const platform = document.querySelector('.desc-platform-btn.active').dataset.platform;
            const length = document.querySelector('.desc-length-btn.active').dataset.length;

            if (!productName) {
                alert('Mohon isi nama produk!');
                return;
            }

            const outputContainer = document.getElementById('desc-output-container');
            const placeholder = document.getElementById('desc-placeholder');
            const output = document.getElementById('desc-output');
            const platformLabel = document.getElementById('desc-platform-label');

            // Show loading
            descGenerateBtn.disabled = true;
            descGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
            placeholder.classList.add('hidden');
            outputContainer.classList.remove('hidden');
            output.innerHTML = '<div class="text-center text-gray-500"><i class="fas fa-spinner fa-spin text-2xl"></i></div>';

            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                // Unicode escape sequences untuk obfuscation safety
                const emojiBox = '\uD83D\uDCE6'; // 📦
                const emojiTag = '\uD83C\uDFF7\uFE0F'; // 🏷️
                const emojiSparkle = '\u2728'; // ✨
                const emojiLink = '\uD83D\uDD17'; // 🔗
                const emojiNum1 = '\u0031\uFE0F\u20E3'; // 1️⃣
                const emojiNum2 = '\u0032\uFE0F\u20E3'; // 2️⃣
                const emojiNum3 = '\u0033\uFE0F\u20E3'; // 3️⃣
                const emojiNum4 = '\u0034\uFE0F\u20E3'; // 4️⃣
                const emojiNum5 = '\u0035\uFE0F\u20E3'; // 5️⃣
                const emojiNum6 = '\u0036\uFE0F\u20E3'; // 6️⃣
                const emojiNum7 = '\u0037\uFE0F\u20E3'; // 7️⃣
                const emojiNum8 = '\u0038\uFE0F\u20E3'; // 8️⃣
                const emojiNum9 = '\u0039\uFE0F\u20E3'; // 9️⃣
                const emojiRuler = '\uD83D\uDCCF'; // 📏
                const emojiScale = '\u2696\uFE0F'; // ⚖️
                const emojiPalette = '\uD83C\uDFA8'; // 🎨
                const emojiCheck = '\u2713'; // ✓
                const emojiStar = '\u2B50'; // ⭐
                const emojiArrow = '\u27A4'; // ➤
                const emojiFire = '\uD83D\uDD25'; // 🔥
                const emoji100 = '\uD83D\uDCAF'; // 💯
                const emojiX = '\u274C'; // ❌
                const emojiCheckGreen = '\u2705'; // ✅

                const prompt = `Kamu adalah seorang copywriter profesional e-commerce dengan spesialisasi di ${platform === 'shopee' ? 'Shopee' : 'TikTok Shop'}. Buatkan deskripsi produk yang SANGAT MENARIK, PROFESIONAL, dan OPTIMAL untuk SEO.

INFORMASI PRODUK:
${emojiBox} Nama Produk: ${productName}
${category ? `${emojiTag} Kategori: ${category}` : ''}
${features ? `${emojiSparkle} Fitur/Keunggulan: ${features}` : ''}
${productLink ? `${emojiLink} Link: ${productLink}` : ''}

PANJANG DESKRIPSI: ${length === 'short' ? 'PENDEK (3-4 paragraf padat, 200-300 kata, cocok untuk TikTok Shop)' : 'PANJANG (5-7 paragraf detail, 400-600 kata, cocok untuk Shopee)'}

STRUKTUR WAJIB YANG HARUS KAMU BUAT:

${emojiNum1} PEMBUKA MENARIK (1 paragraf)
   - Hook pembuka yang eye-catching dengan emoji
   - Highlight manfaat utama produk
   - Ciptakan sense of urgency atau eksklusivitas

${emojiNum2} DESKRIPSI DETAIL PRODUK (2-3 paragraf)
   - Jelaskan fitur dan spesifikasi lengkap
   - Sebutkan bahan/material, ukuran, varian warna (jika ada)
   - Emphasize unique selling point (USP)
   - Gunakan kata-kata sensory (lembut, halus, premium, dll)

${emojiNum3} MANFAAT & KEUNGGULAN (bullet points)
   Gunakan simbol ${emojiCheck} atau ${emojiStar} untuk list:
   ${emojiCheck} Minimal 5-7 poin keunggulan
   ${emojiCheck} Fokus pada benefit, bukan hanya feature
   ${emojiCheck} Gunakan angka spesifik jika memungkinkan (misal: "Tahan hingga 24 jam")

${emojiNum4} CARA PENGGUNAAN/TIPS (jika relevan)
   - Berikan panduan singkat cara pakai/merawat
   - Tambahkan tips memaksimalkan produk

${emojiNum5} TESTIMONI/SOCIAL PROOF (1 paragraf pendek)
   - Sebutkan "Sudah dipercaya ribuan customer"
   - Highlight rating tinggi/best seller (jika disebutkan)
   - Mention BPOM/sertifikat (jika produk kesehatan/kecantikan)

${emojiNum6} SPESIFIKASI TEKNIS (bullet points)
   Buat list spesifikasi seperti:
   ${emojiRuler} Ukuran: ...
   ${emojiScale} Berat: ...
   ${emojiPalette} Varian: ...
   ${emojiBox} Isi Paket: ...

${emojiNum7} GARANSI & KEAMANAN BELANJA
   - Sebutkan garansi produk/uang kembali
   ${platform === 'shopee' ? '- Highlight "Gratis Ongkir", "COD tersedia", "Garansi Shopee"' : '- Mention "Free Shipping", "COD available"'}

${emojiNum8} CALL TO ACTION KUAT
   - Buat CTA yang compelling dan urgent
   ${platform === 'shopee' ? '- "Klik BELI SEKARANG" atau "Masukkan keranjang sebelum kehabisan!"' : '- "Check keranjang kuning!" atau "Link di bio, buruan checkout!"'}

${emojiNum9} HASHTAG STRATEGIS (10-15 hashtag)
   - Mix hashtag trending + niche specific
   - Include hashtag kategori produk
   - Tambahkan hashtag promo/diskon
   - Format: #HashtagProduk #KategoriProduk #${platform === 'shopee' ? 'Shopee' : 'TikTokShop'} #PromoHariIni #dll

ATURAN FORMATTING:
${emojiX} JANGAN gunakan markdown (**bold**, __italic__, ## heading)
${emojiX} JANGAN gunakan tanda ** atau __ atau ## atau ###
${emojiCheckGreen} Gunakan HURUF KAPITAL untuk EMPHASIZE
${emojiCheckGreen} Gunakan emoji yang RELEVAN dan menarik (tapi jangan berlebihan)
${emojiCheckGreen} Gunakan simbol ${emojiCheck} ${emojiStar} ${emojiArrow} ${emojiFire} ${emoji100} untuk bullet points
${emojiCheckGreen} Spasi antar section untuk readability
${emojiCheckGreen} Gunakan line breaks untuk memisahkan paragraf

TONE & STYLE:
- ${platform === 'shopee' ? 'Profesional tapi tetap friendly, persuasive namun trustworthy' : 'Energik, casual, trendy, relatable untuk Gen Z/Milenial'}
- Hindari kata-kata terlalu formal atau kaku
- Natural seperti rekomendasi teman
- Fokus pada BENEFIT bukan hanya feature
- Ciptakan FOMO (Fear of Missing Out)

OUTPUT FORMAT:
[Tulis deskripsi lengkap sesuai struktur di atas, tanpa markdown, plain text compatible untuk Shopee/TikTok]`;

                const payload = {
                    contents: [{ parts: [{ text: prompt }] }],
                    generationConfig: {
                        temperature: 0.9,
                        maxOutputTokens: 3072
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!response.ok) {
                    const errorData = await response.json();
                    throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`);
                }

                const result = await response.json();

                if (!result.candidates || !result.candidates[0].content || !result.candidates[0].content.parts || !result.candidates[0].content.parts[0].text) {
                    throw new Error("Invalid response structure from API.");
                }

                let description = result.candidates[0].content.parts[0].text;

                // Bersihkan markdown dari hasil
                description = cleanMarkdown(description);

                output.textContent = description;
                platformLabel.textContent = platform === 'shopee' ? 'Shopee' : 'TikTok Shop';

            } catch (error) {
                console.error('Error:', error);
                output.innerHTML = `<div class="text-red-500">Gagal generate deskripsi: ${error.message}</div>`;
            } finally {
                descGenerateBtn.disabled = false;
                descGenerateBtn.innerHTML = '<i class="fas fa-sparkles mr-2"></i>Generate Deskripsi';
            }
        });

        setupCopyButton('desc-copy-btn', 'desc-output');
    }

    // 2. GENERATOR IDE KONTEN TIKTOK
    setupToggleButtons('#content-ide-konten-tiktok', 'tiktok-type-btn');
    setupToggleButtons('#content-ide-konten-tiktok', 'tiktok-duration-btn');

    const tiktokGenerateBtn = document.getElementById('tiktok-generate-btn');
    if (tiktokGenerateBtn) {
        tiktokGenerateBtn.addEventListener('click', async () => {
            const category = document.getElementById('tiktok-category').value;
            const product = document.getElementById('tiktok-product').value.trim();
            const audience = document.getElementById('tiktok-audience').value;
            const type = document.querySelector('.tiktok-type-btn.active').dataset.type;
            const duration = document.querySelector('.tiktok-duration-btn.active').dataset.duration;

            if (!category) {
                alert('Mohon pilih kategori produk!');
                return;
            }

            const outputContainer = document.getElementById('tiktok-output-container');
            const placeholder = document.getElementById('tiktok-placeholder');
            const hookOutput = document.getElementById('tiktok-hook');
            const structureOutput = document.getElementById('tiktok-structure');
            const audioOutput = document.getElementById('tiktok-audio');
            const hashtagsOutput = document.getElementById('tiktok-hashtags');

            // Show loading
            tiktokGenerateBtn.disabled = true;
            tiktokGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
            placeholder.classList.add('hidden');
            outputContainer.classList.remove('hidden');
            hookOutput.innerHTML = '<i class="fas fa-spinner fa-spin"></i>';
            structureOutput.innerHTML = '<i class="fas fa-spinner fa-spin"></i>';
            audioOutput.innerHTML = '<i class="fas fa-spinner fa-spin"></i>';
            hashtagsOutput.innerHTML = '<i class="fas fa-spinner fa-spin"></i>';

            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const durationText = duration === 'short' ? '15-30 detik' : duration === 'medium' ? '30-60 detik' : '60+ detik';
                const typeText = type === 'review' ? 'Review produk' : type === 'tutorial' ? 'Tutorial penggunaan' : type === 'unboxing' ? 'Unboxing' : 'Perbandingan produk';

                // Unicode escape sequences untuk obfuscation safety - TikTok specific
                const tiktokEmoji1 = '\u0031\uFE0F\u20E3'; // 1️⃣
                const tiktokEmoji2 = '\u0032\uFE0F\u20E3'; // 2️⃣
                const tiktokEmoji3 = '\u0033\uFE0F\u20E3'; // 3️⃣
                const tiktokEmoji4 = '\u0034\uFE0F\u20E3'; // 4️⃣
                const tiktokEmoji5 = '\u0035\uFE0F\u20E3'; // 5️⃣
                const tiktokEmoji6 = '\u0036\uFE0F\u20E3'; // 6️⃣
                const emojiTarget = '\uD83C\uDFAF'; // 🎯
                const emojiBox = '\uD83D\uDCE6'; // 📦
                const emojiPeople = '\uD83D\uDC65'; // 👥
                const emojiClapper = '\uD83C\uDFAC'; // 🎬
                const emojiTimer = '\u23F1\uFE0F'; // ⏱️
                const emojiClock = '\u23F0'; // ⏰
                const emojiMusic = '\uD83C\uDFB5'; // 🎵
                const emojiFire = '\uD83D\uDD25'; // 🔥
                const emojiPin = '\uD83D\uDCCD'; // 📍
                const emojiMoney = '\uD83D\uDCB0'; // 💰
                const emojiTag = '\uD83C\uDFF7\uFE0F'; // 🏷️
                const emojiCheck = '\u2713'; // ✓
                const emojiX = '\u274C'; // ❌
                const emojiCheckGreen = '\u2705'; // ✅
                const lineCharTikTok = '\u2501'; // ━

                const prompt = `Kamu adalah Content Creator Strategist TikTok PROFESIONAL yang ahli membuat konten VIRAL dan high-converting untuk affiliate marketing. Buatkan ide konten TikTok yang DETAIL, STRATEGIS, dan PROVEN untuk meningkatkan penjualan.

BRIEF KONTEN:
${emojiTarget} Kategori: ${category}
${product ? `${emojiBox} Produk: ${product}` : ''}
${emojiPeople} Target Audience: ${audience}
${emojiClapper} Tipe Konten: ${typeText}
${emojiTimer} Durasi Target: ${durationText}

STRUKTUR LENGKAP YANG HARUS KAMU BUAT:

${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}
${tiktokEmoji1} HOOK PEMBUKA (0-3 detik pertama)
${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}

Buat 3 ALTERNATIF HOOK PEMBUKA yang POWERFUL dan PROVEN STOP SCROLLING:

HOOK A (Pattern: Shock Value)
[Buat hook yang mengejutkan/controversial tapi masih relevan]
Contoh format: "STOP! [Fakta mengejutkan]..." atau "Jangan [action] sebelum lihat ini!"

HOOK B (Pattern: Problem-Solution)
[Buat hook yang langsung address pain point audience]
Contoh format: "Kamu tipe yang [masalah]? Ini solusinya..." atau "POV: [situasi relatable]"

HOOK C (Pattern: Curiosity Gap)
[Buat hook yang bikin penasaran dengan angka/list]
Contoh format: "[Angka] alasan kenapa..." atau "Ternyata [fakta unik]..."

PILIHAN TERBAIK: [Sebutkan Hook A/B/C yang paling cocok dan jelaskan kenapa]

${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}
${tiktokEmoji2} STRUKTUR VIDEO (SECOND BY SECOND BREAKDOWN)
${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}

Berikan TIMELINE DETAIL dari DETIK 0 hingga SELESAI:

${emojiClock} 0-3 detik:
[Hook pembuka - visual apa yang ditampilkan, text overlay apa, ekspresi/gesture]

${emojiClock} 4-8 detik:
[Problem statement atau transisi - apa yang dilakukan, bagaimana menunjukkan masalah]

${emojiClock} 9-15 detik:
[Introduction produk - cara show produk, angle kamera, fokus pada apa]

${emojiClock} 16-25 detik:
[Demo/tutorial/before-after - step by step apa yang ditunjukkan]

${emojiClock} 26-35 detik:
[Key benefits & features - highlight 2-3 manfaat utama dengan visual]

${emojiClock} 36-45 detik:
[Social proof/testimonial/hasil - tampilkan bukti, review, atau hasil]

${emojiClock} 46-60+ detik:
[CTA & closing - cara ajak audience klik link, urgency/promo, ending memorable]

CATATAN PENTING:
- Sebutkan text overlay yang harus muncul
- Detailkan transisi antar scene
- Jelaskan ekspresi/energy yang dibutuhkan

${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}
${tiktokEmoji3} SARAN AUDIO/MUSIK TRENDING
${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}

Berikan 5 REKOMENDASI AUDIO dengan REASONING:

${emojiMusic} Audio #1: [Nama lagu/sound trending]
   - Vibe: [Energetik/Chill/Emotional/dll]
   - Cocok untuk: [Scene tertentu]
   - Kenapa: [Alasan strategis kenapa audio ini cocok]

${emojiMusic} Audio #2: [Nama lagu/sound trending]
   - Vibe: [...]
   - Cocok untuk: [...]
   - Kenapa: [...]

[Lanjutkan untuk Audio #3, #4, #5]

ALTERNATIF: Voiceover sendiri (jika produk butuh penjelasan detail)

${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}
${tiktokEmoji4} HASHTAG STRATEGY (15-20 hashtag)
${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}

KATEGORI HASHTAG:

${emojiFire} TRENDING HASHTAG (3-5):
#[hashtag viral saat ini] #[...] #[...]

${emojiTarget} NICHE HASHTAG (4-6):
#[hashtag kategori produk specific] #[...] #[...]

${emojiPin} LOKASI HASHTAG (2-3):
#FYPIndonesia #[...] #[...]

${emojiMoney} AFFILIATE/PROMO HASHTAG (3-4):
#TikTokShop #[...] #[...]

${emojiTag} BRANDED/PRODUK HASHTAG (2-3):
#[nama brand/produk] #[...]

${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}
${tiktokEmoji5} CAPTION COPYWRITING
${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}

Tulis CAPTION LENGKAP yang:
- Opening menarik (1-2 kalimat)
- Body menjelaskan value proposition (2-3 kalimat)
- CTA jelas dan urgent (1 kalimat)
- Include emoji yang relevan
- Maksimal 150 karakter (optimal untuk TikTok)

${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}
${tiktokEmoji6} PRO TIPS & BEST PRACTICES
${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}${lineCharTikTok}

TIPS EXECUTION:
${emojiCheck} [Tip teknis untuk shooting/lighting/angle]
${emojiCheck} [Tip editing/transisi yang cocok]
${emojiCheck} [Tip timing posting optimal]

TIPS ENGAGEMENT:
${emojiCheck} [Cara encourage comment]
${emojiCheck} [Cara bikin audience penasaran baca caption]
${emojiCheck} [Strategi reply comment untuk boost algorithm]

TIPS CONVERSION:
${emojiCheck} [Cara subtle tapi efektif promosi link]
${emojiCheck} [Strategi create urgency/FOMO]
${emojiCheck} [Follow-up content ideas]

ATURAN FORMATTING:
${emojiX} NO markdown (**bold**, __italic__, ## heading)
${emojiX} NO tanda ** atau __ atau ## atau ###
${emojiCheckGreen} Gunakan HURUF KAPITAL untuk EMPHASIZE
${emojiCheckGreen} Gunakan emoji STRATEGIS (jangan berlebihan)
${emojiCheckGreen} Gunakan simbol ${lineCharTikTok} - • ${emojiCheck} > untuk structure
${emojiCheckGreen} Spasi clear untuk readability

TONE & APPROACH:
- AUTHENTIC, bukan overly scripted
- RELATABLE untuk ${audience}
- ENGAGING tapi bukan clickbait menyesatkan
- STRATEGIC dengan clear goal (awareness → interest → click)
- TRENDY tapi timeless (bisa re-use format)

OUTPUT FORMAT:
[Tulis semua section lengkap sesuai struktur di atas, detail, actionable, dan siap execute]`;

                const payload = {
                    contents: [{ parts: [{ text: prompt }] }],
                    generationConfig: {
                        temperature: 1.0,
                        maxOutputTokens: 4096
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!response.ok) {
                    const errorData = await response.json();
                    throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`);
                }

                const result = await response.json();

                if (!result.candidates || !result.candidates[0].content || !result.candidates[0].content.parts || !result.candidates[0].content.parts[0].text) {
                    throw new Error("Invalid response structure from API.");
                }

                let content = result.candidates[0].content.parts[0].text;

                // Bersihkan markdown dari hasil
                content = cleanMarkdown(content);

                console.log('TikTok content received:', content);

                // Parse output dengan pattern yang lebih flexible
                // Match untuk Hook Pembuka (1️⃣) - menggunakan Unicode escape untuk obfuscation safety
                const emoji1 = '\u0031\uFE0F\u20E3'; // 1️⃣
                const emoji2 = '\u0032\uFE0F\u20E3'; // 2️⃣
                const emoji3 = '\u0033\uFE0F\u20E3'; // 3️⃣
                const emoji4 = '\u0034\uFE0F\u20E3'; // 4️⃣
                const emoji5 = '\u0035\uFE0F\u20E3'; // 5️⃣
                const lineChar = '\u2501'; // ━

                const hookMatch = content.match(new RegExp(emoji1 + '\\s*HOOK PEMBUKA[^\\n]*\\n' + lineChar + '+\\n([\\s\\S]*?)(?=\\n' + lineChar + '+\\n' + emoji2 + '|$)', 'i'));

                // Match untuk Struktur Video (2️⃣)
                const structureMatch = content.match(new RegExp(emoji2 + '\\s*STRUKTUR VIDEO[^\\n]*\\n' + lineChar + '+\\n([\\s\\S]*?)(?=\\n' + lineChar + '+\\n' + emoji3 + '|$)', 'i'));

                // Match untuk Saran Audio (3️⃣)
                const audioMatch = content.match(new RegExp(emoji3 + '\\s*SARAN AUDIO[^\\n]*\\n' + lineChar + '+\\n([\\s\\S]*?)(?=\\n' + lineChar + '+\\n' + emoji4 + '|$)', 'i'));

                // Match untuk Hashtag (4️⃣)
                const hashtagMatch = content.match(new RegExp(emoji4 + '\\s*HASHTAG STRATEGY[^\\n]*\\n' + lineChar + '+\\n([\\s\\S]*?)(?=\\n' + lineChar + '+\\n' + emoji5 + '|$)', 'i'));

                // Jika parsing berhasil, tampilkan per section
                if (hookMatch || structureMatch || audioMatch || hashtagMatch) {
                    hookOutput.textContent = hookMatch ? hookMatch[1].trim() : 'Bagian hook tidak ditemukan dalam hasil';
                    structureOutput.textContent = structureMatch ? structureMatch[1].trim() : 'Bagian struktur tidak ditemukan dalam hasil';
                    audioOutput.textContent = audioMatch ? audioMatch[1].trim() : 'Bagian audio tidak ditemukan dalam hasil';
                    hashtagsOutput.textContent = hashtagMatch ? hashtagMatch[1].trim() : 'Bagian hashtag tidak ditemukan dalam hasil';
                } else {
                    // Fallback: jika parsing gagal, tampilkan semua content di setiap section
                    console.warn('TikTok parsing failed, showing raw content');
                    hookOutput.textContent = content;
                    structureOutput.textContent = '(Lihat di section Hook Pembuka)';
                    audioOutput.textContent = '(Lihat di section Hook Pembuka)';
                    hashtagsOutput.textContent = '(Lihat di section Hook Pembuka)';
                }

            } catch (error) {
                console.error('Error:', error);
                const errorMsg = `<div class="text-red-500 text-xs">Gagal: ${error.message}</div>`;
                hookOutput.innerHTML = errorMsg;
                structureOutput.innerHTML = errorMsg;
                audioOutput.innerHTML = errorMsg;
                hashtagsOutput.innerHTML = errorMsg;
            } finally {
                tiktokGenerateBtn.disabled = false;
                tiktokGenerateBtn.innerHTML = '<i class="fas fa-magic mr-2"></i>Generate Ide Konten';
            }
        });

        // Setup copy buttons for TikTok outputs (works on desktop & mobile)
        document.querySelectorAll('.tiktok-copy-btn').forEach(btn => {
            const handleCopy = async (e) => {
                e.stopPropagation();

                const targetId = btn.dataset.target;
                const target = document.getElementById(targetId);
                if (!target || !target.textContent) {
                    console.warn('Target not found or empty:', targetId);
                    return;
                }

                try {
                    await navigator.clipboard.writeText(target.textContent);
                    const originalHTML = btn.innerHTML;
                    btn.innerHTML = '<i class="fas fa-check"></i>';
                    btn.classList.add('text-green-600');
                    setTimeout(() => {
                        btn.innerHTML = originalHTML;
                        btn.classList.remove('text-green-600');
                    }, 2000);
                } catch (err) {
                    console.error('Failed to copy:', err);
                    alert('Gagal menyalin. Silakan coba lagi.');
                }
            };

            btn.addEventListener('click', handleCopy);
        });
    }

    // 3. GENERATOR SCRIPT STORY IKLAN
    setupToggleButtons('#content-script-story-iklan', 'story-tone-btn');
    setupToggleButtons('#content-script-story-iklan', 'story-length-btn');

    const storyScriptGenerateBtn = document.getElementById('story-script-generate-btn');
    if (storyScriptGenerateBtn) {
        storyScriptGenerateBtn.addEventListener('click', async () => {
            const targetAudience = document.getElementById('story-target-audience').value;
            const productName = document.getElementById('story-product-name').value.trim();
            const problem = document.getElementById('story-problem').value.trim();
            const benefits = document.getElementById('story-benefits').value.trim();
            const tone = document.querySelector('.story-tone-btn.active').dataset.tone;
            const length = document.querySelector('.story-length-btn.active').dataset.length;
            const affiliateLink = document.getElementById('story-affiliate-link').value.trim();

            if (!targetAudience || !productName || !problem) {
                alert('Mohon isi semua field yang wajib (*)!');
                return;
            }

            const outputContainer = document.getElementById('story-script-output-container');
            const placeholder = document.getElementById('story-script-placeholder');
            const scriptOutput = document.getElementById('story-script-output');
            const ctaOutput = document.getElementById('story-cta');

            // Show loading
            storyScriptGenerateBtn.disabled = true;
            storyScriptGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
            placeholder.classList.add('hidden');
            outputContainer.classList.remove('hidden');
            scriptOutput.innerHTML = '<div class="text-center text-gray-500"><i class="fas fa-spinner fa-spin"></i></div>';
            ctaOutput.innerHTML = '<div class="text-center text-gray-500"><i class="fas fa-spinner fa-spin"></i></div>';

            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const toneText = tone === 'friendly' ? 'Ramah dan bersahabat' : tone === 'professional' ? 'Profesional dan informatif' : tone === 'casual' ? 'Santai dan relatable' : 'Antusias dan energik';
                const lengthText = length === 'short' ? '30-60 detik' : '60-120 detik';

                const prompt = `Buatkan script narasi iklan produk affiliate yang natural dan meyakinkan dengan detail:

Target Audience: ${targetAudience}
Produk: ${productName}
Masalah yang Dipecahkan: ${problem}
${benefits ? `Manfaat/Solusi: ${benefits}` : ''}
Tone: ${toneText}
Durasi: ${lengthText}
${affiliateLink ? `Link Affiliate: ${affiliateLink}` : ''}

Buat script narasi dengan struktur STORY TELLING yang natural:

1. OPENING - Personal Hook (5-10 detik):
   Mulai dengan pengalaman pribadi atau situasi yang relatable dengan target audience
   Contoh: "Dulu aku juga ngalamin yang sama..." atau "Tau gak sih, aku sempat frustasi karena..."

2. PROBLEM IDENTIFICATION (10-15 detik):
   Jelaskan masalah secara detail dengan bahasa yang emosional dan relatable
   Buat audience merasa "Ini gue banget!"

3. DISCOVERY MOMENT (10-15 detik):
   Ceritakan bagaimana menemukan produk ini (jangan langsung jualan)
   Contoh: "Sampai akhirnya temen aku rekomendasiin..." atau "Iseng-iseng coba..."

4. EXPERIENCE & RESULTS (15-20 detik):
   Sharing pengalaman menggunakan produk dengan jujur
   Sebutkan hasil nyata (before-after jika ada)
   Tetap natural, boleh sebutkan kekurangan kecil tapi dibalance dengan kelebihan

5. RECOMMENDATION (10-15 detik):
   Rekomendasikan produk dengan tulus, bukan hard selling
   Sebutkan untuk siapa produk ini cocok

6. SOFT CTA (5-10 detik):
   Call-to-action yang soft dan tidak memaksa
   ${affiliateLink ? `Sebutkan link tersedia di deskripsi/bio` : 'Arahkan untuk mencari produk'}

PENTING:
- Gunakan bahasa ${targetAudience} yang natural
- Jangan terlalu formal kecuali tone professional
- Sertakan filler words yang wajar (seperti "gitu", "jadi", "nah", "terus")
- Buat terasa seperti sharing ke teman, bukan iklan
- Hindari klaim berlebihan
- JANGAN gunakan format markdown seperti **bold** atau __underline__
- JANGAN gunakan tanda ** atau __ sama sekali
- Gunakan HURUF KAPITAL atau emoji jika perlu penekanan
- Format harus plain text yang bisa langsung direcord
- Script harus natural seperti berbicara spontan

Tulis script dalam format paragraf yang bisa langsung dibaca/direcord tanpa tanda markdown.`;

                const payload = {
                    contents: [{ parts: [{ text: prompt }] }],
                    generationConfig: {
                        temperature: 0.9,
                        maxOutputTokens: 2048
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!response.ok) {
                    const errorData = await response.json();
                    throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`);
                }

                const result = await response.json();

                if (!result.candidates || !result.candidates[0].content || !result.candidates[0].content.parts || !result.candidates[0].content.parts[0].text) {
                    throw new Error("Invalid response structure from API.");
                }

                let script = result.candidates[0].content.parts[0].text;

                // Bersihkan markdown dari hasil
                script = cleanMarkdown(script);

                scriptOutput.textContent = script;

                // Generate CTA suggestion - Unicode escape untuk obfuscation safety
                const sparkleEmojiCTA = '\u2728'; // ✨
                const dizzyEmojiCTA = '\uD83D\uDCAB'; // 💫
                const fireEmojiCTA = '\uD83D\uDD25'; // 🔥
                let ctaText = sparkleEmojiCTA + ' "Link ada di bio ya, atau bisa search langsung nama produknya!"\n';
                ctaText += dizzyEmojiCTA + ' "Kalau mau coba, aku tinggalin link di deskripsi ya!"\n';
                ctaText += fireEmojiCTA + ' "Swipe up atau check keranjang kuning untuk info lengkapnya!"';
                ctaOutput.textContent = ctaText;

            } catch (error) {
                console.error('Error:', error);
                scriptOutput.innerHTML = `<div class="text-red-500">Gagal generate script: ${error.message}</div>`;
                ctaOutput.innerHTML = '';
            } finally {
                storyScriptGenerateBtn.disabled = false;
                storyScriptGenerateBtn.innerHTML = '<i class="fas fa-wand-magic-sparkles mr-2"></i>Generate Script Story';
            }
        });

        setupCopyButton('story-script-copy-btn', 'story-script-output');
    }

    // ============================================
    // ALAT BISNIS FEATURES
    // ============================================

    // 1. RENCANA BISNIS & SWOT
    const bpGenerateBtn = document.getElementById('bp-generate-btn');
    if (bpGenerateBtn) {
        bpGenerateBtn.addEventListener('click', async () => {
            const jenisBisnis = document.getElementById('bp-jenis-bisnis').value.trim();
            const lokasi = document.getElementById('bp-lokasi').value.trim();
            const targetPasar = document.getElementById('bp-target-pasar').value.trim();
            const modal = document.getElementById('bp-modal').value.trim();
            const infoTambahan = document.getElementById('bp-info-tambahan').value.trim();

            if (!jenisBisnis) {
                alert('Mohon isi jenis bisnis!');
                return;
            }

            const outputContainer = document.getElementById('bp-output-container');
            const placeholder = document.getElementById('bp-placeholder');
            const output = document.getElementById('bp-output');

            // Show loading
            bpGenerateBtn.disabled = true;
            bpGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
            placeholder.classList.add('hidden');
            outputContainer.classList.remove('hidden');
            output.innerHTML = '<div class="text-center text-gray-500"><i class="fas fa-spinner fa-spin text-2xl"></i></div>';

            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const prompt = `Buatkan rencana bisnis dan analisis SWOT profesional untuk bisnis berikut:

Jenis Bisnis: ${jenisBisnis}
${lokasi ? `Lokasi: ${lokasi}` : ''}
${targetPasar ? `Target Pasar: ${targetPasar}` : ''}
${modal ? `Modal Awal: ${modal}` : ''}
${infoTambahan ? `Info Tambahan: ${infoTambahan}` : ''}

Format output harus mencakup:

1. RINGKASAN EKSEKUTIF
   - Visi dan misi bisnis
   - Tujuan utama (jangka pendek & panjang)

2. ANALISIS PASAR
   - Target market dan segmentasi
   - Analisis kompetitor
   - Peluang pasar

3. STRATEGI BISNIS
   - Model bisnis
   - Strategi pemasaran
   - Strategi operasional
   - Strategi SDM
   - Strategi keuangan

4. ANALISIS SWOT
   STRENGTHS (Kekuatan):
   • [3-5 poin kekuatan internal]

   WEAKNESSES (Kelemahan):
   • [3-5 poin kelemahan internal]

   OPPORTUNITIES (Peluang):
   • [3-5 poin peluang eksternal]

   THREATS (Ancaman):
   • [3-5 poin ancaman eksternal]

5. PROYEKSI KEUANGAN (1-3 tahun)
   - Estimasi modal awal
   - Proyeksi revenue
   - Break-even point
   - ROI timeline

6. RENCANA AKSI
   - Milestone 3 bulan pertama
   - Target 6 bulan
   - Roadmap 1-3 tahun

PENTING:
- JANGAN gunakan format markdown seperti **bold** atau __underline__
- Gunakan HURUF KAPITAL untuk judul
- Gunakan simbol • ✓ ➤ untuk bullet points
- Berikan analisis yang realistis dan actionable
- Sesuaikan dengan jenis bisnis dan skala yang sesuai

Berikan output yang profesional dan mudah dipahami.`;

                const payload = {
                    contents: [{ parts: [{ text: prompt }] }],
                    generationConfig: {
                        temperature: 0.9,
                        maxOutputTokens: 4096
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!response.ok) {
                    const errorData = await response.json();
                    throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`);
                }

                const result = await response.json();

                if (!result.candidates || !result.candidates[0].content || !result.candidates[0].content.parts || !result.candidates[0].content.parts[0].text) {
                    throw new Error("Invalid response structure from API.");
                }

                let businessPlan = result.candidates[0].content.parts[0].text;
                businessPlan = cleanMarkdown(businessPlan);
                output.textContent = businessPlan;

            } catch (error) {
                console.error('Error:', error);
                output.innerHTML = `<div class="text-red-500">Gagal generate rencana bisnis: ${error.message}</div>`;
            } finally {
                bpGenerateBtn.disabled = false;
                bpGenerateBtn.innerHTML = '<i class="fas fa-sparkles mr-2"></i>Generate Rencana Bisnis';
            }
        });

        setupCopyButton('bp-copy-btn', 'bp-output');
    }

    // 2. HR ASSISTANT
    const hrGenerateBtn = document.getElementById('hr-generate-btn');
    if (hrGenerateBtn) {
        hrGenerateBtn.addEventListener('click', async () => {
            const posisi = document.getElementById('hr-posisi').value.trim();
            const industri = document.getElementById('hr-industri').value.trim();
            const level = document.getElementById('hr-level').value;
            const keahlian = document.getElementById('hr-keahlian').value.trim();

            if (!posisi) {
                alert('Mohon isi nama posisi!');
                return;
            }

            const outputContainer = document.getElementById('hr-output-container');
            const placeholder = document.getElementById('hr-placeholder');
            const output = document.getElementById('hr-output');

            // Show loading
            hrGenerateBtn.disabled = true;
            hrGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
            placeholder.classList.add('hidden');
            outputContainer.classList.remove('hidden');
            output.innerHTML = '<div class="text-center text-gray-500"><i class="fas fa-spinner fa-spin text-2xl"></i></div>';

            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const levelText = {
                    'entry': 'Entry Level / Fresh Graduate',
                    'junior': 'Junior (1-3 tahun)',
                    'senior': 'Senior (3-5 tahun)',
                    'lead': 'Lead / Supervisor',
                    'manager': 'Manager',
                    'director': 'Director / C-Level'
                }[level] || 'Entry Level';

                const prompt = `Buatkan Job Description (JD) profesional untuk posisi berikut:

Posisi: ${posisi}
${industri ? `Industri/Bidang: ${industri}` : ''}
Level: ${levelText}
${keahlian ? `Keahlian Khusus: ${keahlian}` : ''}

Format JD harus mencakup:

1. RINGKASAN POSISI
   [1-2 paragraf overview tentang posisi ini]

2. TANGGUNG JAWAB UTAMA
   • [5-8 tanggung jawab spesifik]
   • Gunakan action verbs (mengelola, mengembangkan, menganalisis, dll)
   • Sesuaikan dengan level ${levelText}

3. KUALIFIKASI
   Pendidikan:
   • [Requirement pendidikan]

   Pengalaman:
   • [Pengalaman yang dibutuhkan]

   Keahlian Teknis:
   • [Hard skills spesifik]

   Keahlian Non-Teknis:
   • [Soft skills yang diperlukan]

4. NILAI TAMBAH (Nice to Have)
   • [2-4 kualifikasi bonus]

5. BENEFIT & FASILITAS
   • [4-6 benefit yang menarik]

6. PERTANYAAN INTERVIEW (BONUS)
   • [3-5 pertanyaan interview yang sesuai untuk posisi ini]

PENTING:
- JANGAN gunakan format markdown
- Gunakan bahasa profesional tapi friendly
- Sesuaikan tanggung jawab dengan level (${levelText})
- Buat deskripsi yang menarik kandidat berkualitas`;

                const payload = {
                    contents: [{ parts: [{ text: prompt }] }],
                    generationConfig: {
                        temperature: 0.9,
                        maxOutputTokens: 2048
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!response.ok) {
                    const errorData = await response.json();
                    throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`);
                }

                const result = await response.json();

                if (!result.candidates || !result.candidates[0].content || !result.candidates[0].content.parts || !result.candidates[0].content.parts[0].text) {
                    throw new Error("Invalid response structure from API.");
                }

                let hrContent = result.candidates[0].content.parts[0].text;
                hrContent = cleanMarkdown(hrContent);
                output.textContent = hrContent;

            } catch (error) {
                console.error('Error:', error);
                output.innerHTML = `<div class="text-red-500">Gagal generate HR content: ${error.message}</div>`;
            } finally {
                hrGenerateBtn.disabled = false;
                hrGenerateBtn.innerHTML = '<i class="fas fa-sparkles mr-2"></i>Generate Job Description';
            }
        });

        setupCopyButton('hr-copy-btn', 'hr-output');
    }

    // 3. KONTEN MARKETING MULTI-PLATFORM
    const kmGenerateBtn = document.getElementById('km-generate-btn');
    if (kmGenerateBtn) {
        kmGenerateBtn.addEventListener('click', async () => {
            const produk = document.getElementById('km-produk').value.trim();
            const promosi = document.getElementById('km-promosi').value.trim();
            const info = document.getElementById('km-info').value.trim();

            const platforms = [];
            if (document.getElementById('km-platform-ig').checked) platforms.push('instagram');
            if (document.getElementById('km-platform-fb').checked) platforms.push('facebook');
            if (document.getElementById('km-platform-email').checked) platforms.push('email');
            if (document.getElementById('km-platform-ads').checked) platforms.push('google-ads');

            if (!produk || platforms.length === 0) {
                alert('Mohon isi nama produk/layanan dan pilih minimal 1 platform!');
                return;
            }

            const outputContainer = document.getElementById('km-output-container');
            const placeholder = document.getElementById('km-placeholder');
            const output = document.getElementById('km-output');

            // Show loading
            kmGenerateBtn.disabled = true;
            kmGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
            placeholder.classList.add('hidden');
            outputContainer.classList.remove('hidden');
            output.innerHTML = '<div class="text-center text-gray-500"><i class="fas fa-spinner fa-spin text-2xl"></i></div>';

            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const platformNames = {
                    'instagram': 'Instagram',
                    'facebook': 'Facebook',
                    'email': 'Email Marketing',
                    'google-ads': 'Google Ads'
                };

                const selectedPlatforms = platforms.map(p => platformNames[p]).join(', ');

                const prompt = `Buatkan konten marketing multi-platform untuk:

Produk/Layanan: ${produk}
${promosi ? `Jenis Promosi: ${promosi}` : ''}
${info ? `Info Tambahan: ${info}` : ''}
Platform: ${selectedPlatforms}

Buatkan konten yang dioptimalkan untuk SETIAP platform yang dipilih:

${platforms.includes('instagram') ? `
=== INSTAGRAM ===
📱 FEED POST:
[Caption engaging dengan emoji, max 2200 karakter]
[Call-to-action yang jelas]
[Hashtag relevan]

📸 STORY IDEAS:
• [2-3 ide konten story dengan sticker/poll]
` : ''}

${platforms.includes('facebook') ? `
=== FACEBOOK ===
📢 POST:
[Caption storytelling approach]
[Include pertanyaan untuk engagement]
[CTA yang jelas]
[Hashtag relevan]
` : ''}

${platforms.includes('email') ? `
=== EMAIL MARKETING ===
📧 SUBJECT LINE:
[5 variasi subject line yang menarik]

📝 EMAIL BODY:
[Header/Hero text]
[Body copy dengan benefit produk]
[CTA button text]
[Footer text]
` : ''}

${platforms.includes('google-ads') ? `
=== GOOGLE ADS ===
🔍 SEARCH ADS:
Headline 1: [Max 30 karakter]
Headline 2: [Max 30 karakter]
Headline 3: [Max 30 karakter]
Description 1: [Max 90 karakter]
Description 2: [Max 90 karakter]

📱 DISPLAY ADS:
Short Headline: [Max 30 karakter]
Long Headline: [Max 90 karakter]
Description: [Max 90 karakter]
` : ''}

TIPS IMPLEMENTASI:
• Best time to post untuk setiap platform
• Visual suggestions (jenis foto/video yang cocok)
• Call-to-action yang efektif

PENTING:
- JANGAN gunakan format markdown **bold**
- Sesuaikan konten dengan karakteristik SETIAP platform
- Fokus pada benefit produk dan promosi
- Gunakan emoji secukupnya, tidak berlebihan
- Include sense of urgency jika ada promosi terbatas`;

                const payload = {
                    contents: [{ parts: [{ text: prompt }] }],
                    generationConfig: {
                        temperature: 0.9,
                        maxOutputTokens: 2048
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!response.ok) {
                    const errorData = await response.json();
                    throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`);
                }

                const result = await response.json();

                if (!result.candidates || !result.candidates[0].content || !result.candidates[0].content.parts || !result.candidates[0].content.parts[0].text) {
                    throw new Error("Invalid response structure from API.");
                }

                let marketingContent = result.candidates[0].content.parts[0].text;
                marketingContent = cleanMarkdown(marketingContent);
                output.textContent = marketingContent;

            } catch (error) {
                console.error('Error:', error);
                output.innerHTML = `<div class="text-red-500">Gagal generate konten marketing: ${error.message}</div>`;
            } finally {
                kmGenerateBtn.disabled = false;
                kmGenerateBtn.innerHTML = '<i class="fas fa-sparkles mr-2"></i>Generate Konten Marketing';
            }
        });

        setupCopyButton('km-copy-btn', 'km-output');
    }

    // 4. RISET PASAR & IDE PRODUK
    const rmGenerateBtn = document.getElementById('rm-generate-btn');
    if (rmGenerateBtn) {
        rmGenerateBtn.addEventListener('click', async () => {
            const bisnis = document.getElementById('rm-bisnis').value.trim();
            const tujuan = document.getElementById('rm-tujuan').value;
            const target = document.getElementById('rm-target').value.trim();
            const preferensi = document.getElementById('rm-preferensi').value.trim();

            if (!bisnis) {
                alert('Mohon isi jenis bisnis saat ini!');
                return;
            }

            const outputContainer = document.getElementById('rm-output-container');
            const placeholder = document.getElementById('rm-placeholder');
            const output = document.getElementById('rm-output');

            // Show loading
            rmGenerateBtn.disabled = true;
            rmGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
            placeholder.classList.add('hidden');
            outputContainer.classList.remove('hidden');
            output.innerHTML = '<div class="text-center text-gray-500"><i class="fas fa-spinner fa-spin text-2xl"></i></div>';

            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const tujuanText = {
                    'produk-baru': 'Ide Produk/Menu Baru',
                    'ekspansi': 'Peluang Ekspansi Bisnis',
                    'tren': 'Analisis Tren Pasar',
                    'kompetitor': 'Analisis Kompetitor'
                }[tujuan] || 'Riset Pasar';

                const prompt = `Lakukan riset pasar dan berikan rekomendasi untuk bisnis berikut:

Jenis Bisnis: ${bisnis}
Tujuan Riset: ${tujuanText}
${target ? `Lokasi/Target Market: ${target}` : ''}
${preferensi ? `Preferensi Khusus: ${preferensi}` : ''}

Buatkan analisis dan rekomendasi yang fokus pada: ${tujuanText}

Format output harus mencakup:

1. RINGKASAN EKSEKUTIF
   • Key findings dari riset
   • Rekomendasi utama

2. ANALISIS PASAR
   • Overview kondisi pasar saat ini
   • Tren yang relevan dengan bisnis Anda
   • Ukuran pasar dan proyeksi pertumbuhan

3. ANALISIS KOMPETITOR
   • Kompetitor utama (3-5)
   • Keunggulan dan kelemahan mereka
   • Gap opportunity yang bisa dimanfaatkan

4. REKOMENDASI SPESIFIK
   ${tujuan === 'produk-baru' ? `
   • 5-7 ide produk/menu baru yang potensial
   • Target market untuk setiap produk
   • Estimasi harga dan profitability
   • Cara validasi ide sebelum launch` : ''}

   ${tujuan === 'ekspansi' ? `
   • Peluang ekspansi (lokasi baru, channel baru, dll)
   • Market potential untuk ekspansi
   • Resources yang dibutuhkan
   • Timeline dan milestone` : ''}

   ${tujuan === 'tren' ? `
   • 5-7 tren terkini yang relevan
   • Impact tren terhadap bisnis Anda
   • Cara memanfaatkan tren
   • Prediksi tren masa depan` : ''}

   ${tujuan === 'kompetitor' ? `
   • Analisis detail 3-5 kompetitor utama
   • Pricing strategy mereka
   • Marketing approach mereka
   • Cara differentiate dari kompetitor` : ''}

5. ACTION PLAN
   • Langkah konkret yang bisa dilakukan (prioritas tinggi)
   • Timeline implementasi
   • Estimasi investasi (jika relevan)

6. RISK & MITIGATION
   • Risiko yang perlu diantisipasi
   • Strategi mitigasi risiko

PENTING:
- JANGAN gunakan format markdown **bold**
- Berikan data dan insight yang realistis untuk pasar Indonesia
- Fokus pada actionable recommendations
- Sesuaikan dengan jenis bisnis: ${bisnis}`;

                const payload = {
                    contents: [{ parts: [{ text: prompt }] }],
                    generationConfig: {
                        temperature: 0.9,
                        maxOutputTokens: 2048
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!response.ok) {
                    const errorData = await response.json();
                    throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`);
                }

                const result = await response.json();

                if (!result.candidates || !result.candidates[0].content || !result.candidates[0].content.parts || !result.candidates[0].content.parts[0].text) {
                    throw new Error("Invalid response structure from API.");
                }

                let marketResearch = result.candidates[0].content.parts[0].text;
                marketResearch = cleanMarkdown(marketResearch);
                output.textContent = marketResearch;

            } catch (error) {
                console.error('Error:', error);
                output.innerHTML = `<div class="text-red-500">Gagal generate riset pasar: ${error.message}</div>`;
            } finally {
                rmGenerateBtn.disabled = false;
                rmGenerateBtn.innerHTML = '<i class="fas fa-sparkles mr-2"></i>Generate Riset & Ide';
            }
        });

        setupCopyButton('rm-copy-btn', 'rm-output');
    }

    // 5. FINANCIAL FORECAST / SIMULATOR ANGGARAN
    const ffGenerateBtn = document.getElementById('ff-generate-btn');
    if (ffGenerateBtn) {
        ffGenerateBtn.addEventListener('click', async () => {
            const pendapatan = document.getElementById('ff-pendapatan').value.trim();
            const tipe = document.getElementById('ff-tipe').value;
            const industri = document.getElementById('ff-industri').value.trim();
            const skenario = document.getElementById('ff-skenario').value;
            const detail = document.getElementById('ff-detail').value.trim();

            if (!pendapatan) {
                alert('Mohon isi proyeksi pendapatan bulanan!');
                return;
            }

            const outputContainer = document.getElementById('ff-output-container');
            const placeholder = document.getElementById('ff-placeholder');
            const output = document.getElementById('ff-output');

            // Show loading
            ffGenerateBtn.disabled = true;
            ffGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
            placeholder.classList.add('hidden');
            outputContainer.classList.remove('hidden');
            output.innerHTML = '<div class="text-center text-gray-500"><i class="fas fa-spinner fa-spin text-2xl"></i></div>';

            try {
                const apiKey = "";
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                const tipeText = {
                    'umkm': 'UMKM / Bisnis Kecil',
                    'menengah': 'Bisnis Menengah',
                    'besar': 'Bisnis Besar',
                    'startup': 'Startup'
                }[tipe] || 'UMKM';

                const skenarioText = {
                    'normal': 'Normal - Alokasi Anggaran Standar',
                    'growth': 'Growth - Fokus Ekspansi & Marketing',
                    'efficiency': 'Efficiency - Fokus Penghematan',
                    'custom': 'Custom Scenario'
                }[skenario] || 'Normal';

                const prompt = `Buatkan financial forecast dan simulasi anggaran untuk bisnis berikut:

Proyeksi Pendapatan Bulanan: ${pendapatan}
Tipe Bisnis: ${tipeText}
${industri ? `Industri: ${industri}` : ''}
Skenario: ${skenarioText}
${detail ? `Detail Tambahan: ${detail}` : ''}

Buatkan analisis finansial dan simulasi anggaran yang mencakup:

1. RINGKASAN EKSEKUTIF
   • Proyeksi pendapatan bulanan: ${pendapatan}
   • Tipe bisnis: ${tipeText}
   • Skenario: ${skenarioText}

2. ALOKASI ANGGARAN BULANAN (BREAKDOWN)
   Berdasarkan pendapatan ${pendapatan}, buatkan alokasi anggaran untuk:

   BIAYA OPERASIONAL:
   • Gaji & SDM: [X%] = Rp [...]
   • Sewa/Utilitas: [X%] = Rp [...]
   • Bahan Baku/Inventory: [X%] = Rp [...]
   • Marketing & Promosi: [X%] = Rp [...]
   • Operasional Lainnya: [X%] = Rp [...]

   INVESTASI & PENGEMBANGAN:
   • [Sesuaikan dengan skenario ${skenarioText}]

   CADANGAN & PROFIT:
   • Emergency Fund: [X%] = Rp [...]
   • Net Profit Target: [X%] = Rp [...]

   TOTAL: 100% = ${pendapatan}

3. PROYEKSI CASH FLOW (6 BULAN)
   Bulan 1-6:
   • Revenue: [...]
   • Total Expenses: [...]
   • Net Cash Flow: [...]
   • Cumulative Cash: [...]

4. ANALISIS SKENARIO
   ${skenario === 'growth' ? 'Fokus pada ekspansi dan marketing (alokasi marketing lebih tinggi)' : ''}
   ${skenario === 'efficiency' ? 'Fokus pada penghematan (optimasi biaya operasional)' : ''}
   ${skenario === 'normal' ? 'Alokasi standar balanced' : ''}
   ${skenario === 'custom' ? 'Sesuaikan dengan detail yang diberikan' : ''}

5. KEY FINANCIAL RATIOS
   • Profit Margin Target: [...]%
   • Operating Ratio: [...]%
   • Cash Reserve Ratio: [...]%
   • Marketing to Revenue Ratio: [...]%

6. TIPS PENGELOLAAN KEUANGAN
   Berdasarkan tipe bisnis (${tipeText}):
   • [3-5 tips spesifik untuk mengelola keuangan]
   • Red flags yang perlu diwaspadai
   • Strategi optimasi cash flow

7. REKOMENDASI PRIORITAS
   • Top 3 hal yang harus diperhatikan
   • Area yang bisa di-optimalkan
   • Warning signs jika ada cash flow issue

8. TEMPLATE TRACKING BULANAN
   [Berikan format sederhana untuk tracking harian/mingguan]

PENTING:
- JANGAN gunakan format markdown **bold**
- Gunakan angka realistis untuk industri ${industri || 'bisnis'}
- Sesuaikan alokasi dengan tipe bisnis ${tipeText}
- Format angka dengan pemisah ribuan (Rp 10.000.000)
- Berikan insight yang actionable dan praktis`;

                const payload = {
                    contents: [{ parts: [{ text: prompt }] }],
                    generationConfig: {
                        temperature: 0.9,
                        maxOutputTokens: 2048
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                if (!response.ok) {
                    const errorData = await response.json();
                    throw new Error(errorData.error?.message || `HTTP error! status: ${response.status}`);
                }

                const result = await response.json();

                if (!result.candidates || !result.candidates[0].content || !result.candidates[0].content.parts || !result.candidates[0].content.parts[0].text) {
                    throw new Error("Invalid response structure from API.");
                }

                let financialForecast = result.candidates[0].content.parts[0].text;
                financialForecast = cleanMarkdown(financialForecast);
                output.textContent = financialForecast;

            } catch (error) {
                console.error('Error:', error);
                output.innerHTML = `<div class="text-red-500">Gagal generate financial forecast: ${error.message}</div>`;
            } finally {
                ffGenerateBtn.disabled = false;
                ffGenerateBtn.innerHTML = '<i class="fas fa-sparkles mr-2"></i>Generate Proyeksi Anggaran';
            }
        });

        setupCopyButton('ff-copy-btn', 'ff-output');
    }

    // ============================================
    // POSTER GENERATOR
    // ============================================

    (function() {
        const apiKey = "";
        let posterImageData = null;
        let generatedPosters = [];

        // Elements
        const uploadBox = document.getElementById('poster-upload-box');
        const imageInput = document.getElementById('poster-image-input');
        const previewContainer = document.getElementById('poster-preview-container');
        const previewImage = document.getElementById('poster-preview-image');
        const removeImageBtn = document.getElementById('poster-remove-image');

        const posterType = document.getElementById('poster-type');
        const posterTitle = document.getElementById('poster-title');
        const posterSubtitle = document.getElementById('poster-subtitle');
        const posterDescription = document.getElementById('poster-description');
        const posterContact = document.getElementById('poster-contact');

        const generateBtn = document.getElementById('poster-generate-btn');
        const resultsGrid = document.getElementById('poster-results-grid');
        const placeholder = document.getElementById('poster-placeholder');
        const downloadAllBtn = document.getElementById('poster-download-all-btn');

        if (!uploadBox) return;

        // Toggle button handlers
        setupToggleButtons('#content-poster-generator', 'poster-color-btn');
        setupToggleButtons('#content-poster-generator', 'poster-style-btn');
        setupToggleButtons('#content-poster-generator', 'poster-size-btn');
        setupToggleButtons('#content-poster-generator', 'poster-count-btn');

        // Upload handling
        uploadBox.addEventListener('click', () => imageInput.click());

        uploadBox.addEventListener('dragover', (e) => {
            e.preventDefault();
            uploadBox.style.borderColor = '#8B5CF6';
        });

        uploadBox.addEventListener('dragleave', () => {
            uploadBox.style.borderColor = '#D1D5DB';
        });

        uploadBox.addEventListener('drop', async (e) => {
            e.preventDefault();
            uploadBox.style.borderColor = '#D1D5DB';
            const file = e.dataTransfer.files[0];
            if (file) await handleImageUpload(file);
        });

        imageInput.addEventListener('change', async (e) => {
            const file = e.target.files[0];
            if (file) await handleImageUpload(file);
        });

        removeImageBtn.addEventListener('click', () => {
            posterImageData = null;
            previewContainer.classList.add('hidden');
            uploadBox.classList.remove('hidden');
            imageInput.value = '';
        });

        async function handleImageUpload(file) {
            if (!file.type.startsWith('image/')) {
                alert('Please upload an image file!');
                return;
            }

            try {
                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        alert('Failed to convert HEIC image');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onload = (e) => {
                    const base64 = e.target.result.split(',')[1];
                    posterImageData = {
                        mimeType: processedFile.type,
                        data: base64
                    };

                    previewImage.src = e.target.result;
                    uploadBox.classList.add('hidden');
                    previewContainer.classList.remove('hidden');
                };
                reader.readAsDataURL(processedFile);

            } catch (error) {
                console.error('Error uploading image:', error);
                alert('Error uploading image');
            }
        }

        // Generate button handler
        generateBtn.addEventListener('click', async () => {
            const title = posterTitle.value.trim();

            if (!title) {
                alert('Mohon isi judul poster!');
                return;
            }

            const type = posterType.value;
            const subtitle = posterSubtitle.value.trim();
            const description = posterDescription.value.trim();
            const contact = posterContact.value.trim();

            const selectedColor = document.querySelector('.poster-color-btn.active').dataset.color;
            const selectedStyle = document.querySelector('.poster-style-btn.active').dataset.style;
            const selectedRatio = document.querySelector('.poster-size-btn.active').dataset.ratio;
            const selectedCount = parseInt(document.querySelector('.poster-count-btn.active').dataset.count);

            // Show loading
            generateBtn.disabled = true;
            generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';
            placeholder.classList.add('hidden');
            resultsGrid.classList.remove('hidden');
            resultsGrid.innerHTML = '';
            generatedPosters = [];

            // Create placeholder cards
            for (let i = 1; i <= selectedCount; i++) {
                const card = document.createElement('div');
                card.id = `poster-card-${i}`;
                card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center';
                card.style.aspectRatio = selectedRatio.replace(':', '/');
                card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-purple-600"></div>';
                resultsGrid.appendChild(card);
            }

            // Build comprehensive prompt
            const typeDescriptions = {
                'event': 'Event atau acara',
                'promo': 'Promosi, sale, atau diskon',
                'social-media': 'Social media post',
                'workshop': 'Workshop atau pelatihan',
                'seminar': 'Seminar atau webinar',
                'concert': 'Konser atau musik',
                'sport': 'Olahraga atau turnamen',
                'movie': 'Film atau cinema',
                'food': 'Makanan atau kuliner',
                'recruitment': 'Lowongan kerja',
                'announcement': 'Pengumuman atau informasi',
                'custom': 'Custom'
            };

            const colorDescriptions = {
                'vibrant': 'warna vibrant dengan gradien orange ke merah yang energik',
                'professional': 'warna profesional dengan gradien biru gelap yang corporate',
                'elegant': 'warna elegant hitam dan abu-abu yang sophisticated',
                'nature': 'warna natural hijau yang fresh',
                'sunset': 'warna sunset orange ke merah yang warm',
                'ocean': 'warna ocean biru cyan yang refreshing',
                'pastel': 'warna pastel pink dan ungu yang soft',
                'neon': 'warna neon purple dan pink yang eye-catching'
            };

            const styleDescriptions = {
                'modern': 'style modern dengan typography clean, geometric shapes, dan layout balanced',
                'minimalist': 'style minimalist dengan banyak white space, simple elements, dan fokus pada typography',
                'bold': 'style bold dengan warna kontras tinggi, typography tebal, dan visual impactful',
                'vintage': 'style vintage retro dengan texture aged, typography classic',
                'professional': 'style professional corporate dengan layout formal dan typography readable',
                'creative': 'style creative artistic dengan elemen unik dan layout experimental',
                'elegant': 'style elegant dengan typography serif, ornaments, dan layout symmetrical',
                'playful': 'style playful fun dengan ilustrasi cheerful dan typography rounded'
            };

            const basePrompt = `Create a professional poster design for ${typeDescriptions[type]}.

POSTER CONTENT:
Title: ${title}
${subtitle ? `Subtitle: ${subtitle}` : ''}
${description ? `Details: ${description}` : ''}
${contact ? `Contact: ${contact}` : ''}

DESIGN SPECIFICATIONS:
- Color Theme: ${colorDescriptions[selectedColor]}
- Design Style: ${styleDescriptions[selectedStyle]}
- Aspect Ratio: ${selectedRatio}
- Layout: Sesuaikan dengan jenis ${typeDescriptions[type]}

IMPORTANT DESIGN REQUIREMENTS:
1. TYPOGRAPHY:
   - Title harus BOLD, LARGE, dan menjadi focal point
   - Gunakan hierarchy yang jelas (title >> subtitle >> details)
   - Font readable dan sesuai dengan style yang dipilih
   - Kerning dan spacing yang profesional

2. LAYOUT & COMPOSITION:
   - Balance visual weight dengan baik
   - Gunakan grid system untuk alignment
   - White space yang cukup, jangan terlalu crowded
   - Visual flow yang natural dari top ke bottom

3. COLOR USAGE:
   - Apply ${colorDescriptions[selectedColor]}
   - Gunakan kontras yang baik untuk readability
   - Background dan foreground yang harmonis
   - Accent colors untuk highlight important info

4. VISUAL ELEMENTS:
   - Tambahkan decorative elements sesuai ${styleDescriptions[selectedStyle]}
   ${posterImageData ? '- Integrasikan uploaded image sebagai focal point atau background element' : '- Tambahkan relevant icons atau illustrations'}
   - Border atau frame jika sesuai dengan style
   - Pattern atau texture subtle untuk depth

5. INFORMATION HIERARCHY:
   - Title: Paling prominent di top atau center
   ${subtitle ? '- Subtitle: Secondary emphasis' : ''}
   ${description ? '- Details: Clear dan organized dengan bullet points atau sections' : ''}
   ${contact ? '- Contact info: Bottom atau corner dengan good visibility' : ''}

6. PROFESSIONAL TOUCHES:
   - Shadows atau depth untuk dimension
   - Consistent style throughout
   - Print-ready quality
   - Instagram/social media ready

FINAL OUTPUT:
Generate a complete, print-ready poster yang eye-catching, professional, dan sesuai untuk ${typeDescriptions[type]}. Make it STUNNING dan ready to use!`;

            // Generate all posters with retry
            let attempts = 0;
            const MAX_ATTEMPTS = 3;

            while (attempts < MAX_ATTEMPTS) {
                attempts++;
                generatedPosters = [];
                await Promise.allSettled(
                    Array.from({ length: selectedCount }, (_, i) =>
                        generateSinglePoster(i + 1, basePrompt, selectedRatio, posterImageData)
                    )
                );
                const successCount = generatedPosters.filter(Boolean).length;
                if (successCount > 0) break;
            }

            // Remove empty spinner cards
            document.querySelectorAll('[id^="poster-card-"]').forEach(card => {
                if (!card.querySelector('img')) card.remove();
            });
            const finalSuccessPosters = generatedPosters.filter(Boolean).length;
            if (finalSuccessPosters === 0) {
                alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                placeholder.classList.remove('hidden');
                resultsGrid.classList.add('hidden');
            } else {
                downloadAllBtn.classList.remove('hidden');
            }

            generateBtn.disabled = false;
            generateBtn.innerHTML = '<i class="fas fa-magic mr-2"></i>Generate Poster';
        });

        async function generateSinglePoster(index, prompt, aspectRatio, imageData) {
            const card = document.getElementById(`poster-card-${index}`);

            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const parts = [{ text: prompt }];

                if (imageData) {
                    parts.push({
                        inlineData: {
                            mimeType: imageData.mimeType,
                            data: imageData.data
                        }
                    });
                }

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: {
                            aspectRatio: aspectRatio
                        }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imagePartData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData);

                if (!imagePartData) throw new Error('No image data in response');

                const base64Data = imagePartData.inlineData.data;
                const mimeType = imagePartData.inlineData.mimeType || 'image/png';
                const imageUrl = `data:${mimeType};base64,${base64Data}`;
                const filename = `poster_${Date.now()}_${index}.png`;
                generatedPosters[index - 1] = { url: imageUrl, filename };

                if (card) {
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Poster ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-purple-500 to-pink-500 hover:from-purple-600 hover:to-pink-600 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }

            } catch (error) {
                console.error(`Error generating poster ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        resultsGrid.addEventListener('click', async (e) => {
            e.stopPropagation();

            const actionBtn = e.target.classList.contains('action-btn') ?
                            e.target : e.target.closest('.action-btn[data-action]');
            if (!actionBtn) return;

            const action = actionBtn.dataset.action;
            const index = parseInt(actionBtn.dataset.index, 10);
            const posterData = generatedPosters[index];
            if (!posterData) return;

            if (action === 'preview') {
                window.showPreviewModal(posterData.url);
            } else if (action === 'download') {
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(posterData.url, posterData.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(posterData.url, posterData.filename);
                }
            }
        });

        // Download all
        downloadAllBtn.addEventListener('click', async () => {
            const validPosters = generatedPosters.filter(Boolean);
            if (validPosters.length === 0) return;

            downloadAllBtn.disabled = true;
            downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

            for (let i = 0; i < validPosters.length; i++) {
                const poster = validPosters[i];
                if (window.downloadDataURINew) {
                    await window.downloadDataURINew(poster.url, poster.filename);
                } else if (window.downloadImage) {
                    await window.downloadImage(poster.url, poster.filename);
                }
                await new Promise(resolve => setTimeout(resolve, 500));
            }

            downloadAllBtn.disabled = false;
            downloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
        });

    })();

    // ==================== OBJECT REMOVER ====================
    (function() {
        const apiKey = "";
        const uploadInput = document.getElementById('remover-upload-input');
        const uploadBox = document.getElementById('remover-upload-box');
        const uploadPlaceholder = document.getElementById('remover-upload-placeholder');
        const previewContainer = document.getElementById('remover-preview-container');
        const previewImage = document.getElementById('remover-preview-image');
        const changePhotoBtn = document.getElementById('remover-change-photo-btn');
        const modeSection = document.getElementById('remover-mode-section');
        const manualTools = document.getElementById('remover-manual-tools');
        const autoOptions = document.getElementById('remover-auto-options');
        const watermarkOptions = document.getElementById('remover-watermark-options');
        const variationSection = document.getElementById('remover-variation-section');
        const generateSection = document.getElementById('remover-generate-section');
        const generateBtn = document.getElementById('remover-generate-btn');
        const resultsSection = document.getElementById('remover-results-section');
        const resultsGrid = document.getElementById('remover-results-grid');
        const downloadAllBtn = document.getElementById('remover-download-all-btn');

        // Canvas elements
        const canvas = document.getElementById('remover-canvas');
        const ctx = canvas ? canvas.getContext('2d') : null;
        const brushSizeSlider = document.getElementById('remover-brush-size');
        const brushSizeValue = document.getElementById('brush-size-value');
        const undoBtn = document.getElementById('remover-undo-btn');
        const redoBtn = document.getElementById('remover-redo-btn');
        const clearBtn = document.getElementById('remover-clear-btn');
        const resetBtn = document.getElementById('remover-reset-btn');

        let uploadedImage = null;
        let uploadedImageData = null;
        let selectedMode = 'auto';
        let selectedVariations = 2;
        let isDrawing = false;
        let brushSize = 20;
        let maskHistory = [];
        let historyIndex = -1;
        let generatedResults = [];

        // Upload handling
        if (uploadBox && uploadInput) {
            uploadBox.addEventListener('click', () => uploadInput.click());

            uploadBox.addEventListener('dragover', (e) => {
                e.preventDefault();
                uploadBox.style.borderColor = '#EF4444';
            });

            uploadBox.addEventListener('dragleave', () => {
                uploadBox.style.borderColor = '#D1D5DB';
            });

            uploadBox.addEventListener('drop', (e) => {
                e.preventDefault();
                uploadBox.style.borderColor = '#D1D5DB';
                const file = e.dataTransfer.files[0];
                if (file) handleFileUpload(file);
            });

            uploadInput.addEventListener('change', (e) => {
                const file = e.target.files[0];
                if (file) handleFileUpload(file);
            });
        }

        if (changePhotoBtn) {
            changePhotoBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                uploadInput.click();
            });
        }

        async function handleFileUpload(file) {
            try {
                let processedFile = file;

                // HEIC conversion
                if (file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion error:', err);
                        alert('Gagal mengonversi file HEIC. Silakan gunakan format JPG/PNG.');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onload = (e) => {
                    const img = new Image();
                    img.onload = () => {
                        uploadedImage = img;

                        // Convert to base64
                        const tempCanvas = document.createElement('canvas');
                        tempCanvas.width = img.width;
                        tempCanvas.height = img.height;
                        const tempCtx = tempCanvas.getContext('2d');
                        tempCtx.drawImage(img, 0, 0);
                        const base64 = tempCanvas.toDataURL('image/png').split(',')[1];

                        uploadedImageData = {
                            data: base64,
                            mimeType: 'image/png'
                        };

                        // Show preview
                        previewImage.src = e.target.result;
                        uploadPlaceholder.classList.add('hidden');
                        previewContainer.classList.remove('hidden');

                        // Show next sections
                        modeSection.classList.remove('hidden');
                        updateUIBasedOnMode();
                        variationSection.classList.remove('hidden');
                        generateSection.classList.remove('hidden');

                        // Setup canvas for manual mode
                        if (canvas) {
                            setupCanvas(img);
                        }
                    };
                    img.src = e.target.result;
                };
                reader.readAsDataURL(processedFile);
            } catch (error) {
                console.error('Upload error:', error);
                alert('Gagal mengupload foto. Silakan coba lagi.');
            }
        }

        function setupCanvas(img) {
            const maxWidth = 800;
            const scale = Math.min(1, maxWidth / img.width);

            canvas.width = img.width * scale;
            canvas.height = img.height * scale;

            drawCanvas();
            saveMaskHistory();
        }

        function drawCanvas() {
            if (!canvas || !ctx || !uploadedImage) return;

            ctx.clearRect(0, 0, canvas.width, canvas.height);
            ctx.drawImage(uploadedImage, 0, 0, canvas.width, canvas.height);
        }

        // Mode selection
        document.querySelectorAll('.remover-mode-btn').forEach(btn => {
            btn.addEventListener('click', () => {
                document.querySelectorAll('.remover-mode-btn').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                selectedMode = btn.dataset.mode;
                updateUIBasedOnMode();
            });
        });

        function updateUIBasedOnMode() {
            const btnSpan = generateBtn ? generateBtn.querySelector('span') : null;
            if (selectedMode === 'manual') {
                manualTools.classList.remove('hidden');
                autoOptions.classList.add('hidden');
                if (watermarkOptions) watermarkOptions.classList.add('hidden');
                if (btnSpan) btnSpan.textContent = 'Hapus Object Sekarang';
            } else if (selectedMode === 'watermark') {
                manualTools.classList.add('hidden');
                autoOptions.classList.add('hidden');
                if (watermarkOptions) watermarkOptions.classList.remove('hidden');
                if (btnSpan) btnSpan.textContent = 'Hapus Watermark Sekarang';
            } else {
                manualTools.classList.add('hidden');
                autoOptions.classList.remove('hidden');
                if (watermarkOptions) watermarkOptions.classList.add('hidden');
                if (btnSpan) btnSpan.textContent = 'Hapus Object Sekarang';
            }
        }

        // Variation selection
        document.querySelectorAll('.remover-variation-btn').forEach(btn => {
            btn.addEventListener('click', () => {
                document.querySelectorAll('.remover-variation-btn').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                selectedVariations = parseInt(btn.dataset.count);
            });
        });

        // Brush controls
        if (brushSizeSlider && brushSizeValue) {
            brushSizeSlider.addEventListener('input', (e) => {
                brushSize = parseInt(e.target.value);
                brushSizeValue.textContent = brushSize;
            });
        }

        // Canvas drawing
        if (canvas) {
            let lastX = 0;
            let lastY = 0;

            canvas.addEventListener('mousedown', startDrawing);
            canvas.addEventListener('mousemove', draw);
            canvas.addEventListener('mouseup', stopDrawing);
            canvas.addEventListener('mouseout', stopDrawing);

            // Touch support
            canvas.addEventListener('touchstart', (e) => {
                e.preventDefault();
                const touch = e.touches[0];
                const rect = canvas.getBoundingClientRect();
                lastX = touch.clientX - rect.left;
                lastY = touch.clientY - rect.top;
                isDrawing = true;
            });

            canvas.addEventListener('touchmove', (e) => {
                e.preventDefault();
                if (!isDrawing) return;
                const touch = e.touches[0];
                const rect = canvas.getBoundingClientRect();
                drawLine(lastX, lastY, touch.clientX - rect.left, touch.clientY - rect.top);
            });

            canvas.addEventListener('touchend', stopDrawing);

            function startDrawing(e) {
                isDrawing = true;
                const rect = canvas.getBoundingClientRect();
                lastX = e.clientX - rect.left;
                lastY = e.clientY - rect.top;
            }

            function draw(e) {
                if (!isDrawing) return;
                const rect = canvas.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                drawLine(lastX, lastY, x, y);
            }

            function drawLine(x1, y1, x2, y2) {
                ctx.strokeStyle = 'rgba(239, 68, 68, 0.5)';
                ctx.lineWidth = brushSize;
                ctx.lineCap = 'round';
                ctx.lineJoin = 'round';

                ctx.beginPath();
                ctx.moveTo(x1, y1);
                ctx.lineTo(x2, y2);
                ctx.stroke();

                lastX = x2;
                lastY = y2;
            }

            function stopDrawing() {
                if (isDrawing) {
                    isDrawing = false;
                    saveMaskHistory();
                }
            }
        }

        // History management
        function saveMaskHistory() {
            if (!canvas) return;

            historyIndex++;
            maskHistory = maskHistory.slice(0, historyIndex);
            maskHistory.push(canvas.toDataURL());

            if (maskHistory.length > 20) {
                maskHistory.shift();
                historyIndex--;
            }
        }

        if (undoBtn) {
            undoBtn.addEventListener('click', () => {
                if (historyIndex > 0) {
                    historyIndex--;
                    loadMaskFromHistory();
                }
            });
        }

        if (redoBtn) {
            redoBtn.addEventListener('click', () => {
                if (historyIndex < maskHistory.length - 1) {
                    historyIndex++;
                    loadMaskFromHistory();
                }
            });
        }

        function loadMaskFromHistory() {
            if (!canvas || !ctx || historyIndex < 0 || historyIndex >= maskHistory.length) return;

            const img = new Image();
            img.onload = () => {
                ctx.clearRect(0, 0, canvas.width, canvas.height);
                ctx.drawImage(img, 0, 0);
            };
            img.src = maskHistory[historyIndex];
        }

        if (clearBtn) {
            clearBtn.addEventListener('click', () => {
                if (confirm('Hapus semua tandakan?')) {
                    drawCanvas();
                    saveMaskHistory();
                }
            });
        }

        if (resetBtn) {
            resetBtn.addEventListener('click', () => {
                if (confirm('Reset ke foto asli?')) {
                    historyIndex = 0;
                    maskHistory = [];
                    drawCanvas();
                    saveMaskHistory();
                }
            });
        }

        // Generate button
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!uploadedImageData) {
                    alert('Silakan upload foto terlebih dahulu!');
                    return;
                }

                generateBtn.disabled = true;
                const loadingLabel = selectedMode === 'watermark' ? 'Menghapus Watermark...' : 'Menghapus Object...';
                generateBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i>${loadingLabel}`;

                resultsSection.classList.remove('hidden');
                generatedResults = [];

                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedResults = new Array(selectedVariations).fill(null);
                    resultsGrid.innerHTML = '';
                    for (let i = 1; i <= selectedVariations; i++) {
                        const card = document.createElement('div');
                        card.id = `remover-card-${i}`;
                        card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-[3/4]';
                        card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-red-600"></div>';
                        resultsGrid.appendChild(card);
                    }
                    await Promise.allSettled(Array.from({ length: selectedVariations }, (_, i) => generateSingleResult(i + 1)));
                    successCount = generatedResults.filter(Boolean).length;
                }

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    generateBtn.disabled = false;
                    generateBtn.innerHTML = `<i class="fas fa-magic mr-2"></i><span>${selectedMode === 'watermark' ? 'Hapus Watermark Sekarang' : 'Hapus Object Sekarang'}</span>`;
                    return;
                }

                document.querySelectorAll('[id^="remover-card-"]').forEach(card => { if (!card.querySelector('img')) card.remove(); });
                generatedResults = generatedResults.filter(Boolean);
                if (generatedResults.length > 0) {
                    downloadAllBtn.classList.remove('hidden');
                }

                generateBtn.disabled = false;
                generateBtn.innerHTML = `<i class="fas fa-magic mr-2"></i><span>${selectedMode === 'watermark' ? 'Hapus Watermark Sekarang' : 'Hapus Object Sekarang'}</span>`;
            });
        }

        async function generateSingleResult(index) {
            const card = document.getElementById(`remover-card-${index}`);

            try {
                let prompt = '';
                let maskData = null;

                if (selectedMode === 'manual') {
                    // Extract mask from canvas
                    const maskCanvas = document.createElement('canvas');
                    maskCanvas.width = canvas.width;
                    maskCanvas.height = canvas.height;
                    const maskCtx = maskCanvas.getContext('2d');

                    // Get only the red mask layer
                    maskCtx.drawImage(canvas, 0, 0);
                    const imageData = maskCtx.getImageData(0, 0, maskCanvas.width, maskCanvas.height);
                    const data = imageData.data;

                    // Create black/white mask (white = areas to remove)
                    for (let i = 0; i < data.length; i += 4) {
                        const red = data[i];
                        const isMarked = red > 200; // Red marked areas

                        data[i] = isMarked ? 255 : 0;     // R
                        data[i + 1] = isMarked ? 255 : 0; // G
                        data[i + 2] = isMarked ? 255 : 0; // B
                        data[i + 3] = 255;                // A
                    }

                    maskCtx.putImageData(imageData, 0, 0);
                    maskData = maskCanvas.toDataURL('image/png').split(',')[1];

                    prompt = `CRITICAL OBJECT REMOVAL TASK - INPAINTING REQUIRED:

You MUST completely remove the marked/red areas from this image and fill them perfectly.

MANDATORY ACTIONS:
✓ IDENTIFY all areas marked in RED - these are the objects to REMOVE
✓ DELETE these marked objects COMPLETELY from the image
✓ FILL the empty areas with appropriate background content
✓ EXTEND surrounding patterns, textures, and colors naturally into the removed areas
✓ RECONSTRUCT background based on context clues from surrounding areas
✓ BLEND filled areas seamlessly - NO visible edges, seams, or artifacts
✓ MATCH lighting, shadows, and perspective of the surrounding area
✓ PRESERVE all unmarked areas exactly as they are

CRITICAL INPAINTING RULES:
1. The RED marked areas MUST be completely removed
2. Fill removed areas by intelligently extending the background
3. Use surrounding context to reconstruct what should be there
4. Make it look like the marked objects NEVER existed
5. Result MUST show obvious removal - the marked objects should be GONE
6. NO traces, outlines, or remnants of removed objects
7. Perfect photorealistic blending and reconstruction

VARIATION ${index}: ${index === 1 ? 'Standard inpainting approach' : 'Alternative fill pattern/technique'}

Generate a clean image with ALL marked objects removed and areas filled seamlessly.`;
                } else if (selectedMode === 'watermark') {
                    // Watermark mode
                    const watermarkTextEl = document.getElementById('remover-watermark-text');
                    const specificWatermark = watermarkTextEl ? watermarkTextEl.value.trim() : '';
                    const watermarkTarget = specificWatermark
                        ? `dengan teks/nama watermark: "${specificWatermark}"`
                        : 'semua jenis watermark yang ada (auto-detect)';

                    prompt = `CRITICAL WATERMARK REMOVAL TASK:

You MUST detect and completely remove ALL watermarks, text overlays, and brand logos from this image.

TARGET: Remove ${watermarkTarget}

MANDATORY DETECTION - Identify and remove:
\u2713 Text watermarks (e.g. "Shutterstock", "Getty Images", "123RF", "iStock")
\u2713 Copyright notices and \u00A9 symbols
\u2713 Brand logos and studio logos overlaid on the image
\u2713 Timestamps and date overlays
\u2713 Social media usernames (@mentions) and handles
\u2713 Promotional text overlays and advertisement labels
\u2713 Photo credit text and photographer names
\u2713 Semi-transparent or fully opaque text/logo overlays
\u2713 Repeated tile watermarks across the image

MANDATORY REMOVAL RULES:
1. COMPLETELY ERASE every watermark, text overlay, and logo detected
2. RECONSTRUCT the underlying image content where watermarks were placed
3. MATCH color, texture, and pattern of surrounding pixels seamlessly
4. BLEND removed areas - NO visible traces, outlines, or ghost text
5. PRESERVE ALL other image content (subjects, background, colors) perfectly
6. Result MUST look like the original clean image with ZERO watermarks
7. If watermarks are semi-transparent, remove them completely including color tint

VARIATION ${index}: ${index === 1 ? 'Standard watermark removal' : 'Alternative reconstruction technique'}

Generate a completely clean image with all watermarks and text overlays removed.`;

                } else {
                    // Auto mode
                    prompt = `CRITICAL OBJECT REMOVAL TASK - AUTO DETECTION:

You MUST automatically detect unwanted objects/distractions and remove them from this image.

MANDATORY DETECTION AND REMOVAL:
✓ DETECT background people who distract from main subject
✓ DETECT watermarks, logos, text overlays, timestamps
✓ DETECT wires, poles, power lines, cables
✓ DETECT photobombers or unwanted people in background
✓ DETECT trash, debris, litter, or clutter
✓ DETECT blemishes, spots, or imperfections
✓ DETECT any other obvious distracting elements

✓ REMOVE ALL detected unwanted objects completely
✓ FILL removed areas with appropriate background reconstruction
✓ PRESERVE the main subject/focus of the image perfectly
✓ CLEAN UP the image to make it professional and distraction-free

CRITICAL AUTO-REMOVAL RULES:
1. Identify what is MAIN SUBJECT (keep) vs DISTRACTION (remove)
2. Remove ALL unwanted elements you detect
3. Fill removed areas by extending surrounding background
4. Make removals invisible - seamless blending required
5. Result MUST look cleaner and more professional than original
6. Main subject must remain untouched and perfect
7. Background should look natural as if distractions never existed

VARIATION ${index}: ${index === 1 ? 'Conservative auto-removal' : 'Aggressive auto-cleanup'}

Generate a professionally cleaned image with all distractions removed.`;
                }

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const parts = [{ text: prompt }];

                // Add original image
                parts.push({
                    inlineData: {
                        mimeType: uploadedImageData.mimeType,
                        data: uploadedImageData.data
                    }
                });

                // Add mask if manual mode
                if (maskData) {
                    parts.push({
                        inlineData: {
                            mimeType: 'image/png',
                            data: maskData
                        }
                    });
                }

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE']
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imagePart = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData);
                if (!imagePart) throw new Error('No image data in response');
                const base64Data = imagePart.inlineData.data;
                const mimeType = imagePart.inlineData.mimeType || 'image/png';
                const imageUrl = `data:${mimeType};base64,${base64Data}`;

                const filePrefix = selectedMode === 'watermark' ? 'watermark_removed' : 'object_removed';
                const filename = `${filePrefix}_${Date.now()}_${index}.png`;
                generatedResults[index - 1] = { url: imageUrl, filename };

                if (card) card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Result ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-red-500 to-red-600 hover:from-red-600 hover:to-red-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;

            } catch (error) {
                console.error(`Error removing object ${index}:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Results action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.classList.contains('action-btn') ?
                                  e.target : e.target.closest('.action-btn[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const resultData = generatedResults[index];

                if (action === 'preview') {
                    window.showPreviewModal(resultData.url);
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(resultData.url, resultData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(resultData.url, resultData.filename);
                    }
                }
            });
        }

        // Download all
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async () => {
                if (generatedResults.length === 0) return;

                downloadAllBtn.disabled = true;
                downloadAllBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Downloading...';

                for (let i = 0; i < generatedResults.length; i++) {
                    const result = generatedResults[i];
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(result.url, result.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(result.url, result.filename);
                    }
                    await new Promise(resolve => setTimeout(resolve, 500));
                }

                downloadAllBtn.disabled = false;
                downloadAllBtn.innerHTML = '<i class="fas fa-download mr-2"></i>Download Semua';
            });
        }

    })();

    // ==================== AI CAPTION GENERATOR ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const imageUpload = document.getElementById('caption-image-upload');
        const uploadBox = document.querySelector('.upload-box-caption');
        const imagePreview = document.getElementById('caption-image-preview');
        const previewContainer = document.getElementById('caption-preview-container');
        const removePreviewBtn = document.getElementById('caption-remove-preview');

        const platformButtons = document.querySelectorAll('#caption-platform-selection [data-platform]');
        const toneButtons = document.querySelectorAll('#caption-tone-selection [data-tone]');
        const countButtons = document.querySelectorAll('#caption-count-selection [data-count]');
        const countText = document.getElementById('caption-count-text');

        const generateBtn = document.getElementById('caption-generate-btn');
        const errorMessage = document.getElementById('caption-error-message');
        const errorText = document.getElementById('caption-error-text');

        const loadingIndicator = document.getElementById('caption-loading-indicator');
        const resultsSection = document.getElementById('caption-results-section');
        const emptyState = document.getElementById('caption-empty-state');
        const resultsGrid = document.getElementById('caption-results-grid');
        const resultsCount = document.getElementById('caption-results-count');
        const downloadAllBtn = document.getElementById('caption-download-all-btn');

        // State
        let originalImageData = null;
        let selectedPlatform = 'instagram';
        let selectedTone = 'casual';
        let selectedCount = 7;
        let generatedCaptions = [];

        // Upload handler
        if (uploadBox) {
            uploadBox.addEventListener('click', () => imageUpload?.click());
        }

        if (imageUpload) {
            imageUpload.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    originalImageData = reader.result;

                    if (imagePreview && previewContainer) {
                        imagePreview.src = reader.result;
                        previewContainer.classList.remove('hidden');
                    }

                    if (generateBtn) generateBtn.disabled = false;
                };
                reader.readAsDataURL(processedFile);
            });
        }

        if (removePreviewBtn) {
            removePreviewBtn.addEventListener('click', () => {
                originalImageData = null;
                imageUpload.value = '';
                if (previewContainer) previewContainer.classList.add('hidden');
                if (imagePreview) imagePreview.src = '';
                if (generateBtn) generateBtn.disabled = true;
            });
        }

        // Platform selection
        platformButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                platformButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedPlatform = btn.dataset.platform;
            });
        });

        // Tone selection
        toneButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                toneButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedTone = btn.dataset.tone;
            });
        });

        // Count selection
        countButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                countButtons.forEach(b => b.classList.remove('count-selected'));
                btn.classList.add('count-selected');
                selectedCount = parseInt(btn.dataset.count, 10);
                if (countText) countText.textContent = selectedCount;
            });
        });

        // Generate captions
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!originalImageData || generateBtn.disabled) return;

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                hideError();
                if (emptyState) emptyState.classList.add('hidden');
                if (resultsSection) resultsSection.classList.add('hidden');
                if (loadingIndicator) loadingIndicator.classList.remove('hidden');

                generatedCaptions = [];
                if (resultsGrid) resultsGrid.innerHTML = '';

                try {
                    const base64 = originalImageData.split(',')[1];

                    const toneDescriptions = {
                        casual: 'santai, friendly, menggunakan bahasa sehari-hari, emoji fun',
                        professional: 'formal, bisnis, trustworthy, persuasif',
                        storytelling: 'bercerita, emotional, engaging, relatable'
                    };

                    const platformDescriptions = {
                        instagram: 'Instagram post (fokus visual aesthetics, storytelling, hashtags)',
                        tiktok: 'TikTok post (energetic, trending, Gen Z language, viral hooks)',
                        facebook: 'Facebook post (informative, conversational, community-focused)'
                    };

                    // Unicode escape sequences untuk obfuscation safety
                    const lineCharCapt = '\u2501'; // ━
                    const checkMark = '\u2713'; // ✓

                    const prompt = `Kamu adalah expert social media copywriter untuk produk affiliate marketing.

TUGAS: Buat ${selectedCount} variasi caption menarik untuk ${platformDescriptions[selectedPlatform]} dengan tone ${toneDescriptions[selectedTone]}.

ANALISA FOTO:
Produk yang difoto adalah: [analisa gambar dan identifikasi produk]
Target audience: [tentukan target audience berdasarkan produk]
Key selling points: [identifikasi keunggulan produk dari visual]

FORMAT OUTPUT (untuk SETIAP caption):
${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}
CAPTION #[nomor]

[HOOK yang powerful dan menarik perhatian - 1-2 kalimat]

[BODY caption yang engaging - jelaskan produk, manfaat, pengalaman - 3-5 kalimat]

[CALL-TO-ACTION yang kuat dan jelas]

HASHTAGS: [15-30 hashtags yang relevant, mix popular & niche]

EMOJI: [suggest 5-10 emoji yang cocok untuk digunakan di caption]
${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}${lineCharCapt}

PENTING:
${checkMark} Setiap caption HARUS berbeda dan unik
${checkMark} Hook harus attention-grabbing dan bikin penasaran
${checkMark} Fokus pada benefit, bukan feature
${checkMark} Include social proof hints (testimoni, rating, etc)
${checkMark} CTA harus spesifik (e.g., "Klik link di bio!", "Swipe up!", "Order sekarang!")
${checkMark} Hashtags campuran: trending + specific + branded
${checkMark} Gunakan copywriting techniques: FOMO, urgency, exclusivity
${checkMark} Natural dan tidak terkesan hard-selling

Generate ${selectedCount} caption variations sekarang!`;

                    const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`;

                    const payload = {
                        contents: [{
                            parts: [
                                { text: prompt },
                                { inlineData: { mimeType: 'image/jpeg', data: base64 } }
                            ]
                        }],
                        generationConfig: {
                            maxOutputTokens: 8192,
                            temperature: 1.0
                        }
                    };

                    const response = await fetch(apiUrl, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(payload)
                    });

                    if (!response.ok) {
                        const errorData = await response.json();
                        console.error('API error response:', errorData);
                        throw new Error(errorData.error?.message || `API error: ${response.status}`);
                    }

                    const result = await response.json();

                    if (!result.candidates?.[0]?.content?.parts?.[0]?.text) {
                        console.error('No text in result:', result);
                        throw new Error('Failed to generate captions - no text returned');
                    }

                    const fullText = result.candidates[0].content.parts[0].text;

                    // Parse captions (split by caption number markers) - menggunakan Unicode escape untuk obfuscation safety
                    const lineCharCaption = '\u2501'; // ━
                    const captionMatches = fullText.split(new RegExp(lineCharCaption + '+'));
                    const parsedCaptions = [];

                    for (let i = 1; i < captionMatches.length; i += 2) {
                        if (captionMatches[i]?.trim()) {
                            parsedCaptions.push(captionMatches[i].trim());
                        }
                    }

                    // If parsing fails, split by CAPTION # markers
                    if (parsedCaptions.length === 0) {
                        const altMatches = fullText.split(/CAPTION #\d+/i);
                        for (let i = 1; i < altMatches.length; i++) {
                            if (altMatches[i]?.trim()) {
                                parsedCaptions.push(altMatches[i].trim());
                            }
                        }
                    }

                    // Store captions
                    generatedCaptions = parsedCaptions.map((caption, index) => ({
                        id: index + 1,
                        text: caption
                    }));

                    if (generatedCaptions.length === 0) {
                        // Fallback: use full text as single caption
                        generatedCaptions = [{ id: 1, text: fullText }];
                    }

                    // Display results
                    displayCaptions();

                    if (loadingIndicator) loadingIndicator.classList.add('hidden');
                    if (resultsSection) resultsSection.classList.remove('hidden');
                    if (resultsCount) resultsCount.textContent = generatedCaptions.length;

                } catch (error) {
                    console.error('Caption generation error:', error);
                    showError(error.message || 'Gagal generate captions. Silakan coba lagi.');
                    if (loadingIndicator) loadingIndicator.classList.add('hidden');
                    if (emptyState) emptyState.classList.remove('hidden');
                } finally {
                    generateBtn.disabled = false;
                    generateBtn.innerHTML = originalHTML;
                }
            });
        }

        function displayCaptions() {
            if (!resultsGrid) return;

            resultsGrid.innerHTML = '';

            generatedCaptions.forEach((caption, index) => {
                const card = document.createElement('div');
                card.className = 'bg-white p-6 rounded-xl shadow-md hover:shadow-lg transition-all duration-300 border-2 border-gray-100';

                card.innerHTML = `
                    <div class="flex items-center justify-between mb-4">
                        <h4 class="font-bold text-gray-800 flex items-center gap-2">
                            <span class="flex items-center justify-center w-8 h-8 rounded-full bg-gradient-to-br from-purple-500 to-pink-500 text-white text-sm">${caption.id}</span>
                            Caption #${caption.id}
                        </h4>
                        <button data-action="copy" data-index="${index}" class="caption-action-btn text-purple-600 hover:text-purple-700 font-semibold text-sm flex items-center gap-1 px-3 py-2 rounded-lg hover:bg-purple-50 transition-all">
                            <i class="fas fa-copy"></i>
                            <span>Copy</span>
                        </button>
                    </div>
                    <div class="text-gray-700 text-sm whitespace-pre-wrap leading-relaxed max-h-96 overflow-y-auto" style="-webkit-overflow-scrolling: touch;">
${caption.text}
                    </div>
                `;

                resultsGrid.appendChild(card);
            });
        }

        // Event delegation for copy buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);

                if (action === 'copy' && generatedCaptions[index]) {
                    try {
                        await navigator.clipboard.writeText(generatedCaptions[index].text);

                        const originalHTML = actionBtn.innerHTML;
                        actionBtn.innerHTML = '<i class="fas fa-check mr-1"></i>Copied!';
                        actionBtn.classList.add('text-green-600');

                        setTimeout(() => {
                            actionBtn.innerHTML = originalHTML;
                            actionBtn.classList.remove('text-green-600');
                        }, 2000);
                    } catch (err) {
                        console.error('Copy failed:', err);
                        showError('Gagal copy caption');
                    }
                }
            });
        }

        // Download all as TXT
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', () => {
                if (generatedCaptions.length === 0) return;

                let txtContent = `AI-GENERATED CAPTIONS - ${selectedPlatform.toUpperCase()}\n`;
                txtContent += `Tone: ${selectedTone}\n`;
                txtContent += `Generated: ${new Date().toLocaleString()}\n`;
                txtContent += `\n${'='.repeat(60)}\n\n`;

                generatedCaptions.forEach((caption, index) => {
                    txtContent += `CAPTION #${index + 1}\n`;
                    txtContent += `${'━'.repeat(60)}\n`;
                    txtContent += `${caption.text}\n`;
                    txtContent += `\n${'━'.repeat(60)}\n\n`;
                });

                const blob = new Blob([txtContent], { type: 'text/plain;charset=utf-8' });
                const filename = `captions_${selectedPlatform}_${Date.now()}.txt`;

                if (window.saveAs) {
                    window.saveAs(blob, filename);
                } else {
                    const url = URL.createObjectURL(blob);
                    const a = document.createElement('a');
                    a.href = url;
                    a.download = filename;
                    a.click();
                    URL.revokeObjectURL(url);
                }
            });
        }

        function showError(message) {
            if (errorText) errorText.textContent = message;
            if (errorMessage) errorMessage.classList.remove('hidden');
        }

        function hideError() {
            if (errorMessage) errorMessage.classList.add('hidden');
        }

    })();

    // ==================== PRODUCT COMPARISON GENERATOR ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const productSlots = [
            {
                slot: document.getElementById('comparison-product-1-slot'),
                upload: document.getElementById('comparison-product-1-upload'),
                preview: document.getElementById('comparison-product-1-preview'),
                remove: document.getElementById('comparison-product-1-remove'),
                data: null
            },
            {
                slot: document.getElementById('comparison-product-2-slot'),
                upload: document.getElementById('comparison-product-2-upload'),
                preview: document.getElementById('comparison-product-2-preview'),
                remove: document.getElementById('comparison-product-2-remove'),
                data: null
            },
            {
                slot: document.getElementById('comparison-product-3-slot'),
                upload: document.getElementById('comparison-product-3-upload'),
                preview: document.getElementById('comparison-product-3-preview'),
                remove: document.getElementById('comparison-product-3-remove'),
                data: null
            }
        ];

        const styleButtons = document.querySelectorAll('#comparison-style-selection [data-style]');
        const aspectButtons = document.querySelectorAll('#comparison-aspect-selection [data-aspect]');

        const showProsCons = document.getElementById('comparison-show-pros-cons');
        const showFeatures = document.getElementById('comparison-show-features');
        const showWinner = document.getElementById('comparison-show-winner');

        const generateBtn = document.getElementById('comparison-generate-btn');
        const errorMessage = document.getElementById('comparison-error-message');
        const errorText = document.getElementById('comparison-error-text');

        // Count selection (1-10), default 4
        let selectedCount = 4;
        const comparisonCountSel = document.getElementById('comparison-count-selection-grid');
        if (comparisonCountSel) {
            comparisonCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                comparisonCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        const loadingIndicator = document.getElementById('comparison-loading-indicator');
        const loadingText = document.getElementById('comparison-loading-text');
        const progressBar = document.getElementById('comparison-progress-bar');
        const progressText = document.getElementById('comparison-progress-text');

        const resultsSection = document.getElementById('comparison-results-section');
        const emptyState = document.getElementById('comparison-empty-state');
        const resultsGrid = document.getElementById('comparison-results-grid');
        const resultsCount = document.getElementById('comparison-results-count');
        const downloadAllBtn = document.getElementById('comparison-download-all-btn');

        // selectedCount sudah dideklarasikan di scope luar (line 65562) sebagai
        // let dengan default 4 + count selector 1-10. Konstanta lama 15 dihapus.

        // State
        let selectedStyle = 'side-by-side';
        let selectedAspect = '16:9';
        let generatedComparisons = [];

        // Product upload handlers
        productSlots.forEach((product, index) => {
            if (product.slot) {
                product.slot.addEventListener('click', () => product.upload?.click());
            }

            if (product.upload) {
                product.upload.addEventListener('change', async (e) => {
                    const file = e.target.files[0];
                    if (!file) return;

                    let processedFile = file;

                    // HEIC conversion
                    if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                        try {
                            const convertedBlob = await heic2any({
                                blob: file,
                                toType: 'image/jpeg',
                                quality: 0.9
                            });
                            processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                                type: 'image/jpeg'
                            });
                        } catch (err) {
                            console.error('HEIC conversion failed:', err);
                            showError('Gagal mengkonversi gambar HEIC');
                            return;
                        }
                    }

                    const reader = new FileReader();
                    reader.onloadend = () => {
                        product.data = reader.result;

                        if (product.preview) {
                            product.preview.src = reader.result;
                            product.preview.classList.remove('hidden');
                        }

                        if (product.slot) {
                            product.slot.classList.add('has-image');
                            const content = product.slot.querySelector('.product-slot-content');
                            if (content) content.classList.add('hidden');
                        }

                        if (product.remove) {
                            product.remove.classList.remove('hidden');
                        }

                        updateGenerateButton();
                    };
                    reader.readAsDataURL(processedFile);
                });
            }

            if (product.remove) {
                product.remove.addEventListener('click', (e) => {
                    e.stopPropagation();
                    product.data = null;
                    product.upload.value = '';

                    if (product.preview) {
                        product.preview.src = '';
                        product.preview.classList.add('hidden');
                    }

                    if (product.slot) {
                        product.slot.classList.remove('has-image');
                        const content = product.slot.querySelector('.product-slot-content');
                        if (content) content.classList.remove('hidden');
                    }

                    product.remove.classList.add('hidden');
                    updateGenerateButton();
                });
            }
        });

        function updateGenerateButton() {
            const hasMinProducts = productSlots[0].data && productSlots[1].data;
            if (generateBtn) generateBtn.disabled = !hasMinProducts;
        }

        // Style selection
        styleButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                styleButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedStyle = btn.dataset.style;
            });
        });

        // Aspect ratio selection
        aspectButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                aspectButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAspect = btn.dataset.aspect;
            });
        });

        // Generate comparisons
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!productSlots[0].data || !productSlots[1].data || generateBtn.disabled) return;

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                hideError();
                if (emptyState) emptyState.classList.add('hidden');
                if (resultsSection) resultsSection.classList.add('hidden');
                if (loadingIndicator) loadingIndicator.classList.remove('hidden');

                generatedComparisons = [];
                if (resultsGrid) resultsGrid.innerHTML = '';

                // Pre-create placeholder cards
                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `comparison-card-${i}`;
                    card.className = 'relative rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-video';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-blue-600"></div>';
                    resultsGrid?.appendChild(card);
                }

                let attempts = 0;
                const MAX_ATTEMPTS = 3;

                while (attempts < MAX_ATTEMPTS) {
                    attempts++;
                    generatedComparisons = [];
                    await Promise.allSettled(
                        Array.from({ length: selectedCount }, (_, i) => generateSingleComparison(i + 1))
                    );
                    const successCount = generatedComparisons.filter(Boolean).length;
                    if (successCount > 0) break;
                }

                // Remove empty spinner cards
                document.querySelectorAll('[id^="comparison-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                if (loadingIndicator) loadingIndicator.classList.add('hidden');

                const finalSuccessComp = generatedComparisons.filter(Boolean).length;
                if (finalSuccessComp === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    if (emptyState) emptyState.classList.remove('hidden');
                } else {
                    if (resultsSection) resultsSection.classList.remove('hidden');
                    if (resultsCount) resultsCount.textContent = finalSuccessComp;
                }

                generateBtn.disabled = false;
                generateBtn.innerHTML = originalHTML;
            });
        }

        async function generateSingleComparison(index) {
            const card = document.getElementById(`comparison-card-${index}`);

            try {
                const products = productSlots.filter(p => p.data).map(p => p.data);
                const base64Images = products.map(data => data.split(',')[1]);

                const featuresList = [];
                if (showProsCons?.checked) featuresList.push('PROS & CONS untuk setiap produk');
                if (showFeatures?.checked) featuresList.push('FEATURE HIGHLIGHTS visual');
                if (showWinner?.checked) featuresList.push('WINNER BADGE / recommendation');

                const variationText = index === 1 ? 'Standard professional comparison' :
                    index === 2 ? 'Focus on value for money perspective' :
                    index === 3 ? 'Emphasize quality & features' :
                    index === 4 ? 'Alternative visual style & perspective' :
                    `Unique creative angle, variation ${index}`;

                const prompt = `Buat visual perbandingan produk yang SANGAT PROFESSIONAL dan MENARIK!

DETAIL PERBANDINGAN:
- Jumlah produk: ${products.length} produk
- Layout style: ${selectedStyle === 'side-by-side' ? 'Side-by-side comparison (produk sejajar)' : 'Grid layout (produk dalam grid)'}
- Aspect ratio: ${selectedAspect}
- Tampilkan: ${featuresList.join(', ')}

PANDUAN DESAIN VISUAL:
\u2713 Layout ${selectedStyle === 'side-by-side' ? 'horizontal side-by-side' : 'grid'} yang clean dan modern
\u2713 Tempatkan produk dalam box/card yang rapi dengan border jelas
\u2713 Background gradient atau solid color yang eye-catching
${showProsCons?.checked ? '\u2713 Tampilkan PROS (\u2713) dan CONS (\u2717) untuk SETIAP produk dalam bullet points' : ''}
${showFeatures?.checked ? '\u2713 Highlight KEY FEATURES dengan icon/badges yang menarik' : ''}
${showWinner?.checked ? '\u2713 Tambahkan WINNER badge/crown/medal pada produk TERBAIK' : ''}
\u2713 Typography yang bold dan mudah dibaca
\u2713 Gunakan warna kontras tinggi untuk readability
\u2713 Professional, trust-worthy, dan persuasive

ANALISA & BANDINGKAN:
1. Identifikasi SETIAP produk dari foto
2. Analisa features, quality, value dari visual
3. Buat comparison yang fair dan objective
4. Tentukan winner berdasarkan overall value

VARIASI ${index}: ${variationText}

Generate comparison image yang SANGAT PROFESSIONAL dan ENGAGING!`;

                const parts = [{ text: prompt }];
                base64Images.forEach(base64 => {
                    parts.push({ inlineData: { mimeType: 'image/jpeg', data: base64 } });
                });

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{ parts }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedAspect }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `comparison_${selectedStyle}_${Date.now()}_${index}.jpg`;

                generatedComparisons[index - 1] = { url: imageUrl, filename: filename };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Comparison ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="comparison-action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="comparison-action-btn bg-gradient-to-r from-blue-600 to-cyan-600 hover:from-blue-700 hover:to-cyan-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }

            } catch (error) {
                console.error(`Comparison ${index} generation error:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedComparisons[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedComparisons.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                const validComparisons = generatedComparisons.filter(Boolean);
                try {
                    for (let i = 0; i < validComparisons.length; i++) {
                        const img = validComparisons[i];
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${validComparisons.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        function showError(message) {
            if (errorText) errorText.textContent = message;
            if (errorMessage) errorMessage.classList.remove('hidden');
        }

        function hideError() {
            if (errorMessage) errorMessage.classList.add('hidden');
        }

    })();

    // ==================== UNBOXING SCENE GENERATOR ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const imageUpload = document.getElementById('unboxing-image-upload');
        const uploadBox = document.querySelector('.upload-box-unboxing');
        const imagePreview = document.getElementById('unboxing-image-preview');
        const previewContainer = document.getElementById('unboxing-preview-container');
        const removePreviewBtn = document.getElementById('unboxing-remove-preview');

        const angleButtons = document.querySelectorAll('#unboxing-angle-selection [data-angle]');
        const styleButtons = document.querySelectorAll('#unboxing-style-selection [data-style]');
        const lightingButtons = document.querySelectorAll('#unboxing-lighting-selection [data-lighting]');
        const aspectButtons = document.querySelectorAll('#unboxing-aspect-selection [data-aspect]');
        const countButtons = document.querySelectorAll('.count-btn-unboxing');
        const countText = document.getElementById('unboxing-count-text');

        const generateBtn = document.getElementById('unboxing-generate-btn');
        const errorMessage = document.getElementById('unboxing-error-message');
        const errorText = document.getElementById('unboxing-error-text');

        const loadingIndicator = document.getElementById('unboxing-loading-indicator');
        const loadingText = document.getElementById('unboxing-loading-text');
        const progressBar = document.getElementById('unboxing-progress-bar');
        const progressText = document.getElementById('unboxing-progress-text');

        const resultsSection = document.getElementById('unboxing-results-section');
        const emptyState = document.getElementById('unboxing-empty-state');
        const resultsGrid = document.getElementById('unboxing-results-grid');
        const resultsCount = document.getElementById('unboxing-results-count');
        const downloadAllBtn = document.getElementById('unboxing-download-all-btn');

        // State
        let originalImageData = null;
        let selectedAngle = 'top';
        let selectedStyle = 'hand-reaching';
        let selectedLighting = 'dramatic';
        let selectedAspect = '16:9';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedScenes = [];

        // Count selection (1-10), default 4
        const unboxingCountSel = document.getElementById('unboxing-count-selection-grid');
        if (unboxingCountSel) {
            unboxingCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                unboxingCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Upload handler
        if (uploadBox) {
            uploadBox.addEventListener('click', () => imageUpload?.click());
        }

        if (imageUpload) {
            imageUpload.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    originalImageData = reader.result;

                    if (imagePreview && previewContainer) {
                        imagePreview.src = reader.result;
                        previewContainer.classList.remove('hidden');
                    }

                    if (generateBtn) generateBtn.disabled = false;
                };
                reader.readAsDataURL(processedFile);
            });
        }

        if (removePreviewBtn) {
            removePreviewBtn.addEventListener('click', () => {
                originalImageData = null;
                imageUpload.value = '';
                if (previewContainer) previewContainer.classList.add('hidden');
                if (imagePreview) imagePreview.src = '';
                if (generateBtn) generateBtn.disabled = true;
            });
        }

        // Selection handlers
        angleButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                angleButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAngle = btn.dataset.angle;
            });
        });

        styleButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                styleButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedStyle = btn.dataset.style;
            });
        });

        lightingButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                lightingButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedLighting = btn.dataset.lighting;
            });
        });

        aspectButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                aspectButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAspect = btn.dataset.aspect;
            });
        });

        // Generate scenes
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!originalImageData || generateBtn.disabled) return;

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                if (emptyState) emptyState.classList.add('hidden');
                if (resultsSection) resultsSection.classList.remove('hidden');

                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedScenes = [];
                    if (resultsGrid) resultsGrid.innerHTML = '';

                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `unboxing-card-${i}`;
                        card.className = 'relative rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-video';
                        card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-orange-600"></div>';
                        resultsGrid?.appendChild(card);
                    }

                    const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                        generateSingleScene(i + 1)
                    );
                    await Promise.allSettled(generationPromises);
                    successCount = resultsGrid?.querySelectorAll('img').length || 0;
                }

                resultsGrid?.querySelectorAll('[id^="unboxing-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                generatedScenes = generatedScenes.filter(Boolean);
                if (resultsCount) resultsCount.textContent = generatedScenes.length;

                generateBtn.disabled = false;
                generateBtn.innerHTML = originalHTML;

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                }
            });
        }

        async function generateSingleScene(index) {
            const card = document.getElementById(`unboxing-card-${index}`);

            try {
                const base64 = originalImageData.split(',')[1];

                const angleDescriptions = {
                    'top': 'Top-down view (bird\'s eye perspective)',
                    'side': 'Side view angle',
                    'three-quarter': '3/4 angle view',
                    'multiple': 'Multiple angles in one composition'
                };

                const styleDescriptions = {
                    'hand-reaching': 'Hand reaching toward the product (anticipation moment)',
                    'hand-opening': 'Hands actively opening/unwrapping the product',
                    'product-revealed': 'Product revealed and displayed beautifully',
                    'packaging': 'Product with premium packaging visible'
                };

                const lightingDescriptions = {
                    'dramatic': 'Dramatic cinematic lighting with shadows and highlights',
                    'natural': 'Natural soft daylight lighting',
                    'soft': 'Soft diffused studio lighting'
                };

                const prompt = `Buat scene UNBOXING yang SANGAT MENARIK dan CINEMATIC untuk produk ini!

UNBOXING SETUP:
- Camera angle: ${angleDescriptions[selectedAngle]}
- Unboxing style: ${styleDescriptions[selectedStyle]}
- Lighting: ${lightingDescriptions[selectedLighting]}
- Aspect ratio: ${selectedAspect}

PANDUAN VISUAL UNBOXING:
✓ Background/surface yang premium dan clean (marble, wood, atau aesthetic backdrop)
✓ ${styleDescriptions[selectedStyle]}
✓ ${lightingDescriptions[selectedLighting]} - create mood dan drama
✓ Produk terlihat JELAS dan menarik
✓ Packaging atau wrapping (jika ada) terlihat premium
✓ Tangan (jika ada) terlihat natural dan well-manicured
✓ Depth of field yang bagus (focus on product)
✓ Color grading yang warm dan inviting
✓ Professional product photography aesthetic
✓ Create sense of EXCITEMENT dan ANTICIPATION

COMPOSITION:
✓ Rule of thirds composition
✓ Negative space yang balanced
✓ Visual hierarchy yang jelas (product sebagai focal point)
✓ Flatlay atau angled shot yang menarik

VARIASI ${index}:
${index === 1 ? 'Standard premium unboxing moment' :
  index === 2 ? 'Different lighting angle & mood' :
  index === 3 ? 'Alternative camera perspective' :
  index === 4 ? 'Creative composition variation' :
  `Unique creative approach #${index}`}

Generate unboxing scene yang SANGAT PROFESSIONAL dan ENGAGING - seperti unboxing video premium!`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: 'image/jpeg', data: base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedAspect }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `unboxing_${selectedAngle}_${selectedStyle}_${Date.now()}_${index}.jpg`;

                generatedScenes[index - 1] = { url: imageUrl, filename: filename };

                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Unboxing ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="unboxing-action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="unboxing-action-btn bg-gradient-to-r from-orange-500 to-orange-600 hover:from-orange-600 hover:to-orange-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;

            } catch (error) {
                console.error(`Scene ${index} generation error:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedScenes[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedScenes.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedScenes.length; i++) {
                        const img = generatedScenes[i];
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedScenes.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        function showError(message) {
            if (errorText) errorText.textContent = message;
            if (errorMessage) errorMessage.classList.remove('hidden');
        }

        function hideError() {
            if (errorMessage) errorMessage.classList.add('hidden');
        }

    })();

    // ==================== BEFORE/AFTER GENERATOR ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const imageUpload = document.getElementById('beforeafter-image-upload');
        const uploadBox = document.querySelector('.upload-box-beforeafter');
        const imagePreview = document.getElementById('beforeafter-image-preview');
        const previewContainer = document.getElementById('beforeafter-preview-container');
        const removePreviewBtn = document.getElementById('beforeafter-remove-preview');

        const categoryButtons = document.querySelectorAll('#beforeafter-category-selection [data-category]');
        const styleButtons = document.querySelectorAll('#beforeafter-style-selection [data-style]');
        const aspectButtons = document.querySelectorAll('#beforeafter-aspect-selection [data-aspect]');
        const countButtons = document.querySelectorAll('.count-btn-beforeafter');
        const countText = document.getElementById('beforeafter-count-text');

        const showLabels = document.getElementById('beforeafter-show-labels');
        const showArrows = document.getElementById('beforeafter-show-arrows');
        const showHighlight = document.getElementById('beforeafter-show-highlight');

        const generateBtn = document.getElementById('beforeafter-generate-btn');
        const errorMessage = document.getElementById('beforeafter-error-message');
        const errorText = document.getElementById('beforeafter-error-text');

        const loadingIndicator = document.getElementById('beforeafter-loading-indicator');
        const loadingText = document.getElementById('beforeafter-loading-text');
        const progressBar = document.getElementById('beforeafter-progress-bar');
        const progressText = document.getElementById('beforeafter-progress-text');

        const resultsSection = document.getElementById('beforeafter-results-section');
        const emptyState = document.getElementById('beforeafter-empty-state');
        const resultsGrid = document.getElementById('beforeafter-results-grid');
        const resultsCount = document.getElementById('beforeafter-results-count');
        const downloadAllBtn = document.getElementById('beforeafter-download-all-btn');

        // State
        let originalImageData = null;
        let selectedCategory = 'skincare';
        let selectedStyle = 'split-screen';
        let selectedAspect = '1:1';
        let selectedCount = 4; // 1-10, default 4 (count selector untuk text mode)
        let generatedResults = [];

        // Count selection (1-10), default 4
        const baCountSel = document.getElementById('ba-count-selection-grid');
        if (baCountSel) {
            baCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                baCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Upload handler
        if (uploadBox) {
            uploadBox.addEventListener('click', () => imageUpload?.click());
        }

        if (imageUpload) {
            imageUpload.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    originalImageData = reader.result;

                    if (imagePreview && previewContainer) {
                        imagePreview.src = reader.result;
                        previewContainer.classList.remove('hidden');
                    }

                    if (generateBtn) generateBtn.disabled = false;
                };
                reader.readAsDataURL(processedFile);
            });
        }

        if (removePreviewBtn) {
            removePreviewBtn.addEventListener('click', () => {
                originalImageData = null;
                imageUpload.value = '';
                if (previewContainer) previewContainer.classList.add('hidden');
                if (imagePreview) imagePreview.src = '';
                if (generateBtn) generateBtn.disabled = true;
            });
        }

        // Selection handlers
        categoryButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                categoryButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCategory = btn.dataset.category;
            });
        });

        styleButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                styleButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedStyle = btn.dataset.style;
            });
        });

        aspectButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                aspectButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAspect = btn.dataset.aspect;
            });
        });

        // Generate results
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!originalImageData || generateBtn.disabled) return;

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                if (emptyState) emptyState.classList.add('hidden');
                if (resultsSection) resultsSection.classList.remove('hidden');

                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedResults = [];
                    if (resultsGrid) resultsGrid.innerHTML = '';

                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `beforeafter-card-${i}`;
                        card.className = 'relative rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-square';
                        card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-green-600"></div>';
                        resultsGrid?.appendChild(card);
                    }

                    const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                        generateSingleResult(i + 1)
                    );
                    await Promise.allSettled(generationPromises);
                    successCount = resultsGrid?.querySelectorAll('img').length || 0;
                }

                resultsGrid?.querySelectorAll('[id^="beforeafter-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                generatedResults = generatedResults.filter(Boolean);
                if (resultsCount) resultsCount.textContent = generatedResults.length;

                generateBtn.disabled = false;
                generateBtn.innerHTML = originalHTML;

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                }
            });
        }

        async function generateSingleResult(index) {
            const card = document.getElementById(`beforeafter-card-${index}`);

            try {
                const base64 = originalImageData.split(',')[1];

                const categoryDescriptions = {
                    'skincare': 'Skincare transformation (clear skin, reduced wrinkles, even tone)',
                    'makeup': 'Makeup transformation (natural to glam)',
                    'hair': 'Hair transformation (color, style, volume)',
                    'fitness': 'Fitness/body transformation (muscle gain, weight loss)',
                    'dental': 'Dental transformation (teeth whitening, alignment)',
                    'other': 'Product transformation results'
                };

                const styleDescriptions = {
                    'split-screen': 'Vertical split screen dengan garis pembatas di tengah',
                    'slider': 'Before/after slider format dengan handle di tengah',
                    'side-by-side': 'Horizontal side-by-side comparison',
                    'stacked': 'Vertical stacked (before on top, after on bottom)'
                };

                const indicators = [];
                if (showLabels?.checked) indicators.push('Text labels "BEFORE" dan "AFTER" yang BOLD dan jelas');
                if (showArrows?.checked) indicators.push('Arrow indicators menunjukkan arah transformasi');
                if (showHighlight?.checked) indicators.push('Highlight/circles pada area perubahan');

                const prompt = `Buat visual BEFORE/AFTER comparison yang SANGAT MENARIK dan PROFESSIONAL!

TRANSFORMATION SETUP:
- Category: ${categoryDescriptions[selectedCategory]}
- Layout style: ${styleDescriptions[selectedStyle]}
- Aspect ratio: ${selectedAspect}
- Tampilkan: ${indicators.join(', ')}

PANDUAN VISUAL BEFORE/AFTER:
✓ Layout ${styleDescriptions[selectedStyle]} yang clean dan professional
✓ BEFORE side: Menampilkan kondisi sebelum menggunakan produk
✓ AFTER side: Menampilkan hasil transformasi yang JELAS dan impressive
${showLabels?.checked ? '✓ Text labels "BEFORE" dan "AFTER" dengan typography yang BOLD dan kontras tinggi' : ''}
${showArrows?.checked ? '✓ Arrow indicators (→) yang jelas menunjukkan transformasi' : ''}
${showHighlight?.checked ? '✓ Circles atau highlight marks pada area yang berubah' : ''}
✓ Background yang bersih dan tidak mengalihkan perhatian
✓ Lighting yang konsisten di kedua sisi
✓ Professional color grading
✓ Trustworthy dan believable transformation
✓ High contrast untuk visibility maksimal

TRANSFORMATION DETAILS untuk ${selectedCategory}:
✓ Perubahan harus terlihat NYATA dan SIGNIFIKAN
✓ Improvement yang believable (tidak over-the-top)
✓ Detail yang jelas visible
✓ Professional photography quality
✓ Before/After harus dari angle yang sama

COMPOSITION:
✓ Perfect alignment antara before & after
✓ Equal space untuk kedua sisi
✓ Visual balance yang baik
✓ Typography yang clean dan modern (jika ada text)

VARIASI ${index}:
${index === 1 ? 'Standard before/after comparison' :
  index === 2 ? 'Different transformation intensity' :
  index === 3 ? 'Alternative visual emphasis' :
  `Creative variation #${index}`}

Generate before/after yang SANGAT CONVINCING dan PROFESSIONAL - perfect untuk showcase product results!`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: 'image/jpeg', data: base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedAspect }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `beforeafter_${selectedCategory}_${selectedStyle}_${Date.now()}_${index}.jpg`;

                generatedResults[index - 1] = { url: imageUrl, filename: filename };

                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Before/After ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="beforeafter-action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="beforeafter-action-btn bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;

            } catch (error) {
                console.error(`Before/After ${index} generation error:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedResults[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedResults.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedResults.length; i++) {
                        const img = generatedResults[i];
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedResults.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        function showError(message) {
            if (errorText) errorText.textContent = message;
            if (errorMessage) errorMessage.classList.remove('hidden');
        }

        function hideError() {
            if (errorMessage) errorMessage.classList.add('hidden');
        }

    })();

    // ==================== PRODUCT MOCKUP GENERATOR ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const imageUpload = document.getElementById('mockup-image-upload');
        const uploadBox = document.querySelector('.upload-box-mockup');
        const imagePreview = document.getElementById('mockup-image-preview');
        const previewContainer = document.getElementById('mockup-preview-container');
        const removePreviewBtn = document.getElementById('mockup-remove-preview');

        const sceneButtons = document.querySelectorAll('#mockup-scene-selection [data-scene]');
        const perspectiveButtons = document.querySelectorAll('#mockup-perspective-selection [data-perspective]');
        const moodButtons = document.querySelectorAll('#mockup-mood-selection [data-mood]');
        const aspectButtons = document.querySelectorAll('#mockup-aspect-selection [data-aspect]');
        const countButtons = document.querySelectorAll('.count-btn-mockup');
        const countText = document.getElementById('mockup-count-text');

        const generateBtn = document.getElementById('mockup-generate-btn');
        const errorMessage = document.getElementById('mockup-error-message');
        const errorText = document.getElementById('mockup-error-text');

        const loadingIndicator = document.getElementById('mockup-loading-indicator');
        const loadingText = document.getElementById('mockup-loading-text');
        const progressBar = document.getElementById('mockup-progress-bar');
        const progressText = document.getElementById('mockup-progress-text');

        const resultsSection = document.getElementById('mockup-results-section');
        const emptyState = document.getElementById('mockup-empty-state');
        const resultsGrid = document.getElementById('mockup-results-grid');
        const resultsCount = document.getElementById('mockup-results-count');
        const downloadAllBtn = document.getElementById('mockup-download-all-btn');

        // State
        let originalImageData = null;
        let selectedScene = 'desk';
        let selectedPerspective = 'angled';
        let selectedMood = 'bright';
        let selectedAspect = '1:1';
        let selectedCount = 4;
        let generatedMockups = [];

        // Upload handler
        if (uploadBox) {
            uploadBox.addEventListener('click', () => imageUpload?.click());
        }

        if (imageUpload) {
            imageUpload.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    originalImageData = reader.result;

                    if (imagePreview && previewContainer) {
                        imagePreview.src = reader.result;
                        previewContainer.classList.remove('hidden');
                    }

                    if (generateBtn) generateBtn.disabled = false;
                };
                reader.readAsDataURL(processedFile);
            });
        }

        if (removePreviewBtn) {
            removePreviewBtn.addEventListener('click', () => {
                originalImageData = null;
                imageUpload.value = '';
                if (previewContainer) previewContainer.classList.add('hidden');
                if (imagePreview) imagePreview.src = '';
                if (generateBtn) generateBtn.disabled = true;
            });
        }

        // Selection handlers
        sceneButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                sceneButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedScene = btn.dataset.scene;
            });
        });

        perspectiveButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                perspectiveButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedPerspective = btn.dataset.perspective;
            });
        });

        moodButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                moodButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedMood = btn.dataset.mood;
            });
        });

        aspectButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                aspectButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAspect = btn.dataset.aspect;
            });
        });

        countButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                countButtons.forEach(b => b.classList.remove('count-selected'));
                btn.classList.add('count-selected');
                selectedCount = parseInt(btn.dataset.count, 10);
                if (countText) countText.textContent = selectedCount;
            });
        });

        // Generate mockups
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!originalImageData || generateBtn.disabled) return;

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                hideError();
                if (emptyState) emptyState.classList.add('hidden');
                if (resultsSection) resultsSection.classList.add('hidden');
                if (loadingIndicator) loadingIndicator.classList.remove('hidden');

                generatedMockups = [];
                if (resultsGrid) resultsGrid.innerHTML = '';

                // Create placeholder cards
                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `mockup-card-${i}`;
                    card.className = 'relative rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-square';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-indigo-600"></div>';
                    resultsGrid?.appendChild(card);
                }

                try {
                    // Generate all mockups in parallel
                    const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                        generateSingleMockup(i + 1)
                    );
                    await Promise.allSettled(generationPromises);

                    if (loadingIndicator) loadingIndicator.classList.add('hidden');
                    if (resultsSection) resultsSection.classList.remove('hidden');
                    if (resultsCount) resultsCount.textContent = generatedMockups.length;

                } catch (error) {
                    console.error('Mockup generation error:', error);
                    showError(error.message || 'Gagal generate mockup. Silakan coba lagi.');
                    if (loadingIndicator) loadingIndicator.classList.add('hidden');
                    if (emptyState) emptyState.classList.remove('hidden');
                } finally {
                    generateBtn.disabled = false;
                    generateBtn.innerHTML = originalHTML;
                }
            });
        }

        async function generateSingleMockup(index) {
            const card = document.getElementById(`mockup-card-${index}`);

            try {
                if (loadingText) loadingText.textContent = `Generating mockup ${index}/${selectedCount}...`;

                const base64 = originalImageData.split(',')[1];

                const sceneDescriptions = {
                    'desk': 'Professional desk workspace dengan laptop, notebook, coffee mug, dan office accessories',
                    'cafe': 'Cozy cafe setting dengan wooden table, natural lighting, dan aesthetic background',
                    'outdoor': 'Natural outdoor environment dengan greenery, sunlight, dan fresh atmosphere',
                    'bedroom': 'Modern bedroom setup dengan bed, nightstand, soft lighting, dan cozy vibes',
                    'kitchen': 'Clean kitchen counter dengan marble surface, kitchen tools, dan bright ambiance',
                    'studio': 'Professional studio dengan clean white/gray background dan perfect lighting'
                };

                const perspectiveDescriptions = {
                    'flat': 'Flat lay perspective (top-down view) untuk product display yang clean',
                    'angled': '45-degree angled view untuk dynamic product showcase',
                    'floating': 'Floating/levitating product dengan dramatic shadow untuk premium feel'
                };

                const moodDescriptions = {
                    'bright': 'Bright & airy dengan natural lighting, high key photography',
                    'warm': 'Warm & cozy dengan golden hour lighting, inviting atmosphere',
                    'dark': 'Dark & moody dengan dramatic shadows, premium aesthetic',
                    'minimal': 'Minimal & clean dengan simple background, focus on product'
                };

                const prompt = `Buat PRODUCT MOCKUP yang SANGAT PROFESSIONAL dan EYE-CATCHING!

MOCKUP SETUP:
- Scene: ${sceneDescriptions[selectedScene]}
- Perspective: ${perspectiveDescriptions[selectedPerspective]}
- Mood/Lighting: ${moodDescriptions[selectedMood]}
- Aspect ratio: ${selectedAspect}

PANDUAN MOCKUP PROFESSIONAL:
✓ Product harus menjadi FOCAL POINT - clearly visible dan prominent
✓ Perspective ${perspectiveDescriptions[selectedPerspective]}
✓ Scene setup ${sceneDescriptions[selectedScene]}
✓ Lighting ${moodDescriptions[selectedMood]}
✓ Photography yang HIGH QUALITY dengan depth of field
✓ Product placement yang STRATEGIC dan appealing
✓ Props dan accessories yang RELEVANT dan tidak mengalihkan perhatian
✓ Color harmony antara product dan environment
✓ Clean composition dengan good negative space
✓ Realistic shadows dan reflections
✓ Professional color grading
✓ Detail yang sharp pada product

SCENE DETAILS:
✓ Environment yang believable dan realistic
✓ Props yang carefully arranged
✓ Background yang complementary (tidak terlalu ramai)
✓ Natural atau artificial lighting yang sesuai mood
✓ Texture dan material yang realistic
✓ Spatial relationship yang make sense

PRODUCT PRESENTATION:
✓ Product harus JELAS terlihat dari angle yang dipilih
✓ Size yang proportional dalam scene
✓ Focus yang sharp pada product details
✓ Kontras yang baik dengan background
✓ Professional packaging/branding visible (jika ada)

VARIASI ${index}:
${index === 1 ? 'Standard mockup dengan classic composition' :
  index === 2 ? 'Alternative camera angle dan product placement' :
  index === 3 ? 'Different props arrangement' :
  `Creative variation #${index} dengan unique styling`}

Generate mockup yang INSTAGRAM-WORTHY dan SALES-BOOSTING - perfect untuk e-commerce, social media, dan marketing materials!`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: 'image/jpeg', data: base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['IMAGE'],
                        imageConfig: { aspectRatio: selectedAspect }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();

                if (!response.ok || !result.candidates?.[0]?.content?.parts?.[0]?.inlineData?.data) {
                    throw new Error(result.error?.message || 'Failed to generate mockup');
                }

                const imageData = result.candidates[0].content.parts[0].inlineData.data;
                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `product_mockup_${selectedScene}_${selectedPerspective}_${Date.now()}_${index}.jpg`;

                // Store at correct index (not push) to maintain order in parallel execution
                generatedMockups[index - 1] = { url: imageUrl, filename: filename };

                // Update card with image
                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Mockup ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="mockup-action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="mockup-action-btn bg-gradient-to-r from-indigo-600 to-indigo-700 hover:from-indigo-700 hover:to-indigo-800 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>se
                `;

                // Update progress
                const progress = Math.round(((index) / selectedCount) * 100);
                if (progressBar) progressBar.style.width = `${progress}%`;
                if (progressText) progressText.textContent = `${index}/${selectedCount}`;

            } catch (error) {
                console.error(`Mockup ${index} generation error:`, error);
                card.className = 'relative rounded-xl overflow-hidden bg-red-50 border-2 border-red-300 flex items-center justify-center p-4 aspect-square';
                card.innerHTML = `
                    <div class="text-center">
                        <i class="fas fa-exclamation-triangle text-3xl text-red-500 mb-2"></i>
                        <p class="text-sm text-red-700">Failed to generate</p>
                    </div>
                `;
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedMockups[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedMockups.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedMockups.length; i++) {
                        const img = generatedMockups[i];
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedMockups.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        function showError(message) {
            if (errorText) errorText.textContent = message;
            if (errorMessage) errorMessage.classList.remove('hidden');
        }

        function hideError() {
            if (errorMessage) errorMessage.classList.add('hidden');
        }

    })();

    // ==================== TESTIMONIAL GENERATOR ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const imageUpload = document.getElementById('testimonial-image-upload');
        const uploadBox = document.querySelector('.upload-box-testimonial');
        const imagePreview = document.getElementById('testimonial-image-preview');
        const previewContainer = document.getElementById('testimonial-preview-container');
        const removePreviewBtn = document.getElementById('testimonial-remove-preview');

        const starButtons = document.querySelectorAll('#testimonial-rating-selection [data-rating]');
        const styleButtons = document.querySelectorAll('#testimonial-style-selection [data-cardstyle]');
        const modeButtons = document.querySelectorAll('#testimonial-mode-selection [data-mode]');
        const aspectButtons = document.querySelectorAll('#testimonial-aspect-selection [data-aspect]');
        const countButtons = document.querySelectorAll('.count-btn-testimonial');
        const countText = document.getElementById('testimonial-count-text');

        const aiModeInputs = document.getElementById('testimonial-ai-inputs');
        const customModeInputs = document.getElementById('testimonial-custom-inputs');
        const productNameInput = document.getElementById('testimonial-product-name');
        const productCategoryInput = document.getElementById('testimonial-product-category');
        const customTextInput = document.getElementById('testimonial-custom-text');
        const customerNameInput = document.getElementById('testimonial-customer-name');
        const customerTitleInput = document.getElementById('testimonial-customer-title');

        const generateBtn = document.getElementById('testimonial-generate-btn');
        const errorMessage = document.getElementById('testimonial-error-message');
        const errorText = document.getElementById('testimonial-error-text');

        const loadingIndicator = document.getElementById('testimonial-loading-indicator');
        const loadingText = document.getElementById('testimonial-loading-text');
        const progressBar = document.getElementById('testimonial-progress-bar');
        const progressText = document.getElementById('testimonial-progress-text');

        const resultsSection = document.getElementById('testimonial-results-section');
        const emptyState = document.getElementById('testimonial-empty-state');
        const resultsGrid = document.getElementById('testimonial-results-grid');
        const resultsCount = document.getElementById('testimonial-results-count');
        const downloadAllBtn = document.getElementById('testimonial-download-all-btn');

        // State
        let originalImageData = null;
        let selectedRating = 5;
        let selectedStyle = 'modern';
        let selectedMode = 'ai';
        let selectedAspect = '1:1';
        let selectedCount = 6;
        let generatedTestimonials = [];

        // Upload handler
        if (uploadBox) {
            uploadBox.addEventListener('click', () => imageUpload?.click());
        }

        if (imageUpload) {
            imageUpload.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    originalImageData = reader.result;

                    if (imagePreview && previewContainer) {
                        imagePreview.src = reader.result;
                        previewContainer.classList.remove('hidden');
                    }

                    updateGenerateButtonState();
                };
                reader.readAsDataURL(processedFile);
            });
        }

        if (removePreviewBtn) {
            removePreviewBtn.addEventListener('click', () => {
                originalImageData = null;
                imageUpload.value = '';
                if (previewContainer) previewContainer.classList.add('hidden');
                if (imagePreview) imagePreview.src = '';
                updateGenerateButtonState();
            });
        }

        // Star rating selection
        starButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                starButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedRating = parseInt(btn.dataset.rating, 10);
            });
        });

        // Card style selection
        styleButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                styleButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedStyle = btn.dataset.cardstyle;
            });
        });

        // Mode selection (AI vs Custom)
        modeButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                modeButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedMode = btn.dataset.mode;

                // Show/hide appropriate inputs
                if (selectedMode === 'ai') {
                    if (aiModeInputs) aiModeInputs.classList.remove('hidden');
                    if (customModeInputs) customModeInputs.classList.add('hidden');
                } else {
                    if (aiModeInputs) aiModeInputs.classList.add('hidden');
                    if (customModeInputs) customModeInputs.classList.remove('hidden');
                }

                updateGenerateButtonState();
            });
        });

        // Aspect ratio selection
        aspectButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                aspectButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAspect = btn.dataset.aspect;
            });
        });

        // Count selection
        countButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                countButtons.forEach(b => b.classList.remove('count-selected'));
                btn.classList.add('count-selected');
                selectedCount = parseInt(btn.dataset.count, 10);
                if (countText) countText.textContent = selectedCount;
            });
        });

        // Input change listeners for validation
        if (productNameInput) productNameInput.addEventListener('input', updateGenerateButtonState);
        if (customTextInput) customTextInput.addEventListener('input', updateGenerateButtonState);

        // Function to update generate button state
        function updateGenerateButtonState() {
            if (!generateBtn) return;

            if (selectedMode === 'ai') {
                // Enable if product name filled OR image uploaded
                const hasProductName = productNameInput?.value.trim();
                const hasImage = originalImageData !== null;
                generateBtn.disabled = !(hasProductName || hasImage);
            } else {
                generateBtn.disabled = !customTextInput?.value.trim();
            }
        }

        // Initialize button state
        updateGenerateButtonState();

        // Generate testimonials
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (generateBtn.disabled) return;

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                hideError();
                if (emptyState) emptyState.classList.add('hidden');
                if (resultsSection) resultsSection.classList.add('hidden');
                if (loadingIndicator) loadingIndicator.classList.remove('hidden');

                generatedTestimonials = [];
                if (resultsGrid) resultsGrid.innerHTML = '';

                // Create placeholder cards
                for (let i = 1; i <= selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `testimonial-card-${i}`;
                    card.className = 'relative rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-square';
                    card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-cyan-600"></div>';
                    resultsGrid?.appendChild(card);
                }

                try {
                    // Generate all testimonials in parallel
                    const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                        generateSingleTestimonial(i + 1)
                    );
                    await Promise.allSettled(generationPromises);

                    if (loadingIndicator) loadingIndicator.classList.add('hidden');
                    if (resultsSection) resultsSection.classList.remove('hidden');
                    if (resultsCount) resultsCount.textContent = generatedTestimonials.length;

                } catch (error) {
                    console.error('Testimonial generation error:', error);
                    showError(error.message || 'Gagal generate testimonial. Silakan coba lagi.');
                    if (loadingIndicator) loadingIndicator.classList.add('hidden');
                    if (emptyState) emptyState.classList.remove('hidden');
                } finally {
                    generateBtn.disabled = false;
                    generateBtn.innerHTML = originalHTML;
                }
            });
        }

        async function generateSingleTestimonial(index) {
            const card = document.getElementById(`testimonial-card-${index}`);

            try {
                if (loadingText) loadingText.textContent = `Generating testimonial ${index}/${selectedCount}...`;

                const styleDescriptions = {
                    'minimal': 'Minimal & clean design dengan simple borders, subtle shadows, dan white background',
                    'modern': 'Modern gradient design dengan bold colors, rounded corners, dan contemporary typography',
                    'classic': 'Classic elegant design dengan serif fonts, gold accents, dan traditional layout',
                    'premium': 'Premium luxury design dengan rich colors, metallic elements, dan sophisticated styling'
                };

                // Prepare testimonial content
                let testimonialContent = '';
                let customerName = '';
                let customerTitle = '';
                const productName = productNameInput?.value.trim() || 'Produk Ini';
                const productCategory = productCategoryInput?.value.trim() || '';

                if (selectedMode === 'ai') {
                    // AI mode - Let image generation create the testimonial text
                    const focusArea = index === 1 ? 'kualitas dan value for money' :
                                     index === 2 ? 'hasil dan transformasi nyata' :
                                     index === 3 ? 'kemudahan penggunaan' :
                                     index === 4 ? 'pelayanan customer service' :
                                     index === 5 ? 'fitur unik dan inovasi' :
                                     'kepuasan keseluruhan dan rekomendasi';

                    testimonialContent = `[AI will generate authentic testimonial about ${productName} focusing on ${focusArea}]`;
                    customerName = '[AI will generate name]';
                    customerTitle = '[AI will generate role]';
                } else {
                    // Custom mode - use user-provided text
                    testimonialContent = customTextInput?.value.trim() || 'Produk yang sangat bagus!';
                    customerName = customerNameInput?.value.trim() || 'Happy Customer';
                    customerTitle = customerTitleInput?.value.trim() || 'Verified Buyer';
                }

                // Generate testimonial card image
                const focusArea = index === 1 ? 'kualitas dan value for money' :
                                 index === 2 ? 'hasil dan transformasi nyata' :
                                 index === 3 ? 'kemudahan penggunaan' :
                                 index === 4 ? 'pelayanan customer service' :
                                 index === 5 ? 'fitur unik dan inovasi' :
                                 'kepuasan keseluruhan dan rekomendasi';

                const imagePrompt = `Buat TESTIMONIAL CARD yang SANGAT PROFESSIONAL dan EYE-CATCHING!

PRODUCT INFO:
- Product: ${productName}${productCategory ? ` (${productCategory})` : ''}
- Rating: ${selectedRating} out of 5 stars
- Focus: ${focusArea}

CARD DESIGN:
- Style: ${styleDescriptions[selectedStyle]}
- Aspect ratio: ${selectedAspect}

${selectedMode === 'ai' ? `
GENERATE CONTENT SECARA OTOMATIS:
✓ Buat testimonial text yang REALISTIC dan CONVINCING tentang ${productName}
✓ Fokus pada ${focusArea}
✓ Testimonial harus terdengar NATURAL dan AUTHENTIC (2-3 kalimat)
✓ Generate customer name (Indonesian atau international name)
✓ Generate customer role/title (contoh: "Online Seller", "Business Owner", "Content Creator", dll)
✓ Sesuaikan tone dengan ${selectedRating} star rating
` : `
USE CUSTOM CONTENT:
✓ Testimonial text: "${testimonialContent}"
✓ Customer name: ${customerName}
✓ Customer title: ${customerTitle}
`}

PANDUAN DESIGN TESTIMONIAL CARD:
✓ Layout ${styleDescriptions[selectedStyle]}
✓ Star rating ${selectedRating}/5 ditampilkan dengan BOLD dan PROMINENT (★★★★★)
✓ Quote marks atau quote icon yang elegant
✓ Testimonial text dengan typography yang READABLE dan well-spaced
✓ Customer name dalam BOLD typography
✓ Customer title/role dalam lighter font style
✓ Profile picture placeholder atau avatar (circular, professional)
✓ Color scheme yang match dengan style (${selectedStyle})
✓ Professional spacing dan alignment
✓ Visual hierarchy yang jelas (rating → quote → name → title)
✓ Subtle background patterns atau gradients (sesuai style)
✓ High-quality graphics dan icons
✓ Border atau frame yang sesuai dengan style theme
✓ Shadow effects untuk depth (sesuai style)

TYPOGRAPHY:
✓ Testimonial text: readable, clean, well-spaced
✓ Customer name: BOLD dan prominent
✓ Customer title: elegant, lighter weight
✓ Star rating: large dan visible dengan gold/yellow color

COLOR & AESTHETIC:
${selectedStyle === 'minimal' ? '✓ White/light gray background, black text, simple borders' :
  selectedStyle === 'modern' ? '✓ Gradient backgrounds (blue/purple/pink), white text, vibrant accents' :
  selectedStyle === 'classic' ? '✓ Cream/beige background, dark text, gold accents, serif fonts' :
  '✓ Dark rich colors, metallic gold/silver accents, luxury feel'}

COMPOSITION:
✓ Balanced layout dengan good spacing
✓ Quote icon atau decorative element
✓ Profile picture area (circular, positioned appropriately)
✓ Clear visual separation between elements
✓ Professional dan trustworthy appearance

VARIASI ${index}:
${index === 1 ? 'Standard layout dengan centered alignment' :
  index === 2 ? 'Left-aligned layout dengan side profile picture' :
  index === 3 ? 'Top profile picture dengan bottom text' :
  `Creative variation #${index} dengan unique layout`}

Generate testimonial card yang CONVINCING dan PROFESSIONAL - perfect untuk website, social media, dan marketing materials!`;

                const imageApiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const imagePayload = {
                    contents: [{
                        parts: [{ text: imagePrompt }]
                    }],
                    generationConfig: {
                        responseModalities: ['IMAGE'],
                        imageConfig: { aspectRatio: selectedAspect }
                    }
                };

                const imageResponse = await fetch(imageApiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(imagePayload)
                });

                const imageResult = await imageResponse.json();

                if (!imageResponse.ok || !imageResult.candidates?.[0]?.content?.parts?.[0]?.inlineData?.data) {
                    throw new Error(imageResult.error?.message || 'Failed to generate testimonial card');
                }

                const imageData = imageResult.candidates[0].content.parts[0].inlineData.data;
                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `testimonial_${selectedStyle}_${selectedRating}star_${Date.now()}_${index}.jpg`;

                generatedTestimonials.push({ url: imageUrl, filename: filename });

                // Update card with image
                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Testimonial ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${generatedTestimonials.length - 1}" class="testimonial-action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${generatedTestimonials.length - 1}" class="testimonial-action-btn bg-gradient-to-r from-cyan-600 to-cyan-700 hover:from-cyan-700 hover:to-cyan-800 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;

                // Update progress
                const progress = Math.round(((index) / selectedCount) * 100);
                if (progressBar) progressBar.style.width = `${progress}%`;
                if (progressText) progressText.textContent = `${index}/${selectedCount}`;

            } catch (error) {
                console.error(`Testimonial ${index} generation error:`, error);
                card.className = 'relative rounded-xl overflow-hidden bg-red-50 border-2 border-red-300 flex items-center justify-center p-4 aspect-square';
                card.innerHTML = `
                    <div class="text-center">
                        <i class="fas fa-exclamation-triangle text-3xl text-red-500 mb-2"></i>
                        <p class="text-sm text-red-700">Failed to generate</p>
                    </div>
                `;
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedTestimonials[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedTestimonials.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedTestimonials.length; i++) {
                        const img = generatedTestimonials[i];
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedTestimonials.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        function showError(message) {
            if (errorText) errorText.textContent = message;
            if (errorMessage) errorMessage.classList.remove('hidden');
        }

        function hideError() {
            if (errorMessage) errorMessage.classList.add('hidden');
        }

    })();

    // ==================== SIZE GUIDE GENERATOR ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const imageUpload = document.getElementById('sizeguide-image-upload');
        const uploadBox = document.querySelector('.upload-box-sizeguide');
        const imagePreview = document.getElementById('sizeguide-image-preview');
        const previewContainer = document.getElementById('sizeguide-preview-container');
        const removePreviewBtn = document.getElementById('sizeguide-remove-preview');

        const comparisonButtons = document.querySelectorAll('[data-comparison]');
        const widthInput = document.getElementById('sizeguide-width');
        const heightInput = document.getElementById('sizeguide-height');
        const depthInput = document.getElementById('sizeguide-depth');
        const styleButtons = document.querySelectorAll('[data-guidestyle]');
        const aspectButtons = document.querySelectorAll('#sizeguide-aspect-selection [data-aspect]');
        const countButtons = document.querySelectorAll('.count-btn-sizeguide');
        const countText = document.getElementById('sizeguide-count-text');

        const generateBtn = document.getElementById('sizeguide-generate-btn');
        const errorMessage = document.getElementById('sizeguide-error-message');
        const errorText = document.getElementById('sizeguide-error-text');

        const loadingIndicator = document.getElementById('sizeguide-loading-indicator');
        const loadingText = document.getElementById('sizeguide-loading-text');
        const progressBar = document.getElementById('sizeguide-progress-bar');
        const progressText = document.getElementById('sizeguide-progress-text');

        const resultsSection = document.getElementById('sizeguide-results-section');
        const emptyState = document.getElementById('sizeguide-empty-state');
        const resultsGrid = document.getElementById('sizeguide-results-grid');
        const resultsCount = document.getElementById('sizeguide-results-count');
        const downloadAllBtn = document.getElementById('sizeguide-download-all-btn');

        // State
        let originalImageData = null;
        let selectedComparison = 'hand';
        let selectedStyle = 'side-by-side';
        let selectedAspect = '1:1';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedGuides = [];

        // Count selection (1-10), default 4
        const sizeguideCountSel = document.getElementById('sizeguide-count-selection-grid');
        if (sizeguideCountSel) {
            sizeguideCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                sizeguideCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Upload handler
        if (uploadBox) {
            uploadBox.addEventListener('click', () => imageUpload?.click());
        }

        if (imageUpload) {
            imageUpload.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    originalImageData = reader.result;

                    if (imagePreview && previewContainer) {
                        imagePreview.src = reader.result;
                        previewContainer.classList.remove('hidden');
                    }

                    if (generateBtn) generateBtn.disabled = false;
                };
                reader.readAsDataURL(processedFile);
            });
        }

        if (removePreviewBtn) {
            removePreviewBtn.addEventListener('click', () => {
                originalImageData = null;
                imageUpload.value = '';
                if (previewContainer) previewContainer.classList.add('hidden');
                if (imagePreview) imagePreview.src = '';
                if (generateBtn) generateBtn.disabled = true;
            });
        }

        // Selection handlers
        comparisonButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                comparisonButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedComparison = btn.dataset.comparison;
            });
        });

        styleButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                styleButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedStyle = btn.dataset.guidestyle;
            });
        });

        aspectButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                aspectButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAspect = btn.dataset.aspect;
            });
        });

        // Generate size guides
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!originalImageData || generateBtn.disabled) return;

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                if (emptyState) emptyState.classList.add('hidden');
                if (resultsSection) resultsSection.classList.remove('hidden');

                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedGuides = [];
                    if (resultsGrid) resultsGrid.innerHTML = '';

                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `sizeguide-card-${i}`;
                        card.className = 'relative rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-square';
                        card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-orange-600"></div>';
                        resultsGrid?.appendChild(card);
                    }

                    const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                        generateSingleGuide(i + 1)
                    );
                    await Promise.allSettled(generationPromises);
                    successCount = resultsGrid?.querySelectorAll('img').length || 0;
                }

                resultsGrid?.querySelectorAll('[id^="sizeguide-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                generatedGuides = generatedGuides.filter(Boolean);
                if (resultsCount) resultsCount.textContent = generatedGuides.length;

                generateBtn.disabled = false;
                generateBtn.innerHTML = originalHTML;

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                }
            });
        }

        async function generateSingleGuide(index) {
            const card = document.getElementById(`sizeguide-card-${index}`);

            try {
                const base64 = originalImageData.split(',')[1];

                // Get optional dimensions
                const width = widthInput?.value.trim() || '';
                const height = heightInput?.value.trim() || '';
                const depth = depthInput?.value.trim() || '';
                const hasDimensions = width || height || depth;

                const comparisonDescriptions = {
                    'hand': 'Human hand for scale (standard adult hand, realistic proportions)',
                    'coin': 'Standard coin (2.5 cm diameter) for size comparison',
                    'ruler': 'Measuring ruler with clear centimeter/inch markings',
                    'bottle': 'Standard water bottle (500ml) for size reference',
                    'phone': 'Modern smartphone (approximately 15cm height) for scale',
                    'card': 'Credit card (8.5 x 5.4 cm) for size reference'
                };

                const styleDescriptions = {
                    'side-by-side': 'Side-by-side layout: product on left, comparison object on right',
                    'overlay': 'Overlay layout: comparison object placed directly on/near product',
                    'dimension-lines': 'Technical dimension lines with measurements and arrows',
                    'infographic': 'Infographic style dengan visual comparison dan size data'
                };

                const prompt = `Buat PRODUCT SIZE GUIDE yang SANGAT INFORMATIF dan PROFESSIONAL!

SIZE GUIDE SETUP:
- Comparison object: ${comparisonDescriptions[selectedComparison]}
- Layout style: ${styleDescriptions[selectedStyle]}
- Aspect ratio: ${selectedAspect}
${hasDimensions ? `- Product dimensions: ${width ? width + ' cm (W)' : ''} ${height ? height + ' cm (H)' : ''} ${depth ? depth + ' cm (D)' : ''}` : ''}

PANDUAN SIZE GUIDE:
✓ Product ditampilkan dengan JELAS dan proportional
✓ Comparison object (${selectedComparison}) harus terlihat PROMINENT untuk reference
✓ Layout: ${styleDescriptions[selectedStyle]}
✓ Size relationship yang AKURAT dan believable
✓ Clean white atau neutral background
✓ Professional product photography quality
✓ Sharp focus pada both product & comparison object
${selectedStyle === 'dimension-lines' ? '✓ Technical dimension lines dengan arrows dan measurements yang clear' : ''}
${selectedStyle === 'infographic' ? '✓ Infographic elements: icons, text labels, visual size comparison' : ''}

SCALE & PROPORTION:
✓ Accurate size relationship between product dan comparison object
✓ Realistic proportions (jangan terlalu besar/kecil)
✓ Clear visual cues untuk size understanding
${hasDimensions ? '✓ Dimension numbers displayed prominently jika ada' : ''}
✓ Professional scale markers jika needed

VISUAL ELEMENTS:
✓ High-quality product image
✓ Comparison object rendered realistically
✓ Good lighting consistency
✓ Clean composition tanpa clutter
${selectedStyle === 'overlay' ? '✓ Comparison object positioned naturally near/on product' : ''}
${selectedStyle === 'side-by-side' ? '✓ Equal visual weight untuk both items' : ''}
${selectedStyle === 'dimension-lines' ? '✓ Engineering-style dimension arrows dan measurement text' : ''}
${selectedStyle === 'infographic' ? '✓ Data visualization elements, icons, comparative graphics' : ''}

TYPOGRAPHY & LABELS:
✓ Clear, readable text untuk measurements (jika ada)
✓ Professional fonts
✓ Good contrast untuk visibility
✓ Size information displayed clearly

VARIASI ${index}:
${index === 1 ? 'Standard front view comparison' :
  index === 2 ? 'Alternative angle atau perspective' :
  index === 3 ? 'Top/side view untuk different dimension reference' :
  `Creative variation #${index} dengan unique presentation`}

Generate size guide yang SANGAT HELPFUL dan PROFESSIONAL - perfect untuk e-commerce product pages, Amazon listings, dan customer education!`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: 'image/jpeg', data: base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedAspect }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `size_guide_${selectedComparison}_${selectedStyle}_${Date.now()}_${index}.jpg`;

                generatedGuides[index - 1] = { url: imageUrl, filename: filename };

                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Size Guide ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="sizeguide-action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="sizeguide-action-btn bg-gradient-to-r from-orange-600 to-orange-700 hover:from-orange-700 hover:to-orange-800 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;

            } catch (error) {
                console.error(`Size guide ${index} generation error:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedGuides[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedGuides.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedGuides.length; i++) {
                        const img = generatedGuides[i];
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedGuides.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        function showError(message) {
            if (errorText) errorText.textContent = message;
            if (errorMessage) errorMessage.classList.remove('hidden');
        }

        function hideError() {
            if (errorMessage) errorMessage.classList.add('hidden');
        }

    })();

    // ==================== VIDEO FRAMES GENERATOR ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const imageUpload = document.getElementById('videoframes-image-upload');
        const uploadBox = document.querySelector('.upload-box-videoframes');
        const imagePreview = document.getElementById('videoframes-image-preview');
        const previewContainer = document.getElementById('videoframes-preview-container');
        const removePreviewBtn = document.getElementById('videoframes-remove-preview');

        const animationButtons = document.querySelectorAll('[data-animation]');
        const backgroundButtons = document.querySelectorAll('[data-background]');
        const aspectButtons = document.querySelectorAll('#videoframes-aspect-selection [data-aspect]');
        const countButtons = document.querySelectorAll('.count-btn-videoframes');
        const countText = document.getElementById('videoframes-count-text');

        const generateBtn = document.getElementById('videoframes-generate-btn');
        const errorMessage = document.getElementById('videoframes-error-message');
        const errorText = document.getElementById('videoframes-error-text');

        const loadingIndicator = document.getElementById('videoframes-loading-indicator');
        const loadingText = document.getElementById('videoframes-loading-text');
        const progressBar = document.getElementById('videoframes-progress-bar');
        const progressText = document.getElementById('videoframes-progress-text');

        const resultsSection = document.getElementById('videoframes-results-section');
        const emptyState = document.getElementById('videoframes-empty-state');
        const resultsGrid = document.getElementById('videoframes-results-grid');
        const resultsCount = document.getElementById('videoframes-results-count');
        const downloadAllBtn = document.getElementById('videoframes-download-all-btn');

        // State
        let originalImageData = null;
        let selectedAnimation = 'rotation';
        let selectedBackground = 'clean-white';
        let selectedAspect = '1:1';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedFrames = [];

        // Count selection (1-10), default 4
        const videoframesCountSel = document.getElementById('videoframes-count-selection-grid');
        if (videoframesCountSel) {
            videoframesCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                videoframesCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Upload handler
        if (uploadBox) {
            uploadBox.addEventListener('click', () => imageUpload?.click());
        }

        if (imageUpload) {
            imageUpload.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    originalImageData = reader.result;

                    if (imagePreview && previewContainer) {
                        imagePreview.src = reader.result;
                        previewContainer.classList.remove('hidden');
                    }

                    if (generateBtn) generateBtn.disabled = false;
                };
                reader.readAsDataURL(processedFile);
            });
        }

        if (removePreviewBtn) {
            removePreviewBtn.addEventListener('click', () => {
                originalImageData = null;
                imageUpload.value = '';
                if (previewContainer) previewContainer.classList.add('hidden');
                if (imagePreview) imagePreview.src = '';
                if (generateBtn) generateBtn.disabled = true;
            });
        }

        // Selection handlers
        animationButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                animationButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAnimation = btn.dataset.animation;
            });
        });

        backgroundButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                backgroundButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedBackground = btn.dataset.background;
            });
        });

        aspectButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                aspectButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAspect = btn.dataset.aspect;
            });
        });

        // Generate video frames
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!originalImageData || generateBtn.disabled) return;

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                if (emptyState) emptyState.classList.add('hidden');
                if (resultsSection) resultsSection.classList.remove('hidden');

                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedFrames = [];
                    if (resultsGrid) resultsGrid.innerHTML = '';

                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `videoframes-card-${i}`;
                        card.className = 'relative rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-square';
                        card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-purple-600"></div>';
                        resultsGrid?.appendChild(card);
                    }

                    const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                        generateSingleFrame(i + 1)
                    );
                    await Promise.allSettled(generationPromises);
                    successCount = resultsGrid?.querySelectorAll('img').length || 0;
                }

                resultsGrid?.querySelectorAll('[id^="videoframes-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                generatedFrames = generatedFrames.filter(Boolean);
                if (resultsCount) resultsCount.textContent = generatedFrames.length;

                generateBtn.disabled = false;
                generateBtn.innerHTML = originalHTML;

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                }
            });
        }

        async function generateSingleFrame(index) {
            const card = document.getElementById(`videoframes-card-${index}`);

            try {
                const base64 = originalImageData.split(',')[1];

                const animationDescriptions = {
                    'rotation': '360° rotation animation - product rotating smoothly from frame to frame',
                    'zoom': 'Zoom in/out animation - progressive zoom progression across frames',
                    'reveal': 'Product reveal animation - gradual appearance with dramatic effect',
                    'pan': 'Pan around animation - camera panning around the product',
                    'float': 'Floating animation - product floating with subtle movement',
                    'explode': 'Exploded view animation - product parts separating to show internal components'
                };

                const backgroundDescriptions = {
                    'clean-white': 'Clean white background - pure white, studio photography style',
                    'gradient': 'Gradient background - smooth color gradient (complementary to product)',
                    'studio': 'Studio setup - professional studio environment dengan lighting equipment visible',
                    'animated': 'Animated background - dynamic background dengan particles atau effects'
                };

                // Calculate frame position in animation sequence
                const framePosition = (index - 1) / (selectedCount - 1); // 0 to 1
                const rotationAngle = Math.round(framePosition * 360); // 0 to 360 degrees

                const prompt = `Buat SEQUENTIAL VIDEO FRAME yang SANGAT PROFESSIONAL untuk product animation!

FRAME SPECIFICATIONS:
- Animation type: ${animationDescriptions[selectedAnimation]}
- Background style: ${backgroundDescriptions[selectedBackground]}
- Aspect ratio: ${selectedAspect}
- Frame ${index} of ${selectedCount} (${Math.round(framePosition * 100)}% progress)

ANIMATION SEQUENCE DETAILS:
${selectedAnimation === 'rotation' ? `✓ Rotation angle: ${rotationAngle}° (smooth rotation progression)
✓ Product rotates smoothly around vertical axis
✓ Maintain consistent lighting across rotation
✓ Show different angles of the product naturally` : ''}
${selectedAnimation === 'zoom' ? `✓ Zoom level: ${Math.round(100 + (framePosition * 100))}% (progressive zoom)
✓ Smooth zoom transition maintaining product center
✓ Focus remains sharp throughout zoom
✓ Natural camera zoom effect` : ''}
${selectedAnimation === 'reveal' ? `✓ Reveal progress: ${Math.round(framePosition * 100)}% visible
✓ Dramatic reveal effect (fade in, wipe, or curtain effect)
✓ Build anticipation with progressive reveal
✓ Smooth transition between frames` : ''}
${selectedAnimation === 'pan' ? `✓ Pan position: ${Math.round(framePosition * 360)}° around product
✓ Camera orbiting smoothly around product
✓ Maintain consistent distance from product
✓ Natural camera movement` : ''}
${selectedAnimation === 'float' ? `✓ Float position: ${Math.round(Math.sin(framePosition * Math.PI * 2) * 30 + 50)}% height
✓ Smooth floating motion (up and down)
✓ Subtle rotation while floating
✓ Natural levitation effect with shadow` : ''}
${selectedAnimation === 'explode' ? `✓ Explosion progress: ${Math.round(framePosition * 100)}%
✓ Product parts separating cleanly
✓ Show internal components/layers
✓ Technical exploded view styling` : ''}

BACKGROUND & LIGHTING:
✓ Background: ${backgroundDescriptions[selectedBackground]}
✓ Consistent lighting across all frames
✓ Professional product photography quality
✓ Clean composition
✓ High contrast untuk product visibility
${selectedBackground === 'gradient' ? '✓ Smooth gradient complementary to product colors' : ''}
${selectedBackground === 'studio' ? '✓ Visible studio equipment, professional photography setup' : ''}
${selectedBackground === 'animated' ? '✓ Dynamic background elements (particles, bokeh, effects)' : ''}

PRODUCT PRESENTATION:
✓ Product remains sharp dan clear
✓ Consistent product appearance across frames
✓ Natural movement progression
✓ Professional color grading
✓ Realistic shadows dan reflections
✓ High-quality rendering
✓ Smooth animation transition

FRAME COHERENCE:
✓ This is frame ${index}/${selectedCount} in sequence
✓ Ensure smooth transition to next frame
✓ Maintain consistent style across sequence
✓ Natural motion progression
✓ No sudden jumps or glitches

VIDEO PRODUCTION QUALITY:
✓ Sharp focus on product
✓ Professional lighting setup
✓ Cinematic quality rendering
✓ Smooth motion blur (if applicable)
✓ Clean edges dan outlines
✓ Ready untuk video compilation

Generate frame ${index} dengan PERFECT CONTINUITY untuk sequential video - siap dijadikan product video yang professional dan engaging!`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: 'image/jpeg', data: base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedAspect }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();

                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `video_frame_${String(index).padStart(3, '0')}_${selectedAnimation}_${Date.now()}.jpg`;

                generatedFrames[index - 1] = { url: imageUrl, filename: filename };

                // Update card with image
                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Frame ${index}">
                    <div class="absolute top-2 left-2 bg-purple-600 text-white px-2 py-1 rounded text-xs font-bold">
                        Frame ${String(index).padStart(2, '0')}
                    </div>
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="videoframes-action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="videoframes-action-btn bg-gradient-to-r from-purple-600 to-violet-700 hover:from-purple-700 hover:to-violet-800 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;

            } catch (error) {
                console.error(`Frame ${index} generation error:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedFrames[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedFrames.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedFrames.length; i++) {
                        const img = generatedFrames[i];
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedFrames.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        function showError(message) {
            if (errorText) errorText.textContent = message;
            if (errorMessage) errorMessage.classList.remove('hidden');
        }

        function hideError() {
            if (errorMessage) errorMessage.classList.add('hidden');
        }

    })();

    // ==================== VEHICLE MODIFIER (AI CAR & MOTORCYCLE EDITOR) ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const imageUpload = document.getElementById('vehicle-image-upload');
        const uploadBox = document.querySelector('.upload-box-vehicle');
        const imagePreview = document.getElementById('vehicle-image-preview');
        const previewContainer = document.getElementById('vehicle-preview-container');
        const removePreviewBtn = document.getElementById('vehicle-remove-preview');

        const vehicleTypeButtons = document.querySelectorAll('[data-vehicletype]');
        const modCategoryButtons = document.querySelectorAll('[data-modcategory]');
        const modStyleButtons = document.querySelectorAll('[data-modstyle]');
        const customDescriptionInput = document.getElementById('vehicle-custom-description');
        const aspectButtons = document.querySelectorAll('#vehicle-aspect-selection [data-aspect]');
        const generateBtn = document.getElementById('vehicle-generate-btn');
        const errorMessage = document.getElementById('vehicle-error-message');
        const errorText = document.getElementById('vehicle-error-text');

        const loadingIndicator = document.getElementById('vehicle-loading-indicator');
        const loadingText = document.getElementById('vehicle-loading-text');
        const progressBar = document.getElementById('vehicle-progress-bar');
        const progressText = document.getElementById('vehicle-progress-text');

        const resultsSection = document.getElementById('vehicle-results-section');
        const emptyState = document.getElementById('vehicle-empty-state');
        const resultsGrid = document.getElementById('vehicle-results-grid');
        const resultsCount = document.getElementById('vehicle-results-count');
        const downloadAllBtn = document.getElementById('vehicle-download-all-btn');

        // State
        let originalImageData = null;
        let selectedVehicleType = 'car';
        let selectedModCategory = 'bodykit';
        let selectedModStyle = 'jdm';
        let selectedAspect = '16:9';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedVehicles = [];

        // Count selection (1-10), default 4
        const vehicleCountSel = document.getElementById('vehicle-count-selection-grid');
        if (vehicleCountSel) {
            vehicleCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                vehicleCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Upload handler
        if (uploadBox) {
            uploadBox.addEventListener('click', () => imageUpload?.click());
        }

        if (imageUpload) {
            imageUpload.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // HEIC conversion
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Gagal mengkonversi gambar HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onloadend = () => {
                    originalImageData = reader.result;

                    if (imagePreview && previewContainer) {
                        imagePreview.src = reader.result;
                        previewContainer.classList.remove('hidden');
                    }

                    if (generateBtn) generateBtn.disabled = false;
                };
                reader.readAsDataURL(processedFile);
            });
        }

        if (removePreviewBtn) {
            removePreviewBtn.addEventListener('click', () => {
                originalImageData = null;
                imageUpload.value = '';
                if (previewContainer) previewContainer.classList.add('hidden');
                if (imagePreview) imagePreview.src = '';
                if (generateBtn) generateBtn.disabled = true;
            });
        }

        // Selection handlers
        vehicleTypeButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                vehicleTypeButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedVehicleType = btn.dataset.vehicletype;
            });
        });

        modCategoryButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                modCategoryButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedModCategory = btn.dataset.modcategory;
            });
        });

        modStyleButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                modStyleButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedModStyle = btn.dataset.modstyle;
            });
        });

        aspectButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                aspectButtons.forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedAspect = btn.dataset.aspect;
            });
        });

        // Generate modified vehicles
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (!originalImageData || generateBtn.disabled) return;

                const originalHTML = generateBtn.innerHTML;
                generateBtn.disabled = true;
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Generating...';

                hideError();
                if (emptyState) emptyState.classList.add('hidden');
                if (resultsSection) resultsSection.classList.add('hidden');
                if (loadingIndicator) loadingIndicator.classList.remove('hidden');

                const MAX_ATTEMPTS = 3;
                let attempts = 0;
                let successCount = 0;

                while (attempts < MAX_ATTEMPTS && successCount === 0) {
                    attempts++;
                    generatedVehicles = [];
                    if (resultsGrid) resultsGrid.innerHTML = '';

                    for (let i = 1; i <= selectedCount; i++) {
                        const card = document.createElement('div');
                        card.id = `vehicle-card-${i}`;
                        card.className = 'relative rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center aspect-video';
                        card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-red-600"></div>';
                        resultsGrid?.appendChild(card);
                    }

                    const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                        generateSingleModification(i + 1)
                    );
                    await Promise.allSettled(generationPromises);
                    successCount = resultsGrid?.querySelectorAll('img').length || 0;
                }

                resultsGrid?.querySelectorAll('[id^="vehicle-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });
                generatedVehicles = generatedVehicles.filter(Boolean);

                if (loadingIndicator) loadingIndicator.classList.add('hidden');
                if (resultsSection) resultsSection.classList.remove('hidden');
                if (resultsCount) resultsCount.textContent = generatedVehicles.length;

                generateBtn.disabled = false;
                generateBtn.innerHTML = originalHTML;

                if (successCount === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                }
            });
        }

        async function generateSingleModification(index) {
            const card = document.getElementById(`vehicle-card-${index}`);

            try {
                const base64 = originalImageData.split(',')[1];
                const customDescription = customDescriptionInput?.value.trim() || '';

                const vehicleTypeDescriptions = {
                    'car': 'Mobil (Car)',
                    'motorcycle': 'Motor (Motorcycle)'
                };

                const modCategoryDescriptions = {
                    'bodykit': 'Body Kit - bumper depan/belakang agresif, side skirts, fender flares, wide body kit',
                    'wheels': 'Wheels/Velg - racing wheels, deep dish rims, spoke wheels, custom offset, tire fitment',
                    'paint': 'Paint/Color - custom paint job, color change, vinyl wrap, matte/glossy finish, racing livery',
                    'lowering': 'Lowering/Stance - slammed suspension, coilovers, air suspension, aggressive camber, fitment',
                    'spoiler': 'Spoiler/Wing - rear spoiler, GT wing, trunk spoiler, roof spoiler, carbon fiber aero',
                    'lighting': 'Lighting/LED - LED headlights, LED taillights, underglow, angel eyes, custom lighting',
                    'interior': 'Interior - racing seats, roll cage, steering wheel, dashboard, custom upholstery',
                    'full-custom': 'Full Custom - kombinasi semua modifikasi untuk look yang completely transformed'
                };

                const modStyleDescriptions = {
                    'jdm': 'JDM Style (Japanese Domestic Market) - clean, functional, iconic JDM aesthetics, aggressive yet refined',
                    'vip': 'VIP/Luxury - low, wide stance, premium wheels, luxurious appearance, executive style',
                    'racing': 'Racing/Track - aerodynamic modifications, racing livery, functional aero parts, track-focused setup',
                    'stance': 'Stance/Slammed - extremely low ride height, aggressive wheel fitment, stretched tires, maximum camber',
                    'offroad': 'Off-road/4x4 - lifted suspension, off-road tires, skid plates, roof racks, adventure-ready',
                    'classic': 'Classic/Retro - vintage styling, period-correct modifications, classic wheels, timeless aesthetic',
                    'modern': 'Modern/Futuristic - cutting-edge design, LED accents, aerodynamic shapes, contemporary styling',
                    'custom': 'Custom/Unique - one-of-a-kind modifications, creative customization, unique personality'
                };

                const prompt = `Buat MODIFIKASI ${vehicleTypeDescriptions[selectedVehicleType].toUpperCase()} yang SANGAT PROFESSIONAL dan REALISTIC dengan AI!

KENDARAAN ORIGINAL:
- Jenis: ${vehicleTypeDescriptions[selectedVehicleType]}
- Pertahankan model dan identitas dasar kendaraan
- Modifikasi hanya pada parts yang diminta

MODIFIKASI YANG DIMINTA:
- Kategori Utama: ${modCategoryDescriptions[selectedModCategory]}
- Style: ${modStyleDescriptions[selectedModStyle]}
- Aspect Ratio: ${selectedAspect}
${customDescription ? `- Custom Request: ${customDescription}` : ''}

PANDUAN MODIFIKASI PROFESSIONAL:
✓ REALISTIS - modifikasi harus believable dan possible dalam real life
✓ MAINTAIN VEHICLE IDENTITY - jangan ubah model/merk kendaraan, hanya modify parts
✓ QUALITY - hasil foto harus HIGH RESOLUTION, professional photography quality
✓ LIGHTING - consistent dengan foto original, realistic shadows dan reflections
✓ INTEGRATION - modifikasi harus seamlessly integrated dengan kendaraan
✓ PROPORTIONS - maintain realistic proportions, jangan over-exaggerated
✓ DETAILS - attention to detail pada every modification

SPESIFIK MODIFIKASI ${modCategoryDescriptions[selectedModCategory].split('-')[0].toUpperCase()}:
${selectedModCategory === 'bodykit' ? `✓ Aggressive bumper design dengan air intakes
✓ Side skirts yang flow dengan body lines
✓ Fender flares atau wide body kit (jika applicable)
✓ Professional fitment dan finish
✓ Maintain functional aspects` : ''}
${selectedModCategory === 'wheels' ? `✓ Aftermarket wheels dengan appropriate size
✓ Proper offset dan fitment untuk ${selectedModStyle} style
✓ Low-profile tires atau appropriate tire setup
✓ Clean wheel design yang complement kendaraan
✓ Realistic brake setup visible through wheels` : ''}
${selectedModCategory === 'paint' ? `✓ High-quality paint finish (matte/glossy sesuai style)
✓ Custom color atau livery design
✓ Professional application tanpa imperfections
✓ Color yang complement body lines
✓ Vinyl wrap appearance jika applicable` : ''}
${selectedModCategory === 'lowering' ? `✓ Lowered ride height sesuai ${selectedModStyle} style
✓ Proper suspension geometry
✓ Aggressive wheel fitment dengan minimal gap
✓ Realistic camber (jangan berlebihan kecuali stance style)
✓ Maintain drivability appearance` : ''}
${selectedModCategory === 'spoiler' ? `✓ Rear spoiler/wing design sesuai ${selectedModStyle} style
✓ Proper mounting dan integration
✓ Aerodynamic appearance
✓ Material finish (carbon fiber, body color, etc.)
✓ Scale appropriate untuk kendaraan` : ''}
${selectedModCategory === 'lighting' ? `✓ Modern LED headlight/taillight conversion
✓ Proper light output appearance
✓ Integration dengan body design
✓ Realistic light effects
✓ Underglow atau accent lighting (jika applicable)` : ''}
${selectedModCategory === 'interior' ? `✓ Visible interior upgrades through windows
✓ Racing seats atau premium seats
✓ Steering wheel upgrade
✓ Roll cage (jika racing style)
✓ Color-coordinated interior` : ''}
${selectedModCategory === 'full-custom' ? `✓ Comprehensive modifications across all categories
✓ Cohesive design theme
✓ Show car quality build
✓ Every detail considered
✓ Jaw-dropping transformation` : ''}

${selectedModStyle} STYLE EXECUTION:
${selectedModStyle === 'jdm' ? '✓ Clean lines, functional aero, period-correct wheels, subtle but impactful' : ''}
${selectedModStyle === 'vip' ? '✓ Low stance, luxury wheels, chrome accents, executive presence' : ''}
${selectedModStyle === 'racing' ? '✓ Aero parts, racing livery, functional cooling, track-ready appearance' : ''}
${selectedModStyle === 'stance' ? '✓ Slammed height, aggressive camber, wide wheels, stretched tires' : ''}
${selectedModStyle === 'offroad' ? '✓ Lifted suspension, all-terrain tires, skid plates, rugged appearance' : ''}
${selectedModStyle === 'classic' ? '✓ Vintage wheels, period styling, clean restoration, timeless look' : ''}
${selectedModStyle === 'modern' ? '✓ Contemporary design, LED accents, sleek lines, futuristic elements' : ''}
${selectedModStyle === 'custom' ? '✓ Unique interpretation, creative freedom, standout personality' : ''}

PHOTOGRAPHY & PRESENTATION:
✓ Professional automotive photography angle
✓ Perfect lighting untuk showcase modifications
✓ Sharp focus pada kendaraan
✓ Realistic environment (street, studio, track sesuai style)
✓ Clean background yang tidak distract
✓ High dynamic range
✓ Realistic reflections pada paint/wheels
✓ Professional color grading

VARIASI ${index}:
${index === 1 ? 'Standard modification dengan classic angle' :
  index === 2 ? 'Alternative angle atau additional modifications' :
  index === 3 ? 'Different color option atau modification intensity' :
  `Creative variation #${index} dengan unique interpretation`}

CRITICAL: Modifikasi harus REALISTIC, PROFESSIONAL, dan POSSIBLE dalam real life. Jangan buat modifikasi yang impossible atau cartoonish. Hasil harus photo-realistic dan believable!

Generate modified vehicle yang STUNNING dan REALISTIC - perfect untuk visualisasi modifikasi, car enthusiasts, dan automotive content!`;

                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: 'image/jpeg', data: base64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedAspect }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();

                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `vehicle_${selectedVehicleType}_${selectedModCategory}_${selectedModStyle}_${Date.now()}_${index}.jpg`;

                generatedVehicles[index - 1] = { url: imageUrl, filename: filename };

                // Update card with image
                card.className = 'relative group rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300';
                card.innerHTML = `
                    <img src="${imageUrl}" class="w-full h-full object-cover" alt="Modified Vehicle ${index}">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-index="${index - 1}" class="vehicle-action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-eye"></i>
                            <span class="hidden sm:inline">Preview</span>
                        </button>
                        <button data-action="download" data-index="${index - 1}" class="vehicle-action-btn bg-gradient-to-r from-red-600 to-red-700 hover:from-red-700 hover:to-red-800 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                            <i class="fas fa-download"></i>
                            <span class="hidden sm:inline">Download</span>
                        </button>
                    </div>
                `;

            } catch (error) {
                console.error(`Vehicle modification ${index} error:`, error);
                if (card) card.innerHTML = '';
            }
        }

        // Event delegation for action buttons
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedVehicles[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(imageData.url, imageData.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(imageData.url, imageData.filename);
                    }
                }
            });
        }

        // Download all
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedVehicles.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedVehicles.length; i++) {
                        const img = generatedVehicles[i];
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedVehicles.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        function showError(message) {
            if (errorText) errorText.textContent = message;
            if (errorMessage) errorMessage.classList.remove('hidden');
        }

        function hideError() {
            if (errorMessage) errorMessage.classList.add('hidden');
        }

    })();

    // ==================== TATTOO EDITOR ====================
    (function() {
        const apiKey = "";

        // DOM Elements
        const imageUpload = document.getElementById('tattoo-image-upload');
        const uploadBox = document.querySelector('.upload-box-tattoo');
        const imagePreview = document.getElementById('tattoo-image-preview');
        const previewContainer = document.getElementById('tattoo-preview-container');
        const removePreviewBtn = document.getElementById('tattoo-remove-preview');

        const descriptionInput = document.getElementById('tattoo-description');
        const styleButtons = document.querySelectorAll('#tattoo-style-selection [data-style]');
        const placementButtons = document.querySelectorAll('#tattoo-placement-selection [data-placement]');
        const sizeButtons = document.querySelectorAll('#tattoo-size-selection [data-size]');
        const colorButtons = document.querySelectorAll('#tattoo-color-selection [data-color]');
        const aspectButtons = document.querySelectorAll('#tattoo-aspect-ratio-selection [data-ratio]');

        const generateBtn = document.getElementById('tattoo-generate-btn');
        const errorMessage = document.getElementById('tattoo-error-message');
        const errorText = document.getElementById('tattoo-error-text');

        const placeholder = document.getElementById('tattoo-placeholder');
        const loadingIndicator = document.getElementById('tattoo-loading');
        const loadingText = document.getElementById('tattoo-loading-text');
        const progressBar = document.getElementById('tattoo-progress-bar');
        const progressText = document.getElementById('tattoo-progress-text');

        const resultsHeader = document.getElementById('tattoo-results-header');
        const resultsGrid = document.getElementById('tattoo-results-grid');
        const resultsCount = document.getElementById('tattoo-results-count');
        const downloadAllBtn = document.getElementById('tattoo-download-all-btn');

        // State
        let originalImageData = null;
        let selectedStyle = 'Realistic';
        let selectedPlacement = 'Arm';
        let selectedSize = 'Medium';
        let selectedColor = 'Black & Gray';
        let selectedAspect = '3:4';
        let selectedCount = 4; // 1-10, default 4 (count selector)
        let generatedImages = [];

        // Count selection (1-10), default 4
        const tattooCountSel = document.getElementById('tattoo-count-selection-grid');
        if (tattooCountSel) {
            tattooCountSel.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                tattooCountSel.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        // Upload handler
        if (uploadBox) {
            uploadBox.addEventListener('click', () => imageUpload?.click());
        }

        if (imageUpload) {
            imageUpload.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;

                // Convert HEIC to JPEG if needed
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        const convertedBlob = await heic2any({
                            blob: file,
                            toType: 'image/jpeg',
                            quality: 0.9
                        });
                        processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), {
                            type: 'image/jpeg'
                        });
                    } catch (err) {
                        console.error('HEIC conversion failed:', err);
                        showError('Failed to convert HEIC image');
                        return;
                    }
                }

                // Read the processed file
                const reader = new FileReader();
                reader.onloadend = () => {
                    originalImageData = reader.result;

                    // Show preview
                    if (imagePreview && previewContainer) {
                        imagePreview.src = reader.result;
                        previewContainer.classList.remove('hidden');
                    }

                    updateGenerateButton();
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove preview handler
        if (removePreviewBtn) {
            removePreviewBtn.addEventListener('click', () => {
                originalImageData = null;
                if (imageUpload) imageUpload.value = '';
                if (previewContainer) previewContainer.classList.add('hidden');
                if (imagePreview) imagePreview.src = '';
                updateGenerateButton();
            });
        }

        // Style selection handler
        if (styleButtons.length > 0) {
            styleButtons.forEach(btn => {
                btn.addEventListener('click', () => {
                    styleButtons.forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    selectedStyle = btn.dataset.style;
                });
            });
        }

        // Placement selection handler
        if (placementButtons.length > 0) {
            placementButtons.forEach(btn => {
                btn.addEventListener('click', () => {
                    placementButtons.forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    selectedPlacement = btn.dataset.placement;
                });
            });
        }

        // Size selection handler
        if (sizeButtons.length > 0) {
            sizeButtons.forEach(btn => {
                btn.addEventListener('click', () => {
                    sizeButtons.forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    selectedSize = btn.dataset.size;
                });
            });
        }

        // Color selection handler
        if (colorButtons.length > 0) {
            colorButtons.forEach(btn => {
                btn.addEventListener('click', () => {
                    colorButtons.forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    selectedColor = btn.dataset.color;
                });
            });
        }

        // Aspect ratio selection handler
        if (aspectButtons.length > 0) {
            aspectButtons.forEach(btn => {
                btn.addEventListener('click', () => {
                    aspectButtons.forEach(b => b.classList.remove('selected'));
                    btn.classList.add('selected');
                    selectedAspect = btn.dataset.ratio;
                });
            });
        }

        // Update generate button state
        function updateGenerateButton() {
            if (!generateBtn) return;

            const hasImage = originalImageData !== null;
            const hasDescription = descriptionInput?.value.trim().length > 0;

            generateBtn.disabled = !(hasImage && hasDescription);
        }

        // Listen to description input
        if (descriptionInput) {
            descriptionInput.addEventListener('input', updateGenerateButton);
        }

        // Generate tattoo designs
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (generateBtn.disabled) return;
                if (!originalImageData || !descriptionInput?.value.trim()) {
                    showError('Please upload a photo and describe your tattoo');
                    return;
                }

                if (placeholder) placeholder.classList.add('hidden');
                if (loadingIndicator) loadingIndicator.classList.remove('hidden');

                generateBtn.disabled = true;

                let attempts = 0;
                const MAX_ATTEMPTS = 3;
                let successCount = 0;

                try {
                    while (attempts < MAX_ATTEMPTS && successCount === 0) {
                        attempts++;
                        generatedImages = [];
                        if (resultsGrid) {
                            resultsGrid.innerHTML = '';
                            resultsGrid.classList.remove('hidden');
                        }

                        for (let i = 1; i <= selectedCount; i++) {
                            const card = document.createElement('div');
                            card.id = `tattoo-card-${i}`;
                            card.className = 'relative group rounded-xl overflow-hidden bg-gradient-to-br from-purple-100 to-violet-100 flex items-center justify-center aspect-[3/4]';
                            card.innerHTML = '<div class="animate-spin rounded-full h-10 w-10 border-b-2 border-purple-600"></div>';
                            if (resultsGrid) resultsGrid.appendChild(card);
                        }

                        const generationPromises = Array.from({ length: selectedCount }, (_, i) =>
                            generateSingleTattoo(i + 1)
                        );
                        await Promise.allSettled(generationPromises);

                        if (resultsGrid) {
                            resultsGrid.querySelectorAll('[id^="tattoo-card-"]').forEach(card => {
                                if (!card.querySelector('img')) card.remove();
                            });
                        }
                        successCount = generatedImages.filter(Boolean).length;
                    }

                    if (loadingIndicator) loadingIndicator.classList.add('hidden');

                    if (successCount === 0) {
                        if (placeholder) placeholder.classList.remove('hidden');
                        alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    } else {
                        if (resultsHeader) resultsHeader.classList.remove('hidden');
                        if (resultsCount) resultsCount.textContent = successCount;
                        if (window.innerWidth < 1024 && resultsHeader) {
                            setTimeout(() => resultsHeader.scrollIntoView({ behavior: 'smooth', block: 'start' }), 300);
                        }
                    }
                } finally {
                    generateBtn.disabled = false;
                    updateGenerateButton();
                }
            });
        }

        // Generate single tattoo
        async function generateSingleTattoo(index) {
            const card = document.getElementById(`tattoo-card-${index}`);
            try {

                // Create prompt
                const tattooDesc = descriptionInput.value.trim();
                const prompt = `Generate a photo of this person with a ${selectedStyle} tattoo on their ${selectedPlacement.toLowerCase()}.

TATTOO DETAILS:
${tattooDesc}

STYLE: ${selectedStyle}
SIZE: ${selectedSize}
COLOR: ${selectedColor}
PLACEMENT: ${selectedPlacement}

Create a realistic photo showing this person with the tattoo. The tattoo should look professionally done and naturally part of their skin. Keep the person's appearance, pose, and overall photo composition similar to the reference. The tattoo is the only change - everything else stays the same.

${selectedStyle === 'Realistic' ? 'Use photorealistic tattoo art with fine details and proper shading.' : ''}
${selectedStyle === 'Traditional' ? 'Use bold outlines and classic American traditional tattoo style.' : ''}
${selectedStyle === 'Minimalist' ? 'Use simple clean lines and minimal design.' : ''}
${selectedStyle === 'Watercolor' ? 'Use soft watercolor tattoo style with artistic color splashes.' : ''}
${selectedStyle === 'Tribal' ? 'Use bold tribal patterns with strong black designs.' : ''}
${selectedStyle === 'Japanese' ? 'Use traditional Japanese Irezumi style.' : ''}
${selectedStyle === 'Neo-Traditional' ? 'Use modern neo-traditional style with vibrant colors.' : ''}
${selectedStyle === 'Geometric' ? 'Use precise geometric patterns and symmetry.' : ''}
${selectedStyle === 'Blackwork' ? 'Use solid black ink with bold graphic designs.' : ''}
${selectedStyle === 'Dotwork' ? 'Use dotwork/stippling technique.' : ''}

Make it look like a real ${selectedSize.toLowerCase()} ${selectedColor.toLowerCase()} tattoo that's actually on their skin, not a sticker or overlay.`;

                // Prepare API request
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const base64Image = originalImageData.split(',')[1];

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            {
                                inlineData: {
                                    mimeType: 'image/jpeg',
                                    data: base64Image
                                }
                            }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: {
                            aspectRatio: selectedAspect
                        }
                    }
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (!imageData) throw new Error('No image data received');

                const imageUrl = `data:image/jpeg;base64,${imageData}`;
                const filename = `tattoo-${selectedStyle.toLowerCase()}-${selectedPlacement.toLowerCase()}-${Date.now()}-${index}.jpg`;

                generatedImages[index - 1] = { url: imageUrl, filename };

                if (card) {
                    card.className = 'relative group rounded-xl overflow-hidden bg-gray-100 aspect-[3/4] shadow-lg hover:shadow-2xl transition-all duration-300';
                    card.innerHTML = `
                        <img src="${imageUrl}" class="w-full h-full object-cover" alt="Tattoo Design ${index}">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                            <button data-action="preview" data-index="${index - 1}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-eye"></i>
                                <span class="hidden sm:inline">Preview</span>
                            </button>
                            <button data-action="download" data-index="${index - 1}" class="action-btn bg-gradient-to-r from-purple-500 to-violet-600 hover:from-purple-600 hover:to-violet-700 text-white px-4 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-2">
                                <i class="fas fa-download"></i>
                                <span class="hidden sm:inline">Download</span>
                            </button>
                        </div>
                    `;
                }

            } catch (error) {
                console.error('Error generating tattoo:', error);
                if (card) card.innerHTML = '';
            }
        }

        // Results grid click handler (preview and download)
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();

                const actionBtn = e.target.closest('[data-action]');
                if (!actionBtn) return;

                const action = actionBtn.dataset.action;
                const index = parseInt(actionBtn.dataset.index, 10);
                const imageData = generatedImages[index];

                if (!imageData) return;

                if (action === 'preview') {
                    if (window.showPreviewModal) {
                        window.showPreviewModal(imageData.url);
                    }
                } else if (action === 'download') {
                    try {
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(imageData.url, imageData.filename);
                        } else if (window.downloadImage) {
                            await window.downloadImage(imageData.url, imageData.filename);
                        }
                    } catch (error) {
                        console.error('Download error:', error);
                    }
                }
            });
        }

        // Download all handler
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();

                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    for (let i = 0; i < generatedImages.length; i++) {
                        const img = generatedImages[i];
                        if (!img) continue;
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>Downloading ${i + 1}/${generatedImages.length}...</span>`;

                        try {
                            if (window.downloadDataURINew) {
                                await window.downloadDataURINew(img.url, img.filename);
                            } else if (window.downloadImage) {
                                await window.downloadImage(img.url, img.filename);
                            }
                            await new Promise(resolve => setTimeout(resolve, 500));
                        } catch (error) {
                            console.error(`Error downloading image ${i + 1}:`, error);
                        }
                    }

                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Complete!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }

        // Show error message
        function showError(message) {
            if (errorText) errorText.textContent = message;
            if (errorMessage) errorMessage.classList.remove('hidden');
            if (loadingIndicator) loadingIndicator.classList.add('hidden');
            if (placeholder) placeholder.classList.remove('hidden');

            setTimeout(() => {
                if (errorMessage) errorMessage.classList.add('hidden');
            }, 5000);
        }

    })();

    // ==================== MEMBUAT MODEL ====================
    (function() {
        const apiKey = "";
        const imageInput = document.getElementById('buat-model-image-input');
        const uploadArea = document.getElementById('buat-model-upload-area');
        const previewContainer = document.getElementById('buat-model-preview-container');
        const previewImg = document.getElementById('buat-model-preview');
        const removeBtn = document.getElementById('buat-model-remove-btn');
        const generateBtn = document.getElementById('buat-model-generate-btn');
        const resultsGrid = document.getElementById('buat-model-results-grid');
        const downloadAllBtn = document.getElementById('buat-model-download-all-btn');
        const countText = document.getElementById('buat-model-count-text');
        const outfitInput = document.getElementById('buat-model-outfit-input');

        let faceImageBase64 = null;
        let faceImageMimeType = null;
        let selectedGender = 'pria';
        let selectedType = 'full-body';
        let selectedRatio = '3:4';
        let selectedCount = 4;
        let generatedImages = [];

        // HEIC conversion
        async function convertHeicToJpeg(file) {
            if (window.heic2any) {
                try {
                    const convertedBlob = await heic2any({
                        blob: file,
                        toType: 'image/jpeg',
                        quality: 0.9
                    });
                    return new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                } catch (err) {
                    console.error('HEIC conversion failed:', err);
                    throw new Error('Gagal mengkonversi gambar HEIC');
                }
            }
            throw new Error('HEIC converter not available');
        }

        function updateGenerateButtonState() {
            generateBtn.disabled = !faceImageBase64;
        }

        // Image upload handler
        if (imageInput) {
            imageInput.addEventListener('change', async (e) => {
                const file = e.target.files[0];
                if (!file) return;

                let processedFile = file;
                if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                    try {
                        processedFile = await convertHeicToJpeg(file);
                    } catch (err) {
                        alert('Gagal mengkonversi file HEIC');
                        return;
                    }
                }

                const reader = new FileReader();
                reader.onload = (event) => {
                    const result = event.target.result;
                    const parts = result.split(',');
                    faceImageMimeType = parts[0].match(/:(.*?);/)[1];
                    faceImageBase64 = parts[1];

                    previewImg.src = result;
                    uploadArea.classList.add('hidden');
                    previewContainer.classList.remove('hidden');
                    updateGenerateButtonState();
                };
                reader.readAsDataURL(processedFile);
            });
        }

        // Remove image
        if (removeBtn) {
            removeBtn.addEventListener('click', () => {
                faceImageBase64 = null;
                faceImageMimeType = null;
                imageInput.value = '';
                previewContainer.classList.add('hidden');
                uploadArea.classList.remove('hidden');
                updateGenerateButtonState();
            });
        }

        // Drag & Drop support
        if (uploadArea) {
            const uploadLabel = uploadArea.querySelector('label');

            uploadArea.addEventListener('dragover', (e) => {
                e.preventDefault();
                e.stopPropagation();
                uploadArea.classList.add('drag-over');
            });

            uploadArea.addEventListener('dragleave', (e) => {
                e.preventDefault();
                e.stopPropagation();
                uploadArea.classList.remove('drag-over');
            });

            uploadArea.addEventListener('drop', async (e) => {
                e.preventDefault();
                e.stopPropagation();
                uploadArea.classList.remove('drag-over');

                const files = e.dataTransfer?.files;
                if (files && files.length > 0) {
                    const file = files[0];
                    if (file.type.startsWith('image/') || file.name.toLowerCase().endsWith('.heic')) {
                        let processedFile = file;
                        if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                            try {
                                processedFile = await convertHeicToJpeg(file);
                            } catch (err) {
                                alert('Gagal mengkonversi file HEIC');
                                return;
                            }
                        }

                        const reader = new FileReader();
                        reader.onload = (event) => {
                            const result = event.target.result;
                            const parts = result.split(',');
                            faceImageMimeType = parts[0].match(/:(.*?);/)[1];
                            faceImageBase64 = parts[1];

                            previewImg.src = result;
                            uploadArea.classList.add('hidden');
                            previewContainer.classList.remove('hidden');
                            updateGenerateButtonState();
                        };
                        reader.readAsDataURL(processedFile);
                    }
                }
            });
        }

        // Gender selection
        const genderSelection = document.getElementById('buat-model-gender-selection');
        if (genderSelection) {
            genderSelection.addEventListener('click', (e) => {
                const btn = e.target.closest('.gender-btn-model');
                if (!btn) return;

                document.querySelectorAll('.gender-btn-model').forEach(b => {
                    b.classList.remove('selected');
                    b.style.borderColor = '';
                    b.style.background = '';
                    b.style.color = '';
                    b.style.boxShadow = '';
                    const icon = b.querySelector('i');
                    if (icon) icon.classList.add('text-violet-500');
                    const span = b.querySelector('span');
                    if (span) span.classList.add('text-gray-700');
                });

                btn.classList.add('selected');
                btn.style.borderColor = '#7c3aed';
                btn.style.background = 'linear-gradient(135deg, #7c3aed, #6366f1)';
                btn.style.color = 'white';
                btn.style.boxShadow = '0 4px 12px rgba(124, 58, 237, 0.3)';
                const icon = btn.querySelector('i');
                if (icon) icon.classList.remove('text-violet-500');
                const span = btn.querySelector('span');
                if (span) span.classList.remove('text-gray-700');

                selectedGender = btn.dataset.gender;
            });
        }

        // Type selection
        const typeSelection = document.getElementById('buat-model-type-selection');
        if (typeSelection) {
            typeSelection.addEventListener('click', (e) => {
                const btn = e.target.closest('.type-btn-model');
                if (!btn) return;

                document.querySelectorAll('.type-btn-model').forEach(b => {
                    b.classList.remove('selected');
                    b.style.borderColor = '';
                    b.style.background = '';
                    b.style.color = '';
                    b.style.boxShadow = '';
                    const icon = b.querySelector('i');
                    if (icon) icon.classList.add('text-violet-500');
                    const spans = b.querySelectorAll('span');
                    spans.forEach(s => {
                        s.classList.remove('opacity-80');
                        if (s.classList.contains('font-semibold')) s.classList.add('text-gray-700');
                        else s.classList.add('text-gray-500');
                    });
                });

                btn.classList.add('selected');
                btn.style.borderColor = '#7c3aed';
                btn.style.background = 'linear-gradient(135deg, #7c3aed, #6366f1)';
                btn.style.color = 'white';
                btn.style.boxShadow = '0 4px 12px rgba(124, 58, 237, 0.3)';
                const icon = btn.querySelector('i');
                if (icon) icon.classList.remove('text-violet-500');
                const spans = btn.querySelectorAll('span');
                spans.forEach(s => {
                    s.classList.remove('text-gray-700', 'text-gray-500');
                    if (!s.classList.contains('font-semibold')) s.classList.add('opacity-80');
                });

                selectedType = btn.dataset.type;
            });
        }

        // Ratio selection
        const ratioSelection = document.getElementById('buat-model-ratio-selection');
        if (ratioSelection) {
            ratioSelection.addEventListener('click', (e) => {
                const btn = e.target.closest('.ratio-btn-model');
                if (!btn) return;

                document.querySelectorAll('.ratio-btn-model').forEach(b => {
                    b.classList.remove('selected');
                    b.style.borderColor = '';
                    b.style.background = '';
                    b.style.color = '';
                    b.style.boxShadow = '';
                });

                btn.classList.add('selected');
                btn.style.borderColor = '#7c3aed';
                btn.style.background = 'linear-gradient(135deg, #7c3aed, #6366f1)';
                btn.style.color = 'white';
                btn.style.boxShadow = '0 4px 12px rgba(124, 58, 237, 0.3)';

                selectedRatio = btn.dataset.ratio;
            });
        }

        // Count selection (1-10), default 4
        const countSelection = document.getElementById('buat-model-count-selection');
        if (countSelection) {
            countSelection.addEventListener('click', (e) => {
                const btn = e.target.closest('button[data-count]');
                if (!btn) return;
                countSelection.querySelectorAll('button').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                selectedCount = parseInt(btn.dataset.count, 10);
            });
        }

        let stopRequested = false;
        const stopBtn = document.getElementById('buat-model-stop-btn');

        if (stopBtn) {
            stopBtn.addEventListener('click', () => {
                stopRequested = true;
            });
        }

        function renderSuccessfulResults(aspectClass) {
            resultsGrid.innerHTML = '';
            const successful = generatedImages.filter(img => img);
            if (successful.length === 0) return;
            successful.forEach((img, idx) => {
                const card = document.createElement('div');
                card.className = `relative group rounded-xl overflow-hidden bg-gradient-to-br from-violet-100 to-indigo-100 ${aspectClass}`;
                card.innerHTML = `
                    <img src="${img.url}" class="w-full h-full object-cover" alt="Model Photo">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/30 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                        <button data-action="preview" data-result-index="${idx}" class="action-btn min-w-[44px] min-h-[44px] bg-white/90 hover:bg-white text-gray-800 px-4 py-2.5 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center justify-center gap-2 text-sm">
                            <i class="fas fa-eye pointer-events-none"></i>
                            <span class="pointer-events-none">Preview</span>
                        </button>
                        <button data-action="download" data-result-index="${idx}" class="action-btn min-w-[44px] min-h-[44px] bg-gradient-to-r from-violet-500 to-indigo-600 hover:from-violet-600 hover:to-indigo-700 text-white px-4 py-2.5 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center justify-center gap-2 text-sm">
                            <i class="fas fa-download pointer-events-none"></i>
                            <span class="pointer-events-none">Download</span>
                        </button>
                    </div>
                `;
                resultsGrid.appendChild(card);
            });
            downloadAllBtn.classList.remove('hidden');
            if (window.innerWidth < 1024) {
                resultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' });
            }
        }

        // Generate model photos
        if (generateBtn) {
            generateBtn.addEventListener('click', async () => {
                if (generateBtn.disabled) return;
                if (!faceImageBase64) return;

                stopRequested = false;
                generateBtn.disabled = true;
                generateBtn.classList.add('hidden');
                if (stopBtn) stopBtn.classList.remove('hidden');
                generatedImages = new Array(selectedCount).fill(null);
                downloadAllBtn.classList.add('hidden');

                // Create placeholder cards
                resultsGrid.innerHTML = '';
                let aspectClass = 'aspect-[3/4]';
                if (selectedRatio === '1:1') aspectClass = 'aspect-square';
                else if (selectedRatio === '9:16') aspectClass = 'aspect-[9/16]';
                else if (selectedRatio === '16:9') aspectClass = 'aspect-video';

                for (let i = 0; i < selectedCount; i++) {
                    const card = document.createElement('div');
                    card.id = `buat-model-card-${i}`;
                    card.className = `relative group rounded-xl overflow-hidden bg-gradient-to-br from-violet-100 to-indigo-100 ${aspectClass} flex items-center justify-center`;
                    card.innerHTML = '<div class="loader !border-l-violet-600"></div>';
                    resultsGrid.appendChild(card);
                }

                // Auto-scroll to results on mobile
                setTimeout(() => {
                    if (window.innerWidth < 1024) {
                        resultsGrid.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }
                }, 300);

                try {
                    // Generate images in parallel
                    const promises = Array.from({ length: selectedCount }, (_, i) => generateSingleModelPhoto(i));
                    await Promise.allSettled(promises);

                    renderSuccessfulResults(aspectClass);
                } finally {
                    generateBtn.disabled = false;
                    generateBtn.classList.remove('hidden');
                    if (stopBtn) stopBtn.classList.add('hidden');
                }
            });
        }

        async function generateSingleModelPhoto(index) {
            const card = document.getElementById(`buat-model-card-${index}`);
            if (!card || stopRequested) return;

            try {
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image-preview:generateContent?key=${apiKey}`;

                const genderText = selectedGender === 'pria' ? 'male model, man' : 'female model, woman';
                const outfitText = outfitInput?.value.trim() || (selectedGender === 'pria' ? 'casual white t-shirt and dark jeans' : 'casual blouse and jeans');

                let poseText = '';
                if (selectedType === 'full-body') {
                    poseText = 'full body shot from head to feet, standing straight facing camera, confident pose, arms relaxed at sides';
                } else if (selectedType === 'half-body') {
                    poseText = 'half body shot from waist up, facing camera directly, natural relaxed pose, hands visible';
                } else {
                    poseText = 'portrait close-up, head and shoulders, facing camera directly, natural expression';
                }

                const aspectRatioText = selectedRatio === '1:1' ? 'square 1:1 aspect ratio' : selectedRatio === '9:16' ? 'vertical 9:16 portrait aspect ratio' : selectedRatio === '16:9' ? 'horizontal 16:9 landscape aspect ratio' : 'portrait 3:4 aspect ratio';

                const prompt = `Create a professional high-quality photo of a ${genderText} with the EXACT same face as the reference photo provided. Output image in ${aspectRatioText}.

CRITICAL REQUIREMENTS:
- The face MUST be IDENTICAL to the reference photo (same facial features, skin tone, facial structure)
- ${poseText}
- Wearing: ${outfitText}
- Simple clean background (solid light gray or white studio background)
- Professional studio lighting, soft and flattering
- Ultra sharp, high resolution, 8K quality
- Natural skin texture, photorealistic
- Looking directly at camera with confident, approachable expression
- Clean, professional model photography style

This photo will be used as a consistent model reference for various product photography and content creation.`;

                const payload = {
                    contents: [{
                        parts: [
                            { text: prompt },
                            { inlineData: { mimeType: faceImageMimeType, data: faceImageBase64 } }
                        ]
                    }],
                    generationConfig: {
                        responseModalities: ['TEXT', 'IMAGE'],
                        imageConfig: { aspectRatio: selectedRatio }
                    },
                    safetySettings: [
                        { category: "HARM_CATEGORY_HARASSMENT", threshold: "BLOCK_NONE" },
                        { category: "HARM_CATEGORY_HATE_SPEECH", threshold: "BLOCK_NONE" },
                        { category: "HARM_CATEGORY_SEXUALLY_EXPLICIT", threshold: "BLOCK_NONE" },
                        { category: "HARM_CATEGORY_DANGEROUS_CONTENT", threshold: "BLOCK_NONE" }
                    ]
                };

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                });

                const result = await response.json();
                const imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;

                if (imageData) {
                    const imageUrl = `data:image/png;base64,${imageData}`;
                    const filename = `model_${selectedType}_${selectedGender}_${index + 1}.png`;
                    generatedImages[index] = { url: imageUrl, filename };
                    card.innerHTML = `<img src="${imageUrl}" class="w-full h-full object-cover" alt="Model Photo">`;
                } else {
                    throw new Error('No image data received');
                }
            } catch (error) {
                console.error(`Error generating model photo ${index}:`, error);
                card.innerHTML = '';
            }
        }

        // Action buttons (preview/download)
        if (resultsGrid) {
            resultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();
                const btn = e.target.closest('[data-action]');
                if (!btn) return;

                const action = btn.dataset.action;
                const resultIndex = parseInt(btn.dataset.resultIndex, 10);
                const successful = generatedImages.filter(img => img);
                const img = successful[resultIndex];

                if (!img) return;

                if (action === 'preview' && window.showPreviewModal) {
                    window.showPreviewModal(img.url);
                } else if (action === 'download') {
                    if (window.downloadDataURINew) {
                        await window.downloadDataURINew(img.url, img.filename);
                    } else if (window.downloadImage) {
                        await window.downloadImage(img.url, img.filename);
                    }
                }
            });
        }

        // Download all
        if (downloadAllBtn) {
            downloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();
                if (generatedImages.length === 0 || downloadAllBtn.disabled) return;

                const originalHTML = downloadAllBtn.innerHTML;
                downloadAllBtn.disabled = true;

                try {
                    const imagesToDownload = generatedImages.filter(img => img);
                    for (let i = 0; i < imagesToDownload.length; i++) {
                        downloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>${i + 1}/${imagesToDownload.length}</span>`;
                        const img = imagesToDownload[i];
                        if (window.downloadDataURINew) {
                            await window.downloadDataURINew(img.url, img.filename);
                        } else if (window.downloadImage) {
                            await window.downloadImage(img.url, img.filename);
                        }
                        await new Promise(r => setTimeout(r, 500));
                    }
                    downloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    downloadAllBtn.innerHTML = '<i class="fas fa-times mr-2"></i><span>Error</span>';
                    setTimeout(() => {
                        downloadAllBtn.disabled = false;
                        downloadAllBtn.innerHTML = originalHTML;
                    }, 2000);
                }
            });
        }
    })();

    // ==================== FOTOGENIC ====================
    (function() {
        const apiKey = "";

        const effectsLibrary = {
            'Film & Vintage': [
                { name: 'Film Grain', icon: 'fa-film', prompt: 'Apply authentic analog film grain texture with subtle color shifts, slight vignetting, and that warm organic feel of 35mm photography. Preserve skin tones and key subjects clearly.' },
                { name: 'Polaroid', icon: 'fa-camera-retro', prompt: 'Apply Polaroid instant photo aesthetic with faded whites, slightly desaturated colors, soft pastel midtones, and that dreamy overexposed vintage look.' },
                { name: 'Kodachrome', icon: 'fa-palette', prompt: 'Apply classic Kodachrome film look with rich saturated reds and greens, warm yellows, deep blacks, and that iconic 1960s-70s color slide film rendering.' },
                { name: 'Cross Process', icon: 'fa-shuffle', prompt: 'Apply cross-processing effect with high contrast, shifted color channels — cyan/teal shadows, yellow-orange highlights, oversaturated hues, dramatic split-tone.' },
                { name: 'Lomography', icon: 'fa-circle', prompt: 'Apply Lomography plastic lens aesthetic with heavy vignetting, color shifts toward cyan and magenta, slight blur on edges, and dreamy saturated center.' },
                { name: 'VHS Tape', icon: 'fa-tv', prompt: 'Apply 1980s VHS tape recording look with horizontal scan lines, slight color bleeding, magenta/cyan channel misalignment, reduced sharpness, and analog noise.' },
                { name: 'Faded Film', icon: 'fa-image', prompt: 'Apply faded old film look with lifted blacks, desaturated muted tones, slight yellow-green cast, and that aged old photograph quality.' },
                { name: 'Tungsten Shift', icon: 'fa-lightbulb', prompt: 'Apply tungsten light film correction with warm orange-amber color shift, deep shadows, rich skin tones, and that indoor artificial lighting ambiance.' }
            ],
            'Cinematic & Moody': [
                { name: 'Hollywood', icon: 'fa-star', prompt: 'Apply Hollywood blockbuster color grade with teal-orange color balance, deep rich blacks, lifted midtones, cinematic contrast, and professional film look DI finishing.' },
                { name: 'Noir', icon: 'fa-moon', prompt: 'Apply classic Film Noir look with high contrast black and white, deep shadows, dramatic lighting, and that dark moody detective film aesthetic.' },
                { name: 'Dark Cinema', icon: 'fa-film', prompt: 'Apply dark cinematic grade with crushed blacks, desaturated highlights, cool blue-grey tones, minimal color, and that brooding dramatic arthouse film look.' },
                { name: 'Blade Runner', icon: 'fa-robot', prompt: 'Apply Blade Runner neo-noir cyberpunk grade with deep blue-purple shadows, neon color accents, hazy rain-slicked atmosphere, and urban dystopian mood.' },
                { name: 'Wes Anderson', icon: 'fa-palette', prompt: 'Apply Wes Anderson symmetrical pastel color palette with muted pinks, yellows, greens, warm midtones, flat graphic aesthetic, and whimsical storybook quality.' },
                { name: 'Blue Hour', icon: 'fa-clock', prompt: 'Apply blue hour twilight grade with deep ocean blues, magenta purple horizon, balanced natural night light, and that magical golden-to-blue dusk transition.' },
                { name: 'Smoke & Haze', icon: 'fa-smog', prompt: 'Apply atmospheric smoke and haze grade with milky diffused light, lifted fog over shadows, soft contrast, and ethereal dreamlike environmental ambiance.' },
                { name: 'Moonlight', icon: 'fa-moon', prompt: 'Apply moonlit night grade with cool silver-blue tones, deep navy shadows, soft luminescent highlights, and that romantic nocturnal silver light aesthetic.' }
            ],
            'Pop & Neon': [
                { name: 'Cyberpunk', icon: 'fa-bolt', prompt: 'Apply cyberpunk neon city aesthetic with high contrast, electric magenta and cyan neon glows, deep dark shadows, and futuristic urban energy.' },
                { name: 'Neon Tokyo', icon: 'fa-city', prompt: 'Apply Neon Tokyo street aesthetic with vivid neon pink, blue, and yellow color accents, high saturation, urban energy, and Japanese urban night scene quality.' },
                { name: 'Y2K Glam', icon: 'fa-star', prompt: 'Apply Y2K era glam aesthetic with metallic sheen, holographic color shifts, sparkle and glitter overlays, bubblegum pinks, and that early 2000s pop star visual.' },
                { name: 'Glitch Art', icon: 'fa-wifi', prompt: 'Apply digital glitch art effect with RGB channel separation, pixel displacement, scan line artifacts, data corruption aesthetic, and digital error visual language.' },
                { name: 'Holographic', icon: 'fa-gem', prompt: 'Apply holographic prism effect with rainbow color spectrum shifts, iridescent sheen, chrome metallic quality, and that holographic foil prismatic light diffraction.' },
                { name: 'Electric', icon: 'fa-plug', prompt: 'Apply electric energy grade with high voltage contrast, electric blue and white light bursts, dramatic highlights, and that powerful electric charge visual energy.' },
                { name: 'UV Night', icon: 'fa-sun', prompt: 'Apply UV black light night club effect with glowing neon colors under ultraviolet light — vivid blues, purples, pinks on dark black background.' },
                { name: 'Synthwave', icon: 'fa-music', prompt: 'Apply 1980s synthwave retro-futurist aesthetic with purple-pink gradient sky, neon grid lines, chrome text effect, and that vaporwave sunset visual.' }
            ],
            'Natural & Warm': [
                { name: 'Golden Hour', icon: 'fa-sun', prompt: 'Apply golden hour sunset light with warm golden orange glow, long soft shadows, lifted warm shadows, and that magical hour before sunset cinematic quality.' },
                { name: 'Forest Bath', icon: 'fa-tree', prompt: 'Apply forest bathing green atmosphere with lush natural greens, cool forest light filtering through leaves, earthy tones, and serene nature quality.' },
                { name: 'Beach Summer', icon: 'fa-water', prompt: 'Apply bright summer beach look with high key bright whites, warm sandy tones, aquamarine blue-green, bleached highlights, and carefree summer vacation energy.' },
                { name: 'Sunrise Glow', icon: 'fa-cloud-sun', prompt: 'Apply early morning sunrise with soft peach-pink warm light, gentle golden rays, fresh morning atmosphere, and that hopeful new day luminous quality.' },
                { name: 'Desert Warm', icon: 'fa-sun', prompt: 'Apply desert landscape warmth with rich terracotta and ochre tones, high ambient warmth, dry arid atmosphere, and that southwest desert canyon aesthetic.' },
                { name: 'Autumn Leaves', icon: 'fa-leaf', prompt: 'Apply autumn fall foliage palette with rich warm reds, oranges, and yellows, soft diffused light, and that cozy nostalgic fall season ambiance.' },
                { name: 'Misty Morning', icon: 'fa-cloud', prompt: 'Apply misty morning fog look with soft diffused light, cool blue-grey mist, gentle haze over landscape, and that serene peaceful early morning atmosphere.' },
                { name: 'Tropical Vibrant', icon: 'fa-seedling', prompt: 'Apply tropical paradise look with vivid saturated greens and blues, high brightness, lush vegetation colors, and that vibrant energetic tropical destination feel.' }
            ],
            'Aesthetic & Soft': [
                { name: 'Pastel Dream', icon: 'fa-cloud', prompt: 'Apply soft pastel dream look with gentle washed-out pastels — baby pink, lavender, mint, peach — low contrast, airy quality, and dreamy kawaii aesthetic.' },
                { name: 'VSCO Aesthetic', icon: 'fa-camera', prompt: 'Apply popular VSCO filter aesthetic with slight fade, warm film tones, lifted blacks, subtle grain, and that clean Instagram-worthy lifestyle photography look.' },
                { name: 'Minimalist White', icon: 'fa-square', prompt: 'Apply clean minimalist white look with high key overexposed whites, clean bright tones, minimal shadows, and that Scandinavian clean modern editorial aesthetic.' },
                { name: 'Rose Garden', icon: 'fa-heart', prompt: 'Apply romantic rose garden aesthetic with warm rosy pink tones, soft diffused light, gentle bloom, and that feminine floral romantic photography quality.' },
                { name: 'Cloud Soft', icon: 'fa-cloud', prompt: 'Apply ultra-soft cloud-like look with bright airy tones, very low contrast, soft haze over entire image, and that ethereal weightless floating aesthetic.' },
                { name: 'Cherry Blossom', icon: 'fa-spa', prompt: 'Apply Japanese cherry blossom sakura aesthetic with delicate pink tones, soft hazy spring light, gentle petals feeling, and that fleeting beautiful spring moment.' },
                { name: 'Cottagecore', icon: 'fa-home', prompt: 'Apply cottagecore aesthetic with warm earthy tones, soft natural greens, vintage whimsy, gentle floral, and that cozy romantic countryside cottage living quality.' },
                { name: 'Dreamy Blur', icon: 'fa-eye', prompt: 'Apply dreamy soft focus blur effect with gentle gaussian diffusion on edges, soft ethereal glow on highlights, and that romantic soft-focus portrait lens quality.' }
            ],
            'Fashion & Editorial': [
                { name: 'Magazine Cover', icon: 'fa-newspaper', prompt: 'Apply high-end fashion magazine cover look with perfect clean skin, bright editorial lighting, rich color pop, and that Vogue/Bazaar professional editorial quality.' },
                { name: 'Runway Show', icon: 'fa-person-walking', prompt: 'Apply fashion runway show aesthetic with sharp clean white light, stark high contrast, pure white background quality, and that catwalk presentation professional look.' },
                { name: 'Vogue Black', icon: 'fa-circle-dot', prompt: 'Apply Vogue Black editorial dark fashion aesthetic with deep rich blacks, dramatic studio lighting, high contrast, luxury fashion brand quality.' },
                { name: 'Luxury Gold', icon: 'fa-crown', prompt: 'Apply luxury gold editorial look with warm golden tones, rich amber highlights, opulent warm light, and that high-end luxury fashion brand campaign aesthetic.' },
                { name: 'Street Style', icon: 'fa-road', prompt: 'Apply urban street style look with gritty city tones, desaturated concrete palette, strong contrast, and that fashion week street photography documentary aesthetic.' },
                { name: 'Beauty Vlog', icon: 'fa-video', prompt: 'Apply beauty vlog soft glamour look with perfect glowing skin, soft warm lighting, bright airy background, and that YouTube beauty influencer well-lit aesthetic.' },
                { name: 'Campaign Shot', icon: 'fa-bullhorn', prompt: 'Apply advertising campaign quality with perfect commercial photography lighting, clean precise color, professional retouching look, and that major brand campaign aesthetic.' },
                { name: 'Bold Editorial', icon: 'fa-bold', prompt: 'Apply bold high-fashion editorial look with high saturation, strong graphic composition, vivid color blocking, and that avant-garde creative director artistic vision.' }
            ],
            'Retro & Classic': [
                { name: '70s Kodacolor', icon: 'fa-film', prompt: 'Apply 1970s Kodacolor print film look with warm orange-brown color cast, faded yellow highlights, soft greens, and that nostalgic 1970s family album photo aesthetic.' },
                { name: '80s Neon', icon: 'fa-bolt', prompt: 'Apply 1980s neon pop culture aesthetic with electric colors, strong pink and teal contrast, high saturation, and that decade of excess visual energy.' },
                { name: '90s Grunge', icon: 'fa-guitar', prompt: 'Apply 1990s grunge aesthetic with desaturated washed tones, green-cyan color cast, raw documentary look, and that analog print 1990s underground cultural feel.' },
                { name: 'Faded 90s', icon: 'fa-camera', prompt: 'Apply faded 1990s photo print look with heavily lifted blacks, washed cyan highlights, low contrast, and that old Fujifilm print photo color quality.' },
                { name: 'Disco Fever', icon: 'fa-music', prompt: 'Apply 1970s Disco era look with warm amber glow, high saturation, sparkle and shimmer quality, and that Studio 54 disco ball party night aesthetic.' },
                { name: 'Classic B&W', icon: 'fa-circle-half-stroke', prompt: 'Apply classic black and white film conversion with rich tonal range, deep blacks, bright whites, silver gelatin print quality, and traditional darkroom aesthetic.' },
                { name: 'Sepia Vintage', icon: 'fa-photo-film', prompt: 'Apply antique sepia tone look with warm brown monochrome, aged paper texture feeling, vignetting, and that Victorian era or early 1900s photograph quality.' },
                { name: 'Old Hollywood', icon: 'fa-film', prompt: 'Apply Old Hollywood golden age glamour look with silver screen high key lighting, soft diffusion on highlights, glamour portrait quality, and 1940s cinema star aesthetic.' }
            ]
        };

        const intensityDescriptions = {
            'ringan': 'Apply this effect very subtly at approximately 30% strength. Barely noticeable — a light touch enhancing the photo without being obvious. Keep it natural and realistic.',
            'sedang': 'Apply this effect at medium strength, approximately 60%. Clearly visible but not overwhelming. Balance between original photo and the styled effect.',
            'kuat': 'Apply this effect at full dramatic strength, 100%. Make the effect very pronounced and impactful. Push the style to maximum while keeping the subject recognizable.'
        };

        const variationNotes = {
            1: 'Standard variation with primary color emphasis of this effect.',
            2: 'Slightly different tone curve, emphasize shadows more deeply.',
            3: 'Brighter exposure variant, lift highlights slightly.',
            4: 'More contrast variant, deeper blacks.',
            5: 'Warmer white balance shift than standard.',
            6: 'Cooler color temperature variant of this effect.'
        };

        // State
        let ftgImageData = null;
        let ftgCategory = 'Film & Vintage';
        let ftgEffectName = '';
        let ftgEffectPrompt = '';
        let ftgIntensity = 'sedang';
        let ftgRatio = '3:4';
        let ftgCount = 4;
        let ftgImages = [];
        let ftgGenerating = false;

        // DOM refs
        const ftgUploadInput = document.getElementById('fotogenic-upload-input');
        const ftgUploadBox = document.getElementById('fotogenic-upload-box');
        const ftgPreviewWrapper = document.getElementById('fotogenic-preview-wrapper');
        const ftgPreviewImage = document.getElementById('fotogenic-preview-image');
        const ftgRemoveWrap = document.getElementById('fotogenic-remove-wrap');
        const ftgCatOptions = document.getElementById('fotogenic-cat-options');
        const ftgEffectGrid = document.getElementById('fotogenic-effect-grid');
        const ftgCustomContainer = document.getElementById('fotogenic-custom-container');
        const ftgCustomInput = document.getElementById('fotogenic-custom-input');
        const ftgIntensityOptions = document.getElementById('fotogenic-intensity-options');
        const ftgRatioOptions = document.getElementById('fotogenic-ratio-options');
        const ftgCountOptions = document.getElementById('fotogenic-count-options');
        const ftgGenerateBtn = document.getElementById('fotogenic-generate-btn');
        const ftgGenerateLabel = document.getElementById('fotogenic-generate-label');
        const ftgResultsPlaceholder = document.getElementById('fotogenic-placeholder');
        const ftgResultsContainer = document.getElementById('fotogenic-results-container');
        const ftgResultsGrid = document.getElementById('fotogenic-results-grid');
        const ftgDownloadAllBtn = document.getElementById('fotogenic-download-all-btn');

        if (!ftgUploadInput) return;

        // ---- RENDER EFFECTS ----
        function ftgRenderEffects(category) {
            if (!ftgEffectGrid) return;
            ftgEffectGrid.innerHTML = '';
            if (category === 'Custom') {
                ftgEffectName = 'Custom';
                ftgEffectPrompt = '';
                if (ftgCustomContainer) ftgCustomContainer.classList.remove('hidden');
                ftgUpdateGenerateBtn();
                return;
            }
            if (ftgCustomContainer) ftgCustomContainer.classList.add('hidden');
            const effects = effectsLibrary[category] || [];
            effects.forEach(effect => {
                const btn = document.createElement('button');
                btn.type = 'button';
                btn.className = 'fotogenic-effect-btn' + (effect.name === ftgEffectName ? ' selected' : '');
                btn.dataset.effectName = effect.name;
                btn.dataset.effectPrompt = effect.prompt;
                btn.innerHTML = `<i class="fas ${effect.icon} pointer-events-none mb-1 text-sm"></i><span class="pointer-events-none text-xs leading-tight">${effect.name}</span>`;
                ftgEffectGrid.appendChild(btn);
            });
        }

        ftgRenderEffects(ftgCategory);

        // ---- CATEGORY FILTER ----
        if (ftgCatOptions) {
            ftgCatOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('.fotogenic-cat-btn');
                if (!btn) return;
                ftgCatOptions.querySelectorAll('.fotogenic-cat-btn').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                const newCat = btn.dataset.category;
                if (newCat !== ftgCategory) {
                    ftgCategory = newCat;
                    if (ftgCategory !== 'Custom') {
                        const catEffects = effectsLibrary[ftgCategory] || [];
                        if (!catEffects.some(ef => ef.name === ftgEffectName)) {
                            ftgEffectName = '';
                            ftgEffectPrompt = '';
                        }
                    }
                    ftgRenderEffects(ftgCategory);
                    ftgUpdateGenerateBtn();
                }
            });
        }

        // ---- EFFECT SELECTION ----
        if (ftgEffectGrid) {
            ftgEffectGrid.addEventListener('click', (e) => {
                const btn = e.target.closest('.fotogenic-effect-btn');
                if (!btn) return;
                ftgEffectGrid.querySelectorAll('.fotogenic-effect-btn').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                ftgEffectName = btn.dataset.effectName;
                ftgEffectPrompt = btn.dataset.effectPrompt;
                if (ftgCustomContainer) ftgCustomContainer.classList.add('hidden');
                ftgUpdateGenerateBtn();
            });
        }

        if (ftgCustomInput) {
            ftgCustomInput.addEventListener('input', ftgUpdateGenerateBtn);
        }

        // ---- UPLOAD ----
        if (ftgUploadInput) {
            ftgUploadInput.addEventListener('change', ftgHandleUpload);
        }

        if (ftgUploadBox) {
            ftgUploadBox.addEventListener('click', () => {
                if (ftgUploadInput) ftgUploadInput.click();
            });
            ftgUploadBox.addEventListener('dragover', (e) => {
                e.preventDefault();
                ftgUploadBox.style.borderColor = '#06b6d4';
                ftgUploadBox.style.background = 'rgba(6,182,212,0.06)';
            });
            ftgUploadBox.addEventListener('dragleave', () => {
                ftgUploadBox.style.borderColor = '';
                ftgUploadBox.style.background = '';
            });
            ftgUploadBox.addEventListener('drop', (e) => {
                e.preventDefault();
                ftgUploadBox.style.borderColor = '';
                ftgUploadBox.style.background = '';
                if (e.dataTransfer.files.length > 0) {
                    ftgUploadInput.files = e.dataTransfer.files;
                    ftgHandleUpload({ target: ftgUploadInput });
                }
            });
        }

        async function ftgHandleUpload(e) {
            const file = e.target.files[0];
            if (!file) return;
            let processedFile = file;
            if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
                try {
                    const convertedBlob = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.9 });
                    processedFile = new File([convertedBlob], file.name.replace(/\.heic$/i, '.jpg'), { type: 'image/jpeg' });
                } catch (err) {
                    console.error('HEIC conversion failed:', err);
                    return;
                }
            }
            const reader = new FileReader();
            reader.onloadend = () => {
                ftgImageData = reader.result;
                if (ftgPreviewImage) ftgPreviewImage.src = reader.result;
                const ftgPlaceholderEl = document.getElementById('fotogenic-upload-placeholder');
                if (ftgPlaceholderEl) ftgPlaceholderEl.classList.add('hidden');
                if (ftgPreviewWrapper) ftgPreviewWrapper.classList.remove('hidden');
                if (ftgRemoveWrap) ftgRemoveWrap.classList.remove('hidden');
                ftgUpdateGenerateBtn();
            };
            reader.readAsDataURL(processedFile);
        }

        if (ftgRemoveWrap) {
            ftgRemoveWrap.addEventListener('click', () => {
                ftgImageData = null;
                if (ftgUploadInput) ftgUploadInput.value = '';
                if (ftgPreviewImage) ftgPreviewImage.src = '';
                if (ftgPreviewWrapper) ftgPreviewWrapper.classList.add('hidden');
                if (ftgRemoveWrap) ftgRemoveWrap.classList.add('hidden');
                const ftgPlaceholderEl = document.getElementById('fotogenic-upload-placeholder');
                if (ftgPlaceholderEl) ftgPlaceholderEl.classList.remove('hidden');
                ftgUpdateGenerateBtn();
            });
        }

        // ---- INTENSITY / RATIO / COUNT ----
        if (ftgIntensityOptions) {
            ftgIntensityOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn || !ftgIntensityOptions.contains(btn)) return;
                ftgIntensityOptions.querySelectorAll('[data-value]').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                ftgIntensity = btn.dataset.value;
            });
        }

        if (ftgRatioOptions) {
            ftgRatioOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn || !ftgRatioOptions.contains(btn)) return;
                ftgRatioOptions.querySelectorAll('[data-value]').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                ftgRatio = btn.dataset.value;
            });
        }

        if (ftgCountOptions) {
            ftgCountOptions.addEventListener('click', (e) => {
                const btn = e.target.closest('[data-value]');
                if (!btn || !ftgCountOptions.contains(btn)) return;
                ftgCountOptions.querySelectorAll('[data-value]').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
                ftgCount = parseInt(btn.dataset.value, 10);
                ftgUpdateGenerateBtn();
            });
        }

        // ---- HELPERS ----
        function ftgUpdateGenerateBtn() {
            if (!ftgGenerateBtn) return;
            const hasPhoto = !!ftgImageData;
            const hasEffect = ftgCategory === 'Custom'
                ? (ftgCustomInput && ftgCustomInput.value.trim().length > 0)
                : !!ftgEffectName;
            ftgGenerateBtn.disabled = !(hasPhoto && hasEffect) || ftgGenerating;
            if (ftgGenerateLabel) ftgGenerateLabel.textContent = `Generate ${ftgCount} Variasi`;
        }

        function ftgGetAspectClass(ratio) {
            const map = { '1:1': 'aspect-square', '3:4': 'aspect-[3/4]', '4:5': 'aspect-[4/5]', '9:16': 'aspect-[9/16]', '16:9': 'aspect-[16/9]' };
            return map[ratio] || 'aspect-[3/4]';
        }

        function ftgBuildCard(imageUrl, idx) {
            return `
                <img src="${imageUrl}" class="w-full h-full object-cover" alt="Fotogenic ${idx + 1}">
                <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/30 to-transparent opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-center pb-4 gap-2">
                    <button data-action="preview" data-index="${idx}" class="action-btn bg-white/90 hover:bg-white text-gray-800 px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-1 text-xs">
                        <i class="fas fa-eye pointer-events-none"></i>
                        <span class="hidden sm:inline pointer-events-none">Preview</span>
                    </button>
                    <button data-action="download" data-index="${idx}" class="action-btn bg-gradient-to-r from-cyan-500 to-indigo-600 hover:from-cyan-600 hover:to-indigo-700 text-white px-3 py-2 rounded-full font-semibold shadow-lg transition-all duration-200 hover:scale-105 flex items-center gap-1 text-xs">
                        <i class="fas fa-download pointer-events-none"></i>
                        <span class="hidden sm:inline pointer-events-none">Download</span>
                    </button>
                </div>
            `;
        }

        // ---- GENERATE ----
        if (ftgGenerateBtn) {
            ftgGenerateBtn.addEventListener('click', async () => {
                if (!ftgImageData || ftgGenerating) return;
                const hasEffect = ftgCategory === 'Custom'
                    ? (ftgCustomInput && ftgCustomInput.value.trim().length > 0)
                    : !!ftgEffectName;
                if (!hasEffect) return;

                ftgGenerating = true;
                ftgImages = [];
                ftgGenerateBtn.disabled = true;
                ftgGenerateBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2 pointer-events-none"></i><span class="pointer-events-none">Generating...</span>';

                if (ftgResultsPlaceholder) ftgResultsPlaceholder.classList.add('hidden');
                if (ftgResultsContainer) ftgResultsContainer.classList.remove('hidden');
                if (ftgResultsGrid) ftgResultsGrid.innerHTML = '';
                if (ftgDownloadAllBtn) { ftgDownloadAllBtn.disabled = true; ftgDownloadAllBtn.classList.add('hidden'); }

                const aspectClass = ftgGetAspectClass(ftgRatio);
                for (let i = 1; i <= ftgCount; i++) {
                    const card = document.createElement('div');
                    card.id = `fotogenic-card-${i}`;
                    card.className = `relative group rounded-xl overflow-hidden bg-gray-100 flex items-center justify-center ${aspectClass}`;
                    card.innerHTML = `<div class="text-center p-4"><div class="animate-spin rounded-full h-10 w-10 border-b-2 border-cyan-500 mx-auto mb-2"></div><p class="text-xs text-gray-500">Variasi ${i}...</p></div>`;
                    if (ftgResultsGrid) ftgResultsGrid.appendChild(card);
                }

                if (ftgResultsContainer) ftgResultsContainer.scrollIntoView({ behavior: 'smooth', block: 'start' });

                let attempts = 0;
                const MAX_ATTEMPTS = 3;

                while (attempts < MAX_ATTEMPTS) {
                    attempts++;
                    ftgImages = [];
                    await Promise.allSettled(
                        Array.from({ length: ftgCount }, (_, i) => ftgGenerateSingle(i + 1))
                    );
                    const successCount = ftgImages.filter(Boolean).length;
                    if (successCount > 0) break;
                }

                // Remove empty spinner cards
                document.querySelectorAll('[id^="fotogenic-card-"]').forEach(card => {
                    if (!card.querySelector('img')) card.remove();
                });

                ftgGenerating = false;
                ftgUpdateGenerateBtn();

                const finalSuccessFtg = ftgImages.filter(Boolean).length;
                if (finalSuccessFtg === 0) {
                    alert('Akun Google ini sudah mencapai batas, silahkan gunakan akun Google lain');
                    if (ftgResultsPlaceholder) ftgResultsPlaceholder.classList.remove('hidden');
                    if (ftgResultsContainer) ftgResultsContainer.classList.add('hidden');
                } else if (ftgDownloadAllBtn) {
                    ftgDownloadAllBtn.classList.remove('hidden');
                    ftgDownloadAllBtn.disabled = false;
                }
            });
        }

        async function ftgGenerateSingle(index) {
            const card = document.getElementById(`fotogenic-card-${index}`);
            try {
                const effectName = ftgCategory === 'Custom' ? 'Custom Effect' : ftgEffectName;
                const effectPrompt = ftgCategory === 'Custom'
                    ? (ftgCustomInput ? ftgCustomInput.value.trim() : '')
                    : ftgEffectPrompt;

                const prompt = `Terapkan efek foto "${effectName}" pada foto ini.

DESKRIPSI EFEK: ${effectPrompt}

INTENSITAS: ${intensityDescriptions[ftgIntensity]}

ATURAN PENTING:
- Pertahankan subjek utama (orang/produk) tetap jelas terlihat
- Jaga komposisi dan framing asli foto
- Terapkan sebagai style editing foto profesional
- Hasil harus tampak seperti foto yang diedit secara profesional
- JANGAN tambahkan teks, watermark, atau logo apapun
- Pertahankan identitas wajah jika ada orang di foto

VARIASI ${index}: ${variationNotes[index] || 'Variasi standar dengan slight different color emphasis'}`;

                const base64 = ftgImageData.split(',')[1];
                const mimeType = (ftgImageData.match(/^data:([^;]+);/) || [])[1] || 'image/jpeg';

                const payload = {
                    contents: [{ parts: [{ text: prompt }, { inlineData: { mimeType, data: base64 } }] }],
                    generationConfig: { responseModalities: ['TEXT', 'IMAGE'], imageConfig: { aspectRatio: ftgRatio } }
                };

                const models = ['gemini-2.5-flash-image-preview', 'gemini-2.0-flash-preview-image-generation'];
                let imageData = null;

                for (let m = 0; m < models.length; m++) {
                    try {
                        const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/${models[m]}:generateContent?key=${apiKey}`;
                        const resp = await fetch(apiUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
                        const result = await resp.json();
                        imageData = result?.candidates?.[0]?.content?.parts?.find(p => p.inlineData)?.inlineData?.data;
                        if (imageData) break;
                    } catch (err) {
                        if (m >= models.length - 1) throw err;
                        await new Promise(r => setTimeout(r, 800));
                    }
                }

                if (!imageData) throw new Error('No image data returned');

                const imageUrl = `data:image/png;base64,${imageData}`;
                const filename = `fotogenic_${effectName.replace(/\s+/g, '_').toLowerCase()}_v${index}.png`;
                ftgImages[index - 1] = { url: imageUrl, filename };

                if (card) card.innerHTML = ftgBuildCard(imageUrl, index - 1);

            } catch (error) {
                console.error(`Fotogenic error (${index}):`, error);
                if (card) card.innerHTML = '';
            }
        }

        // ---- RESULTS DELEGATION ----
        if (ftgResultsGrid) {
            ftgResultsGrid.addEventListener('click', async (e) => {
                e.stopPropagation();
                const btn = e.target.closest('[data-action]');
                if (!btn) return;
                const action = btn.dataset.action;
                const idx = parseInt(btn.dataset.index, 10);
                const img = ftgImages[idx];
                if (!img) return;
                if (action === 'preview' && window.showPreviewModal) {
                    window.showPreviewModal(img.url);
                } else if (action === 'download') {
                    if (window.downloadDataURINew) await window.downloadDataURINew(img.url, img.filename);
                    else if (window.downloadImage) await window.downloadImage(img.url, img.filename);
                }
            });
        }

        // ---- DOWNLOAD ALL ----
        if (ftgDownloadAllBtn) {
            ftgDownloadAllBtn.addEventListener('click', async (e) => {
                e.stopPropagation();
                if (ftgDownloadAllBtn.disabled) return;
                const validImages = ftgImages.filter(img => img);
                if (validImages.length === 0) return;
                const originalHTML = ftgDownloadAllBtn.innerHTML;
                ftgDownloadAllBtn.disabled = true;
                try {
                    for (let i = 0; i < validImages.length; i++) {
                        ftgDownloadAllBtn.innerHTML = `<i class="fas fa-spinner fa-spin mr-2"></i><span>${i + 1}/${validImages.length}...</span>`;
                        const img = validImages[i];
                        try {
                            if (window.downloadDataURINew) await window.downloadDataURINew(img.url, img.filename);
                            else if (window.downloadImage) await window.downloadImage(img.url, img.filename);
                            await new Promise(r => setTimeout(r, 500));
                        } catch (err) { console.error(`Download err ${i + 1}:`, err); }
                    }
                    ftgDownloadAllBtn.innerHTML = '<i class="fas fa-check mr-2"></i><span>Selesai!</span>';
                    setTimeout(() => { ftgDownloadAllBtn.disabled = false; ftgDownloadAllBtn.innerHTML = originalHTML; }, 2000);
                } catch (error) {
                    console.error('Download all error:', error);
                    ftgDownloadAllBtn.disabled = false;
                    ftgDownloadAllBtn.innerHTML = originalHTML;
                }
            });
        }

        // ---- TAB ACTIVATE ----
        const ftgTabDesktop = document.querySelector('.main-tab-btn[data-tab="fotogenic"]');
        const ftgTabMobile = document.querySelector('.mobile-tab-btn[data-tab="fotogenic"]');
        function ftgOnActivate() {
            if (ftgEffectGrid && ftgEffectGrid.children.length === 0) ftgRenderEffects(ftgCategory);
        }
        if (ftgTabDesktop) ftgTabDesktop.addEventListener('click', ftgOnActivate);
        if (ftgTabMobile) ftgTabMobile.addEventListener('click', ftgOnActivate);

    })();

});
